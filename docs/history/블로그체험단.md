# 블로그체험단 — 구현 기록 (M1~M6, 2026-08)

> CLAUDE.md 에서 분리한 **완료된 구현의 단계별 기록**입니다(M6 + 프로덕션 E2E 완주 2026-08-19).
> 지금 지켜야 하는 규율은 CLAUDE.md 의 「블로그체험단」 절에 요약돼 있습니다.
> ⚠ 원문 제목이 "구현 착수 전"이었으나 실제로는 M6 까지 완료된 상태였습니다(제목만 낡아 있었음).

### 블로그체험단 지원 계획 (PRD 확정 · 구현 착수 전)
- **PRD = `frontend/docs/prd-blog-campaign.html`**(v1.0, 2026-08-10 사용자 확정 반영). 발단 = (주)바를참스킨 「0804)비타민,글루타치온_블로그 50건」이 무시트 이관에서 **구매일자 빈 행 전부 무음 누락**(`readPreparedRows`가 날짜 파싱 성공만 준비 행으로 인정 — 빈 칸은 `datelessFilled`에도 안 잡힘, 점검표 ①도 같은 판정이라 초록인 채 이관).
- **사용자 확정**: ① 분류 = **별도 축 `work_kind`**(review 기본 | blog — 리뷰타입은 리뷰체험단 하위 값, 블로그엔 리뷰타입 없음) ② 블로거도 구매양식 제출(간소화 — 1차는 동일 칸) ③ 모집공고로도 모집(이번 범위) ④ 결과물 = **포스팅URL만 제출 = 리뷰제출 완료** ⑤ 준비 행 기준: 바를참스킨 = 번호 1~50, **앞으로의 블로그 작업 = 리뷰오더 총원(recruit_count)** ⑥ 바를참스킨 1회 동기화 = **전용기능 없이 그 작업만**(app_settings 가드 1회성 부팅 잡, migration 057 패턴).
- **단계 M1~M5**(PRD §09): M1 work_kind 축 / M2 blog 준비 행 = 총원 분기(`readPreparedRows` 한 곳) + **`columnResolver` urlKeywords의 '블로그'·'포스팅' 헤더 제외**(블로그URL이 product_url 하이재킹하는 즉시 위험) / M3 바를참스킨 1회 동기화 / M4 포스팅URL 제출(blog 탭 = `/포스팅/` 우선 기입·URL-only 완료·**큐 `review_submit` 핸들러 memo 유실 기존 버그 수리**·검수 격리) / M5 blog 공고(apply 시 `campaign_applications.blog_url` 필수).
- **M1 완료 (migration 099)**: `work_orders`·`tab_configs`·`recruit_campaigns` 에 `work_kind TEXT DEFAULT ''` — **컬럼 추가만·백필 0·DB CHECK 없음**(087 리뷰타입 규율, 082 의 42804 FK 사고 회피). **빈 값 = review = 오늘 동작 100% 불변**(opt-in).
  - ★★ **판정 단일 출처 = `utils/workKind.js`**(`normalizeWorkKind`·`isBlogKind`·`resolveWorkKind`(공고 > 탭)·`workKindForStore`). 라우트·화면에 규칙 사본 0, 프론트는 라벨 최소 사본(`WO_KIND_LABELS`)만 두고 **회귀가드가 서버 `WORK_KINDS` 와의 일치를 고정**(workManager 사본 규율).
  - ★★ **`workKindForStore` 는 `''` 를 `''` 로 남긴다** — `normalizeWorkKind` 처럼 `review` 로 접으면 **"안 보냈다"와 "리뷰로 정했다"가 같아져** 접수 업서트의 blank-only(`COALESCE(NULLIF(tab_configs.work_kind,''), EXCLUDED.work_kind)`)가 무력화되고 관리자가 탭에서 고친 값을 2차 접수가 덮는다.
  - **전파**: 인트라넷 `REVIEW_ORDER_FIELDS` → `INTAKE_EDITABLE_FIELDS`·`_insertWorkOrder`·`/intake/list` SELECT → **접수 업서트가 `tab_configs` 로 blank-only 전파** → 공고 create INSERT·update **CASE 센티널**(null=유지 / `''`=해제). ★ `_scopedCampaignEdit`·`ADMIN_EDIT_FIELDS` **미포함**(유형 변경 = 준비 행 기준이 바뀌는 구조 변경이라 스코프 토큰·수동 수정 화이트리스트에 열지 않는다).
  - **인트라넷 폼**: 계약건 카드 **위**에 「체험단 종류」 선택(리뷰/블로그) — 블로그를 고르면 **리뷰타입 카드를 숨기고 `review_type` 을 미전송**(별도 축 = 블로그엔 리뷰타입이 없다). `REVIEW_ORDER_FIELDS` 에 `work_kind` 를 **배열 끝**에 넣었다(순서는 런타임 의미 없음 — 머리에 넣으면 기존 가드의 200자 창에서 `work_sheet_url` 이 밀려 나간다, 실측).
  - ★ **표시**: 작업오더 상세 `체험단 종류` 줄은 값이 있을 때만 렌더 → **과거 오더 화면 불변**. 발행 프리필도 블로그면 `review_type: ""`.
  - ★ `REQUIRED_SCHEMA` 3종 등록(컬럼 없으면 접수·발행이 전면 42703 이 되는 것을 부팅에서 막는다).
  - ★ **양방향 가산적 · 배포 순서 무관**(088 계약건과 같은 성질) — 리뷰웹만 먼저면 값이 안 실려 오고, 인트라넷만 먼저면 리뷰웹이 필드를 버린다.
  - 회귀가드 `tests/blogWorkKind.test.js`(48케이스 — 순수함수 실행 · 마이그레이션 모양(TEXT·CHECK 0·백필 0) · **INSERT 컬럼 수 ≡ 자리표시자 연속성 ≡ 파라미터 수 계수** · blank-only · CASE 센티널 · 권한 경계 · 프론트 라벨 사본 일치 · NUL 위생).
- **M2 완료 — 준비 행 기준 분기 + URL 열 하이재킹 차단**:
  - ★★ **블로그 준비 행 = 값이 하나라도 있는 행**(구매일자 무관). 날짜 기준으로 세면 준비 행이 **통째로 0**이 되어 무음 누락된다(바를참스킨 0804 건이 정확히 이것 — 빈 칸은 `datelessFilled` 에도 안 잡히고 점검표 ①도 같은 판정이라 초록인 채 이관됐다). **총원**(연결 작업오더 `recruit_count`)이 있으면 **앞에서부터 그 수까지만**.
  - ★★ **총원이 시트 실체 행보다 많아도 자리를 지어내지 않는다** — `seq` 는 시트 실제 행 번호여야 하고(어긋나면 주문 유입 시 `ON CONFLICT (sheet,tab,seq)` 가 제자리 갱신을 못 해 표가 두 겹) 없는 줄의 번호는 알 수 없다. 대신 `shortOfTotal`·`beyondTotal`·`dateEmpty` 로 **고지**한다.
  - ★★ **분기는 `readPreparedRows` 한 곳** — 전수 점검·백필·점검표 ①·일괄 점검이 전부 여기를 지나므로 기준이 갈릴 수 없다. **호출부 4곳이 전부 `tabName` 을 넘긴다**(안 넘기면 종류를 판정할 수 없어 그 경로에서만 블로그 탭이 조용히 날짜 기준으로 떨어진다 — 회귀가드가 고정).
  - ★ **판정 불가(미등록 탭·조회 실패·tabName 미전달) = 종전 동작(날짜 기준)** — 모른다고 블로그로 단정하면 **리뷰 작업 전체의 준비 행 기준이 바뀐다**(오탐이 훨씬 비싸다). ★ 블로그 탭은 **날짜 칸이 아예 없어도** `no_date_column` 으로 막히지 않는다.
  - ★★ **블로그URL·포스팅URL 이 상품URL 을 하이재킹하지 않는다**(`columnResolver` `URL_EXCLUDE_PATTERNS`): `urlKeywords` 는 `'url'`·`'링크'` **부분일치 + `findIndex`(첫 매칭 승)** 라, 그 열이 상품URL보다 왼쪽이면 `review_index.product_url` 이 통째로 블로그 주소가 된다(리뷰어 화면 '상품 페이지'가 남의 블로그로 나가고 검수 대조도 어긋난다). ★ **확실한 두 낱말만** 막는다 — 넓히면 멀쩡한 상품열이 빈 값이 된다. 전부 걸리면 `-1` → 빈 값(포스팅 주소를 상품 주소로 내보내느니 빈 값).
  - ★★ **사본 방지로 공유 조각 둘을 뽑았다**: ① `utils/campaignTabLateral`(공고 LATERAL — **gid 폴백** + **값이 있는 최신 공고**, 칸 이름은 허용목록) = `reviewTypeContext`·`workKindContext` 공용 ② `utils/workOrderLink`(작업오더 링크 **ORDER BY link_rank** — OR 로만 묶으면 최신 폴백 오더가 이긴다) = `reviewInspect`·`sheetSlotSync` 공용. 탭 종류 조회는 `workKindContext.service`(60초 캐시·fail-soft·실패는 미캐시)로 모아 `utils/workKind`(공고 > 탭)를 단일 출처로 쓴다.
  - ⚠ **리터럴 NUL 실측(또 밟음)**: `reviewTypeContext` 의 캐시 키 구분자(의도된 NUL)를 복사해 오면서 새 파일에 **NUL 2개**가 딸려왔다 — 회귀가드가 잡았다. 새 코드에는 **`\u0000` 이스케이프 표기**를 쓴다(이 CLAUDE.md 를 쓰다가도 한 번 더 밟았다 — 설명문에도 글자 그대로 넣지 말 것).
  - 회귀가드 `tests/blogPreparedRows.test.js`(24케이스 + `PGTEST_URL` 시 25 — 스텁 pool 로 **서비스 실제 실행**(총원 상한·부족분 고지·fail-safe 3갈래) + `parseTabRows` 실행으로 URL 하이재킹 대조 + 공유 조각 규칙 + **진짜 PG16 으로 `filled` 판정**(스텁은 SQL 을 해석하지 않아 `btrim` 누락을 못 잡는다), **변이시험 9종 검출 확인**). ⚠ 리팩터로 드리프트한 `inspectReviewTypeHint` 의 LATERAL 검사를 **실행 SQL 대조**로 갱신했다(검사 의미 동일·더 강함). ⚠ 이 변경으로 컬럼 목록 꼬리가 밀려 기존 가드 4종의 패턴을 함께 갱신했다(검사 의미 불변) — `workOrderAutoFill`·`sheetlessOrderAccept`·`reviewType`·`campaignCarryHold`. ★★ **INSERT 컬럼 검사에 닫는 괄호 앵커(`…, x\)`)를 쓰지 말 것** — 컬럼이 꼬리에 붙을 때마다 조용히 빨개진다(`reviewType` 은 098 부터, 이번에 `[,)]` 로 교정). ⚠ 착수 시점에 이미 빨간 가드 3종(`woCampaignMapDoc`·`paymentBatch`·`worktableTemplate`)은 이 변경과 무관(전자는 2026-08-07 정원 기준 전환 여파).
- **M3 완료 — (주)바를참스킨 0804 1회 동기화**(`blog0804Backfill.service.js`, 시트 `1kLjLp…` gid `545717806`):
  - ★★ **왜 M2 만으로는 안 되나** — 그 탭은 **이미 무시트 이관**돼 시스템이 시트를 더 안 읽고, 내부 시트 사본(RAW 미러)도 **작업표 기준으로 재생성**돼 빈 행 기록 자체가 없다. M2 가 판정을 고쳐도 **읽을 원본이 없다** → **시트를 한 번은 다시 읽어야** 1~50 자리를 표에 만들 수 있다.
  - **방식 = 부팅 1회성 잡 + `app_settings` 가드**(`blog_0804_slot_backfill_done` — migration 057·제출열 핫픽스와 같은 선례). ★ **전용 화면·엔드포인트 0**(사용자 확정 "별도 전용기능 구현 필요없이 그 작업만") — 회귀가드가 라우트 노출 0을 고정. 킬스위치 `BLOG_0804_BACKFILL=0`.
  - ★★ **판정 사본 0 — M2 와 같은 함수를 쓴다**: 준비 행 고르기를 `sheetSlotSync.pickBlogPrepared`(값 있는 행만·총원까지만·부족/초과는 숫자로 고지) · `rowHasValue` · `rowJsonFromHeaders` 로 **추출**해 `readPreparedRows`(미러 경로)와 이 잡(라이브 시트 경로)이 같은 규칙을 탄다. 사본을 두면 **"점검이 세는 행 ≠ 만들어지는 자리"** 로 갈린다. 헤더 탐지(`utils/sheetHeader`)·슬롯 INSERT(`participants.createSlotsFromSheetRows`)·장부(`rebuildLedgers`)·작업오더 지목(`workOrderForTabSql`)도 전부 기존 단일 출처.
  - ★★ **추가만 · 시트 쓰기 0**: `ON CONFLICT DO NOTHING`(기존 행·제출·입금 표시 무접촉) · `campaign_participants` 에 UPDATE/DELETE 0 · 시트는 **읽기 1회뿐**(저우선 lane = 주문 쓰기 슬롯 무접촉, gid 우선, `FORMATTED_VALUE` = 미러와 같은 표기). 무시트 크론 게이트(`sheetlessScope`)는 **주기 작업용**이라 이 명시적 1회 읽기와 무관하다.
  - ★★ **seq = 시트 실제 행 번호**(배열 인덱스 + 1) — 어긋나면 주문 유입 시 `ON CONFLICT (sheet_id, tab_name, seq)` 가 제자리 갱신을 못 해 **표가 두 겹**이 된다. 총원은 **하드 상한 50**(「…블로그 50건」) — 그 아래 값 있는 줄이 있어도 자리를 만들지 않고 `beyondTotal` 로 고지.
  - ★ **분류는 blank-only** — `tab_configs`·`work_orders` 의 `work_kind` 는 `COALESCE(work_kind,'')=''` 일 때만 `'blog'` 로 채운다(관리자가 정해 둔 값을 1회성 잡이 덮지 않는다).
  - ★ **fail-soft 3단**: 실패하면 **가드를 안 남겨** 다음 부팅에 재시도(자리도 안 만든다) / 장부 재생성 실패·분류 실패는 **자리 생성을 되돌리지 않고** 사유만 보고 / 부팅은 어떤 경우에도 계속. `already_done` 은 조용히(평상시 로그 소음 0).
  - **결과**: 작업보드 표에 1~50번 전부 표시(모집·배정 전 = 빈 슬롯) → 이후 모집공고 참여(M5) 또는 관리자 수기 입력(그리드 편집)으로 채워진다.
  - 회귀가드 `tests/blog0804Backfill.test.js`(37케이스 — 스텁 pool + 주입 deps 로 **잡 전체를 실제 실행**(seq 계산·상한·기존 자리 스킵·가드 4갈래·dryRun 쓰기 0·blank-only·lane) + 순수함수 실행 + 배선, **변이시험 14종 검출 확인**).
- **M4-1 완료 — memo(비고 / 포스팅URL) 열 단일 출처 + 큐 유실 수리**(기록 경로만. 제출 게이트·리뷰어 화면은 M4-2):
  - ★★ **열 고르기 단일 출처 = `utils/memoColumn.js`**(`pickMemoColumnIndex`/`pickMemoColumnName`): 소비처 셋이 같은 규칙을 써야 한다 — ① `POST /api/submit/review` 배경 시트 쓰기 ② 그 실패분을 다시 쓰는 큐(`review_submit`) ③ 무시트 탭의 작업표 칸 기록. 갈라지면 **처음 제출은 포스팅 칸에, 재시도는 비고 칸에** 들어가 값이 흩어진다.
  - ★★ **블로그는 우선순위가 뒤집힌다**(`포스팅` → `비고`) — 결과물이 포스팅URL 이므로. ★ **폴백(`비고`)은 유지**: 바를참스킨 기존 시트는 「비고(닉네임)」 열을 결과물 칸으로 대용해 왔으므로 없애면 그 탭의 기존 기록 위치가 바뀐다. ★ **리뷰체험단(기본·판정 불가 포함)은 종전 순서 그대로**(`비고` → `포스팅`) = 무회귀.
  - ★★ **판정 실패도 리뷰 경로**(`isBlogKind(null) === false`) — 모른다고 블로그로 단정하면 리뷰 작업의 기록 칸이 통째로 바뀐다.
  - ★★ **큐 `review_submit` 의 memo 유실은 기존 버그였다**(블로그와 무관): 배경 시트 쓰기가 실패해 큐로 넘어가면 핸들러가 `payload.memo` 를 **읽지 않아** 제출 칸만 찍히고 **비고/포스팅 값이 영영 사라졌다**. 페이로드에 `memo`·`blog` 를 실어 큐도 같은 함수로 같은 칸에 쓴다. 열이 없으면 `logger.warn`(조용한 소실 금지).
  - **무시트 기록** `sheetlessStatus.markSheetlessMemo`(`markStatusCell` 과 실행부 공유 `_writeCellAndRebuild`): `COALESCE(detected_headers, headers)` 로 열 이름을 읽어 작업표 칸에 쓰고 장부를 재생성한다. ★ **시트 기반 탭·판정 실패는 `{handled:false}`** → 호출부가 자기 시트 쓰기 경로를 그대로 탄다(`handled:true` 로 접으면 **시트 탭의 비고가 조용히 사라진다**). ★ 열이 없으면 `no_memo_column` 으로 **정직하게 거부**(성공으로 꾸미지 않는다).
  - ★ **무시트 판정과 memo 기록에 같은 `blog` 값을 넘긴다** — 하나만 빠지면 시트 경로는 포스팅, 무시트 경로는 비고로 갈린다(변이시험이 잡은 가드 구멍 2종 중 하나).
  - **`/api/search` 응답에 `workKind` 동봉**(`workKindsForTabs` 배치 — 탭 중복 제거 + 60초 캐시, 실패는 빈 맵 = 필드 없음): M4-2 리뷰어 화면이 쓸 재료를 미리 싣는다(**신규 엔드포인트 0**).
  - 회귀가드 `tests/blogMemoColumn.test.js`(35케이스 — 열 고르기 순수함수 실행(블로그 역전·폴백·리뷰 무회귀) + 소비처 3곳 배선 + 큐 페이로드 왕복 + `handled:false` 계약, **변이시험 13종 검출 확인**). ⚠ `sheetlessStatusPersist` 가드가 입금 기록 경로를 `payment.routes.js` 로 보고 있었으나 그 코드는 입금 M2(migration 100)에서 `paymentApply.service.js` 로 옮겨져 **착수 전부터 빨간 상태**였다 — 대상 파일만 갱신(검사 의미 불변).
- **M4-2 완료 — 포스팅URL 제출(파일 0건 허용) + 검수 격리**:
  - ★★ **판정 단일 출처 = `utils/blogPostUrl.js`**(`isPostUrl`·`POST_URL_HINT`). ★ **도메인 화이트리스트 금지** — 리뷰어가 어느 플랫폼에 쓸지는 우리가 정하지 않는다(네이버·티스토리·브런치·자체 도메인…). 목록을 두는 순간 **정상 제출이 막히는 쪽**이 되고 그건 1차 필터의 실패 선호(오탐 금지)와 정반대다. 통과 기준 = `http(s)` 절대 URL + 호스트에 점 1개 이상, **공백·따옴표·꺾쇠는 거부**(시트 칸·화면에 그대로 나가는 값이라 속성 breakout 여지를 없앤다). 프론트는 최소 사본(`_isPostUrl`)을 두고 **회귀가드가 14케이스로 서버 규칙과의 일치를 고정**(workManager 사본 규율).
  - ★★ **서버가 최종 방어**(`POST /api/submit/review`): blog 탭이면 `memo` 필수 + URL 검증(`post_url_required`) — 화면만 막으면 우회된다. **슬롯 대조(`DISTINCT slot_key`)는 blog 에서 건너뛴다** — 결과물이 URL 이라 현영 겸 blog 탭이면 리뷰 캡처 슬롯을 영영 못 채워 **완료 판정이 구조적으로 불가능**해진다(사용자 확정 ④ "포스팅URL만 제출 = 리뷰제출 완료").
  - ★★ **검수 격리는 087 구매확정 안전핀과 같은 규율**: 1차 필터 `precheckPolicy` 가 `workKind==='blog'` 이면 `skip('blog_tab')` — AI 는 블로그 화면을 아는 종류가 없어 `other` 로 떨어뜨리고 확신 0.9 이상이면 **첨부를 잠근다**(= 정상 제출 전면 차단 = 참여 소각). 2차 검수는 **형식·상품명·본문겹침만 skip** 하고 ★ **같은 파일(중복) 검사는 그대로 둔다**(캡처 재탕은 blog 에서도 신호 — 회귀가드가 duplicate 줄에 blog 게이트가 없음을 고정).
  - ★ **판정 재료는 `loadTabExpectations` 가 같은 쿼리에 싣는다**(`utils/campaignTabLateral` 공유 LATERAL + `resolveWorkKind`) — 왕복 순증 0 · 규칙 사본 0. `workKind` 없으면(구버전·조회 실패) **전부 종전 판정 그대로**.
  - **리뷰어 화면**: 세 렌더 지점(단건 `memoTxt` · 다건 `mrMemo_<i>` · 슬롯 `csMemo`)이 **`_applyBlogMemoUi` 한 함수**로 수렴 — placeholder 교체 + 파란 안내 한 줄 + `inputmode="url"`. ★ blog 가 아니면 **원래 placeholder 를 복원**(`dataset.memoPh`)해 리뷰체험단 문구 무회귀. ★ 종류 판정은 **서버가 준 `workKind` 정확 일치**(탭명·상품명 추측 금지 — 회귀가드가 함수 본문에 `includes(` 부재를 고정).
  - ★★ **제출 2경로 모두 파일 0건 허용**: 단건·다건(`submitReview`)은 **행별로** blog 를 판정해 `emptySlots` 에서 빼고 URL 만 검증하며, 파일이 없으면 **업로드 호출 자체를 건너뛴다**(빈 `files` 로 `gasPostUpload` 를 부르면 Drive 에 빈 요청이 생기고 `_rtStayed`(라우팅 결과) 판정이 "남은 파일 0"으로 읽혀 정상 제출을 막는다). ★ 그 짝으로 **`reviewerName` 선언을 업로드 블록 밖으로 올린다** — 안 올리면 0건 제출에서 ReferenceError.
  - ★ **곁다리로 고친 기존 버그**: 슬롯 제출(`_submitReviewSlots`)의 `uploadErrors.length === slotsToUpload.length` 는 **0건일 때 `0===0` 이 참**이라 아무 오류 없이 "업로드 실패"로 되돌아갔다 → `slotsToUpload.length > 0 &&` 가드.
  - 회귀가드 `tests/blogPostUrlSubmit.test.js`(29케이스 — 순수함수 실행 · **스텁 pool 로 `/review` 라우트 실제 호출**(거부 시 `UPDATE review_index` 0회 · 리뷰 무회귀 · null 판정 폴백 · blog 만 슬롯 대조 생략) · `precheckPolicy` 실행 · **프론트 블록 vm 실행으로 사본 일치 14케이스** · 배선 · NUL 위생, **변이시험 13종 검출 확인**). ⚠ 가드 함정 2종: **`const` 는 컨텍스트 객체의 프로퍼티가 되지 않는다**(`vm.runInContext('이름', sandbox)` 로 식 평가해 읽을 것 — 함수 선언만 프로퍼티가 된다) · **거리 기반 정규식 창(`[\s\S]{0,200}`)으로 순서를 검사하지 말 것**(주석 한 줄에 창이 넘쳐 조용히 빨개진다 → `indexOf` 순서 비교, 단 `-1 < n` 이 항상 참이므로 **존재 확인을 먼저**).
- **M5-1 완료 — 블로그 주소 수집·전파 (migration 101)**:
  - ★★ **사용자 확정 3건(2026-08-10)**: ① **블로그URL = 블로그 주소**(blog.naver.com/아이디 — 쓴 글 주소인 **포스팅URL과 별개 칸**, 그쪽은 M4-1 memo 열 담당) ② 입력 주체는 **리뷰어(공고 참여 시) + 관리자(다른 채널로 모집한 블로거 사전등록)** ③ 구매양식은 **블로거가 직접** 작성(관리자는 명단·블로그 주소만).
  - **모델** = `campaign_applications.blog_url` · `order_submissions.blog_url`(101 — **컬럼 추가만·백필 0·CHECK 0·FK 0**, 087/082 규율). `REQUIRED_SCHEMA` 2종 등록(둘 다 INSERT 목록에 들어가 없으면 **참여·주문 원장이 전면 42703**).
  - ★★ **apply 게이트는 자리를 잡기 전에** — blog 공고면 403 `blog_url_required` 를 **`pool.connect()`·홀드 INSERT 앞**에서 반환한다(내정보 완비 게이트와 같은 자리 = **자리 미점유 · 그날 참여권 미소각**). 종류 판정은 `utils/workKind`(공고 > 탭), 형식은 `utils/blogPostUrl.isPostUrl` — **규칙 사본 0**.
  - ★★ **값의 출처는 서버 홀드 하나** — 제출 시 `_authoritativeHold` 가 `hold_token` 으로 조회한 `ca.blog_url` 을 쓴다(요청 본문 불신 = `option_key` 서버권위와 같은 규율). 홀드→주문 전파는 **`COALESCE(NULLIF(os.blog_url,''), ca.blog_url)`** — **관리자가 사전등록해 둔 값을 덮지 않는다**.
  - ★★★ **매퍼 규칙 순서가 곧 정확성**: `블로그주소` 는 아래 `key.includes('주소')` 에 걸려 **배송 주소가 그 칸에 찍히고**, `블로그URL` 은 어느 규칙에도 안 걸려 영영 안 채워졌다 → 블로그 규칙을 **주문자·주소보다 앞**에 둔다. ★ **빈 값은 `''` 가 아니라 `null`(=안 씀)** — `buildBatchUpdateData` 는 null 만 걸러내므로 빈 문자열은 **그 칸을 지우는 쓰기**가 된다(7/31 옵션 칸 사고와 같은 메커니즘 — 사전등록 주소를 다음 제출이 지우면 안 된다). ★ `포스팅` 은 이 규칙에 안 걸려 두 칸이 섞이지 않는다.
  - **리뷰어 화면**(`campaign.html`): 공개 응답에 **`work_kind` 동봉**(참여형 화이트리스트) → blog 공고면 참여 직전 **블로그 주소 시트**(`#blogSheet`)를 띄운다. ★ 화면 검사(`_isBlogUrl`)는 **서버 규칙과 글자 그대로 같고**(회귀가드가 14케이스 실행 대조) **최종 방어는 서버** — 403 을 받으면 사유를 그대로 보이고 다시 받는다(막다른 길 금지). 주소는 잘 안 바뀌므로 `localStorage` 에 기억해 다음 공고에서 프리필.
  - 회귀가드 `tests/blogCampaignApply.test.js`(50케이스 — 매퍼 **실행** · 게이트 위치 · INSERT 컬럼≡자리≡파라미터 · 전파 COALESCE · 프론트 규칙 vm 실행 대조, **변이시험 15종 검출 확인**). ⚠ 가드 함정 3종(변이시험이 실제로 뚫었다): **`[\s\S]*` 로 두 SQL 문장을 가로질러 재면 한 쪽 주석 처리를 통과시킨다**(문장 단위로 볼 것) · **문자열 존재 검사는 `if(false && …)` 를 통과시킨다**(분기 형태를 고정할 것) · **주석 처리해도 문자열은 남는다**(줄 주석을 걷어낸 뒤 볼 것). ⚠ 컬럼 추가로 자리표시자 꼬리가 밀려 기존 가드 3종(`campaignMultiAccount`·`campaignOptionsGuards`·`campaignM2Guards`)의 패턴을 갱신했다 — **꼬리를 고정하지 말 것**(`(,\$\d+)*`).
- **M5-2 완료 — 작업보드 [＋ 블로거 추가] (관리자 사전등록 · 스키마 변경 0)**:
  - **왜 필요한가**: 블로그체험단은 **모집공고 밖(카톡·직접 섭외)** 으로 데려오는 블로거가 있다. 그 사람은 공고 참여(홀드)를 하지 않아 M5-1 의 apply 경로로는 명단에 못 들어오고, 담당자가 표를 눈으로 채워도 **연락처가 없으면 그 블로거가 자기 참여 내역·리뷰 제출 화면을 영영 못 찾는다**(검색 키가 phone8). 창구 = **작업보드 [⋯] 도구**(사용자 확정 — 관제 패널이 아니다).
  - ★★ **판정·쓰기 사본 0** — 무시트 `utils/sheetlessScope` · 종류 `utils/workKind`(공고 > 탭, `workKindContext.service`) · 주소 형식 `utils/blogPostUrl.isPostUrl`(**리뷰어 입력과 같은 규칙**) · 이름/연락처 열 `utils/applicantColumns` · 줄 INSERT `participants.createSlotsFromSheetRows`(campaign_participants 쓰기 소유자) · 장부 `sheetlessLedger.rebuildLedgers`.
  - ★★ **블로그 주소를 넣을 칸 = `orderLedger.blogUrlWriteColumns`(매퍼 파생)** — 센티널을 주입해 `mapOrderToSheetRow` 를 실제로 돌리고 그 값이 나온 열만 고른다(`optionWriteColumns` 와 같은 기법). 헤더 문자열 사본(`includes('블로그')`)을 두면 **"사전등록이 넣은 칸 ≠ 제출이 쓰는 칸"** 으로 갈린다. 센티널에 공백·NUL 금지.
  - ★★ **fail-closed 거부 7종**(성공으로 꾸미지 않는다): 시트 기반 탭(`not_sheetless` — 여기서 시트 append 경로를 새로 열면 쓰기 표면이 늘고 그 줄의 진실원본이 갈린다) · 미등록 탭(`tab_not_registered` — 접수 단일 관문 유지) · 블로그 아님(`not_blog`) · 열 구성 불명(`no_headers`) · 이름/연락처/블로그 열 없음 · **같은 연락처가 이미 그 표에 있음**(`duplicate`, 몇 번 줄인지 말한다) · 이름·연락처 8자리·주소 형식.
  - ★ **`seq = MAX(seq)+1` — 삭제된 줄도 세어 재사용하지 않는다**(`(sheet_id, tab_name, seq)` 키 충돌 = 표가 두 겹). `dryRun` 은 쓰기 0으로 자리 번호만 돌려준다.
  - **API = `POST /api/trackb/worktable/add-blogger`** — 게이트는 **`_ensureEditScope`**(master/admin 전체 · staff 담당 탭만 · 광고주 차단). 그리드 셀 편집과 같은 범위이고 정원·총량을 바꾸지 않으므로 adminOrMaster 까지 좁히지 않았다. 400대 매핑(`BlogRegisterError`) · 42P01/42703 은 `not_ready`.
  - ★ **화면 게이트 `_brCanAdd`** = 무시트 ∧ **`workKind !== 'review'`** ∧ 내부인. **명시적으로 review 인 작업만 숨긴다** — 미설정(빈 값)은 서버가 판정하게 열어 둔다(안 그러면 "블로그 작업인데 버튼이 없다"와 "리뷰 작업에 쓸모없는 버튼" 중 하나를 반드시 만든다). 재료는 `listActiveTabs` 가 **이미 조인한 `tab_configs`** 에서 `workKind` 한 칸만 더 실어 **쿼리 순증 0**.
  - ★ 모달은 **body 직속** · **바깥클릭으로 닫지 않는다**(입력값 보호, Esc·[취소]만) · Esc 리스너 **최상위 1회** · 재렌더가 입력값을 보존(IME) · onclick 문자열 보간 0.
  - ⚠ **구매양식은 그 블로거가 직접 제출한다**(사용자 확정) — 여기서는 명단·블로그 주소까지만. 사전등록한 주소를 뒤이은 제출이 덮지 않는 것은 M5-1 의 `COALESCE(NULLIF(...))` 전파 + 매퍼가 빈 값에 `null`(=안 씀)을 돌려주는 규칙이 이미 보장한다.
  - 회귀가드 `tests/blogRegister.test.js`(61케이스 — 매퍼 파생 열 실행 · 스텁 pool 로 서비스 **실제 실행**(거부 13갈래 + 성공 + dryRun 쓰기 0 + 공고>탭 + 직접 INSERT 0) · 라우터 스택 실검사 · `_brCanAdd` vm 실행 7조합 · NUL 위생, **변이시험 16종 검출 확인**). ⚠ 곁다리로 `sheetlessBadge` 가드의 "시트 직링크 0" 검사를 고쳤다 — 시트 가져오기 마법사의 **입력칸 placeholder 안내문**을 링크로 세어 착수 전부터 빨간 상태였다(안내 문구는 링크가 아니다).
- ⚠ 미확정 가정 1건(PRD §02): 구매양식 간소화 범위(1차 동일).
- **M6 완료 — 승인제 참여 + 필수 3열 + 결과물 캡처+URL (migration 127, 사용자 확정 2026-08-19)**:
  - ★★ **워크플로우 = 블로그URL 제출(`blog_pending`, TTL 없음·정원 미점유) → 관리자 [승인]/[반려] → 승인된 블로거만 구매 → 결과물 = 리뷰 캡처 + 포스팅결과URL 둘 다**. 확정 5건 = ① 승인한 사람만 정원 계수(승인 시점 재검사) ② 승인 후 **24h(`BLOG_PURCHASE_TTL_MIN` 1440) 내 미구매 = 자동 취소** ③ 반려 즉시 재신청 가능(부분유니크에서 `blog_rejected` 제외) ④ 알림 = 공고 페이지 상태 표시 + 1:1 문의 자동 메시지(`csBridge.postAdminNotice`, COMMIT 뒤 fail-soft) ⑤ 포스팅제출일 = 시스템 자동 기록.
  - ★★ **승인 = `status='applied'` 전이(별도 approved 상태 금지)** — 유효홀드 판정(applied+expires_at)을 쓰는 소비처가 십수 곳(카운트·스윕·confirmHoldInTx·work-detail·cancel·change-option·my-status)이라 새 상태를 만들면 전부 분기가 필요하고 한 곳만 놓쳐도 승인 건이 조용히 새거나 굳는다. 승인 뒤는 **기존 홀드 파이프라인 그대로**(만료→expired→late 구제→관제 수동확정까지 동일). 만료 스윕은 여전히 `applied`만(가드가 `campaignHold`의 blog_pending 참조 0 고정).
  - ★ **대기/반려는 '만료'가 아니다** — work-detail 이 `pending_approval`/`apply_rejected` 전용 reason 반환(구버전 화면은 모르는 reason=재시도라 토큰 보존). campaign.html 은 그 reason 으로 대기(30초 폴링)/반려(사유+[다시 신청]) 화면을 그린다. 신청 취소는 cancel 이 `blog_pending`도 받는다. 킬스위치 `BLOG_APPROVAL_FLOW=0`(신규 신청만 종전 즉시-홀드 — 이미 대기 중인 건은 승인 API 로 소화).
  - ★★ **작업표 필수 3열 = `블로그URL`·`포스팅결과URL`·`포스팅제출일`** — `worktablePlan.ensureBlogColumns`(courier 송장열과 같은 규율: 템플릿에 없어도 시스템이 붙임, 존재 판정 = 실제 기입 규칙과 동일). **blog 작업표는 구매일자를 깔지 않는다**(빈 칸 = 정상 모양 — 바를참스킨 0804 그대로). ★★ `memoColumn.isPostDateHeader` 가 URL 칸↔날짜 칸을 가른다 — 없으면 포스팅URL 이 제출일 칸에, 날짜가 URL 칸에 들어간다(양방향). 매퍼는 포스팅결과URL·포스팅제출일 칸에 null(무접촉).
  - ★★ **결과물 = 캡처+URL 둘 다(M4-2 "URL만=완료·캡처 0장 허용" 확정을 사용자가 뒤집음)** — 서버 `/submit/review` 가 원장(review_submissions)+대표 이미지로 캡처 존재를 최종 방어(`capture_required`, 조회 실패는 fail-open warn). 프론트 3경로(단건·다건·슬롯)도 캡처 필수(슬롯 재제출은 기제출 슬롯 있으면 예외). **포스팅제출일**은 완료 시 KST 날짜를 시트 bg 쓰기·큐 재시도(`payload.postDate` — 재시도 시각 아님)·무시트(`markSheetlessPostDate`) 세 경로가 같은 열 고르기로 기록.
  - **관제(📡)**: blog_pending 전용 승인 큐(오래 기다린 순, 블로그 링크는 https 검증+noopener, 반려 사유는 인라인 입력 — prompt 금지) + 카드 관제 버튼 보라 배지(`ops.blogPending`). 승인/반려 API = `/api/campaign/admin/:id/blog-approve|blog-reject`(adminOrMaster) + trackb 위임(confirm 과 같은 2단 게이트). 반려 사유 필수(리뷰어에게 그대로 전달 — pool.connect 앞에서 검사).
  - **리뷰어 홈 공고 2탭** — index.html 미리보기·recruit.html 목록에 「리뷰체험단|블로그체험단」 탭(판정 = `work_kind==='blog'` 정확 일치·빈 값=리뷰, 렌더러는 cardHtml 한 벌 — 탭은 배열 필터만). blog 카드 = 📝 배지 + CTA 「신청하기」 + pt-live 「승인제」. 레거시 공개 화이트리스트에도 work_kind 합류.
  - ★ **발행 모달 `rf_work_kind` hidden 배선** — 작업오더 프리필·편집 로드가 채우고 payload 가 읽는다(blog 면 리뷰타입 카드 숨김+안내 배너, review_type 미전송). **blog 일건수 정규화 = daily가 비면 총모집(무제한=9999)** — 프론트 payload(★ `payload.daily_limit` 대입 **뒤**에서 — 앞에 두면 덮여서 무효, 실측) + 서버 create 이중 방어. 상태엔진 무수정(daily=총원이면 daily_done 이 총원 마감보다 먼저 올 수 없다). 프론트 자동점검도 blog 는 일건수 요구 면제.
  - 회귀가드 `tests/blogApprovalFlow.test.js`(59케이스 — 스텁 pool 로 승인/반려 **실제 실행**(정원 재검사·옵션 소진·멱등·사유 필수·잠금 왕복 0) + migration 모양 + 스윕 무접촉 + 3열/열 오염 실행 + 배선). ⚠ 이 변경으로 가드 4종 패턴 갱신(검사 의미: `blogPostUrlSubmit` 은 **새 확정으로 의미 변경**(캡처 필수) / `campaignM2Guards`·`campaignOptionsGuards`·`campaignM2FrontGuards` 는 불변 — apply INSERT status 가 리터럴→$12 파라미터). ⚠ 착수 전부터 빨간 가드(무관): `campaignReviewerHidden`·`campaignPinnedPopular`·`homeCampaignButton`·`workdeskOrdersCampaigns`.
  - 잔여(문서화): ① [＋ 블로거 추가](M5-2)는 작업표 줄만 — campaign_applications 미생성이라 공고 정원에는 안 잡힌다(관제 sheetInfo 의 로스터 대조가 차이를 보여준다) ② 승인 후 24h 만료 건 재신청은 기존 당일 규칙을 따른다(반려와 달리 즉시 예외 없음 — 관제 수동확정이 구제) ③ 관리자가 승인을 되돌리는 창구는 없다(24h 만료로 자연 회수) ④ blog 공고는 🔥 인기상품 지정을 지양(선행참여 크레딧 개념이 승인제와 안 맞는다 — 게이트 미변경).

- **M6 프로덕션 E2E 완주 (2026-08-19)** — 오더(blog)→무시트 접수→3열→숨김 공고→신청(URL 없음 403/URL pending)→관제 큐(blog_url·ops.blogPending)→반려(사유 필수·리뷰어에 사유 표시)→**즉시 재신청**→승인(24.0h TTL)→구매(홀드 confirmed·블로그URL 칸 기입)→캡처 없이 제출 `capture_required`→캡처+포스팅URL 제출→`리뷰='제출'`·`포스팅결과URL`·`포스팅제출일` 기입까지 실서버에서 확인. 테스트 데이터는 정리(빈 테스트 탭 3개·테스트 리뷰어 E2E블로거·Drive 1px 캡처 4장만 잔존 — E2E블로거는 120 FK RESTRICT 로 삭제 불가).
  - ★★ **E2E 가 잡은 실버그: '포스팅제출일'이 리뷰제출 열로 오탐** — 완료신호 '제출' 포함이라 2단계 제출열 탐지가 그 칸을 골라, 제출 표시가 날짜 칸에 들어갔다 날짜 스탬프에 덮이고 진짜 '리뷰' 칸은 비어 **is_submitted 가 장부 재생성마다 FALSE 로 무너졌다**. → `SUBMIT_EXCLUDE_PATTERNS` 에 '포스팅' 추가(제외 후 3단계가 '리뷰' 단독 열 채택), 회귀가드 `columnResolver.test.js` 케이스4e.
  - ⚠ 작업보드 `submitted` 플래그는 투영(10분 크론) 뒤에 켜진다 — `review_index.is_submitted` 는 즉시.
