# 탈 구글시트 이관 — 진행 기록 (W1~W5, 2026-08)

> CLAUDE.md 에서 분리한 **완료된 마이그레이션의 진행 기록**입니다.
> 지금도 지켜야 하는 규율은 CLAUDE.md 의 「탈 구글시트(무시트 운영)」 절에 요약돼 있고,
> 이 문서는 **왜 그렇게 정했는지·무엇을 밟았는지**를 찾을 때 봅니다.
> 완료 상태: 전 구간 프로덕션 E2E 완주(2026-08-08). 남은 358건(연도 미확정 과거 자료)은 이관하지 않습니다.

### ★★ 시트 우위 동기화 1차 (점검 → 슬롯 백필 → 공고 정원 교정) — `sheetSlotSync.service.js`
- **사고(2026-08-07 우레온 100건)**: 시트엔 100행이 준비돼 있는데 리뷰웹 표는 **20행**에서 끝나고 공고는 "옵션 1종 · 전체 마감". 원인 = **검색인덱스 파서가 이름 없는 행을 버린다**(`columnResolver` `if (!name) return null`) → 구매일자·리뷰옵션만 적힌 준비 행 80개는 표에 **자리조차 생기지 않는다**(063 이 RAW 미러에서 일정을 읽는 이유와 같은 구조). 정원 잠금은 별개 층 = `campaign_options.recruit_total` 소진.
- ★★ **원칙: 기준은 리뷰웹시스템이되, 시트가 더 많으면 먼저 표를 그만큼 늘린 뒤에 끊는다.** 순서 고정 = 점검 → 백필 → 정원 교정 → (다음 단계) 연결 끊기.
- **준비 행 정의 = 구매일자가 날짜로 해석되는 행**(그 칸이 곧 063 의 그날 정원 재료 — 다른 기준을 쓰면 "표는 늘렸는데 일정은 그대로"). ★ 값은 있는데 날짜로 안 읽히는 칸은 `datelessFilled` 로 **세어서 고지**(조용히 버리지 않는다).
- ★★ **판정 사본 금지**: 헤더 = `utils/sheetHeader` · 날짜열 = `campaignSchedule.findDateColumnIndex` · **옵션 칸 = `orderLedger.optionWriteColumns`**(헤더에 '옵션'이 든 칸을 직접 고르면 `옵션금액`(결제금액)·`비고(옵션확인)`을 오분류 — 이미 밟은 함정). 읽기 = **RAW 미러만**(시트 API 0), `cells->>$n::int` 캐스트 준수.
- ★★ **슬롯은 `participants.createSlotsFromSheetRows` 한 곳에서만 INSERT**(campaign_participants 쓰기 소유자): **seq = 시트 실제 행 번호**(어긋나면 주문 유입 시 `ON CONFLICT (sheet_id,tab_name,seq)` 가 제자리 갱신을 못 해 표가 두 겹) · `source='worktable'`(**'manual' 금지** — 그러면 리뷰를 내도 리뷰제출·입금 표시가 영영 안 켜진다) · `row_json`(헤더명→값, import 와 같은 모양이라 그리드가 구매일자·리뷰옵션을 그대로 그림) · **ON CONFLICT DO NOTHING**(추가만·멱등·비파괴).
- ★ **백필의 gid 는 서버가 `tab_configs` 에서 다시 구한다** — 클라이언트 gid 를 믿으면 낡은 화면이 엉뚱한 탭 행을 이 표로 밀어 넣는다(작업표 탭 삭제와 같은 규율). 라우트 **기본은 미리보기**(`dryRun !== false` — 값이 빠진 요청이 곧바로 실행되면 안 된다).
- ★★ **정원 교정은 더 여는 쪽만**(`evaluateQuota` 순수함수 단일 출처): `0`(무제한) 미접촉 · 이미 충분하면 미접촉(**줄이지 않는다**) · **옵션 2개 이상이면 자동 배분 금지**(`multi_option_manual` — 어느 옵션에 몇 명인지는 시스템이 정할 수 없다) · `daily_limit` 미접촉(그날 정원은 구매일자 분배가 정한다). **쓰기 표면 = `recruit_total` 두 칸뿐**이고 UPDATE 는 조건부(`> 0 AND < target`)라 남이 올려둔 값을 되돌리지 않는다. target 은 **서버가 표에서 다시 센다**(화면 숫자 불신) + 그 공고가 정말 이 탭에 연결됐는지 검증(`campaign_mismatch`).
- **화면** = `frontend/sheet-sync-audit.html` 아래쪽 "시트 우위 점검" 섹션(시트 준비/시스템 표/부족/투입 기간/연결 공고 정원 나란히, **미리보기 → confirm → 실행** 2단계, onclick 은 인덱스만). 라우트 3종 `/api/trackb/sheet-sync/slot-audit·slot-backfill·quota-fix`(adminOrMaster).
- 회귀가드 `tests/sheetSlotSync.test.js`(38케이스 — 스텁 pool 로 서비스·라우트 실제 실행, **변이시험 12종 검출 확인**) + `tests/sheetSlotSyncPg.test.js`(**진짜 PG16** 11케이스 — 우레온 시나리오 100행 재현: 캐스트 누락 시 전 행 NULL 재현 · LATERAL/튜플 IN 실행 · 백필 멱등·기존 행 보존 · 조건부 UPDATE). ⚠ 스텁은 SQL 을 해석하지 않으므로 이 둘을 함께 돌릴 것.
- ★ **다음 단계 전제**: 연결 끊기(D)는 **무시트 검색·행배정(F2)** 이 준비된 뒤에만 연다 — 먼저 끊으면 "표는 100명분인데 리뷰어가 검색하면 안 나오는" 작업이 된다.

### 탈 구글시트 최종 단계 계획
- **시트 우위 동기화 → 연결 끊기(마지막 트리거)** = `frontend/docs/탈구글시트_시트우위동기화_연결끊기_와이어프레임.html`(2026-08-07, 우레온 100건 사례 발단). 원칙 = **기준은 리뷰웹시스템이되, 시트에 준비 행(구매일자 채워진 이름 없는 행 — `columnResolver`가 버려 표에 안 들어옴)이 더 많으면 먼저 그만큼 표 슬롯을 백필한 뒤 시트를 끊는다**. 4단계 = 시트 우위 점검(RAW 미러 대조, 반영 점검 화면 확장) → 슬롯 백필(worktable 슬롯 방식 재사용, seq=시트 행번호·구매일자/리뷰옵션 동반) → 검증+공고 정원 1클릭 교정(자동 변경 금지) → 연결 끊기(완전 분리 — 읽기·쓰기 중단, 게이트=인원 일치+parity 0+반영대기 0, 재연결 비상구). ★ **무시트 검색·행배정(F2)이 준비되기 전에 끊기를 열지 않는다**(끊으면 검색 안 되는 작업이 됨) — 점검·백필·정원 교정(1차)은 F2 없이 안전해 먼저 배포.
- **최종점검·결정 문서** = `frontend/docs/탈구글시트_최종점검_결정사안_와이어프레임.html`(2026-08-07 — 걸림돌 지도 🔴구조3·🟡공백5 + 해결 전략 길A/길B(권장 B = "장부 3권(review_index·index_master·RAW 미러)은 남기고 채우는 쪽만 작업표로 교체" = 소비처 수십 곳 무수정) + **사용자 결정 사안 D1~D7 선택 카드**(클릭 선택→요약) + 전환 스위치 설계(전역 기본값 + 자동 점검표 fail-closed + 작업 단위 예외) + W1~W4 로드맵. 목표 = **신호 한 번에 즉시 전환 가능한 "준비 완료" 상태**). 실측 확인된 핵심 걸림돌: ① 전 시스템 키 = (sheet_id, tab_name) — 가상 ID 도입 시 smartBuild·rawMirror 의 `campaigns∪tab_configs` 스윕이 구글 404 반복(제외 게이트 필수) ② `listActiveTabs` 가 raw_sheet_tabs∩index_master 출발이라 무시트 작업은 접수해도 작업보드에 안 뜸 ③ search.service 전부 review_index 기반(stale-pl 보안 게이트 포함) ④ 주문 mirror_status 라이프사이클에 "시트 없음" 종결 상태 부재(없으면 reconcile 헛돌기 + order_unmirrored 알림 도배) ⑤ 063 시트 일정·tabStatsMap·입금 M1 대상 추출·Drive 폴더 {시트제목} 경로가 전부 시트 파생.
- ★★ **사용자 확정(2026-08-07) D1~D7**: D1-a **길 B**(장부 3권 유지·채우는 쪽만 작업표로 교체) · D2-a 폴더 이름 = **업체명 자동** · D3-a 날짜별 정원 = **campaign_daily_plans(달력) 일원화**(작업표 생성 시 날짜 분배를 달력에 자동 저장) · D4-b 인트라넷 시트URL 칸 **완전 제거**(리뷰웹 접수의 시트 병행 경로는 유지) · D5-a 광고주 시트 공유 = 전환과 함께 자동 소멸 · D6-a **전역 스위치 1개 + 작업 단위 예외** · D7-a 준비 완료 기준 = 테스트 공고 + 실전 1건 완주. ★ "스위치" = 물리 토글이 아니라 **완전 전환 시점**(자동 점검표 fail-closed 가 그 시점 선언의 조건, 시점 결정은 사람).
- ★★ **범위 확정(2026-08-07 추가) — 기존 활성 작업까지 전부 대체**: F5 의 "새 작업만 무시트·기존은 자연 종료"를 사용자 확정으로 **"기존 활성 작업 전량 이관 포함"** 으로 확대(W5). 길 B 라 기존 작업은 **키((sheet_id,tab_name))·장부 3권을 그대로 두고 채우는 손만 시트→작업표로 교체** = 가상 ID·데이터 이동 불필요. 이관 절차(작업별) = 시트우위 점검 → 슬롯 백필 → 최종 미러 + parity 대조 → 연결 끊기(그 작업만 시트 읽기/쓰기 중단 + `sheetNotice` 로 시트에 "이관됨" 공지 기입) → 재연결 비상구. 일괄 이관은 **대조 통과분만**(cutoverAll 규율 — force 미전달, 미통과는 사유 보고). ⚠ **레거시 공고(participation_mode=false)의 참여 행추가는 `appendSheet` 직접 경로**(campaign.routes.js:2303 실측) — 이관된 작업에서는 이 쓰기가 작업표로 가도록 분기 필요(주문 원장 큐와 별개의 시트 쓰기 경로라 W5 에서 반드시 처리). 최종 상태 = 시트를 읽는 작업 0건.
- ★★ **투입스케쥴 확정 구조(착수 전 질문 답)**: 종전 수작업(시트 구매일자에 8/7×5행·8/8×5행·주말 건너뛰기 수동 기입)의 대체는 **이미 완성된 작업표 계획(worktablePlan.distributeDates)** — 인트라넷은 **진행방향만** 전송(투입시작일·일일건수·총건수·구매시간대, 실측)하고 **표 생성은 리뷰웹**(시작일부터 일건수씩 분배·주말 제외 기본 `skipWeekends`·제외 날짜(공휴일·휴무) 담당자 지정·`M / D (요일)` 표기). 탈시트 후 분배 결과의 저장처만 시트 셀 → **달력(D3-a)** 으로 바뀐다. **공백 1건** = 인트라넷 오더 폼에 주말 제외 여부·휴무일 신호가 없음 → W2 에서 오더 폼에 [주말 제외(기본 켬)]·[휴무일(선택)] 2칸 추가 → 작업오더 경유 미리보기 프리필(가산적 — 없으면 종전대로 담당자가 미리보기에서 지정).
### ★★ 탈 구글시트 W1 — 무시트 표식 + 장부 생성기 + 크론 단속 (migration 096)
- **길 B(사용자 확정 D1-a) 의 실행부**: 장부 3권(`raw_sheet_tabs`/`raw_sheet_rows` · `index_master` · `review_index`)은 **그대로 두고 채우는 손만** 시트 → 작업표(`campaign_participants`)로 바꾼다 → 검색·행배정·리뷰 내역·홈 통계·입금 대상 추출 등 **소비처 수십 곳이 무수정**이고, 검증된 보안 게이트(stale `participation_links` fail-closed)도 그대로 재사용된다.
- **표식** `tab_configs.sheetless`(096, +`sheetless_at`/`sheetless_by`) — **컬럼 추가만·기본 FALSE = 배포 즉시 동작 100% 불변**(opt-in). ★ **탭 단위**인 이유 = 기존 활성 작업 이관(W5)은 **한 시트 안에서 탭별로** 진행되므로 같은 시트에 시트 기반 탭이 남아 있으면 그 시트는 계속 읽어야 한다.
- ★★ **"구글에 물어야 하는가" 판정 단일 출처 = `utils/sheetlessScope.js`** — 시트를 부르는 주기 작업이 여럿(smartBuild·전체빌드·변경감지·RAW 미러)이고 각자 `campaigns ∪ tab_configs` 를 통째로 순회하므로, 한 곳이라도 새면 **매 사이클 구글 404 반복**이다. 시트 단위 제외는 **`BOOL_AND` = 등록 탭이 전부 무시트일 때만**(OR 로 완화하면 시트 기반 탭이 남은 시트가 통째로 안 읽힌다), 탭 판정은 **이름 → gid 폴백**(리네임 대비) + ★ **빈 gid 는 키를 만들지 않는다**(만들면 gid 없는 탭이 한 키로 뭉쳐 전부 매칭). ★ **조회 실패·컬럼 미적용은 빈 집합(fail-open)** — 게이트가 죽었다고 스윕을 멈추면 시트 기반 작업 전체가 갱신을 멈춘다(무시트 탭을 한 사이클 더 읽는 쪽이 훨씬 가볍다).
- ★★★ **크론 단속은 네 곳 전부 — smartBuild 를 빠뜨리면 이 기능이 통째로 무의미하다**: 5분 크론 `smartBuild.service` 가 이관한 탭을 계속 읽어 **작업표로 만든 장부를 옛 시트 값으로 덮는다**(= 시스템에서 편집한 내용이 되돌아간다). 구현 중 실제로 빠져 있었고 **회귀가드가 잡았다**. 게이트는 **시트 단위 + 탭 단위 두 겹**을 네 경로(`smartBuild` · `buildIndexSmart` · `checkDirtySheets` · `rawMirror`)에 모두 건다. ★ 제외 건수는 요약 로그에 **고지**(조용한 누락 금지).
- ★★ **장부 생성기 `sheetlessLedger.service.rebuildLedgers`** — 작업표 → 장부 3권을 **한 트랜잭션**으로 재기록. ★★ **검색 명단은 기존 파서 `indexBuilder.parseTabRows` 를 그대로 통과**시킨다(작업표 행을 시트 모양 2차원 배열로 재조립해 넣는다) — 키워드(`loadKeywordsFromDB`)·컬럼 매핑(`getTabColumnIndexMap`)까지 시트 경로와 같은 것을 쓰므로 **이름열 우선순위·제출/입금 판정·phone8 추출·`submit_col` 감지에 사본이 0**이다. 판정 표를 복사하면 "시트 작업과 무시트 작업이 서로 다르게 검수·검색되는" 드리프트가 조용히 생긴다.
- ★★ **두 장부의 행 수가 다른 것은 의도**: `raw_sheet_rows` 는 **빈 슬롯 포함 전 행**(행 배정 `claimRow` 가 빈 줄을 찾는 재료) / `review_index` 는 **이름 있는 행만**(파서가 `if (!name) return null` — 검색에 빈 줄이 뜨면 안 된다). ★ **seq = 시트 실제 행 번호 자리를 지킨다**(주문 배정·투영·claim 이 전부 이 번호를 키로 쓴다 — 앞으로 당기면 표가 두 겹).
- ★ **fail-closed 3종**: 시트 기반 탭(`not_sheetless` — 시트 값 덮어쓰기 차단) · 미등록 탭(`tab_not_registered` — 접수 단일 관문 유지) · 열 구성 불명(`no_headers` — 빈 장부를 만들지 않는다). 헤더 우선순위 = **저장된 장부 헤더 → 호출자 열 구성 → row_json 키**(재생성마다 열이 흔들리지 않게).
- 회귀가드 `tests/sheetlessLedger.test.js`(54케이스 — 순수함수 · 스텁 pool 로 서비스 실제 실행 · 크론 배선 + **진짜 PG16 으로 "생성한 장부가 `listActiveTabs` 에 실제로 뜨는지"** = 최종점검 문서의 구조 2 걸림돌 해소 확인, **변이시험 12종 검출 확인**). ⚠ 변이시험이 잡은 약한 단언 3종을 강화했다 — 빈 gid 검증은 **손으로 만든 Set 이 아니라 실제 조회 결과**로, 카운터는 **로그 문구뿐 아니라 증가문까지**.
### ★★ 탈 구글시트 W2-a — 시트 없는 접수 + 주문 즉시 완결 + 폴더 업체명
- **W1 이 "장부를 만드는 손"을 바꿨다면 W2-a 는 "작업의 한 바퀴"를 무시트로 돌린다** — 접수 → 리뷰어 검색 → 구매양식 제출 → 리뷰 캡처 업로드가 구글 호출 0으로 완결된다. 전부 `sheetless=TRUE` 탭에서만 동작(표식이 아직 아무 데도 안 켜져 있어 배포 시 운영 영향 0).
- **① 시트 없는 접수(F1)** = `POST /api/order/admin/accept {sheetless:true}` (`sheetlessAccept.service.js`): 가상 시트ID(`wt_` + hex20 = 23자 — 구글 ID 형식 검증에 안 걸리게) + 가상 gid(숫자 문자열 — `/^\d+$/` 를 요구하는 곳이 여럿) 발급 → `campaigns`·`tab_configs`(sheetless=TRUE) 등록 → `createSlotsFromSheetRows`(작업표 행) → `rebuildLedgers`(장부 3권). **구글 API 호출 0**.
  - ★★ **등록 단일 관문은 그대로 접수** — 이 서비스는 접수 라우트의 "탭 해석" 단계만 대신한다. **campaigns/tab_configs 업서트 · 상태 전이 · 계약 자동 연결(088)은 두 경로가 같은 코드**(회귀가드가 각 SQL 이 파일에 **한 곳뿐**임을 센다 — 사본을 두면 무시트 작업만 메타가 비거나 계약이 안 붙는다).
  - ★★ **이관은 되돌리지 않는다**: 업서트의 `sheetless = COALESCE(tab_configs.sheetless,FALSE) OR EXCLUDED.sheetless`. 시트 URL 로 재접수했다고 조용히 시트 기반으로 되돌리면 **크론이 옛 시트 값으로 장부를 덮어** 시스템에서 편집한 내용이 사라진다(fail-closed — 되돌리기는 별도 명시 경로로만).
  - ★ **무시트 탭의 `sheet_url` 은 빈 값** — 구글 URL 로 조립하면 죽은 링크가 되고 화면·감사 도구가 진짜 시트로 오인해 연다. ★ 값 생성은 시트 경로와 **같은 `planToSheetValues`**(미리보기 ≠ 실제 표 방지), 계획은 **서버가 다시 계산**(`buildWorktablePlan` — 화면이 보낸 행 목록 불신). ★ 헤더 행 = 1(덮을 템플릿 메타·공지문이 없다). ★ 재접수(2차)는 **새 작업표를 또 만들지 않는다**(가상 ID 재사용, 차수는 종전대로 '차수' 컬럼이 담당).
  - ★★★ **무시트가 기본이다 — 시트URL 없는 접수는 막다른 길이 아니다 (사용자 확정 2026-08-10)**: 인트라넷 리뷰오더에는 시트URL 칸이 없는데(D4-b) 접수가 URL+gid 를 강제해 **인트라넷에서 넘어온 오더를 접수할 방법이 아예 없었다**(`접수 실패: 작업시트탭URL이 없습니다`). → **판정 단일 출처 `sheetlessAccept.resolveAcceptMode`**(라우트·화면에 사본 0): ① `body.sheetless===true` = 명시 요청 ② **이미 무시트인 오더는 되돌리지 않는다**(URL 이 뒤늦게 붙어도 시트 경로로 안 간다 — `tab_configs` 의 `OR` 병합과 같은 fail-closed) ③ `body.sheetless===false` = 시트 경로 ④ URL 있음 = 시트 경로(종전 그대로) ⑤ **URL 없음 = 무시트**. ★ **URL 이 "있는데 형식이 틀린" 경우는 추측하지 않는다** — 사람이 붙일 의도로 넣은 값이라 종전대로 사유를 말하고 멈춘다(조용히 다른 길로 가지 않는다). ★ 되돌리기 = 킬스위치 **`SHEETLESS_ACCEPT_DEFAULT=0`**(그때는 종전 400 안내로 떨어진다 — 그 문구·gid 검증은 코드에 그대로 남아 있다).
  - ★ **화면은 확인창 문구만 가른다**(값을 보내지 않는다 — 서버가 최종 판정): `_woAcceptSheetless`(work-order-detail.js 공유, 표시용) 로 리뷰웹시스템[3버전]·관리자 대시보드가 **같은 문구**를 쓴다. 관리자 대시보드의 "URL 없으면 접수 차단" 선검사는 **제거**(막다른 길이었다). ★ 무시트 접수가 막히면(건수 0 · 표준 열 미설정 등) `blockers` 를 **문장 그대로** 보여준다 — "접수 실패"로 뭉뚱그리면 무엇을 고칠지 알 수 없다. 성공 토스트는 `(무시트 · 작업표 N줄)` 로 무엇이 생겼는지 말한다.
  - ★★ **접수 흐름에 구글시트 생성 창구는 없다 (사용자 확정 2026-08-10 "구글시트 생성이 필요하면 없애")**: 작업오더 [📋 작업표] 미리보기의 유일한 실행 버튼이 **[이 구성으로 접수하기]**(`wtpAccept`) 하나이고, 그것이 **접수하기와 같은 `_woAccept` 한 벌**로 수렴한다(미리보기에서 조정한 `planOptions`(총건수·일건수·시작일·주말 제외·휴무일·채널·작업유형)와 `작업 이름`을 그대로 실어 보낸다 — 서버는 종전대로 **계획을 다시 계산**한다). 종전의 [이 구성으로 만들기](구글시트 탭 복사·행 생성)·[표 줄 내리기]·[시트 탭 삭제] UI 는 **제거** — 남겨 두면 그 경로가 `work_orders.work_sheet_url` 을 다시 붙여 **다음 접수가 시트 경로로 되돌아간다**(`resolveAcceptMode` ④). ★ **서버 라우트 `/worktable/create`·`/delete`·`/delete-tab` 은 그대로 둔다**(되살리기 쉽게 · 회귀가드가 등록 생존을 고정) — 없앤 것은 **화면 창구**뿐. ★ 버튼 비활성 사유는 3갈래로 구분해 말한다(계획 잠김 / **이미 접수됨** / 시트탭URL 보유 = "접수 시 그 시트 탭이 등록됩니다 · 아래 구성은 미적용").
  - ⚠ **운영 함의**: 접수 전에 **설정 › 작업표 표준 열**(공통 열)이 정해져 있어야 한다 — 비어 있으면 `no_columns` 로 막히고 화면이 그 사유를 말한다.
- **② 주문 즉시 완결**(`sheetlessOrder.service.js`): 무시트 탭은 `order_append` 큐를 **안 타고** 작업표 `row_json` 에 직접 기록 → 장부 재생성 → 완결. 같은 요청 안에서 끝나므로 큐·재시도·시트 사후검증이 통째로 필요 없어진다(최종점검 문서의 "시트 없음 종결 상태 부재 → reconcile 헛돌기 + 알림 도배" 해소).
  - ★★ **`mirror_status` 는 그대로 `'written'`**(새 상태 미도입): 'written' 은 **이미 종결 상태**이고 소비처가 여럿(reconcile 제외 · 배치 스케줄러 제외 · 미반영 알림 제외 · Track B 금액 집계 · 교체요청 자동해결)이다. 새 값을 만들면 그 목록을 전부 훑어야 하고 **한 곳만 놓쳐도 무시트 주문이 조용히 "미완결"** 이 된다. 대신 **시트를 실제로 읽어 대조하는 경로에서만** 무시트 탭을 뺀다.
  - ★★ **매핑 사본 0** — 어느 칸에 무엇을 쓸지는 `mapOrderToSheetRow`(orderLedger 단일 출처)가 정한다. 옵션 칸 판정도 **`optionWriteColumns`(매퍼 파생)** — `optionColIndexes`(헤더에 '옵션' 포함)를 쓰면 `옵션금액`(=결제금액)·`비고(옵션확인)` 을 오분류해 **금액·비고가 조용히 사라진다**(막으려던 것보다 큰 손실). **옵션 blank-only**(관리자 작업지시 '텍스트'·'포토리뷰' 보존)도 시트 경로와 같은 규율.
  - ★★ **시트 쓰기 차단 4중**: ① 리뷰어 제출(`submit.routes`) ② 외부모집 수동제출(`manualOrder`) ③ **복구(reconcile)는 큐 대신 작업표 재기록**(목록에서 통째로 빼면 그 주문이 영영 복구되지 않으므로 **대체 경로를 준다**, 요약에 `sheetlessWritten`) ④ **큐 실행부 fail-closed 백스톱**(`_executeBatch` 시작 = **가드 읽기·시트 쓰기 앞**) — 이관 **전에** 쌓여 있던 큐·새 호출부·판정 실패분이 여기서 막힌다. 큐 항목은 버리지 않고 `done` + 사유.
  - ★ **제외**: written 사후검증(`writtenVerify` — 무시트 장부는 우리가 만든 것이라 대조 대상이 아니고, 재생성 타이밍에 따라 정상 주문을 '소실'로 오판해 강등 → reconcile → 구글 쓰기로 번진다) · 시트 반영 배치 스케줄러. ★ `last_sheet_write_sig` 를 남기지 않아 **시트 역동기화 탐지가 이 주문을 건드리지 않는다**.
  - ★ **완결 순서가 계약**: 작업표 기록 → **장부 재생성 성공** → `markOrderWritten` → 신원 링크. 장부 재생성이 실패하면 **완결로 찍지 않고 신원 링크도 남기지 않는다**(중간 실패에 링크를 남기면 재배정된 행이 옛 주인에게도 열리는 교차노출 — 레포가 이미 겪은 사고). 열 구성 불명(`no_headers`)도 조용히 완결시키지 않는다.
  - ★ 표 끝을 넘어 배정된 자리는 **줄을 만든다**(`source='worktable'` — `'manual'` 이면 상태 CASE 가 인정하지 않아 리뷰제출·입금 표시가 영영 안 켜진다). 작업보드 표가 비지 않게 `reviewer_name`/`phone8`/`option_text` 도 채우되 **빈 값으로 기존 값을 덮지 않는다**(`COALESCE(NULLIF(...))`).
  - ★ `rebuildLedgers` 에 **탭 단위 `pg_advisory_xact_lock`** 추가 — 주문이 몰리면 같은 탭의 장부 재생성이 겹치는데 그 함수는 `review_index` 를 **지우고 다시 넣는다**(겹치면 검색 명단이 잠깐 비는 창 = 리뷰어에겐 "내 참여가 사라짐"). 트랜잭션 스코프라 누수 불가.
- **③ Drive 폴더 1단 = 업체명(D2-a, `folderTitle.service.js`)**: `AI_REVIEW_FOLDER → {1단} → {탭명} → [리뷰]` 의 {1단}이 지금까지 **구글 스프레드시트 파일 제목**이었다 — 무시트에는 그 파일이 없다. 업체(광고주) → 캠페인 이름 → 작업 이름 순.
  - ★★ **시트 기반 탭에는 `null` 을 돌려준다** → 호출부가 **자기 기존 로직을 그대로** 탄다(이미 만들어진 수백 개 폴더와 경로가 갈라지지 않게 = 바이트 불변). 조회 실패도 `null`(이름을 지어내면 엉뚱한 폴더가 생긴다).
  - ★ 소비처 **3곳이 같은 해석기**(폴더 사전생성 `reviewFolders` · 리뷰 캡처 업로드 `diag/review-upload` · 이미지 교체요청 `reviewEdit`) — 갈리면 **같은 탭의 캡처가 폴더 두 곳에 생긴다**. 업로드 경로는 무시트면 `getSpreadsheetMeta`(404) 를 건너뛴다.
- 회귀가드 `tests/sheetlessOrderAccept.test.js`(82케이스 — 매핑 순수함수 실행 · 스텁 pool 로 서비스 실제 실행 · 라우터 스택 실검사 · 4중 차단 배선 · 소스 위생, **변이시험 18종 검출 확인**) + `tests/sheetlessOrderAcceptPg.test.js`(**진짜 PG16** 16케이스 — 업서트 컬럼≡자리표시자≡파라미터 · 이관 되돌림 금지 · `NOT EXISTS` 상관 서브쿼리가 **그 행만** 빼는지 · 폴더 이름 우선순위 · row_json 병합). ⚠ 변이시험이 약한 단언 2종을 잡아 강화했다 — **위치만 보는 단언은 `false &&` 한 줄로 무력화된다**(판정식 자체를 고정할 것), **성공 경로만 세는 단언은 실패 경로의 부작용을 못 본다**(실패 시 신원 링크 부재까지 확인).
### ★★ 탈 구글시트 W2-b — 날짜 분배를 달력으로(D3-a) + 인트라넷 일정 신호 2칸 (migration 097)
- **시트 시절의 "그날 정원"은 시트 구매일자 칸이 정했다**(063 이 그 칸을 읽어 파생). 무시트에는 그 칸이 없으므로 **같은 분배를 달력(`campaign_daily_plans`)에 옮겨 담고 그 뒤로는 달력이 진실원본**(사용자 확정 D3-a). 095 의 [📅 인원] 조절 모달이 그대로 조작 창구가 된다.
- ★★ **두 조각은 반드시 함께 나간다** — ㉮ `deriveSchedules` 에서 무시트 탭 제외 ㉯ 발행 시 달력 프리필. ㉮ 만 하면 정원이 발행폼 값으로 되돌아가고, **㉯ 만 하면 `savePlans` 가 `schedule_driven` 으로 저장을 거부해** 조절 모달이 영영 잠긴다(시트 일정 캠페인은 "시트에서 조절" 이 규칙이라 무시트에는 조절 창구가 없어진다).
- **① 시트 일정 제외**(`campaignSchedule.deriveSchedules`): 캐시 조회 뒤 남은 후보를 `tab_configs` 로 훑어 `sheetless=TRUE` 인 탭을 **`null`(미적용)로 확정하고 캐시에 넣는다** — 그 뒤 RAW 미러 조회 자체를 건너뛴다(무시트 탭은 `raw_sheet_rows` 가 우리가 만든 장부라 읽어도 되지만, 읽으면 달력과 두 진실원본이 생긴다). ★ **조회 실패는 fail-open**(종전 경로 = 시트 일정 판정 계속) — 이 게이트가 죽었다고 정원 계산이 멈추면 안 된다.
- **② 발행 시 달력 프리필**(`sheetlessDailyPlan.service.prefillFromWorktable`, `POST /api/campaign/admin/create` 말미): 작업표 `row_json` 의 날짜 칸을 세어 `campaign_daily_plans` 로 옮긴다. ★★ **날짜 컬럼 찾기·연도 추론은 기존 단일 출처 그대로**(`campaignSchedule.findDateColumnIndex` + `utils/koreanDate.parseDateColumn`) — 여기서 규칙을 다시 만들면 "작업표는 8/7 인데 달력은 8/8" 로 갈린다. ★ **`ON CONFLICT DO NOTHING`**(관리자가 손으로 조절해 둔 값을 공고 수정 한 번에 되돌리지 않는다 — 095 "조절 = 계획 변경" 규율) · ★ **지난 날짜 미삽입**(`savePlans` 가 과거 날짜를 거부하므로 화면에서 지울 수도 없는 행이 쌓인다) · 상한 `MAX_PLAN_DAYS`(400) · ★ **fail-soft**(실패해도 발행은 성공, 사유를 `planPrefill` 로 응답에 실어 화면이 말한다).
- ★ **참여형 + 무시트 + 연결 탭이 있을 때만** 동작 — 시트 기반 공고는 판정조차 하지 않으므로 **발행 응답이 한 바이트도 안 바뀐다**(`planPrefill` 필드 자체가 없다).
- **③ 인트라넷 일정 신호 2칸**(migration 097 `work_orders.skip_weekends BOOLEAN` · `holidays TEXT`): 인트라넷 리뷰오더 폼에 **[주말 제외](기본 켬)** 체크와 **[휴무일]** 입력칸을 두고 오더에 실어 보낸다 → 리뷰웹 작업표 계획(`worktablePlan.buildWorktablePlan`)이 **프리필**로 쓴다(담당자가 미리보기에서 매번 다시 지정하던 "공백 1건" 해소).
  - ★★ **"미전송(NULL)"과 "끔(false)"을 구분한다** — `skip_weekends` 가 NULL 이면 종전 기본값(주말 제외)이 그대로 적용되고, `false` 일 때만 주말을 포함한다. 구분하지 않으면 **인트라넷 배포가 늦은 동안 주말 제외가 조용히 꺼진다**. 미리보기에서 사람이 준 값(`o.skipWeekends`)은 언제나 오더 값을 이긴다.
  - ★ **휴무일 검증은 서버가 최종**(`_holidaysJson` — `YYYY-MM-DD` 형식 밖 값은 조용히 버리고 중복 제거·정렬, `JSON.parse` 실패도 빈 배열 = 날짜 분배가 통째로 깨지는 것보다 낫다). 진행 기간 밖 날짜는 종전대로 `holiday_outside` **경고만**(오탐으로 정상 생성을 막지 않는다).
  - ★ **양방향 가산적 · 배포 순서 무관** — 리뷰웹만 먼저면 두 칸이 NULL 로 남아 종전 동작, 인트라넷만 먼저면 리뷰웹이 필드를 버린다(088 계약건과 같은 성질).
  - ★ `REQUIRED_SCHEMA` 에 `tab_configs.sheetless`·`work_orders.skip_weekends`·`holidays` 등록(컬럼 없으면 접수·계획 경로가 42703 으로 죽는 것을 부팅에서 막는다).
- 회귀가드 `tests/sheetlessDailyPlan.test.js`(45케이스 — 날짜 판독·프리필을 스텁 pool 로 **실제 실행**(덮어쓰기 금지·과거 날짜 제외·상한) + `deriveSchedules` 제외 실행 + **INSERT 컬럼 수 ≡ 자리표시자 수 ≡ 파라미터 수 계수** + 신호 우선순위(NULL↔false) 실행 + 배선).
### ★★ 탈 구글시트 W3-a — 장부 재생성이 시스템 기록을 지우던 문제
- **W3("뒤쪽 단계 잇기") 착수해 보니 핵심은 데이터 유실 버그였다**(진짜 PG16 으로 재현 확인): 무시트 탭의 `review_index` 는 `rebuildLedgers` 가 **지우고 작업표에서 다시 만드는데**, 시스템이 그 표에 **직접 UPDATE** 하는 값들이 재생성 목록에 없어 **주문 한 건만 더 들어와도 전부 증발**했다 — 입금 완료(`is_submitted2='PAID'`) · 리뷰 제출(`is_submitted`) · **대표 리뷰 이미지(`review_file_*` 5칸)**. 시트 기반 탭은 같은 값이 **시트 칸에도 써져** 살아남지만, 무시트는 시트 쓰기가 막혀 있어(W2-a 4중 차단) **어디에도 안 남는다**.
- ★★ **값의 성격에 따라 두 갈래 — 합치지 말 것**:
  - ㉮ **시트에도 칸이 있는 값(제출·입금) = 작업표 칸에 쓴다**(진실원본 일원화, `sheetlessStatus.service.markStatusCell` 단일 경로). **보존(스냅샷 복원)으로 처리하면 안 된다** — 작업표에서 사람이 그 칸을 지워도 장부만 남아 갈라지고, "작업표가 진실원본"이라는 W1 의 전제가 깨진다.
  - ㉯ **시트에 칸이 없는 시스템 전용 값(대표 이미지) = 재생성 시 보존** — DELETE 전 스냅샷 → 재INSERT 후 복원(앵커 = `row_index`), 결과를 `filesKept`/`filesSeen` 으로 **보고**(조용한 소실 감지).
- ★★ **어느 칸이 리뷰제출·입금 열인가 = 파서가 감지해 둔 `review_index.submit_col/submit_col2`**(판정 사본 0). 그 값은 `parseTabRows` 가 그 탭에서 실제로 고른 헤더명이라, 거기에 쓰면 다음 재생성 때 **같은 파서가 그대로 읽어 낸다**(왕복 일치가 구조적으로 보장). 표기도 시트 경로와 동일(`'O'` / 이체 시각 스탬프).
- ★ **`handled` 신호로 분기**: 시트 기반 탭이면 `{handled:false}` → 호출부는 자기 기존 경로(시트 쓰기)를 그대로 탄다(**기존 동작 무변경**). ★ **판정 실패도 `handled:false`**(fail-open) — `handled:true` 로 접으면 **시트 기반 탭의 입금 기록이 조용히 사라진다**.
- **배선 3곳**: ① `submit.routes` 완료 분기(fail-soft — 제출은 막지 않는다) ② `payment.routes` 무시트면 기록 후 **시트 쓰기·`deposit_mark` 큐로 내려가지 않는다**(시트 경로·큐 폴백은 그대로 = 무회귀) ③ `trackB.executeWriteback` → `_writebackSheetless`.
  - ★★ ③ 을 빼면 **영원히 수렴하지 않는다** — 행마다 `readSheet` 가 404 로 떨어져 `deferred` 만 쌓이고 화면엔 "반영 대기"로만 보인다. ★ 앵커 3종(`order`·`manual`·`identity`)을 모두 해석하되 **모호한 identity(동명이인)는 쓰지 않는다**(fail-closed — 엉뚱한 줄에 입금 표시가 남는다). ★ **해제(false)는 `held`** — blank-only 규율상 시트 경로도 값을 지우지 못하므로 무시트에서만 지우기를 열면 두 경로의 의미가 갈린다.
- ★ **운영 메모**: 작업표의 입금 칸 이름은 **파서가 아는 이름**이어야 한다(`입금`·`입금완료`·`입금확인`·`입금여부`·`페이백` — `columnResolver` PAYMENT_EXACT/PARTIAL). 표준 열 프리셋의 `입금` 이 그렇다. `입금일자` 처럼 다른 이름이면 `no_status_column` 사유로 **정직하게 거부**한다(조용한 실패 아님).
- 회귀가드 `tests/sheetlessStatusPersist.test.js`(45케이스 — 스텁 실행·배선·무회귀) + `tests/sheetlessStatusPersistPg.test.js`(**진짜 PG16 16케이스** — 사고 재현 → 보존 확인 → 작업표 왕복 → 멱등 → 병합이 다른 칸 미삭제 → 시트 기반 무접촉), **변이시험 10종 검출 확인**. ⚠ 변이시험이 약한 단언 2종을 잡아 강화했다 — **보고 카운터는 값까지 단언할 것**(`+= 0` 으로 바꿔도 통과했다), **실행으로 못 만드는 갈래(모듈 로드 실패 catch)는 코드 모양을 고정할 것**.
### ★ 탈 구글시트 W3-b(A) — 무시트 표시(배지) + 죽은 시트링크 제거 (F4, 스키마 변경 0)
- **문제**: W1~W3-a 로 무시트 작업이 실제로 돌아가는데 **화면 어디에도 "이 작업은 구글시트를 안 쓴다"는 표시가 없었다** — 담당자는 매번 시트를 찾으러 가고, 링크는 가상 시트ID(`wt_…`)로 조립돼 **죽은 구글 링크**가 열렸다.
- ★★ **판정은 서버가 실어 준 플래그 하나(`sheetless`) — ID 모양(`wt_`)으로 추측하지 않는다**: **이관된 기존 작업(W5)은 진짜 시트 ID 를 그대로 쓰면서 무시트가 되므로** 모양 판정은 그 케이스를 통째로 놓친다(회귀가드가 그 시나리오를 실행으로 고정). 재료는 **이미 있던 `tab_configs` 조인**에 컬럼 하나만 얹어 **쿼리 순증 0** — 소비처 3곳 = `listActiveTabs`(홈 목록·작업보드) · `ownedTabsForAdvertiser`(업체관리 표) · `auditSheetSync`(반영 점검).
- ★ **광고주에게는 안 나간다** — `advertiserWorkSummary` 가 **화이트리스트 재구성**이라 자동 격리(가드가 스프레드(`...t`) 전환까지 금지해 그 방어가 조용히 풀리는 것을 막는다).
- ★★ **배지 렌더러는 한 벌**(`_nsBadge`, workdesk.html) — 홈 작업목록 · 작업보드 상단 · 업체관리 연결탭 표 공용(사본을 두면 한쪽만 안 바뀐다). ★ **`sheetless === true` 일 때만** 그린다 — **필드 자체가 없으면(구버전 백엔드) 아무것도 안 그린다**(모르는 것을 "시트를 쓴다"로 단정하지 않는다). ★ 작업보드 상단은 **광고주 화면 제외**(외부 사용자에게 내부 이관 상태를 새로 노출하지 않는다). CSS 는 `ns-` 접두 신설이고 **홈 CSS 블록 밖**에 둔다(홈 스코프 가드의 의미를 흐리지 않게).
- **죽은 링크 제거 2곳**: ① 반영 점검(`_gidOut`) — 무시트면 **`tabUrl: null` + `sheetless`** 로 사유를 말한다(빈 링크 > 죽은 링크 — 그 함수가 이미 "엉뚱한 탭보다 빈 링크"로 세워 둔 규율과 같다), 화면은 링크 대신 `무시트` 배지. ② 모집공고 카드 `📄 시트` → **`🗒 무시트` 비활성 + 사유**. ★ 카드는 서버 플래그를 못 받는 응답이라(공개/관리자 목록 `SELECT *`) **가상 시트ID 접두 최소 사본**(`_VIRTUAL_SHEET_PREFIX`)을 쓰고 **회귀가드가 서버 `sheetlessAccept.VIRTUAL_SHEET_PREFIX` 와의 일치를 고정**한다(workManager 사본 규율).
- ★ **업체 링크 일원화는 이미 충족돼 있었다** — `workdesk.html` 에 `docs.google.com` 직링크가 **0개**(광고주·내부 공통, 폴더 바로가기는 Drive 링크라 별개). 회귀가드가 그 0을 고정해 되살아나는 것을 막는다.
- 회귀가드 `tests/sheetlessBadge.test.js`(27케이스 — 스텁 pool 로 서비스·audit 실제 실행(이관 작업 시나리오 포함) + 배지 렌더러 vm 실행(true/false/필드부재/느슨한참) + 접두 사본 일치 + 광고주 미노출 + 링크 0, **변이시험 10종 검출 확인**).
### ★★ 탈 구글시트 W4(C) — 전환 관리 화면 (무시트 표식을 켜는 유일한 창구)
- **문제**: 표식(096)은 W1 에 만들었는데 **켜는 화면이 없어 이관을 시작할 방법 자체가 없었다**. 사용자 확정 3건 = ① **점검표 + 켜기만**(백필·미러 같은 준비는 이미 반영 점검 화면에 있다 — 한 버튼으로 묶으면 중간 실패 시 어디까지 됐는지 사람이 알 수 없다) ② **리뷰웹시스템[3버전] 새 상단 탭 [탈시트 전환]** ③ **adminOrMaster**(실무 담당자가 직접 이관하고, 실수 방어는 점검표가 맡는다 — 타이핑 확정은 2026-08-07 제거).
- ★★ **점검표 5항목은 fail-closed — `unknown` 도 통과가 아니다**: ① 시트 준비 줄 ≤ 시스템 표 ② 시트↔시스템 진짜 불일치 0 ③ 미반영 주문 인계 가능 ④ 시트 사본 최신 ⑤ 열 구성 인식. 이관은 "그 시트를 더 이상 읽지 않는다"는 선언이라 잘못 켜면 **그 사이 시트 편집이 아무도 모르게 흘러간다** — 확신이 없으면 열지 않는다. `blocking`(막힌 항목 키)과 **다음 행동(hint)** 을 함께 돌려준다.
- ★★ **③ 미반영 주문 = 시트가 아니라 작업표로 인계한다 (사용자 확정 2026-08-07 "B로 진행")**: 종전엔 "반영 대기 0"을 요구해 실제 운영에서 이관이 막혔다. 그런데 **곧 안 읽을 시트에 다시 쓰는 것은 낭비**이고, 더 결정적으로 **`stuck_manual` 은 reconcile 이 제외해 시트 재기록으로 영영 안 풀린다** → 그런 주문이 하나라도 있는 작업은 "시트에 다 쓰고 나서 이관"이 **구조적으로 불가능**했다.
  - ★★ **실행부는 `sheetlessOrder.writeOrderToWorktable` 한 벌**(사본 0) — 이관 후 신규 주문이 타는 바로 그 경로다(매핑 `mapOrderToSheetRow`·옵션 blank-only·장부 재생성·완결 표시·신원 링크가 전부 거기 있다). 원장 행 → orderData 변환도 **`orderLedger._osRowToOrderData` 재사용**(큐 실행부와 같은 함수).
  - ★★ **표식을 켠 뒤에 인계한다** — `writeOrderToWorktable` 이 부르는 장부 재생성이 시트 기반 탭을 `not_sheetless` 로 막는다(그 게이트를 풀지 않는다). 회귀가드가 순서 계약을 실행으로 고정.
  - ★★ **통과시키는 것은 완화가 아니라 사실 반영** — 막으려던 위험("시트에도 표에도 안 남는다")이 인계로 사라졌다. ★ 그러나 **배정된 줄이 없는 주문은 옮길 자리가 없어 진짜로 사라지므로 계속 잠근다**(fail-closed 는 그 갈래에 살아 있다). 조회 실패도 `unknown` = 잠금.
  - ★ **절대 throw 하지 않는다** — 인계 실패·조회 실패도 이관은 유지하고 사유를 응답에 싣는다(되돌리면 "표식은 껐는데 시트 안내문은 붙은" 어중간한 상태가 된다). 남은 건은 ㉮ 2분 reconcile 무시트 분기 ㉯ 재실행(멱등)이 메운다 → **상한 초과 시 `stuck_manual` 을 먼저** 처리한다(그것만 ㉮로 안 풀린다).
  - ★ 화면은 **확인창이 처리 방식을 먼저 말하고**(시트가 아니라 시스템 표로) **결과가 옮긴 건수·실패·자리없음·절단을 고지**한다(조용한 누락 금지). 킬스위치 `SHEETLESS_CUTOVER_HANDOFF=0` · 상한 `SHEETLESS_CUTOVER_HANDOFF_CAP`(200).
  - ⚠ **변이시험이 잡은 약한 단언**: 실패 경로가 **예외**와 **`{ok:false}` 반환** 둘인데 예외만 검사하면 `out.failed++` 를 `+= 0` 으로 바꿔도 통과한다 — 둘 다 케이스를 두고 **건수 값까지** 단언할 것.
- ★★ **판정 재료 사본 0**: 시트 우위 = `sheetSlotSync.readPreparedRows` · 대조 = `trackB.parityReport` · 열 구성 = `sheetlessLedger.rebuildLedgers` · 연도 필터 = `utils/tabActivity`. 헤더 탐지·날짜 파서 사본을 만들지 않는다(회귀가드가 부재를 고정).
- ★★ **`rebuildLedgers` 에 `preflight` 추가 — `dryRun` 과 함께일 때만 동작**: 이관 **전에** 열 구성을 확인해야 하는데 그 함수의 `not_sheetless` 게이트가 막았다. 게이트가 막으려는 것은 **시트 값 덮어쓰기**인데 dry-run 은 한 줄도 쓰지 않으므로 이 조합만 연다. ★ `preflight` 단독(=쓰기)은 종전대로 거부(완화 금지 — 회귀가드가 조건식 자체를 고정).
- ★★ **이관 게이트는 점검표 하나** — **작업명 타이핑 확정은 제거**(사용자 확정 2026-08-07 "그냥 이 단계 없이 이관하도록"). 오클릭 방어는 두 겹으로 남는다: ① 화면 확인창이 **그 작업 이름을 문장에 넣어** 보여준다(옆 줄을 눌렀다면 거기서 알아챈다) ② 점검표 fail-closed 라 확신 없는 작업은 [이관] 버튼 자체가 안 나온다. 게다가 **되돌리기([재연결])는 점검표 없이 언제든** 가능하다(파괴적 조작이 아니다 — 관측 뷰 SoT 플립의 탭명 타이핑 마찰과 성격이 다르다). ★ 되살릴 거면 **화면·서버·가드 세 곳을 같이** — 서버만 켜면 화면이 값을 못 보내 **전면 잠금**, 화면만 켜면 죽은 입력칸이 된다(회귀가드가 세 곳 모두에서 흔적 0을 고정). 점검 실패 시 **쓰기 쿼리 0** 은 그대로. `force` 는 **라우트에서 `=== true` 일 때만**(문자열 `'true'`·`1` 로 안 열린다) 넘기고 `UNVERIFIED` 로 로그·응답에 표시.
- ★ **부수효과 2종은 이관을 되돌리지 않는다**: 장부 재생성(`rebuildLedgers`) · 시트 안내문(`applySheetNotice`, **force:true** — 기존 "행 삭제 금지" 공지를 대신해야 직원이 그 시트에서 계속 작업하지 않는다). 둘 다 실패해도 이관은 유지하고 **사유를 응답에 실어 화면이 말한다**(조용한 실패 금지). ★ 안내 문구는 `validateNoticeText` 를 통과해야 한다(헤더 키워드 2개 이상이면 그 탭 파싱이 통째로 깨진다 — 회귀가드가 실행으로 확인).
- ★ **되돌리기(재연결)** 는 점검표를 요구하지 않는다(비상구). ★ `sheetless_at`/`sheetless_by` 는 **지우지 않는다** — 이력 테이블이 없어 그것이 "언제 누가 이관했나"의 유일한 기록이다. ★ 0행은 **"되돌렸습니다"로 꾸미지 않는다**.
- ★ **이미 이관된 작업은 연도 필터로 숨기지 않는다** — "이관됨 N" 이 조용히 줄면 현황이 거짓말이 된다. 남은 작업만 연도 하한(기본 2026~)을 탄다.
- ★★★ **준비 자리 부족(①)은 그 자리에서 채운다 — 두 도구가 헷갈려 실제로 사고가 났다(운영 실측 2026-08-07)**: 담당자가 17건을 전부 "반영처리" 했는데 **1건만** 해결됐다. `sheet-sync-audit.html` 에는 비슷한 이름의 처리가 **둘**(위쪽 표 [반영]=`repair`(미러→빌드→투영) / 아래쪽 ➕ 시트 우위 점검 [자리 추가]=`slot-backfill`)인데 안내가 **화면 이름만** 말해서 전부 위쪽을 눌렀다. ★★ **repair 로는 구조적으로 못 채운다** — 부족분은 구매일자만 적힌 **이름 없는 준비 자리**라 파서가 버려(`columnResolver` `if (!name) return null`) 애초에 `review_index` 에 없다(해결된 1건은 review_index 에 109명이 있고 작업표만 0이던 다른 케이스였다). → 점검표 ① 의 `fix` = **`'backfill'`**(≠`'audit'`), 막힌 항목 바로 아래 [➕ 표에 자리 채우기]가 `slot-backfill` 을 실행하고 **자동 재점검**한다(고를 여지 없이 맞는 도구 하나만). ★ 몇 줄이 생기는지 점검 문장으로 보여주고 confirm(조용한 대량 생성 금지) · 시트 무접촉 · 중복 실행 안전.
- ★★ **막힌 항목은 그 자리에서 처리한다** — 종전 안내는 `시트 데이터 반영 점검 화면에서…` 라고만 했는데 **그 화면은 nav 어디에도 없어**(링크 0곳) 주소를 직접 쳐야 열렸다(사용자 신고 2026-08-07). 무엇을 누를지는 **서버가 `check.fix` 로 말하고**(`refresh`=시트 새로고침 · `audit`=반영 점검 열기) 화면은 그대로 그린다. ★ [↻ 시트 새로고침]은 **기존 반영 도구 `/sheet-sync/repair` 재사용**(신규 엔드포인트 0)이고 **끝나면 자동으로 다시 점검**한다(새로고침만 하고 끝내면 사람이 또 눌러야 한다). ★ 오래 걸리는 조치는 진행 문구를 보여준다(모르면 다시 누른다). ★ 헤더에도 [↗ 반영 점검] — 그 화면으로 가는 길이 화면 안에 있어야 한다.
- ★ **화면은 서버 판정을 그대로 그린다**(프론트 재판정 0 — 화면은 초록인데 서버가 거부하는 상태가 구조적으로 불가능). onclick 은 **인덱스만**. 자리표시자를 깐 함수는 **어떤 예외에도 사유 + [다시 시도]로 끝낸다**. 헤더 캡 = 본문 캡(1120px 같은 값). CSS 는 `co-` 접두 신설.
- **쓰기 표면 = `tab_configs.sheetless` 한 칸뿐**(운영 테이블 무접촉, 회귀가드가 고정). 라우트 4종 `/api/trackb/sheetless/list·checklist·cutover·reconnect`(42P01 = `not_ready`).
- 회귀가드 `tests/sheetlessCutover.test.js`(52케이스 — 라우터 스택 실검사 + 스텁 pool 로 점검표·이관·되돌리기 **실제 실행** + preflight 게이트 + 화면 배선 + **렌더 블록 vm 실행으로 무한로딩 차단**, **변이시험 17종 검출 확인**) + 실 http 오리진 브라우저 16시나리오. ⚠ 변이시험이 약한 단언 2종을 잡아 강화했다 — **파일 전체를 보는 단언은 다른 라우트의 같은 표현이 대신 통과시킨다**(라우트 본문으로 스코프할 것, 경계는 `});` 가 아니라 다음 `router.`), **"호출문이 있나"는 정의를 지워도 통과한다**(실행해서 볼 것).
### ★★ 탈 구글시트 W4-D — 점검표 ④⑤ 가 제 일을 못 하고 있었다 (실측 2026-08-07)
- **W4(C) 화면을 실제 운영 데이터에 대 보니**: 등록 작업 **86건이 전부 ④에서 잠겼고**, ⑤ 는 `열 2개 · 표 500줄 → 검색 명단 0명` 인 탭을 **초록으로 통과**시켰다. fail-closed 라 해놓고 한쪽은 전부 막고 다른 쪽은 아무것도 안 막은 상태.
- ★★★ **④ 비교 대상은 탭이 아니라 시트다 (운영 실측 오탐 2026-08-07)**: 미러는 **내용이 바뀐 탭만** `_upsertTabMeta` 하고 checksum 같은 탭은 통째로 건너뛴다 → 한 시트에서 다른 탭만 바뀌면 이 탭의 `sheet_modified_at`·`mirrored_at` 은 옛날에 머물러 **그 시트의 조용한 탭 전부가 영구 잠금**이 된다(실측: 퓨비아 두 탭 = 미러 갱신 552분 전 · 시트 수정 465분 전 → "안 읽어 왔습니다". 미러는 5분마다 도는데 465분간 못 읽었을 리가 없다 = 그때 읽고 같아서 건너뛴 것). → **미러가 자기 스킵 판정에 쓰는 값과 같은 것**(`MAX(sheet_modified_at)` per sheet = `_mirrorOneSheet` 의 `lastKnown`)을 본다. 시트 단위 값보다 새로운 수정이면 **여전히 잠근다**(완화 아님). ⚠ 잔여: 시트를 **열기만 하고 안 고치면** 파일 수정시각만 올라가고 어느 탭도 안 바뀌어 계속 잠긴다 — [↻ 시트 새로고침](force 미러)이 탈출구이고, 그 시트는 미러가 매 사이클 헛되이 전체 재읽기를 반복한다(별건).
- ★★ **④ 판정 재설계 — 경과시간이 아니라 "그 뒤로 바뀌었나"**: `raw_sheet_tabs.mirrored_at` 은 **"확인한 시각"이 아니라 "내용이 바뀌어 다시 읽은 시각"** 이다(`rawMirror._upsertTabMeta` — 시트 단위 `remote <= lastKnown` 조기반환 · 탭 단위 `checksum` 동일 시 `continue`). 즉 **조용한 작업일수록 값이 낡아져 이관하기 가장 좋은 작업(끝나서 조용한 작업)이 영구히 잠긴다**. → **Drive 최종수정 시각을 그 자리에서 1콜 조회**해 우리가 읽어 둔 시점(`sheet_modified_at`, 없으면 `mirrored_at`)과 대조한다. ★ 사람이 [점검]을 누를 때만·탭당 1콜·**drive lane**(주문 쓰기 슬롯 무접촉) — [연도 확인] probe 와 같은 규율. ★ 조회 실패·미러 기록 없음은 `unknown` = **잠금**(모르면 열지 않는다).
- ★★ **⑤ 판정 재설계 — 예외 유무가 아니라 인원 대조**: 종전엔 `rebuildLedgers` 가 throw 하지 않으면 통과였다. 지금은 **지금 `review_index` 명단 수 vs 이관 후 명단 수**를 대조해 **줄어들면 잠근다**(그 차이가 곧 리뷰어 검색에서 사라지는 인원). 지금 수를 못 세면 `unknown`.
- ★★ **⑤ 의 근본 원인 = `rebuildLedgers` 가 `raw_sheet_tabs.headers` 를 읽고 있었다** — 그 칸은 **시트 A1 행**(대개 캠페인 정보 한두 칸)이고 진짜 열 이름 줄은 `detected_headers` 다. → `detected_headers || headers` 로 수정. 무시트 탭은 재생성 시 두 칸에 **같은 값**을 쓰므로 그쪽 동작은 불변.
- ★ **스텁으로는 못 잡는 자리 2곳**(변이시험이 실측): ㉮ 헤더 출처 드리프트는 **`sheetlessCutover` 가드가 `rebuildLedgers` 를 스텁하므로 보이지 않는다** → `sheetlessLedger` 가드에 **두 칸을 다른 값으로 준 케이스**를 두어 실행으로 고정 ㉯ 스텁은 SQL 을 해석하지 않아 **`MAX(seq)` ↔ `COUNT(*)` 를 구별 못 한다** → 쿼리 모양 자체를 고정(번호 재사용 = `(sheet,tab,seq)` 키 충돌).
- **레거시 공고 참여 행추가 무시트 분기(W5 전제)** — `participation_mode=false` 공고의 참여 확정은 주문 원장·큐를 **거치지 않고** `campaign.routes._addApplicationToSheet` 가 `appendSheet` 로 **시트에 직접** 쓴다. 이관된 작업에서는 그 시트를 아무도 읽지 않으므로 그대로 두면 **참여자가 명단에서 통째로 사라진다**(시트에는 남지만 시스템 표·검색 어디에도 안 들어온다).
  - `sheetlessApplicant.service.addApplicantRow` 가 무시트 탭이면 작업표에 넣고 장부를 재생성한다. **시트 기반이면 `{handled:false}`** → 호출부가 자기 기존 경로를 그대로 탄다(**무회귀**). ★ **판정 실패도 `handled:false`**(fail-open — 시트 기반이 절대 다수다).
  - ★★ **무시트인데 실패하면 `throw`** — 조용히 `false` 를 돌려주면 호출부가 시트에 쓰고 그 줄은 아무도 읽지 않는 곳에 남는다(= 참여자 실종). 실패는 드러나야 한다.
  - ★ **작업표 INSERT 는 `participants.createSlotsFromSheetRows` 한 곳**(쓰기 소유자 규율) · `seq = MAX(seq)+1` **삭제된 줄도 세어 재사용 금지** · gid 는 **서버가 `tab_configs` 에서 다시 구한다**(클라 값 불신) · 열 이름은 **`detected_headers` 우선**.
  - ★★ **열 판정·헤더 키워드 단일 출처 = `utils/applicantColumns`** — 시트 append 경로와 작업표 경로가 같은 표를 쓴다(사본을 두면 "시트 작업은 연락처가 들어가는데 이관된 작업은 빈칸"으로 갈린다). ⚠ `campaign.routes.js` 는 **의도된 NUL**(복합키 구분자)이 있어 grep 가드를 못 건다 — 그래서 판정을 이 util 로 빼서 가드가 붙을 자리를 만들었다(가드는 라우트를 읽을 때 NUL 을 이스케이프 표기(`String.fromCharCode(0)`)로 제거하고 본다 — 가드 파일에 NUL 을 글자 그대로 넣으면 그 파일까지 바이너리가 된다).
- ★★ **준비 자리 일괄 점검**(`sweepPreparedRows` · `GET /api/trackb/sheetless/slot-sweep` · 헤더 [🔎 준비 자리 일괄 점검]): 점검표 ①과 **같은 판정을 남은 작업 전체에** 한 번에 돌려 **시트가 더 많은 작업만** 추린다 — 85건을 하나씩 누르던 것이 착수 자체를 막고 있었다. ★ **읽기 전용·RAW 미러만·시트 API 0**(85탭을 훑어도 구글 쿼터 무영향) ★ 판정 재료는 `sheetSlotSync.readPreparedRows` 사본 0 ★ **판정 불가(gid 없음·미러 없음·구매일자 칸 없음)를 "이상 없음"으로 꾸미지 않는다** — 따로 세어 사유와 함께 돌려준다. ★★ **왜 준비 자리만 보나** — 제출 데이터(구매양식·리뷰)는 전부 DB-first 라 시트를 볼 이유가 없다. 시트에만 있고 DB엔 없는 것은 **구매일자만 적힌 아직 안 팔린 자리**뿐이다(파서가 이름 없는 행을 버려 검색 명단·작업표 어디에도 없다).
- ★★★ **④ 는 잠그지 않는다 — 대신 이관이 시트를 마지막으로 한 번 더 읽는다 (사용자 확정 2026-08-07)**: 지시 = *"구매일자로 볼 때 총인원수가 시스템표에서도 만족하면 그냥 이관해. 시트가 x분 전에 수정되었다는 건 리뷰어가 리뷰를 제출한 것이고, 서버에 기록이 남아있으니 시스템표로 이관하고 반영되어도 늦지 않다."* 실제로 ④ 하나 때문에 86건이 전부 잠겨 착수가 불가능했다.
  - ★★ **통과시키는 것은 완화가 아니라 사실 반영**(③ 미반영 주문 인계와 같은 논리): 주문·리뷰는 전부 **DB-first** 라 "시트가 방금 바뀜"의 실체는 대개 이미 서버에 있는 제출이다. 시트에만 있고 서버엔 없는 것은 ㉮ **구매일자만 적힌 준비 자리**(①이 계속 잠근다 — 반영으로는 못 채운다, 파서가 이름 없는 행을 버린다) ㉯ **직원이 손으로 채운 줄** 둘뿐인데, ㉯ 는 **이관 직전 마지막 반영**이 담는다.
  - ★★ **마지막 반영 `_reflectSheetOnce` = 기존 반영 도구 한 벌**(`sheetSyncAudit.repairSheetSync` = 미러(force) → 인덱스 빌드 → **투영**, 사본 0). ★ **투영까지 가야 한다** — 뒤이은 장부 재생성은 *작업표*에서 장부를 다시 만들므로, `review_index` 까지만 담고 `campaign_participants` 로 넘기지 않으면 그 인원이 **그 자리에서 다시 지워진다**. ★★ **순서가 계약: 표식(`sheetless`)을 켜기 전**(켠 뒤에는 `sheetlessScope` 가 미러·빌드에서 그 탭을 빼 아무것도 못 읽는다) — 회귀가드가 호출 순서를 **실행으로** 고정. ★ 실패해도 이관은 유지하고 사유를 응답(`reflect`)에 실어 화면이 조치([재연결] → [↻ 시트 새로고침] → 재이관)까지 말한다. 킬스위치 `SHEETLESS_CUTOVER_REFLECT=0`.
  - ★★ **경고 전용(advisory)은 ④ 하나뿐** — `blocking = state!=='pass' && !advisory`. ①(준비 자리)·②(대조)·③(자리 없는 주문)·⑤(명단 감소)는 **이관으로 복구되지 않으므로** 계속 잠근다(회귀가드가 "advisory 는 mirror 하나"를 실행으로 고정 — 여기에 항목을 더하면 이관이 조용히 헐거워진다).
  - ★ 화면은 그 항목을 **✕ 가 아니라 ⚠** 로 그린다(같은 ✕ 면 "빨간 게 있는데 왜 이관 버튼이 살아 있지?"가 된다) + [이관] 옆에 "⚠ 항목은 막지 않습니다 · 끊기 직전에 시트를 마지막으로 한 번 더 읽어 반영합니다". ★ ⑤ 막힘에도 `fix:'refresh'` 를 붙였다 — 가장 흔한 원인(인덱스엔 있는데 아직 투영 안 됨)이 그 버튼 한 번으로 풀린다.
  - ⚠ 이관이 시트를 읽는 만큼(20초쯤) 오래 걸리므로 화면이 **진행 문구**를 띄운다(모르면 다시 누른다).
- 회귀가드 `tests/sheetlessCutover.test.js`(141케이스 — 점검표 렌더를 vm 실행해 [이관] 버튼 생존·입력칸 부재·**advisory ⚠ 렌더** 고정 + **스텁으로 인계·마지막 반영 실제 실행**(호출 형태·**반영 → 표식 순서 계약**·실패 2경로·킬스위치·조회 실패)) · `tests/sheetlessApplicant.test.js`(37케이스 신설) · `tests/sheetlessLedger.test.js`(51케이스), **변이시험 13종 검출 확인**.
### ★★ 남은 14건 대조로 드러난 판정 결함 2종 (운영 실측 2026-08-07 · 72건 이관 후)
- **방법**: 구글시트 사본(RAW 미러)과 작업보드 표를 **줄 번호끼리**(시트 행번호 ↔ `campaign_participants.seq`) 맞춰 2,300여 줄을 한 칸씩 대조 → **14건 중 12건은 값이 한 칸도 다르지 않았다**(막힌 이유가 데이터가 아니라 판정 방식).
- ★★★ **① parity 는 한 번호에 여러 줄인 사람을 못 다뤘다 — 줄 번호로 짝을 맞춘다**(`trackB.classifyParity` + `_pairByRow`): 종전엔 phone8 로 묶은 뒤 **각 목록의 첫 줄(`arr[0]` vs `bl[0]`)** 만 비교했는데, **가족 공용 번호·차수 재참여**로 한 번호에 여러 줄인 작업에서 A(검색 명단)와 B(작업보드 표)의 **줄 순서가 달라 서로 다른 줄끼리** 대조돼 "설명되지 않는 차이"가 났다(9개 작업 27건 전부 허위 — 위프 800건·장수산업 900건 등).
  - ★ **짝짓기 키는 여전히 phone8**(행번호 짝짓기 금지 원칙 불변) — 줄 번호는 **그 그룹 안에서만** 쓴다. 그래서 `A_only`/`B_only`(멤버십) 판정은 종전과 완전히 같다(회귀가드가 "줄 번호가 같아도 번호가 다르면 A_only+B_only" 를 고정).
  - ★ **줄 번호로 못 맞춘 줄은 종전대로 순서 비교**(폴백) — 모른다고 통과시키지 않는다. **한 쌍이라도 어긋나면 real**(fail-closed 방향 불변) + `realSample.row` 로 **어느 줄인지** 말한다.
  - ★ 재료는 A `review_index.row_index` · B `campaign_participants.seq`(투영이 시트 행번호를 그대로 복제 — 두 좌표가 같다는 기존 계약).
- ★★ **② `구매날짜` 표기를 날짜 칸으로 못 봤다**(`campaignSchedule.DATE_HEADER_KEYWORDS`): 키워드가 `구매일·시작일·주문일·배정일·진행일` 뿐이라 **`구매날짜`(구매+날짜)** 가 안 걸렸다 — 실측 105탭 중 **14탭**. 그 탭들은 이관 점검 ①이 `no_date_column`(모름=잠금)으로 막히고 **시트 일정 자동 인식(063)도 배포 이래 한 번도 걸리지 않았다**.
  - ★★ **`날짜`·`일자` 로 넓히지 말 것** — `findDateColumnIndex` 는 **가장 앞선 매칭**을 쓰므로 `입금일자`·`리뷰날짜` 를 구매일로 오인해 **그날 정원이 통째로 틀어진다**(회귀가드가 그 오인 부재를 고정).
  - ⚠ 이 수정으로 그 14탭에 **063 시트 일정이 새로 적용**된다 — 배포 시점 활성 공고 6건의 연결 탭은 전부 `구매일자` 라 정원 변동 없음을 실측 확인. 이상 시 킬스위치 `CAMPAIGN_SHEET_SCHEDULE=0`.
- **남은 진짜 차이 2건**(문서화): 유일기획_2026(로켓) **1줄**(시트 45행이 명단·표에 없음 — [↻ 시트 새로고침]으로 해소) / **쿠팡(26년) 155줄**(시트 205명 = 1~7차인데 검색 명단·표엔 5·6차 50명(시트 152~201행)만 활성. 표에는 216줄이 남아 있어 **이관하면 50 → 216으로 되살아난다**. 구글시트 파일은 7/29 이후 수정 없음 = 시트 편집 탓이 아니고, 원인은 **미조사·별건**).
- 회귀가드 `tests/parityRowPairing.test.js`(20케이스 — 실측 픽스처로 `classifyParity` **실행**: 순서 뒤섞임 허위 0 · 진짜 차이 검출 + 행 표기 · phone8 멤버십 불변 · 줄번호 없을 때 폴백 · 배선) + `campaignSchedule.test.js`(22케이스로 확장 — `구매날짜` 인식 · 입금/리뷰 날짜 오인 부재).

### ★★ W5 이관 완료 후 드러난 것 2종 — 목록 밖 358건 · 되살아난 옛 차수 (운영 실측 2026-08-07)
- **상황**: 전환 화면이 `이관됨 86 · 남음 0` 이라 "전부 끝"으로 보였는데, 서버를 다시 세어 보니 **등록 작업 444건 중 358건이 목록 밖**에 있었다(구매일 연도 미확정 → 기본 필터 2026~ 에서 제외). 그 358건은 **여전히 시트를 읽는다**(등록 시트 35개 중 16개가 크론 대상). 구성 = 표 0줄 335건(6개 시트 · 268건이 `어니스트캄_업무시트` 하나 · 검색인덱스에 아예 없는 유령 등록) + 표에 줄 있는 23건(검색 명단 합계 2,849명). ★ **사용자 확정 = 이관하지 않는다(과거 자료)**.
- ★★ **화면이 그 존재를 말하지 않던 것이 결함**: 서버는 `yearUnknown` 을 응답에 실어 보내는데 전환 화면은 `filteredOld` 만 그렸다(그 값이 0이라 화면은 침묵) → **"남음 0"이 "전부 끝"으로 읽힌다**. 지금은 `구매일 연도가 확정되지 않은 N건은 과거 자료로 보고 목록에서 제외했습니다 — 이관 대상이 아닙니다 [보기]`(`_coToggleUnknown` → `includeUnknown=1`). ★ **`yearUnknown` 이 0이거나 필드가 없으면 아무 말도 하지 않는다**(구버전 백엔드·해당 없음을 "제외했다"로 꾸미지 않는다).
- ★★★ **이관이 옛 차수를 되살렸다 — `active=FALSE` 를 장부 재생성이 무시했다**: 쿠팡(26년)은 이관 전 검색 명단이 **5·6차 50명**이었는데 이관 후 **216명**이 됐다(1·2·3·4·7차 + 빈 차수 166줄 부활). 원인 사슬 = ① 이관 직전 마지막 반영의 투영이 `_reconcileSeen` 으로 **시트에 더는 없는 import 줄**을 `active=FALSE` 로 내림 → ② 뒤이은 `rebuildLedgers` 는 **`deleted_at` 만 보고** 그 줄들을 장부에 다시 넣음 → ③ 다음 투영의 `_enrichTab` 이 `active=TRUE, absent_since=NULL` 로 되살려 **자기강화 루프**로 굳음(`importTabFromIndex` 는 `active` 를 안 켜지만 `_enrichTab` 이 켠다).
  - **수정**: 이관에서 **표식을 켠 뒤 · 장부 재생성 앞**에 `participants.retireInactiveImportRows` 로 그 줄들을 **소프트 삭제로 승격**한다(응답 `retired`). ★ `source='import'` 만 — 준비 자리(`worktable`)·수기(`manual`)는 seen-set 대상이 아니라 비활성이 될 수 없고, 건드리면 **빈 슬롯이 통째로 사라진다**. ★ fail-soft(실패해도 이관 유지 + 사유 응답).
  - ★★ **`rebuildLedgers` 에 `active` 필터를 넣지 않았다(의도)** — 넣으면 **한 번 비활성이 된 줄은 이름을 되살려도 영영 명단에 못 돌아오는 일방통행**이 된다(작업표에서 이름을 지웠다 다시 넣는 정상 복구가 막힌다). 은퇴는 **`deleted_at`(명시적 조작)** 으로만 한다.
- **줄 정리 창구**(사용자 확정 "작업보드에서 옛 차수 줄을 정리한다"): 무시트 작업의 작업보드 [⋯] 도구 → **[🧹 줄 정리]**(`_wrCanRetire` = 무시트 ∧ admin/master). 차수별 표(줄·이름·제출·입금)에서 골라 **미리보기 → 실행** 2단계. `POST /api/trackb/worktable/retire-rows`(adminOrMaster · **dryRun 기본** · `LedgerError` 는 400대).
  - ★★ **순서가 계약: 작업표 soft-delete → 장부 재생성**. 반대로 하면 10분 투영의 `importTabFromIndex` 가 **`deleted_at = NULL`** 로 되살린다 — 그 upsert 는 **장부에 있는 줄만** 건드리므로 장부에서 먼저 빼 두면 그 뒤로는 영원히 안전하다.
  - ★★ **`campaign_participants` 쓰기는 `participants.service` 소유**(쓰기 소유자 규율) — 장부 모듈은 게이트·순서·재생성만 맡는다. 그래서 `sheetlessCutover` 의 **쓰기 표면은 여전히 `tab_configs` 한 곳**(회귀가드가 SQL 을 세어 고정).
  - ★ **무시트 탭만**(시트 기반은 표에서만 내려도 다음 빌드가 되살린다 — 사유를 말하고 거부) · **대상 미선택은 `empty` 거부**(조용히 아무것도 안 하지 않는다) · 조회와 삭제가 **같은 조건**(미리보기 ≠ 결과 방지) · 차수 **빈 값('(빈값)')도 고를 수 있다** · 대상은 **서버가 조건으로 다시 고른다**(화면이 보낸 행 목록 불신) · onclick 은 **인덱스만**(차수는 시트발 문자열) · 오버레이 **body 직속** · **바깥클릭으로 닫지 않는다**(고른 것이 날아가지 않게, Esc·[닫기]만) · 재료는 **이미 받아 둔 표**(`STATE.wd.roster`)에서 센다(신규 조회 0) · CSS 는 `.wbl-wrt`(홈 CSS 스코프 가드 계약 유지).
  - ⚠ 정리한 줄은 **리뷰어 "리뷰 내역"에서도 사라진다**(제출완료 포함) — confirm 이 그 사실과 건수를 말한다. 되돌리기는 소프트 삭제라 가능하지만 **화면 창구는 없다**(필요 시 DB).
- 회귀가드 `tests/sheetlessRetireRows.test.js`(48케이스 — 쓰기 소유자·게이트·순서 계약·미리보기 쓰기 0·이관 은퇴 범위·라우트/화면 배선, **변이시험 6종 검출 확인** — 죽은 호출(`if(0) await …`)·라우트 스코프 단언 2종은 변이시험이 잡아 강화했다) + 실 http 오리진 브라우저 실측(모달 렌더·(빈값) 라벨·미리보기 후 실행 활성·페이로드·Esc·역할 게이트 3종·전환 화면 고지 토글).

### ★★ 탈 구글시트 전 구간 실전 테스트 (D7-a 준비 완료 확인 · 2026-08-08 프로덕션 실계정)
- **결과 보고서 = `frontend/docs/탈구글시트_E2E_테스트결과.html`**(69항목 실측값 · 발견 2건 · 정리 내역 · 미확인 한계). 실서버에 **테스트 작업 2건을 실제로 만들어** 인트라넷 리뷰오더 접수 → 무시트 접수(F1) → 작업보드 목록 → 리뷰어 검색 → 구매양식 제출 → 리뷰제출·입금 표시 → 모집공고 발행·달력 프리필까지 **구글 시트 호출 0으로 완주**했고, 뒤에 만든 데이터는 [🧹 줄 정리]·공고 삭제·오더 소프트삭제로 되돌렸다(빈 테스트 탭 2개만 잔존 — 화면 창구가 없어 강제 삭제하지 않음).
- ★★ **발견 2건은 둘 다 "조용한 실패"였다 — 화면·데이터는 멀쩡해 보였다**(그래서 한 바퀴를 실제로 돌리지 않으면 못 찾는다):
  - **F-1(중간) 리뷰제출 표시가 무시트 탭에도 시트 쓰기를 시도**(`submit.routes` Step 3 배경 작업, PR #650): W2-a 의 "시트 쓰기 차단 4중"은 **구매양식 제출 경로**를 덮었는데 리뷰제출 표시는 **별도 라우트라 빠져 있었다**. 큐 실행부 백스톱이 실제 쓰기는 막아 **데이터 오염 0**이었지만 제출 1건마다 구글 404 1회 + `flow=review_submit` warn 오류로그 + 무의미한 큐 항목이 쌓였다. → `setImmediate` **본문 맨 앞**(헤더 읽기·`writeSheet` 앞)에 `isSheetless` 게이트, 판정 실패는 fail-open. ★ `markStatusCell` 결과를 재활용하지 않고 **따로 판정한다** — 그 호출은 `if(complete)` 안에만 있어 **부분 제출 갈래를 안 덮는다**. 차단은 이제 **5중**.
  - **F-2(높음) 무시트 공고의 달력 프리필(D3-a)이 배포 이래 한 번도 동작하지 않았다**(`sheetlessDailyPlan.readWorktableDates`, PR #651): 작업표 구매일자는 `planToSheetValues` 가 **전부 `M / D (요일)`** 로 써서 그 열에 **연도가 한 칸도 없는데**, 이 호출부만 `parseDateColumn` 에 **`fallbackAnchor` 를 안 넘겼다**(063 시트 일정·시트 우위 점검 2곳은 이미 넘긴다) → 전 행 null → `planPrefill:{ok:false,reason:'unparsable'}` 로 **fail-soft 하게 조용히 죽어** 있었다. 공고는 `daily_limit` 로 정상 동작해 눈에 띄지 않았다.
- ★★ **회귀가드의 픽스처가 제품이 쓰지 않는 모양이면 그 버그를 통과시킨다**(F-2 의 진짜 교훈): 기존 `sheetlessDailyPlan` 가드는 `'2026-08-07'`(ISO)로 검증해 **앵커 없이도 통과**했다. 지금은 **실제 작업표 표기(`M / D (요일)`)** 로 고정한다 — 픽스처를 ISO 로 되돌리면 이 회귀가 그대로 살아난다.
- **테스트로 확인된 것**: 가상 시트ID(`wt_`+hex20=23자)·숫자 gid 발급 · 작업표 seq = 헤더행+1 부터 · `source='worktable'` · 주문이 **큐 없이 같은 요청에서 `mirror_status='written'`**(562ms, 준비 자리 seq 2·3 에 제자리 배정) · 리뷰제출 `O`·입금 시각 스탬프가 **작업표 칸에 기록되어 장부 재생성을 반복해도 보존**(W3-a) · 무시트 배지·죽은 시트링크 제거 · 097 일정 신호(주말 제외)가 날짜 분배에 실제 반영.
- ⚠ **이번 테스트로 확인되지 않은 것**(보고서 §5 에 명시): 사람이 인트라넷 폼을 직접 제출하는 화면 조작(자격증명 없음 — 배포 자산 검사 + 수신측 API 로 대체) · Drive 이미지 업로드 · D2-a 폴더 1단 업체명(테스트 탭에 업체 매핑이 없어 캠페인명 폴백만 확인) · **이미 이관된 86건의 재검증**(신규 무시트 작업만 확인) · 동시 부하.
- **다음(W5 잔여)**: 입금 M2(이체결과 파일 자동 짝맞추기)는 **별도 기능 트랙**. 남은 358건은 **이관하지 않는다**(과거 자료 — 화면이 건수를 고지).

- **계획 문서** = `frontend/docs/탈구글시트_최종단계_와이어프레임.html`(비개발자용, F1~F5). 요지: 접수 단일 관문은 유지하되 등록 대상을 시트 탭 → **시스템 작업표(무시트)**로(F1), 무시트 작업은 검색·행배정을 작업표 직접 조회로(F2 — **F2가 F1보다 먼저** 준비되어야 함: 작업표만 만들고 검색이 안 되면 접수했는데 리뷰어가 못 찾는다), 입금 M2·화면 단일화(F3·F4), **기존 작업 강제 이관 없음**(F5 — 새 작업만 무시트, 진행 중은 시트 병행 유지 후 마감 소멸).
