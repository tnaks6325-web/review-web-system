# 2026-08-24 패치노트 — 안 쓰는 구글시트를 그만 읽습니다

## 적용 대상

- 리뷰웹시스템[3버전] 서버 내부 동작 (화면 변화 **없음**)

## 배경

시스템은 3분마다 구글시트를 열어 "사람이 시트에서 값을 고쳤나"를 확인하고 있었습니다.
그런데 **작업이 전부 무시트로 옮겨간 지금, 아무도 그 시트를 고치지 않습니다.**

2026-08-24 본섭 실측:

- 닫히지 않은 작업 **113개가 전부 무시트** (아직 시트를 쓰는 작업 **0개**)
- 그런데도 감지는 계속 돌아 **하루 약 2,880번** 구글시트를 열고 있었습니다
- 쌓여 있던 「원장 불일치」 **108건이 전원 무시트 작업** 소속이었습니다

무시트로 바꿔도 "이 주문은 시트에 써졌다"는 표식이 남기 때문입니다. 감지는 그 표식만 보고
옛 시트를 계속 열었습니다.

## 수정 내용

**무시트 작업은 감지 대상에서 뺍니다.**

- 판정은 이미 있던 **같은 기준**을 씁니다 — 스마트빌드·미러·변경감지가 쓰는 그 판정에
  역동기화만 빠져 있었습니다. 새 규칙을 만들지 않았습니다.
- 세 군데를 막았습니다: 감지 자체 · 3분 주기의 대상 고르기 · 자동적용의 재확인 읽기.
- **영구 배제가 아닙니다.** 시트를 쓰는 작업을 새로 만들거나 다시 연결하면 그 즉시 다시 봅니다.
- 판정을 못 하면 **종전대로 봅니다.** 확인이 안 된다고 감지를 멈추지 않습니다.

## 확인해 주세요

- **화면은 그대로입니다.** 지금 쌓여 있는 108건은 **지우지 않습니다** — 사람이 판단할 몫이라
  조용히 정리하지 않았습니다. 다만 무시트 작업에서 **새로 늘지는 않습니다.**
- ⚠ 그 108건을 그대로 적용하면 **아무도 안 쓰는 시트의 값으로 주문원장을 덮게 됩니다.**
  어떻게 처리할지는 별도로 정해야 합니다(이 패치는 더 쌓이는 것만 막습니다).
- 이체 금액이 잘못 나가는 문제는 이 패치가 아니라 **작업보드 기준으로 바꾸는 쪽**에서 다룹니다.

## 영향 범위

- 마이그레이션 **없음** · 화면 변경 **없음** · 신규 API **없음** · 저장하는 값 **없음**
- 3분 주기 작업의 보고에 `sheetlessSkipped`(건너뛴 수)가 붙습니다 — 조용히 줄어들면
  현황이 거짓말이 되므로 숫자로 말하게 했습니다.
- 시트를 쓰는 작업이 남아 있다면 **종전과 완전히 같이** 동작합니다.

## 검증

- `tests/reverseSyncSheetlessSkip.test.js` **12건** — 문자열 검사가 아니라 구글시트를 부르면
  즉시 터지는 스텁 위에서 **실제로 실행**해 호출 0을 확인합니다.
- 변이시험 2종(감지 게이트 제거 · 주기 필터 제거) 모두 검출 확인.
- 서버 테스트 **370개 중 369개 통과**. 유일한 실패 `blogPreparedRows` 는 **이 변경 이전부터
  main 에서 빨간 상태**입니다(별건으로 보고).

## 롤백

- 본 변경 커밋을 되돌리면 됩니다. 저장하는 값이 없어 남는 흔적이 없습니다.
