# 2026-08-19 패치 노트 — 작업보드 중복 반영 재발 방지 + 중복 줄 정리 도구

앞선 긴급 조치(PR #825)는 **중복을 만들던 원인**을 멈췄다. 이번은 ① 구조적으로 다시 못 생기게 막고
② 이미 쌓인 중복 줄을 안전하게 정리할 창구를 만든다. **데이터를 자동으로 지우지 않는다.**

## 왜 필요한가

무시트(탈 구글시트) 경로는 시트 시절의 `sheet_row_claims`(`(sheet_id, tab_name, dedup_key)` 유니크)를
건너뛴다(`skipSheetMirror`). 그런데 `campaign_participants.order_submission_id` 도 유니크가 아니라
**"주문 1건 = 줄 1개" 를 강제하는 것이 어디에도 없었다.** 주문원장 행이 하나 더 생기면 작업보드 줄도
하나 더 생긴다.

## 변경 ① 기록 단계 — 같은 구매면 새 줄을 만들지 않는다

`sheetlessOrder.writeOrderToWorktable` 이 빈 슬롯을 선점하기 **전에**, 같은 탭에
**주문번호(숫자만) + 연락처(숫자만)** 가 같은 줄이 있는지 본다. 있으면 `duplicate_row` 로
**그 줄을 가리켜 돌려주고 새 슬롯을 먹지 않는다.**

- ★ 기존 줄의 `order_submission_id` 는 **바꾸지 않는다** — 링크를 빼앗으면 원래 주문이 "미반영"으로
  되살아나 복구 잡이 또 줄을 만든다(막으려던 사고의 재현).
- ★ 리뷰어에게는 `ok: true` 로 돌려준다(이미 접수된 구매다 — 실패로 보이면 또 낸다).
- ★ 주문번호가 6자리 미만(비번호 쿠팡 등)이면 **판정하지 않는다**(fail-open).
- ★ 행 번호를 명시한 레거시 복구 경로는 종전 그대로.

## 변경 ② 주문원장 단계 — 날짜를 넘는 같은 구매 차단 (무시트 전용)

종전 중복 가드는 **오늘(KST) 제출분**만, **15개 필드 완전 일치**로 봤다. 어제 것 재제출이나 메모 한 글자
차이로 뚫렸다. `findEquivalentOrderInTx` 를 더해 **주문번호·연락처·수취인·결제금액 4개**가 같으면
날짜와 무관하게 같은 구매로 본다.

- ★ **무시트 경로에서만** 켠다(`crossDay: !!orderScope.sheetless`) — 시트 경로에는 claim 유니크가
  최종 수렴점으로 남아 있어 동작을 바꿀 이유가 없다(무회귀).
- ★ 당일 판정이 먼저 돌고, 못 잡을 때만 이 판정이 돈다. 취소(`deleted_at`)된 주문은 제외.

## 변경 ③ 이미 쌓인 중복 줄 정리 도구

`POST /api/trackb/worktable/dedupe-rows` (**adminOrMaster**, **dryRun 기본**) ·
`sheetlessLedger.dedupeRows`

- 판정 키는 ①과 **같다**(갈리면 "화면은 중복인데 정리는 안 되는" 상태가 된다).
- 가장 이른 줄을 남기고 나머지를 **소프트 삭제**(`deleted_at`) + 그 중복 주문만 `mirror_status='canceled'`.
- ★★ **다음 그룹은 손대지 않고 사유와 함께 보고만 한다**
  - `in_payment_batch` — 지울 줄이 **입금 회차(대기·완료)에 담겨 있다.** `uq_payment_items_active` 가
    `(sheet_id, tab_name, row_index)` 기준이라 중복 줄이 각각 별개 이체 건으로 잡힌다. 조용히 지우면
    **이미 나간 이체의 근거가 사라진다.**
  - `used_row_is_not_first` — 지울 줄에 리뷰제출·입금 표시가 있고 남길 줄에는 없다(실제로 쓰인 줄이 뒤에 있다).
- ★ 시트 기반 탭은 거부(표에서만 내려도 다음 시트 반영이 되살린다). 작업표 쓰기는
  `participants.service` 가, 주문 취소는 `orderLedger.service` 가 한다(쓰기 소유자 규율).

## 변경 ④ 화면 창구 — 작업보드 [⋯] › ♻ 중복 정리

★★ **판정 규칙(사용자 확정 2026-08-19)**: **주문번호(숫자 6자리 이상)와 연락처가 모두 같을 때만** 중복이다.
주문번호가 없거나 6자리 미만인 줄은 **대상에서 제외**하고, 같은 사람이 **다른 주문번호**로 여러 번 참여한
것은 중복이 아니다. 미리보기 표에 **주문번호 열**을 두어 사람이 판정 근거를 눈으로 확인한다(보류 목록에도 표기).


무시트 작업의 작업 도구 메뉴에 **[♻ 중복 정리]** 를 뒀다(게이트는 [🧹 줄 정리]와 같다 — 무시트 + master/admin).
**[미리보기] → 확인창 → [정리 실행]** 2단계이고, 판정·대상 선정은 전부 서버가 한다(화면은 결과를 그릴 뿐).
보류 그룹은 사유 문장을 **그대로** 보여준다.

★★ **화면 게이트 = 서버 게이트(adminOrMaster)** — `_wrCanRetire()` 를 재사용하지 않는다.
2026-08-18 '작업보드 staff 전체권한' 으로 줄 정리 쪽은 `_isInternalRole()`(staff 포함)로 넓어졌는데,
따라 넓히면 **AE 에게 눌러도 403 나는 죽은 버튼**이 생긴다. 이 조작은 줄 정리와 달리 주문 취소까지
동반해 되돌리기가 더 무겁다.

## 검증

- 신규 가드 `server/tests/worktableDuplicateGuard.test.js` **43케이스** — 스텁 pool 로 서비스를 **실제 실행**
  (중복이면 슬롯 미소비·링크 미탈취·ROLLBACK / 중복 없으면 무회귀 / fail-open / 정리 도구의 보류 2규칙)
- **변이시험 6종 검출 확인**: 중복 조회 제거 · crossDay 상시 ON · 취소주문 포함 · 입금 보류 규칙 제거 ·
  dryRun 기본 뒤집기 · 남길 줄을 마지막으로
- 인접 가드 통과: `sheetlessOrderAccept`(89) · `sheetlessPurchaseSubmit` · `sheetlessLedger` ·
  `sheetlessStatusPersist` · `orderLedger`·`orderLedgerPRA/PRB` · `payment*` 21종 · `campaignBatchSubmit` ·
  `optionColumnPreserve` · `workboardFinish` · `trackB`

## 곁다리로 되살린 가드 1건

`workboardWorktabs` 의 **"M2 쓰기 표면은 신규 2테이블뿐"** 검사가 착수 전부터 빨간 상태였다.
종료 경계를 `module.exports` 로 잡아, 8/18 에 M2 구역 **아래**로 들어온 다른 기능의 쿼리까지 빨려 들어갔다.
소스에 닫는 마커 `// ══ /M2` 를 넣고 가드가 마커까지만 자르도록 했다(검사 의미 불변 · 변이시험으로 검출 확인).

## 곁다리로 되살린 가드 2건째 — 이관 서비스 쓰기 표면

`sheetlessRetireRows` 의 **"이관 서비스의 쓰기 표면은 `tab_configs` 한 곳"** 도 착수 전부터 빨갰다.
원인을 확인해 보니 8/18 에 들어온 **`_provisionUnlinkedCampaign`**(연결 없는 활성 공고에 서버 작업표를
만들어 주는 경로)이 등록·연결 4테이블을 쓴다. 작업표 생성 자체는 접수와 **같은 서비스**
(`sheetlessAccept.createSheetlessWorktable`)에 위임하므로 사본은 없다 — **의도된 기능**으로 판단했다.

그래서 가드를 지우지 않고 **두 구역으로 쪼갰다**(검사 의미는 오히려 강해졌다):

- 이관 본체 = 여전히 `tab_configs` 한 곳
- `_provisionUnlinkedCampaign` 구역 = `campaigns`·`tab_configs`·`recruit_campaigns`·`work_orders` **허용목록**을
  넘으면 실패 + 작업표 생성 위임(사본 금지) + `campaign_participants` 직접 쓰기 금지

변이시험 3종 검출 확인(본체에 다른 테이블 쓰기 · 프로비저닝의 작업표 직접 쓰기 · 위임을 사본으로 대체).

⚠ **`homeTasklistFilters` 는 여전히 빨갛다**(착수 전부터 · 원인 미조사 · 이번 변경과 무관).

## 롤백

이 PR의 병합 커밋을 되돌리면 된다. 마이그레이션 없음 · 데이터 변경 없음(정리 도구는 사람이 실행할 때만 동작).
