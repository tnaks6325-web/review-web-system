# 2026-08-24 패치노트 — 닫힌 작업의 시트도 그만 읽습니다 (앞 패치 보완)

## 적용 대상

- 리뷰웹시스템[3버전] 서버 내부 동작 (화면 변화 **없음**)

## 배경

바로 앞 패치(#1143)로 "무시트 작업의 구글시트는 읽지 않는다"를 넣었습니다. 배포 후 실제로
확인해 보니 **무시트 작업은 막혔는데 계속 새 건이 생기고 있었습니다.**

3분 만에 19건이 새로 생겼고, 전부 **닫힌 작업 3개**에서 나왔습니다(같은 구글시트 하나).

앞 패치는 "무시트인 작업을 빼는" 방식이라 두 가지가 그대로 새어 나갔습니다.

- **닫힌 작업** — 무시트가 아니라서 제외 목록에 안 잡힙니다. 주문을 받지 않으니 그 시트를
  사람이 고칠 일도, 고쳐도 반영할 곳도 없습니다.
- **아카이브된 작업** — 목록에서 아예 지워져서 제외 목록에 **담길 수조차 없습니다.**

## 수정 내용

**"빼는 방식"을 "허용하는 방식"으로 뒤집었습니다.**

이제 **지금도 사람이 고칠 수 있는 시트**만 읽습니다 — 등록되어 있고, 무시트가 아니고,
닫히지 않은 작업. 그 밖은 전부 읽지 않습니다.

- 판정은 앞 패치와 같은 곳(**단일 출처**)에 두었습니다. 이 규칙은 시스템이 이미 시트 단위로는
  갖고 있던 것이고, 작업 단위에 없던 것을 채운 것입니다.
- **되살아납니다** — 마감을 풀거나 시트를 다시 연결하면 그 즉시 다시 봅니다.
- **판정을 못 하면 종전대로 읽습니다.** 허용 방식은 잘못 만들면 "확인이 안 되니 전부 차단"이
  되기 쉬운데, 그건 게이트 고장이 곧 기능 정지가 됩니다. 그렇게 되지 않도록 못 박았습니다.

## 확인해 주세요

- **화면은 그대로입니다.** 저장하는 값도, 새 창구도 없습니다.
- 3분 주기 작업 보고의 건너뛴 수 항목 이름이 정확해졌습니다(무시트만이 아니라 감지 대상이
  아닌 작업 전체를 셉니다).

## 영향 범위

- 마이그레이션 **없음** · 화면 변경 **없음** · 신규 API **없음** · 저장하는 값 **없음**
- 시트를 실제로 쓰는 작업이 있다면 **종전과 완전히 같이** 동작합니다.

## 검증

- `tests/reverseSyncSheetlessSkip.test.js` **21건** — 구글시트를 부르면 즉시 터지는 스텁 위에서
  실제로 실행합니다. 무시트·마감·아카이브 **세 갈래 모두** 호출 0인지 확인합니다.
- 변이시험 3종 전부 검출 — 마감 조건 제거 / 판정 실패 시 전부 차단 / 종전대로 읽기 해제.
- 서버 테스트 **372개 중 371개 통과**. 유일한 실패 `blogPreparedRows` 는 이 변경 이전부터
  main 에서 빨간 상태입니다(별건).

## 롤백

- 본 변경 커밋을 되돌리면 됩니다. 저장하는 값이 없어 남는 흔적이 없습니다.
