# ★ 리뷰검수 🚫 주문취소 유형 + 캡처 돋보기 (2026-08-23 신고)
- **① 주문취소 = 리뷰어의 작업취소**: `취소완료`·`주문취소`·`취소 신청` 같은 표시가 보이는 주문 화면. AI 판별 종류에 **`order_cancel` 한 줄만 추가**하고(기존 `review`·`receipt`·`purchase_confirm` 정의는 무변경) `order_capture` 에는 **취소 표시가 보이면 양보한다**는 배타 조건만 덧붙였다. 캐시 접두 `classify6:` → **`classify7:`**(판정 기준이 바뀌면 반드시 — 안 올리면 옛 판정이 히트해 조용히 무효).
- ★★ **유형 축은 `utils/inspectIssueTypes` 규칙표 맨 앞에 `cancel` 을 합류**시켜 **JS 판정과 SQL 집계가 같은 규칙에서 파생**된다(사본 0). `format_fail` 과 **겹쳐서 세어지는 것은 의도** — 취소 화면은 리뷰 화면이 아닌 것도 사실이라 두 칩에 모두 잡힌다(합계 ≠ total 은 화면이 이미 말한다).
- ★ **주 이슈 우선순위에서 맨 앞** — 담당자에게 "리뷰 화면이 아님"보다 **"취소됐다"** 가 먼저 읽혀야 한다. **주 조치는 [✕ 불량]으로 종결**(이동 버튼 아님 — 어느 칸으로 옮겨도 취소는 취소다). ★ **실제 주문취소 처리는 작업보드에서 사람이** 한다(되돌리기 어려운 조작을 검수 화면에 두지 않는다).
- ★★ **근거 문장이 "리뷰 화면이 아닙니다"라고 말하지 않는다**(카드·상세 두 곳) — 그렇게 말하면 담당자가 **다른 칸으로 옮기려 든다**. 무엇으로 보이는지가 아니라 **무슨 일이 일어났는지**를 말한다(`주문이 취소된 화면입니다 — 리뷰어가 이 작업을 취소한 것으로 보입니다`). 반려 문구(`inspectMessages` `cancel`)도 **재제출을 요구하지 않고** 사실 확인을 청한다.
- ★ 유형 색 띠 `.nc-type.t-cancel`(보라) — **없으면 회색 `t-none` 으로 떨어져 신호가 죽는다**(빨간 벽에 섞이지 않게 빨강을 피했다).
- **② 돋보기**: 카드 썸네일 우하단 **🔍** · 상세 팝업 이미지 클릭(`rizoomable`) → 원본 크게 보기. ★ **실행부는 기존 라이트박스 `woImageModal` 한 벌**(새 오버레이 0 — `z-index 99999` 라 상세 팝업 `#riModal`(9000) 위에 뜬다). ★ onclick 은 **인덱스만**(파일ID 보간 금지) · 카드 클릭(상세 열기)과는 **`stopPropagation`** 으로 분리 · 이미지가 없으면 버튼을 그리지 않는다.
- ★★★ **문자열 grep 가드는 "어느 함수 안인지"를 못 본다 (2026-08-23 실측)**: 취소 카드의 주 조치 블록이 `_riCardActions` 가 아니라 **`_riTypeLabel`(라벨 문자열 함수)** 안에 잘못 들어가 `A is not defined` 로 **리뷰검수 화면이 통째로 죽었는데** 가드는 초록이었다(문자열은 멀쩡히 있었다). **브라우저 실행이 잡았다** → 가드가 **함수 본문을 잘라서** 그 안에 있는지 본다. 같은 계열 = 리뷰검수 무한로딩(프리변수) 사고.
- 회귀가드 `tests/inspectCancelAndZoom.test.js`(32 — 판정 축(JS·SQL·`got`/`kind` 양쪽) · 화면 키 ≡ 서버 키 순서 · 함수 스코프 · 라이트박스 재사용 · onclick 인덱스 · 안내문구 톤, **변이시험 16종 검출 확인**) + 실 http 오리진 브라우저 실측(카드 렌더·🔍 → 라이트박스·상세 위 스택·JS 오류 0). ⚠ kind 목록이 늘어 `fileAutoRoute`·`reviewType`·`purchaseConfirmSignals` 가드를 **부분집합/범위 검사**로 갱신했다(검사 의미 불변 — 앞으로 kind 가 늘어도 안 깨진다).
- 회귀가드 `tests/purchaseConfirmSignals.test.js`(30 — 3줄 불변 · 넓힌 범위 · 리뷰 적립 제외 · 캐시 접두 · **허용 목록 불변** · 2차 검수 4갈래 **실제 실행** · 화면 안내 vm 실행, **변이시험 9종 검출 확인**) + 실 http 오리진 브라우저 실측(구매확정 탭에만 안내 · JS 오류 0).
