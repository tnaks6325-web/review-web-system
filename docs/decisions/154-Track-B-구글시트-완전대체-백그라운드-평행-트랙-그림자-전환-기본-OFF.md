# Track B — 구글시트 완전대체 백그라운드 평행 트랙 (그림자 → 전환) — 기본 OFF
구글시트(업체별 작업내역·작업세부 열람/편집, 접근자 AE·리뷰웹관리자·광고주)를 **단일 통합 UI + DB**로 무중단 대체하는 평행 트랙. 현 시스템(Track A)은 **그대로 두고**(변경 0), 라이브를 읽어 B 모델을 만들며 **추가만·읽기만·격리** 3원칙으로 라이브에 일절 무영향. 준비되면 작업별로 전환·폐기. 서비스 `trackB.service.js`, 라우트 `/api/trackb`(`trackB.routes.js`), 프론트 `frontend/workdesk.html`(2뷰: 작업보드/소유지정), 마이그레이션 047·048. **라이브 코드가 Track B 참조 0**(되돌리기 = 마운트 제거 + 047/048 드롭).
- **실험 부품(기본 OFF) = `docs/history/track-b-실험트랙.md`**: 격리저장소(047)·그림자 투영(`TRACK_B_PROJECTION`)·parity 판정·관측 대시보드·투영 커버리지. ★★ **판정 사본 금지**: parity 짝짓기는 **phone8**(행번호 금지, 그룹 안에서만 줄번호 보조) · cutover 게이트는 **`setSourceOfTruth` 가 지금 시점 라이브 parity real=0** 로 판정하고 일괄 전환은 **force 를 절대 전달하지 않는다** · 커버리지 분모는 `participants.listActiveTabs` 하나.
- **부품4 소유 UI**(adminOrMaster): `GET /advertisers`(소유수 포함)·`/tabs`(활성탭, participants/tabs가 master전용이라 재노출)·`/ownership` GET/POST/DELETE. workdesk.html 소유지정 뷰(**탭 표기='업체관리'**, `renderOwnershipView` 진입 시 **기본 화면=거래처 추가 폼**(`showAddAdvertiser`)+**잔여 시트 수 배너**(미지정 시트=`Object.keys(sheetMap)−ownedSheetIds`))에서 업체 선택→시트/탭 지정·해제. **＋업체 추가**(프론트→`POST /api/portal/advertisers` 재사용)로 거래처 신규 등록 즉시 목록 반영 — **거래처명은 인트라넷 광고주DB 자동완성**(`GET /api/trackb/intranet/advertisers?q=` adminOrMaster 프록시 = `intranetAdvertisers`가 `INTRANET_API_BASE`(기본 inadd-system.pages.dev, 인트라넷=Cloudflare D1로 리뷰PG와 DB 분리 — 연동은 HTTP뿐, 브라우저는 인트라넷 직접 미호출)의 `api/tables/advertisers`를 fetch, 60s 캐시·5s 타임아웃·fail-soft, 이름은 **`business_name`(사업자명)** 기준 — company_name/manager는 레거시 공란, **자동완성 항목에 대표자명(`ceo_name`)·사업자등록번호(`business_number`) 병기** = 사업자 공개정보만, 급여·근태 등 민감필드 미노출), **담당AE는 로그인 사용자명 자동 지정**(읽기전용; workdesk `parseJwt`는 base64url+UTF-8 디코드로 한글 로그인명 안전). 작업보드 왼쪽 작업목록은 **소유지정 업체별 그룹핑**(`scopedActiveTabs`가 `advertiserId/advertiserName` 주석 — 탭지정>시트전체 우선, 미지정 그룹 마지막). **담당AE 매칭**: 소유지정에서 master/admin이 업체 `advertisers.inad_pm` 조회·수정(`POST /api/trackb/advertisers/inad-pm` adminOrMaster, TRIM 저장 — staff 스코프 키 정합) — AE 후보 자동완성은 인트라넷 users 프록시(`GET /api/trackb/intranet/users` adminOrMaster, display_name·username·부서만 반환 = **비밀번호 등 민감필드 매핑에서 폐기**, 60s 캐시 fail-soft). 업체 목록에 `AE 미지정` 경고 서브라벨, 업체추가 폼은 master/admin이 담당AE 직접 지정(거래처 선택 시 인트라넷 광고주DB `manager` 자동 제안, staff는 본인 고정). 회귀가드 `tests/trackBAeMatch.test.js`. **업체추가 3종 강화**(`tests/trackBIntranetSso.test.js` 2e/2f/2.5): ① **거래처 등록 게이트** — 인트라넷 광고주DB(business_name)에 **정확일치**하는 이름만 등록 허용(`isRegisteredIntranetAdvertiser`→`createAdvertiserScoped` 서버검증; 미등록=422·도달불가=**fail-closed 503**), 프론트는 실시간 자동완성(디바운스 120ms)+등록확인 상태문구로 [＋등록] 버튼 활성화(목록 선택/정확입력 시만). ② **업체 삭제**(`DELETE /api/trackb/advertisers/:id` adminOrMaster·사이드바 × 버튼) = **소프트삭제**(`status='ended'`, 가역)+소유 매핑 해제 — advertisers는 포털 공유 원장이라 하드삭제 금지(FK `ON DELETE CASCADE`로 포털작업 유실 방지), 포털은 status 무필터라 무영향·Track B 사이드바만 숨김. ③ **담당AE 후보 = AE 부서 한정**(`/intranet/users?dept=AE` 서버 필터, datalist 제안만 — 자유입력은 여전히 허용, 입력 시 "✓ 인트라넷 등록 AE 확인됨" 배지). ④ **업체추가 시트 드롭다운은 이미 소유 지정된 시트 제외**(`GET /api/trackb/owned-sheets` = `ownedSheetIds`, 활성 소유 DISTINCT sheet_id — 한 시트 중복 소유 방지, 남은 시트 0이면 안내 옵션). 자동완성 항목에 대표자명·사업자등록번호 병기. **시트그리드 날짜 표기 통일**: 날짜형 헤더(일자/날짜/date)는 표시를 "월/일 (요일)"로 통일(`_fmtKDate` — ISO·일련번호는 요일 계산, 시트식 `7 / 3 (금)`은 정규화, 비정형 텍스트 원본 유지), 셀 편집 시작값은 ISO 정본(`_gridEditVal` 분리 — 표시 축약값으로 오버레이 오염 방지).
- **부품4f-2 업체용(광고주) 뷰어 화면 정돈** (시안 `frontend/docs/design-advertiser-viewer.html`, 사용자 확정): 광고주 role 은 리뷰웹시스템[3버전]이 **전용 레이아웃**으로 뜬다. ① **폭 상한 고정** — `body.advm{--app-max:1680px}`. ★ **선언 순서가 규칙**: QHD 기본값 뒤(같은 특이성 → 광고주 고정이 이김) · `body.widemode` 앞(전체화면은 여전히 해제). ② **작업 선택 = 좌측 세로 목록**(`.wrap.advwrap` 3열 = `.awside` 224px + 본문 + 레일, 상단 가로 탭바(tb1/tb2) 미렌더 — `_renderTabList`가 광고주면 `_renderAdvSidebar`로 분기). ③ **접속 기본 화면 = 내 작업 목록 표**(`_renderAdvHome`, 업체관리 연결탭 표 차용): 시작일·작업명·진행상황·참여제출입금·견적서·계산서·총비용·입금액·입금일·**남은 입금액(=총비용−입금액 화면 파생, 0원=완납 ✓)**. 데이터 = **`GET /api/trackb/my-work-summary`**(advertiser 전용, `advertiserId`는 **토큰에서만** — 쿼리 미수신=IDOR 차단; `advertiserWorkSummary` = `ownedTabsForAdvertiser`+`settlementSummaryForAdvertiser` 재사용에 **광고주 렌즈**: 비고 memo·담당 manager·salesId·amountMismatch **응답에서 폐기**, 정산 노출 토글 OFF 면 인트라넷 프록시 0회+`settlementHidden`). ④ **상세**: 헤더(.mh)·요약 스트립·정산 카드 `max-width:1380px` 캡(`body.advm` 스코프 — 내부 화면 불변), 요약 스트립은 담당 표기 대신 **시작일** 칸, **정산 카드 상시 펼침**(스텝퍼 + 금액 4칸 `_advSettleMoney`: 총비용(견적서 금액 우선)/입금액/입금일/남은 입금액 — 재료는 `settlementForTab`에 추가된 `paidAmount`/`paidDate`). ⑤ **내부 용어 미노출**: 원본(sot) 배지·"계약 연결" 문구·발주 담당 실명·Parity 레일탭은 광고주에게 안 그린다(비공개·미연결 정산은 "정산 정보가 아직 준비되지 않았습니다" 한 줄).
- **부품4f-3 업체 첫 화면 = 대시보드** (시안 `frontend/docs/design-advertiser-dashboard.html`, 사용자 확정): 접속 기본 화면이 목록 표 → **대시보드**(`_renderAdvDash`). 표는 사이드바 [📋 전체 작업](`_renderAdvList`)과 대시보드 하단 "최근 작업"(4건)으로 남는다. **신규 API 0 · 신규 저장 0** — 전부 `/my-work-summary` + 기존 `/workdesk/unseen` 파생. 구성 = ① KPI 4칸(진행 중 작업 · 참여 진척 · 총 계약금액 · 미입금 잔액) ② 진행 중 게이지(진한=제출, 연한=참여−제출, 합계 폭=참여율) ③ 정산 파이프라인(견적→계산서→입금 단계별 건수 + 금액 2칸) ④ 확인 필요(잔액 큰 순 → 계산서 미발행 → 미확인 코멘트, 최대 5건) ⑤ 최근 작업.
  - ★★ **판정·파생 단일 출처**: 진행/완료 = `_awDone`(=`_awStatus`) 하나를 사이드바 그룹·KPI·게이지·표 칩이 공유 / 정산 파생(남은 입금액 = 총비용 − 입금액) = `_awSetl` 하나 — 갈리면 "표는 3,250,000인데 KPI는 다른 값"이 된다.
  - ★★ **표 렌더러 사본 금지**: 행 빌더 `_awRowHtml` + `_awListHtml(items, limit)` **한 벌**을 대시보드(limit 4)와 전체 작업(limit 없음)이 함께 쓴다(회귀가드가 `class="awlhead"` 1회만 존재함을 고정 = 사본 부활 차단).
  - ★ **정산 노출 OFF 업체**는 금액 KPI 2칸과 정산 패널을 **통째로 뺀다**(빈 0원 표시 금지 — 있지도 않은 0원이 사실처럼 보인다). 참여 진척은 **진행 중 작업만** 집계(끝난 숫자가 진척을 희석). 총 계약금액 KPI는 **정산 미연결 건수를 부제로 고지**(조용한 누락 금지).
  - ★ **클래스는 전부 `ad*` 접두** — 기존 `.gauge`(원형 게이지)·`.dgrid`(관측 뷰)·`.kpi`(`.stripA` 하위)와 겹치면 남의 화면이 조용히 무너진다(`worow`→`wol` 전례).
  - ★ 미확인 코멘트 갱신(`refreshUnseen`→`_renderTabList`)이 **대시보드까지 재렌더**한다(사이드바만 그리면 [확인 필요]가 stale). 뒤로/앞으로는 히스토리 항목의 `advView`로 대시보드↔전체 작업 전환을 되짚는다.
- **부품4f-4 업체 상세 화면 3종 보강**(사용자 요청): ① **리뷰제출 컬럼 복구** ② **리뷰 이미지 미리보기 패널** ③ **정산 현황 6칸**.
  - ★★ **광고주 컬럼 화이트리스트에 "상태 칸 선점"**(`_advertiserColumns(raw, {submitCol, submitCol2})`): 그 탭의 `submit_col`/`submit_col2`(= `review_index`가 감지해 `campaign_participants`로 복제한 리뷰제출·입금 열 헤더명)를 **키워드 규칙보다 먼저 선점**한다. 두 사고를 동시에 막는다 — ㉮ 헤더가 키워드에 안 걸리는 탭(실측: 카페/블로그 바이럴)에서 **리뷰제출 열이 통째로 누락**(신고 건) ㉯ `입금일자` 같은 헤더를 위쪽 **구매일자 규칙(`/일자|날짜/`)이 먼저 삼켜** 구매일자 칸에 입금일이 들어가고 입금 칸이 사라지는 오배치. 출력 순서는 규칙 순서 그대로라 **결제금액 → 리뷰제출 → 입금** 위치가 유지된다. ★ 실재하지 않는 헤더명은 무시(빈 열 생성 금지), `submit_col`이 비면 **기존 동작 그대로**(무회귀).
  - **리뷰 이미지 미리보기**(`GET /api/trackb/workdesk/review-images`, `_ensureThreadScope` = 내부인 + 소유 광고주): 행별 파일ID를 `{row_index: [{fileId,slot,at}]}`로 반환. 소스는 **원장 `review_submissions`(032) 1순위 + 대표 이미지 `review_index.review_file_id`(031) 폴백**(그 이전 데이터), 같은 파일ID 중복 제거·행당 12장 상한. ★ **이미지는 기존 무인증 프록시 `/api/drive/image/<id>`가 스트리밍** — 신규 저장소·신규 프록시 0. ★ 키 = `row_index` = `campaign_participants.seq`(투영이 그대로 복제).
  - **화면**: 그리드 우측 `#rvPane`(340px sticky, `.advgw` flex). **행 클릭 · ↑↓ 키**로 선택 이동(위임 1회 바인딩 — 재렌더로 tbody가 갈려도 유지, 입력창·모달 위에서는 방향키 미가로채기), 필터·정렬 재렌더 후 `_rvReapply`로 선택 복원. ★ **미제출 행은 "리뷰 미작성 · 미제출"**, 제출 표시는 있는데 이미지가 없으면 **"리뷰 이미지 미등록"**으로 **구분해서** 안내한다(둘을 뭉뚱그리면 거짓말이 된다). 이미지 클릭 = 기존 `woImageModal` 라이트박스. ★ URL은 **bare `API_BASE`**(`window.API_BASE_URL`은 최상위 const라 항상 undefined — 레포 실측 함정) + 파일ID 형식 검증 후에만 생성.
  - **정산 현황 = 6칸**(견적서·계산서·총비용·입금액·입금일·남은 입금액, `_advSettleFields`): 광고주 화면에서 내부 스텝퍼·[계약 변경/해제]를 대체한다(스텝퍼가 담던 견적/계산서/입금 상태가 6칸에 그대로 들어가 중복). **내부(master/admin/staff)는 종전 스텝퍼 그대로**(무회귀). ★ 남은 입금액 = 총비용 − 입금액 = **목록 표·대시보드와 같은 파생식**.
- 회귀가드 `tests/advertiserViewer.test.js`(라우터 실행·렌즈 실행·배선·CSS 선언 순서·대시보드 단일 출처/사본 금지·컬럼 선점 순수함수 실행·리뷰 이미지 병합).
- **부품4g 등록리뷰어DB 탭(상단탭)**: `GET /api/trackb/reviewers`(검색 `q`=이름 부분일치 ∪ 연락처 숫자 부분일치, `status` 필터, 서버 페이지네이션 `limit`≤200·`offset`) + `POST /api/trackb/reviewers/memo`(admin_memo만). **둘 다 `adminOrMasterMiddleware` — AE(staff)·광고주 차단**(리뷰어DB는 담당 업체와 무관한 전사 개인정보라, AE의 "담당 탭" 스코프 원칙을 깨지 않기 위해 master/admin 전용). nav 버튼도 `isAdmin` 분기에만 렌더. **주민번호·계좌번호를 표에 그대로 표시**(사용자 확정)하므로 화면공유 주의 문구를 상시 노출하고 **수정은 관리자 메모만**(정산·신원 필드는 조회 전용, 가드가 `SET` 대상이 `admin_memo` 하나뿐임을 고정). 기존 `/api/reviewer/list`는 전 행을 한 번에 반환해 이 화면엔 부적합 — 재사용하지 않는다. ★ 동적 WHERE의 **자리표시자↔파라미터 개수 정합**이 이 라우트의 유일한 함정(숫자 없는 이름 검색에서 안 쓸 `$n`을 push해 `bind message supplies N parameters` 500이 나던 버그를 개발 중 실측·수정). 회귀가드 `tests/reviewerDbTab.test.js`(라우터 스택 실검사 + 케이스 자리표시자 검증, `PGTEST_URL` 있으면 **진짜 PG로 실행**).
- **부품4h 작업오더·모집공고 탭(상단탭)**: 리뷰웹시스템[3버전]에서 작업오더 접수·상태변경과 모집공고 발행·수정·관제를 그대로 한다.
  - **권한 2단**: **열람 = 내부인 전원**(master/admin/staff — 광고주 차단, `internalMiddleware`), **편집 = 이름 명단**(`workdesk_editors`, migration 079 · `utils/workdeskEditors.js` · `editorOnlyMiddleware`). 작업오더 접수는 시트/탭을 `tab_configs`·`campaigns`에 등록하는 **단일 관문**이고 공고 발행·수정은 정원·금액을 바꾸므로, 보는 사람 전부에게 열 수 없다(사용자 확정). ★ **master 는 명단 무관 항상 허용**(명단 오설정 잠금 방지), ★ **명단 조회 실패는 읽기 전용으로 수렴**(fail-closed — 편집은 되돌리기 어려우니 모르면 안 연다), ★ 명단에 없는 admin 은 **이 화면만** 읽기 전용(관리자 대시보드는 종전대로). 명단 관리는 master/admin 전용 UI(`openEditorList`)이고 **후보는 인트라넷 직원DB 자동완성**(`GET /api/trackb/intranet/users` 재사용). 제거는 소프트(이력 보존), 변경 즉시 캐시 무효화.
  - ★★ **사본 금지 — 관리자 대시보드와 같은 코드**: 공고 카드는 `campaign-cards.js`, **발행·수정 모달은 `js/recruit-modal.js`**(admin.html 인라인 301줄 마크업 + CSS를 **공유 모듈로 이관** — 두 화면이 `#recruitModalMount`에 같은 것을 마운트), 목록·저장 로직은 `index-recruit.js` 그대로 호출. `index-recruit.js`의 하드코딩 경로 10곳은 `_campApi` + `window.CAMPAIGN_ADMIN_API` 로 추상화 — **Track B 네임스페이스를 admin 과 같은 모양**(`/campaigns/list`·`/campaigns/create`·`/campaigns/:id/flags`…)으로 맞춰 **베이스 문자열 치환만으로** 재사용한다(AE 는 `/api/campaign/admin/*` 에 403이라 필수). 서버도 로직 복제 0 — `_delegate`가 기존 order/campaign 핸들러를 그대로 태우고, **대상 라우트가 사라지면 부팅 시 throw**(조용한 404 금지).
  - ⚠ 모달 추출의 영향: `admin.html`의 `rf_*`를 읽던 회귀가드 9종을 "모듈+admin.html"로 넓혔다(검사 의미 불변). 회귀가드 `tests/workdeskOrdersCampaigns.test.js`(라우터 스택 실검사·fail-closed·사본 금지·펼침행).
  - **작업오더 상세 = 팝업 아닌 펼침행**(행 클릭 → 그 행 바로 아래 `colspan` 행, 리뷰어 로그 탭과 같은 방식). 본문은 **`js/work-order-detail.js` 공유 렌더러**(`_woDetailHtml` — `index-app.js`에서 이관) = `▶ 라벨 : 값` + URL 링크화 + **첨부 이미지(guide-image 프록시·Drive) `<img>` 임베드 + 클릭 라이트박스**(`woImageModal`). admin·admin-siand·workdesk 세 화면이 같은 모듈을 로드한다(사본을 두면 "관리자엔 이미지가 뜨는데 작업보드엔 URL만 뜬다"). 상세는 **열 때 한 번만** 그린다(`dataset.filled` — 30건 동시 이미지 로드 방지). 접수·상태변경은 `_woEditActions` **한 곳**에서만 만들어 표 셀·펼침 패널이 같은 게이트를 쓴다.
  - ★★ **프론트 코드를 모듈로 뺄 때의 실측 함정 2종**(회귀가드가 고정): ① **클래스 이름 충돌** — `.worow`/`.wodetail`/`.wochip`은 이미 작업보드 탭 '작업세부' 패널이 쓰고 있어, 그쪽 `display:grid`가 새 표의 `<tr>`에 걸려 **8칸이 2열 그리드로 접혔다**(표가 통째로 무너짐) + `.wochip` 재정의가 그쪽 칩 색까지 바꿨다 → `wol` 접두로 분리하고 **기존 소유자 규칙 생존·중복 정의 0**을 가드가 확인. ② **함수 범위 오절단** — 한 줄짜리 `function _woNormName(v){…}` 다음 함수까지 딸려와 `woCreateCampaign`(admin 전용)이 모듈에 갇혔고, 공개 목록에 없어 **admin의 '공고 발행' 버튼이 조용히 고장**났다 → 모듈이 관리자 전용 전역(`showToast`·`openRecruitModal`·`_woCache`…)을 참조하면 실패하는 검사 + 모듈 내부 미해결 `_wo*` 참조 0 검사.
  - ★ **추출의 무회귀 증명 방법**(다음에도 이렇게): 같은 픽스처를 `origin/main`과 작업본에 각각 넣어 **실제 브라우저에서 렌더 결과를 바이트 비교**한다(빈 입력·HTML/XSS 케이스 포함) + 이동 함수를 쓰는 소비처 전부를 같은 방식으로 대조 + **모듈만 단독 로드**(호스트 전역 0)해 미해결 참조를 잡는다. grep 가드만으로는 셋 다 못 잡는다.
  - **처리 열 재설계(2026-08-04 사용자 신고)**: ① 상태변경이 `prompt` 로 **영문 상태값**(submitted/reviewing/…)을 타이핑시켜 비개발자는 바꿀 상태를 알 수도 바꿀 수도 없었다 ② 행을 누르면 상세가 펼쳐지는데 [상세] 버튼이 또 있었다 ③ 접수하기/접수완료가 안 갈렸다 ④ 작업오더에서 모집공고로 가는 다리가 아예 없었다.
    - ★★ **상태 이름·전이표·접수 판정·발행 프리필은 공유 모듈(`js/work-order-detail.js`) 한 벌**로 모았다(관리자 대시보드 `index-app.js` 에 있던 `WO_LABELS`/`WO_COLORS`/`WO_TRANSITIONS`/`WO_DELIVERY_MAP`/`WO_CHANNEL_HOSTS`/`_woChannel(FromUrl)`/`_woOptionRows`/`_woFirstProductInfo`/`_woBuildInflowHtml`/`_woPlainGuideToHtml`/`_woReviewImgHtml` 이관 + 신설 `_woCampaignPrefill`). 사본을 두면 같은 상태가 한쪽에선 "검토중" 다른 쪽에선 "접수됨"이 되고, 두 화면이 **서로 다른 프리필로 공고를 만든다**. 라벨은 **접수 관점 한국어**로 통일(`submitted=접수대기`·`reviewing=접수완료`·`published=공고발행`) — 관리자 대시보드 표기도 함께 바뀐다.
    - ★★ **화면 전이표 ⊆ 서버 `ORDER_TRANSITIONS`** — 화면이 제시한 버튼이 서버에서 400 으로 튕기면 안 된다. 회귀가드가 **두 파일에서 표를 실제로 읽어 대조**한다(grep 으로는 값 대조를 못 한다). 상태 변경 모달은 그 표가 허용한 상태만 한국어 버튼으로 내고 각 버튼에 `WO_STATUS_HINTS` 한 줄(“누르면 무슨 일이 일어나는가”)을 붙인다. ★ `reviewing` 힌트에 **"탭 등록은 [접수하기] 버튼이 한다"** 를 명시 — 수동 전이로 접수가 된 줄 알면 리뷰어 검색이 안 열린 채 방치된다.
    - ★ **접수하기 ↔ ✓ 접수완료**(초록·`disabled`·등록된 탭 이름 툴팁): 판정은 `_woAcceptable`(= 서버 `ACCEPT_ELIGIBLE`) — 화면에서 다시 세지 않는다. **눌러도 아무 일 없는 버튼은 두지 않는다.** [상세] 버튼은 제거(행 클릭과 하는 일이 겹친다).
    - ★ **모집공고 즉시 미리보기**: 연결 공고 없으면 `📢 모집공고`(공유 프리필로 발행 모달) / 있으면 `👁 모집공고`(그 공고를 수정 모드로) — 어느 쪽이든 모달 오른쪽에 **리뷰어가 참여 후 보는 실제 화면**이 그려진다. 그래서 workdesk 에 `js/campaign-workdetail.js` 를 **`index-recruit.js` 보다 먼저** 로드한다(없으면 `_renderPreview` 가 `window.CampWorkDetail` 체크에 걸려 **그 카드만 조용히 비어** 보인다 — 실측).
    - ★★ **SSO 토큰 격리 때문에 경로 2개를 더 재기준**: ㉮ 연결 탭 목록 `window.RECRUIT_TABS_API`(기본 `/api/tab/dashboard` → workdesk 는 `/api/trackb/tabs`) — 없으면 연결 탭 드롭다운이 **빈 채로** 열린다. ㉯ 공고 상세(수정 프리필)는 원본이 **무인증 공개** `GET /api/campaign/:id` 라 인트라넷 토큰이 **무시되어 공개 화이트리스트 뷰**가 온다(401 이 아니다) → 모달이 조용히 빈 칸으로 열려 "저장했더니 값이 날아간" 것처럼 보인다. 같은 핸들러를 태우는 `GET /api/trackb/campaigns/:id`(authMiddleware+internal)를 신설해 재기준. ★ `/campaigns/list` 가 `/campaigns/:id` **앞에** 등록돼야 목록이 삼켜지지 않는다(회귀가드가 스택 순서를 고정).
    - ★★ **편집 권한자에게는 전체 편집 페이로드**(코드리뷰 P1): 편집 허용명단에는 **`staff`(AE)도 들어갈 수 있는데**, 원본 상세 핸들러의 `_isAdminReq` 는 JWT role 이 admin/master 일 때만 전체 행을 준다 → staff 편집자는 **수정은 되면서** 공개 화이트리스트 뷰를 받아 폼이 `work_detail`·연결탭·정원·옵션을 **빈 기본값으로 초기화**하고, 저장하면 기존 설정이 조용히 `0`·빈값·`options:[]` 로 **지워진다**. Track B 상세 라우트가 `canEdit` 이면 **서버 코드만 세우는 신뢰 플래그 `req._trustedAdminView`** 를 세워 위임하고 `_isAdminReq` 가 그것을 인정한다. ★ 요청(헤더·쿼리·본문)으로는 만들 수 없고, 프로토타입 오염 대비로 **`hasOwnProperty` 자기 프로퍼티 검사**까지 한다. ★ 판정 실패는 공개 뷰(fail-closed) — 편집은 서버 게이트가 막는다. ★ 플래그를 세우는 곳은 **라우트 한 곳뿐**(회귀가드가 전 라우트 파일에서 개수 1을 고정 — 늘리면 권한 상승 경로가 는다).
    - ★ 리뷰어 화면 미리보기(`campaign.html?preview=1`)는 `/api/campaign/admin/:id/preview` 를 먼저 치고 **401/403 일 때만** `/api/trackb/campaigns/:id/preview` 로 한 번 더 시도한다(그 외 오류까지 재시도하면 실패 원인이 가려진다).
    - 회귀가드 `tests/workdeskOrderActions.test.js`(표 대조·사본 부재·라우터 스택 실검사 + **스텁 pool 로 상세 핸들러 실제 호출**(전체/공개/오염 3분기), 변이시험 검출 확인) + **실제 http 오리진 브라우저 실행검증 시나리오**(버튼 분기·모달 렌더·프리필·미리보기 카드·JS 오류 0). 프리필 이관은 **`HEAD` 의 인라인 조립과 같은 입력에 대해 결과 JSON 이 바이트 동일**함을 vm 으로 대조해 무회귀를 증명했다.
  - **부품4i C/S 문의창구 탭(상단탭)**: 관리자 대시보드의 리뷰어 1:1문의 화면을 리뷰웹시스템[3버전]에서 그대로 쓴다. **master/admin 전용**(`adminOrMasterMiddleware`) — 문의 본문에 리뷰어 실명·연락처·주소·주문정보가 그대로 실려 AE의 "담당 탭" 스코프로 나눌 수 있는 데이터가 아니다(등록리뷰어DB와 같은 판단, 기존 `cs.routes.js` 정책 승계). 서버는 `/api/trackb/cs/*` **7경로 프록시**(`_delegate(_csRoutes, …)` — SQL·로직 복제 0)뿐이고 원본 `/api/cs/*`는 무변경. Track B 경로에 두는 이유 = 인트라넷 SSO 토큰(`via:'intranet'`)이 `/api/cs/*`에 **도달 자체가 불가능**하기 때문.
  - ★ **화면은 `js/cs-inquiry.js` 공유 모듈 한 벌**(마크업 + 로직 전부 — admin.html 인라인 + index-app.js 에서 이관). admin·admin-siand·workdesk 세 화면이 `#csInquiryMount`에 같은 판(`#tab-cs-inquiry`)을 마운트한다. 경로 차이는 **`window.CS_API_BASE` 재기준**(`api.js` `_resolveApiPath` — `/api/cs/`로 시작하는 경로만, 전역 미설정이면 동작 불변)으로 흡수한다. 회귀가드 `tests/csInquiryTab.test.js`.
    - ★★ **이미지 교체요청 = 이 화면 위의 "처리 대기 큐" 띠**(전용 상단탭 폐지, 시안 C 확정 · `frontend/docs/design-cs-review-edit-merge.html`): 위 = 주황 띠(교체요청) / 아래 = **지금 그대로의** 문의 화면. 두 기능을 **영역으로** 구분한다 — 목록에 섞으면(시안 B) 이미지 대조 폭이 대화창 안으로 줄고, 서브탭(시안 A)은 클릭이 하나 늘어난다. 띠는 3모드(`rail` 대기 건만 가로 · `full` [모두 보기]=옛 전용탭 화면 그대로(상태 필터+카드 격자) · `collapsed`), **대기 0건이면 자동으로 한 줄**(`.q0`)로 줄어 문의 화면을 안 가린다. 메뉴 이름은 **`C/S`**(문의+교체요청을 함께 담으므로 "문의"를 뺌).
    - ★ **nav 뱃지 = 합계 한 숫자**(사용자 확정 — 문의 미확인 + 교체요청 대기, 예 5+11 → `🔴16`): 두 값은 **서로 다른 시점에 따로 도착**하므로 `_NAVCS{cs,re}`에 **각각 기억해 두고 합계만 그린다**(`_renderCsNavBadge`) — 한쪽만 들어왔다고 다른 쪽을 0으로 접으면 숫자가 들쭉날쭉해진다. **둘 다 null이면 그대로 둔다**(조회 실패로 뱃지가 사라지면 "손볼 것 없음"으로 오독). AE는 문의를 조회하지 않아 `cs`가 계속 null = 교체요청만 합산. `logout`이 `_NAVCS`도 비운다.
    - ★★ **확대 보기(`zoomPair`)는 두 장을 동시에 드러낸다**(실측 신고): 클릭한 쪽은 카드 썸네일로 캐시돼 즉시 뜨는데 '기존'은 그 자리에서 처음 받아, 그냥 두면 한 장이 뒤늦게 끼어들며 레이아웃이 흔들린다(대조하려고 연 화면인데 비교 시작 시점이 어긋난다) → `opacity:0`으로 시작해 **둘 다 `_painted`(load + `decode`)가 끝난 뒤 한꺼번에** 표시(`_revealTogether`), 그동안 "이미지 불러오는 중…" 표기. ★ `decode`까지 기다린다 — `load`만 보면 디코딩 지연으로 여전히 늦게 그려진다. ★ **fail-open**: 한 장이 `error`여도 진행하고 `_PAIR_WAIT_MS`(4s) 뒤엔 무조건 표시(검은 화면으로 남지 않게). ★ 이 회귀는 정적 grep으로 못 잡는다 → 가짜 img로 **한 쪽만 load를 발생**시켜 그때까지 아무것도 안 보이는지 확인하는 런타임 가드(변이시험으로 실제 검출 확인).
    - ★★ **권한 경계가 "메뉴가 보이나" → "AE 화면에 문의 본문을 그리나"로 옮겼다**: 교체요청은 AE도 처리하므로 **C/S 메뉴가 AE에게도 보인다**. 문의 본문은 여전히 master/admin 전용이고(서버 `adminOrMasterMiddleware` 무변경), AE 화면에는 `renderCsView`가 **`CsInquiry.mount`를 부르지 않는다**(그리지 않으면 요청도 안 나감 + 최상단에 "관리자만 열람" 안내). AE는 띠가 화면 전부라 `full` 모드로 연다. ★ 이 판정은 grep 으로는 스코프·실행경로를 못 보므로 **`renderCsView`를 역할별로 실제 실행**해 mount/목록조회 횟수를 세는 가드로 고정(`tests/reviewEditCsBridge.test.js` §8).
    - ★★ **띠가 먹은 세로만큼 아래 문의 화면을 줄인다**(`_reFitCsHeight`) — 안 하면 답장 입력칸이 화면 밖으로 밀린다. 모듈의 대화 영역 높이는 **인라인 스타일** `calc(100vh - 250px)`라 클래스 CSS로 못 이기고, **공유 모듈을 고치면 관리자 대시보드(띠 없음)까지 같이 줄어든다** → 이 화면에서만 실측 높이를 빼서 인라인 값을 다시 쓴다(모드 전환마다 재계산, `100vh`는 그대로라 창 크기 변화는 브라우저가 따라감).
    - ★ 옛 뷰 이름 `reedit` 은 `switchView` 진입부에서 **`cs` 로 한 번 갈아끼운다**(히스토리·북마크가 빈 화면으로 떨어지는 것 방지, 이때 `full` 로 펼침). 처리 후 목록 갱신(`CS_REVIEW_EDIT_ON_RESOLVED`)은 **뷰 이름이 아니라 `#reQueueMount` 존재**로 판정 — 이름으로 보면 이런 통합 때 갱신이 조용히 멈춘다. 승인·반려·카드는 여전히 **C/S 대화창과 같은 코드**(`cs-review-edit-card.js` + `cs-inquiry.js` 핸들러). `logout`은 `re*` 상태도 비운다(목록에 리뷰어 실명·연락처가 남아 다음 로그인 계정에 stale 렌더되는 것 차단).
  - ★★ **`escHtml` 은 호출 시점에 찾는다**(실측 함정): 공유 모듈은 `index-app.js` **앞에** 로드되므로 로드 시점에 `window.escHtml`을 캡처하면 항상 폴백이 잡힌다. 폴백이 원본과 한 글자라도 다르면(`String(s)` vs `s==null?'':s`, 작은따옴표 이스케이프 유무) **관리자 화면 출력이 조용히 달라진다** → `work-order-detail.js`·`cs-inquiry.js` 둘 다 `escHtml(s)` 함수로 감싸 호출 시점에 해석하고, 폴백은 `index-app.js` 구현과 **동일**하게 맞췄다.
  - ★★ **호스트 연동은 "밖에서 감싸기"가 아니라 "안에서 훅 부르기"**(코드리뷰 지적으로 잡은 실측 버그): 모듈 내부 호출(`loadCsRooms`·`csRefreshBadge`)은 **렉시컬 스코프의 `csUpdateBadge` 를 직접** 부르므로, 호스트가 `window.csUpdateBadge` 를 감싸도 래퍼는 **한 번도 안 불린다** → 리뷰웹시스템[3버전] nav 뱃지가 부팅 시각 값에 영영 머물렀다. 모듈이 `window.CS_ON_BADGE(count)` 훅을 **안에서** 부르고 호스트가 등록한다(훅 미설정 = 관리자 대시보드 동작 불변). `#csInquiryBadge`가 없어도 훅은 불려야 하므로 `if(!b) return` early return 도 제거. ⚠ **내 회귀 테스트가 이걸 놓쳤다** — 뱃지에 "숫자가 있는지"만 봐서 부팅값이 남아 있는 것을 통과로 읽었다. 지금은 부팅값(3)과 목록값(7)을 **다르게** 두고 7로 바뀌는지 본다(약한 단언은 잘못된 이유로 통과한다).
  - ★ **리뷰웹시스템[3버전]에는 SSE 가 없다** → 부팅 1회 조회만으로는 그 뒤 들어온 문의가 영영 안 보인다 → `CS_BADGE_POLL_MS`(60초) 주기 갱신 + `visibilitychange`(가려지면 쉬고 복귀 시 즉시 1회). 관리자에게만 돈다. 폰트어썸은 admin 과 **같은 CDN·같은 버전**을 workdesk 에도 로드(공유 모듈이 `<i class="fas">`를 쓴다).
  - ★★ **모달을 모듈로 뺄 때의 3대 함정**(실측 사고 — 모달이 오버레이가 아니라 **페이지 하단 일반 블록**으로 흘렀다): ① **CSS를 한 줄 어긋나게 잘라** 주석 짝이 깨지고(`/*` 한가운데서 시작) 뒷 줄의 남의 규칙(`.dash-filter-btn`)·여분 `}`까지 딸려와 중괄호가 32 대 33 → **브라우저가 에러 없이 조용히 파싱을 포기**해 `.rf-split{display:flex}`가 통째로 죽었다. ② **마운트 지점이 스크롤 컨테이너 안**(`.ovwrap`)이면 오버레이가 화면 흐름에 섞인다 → **body 직속**(모듈이 마운트 지점 없으면 스스로 만들고, `<head>` 로드 시엔 DOM 준비 후 재시도). ③ **호스트 테마 의존** — `var(--border)`·`var(--p)`는 `css/index.css`(admin 전용)에만 있어 리뷰웹시스템[3버전]에선 무효값이 된다 → 마크업·CSS의 **모든 `var`에 폴백**, 폰트어썸도 없어 **닫기 ×가 안 보여 모달을 못 닫던 것**을 마운트 시 1회 탐지해 글리프 폴백 주입(`injectIconFallback` — 폰트어썸 있으면 무동작).
  - ★ **grep 가드로는 못 잡는다**(선언은 멀쩡히 '있다') → `recruitModalLayout.test.js`가 **토큰을 세어** 주석·중괄호 균형 / 최상위 선택자 소속(`#recruitModal`·`.rf-*`·`@media`만 — 남의 규칙 혼입 차단) / `var` 폴백 유무 / 핵심 선언 생존을 고정한다.
  - **부품4j 설정 탭(상단탭)**: 관리자 대시보드의 설정 3종을 리뷰웹시스템[3버전]에서 그대로 한다 — **내 닉네임(1:1문의 표시 이름)** · **회사 사업자번호(제공정보) + 현금영수증 발행방법 이미지 4채널** · **리뷰어 소식·공지**. Track B가 기존 관리자 대시보드를 대체하려면 이 설정들도 작업보드에서 끝나야 한다(사용자 확정).
    - **권한 2단**(서버 게이트와 프론트 노출이 1:1): **내 닉네임 = 내부인 전원**(`internalMiddleware` — master/admin/AE). AE도 이미지 교체요청을 처리하면 리뷰어 채팅에 메시지를 남기므로 표시 이름이 필요하다(리뷰어 화면은 `닉네임 || '관리자'` fail-closed라 실명은 어차피 안 나가지만, 관리자 화면 책임추적과 리뷰어 구분을 위해 본인이 정한다). **사업자번호·현영 이미지·리뷰어 공지 = master/admin 전용**(`adminOrMasterMiddleware`, 원본 라우트와 동일) — 전사 설정이고 공지는 리뷰어 홈에 그대로 나간다. AE에게는 **패널 자체를 안 그린다**(최종 방어는 서버).
    - **서버 = `/api/trackb/settings/*` 9경로 프록시**(`_delegate` — SQL·로직 복제 0, 원본 `/api/admin/my-nickname`·`/api/tab/*`·`/api/reviewer/notices/*`는 무변경). Track B 경로에 두는 이유는 C/S와 같다: 인트라넷 SSO 토큰(`via:'intranet'`)은 그 원본 경로들에 **도달 자체가 불가능**하다. ★ `/settings/guide-image`는 원본이 무인증인데 여기선 **adminOrMaster로 좁힌다**(용도가 현영 이미지 업로드 하나뿐 — 프록시가 원본보다 넓어지면 안 된다).
    - ★★ **화면은 `js/admin-settings.js` 공유 모듈 한 벌**(admin.html 인라인 마크업 + index-app.js 로직에서 이관). `admin.html`(설정탭 `#adminSettingsMount` + 대시보드 `#rvNoticeMount`) · `admin-siand.html`(닉네임만) · `workdesk.html` 셋이 같은 것을 마운트한다. 경로 차이는 **`window.ADMIN_SETTINGS_API` 재기준**(`CAMPAIGN_ADMIN_API`와 같은 장치, 전역 미설정이면 기존 경로 = admin 동작 불변)으로 흡수.
    - ★ **gasGet/gasPost를 쓰지 않는다** — 액션맵(`api.js`)은 경로가 고정이라 Track B로 재기준할 수 없다. 모듈이 **논리 키 → 경로** 표(기본값 8 + 접미사 8)를 들고 직접 fetch 한다(회귀가드가 두 표의 키 일치를 고정 — 한쪽만 늘리면 404).
    - ★★ **호스트 테마 의존을 "마운트 시 1회 감지"로 흡수**: 마크업은 `var(--t1)`·`class="admin-section-header"`를 **원본 그대로** 두고, `getComputedStyle(documentElement)`에 `--t1`이 없으면(=리뷰웹시스템[3버전]) 마운트 지점에 `as-standalone` 클래스를 붙여 같은 토큰과 헤더 레이아웃을 주입한다. 그래서 **admin.html 렌더 결과가 한 바이트도 안 바뀐다**(recruit-modal.js 때처럼 마크업 전체에 `var` 폴백을 박으면 원본과 달라져 대조가 불가능해진다).
    - ★ **현영 4칸은 `CR_GUIDE_CHANNELS` 표에서 생성**(손으로 적어 둔 4칸 → 채널 추가 시 한 칸 누락 사고가 실제로 있었다). 표 ≡ 서버 `utils/cashReceiptChannels.js` 는 기존 가드가 이미 고정하므로 슬롯 존재가 따라온다.
    - ★ **닉네임 저장 후 열린 문의방 갱신은 훅으로**: 활성 threadId 는 `cs-inquiry.js` 모듈 스코프라 밖에서 못 읽는다 → 그 모듈이 `csReloadActiveConversation` 을 노출하고 설정 모듈이 부른다(전역 사본을 두면 두 값이 갈라진다).
    - **로드 타이밍 불변**: admin.html 은 `autoload:false` 로 마운트하고 값 불러오기는 종전대로 `switchAdminTab` 시점에 한다(리뷰웹시스템[3버전]만 마운트 즉시 로드). 폭 상한은 `#sthead .mh`·`#adminSettingsMount` 둘 다 **1120px**(헤더·본문 같은 값 — 위 규칙).
    - **검증**: `origin/main` 의 인라인 마크업과 모듈 생성 마크업을 **실제 브라우저에서 DOM 정규화 비교**해 동일함을 확인했고(두 블록 모두 일치), 실제 http 오리진에서 admin.html·workdesk.html 을 각각 띄워 기존 경로/Track B 경로·저장 페이로드·역할별 패널 노출을 확인했다. 회귀가드 `tests/workdeskSettingsTab.test.js`(라우터 스택 실검사 + 사본 금지 + **전역 0인 가짜 DOM 에서 mount 실제 실행**).
    - ★★ **화면 구조 = 좌측 목차 + 한 번에 한 묶음**(시안 B 확정, 와이어프레임 `frontend/docs/design-admin-settings-wireframe.html` `?v=B` · 색 시안 `design-admin-settings-ui.html`). 배경도 흰색·내용도 흰색이라 묶음 경계가 `<hr>` 1px 뿐이고 이미지 업로드 슬롯 13칸(현영 발행방법 4 + AI 예시 9)이 같은 모양으로 늘어서 번잡하던 것을 고쳤다. **저장 로직·엔드포인트·필드 ID 는 무변경** — 겉모양만.
      - ★★ **패널은 전부 DOM 에 그려 두고 보이기만 토글한다**(`asSelectPanel` — 안 그린 패널은 id 가 없어 로더가 조용히 no-op 이 되고, admin.html 은 탭 전환 때 로더를 **한 번만** 부르므로 나중에 그 묶음을 열면 값이 빈다). 회귀가드가 이 불변식을 고정.
      - ★ **목차는 패널 2개 이상일 때만** — 1개 마운트(대시보드 공지 카드 `#rvNoticeMount`·AE 닉네임)는 종전 그대로 그린다(공지 패널은 자기 테두리를 유지, B 안에서는 패널이 곧 카드라 `.as-b .as-noticebox` 로 벗긴다).
      - ★ **목차 배지 = 등록 현황**(`미설정`·`2 / 4`·`1 / 13`·`15열`·`2건`) — 화면을 열기 전에 비어 있는 설정이 보인다. 값은 **이미 받아 둔 응답에서 세기만** 한다(신규 API 0, `_setNavBadge`). 작업표 배지는 `_wtRenderCols` 안에서 갱신 — 로더에만 두면 열을 더한 뒤 숫자가 옛값에 머문다.
      - ★ **이미지 슬롯은 목록형 한 벌**(`.as-slot` — 발행방법·AI 예시 공용): 썸네일 52 + 이름 + 상태칩 + 버튼. 날것의 `<input type=file>` 은 `.as-file` 로 감추고 `<label for>` 로 누른다 — **id·onchange 경로는 그대로**(회귀가드가 id 를 본다). AI 예시 줄에는 부가설명을 두지 않는다(13번 반복되면 그 자체가 소음).
      - ★★ **패널 머리 정규화 필수**: admin.html 의 `.admin-section-header` 는 **sticky + 좌우 `-16px` 풀블리드 + 페이지색 반투명 배경**(탭 전체를 가로지르는 머리띠로 설계됨)이라, 흰 패널 안에 그대로 두면 회색 띠가 패널을 뚫고 나가고 스크롤 시 화면 위에 붙는다 → `.as-b .admin-section-header` 로 평범한 제목 줄로 되돌린다(as-standalone 규칙보다 **뒤에** 둬야 이긴다).
      - 마지막으로 본 묶음은 `sessionStorage.as_panel` 로 기억(저장하러 다시 들어오는 화면이라 매번 첫 항목으로 되돌아가면 같은 클릭을 반복한다). 목록에 없는 키면 첫 항목.
- **인트라넷 SSO → 리뷰웹시스템[3버전]**(세션 이어받기): 인트라넷 로그인 성공 시 클라이언트(auth.js)가 `POST /api/admin/intranet-login`(전용 리미터 10/분)으로 리뷰 토큰을 발급받아 `localStorage.review_sso` 저장 → 사이드바 "리뷰웹시스템[3버전]" 버튼이 `workdesk.html#sso=<token>` 프래그먼트 핸드오프(도착 즉시 저장+해시 제거). `loginIntranet`은 인트라넷 서버 `/api/auth/login`(D1 서버측 대조)에 자격 프록시 재검증(비밀번호 결속·fail-closed, **`X-Api-Key=INTRANET_API_KEY` 서버간 인증 헤더 필수** — 인트라넷 API 가드 활성 시 키 없는 서버간 호출은 Unauthorized로 차단돼 SSO 토큰 발급이 실패하므로 trackB 프록시와 동일 채널로 전송, 미설정=구버전 호환 미전송) 후 발급 — **권한은 기본 'staff', 'admin'(전체 탭 열람·편집) 승격 = `INTRANET_SSO_ADMIN_USERS`(csv, 인트라넷 username) 또는 `INTRANET_SSO_ADMIN_GROUPS`(csv, '부서' 또는 '부서|파트' — 예 "콘텐츠운영팀|체험단파트", 로그인 시점 직원DB의 department/part 판정=파트 전원 자동 승격·인사이동 반영, part 미포함 구버전 응답은 fail-closed=과승격 방지)**(인트라넷 role 필드는 자기신고라 불신뢰 — 사용자 확정 정책 1a; department/part는 인사DB=관리자 관할이라 그룹 승격 근거로 사용), **진입 게이트 우선순위: `INTRANET_SSO_ALLOWED_GROUPS`(csv, '부서' 또는 '부서|파트' — 예 "AE,콘텐츠운영팀|체험단파트", 로그인 시점 직원DB 판정=개인 수동등록 불필요·인사이동 자동 반영, TRIM·대소문 무시 정확일치, part 판정은 inadd#164 배포 후) → 미설정 시 `INTRANET_SSO_ALLOWED_ROLES`(csv, 인트라넷 role) → 둘 다 미설정=전 직원**(지정 admin 계정은 게이트 무관 항상 통과). `name=display_name`(inad_pm 매칭 키, 인트라넷 실명은 관리자 관할 가정)·`iu=username`·`ir=인트라넷 role`(감사). ★★**`via:'intranet'` 토큰은 authMiddleware에서 `/api/trackb/*` 전용으로 격리**(외부 발급 신원이 Track A authMiddleware-only 파괴 라우트에 도달하는 폭발반경 차단). **staff 초기매핑 스코프**: 소유지정 뷰 접근 + `tabs?forMapping=1`(staff만 전체 탭명·advertiser 무시) + `POST /api/trackb/advertisers`(staff는 inad_pm=자기 로그인명 강제·23505→409) + ownership 쓰기는 자기 담당(inad_pm) 업체 & **무소유(또는 전부 자기 소유) 시트만**(`sheetAssignableByStaff` — 타 AE 시트로의 자가 스코프 확장 차단, 재배치는 admin 소관). 회귀가드 `tests/trackBIntranetSso.test.js`. ⚠ **인트라넷(inadd-webapp) 배포 정정(2026-08-04 실측)**: 앱 코드는 **`main` 머지 = 자동배포**다(`.github/workflows/deploy.yml` → `cloudflare/wrangler-action`, `pages deploy dist --project-name intranet --branch main`) — "wrangler 수동 배포 필요"는 옛 기술. 다만 **D1 마이그레이션은 여전히 수동**(그 워크플로가 의도적으로 제외 — 운영 `d1_migrations` 추적 테이블이 실제 스키마와 어긋나 `d1 migrations apply --remote` 가 실패한다): `npx wrangler d1 execute inadd-production --remote --file=./migrations/<파일>.sql`.
- **부품4b 작업오더(발주) 연동**(adminOrMaster · readiness d4): workdesk에서 이 탭에 **발주(`work_orders`)를 수동 링크** + **작업세부 표 자동표시** + **명단 골격 준비**. ★★**Track A 무접촉(핵심)**: `work_orders.linked_tab_*` 는 **order.routes 발주 승인(accept) 흐름이 읽어 동작을 분기**한다(비적격 상태+linked_tab 설정 시 승인 멱등 skip, idempotent 판정 `order.routes.js:733,757`) → Track B는 그 컬럼을 **절대 안 쓰고** **전용 링크 테이블 `trackb_work_order_links`(migration 051)** 에 발주↔탭 연결을 저장(`advertiser_campaigns`와 동일 격리 패턴, 탭당 활성 1개 upsert). `linkWorkOrder`/`unlinkWorkOrder`는 이 테이블만 write(work_orders는 존재검증 SELECT만). 작업세부·d4·overview·명단준비의 "유효 링크" = **Track B 링크 우선 → 없으면 `work_orders.linked_tab`(Track A 승인 링크) 폴백**으로 **읽기만**. `GET /work-orders`(드롭다운·링크상태)·`POST /work-order/link`{workOrderId,sheetId,tabName}·`/unlink`{sheetId,tabName}·`/prepare-roster`. 명단 골격은 `prepareRosterSlots`(participants) — 발주 `recruit_count` 기준 **부족분만**(target−현재 활성) 빈 manual 슬롯 gap-fill(멱등·상한 2000·옵션 라운드로빈, seq 레이스 23505 재시도). **주의**: 빈 슬롯(phone8=NULL)은 `overview.b_total`에 잡혀 A 대비 인원 초과로 보일 수 있으나 `classifyParity`가 phone8 없는 행을 걸러 **진짜불일치(real)엔 안 잡힘 → cutover 게이트 무영향**(채워지면 정상화). `workdeskTab` detail이 발주 전체 필드(옵션목록·유입·가이드·특이사항·상품URL 등) 반환. 회귀가드 `tests/trackBWorkOrder.test.js`(work_orders 무접촉 assert 포함).
- **부품4c 탭 스레드(협업+내부메모, P1 · migration 053)**: 관제실 시트의 협업(브랜드사 확인요청·양사 코멘트)+탭별 내부 메모를 탭 컨텍스트 **한 타임라인**으로. `trackb_tab_threads`(kind `comment`|`request`, `internal_only`, status open→confirming→done) + `trackb_thread_seen`(미확인 배지). **광고주 노출 게이트**: `listThread`/`unseenCounts`가 role='advertiser'면 `internal_only=FALSE` **서버 필터**, `addThread`는 광고주 internal 강제 FALSE. **교차 테넌트 방어**: 모든 mutation은 `_ensureThreadScope`(canAccessTab (sheetId,tabName)) + 서비스가 UPDATE/DELETE를 `(sheet_id,tab_name)`에 결속(B1 IDOR 봉합), `unseenCounts`는 staff/advertiser를 `scopedActiveTabs` 소유 탭으로 서버 강제(B2 메타유출 봉합). `GET/POST /workdesk/thread`·`/:id/status`·`DELETE`·`POST /workdesk/unseen`. 프론트 = 탭 상세 스레드 섹션(확인요청/내부만 토글·상태버튼) + 작업목록 💬 배지. 회귀가드 `tests/trackBTabThreads.test.js`.
- **부품4d 정산 파이프라인(P2 · migration 054)**: 탭 ↔ **인트라넷 계약(`sales` C-번호)/견적(`quotes` Q-번호)** 수동 링크 → **4단계 스텝퍼**(마감자료→견적서→계산서→선금/잔금)를 인트라넷 **HTTP GET 프록시**로 읽어 표시(관제실 시트 정산 컬럼 대체). ★ 인트라넷 D1 무접촉 — 링크는 `trackb_settlement_links`(탭당 활성 1)에만 write, sales/quote는 **읽기만**(`_intranetGet` 5초 타임아웃·fail-soft `proxyDown`, `INTRANET_API_BASE`). **Q4-c 금액 광고주 노출**: `settlementForTab` 역할 렌즈 — 광고주는 `trackb_advertiser_prefs.settlement_visible`(기본 TRUE) OFF면 `{hidden:true}`(프록시 미호출·금액 미포함), ON이면 **그 탭에 링크된 sales 단건만** 프록시(타 업체 계약 도달 불가). 링크/해제=`_ensureEditScope`(내부만), 검색=internalMiddleware, 노출토글=adminOrMaster. 프론트 = 정산 스텝퍼 카드+계약 검색 모달, 소유지정에 "정산 노출" 토글. 회귀가드 `tests/trackBSettlement.test.js`. **오링크 방어**: 링크 시 계약 업체명 로그·반환. 견적서(②)는 `quotes.sales_id` 역파생(`_quoteForSales`), sales 단건 20초 캐시(`_salesById`), 광고주 렌즈는 linkedBy·담당자 미노출.
- **부품4d-2 계약 "연결" → 계약 "매칭"(작업명 유사도 자동 추천 · 스키마 변경 0)**: 정산의 계약 붙이기가 **전체 계약 자유검색**이라 ① 담당자가 매번 C-번호를 찾아 입력해야 했고 ② **타 업체 계약을 남의 탭에 붙이는 오링크가 구조적으로 가능**했다 → 후보를 **그 작업을 소유한 업체(광고주DB)의 계약**으로 좁히고, 작업명과 가장 닮은 계약을 **추천**한다.
  - ★★ **판정 단일 출처 = `utils/contractMatch.js`**(`parseTabName`·`similarity`·`scoreContract`·`rankContracts`, 순수함수 — DB·네트워크·`Date.now` 미접근). 예) `7/21파미고_올레샷_블로그 바이럴` → 날짜 `7/21` · 업체 `파미고`(`sales.business_name`) · 브랜드 `올레샷`(`brand_product`) · 작업 `블로그 바이럴`(`contract_detail`·`product_name`) 네 조각을 각각 대조. 가중치 = 업체 40 / 브랜드 30 / 작업 20 / 진행월 10인데 **적용 가능한 항목만으로 정규화**한다(계약에 브랜드가 안 적혀 있다고 감점하면 정보가 부실한 계약이 영영 추천되지 않는다).
  - ★★ **추천은 제안까지만, 확정은 사람이**(자동 링크 금지 — 작업표 학습·옵션 자동점검과 같은 규율). 최고점이 `RECOMMEND_MIN_SCORE`(45) 미만이면 **추천 없음**(빈 추천 > 틀린 추천), 최고점 동점이면 `tie` 로 **화면이 그 사실을 말한다**.
  - ★ **프론트에 판정 사본 0** — 화면은 서버가 준 순서·점수·근거 문장을 그대로 그린다(화면에서 다시 매기면 "추천은 A인데 정렬은 B"로 갈린다). 후보 클릭은 **배열 인덱스**로 넘긴다(onclick 문자열에 인트라넷에서 온 값 주입 금지).
  - ★★ **인트라넷 응답을 항상 이름으로 재필터**(`intranetSalesForAdvertiser`): 범용 테이블 API 의 `where` 파서는 `col=value` 한 쌍만 처리하고 값에 `=` 가 섞이면 **절을 통째로 무시해 전체 계약**을 돌려준다 → 서버 필터를 믿으면 남의 계약이 조용히 후보에 섞인다. `business_name`(신형)·`advertiser_name`(레거시) 두 컬럼 조회 → 0건이면 `search=` 폴백, 전부 **정규화 이름 정확일치**로 다시 거른다. 업체별 30초 캐시.
  - ★ **소유 업체 판정 = `advertiserForTab`**(탭지정 소유 > 시트전체 소유 — 작업목록 그룹핑 주석과 같은 규칙, gid 는 `raw_sheet_tabs` 로 재해석). Track B `advertisers.name` 은 인트라넷 `business_name` 과 **정확일치가 등록 게이트로 강제**되므로 그대로 계약 조회 키가 된다.
  - ★ **폴백 사유를 화면이 설명한다**(조용히 전체를 보여주면 왜 남의 계약이 뜨는지 모른다): `no_advertiser`(업체 미지정 → 업체관리 안내) · `no_contracts`(그 업체 계약 0건). **[전체 계약에서 찾기] 탈출구는 유지**(계약이 다른 이름으로 등록된 정상 케이스) — 단 전체 검색에서는 **자동 추천을 내지 않고**(근거 불충분), 다른 업체 계약은 `다른 업체 계약` 배지 + `confirm` 후에만 매칭된다.
  - ★ **오링크는 경고까지만**(하드블록 금지): `linkSettlement` 가 소유 업체와 계약 업체를 대조해 `mismatch` 신호 + `logger.warn` 만 남긴다(막으면 이름이 다른 정상 계약을 영영 못 붙인다). `advertiser_name` 이 레거시 공란인 신형 계약 때문에 **업체명은 `business_name` 우선**으로 읽는다.
  - **API** `GET /api/trackb/settlement/contract-candidates?sheetId&tabName&scope=advertiser|all&q=` — 게이트는 링크와 **같은 `_ensureEditScope`**(후보를 보는 사람 = 매칭할 사람. 목록에 업체명·계약금액이 실리므로 열람 범위를 링크 권한보다 넓히지 않는다). 인트라넷 D1 무접촉(GET 프록시만), 쓰기 표면은 종전대로 `trackb_settlement_links` 하나. 어휘도 화면 전체에서 **계약 매칭 / 다시 매칭 / 매칭 해제**로 통일.
  - **화면 = 시안 C(스코어 레일형, 사용자 확정)** · 시안 문서 `frontend/docs/design-contract-match-modal.html`(A/B/C, `?v=` 단독 보기). 좌측 유사도 레일(숫자+막대) + **계약명이 헤드라인**(계약상품상세 → 상품명 → 브랜드·상품 순 대체) · 계약금액 · 일치근거 ✓ 박스. ★ **계약고유번호는 배지 줄의 보조 표기** — 회귀가드가 `.lkname`/`.lkcno` 의 크기·굵기로 위계를 고정한다(되돌리면 종전처럼 번호가 제목이 된다). ★ 근거가 없으면 "일치하는 항목 없음"을 **문장으로** 말한다(낮은 유사도를 숫자만으로 흘리지 않는다). ★ 금액은 `amount || contract_amount` 이고 **정산 카드의 총비용(견적서 금액 우선)과 기준이 다르므로** `title="계약금액"` 으로 밝힌다. 행 빌더는 `_lkRowHtml` 한 벌.
  - ★★ **창구는 둘, 팝업과 동기화는 한 벌**: 계약 매칭은 ① 작업보드 정산 카드 ② **업체관리 › 연결된 탭 표의 [계약] 칸**(미매칭이면 [계약 매칭] 어포던스, 매칭됐으면 계약번호 = 클릭 시 다시 매칭) 두 곳에서 시작한다 — 정산을 실제로 챙기는 화면은 업체별 목록이라 거기서 표를 훑다가 바로 붙일 수 있어야 한다. 팝업은 **`openContractMatchModal(ctx)` 한 벌**이고 내부(`_lkLoad`/`pickSales`/`unlinkSettle`)는 **`_lkTab`(팝업 문맥)만** 본다 — `STATE.settle`(작업보드 전용 상태)을 직접 참조하면 업체관리에서 연 팝업이 죽는다. ★ onclick 에는 **인덱스만**(`ownMatchContract(event,${i})`) — 시트에서 온 탭명·계약번호를 문자열로 넣지 않는다(M1 실측 XSS 규율).
  - ★★ **매칭/해제 후 동기화 지점은 `_contractMatchApplied` 하나** — ① 업체관리 연결탭 행(`salesId`·`contractNumber`) 즉시 갱신 + 재렌더 + 정산 요약 재조회 ② **그 탭을 열어 둔 작업보드 정산 카드만** 재조회. 화면마다 따로 갱신하면 "업체관리에서 매칭했는데 작업보드는 미매칭"으로 갈린다. ★ **옛 계약의 요약 캐시(`STATE.ownSettle[키]`)를 반드시 지운다** — 안 지우면 새 계약 줄에 **이전 계약의 견적·입금 금액**이 그대로 남는다. ★ 이 검증은 **문자열 grep 으로 못 한다**(`if(false) delete …` 를 통과시킴) — 가드가 함수를 vm 으로 꺼내 **실행**해 상태 변화를 본다.
  - **어휘**: 화면에서 "연결"은 계약에 쓰지 않는다 — 정산 요약 셀은 `미매칭 · 계약 매칭 필요`, 업체관리 안내는 "계약이 매칭된 탭만 표시". (`발주 연결`·`소유`는 별개 개념이라 그대로.)
  - 회귀가드 `tests/contractMatch.test.js`(판정 순수함수 실행 + 스텁 pool·fetch 로 서비스 실제 호출 + 라우터 스택 실검사 + 행 렌더러 vm 실행 + 프론트 배선·사본 부재 + **동기화 함수 vm 실행** + **그리드 열 수 ≡ 헤더 칸 수**를 세어 고정 — 열을 끼워 넣을 때 가장 흔하게 깨지는 자리, 변이시험 검출 확인) + **브라우저 실행검증**(헤더↔행 열 정렬·업체관리에서 매칭→표 즉시 갱신·작업보드 카드 동기화·해제 왕복).
- **★★ 계약 매칭을 없애는 근본 수정 — 인트라넷 리뷰오더 등록에서 계약건 필수 선택 (migration 088, 사용자 확정)**: 위 계약 매칭은 **작업이 들어온 뒤** 사람이 계약을 찾아 붙이는 사후 보정이다. 인트라넷에서 **오더를 낼 때 계약을 고르게** 하면 그 계약이 작업오더를 타고 넘어와 **접수 시점에 자동 연결**되므로 매칭 단계가 사라진다. ⚠ **계약 매칭 화면은 그대로 남긴다** — 이 규칙 이전에 들어온 과거 작업건을 메우는 용도(사용자 확정: "지금 매칭하는 과정은 직전까지의 작업건들에 계약을 매칭하는 과정").
  - **인트라넷(inadd-webapp)**: 리뷰오더 폼 상단에 `계약건 *` 카드 + 계약 피커(`GET /api/review-order/contracts?scope=mine|all&q=`, D1 읽기 전용). ★ **내 계약 우선 + 전체 검색**(사용자 확정 — 리뷰오더 폼엔 업체 칸이 없어 좁힐 기준이 필요하다). ★★ **견적서 없는 계약은 선택 불가**(사용자 확정) — 단 **목록에서 숨기지 않고** 비활성 줄 + `견적서 없음 · 선택 불가` + [견적서 작성 →]로 보여준다(숨기면 "내 계약이 왜 없지?"가 된다). ★ 안내 문구 = **"상품구입비용이 아직 확정되지 않았다면 견적서를 먼저 작성해 계약을 만들고, 확정된 뒤 견적을 보정하면 된다"**(사용자 확정 — 견적 후보정이 정상 흐름임을 화면이 말해야 한다).
  - ★★ **서버가 최종 방어**(프론트 우회 차단): `POST /api/review-order` 가 `sales_id` 필수 + **계약 실존 + 견적서 존재**를 D1 에서 재검증하고, `contract_number`·`quote_id` 는 **클라이언트 값을 믿지 않고 계약 원본에서 다시 채운다**(낡은 화면·조작 요청 차단). 계약 없는 오더는 리뷰웹으로 **보내지 않는다**.
  - **리뷰웹**: `work_orders.sales_id/contract_number/quote_id`(088 — **컬럼 추가만·백필 0·FK 없음**, 인트라넷 D1 무접촉 규율은 054 와 동일). `INTAKE_EDITABLE_FIELDS`·`_insertWorkOrder` 에 합류하고 `REQUIRED_SCHEMA` 에 `work_orders.sales_id` 등록(컬럼 누락 = **인트라넷 오더 접수 전면 42703**). 작업오더 상세에 `계약건` 줄(값 없는 과거 오더는 줄 자체가 안 나옴 = 화면 불변).
  - ★★ **접수(accept) 시 자동 연결**: `o.sales_id` 가 있으면 `trackb_settlement_links` 에 INSERT(`linked_by='자동(작업오더)'`). ★ **이미 링크가 있으면 덮지 않는다**(`kept_existing` — 사람이 고쳐 놓은 매칭을 재접수 한 번으로 되돌리면 안 된다). ★ **fail-soft** — 연결 실패해도 접수는 성공하고 사유만 응답에 싣는다(계약은 나중에 화면에서 매칭 가능). 쓰기 표면은 링크 INSERT 하나(기존 행 UPDATE 없음).
  - ★★ **INSERT 컬럼 수 ≡ VALUES 자리 수 ≡ 파라미터 수**를 회귀가드가 **세어서** 고정한다 — 컬럼을 중간에 추가할 때 가장 흔하게 깨지는 자리다(변이시험으로 검출 확인). ⚠ 이 변경으로 `workOrderAutoFill` 가드의 옛 패턴(`manager_name, work_manager, status, created_by`)이 드리프트해 함께 갱신했다(검사 의미 불변).
  - **배포**: 리뷰웹·인트라넷 **둘 다 `main` 머지 = 자동배포**(인트라넷은 `.github/workflows/deploy.yml` → `cloudflare/wrangler-action` 이 `pages deploy dist --project-name intranet --branch main`). ★ 두 배포는 서로를 기다리지 않아도 된다 — 리뷰웹만 먼저면 계약 필드가 안 실려 올 뿐 종전 동작 그대로이고, 인트라넷만 먼저면 리뷰웹이 `sales_id` 를 버려 자동 연결만 안 된다(**양방향 가산적**).
- **부품4e 마감자료 자동생성(P3 · migration 055)**: `trackb_tab_closeouts`(마감일·건수·제출건수, 이력 보존). `generateCloseout`(활성 명단 `campaign_participants` 집계→INSERT, 재생성 보존·빈명단 거부)·`latestCloseout`(스텝퍼 ① 소스 — 정의됨=정산 `closeoutAvailable` 라이브)·`closeoutCsv`(UTF-8 BOM, 광고주 렌즈 연락처 끝4자리 마스킹). `POST /settlement/closeout`(내부만·_ensureEditScope)·`GET /settlement/closeout.csv`(내부+소유 광고주·PII, 광고주는 정산 노출 OFF면 차단). 프론트 = 정산 스텝퍼 ① "마감자료 생성/CSV/재생성" 링크. Track A·시트 무접촉(명단 읽기+trackb write만). 회귀가드 `tests/trackBCloseout.test.js`.
- **부품4f 소유지정 연결탭 정산 컬럼(migration 056)**: 소유지정 "연결된 탭" 표(15컬럼 그리드)에 관제실 정산 컬럼 대응 — 참여/제출/입금 우측에 **견적서일·마감자료일·계산서일·입금액/총비용·입금일·비고**. `settlementSummaryForAdvertiser`(내부 전용 배치): 정산 **링크된 탭만** 인트라넷 프록시(같은 sales 공유 탭은 1회 조회 — `_salesById`/`_quoteForSales` 20초 캐시 공유), 입금액=`sales.matched_bank_amount`(입금매칭 누계)·입금일=`matched_bank_date`(최근, `payment_date` 폴백, `_normIntraDate` YYYYMMDD 정규화)·**총비용=견적서상 금액 우선**(계약금액 폴백, 견적≠계약이면 `amountMismatch` ⚠ 신호만 — 자동판정 없음), 실패=`proxyDown`(fail-soft). `GET /ownership/settlement`(**internalMiddleware — 광고주 미도달**, 소유지정 뷰 자체가 내부 전용), 비고=`trackb_tab_memos`(056, 탭당 1행 upsert 자유텍스트) `POST /tab-memo`(_ensureEditScope: staff 담당 탭만). `ownedTabsForAdvertiser`가 정산링크·최신마감·비고 LATERAL 동봉(전부 LIMIT 1 — 행 곱증식 없음). 프론트: 정산 셀 비동기 필(`loadOwnSettlement`, 업체 전환 경합 `ownSettleFor` 가드), 비고 인라인 편집(클릭→input→Enter/blur 저장·Esc 취소, 행 클릭과 stopPropagation 격리). 회귀가드 `tests/trackBOwntabSettlement.test.js`.
- **부품5 편집 가능 작업보드(레드-블루-심판)** — `migration 048 participant_edits`: **오버레이-only + read-time 합성**. 편집을 `campaign_participants` 물리컬럼에 **절대 안 쓰고** 별도 원장에 저장 → `workdeskTab`이 읽을 때 합성. **정렬/재투영이 물리행을 덮어써도 편집 무손실·무오염**(교차노출 근본 차단). 편집은 **B 전용 리허설**(구글시트·주문·검색 무접촉).
  - **앵커 우선순위**: `order_submission_id`(불변 UUID) > `manual`(물리행 id, 재투영 면역) > `identity_key`(중복 아니면) > **거부**(`no_stable_anchor`/`ambiguous_identity`, fail-closed). seq(물리행) 앵커 금지 = 정렬 면역. `_deriveAnchor`가 편집/합성/revert 공유.
  - **안전장치**: bool은 `value_bool` 전용 컬럼(캐스팅 예외 차단, `to_jsonb(NULL::bool)=SQL NULL`이라 COALESCE 합성이 FALSE 보존)·부분유니크 `uq_participant_edits_active`(활성 1행 backstop, 23505→409)·단일 tx+대상행 FOR UPDATE·append-only `reverted_at`(감사 이력)·중복 identity read/write 양쪽 게이트·미부착(orphan) 편집 노출(조용한 증발 방지).
  - **동시성**: 편집 뮤테이션 `/workdesk/edit·/revert·/hide·/add·/edits`는 `authMiddleware`+`_ensureEditScope`(master/admin=전체 · **staff(AE)=담당 탭만**(`canAccessTab`) · advertiser=차단). 투영 cron·수동 bulk(`/project`, participants `/sync`)를 `withJobLock('trackb_project')`로 상호배제(멀티인스턴스 이중투영·seen-set 플래핑 차단, 락키 기존 6개와 비충돌). remove=import행 hidden 오버레이/manual행 soft-delete, add=manual 물리행(retry-on-23505로 seq 원자화). 회귀가드 `tests/participantEdits.test.js`.
  - **역할 스코프·AE 접근**: `/workdesk` read = master/admin(전체 PII) · **staff(AE)=담당 업체 탭+전체 PII·편집** · **advertiser=소유 탭 · 주문/배송/정산 화이트리스트 시트컬럼만 전체노출**(번호·구매일자·주문번호·수취인·ID·연락처·주소·결제금액·리뷰제출일·입금일 — `_advertiserColumns` 키워드매핑, ★은행/계좌번호/예금주 등 미포함 컬럼은 `rowJson`에도 안 실어 전송=데이터 최소화·유출차단; '입금'룩업은 계좌/은행 포함 컬럼 부정룩어헤드로 제외; DB 신원열(참여자 이름·phone8)은 광고주 그리드 미렌더, 동명 시트열 '연락처'는 rowJson값 표시(gCellDisp 광고주분기), 읽기전용, reviewer 차단, 회귀가드 `tests/advertiserColumns.test.js`). 광고주는 workdesk 로그인에 `advertiser-login` 폴백으로 진입(소유 탭만) — 광고주 역할은 nav에 작업보드 탭만 노출(관측·소유지정 숨김)·읽기전용(`canEdit=role!=='advertiser'`). **광고주 로그인 계정은 업체별 발급**(`advertiser_users`, `advertiser_id` 연결): 소유지정 업체 패널의 "광고주 계정" 섹션에서 아이디/비번 발급·재설정·활성토글·삭제 → `POST /api/trackb/advertiser-account`(adminOrMaster, auth.service CRUD 재사용). ★ `/api/admin/advertiser-users`와 로직 동일하나 **Track B 경로로도 노출**한 이유 = 인트라넷 SSO 관리자 토큰(`via:intranet`)은 `/api/admin/*` 격리라 소유지정 UI에서 계정 발급하려면 `/api/trackb/*` 경로 필요. **광고주 전용 접속 링크(매직 링크, migration 058 `trackb_advertiser_links`)**: **업체마다 고유 URL 자동 존재**(`ensureAdvertiserLink` = INSERT ON CONFLICT DO NOTHING, 소유지정 진입 시 selAdv가 ensure 호출 → 모든 업체가 항상 URL 보유, 수동생성 불필요). 소유지정 "광고주 접속 링크" 섹션에서 복사/회전/폐기(`POST /api/trackb/advertiser-link` adminOrMaster: get/ensure/generate(회전)/revoke/enable, 업체당 1토큰). 광고주는 `workdesk#a=<token>`(구 `#adv=` 도 계속 받는다) 열면 boot이 `POST /api/trackb/advertiser-link-login`(무인증·`advertiserLinkLimiter` 30/분)으로 `loginByLinkToken`(auth.service) 교환. ⚠⚠ **아래 로그인 토글은 2026-08-23(#1088 + migration 118)에 폐지됐다** — `loginByLinkToken` 은 `login_required` 를 **읽지 않고**, 화면 배지도 `lockOn=false` 고정이라 **링크는 항상 공개**다(회귀가드 `tests/advertiserLinkNoLogin.test.js` 가 부활을 막는다). 컬럼·`login-required` 라우트는 남아 있으나 **아무도 안 읽는 무효 표면**이다. 아래 문단은 그 이전 이력(되살릴 때의 설계 근거)으로만 읽을 것 — ★★ 옛 **로그인 게이트 = 명시 토글(migration 083)**: 링크는 **기본 공개**(링크만으로 advertiser JWT `via:'link'` 입장)이고, 로그인을 요구할지는 `trackb_advertiser_links.login_required`(기본 FALSE) **하나로만** 정한다 — 업체관리 "광고주 계정" 카드의 **사용/미사용 토글**(`POST /api/trackb/advertiser-link {action:'login-required'}` adminOrMaster). **계정 발급 = 보안 적용이 아니다**(종전엔 활성 계정 존재만으로 자동 잠겨, 계정을 두면서 링크를 열어 두는 선택지가 없었다). 컬럼을 링크 행에 둔 이유 = `loginByLinkToken`이 이미 그 행을 SELECT 해 **쿼리 순증 0**. ★ **켤 때만** 활성 계정 1개 이상을 요구(0개로 켜면 링크도 로그인도 막혀 아무도 못 들어온다) — 끄기는 검사 없음(계정 무손상). ★★ **켠 뒤 계정이 사라져도 열리지 않는다**(fail-closed — "계정 없으면 통과" 폴백을 두면 켜 둔 보안이 조용히 풀린다. 복구는 토글 OFF 또는 계정 재발급). ★ 백필은 **활성 계정이 있던 업체를 TRUE로** 채워 **배포 순간 동작 불변** — 083은 링크 행이 이미 있는 업체(`login_required IS NULL` 인 행만 = 재실행해도 관리자가 끈 값 안 되켬), **084는 "계정은 있는데 링크 행이 아직 없는" 업체**(링크 행은 058이 일괄 생성하지 않고 패널을 열 때 `ensureAdvertiserLink`로 지연 생성되므로, 083만 두면 그 업체들이 배포 후 패널을 여는 순간 DEFAULT FALSE로 공개가 된다 — 코드리뷰로 잡은 실측 구멍). ★ 회전(`generateAdvertiserLink`)·폐기/재활성(`setAdvertiserLinkActive`)의 UPDATE SET 목록에 **`login_required`를 넣지 말 것** — 넣으면 링크 회전 한 번에 잠금이 조용히 풀린다(회귀가드 2.7-2g/h가 고정). ★ 부팅 프리플라이트 `REQUIRED_SCHEMA`에 등록 — 미적용 시 컬럼 참조 3곳이 42703으로 죽어 링크 로그인·업체관리 카드가 전면 500(무신호 전면장애). ★ 켜기/검사는 **한 UPDATE의 `WHERE EXISTS`**(TOCTOU — 검사 직후 마지막 계정이 꺼지는 경합 차단). ⚠ **토글 ON은 이미 접속 중인 세션을 끊지 않는다**(링크 JWT 8시간, 링크 폐기도 동일 한계) — 켤 때 토스트로 고지. ⚠ 공개 링크 안내에 **"마스킹"이라 쓰지 말 것** — 광고주 뷰의 수취인·연락처·주소는 마스킹되지 않는다(`_advertiserColumns`). 프론트: 토글 OFF면 계정 영역을 **흐리게만** 표시하고 **클릭은 막지 않는다**(막으면 계정을 못 만들어 토글을 켤 수 없는 교착), 마지막 활성 계정 비활성/삭제 시 잠금 경고, 안내문 자리에 2줄분 `min-height` 예약(토글 ON/OFF에 카드 높이 불변 — 실측 diff 0). 회귀가드 `tests/advertiserLinkLoginToggle.test.js`, 시안 `frontend/docs/design-advertiser-link-account-toggle.html`. 토큰=추측불가 랜덤(`trackB.service._linkToken` 단일 출처, base64url **12B=16자**), **프래그먼트(#)라 서버로그·Referer 미유출**, 유출 대비 즉시 회전/폐기(active), 종료 거래처 거부. 광고주 뷰 자체가 마스킹·읽기전용·소유탭만이라 노출 최소. 링크 섹션 모드 배지(🔓공개/🔒로그인필요/⛔폐기)는 **`login_required` 플래그로 판정**(서버 게이트와 같은 값 — 계정 존재로 추정하지 않는다). 회귀가드 `tests/trackBIntranetSso.test.js`(2.7 링크 CRUD·ensure·2.7-2 계정 사용 토글·2.8 loginByLinkToken 플래그 게이트). 담당 매핑 = `advertisers.inad_pm=로그인명` 재사용(`scopedTabsForStaff`, **양쪽 `TRIM`으로 공백·표기차 footgun 방어**; 안정 식별자 staff_id 연결은 후속). ★**스코프 판정은 클라이언트가 보낸 `tabGid`를 신뢰하지 않고 `(sheetId,tabName)`으로 gid 재해석**(`canAccessTab`) — 명단·PII가 전부 tabName으로 조회되므로 스코프도 tabName 기준이어야 함(과거 read가 소유 gid를 쿼리스트링에 실어 타 탭 명단을 긁던 교차열람 봉합, read/edit 일원화). 회귀가드 `tests/participantEdits.test.js`(6a~6f2).
- **★★ 업체 접속 링크 = 짧은 URL(`#a=`) · 교환 뒤 해시 유지 (사용자 확정 2026-08-24 「㉡ + A안」)**
  - **발단**: 링크가 **81자**(`https://…pages.dev/workdesk#adv=<32자>`)라 카톡 전달이 불편했고, 더 큰 문제로 boot 이 첫 진입 즉시 해시를 지워 **주소창·히스토리·북마크에 남는 것이 열쇠 없는 `/workdesk`** 였다 → 재방문·새 탭·"관리자가 눌러 보고 주소창을 복사해 전달"이 전부 **빨간 문구 없는 로그인 화면**으로 떨어졌다(실측 신고. 서버는 무로그인으로 열어 주는데 화면만 막힌 것).
  - **무엇을**: 키 `#adv=` → **`#a=`**, 토큰 24B(32자) → **12B(16자)** ⇒ **81자 → 63자**. ★ **경로 `/workdesk` 는 유지**(사용자 확정 (나)안) — `/a` 는 `_redirects` 가 Cloudflare Pages 전용이라 **테섭(Railway)에서 404** 이고 링크는 `location.origin` 기반이라 **테섭에서 복사하면 죽은 링크**가 된다. `/a` 는 별칭으로만 열어 둔다(Pages 는 SPA 폴백이라 명시하지 않으면 `/a` 가 **200 + 리뷰어 홈**으로 뜬다 — `_headers` 에 `/a` no-cache 도 함께: 확장자가 없어 `/*.html` 규칙에 안 걸린다). 짧은 커스텀 도메인을 붙이면 그때 생성 경로도 `/a` 로 옮긴다(→ 34자).
  - ★★ **하위호환 — `#adv=` 폴백을 제거하지 말 것**: 이미 뿌린 링크가 전부 죽는다. 정규식 `[#&]a=` 는 `#adv=` 를 매칭하지 않아(`a` 다음이 `=` 여야 한다) 두 키가 서로를 삼키지 않는다. **기존 32자 토큰은 정확일치 조회라 그대로 유효**(짧아지는 건 신규 발급분뿐).
  - ★★ **A안 = 교환 뒤 해시를 지우지 않는다**: 이 링크는 **주소 자체가 열쇠**라 주소창에 남는 것이 새로운 노출이 아니다(유출 대비 = 기존 [회전]/[폐기]). 대신 **완화 금지 3종**이 함께 성립해야 한다 — ① **`logout()` 이 해시를 지운다**(안 지우면 바로 이어지는 `boot()` 이 다시 교환해 **로그아웃이 안 된다** = 공용 PC에서 다음 사람이 그대로 입장) ② **`_navPush` 가 해시를 보존한다**(`pathname+search+hash` — 빼면 화면 전환 한 번에 열쇠가 증발해 A안이 무의미해진다) ③ **재교환 생략은 "같은 토큰 + 살아 있는 세션"일 때만**(`adv_link_tok` 비교 — 만료 세션·다른 업체 링크는 **반드시** 교환한다. 생략 게이트가 열쇠를 삼키면 안 된다).
  - ★ **해시를 실어도 딥링크가 새지 않는 근거**: `#sso=`·`#go=`·`#w=`·`#view=` 는 **소비 시점에 스스로 해시를 지우므로** `_navPush` 가 도는 시점에 남아 있는 것은 접속 링크 하나뿐이다(회귀가드가 그 세 소비처의 `replaceState` 생존을 함께 고정).
  - ★ **토큰 생성기 단일 출처 `_linkToken()`**(`LINK_TOKEN_BYTES=12`) — `randomBytes(24)` 사본 4곳(업체 ensure/generate + 브랜드 create/rotate)을 이관했다. **브랜드 열람 링크 토큰도 같이 16자**가 된다(부수 이득). 길이를 되돌리려면 이 상수 한 곳.
  - ★ 주소가 깨져 토큰을 못 읽으면 **문구 없는 로그인으로 흘리지 않고** 사유를 말한다(`접속 링크 주소가 깨졌습니다…`).
  - 회귀가드 `tests/advertiserLinkShortUrl.test.js`(23 — 짧은 키·경로 유지·단일 출처·하위호환·해시 유지·logout 해시 제거·`/a` 별칭 + **교환 블록을 vm 으로 꺼내 5갈래 실제 실행**, **변이시험 8종 검출 확인**) + 실 브라우저 6갈래 실측. ⚠ 이 변경으로 `workdeskHistoryNav`(URL 규칙이 "해시 포함"으로 **뒤집힘** — 옛 단언 "해시를 실지 않는다"를 되살리면 A안이 무너진다)·`brandShare`(토큰 길이 기준) 2건을 갱신했다.
- **부품5b 커스텀 열(행별 자유메모) + 셀 배경색(migration 080, 8/3)** — 리뷰웹시스템[3버전] 그리드 사용성 보강. **participant_edits/write-back 파이프라인과 완전 분리된 신규 테이블만**(`trackb_custom_columns`·`trackb_custom_column_values`·`trackb_cell_colors`) — 시트에 절대 안 쓴다(그 쪽 disjoint·guard·tier 불변식 무접촉). 앵커는 부품5와 동일 철학(order>manual>identity, `_resolveAnchorForWrite`가 identity 중복까지 재검증해 ambiguous면 거부 — `workdeskTab` 합성측 게이트와 대칭, 안 맞추면 동명이인 등 중복 identity 행끼리 메모·색이 새 나간다). 열 추가/삭제·값 저장·색 저장 4개 라우트(`/workdesk/custom-column`·`/workdesk/custom-value`·`/workdesk/cell-color`) 전부 `_ensureEditScope`(master/admin 전체·staff 담당 탭만·advertiser 차단) — **advertiser 그리드엔 애초에 `customColumns` 미동봉**(showEdits만). 커스텀 열 삭제는 `deleted_at` 소프트(정의는 보존, 표시열에서만 제외), 값은 `(column_id, anchor)` 유니크 upsert. 셀 배경색은 프론트 드래그로 잡은 (rowId, field) 목록에 색 1개를 일괄 적용(`field`='col:<헤더>'|'ccol:<열id>'|'idname'|'idphone', `#`열 제외) — 행별 FOR UPDATE 트랜잭션으로 순차 적용(레이스 방지, 대용량 드래그는 순차 왕복이라 다소 느릴 수 있음·상한 3000셀). 프론트(`workdesk.html`): 헤더 드래그(열 선택/숨기기)와 별개로 **본문 셀 드래그**(`_ensureCellColorUI`, mousedown~mouseup 이동 감지)로 사각형 범위 선택 → 플로팅 팔레트(`_openColorPopover`)에서 색 클릭 시 낙관적 반영(inline `background-color`, 클래스 기반 배경(`gok` 등)보다 우선)+백그라운드 저장. **단순 클릭(이동 없음)은 종전 셀 편집(`beginEditCell`)이 그대로 처리** — 드래그로 다른 셀까지 이동했을 때만 색상 UI 개입. 커스텀 열 셀도 `col:` 오버레이 셀과 같은 `eCell`/`beginEditCell`/`commitCellEdit` 경로를 타되 되돌리기(↩)는 없음(원본 개념이 없음). 컬럼 표시명은 `_colLabel`(내부 key `ccol:<uuid>` → 사용자가 지은 이름)로 항상 변환해서 보여준다.
- **진실원천 전환(부품6)·write-back 엔진(P2/P2-2)도 같은 문서로 분리** — `source_of_truth` 플래그를 읽는 소비처는 **write-back 엔진뿐**이고 Track A 핫패스(검색·주문원장·인덱스빌드·행배정·큐워커·RAW미러)는 **참조 0**(회귀가드 `trackBSourceOfTruth` 가 fs 스캔으로 고정). 플립은 master 전용, `TRACK_B_WRITEBACK`·`TRACK_B_WRITEBACK_FULL` 이중게이트, 컬럼 disjoint(상태칸 2열만)·blank-only·행이동 재검증이 Track A 무영향의 근거.
