# ★★ 리뷰어 주문취소 + 작업 로그 (2026-08-23 사용자 확정 · 스키마 변경 0)
- **발단**: 리뷰어가 스스로 취소할 수 있는 것은 **구매 전 홀드뿐**(`applied`·`blog_pending`). 구매양식을 낸 뒤에는 창구가 없어 카톡·1:1문의 → 담당자 [행 삭제]로 돌아갔고, 사람이 놓치면 표에 유령 줄이 남았다. 시안 = `frontend/docs/prd-reviewer-order-cancel-tablog.html`.
- ★★ **파괴 실행부 사본 0** — 작업보드 [행 삭제]와 **같은 함수**(`trackB.hideWorkdeskRow` → `cancelOrderSubmission` + `_hideParticipantInTx`): 주문 무효 → 큐 정리 → 자리 반환 → 작업표 줄 제거 → **보충 슬롯**(총 건수 불변) → 표 번호 재부여 → 장부 재생성. 사본을 두면 "리뷰어가 취소하면 보충이 안 되는" 드리프트가 조용히 생긴다. ★ `actorRole` 은 넘기지 않는다 — 그 게이트는 "담당자가 **남의** 구매기록을 지우는" 경우를 막는 것이고 여기는 본인 것이다.
- ★★ **신규 인증 표면 0** — 리뷰 내역 팝업이 이미 통과하는 강한 키(`reviewEdit._verifyRowOwnership`). **주문 번호는 서버가 좌표로 다시 찾는다**(요청 본문에서 받지 않는다 — 낡은 화면·조작 요청이 남의 주문을 지목할 수 없다).
- ★★ **판정 단일 출처 = `reviewerOrderCancel.assessReviewerCancel`** — **화면 게이트(버튼 노출)와 실제 실행이 같은 함수**를 쓴다. 화면이 스스로 정하면 "보이는데 거부"가 된다. 게이트 재료는 **기존 `participation-brief` 에 `cancelable` 로 동봉**(신규 엔드포인트 0 — 리뷰 형태 표시 D 와 같은 규율).
- ★★ **fail-closed 4종(완화 금지)**: **리뷰 제출 완료**(작업표·검색 명단 **둘 다** 본다) · **리뷰비 입금**(작업표 플래그 + 회차 항목) · **시트 기반 탭**(지워도 다음 반영이 되살린다 — `_hideParticipantInTx` 가 최종 방어지만 여기서 먼저 막는다) · **판정 조회 실패**(모르는 채로 지우지 않는다). 거부는 **쓰기 0건**이고 `assess` 에는 쓰기 문장이 없다.
- ★ **리뷰 제출 완료 건은 버튼 자체를 그리지 않는다**(사용자 확정 ②) · 나머지 막힘은 **숨기지 않고 흐리게 + 사유**(왜 못 누르는지 모르면 문의가 는다) · **2단 확인**(사유 선택 **AND** 체크) · 사유 선택지는 **서버 목록을 그린다**(문구 사본 0) · 실패해도 **입력한 사유를 지우지 않는다** · **바깥 클릭으로 닫지 않는다**(입력 보호) · Esc 리스너 최상위 1회 · **여러 건이 묶인 팝업에는 그리지 않는다**(어느 건인지 특정 불가).
- ★ 사유는 `reviewer_event_logs`(`order_canceled_by_reviewer`, `severity:'info'`, `resolved:true` — 담당자가 손볼 것이 없는 사실 기록이라 미확인 알림으로 쌓지 않는다) + **1:1문의방 자동 안내**(`csBridge.postAdminNotice`, 사용자 확정 ④). **둘 다 커밋 뒤 후처리**이고 실패해도 취소를 되돌리지 않는다(이미 끝난 일).
- ★★ **취소 뒤 당일 재참여는 이미 열려 있다 (사용자 확정 ㉯가 코드 변경 0으로 성립)** — 참여 게이트가 전부 **`status='submitted'` 기준**(`_applyParticipation` 의 같은 공고 차단·하루 총량·타계정 하루한도)인데 취소가 그 행을 `cancelled` 로 바꾸기 때문이다. ⚠ CLAUDE.md 의 옛 서술("당일 재신청 0회 · 상태 무관 이력")은 **홀드 취소 흐름의 안내 문구**를 옮긴 것이고 실제 게이트와 다르다 — 이 절이 맞다. 억제는 `CAMPAIGN_DAILY_APPLY_CAP`(10) · `CAMPAIGN_HOLD_CAP`(2)이 한다.
- **작업 로그** = 작업보드 상단 **[🗒 로그]**(`GET /api/trackb/workdesk/activity-log`): ★★ **신규 저장소 0 · 읽기 전용**(쓰기 문장 0 — 회귀가드가 고정) — 이미 쌓이는 8소스를 한 타임라인으로 모은다(유형 7종 = 주문·취소·리뷰·편집·정원·정산·시스템). ★ **소스별 독립 조회** — 하나가 실패해도 나머지는 나오고 **실패한 종류를 말한다**(0건으로 꾸미면 "아무 일도 없었다"로 읽힌다). ★ 게이트는 편집 이력과 **같은 `_ensureEditScope`**(광고주 차단 — 리뷰어 실명·연락처가 실린다) · **gid 는 서버가 `tab_configs` 에서 다시 구한다** · 모든 소스 쿼리에 `LIMIT` · **빈 gid 는 절을 켜지 않는다**.
- ★ **옛 [편집 이력] 버튼·모달은 [편집] 탭으로 흡수**(같은 일을 하는 창구를 둘로 두지 않는다 — 회귀가드가 `showEditHistory` 흔적 0 을 고정). ★ 유형 탭은 **서버가 준 `kinds` 를 그대로** 그린다(라벨 사본 0) · 클래스는 전부 `tl` 접두 · `.tlbd{min-height:0}`(없으면 스크롤이 안 생긴다) · 자리표시자를 깐 함수는 **어떤 예외에도 화면을 종결**시킨다([다시 시도]).
- ★★ **작업 로그는 처음까지 전부 — 스크롤로 과거를 이어 받는다 (커서 `before`, 사용자 확정 2026-08-24)**: 종전엔 최근 60건에서 `최근 기록만 표시했습니다.` 로 끝나 **그 앞을 볼 창구가 없었다**(막다른 길). 지금은 바닥 근처에 닿으면 서버 커서(`nextBefore`)로 다음 묶음을 **이어 붙인다**.
  - ★★ **한 행 = 한 항목(완화 금지)** — 한 행이 두 시각을 내는 소스(주문 **접수/취소** · 편집 **작성/되돌리기** · **마감/복귀**)에 행 단위 커서를 걸면 **커서가 두 시각 사이에 놓이는 순간 한쪽이 조용히 사라진다**(접수 8/20 · 취소 8/22 인 행에서 접수가 영영 안 나온다). → 그 세 소스는 **UNION ALL 로 항목 단위로 펴고**, 커서 절은 **정렬 기준과 같은 식**을 쓴다.
  - ★★ **유형(kind) 조건은 SQL 로 내린다** — JS 에서만 거르면 `[취소]` 탭에서 LIMIT 이 접수 행으로 다 차 **그 페이지가 0건**이 되고 커서가 제자리에 멈춘다.
  - ★ **경계 동시각 유실 방지 = `<=` + 항목별 `id`**(각 표의 PK 파생) — 겹쳐 오는 항목은 **화면이 `id` 로** 걸러낸다(동일성 규칙을 화면이 지어내지 않는다).
  - ★★ **끝을 지어내지 않는다** — 서버가 "더 있다"면서 커서를 안 줬거나(구버전 백엔드) 이어받기가 실패하면 `이 작업의 처음까지 모두 불러왔습니다` 라고 **말하지 않고** 사유 + [다시 시도]를 남긴다. ★ 새로 더한 항목이 0건이면 자동 이어받기를 멈춘다(같은 묶음 무한 재수신 차단).
  - ★ 화면 줄 마크업은 **`_tlEvHtml` 한 벌**(첫 그리기·이어 붙이기 사본 0) · **이어 붙일 때 목록을 다시 그리지 않는다**(보던 자리 유지) · 유형을 바꾸면 **맨 위에서 다시 시작**(안 하면 바닥 감시가 곧바로 발동해 묻지도 않은 과거가 딸려 온다 — 실브라우저가 잡았다) · 늦게 온 응답이 화면을 덮지 않는다(`seq` 가드).
  - ★ 쓰기 문장 0 · 게이트·gid 재조회·응답 `truncated` **무접촉**(구버전 프론트 동작 불변), `before` 는 선택 파라미터라 **배포 스큐 양방향 무해**.
  - 회귀가드 `tests/reviewerOrderCancelAndTabLog.test.js`(58 — 커서 절 전수·UNION ALL·유형 SQL·커서 계약·id 발급·화면 배선) + `tests/tabActivityLogPg.test.js`(**진짜 PG16** — 잘게 끊어 받아도 총 건수가 같고 그 함정 행에서 두 항목이 모두 나오는 것까지 실행 확인), **변이시험 10종 검출 확인**.
- ★★ **컬럼 이름 실측 함정** — `order_submissions` 에는 `name` 이 없다(`orderer`/`recipient`) · `campaign_plan_events` 는 `actor`/`action`/`detail`(event_type/payload 아님). 스텁은 SQL 을 해석하지 않으므로 **컬럼 오타는 실행으로만 잡힌다**.
- ⚠ **브라우저 검증 함정(이번에 실측)**: `index.html` 은 **PWA 서비스워커를 등록**해 `page.route` 가 우회된다 → 리뷰어 화면 실측은 **`newContext({serviceWorkers:'block'})`** 로 열 것. 교차 오리진 stub 에는 `Access-Control-Allow-Origin` 도 필요하다.
- 회귀가드 `tests/reviewerOrderCancelAndTabLog.test.js`(42 — 실행부 사본 0 · fail-closed 6갈래 **실제 실행** · 주문 id 불신 · 로그 읽기 전용/실패 고지 · 라우터 스택 실검사 · 화면 판정 사본 0, **변이시험 16종 검출 확인**) + 실 http 오리진 브라우저 실측(리뷰어 정상·막힘·제출완료 3상태 · 2단 게이트 · payload · 로그 팝업 8탭·필터·실패 고지 · JS 오류 0).
