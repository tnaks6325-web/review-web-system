# ★★ 리뷰검수 오류유형 = **전체 집계**(2026-08-23 신고 — "3,334건인데 칩은 200")
- **신고**: 리뷰검수가 3,334건(의심 3,273 + 불량 61)인데 유형 칩이 `전체 유형 200` 이라 **유형별 실제 분포를 시스템 어디에서도 볼 수 없었다**. 원인 = 목록 API 가 `LIMIT 200`(`listInspections`)인데 화면이 **그 응답 배열을 세고 있었다**. CSV 내보내기도 500 상한이라 그쪽으로도 못 봤다.
- ★★ **판정 단일 출처 = `utils/inspectIssueTypes.js`** — 규칙표(`ISSUE_RULES`) 하나에서 **JS 판정(`issueTypesOf`)과 SQL 집계 조각(`issueTypeCountSql`)을 함께 파생**한다. 전체 집계를 하려면 DB 안에서도 같은 판정이 필요한데 그 규칙을 SQL 에 따로 적으면 사본이 되어 **"칩 건수 ≠ 카드 문장"** 으로 갈린다(`rowNumbering.filledSql`·`tableOrderNum` 과 같은 관용구). 화면 `_riIssueTypes` 는 **필터·재검수 대상 선정용 최소 사본**이고 회귀가드가 같은 픽스처로 두 구현을 실행해 결과 동일을 고정한다.
- ★ **`inspectionTypeCounts`(신규)는 목록 상한과 무관** — `COUNT(*) FILTER` 한 번. 한 건이 여러 유형이면 각 칩에 **모두** 세어지므로 `합계 ≠ total` 이고 화면이 그 사실을 계속 말한다.
- ★★ **staff 담당 탭 스코프를 SQL 로 내렸다** — 종전에는 전체에서 뽑은 200건을 라우트가 `allow.has(...)` 로 걸러서 ① 담당 건이 그 200건 밖이면 **화면이 거의 비었고** ② 요약(통과/의심/불량)도 **200 에서 잘려 있었다**(조용한 절단). 목록·요약·유형집계가 **같은 `tabs` 인자**를 받는다(`_riScope` 공유 조각). ★ **빈 배열 = 담당 0개 = 0건**(모른다고 전체를 세지 않는다 — fail-closed).
- ★ **fail-soft 는 조용하지 않다** — 집계 실패는 응답에 **필드를 안 싣고**(0 으로 꾸미지 않는다) 화면이 종전 폴백(표시된 건 기준)으로 그리며 `· 표시된 건 기준` 이라고 말한다. 구버전 백엔드도 같은 경로 = 동작 불변.
- ★ **칩(전체)과 목록(최근 200건)이 다르므로 말한다**: `· 목록은 최근 200건만 표시(칩 숫자는 전체 기준)` + 유형을 골랐는데 카드가 없으면 **"없다"와 "이 목록에 안 실렸다"를 구분**해 안내한다(뭉뚱그리면 담당자가 원인을 엉뚱한 데서 찾는다). ★ **[♻ 재검수] 대상은 종전 그대로 "화면에 걸러 놓은 것"**(로드된 file_id 목록) — 확인창이 그 건수를 말한다.
- 회귀가드 `tests/inspectTypeCounts.test.js`(57 — 판정 순수함수 · **화면 사본 ≡ 서버 판정 vm 실행** · 배선 · **진짜 PG16 으로 SQL 집계 ≡ JS 판정 + 서비스 함수 실제 실행**(스코프 `unnest` 조인·상태 절·확인됨 제외), **변이시험 10종 검출 확인**) + 실 http 오리진 브라우저 실측(칩 전체 건수·잘림 고지·빈 유형 안내·구버전 폴백·JS 오류 0). ⚠ 이 변경으로 `reviewInspect` 의 staff 스코프 패턴을 갱신했다 — **검사 의미는 같거나 더 강하다**(목록·요약·집계가 같은 담당 탭 목록을 받는다).

- **리뷰검수 화면 정렬·인터랙션(사용자 확정 2026-08-06)**: ★★ **헤더 버튼바(`#rihead .mh`) 캡 = 카드 영역(`.ricards`) 캡과 같은 값(1400px)** — 캡이 없으면 `.sp{flex:1}` 이 남는 폭을 다 먹어 버튼이 카드보다 한참 오른쪽에 뜬다(작업오더·모집공고와 같은 처리). 두 숫자를 따로 적어 두면 한쪽만 바뀌어 조용히 어긋나므로 **회귀가드가 값 일치와 5장/줄**(5×268+4×11=1384 < 1400 < 6장 문턱 1663)을 고정. `.ovnote` 도 같은 폭. ★ **버튼 인터랙션은 틴트 농도·테두리·1px 눌림만** — 변형별 색(m-red/m-green…)을 다시 적지 않고 `filter:brightness` 로 공통 처리하고, **호버에서 채움으로 바뀌지 않는다**(빨간 벽 방지 규율 유지). `prefers-reduced-motion` 이면 이동·전환만 끄고 색 변화는 남긴다. ★★ **확인 팝업·[⋯] 메뉴는 바깥 클릭으로 닫지 않는다** — 입력해 둔 반려 사유가 실수 클릭 한 번에 날아가던 것을 막는다. 대신 **닫는 길([취소]/[닫기])은 반드시 남긴다**(못 닫는 팝업 금지 — 회귀가드가 고정).
- ★★ **“구매확정 작업인데 리뷰검수가 불량” — 원인이 화면에 안 보이던 문제 (실사고 2026-08-06)**: 이노크아든_면마스크는 구매확정 작업이고 구매확정 캡처가 제출되는데 검수가 `리뷰 화면이 아닙니다` 불량을 냈다. 원인은 **리뷰타입 판정이 null** — `tab_configs.review_type` 에 **배송유형 `실배송`** 이 들어 있고(접수 업서트가 `COALESCE(NULLIF(...))` 라 기존 값을 보존하므로 뒤늦은 작업오더 값이 덮지 않는다) 공고에도 리뷰타입이 없었다. `resolveReviewType` 은 실배송/빈박스를 **배송유형이라 null 로 떨어뜨리므로** `_okKinds` 가 `['review']` 가 되어 정상 제출이 전건 불량이 됐다.
  - ★★ **판정을 바꾸지 않았다** — 고친 것은 **“왜 그렇게 판정했는지가 어디에도 안 보이던 것”**. `reviewTypeContext.reviewTypeDetailsForTabs` 가 판정 + **근거값(`tabRaw`/`campRaw`)** 을 **한 쿼리로**(N+1 금지, 단건 조회와 **같은 LATERAL 최신 1행** 모양) 돌려주고 `/review-inspect/list` 가 실어 보낸다. 판정 규칙 사본은 여전히 0(`utils/reviewType` 단일 출처).
  - **상세 팝업 ‘리뷰 화면·채널’ 줄**이 문장으로 말한다 — 구매확정 작업이면 `구매확정 완료 화면도 정상으로 봅니다` / 미지정이면 `리뷰 화면을 기대합니다` + 탭 값이 실배송·빈박스면 **`배송유형이라 리뷰타입으로 인정되지 않습니다`** 까지.
  - ★ **카드 조치 안내는 한 케이스에만** — `구매확정 캡처 + 리뷰타입 미설정 + 불량` 일 때만 `공고에서 구매확정으로 저장 → [♻ 재검수]`. 현금영수증 판별·중복 등 **조치가 다른 유형에는 뜨지 않는다**(늑대소년 방지).
  - ★★ **리뷰타입 정보가 없으면(구버전 백엔드·조회 실패) 아무 말도 하지 않는다** — 없는 것을 “미지정”으로 꾸미면 멀쩡한 구매확정 탭에도 잘못된 안내가 뜬다. 목록 조회는 fail-soft(표시만 생략).
  - ★★ **자동 인식 복구(접수 업서트)**: 신규 접수는 작업오더의 `구매확정` 이 `tab_configs.review_type` 에 그대로 들어가 **자동으로 통과**되지만, **이미 값이 있는 탭**은 `COALESCE(NULLIF(기존,''), 새값)` 이 무조건 보존해 영영 안 채워졌다 → **리뷰타입 칸에 든 값이 배송유형(`실배송`·`빈박스`)일 때만** 작업오더 값으로 **교체**하고 그 값은 `delivery_type` 으로 **이관**(★ blank-only — 이미 채운 배송유형은 안 덮는다). **유효한 리뷰타입·관리자 설정은 종전대로 보존**(2차 접수가 덮지 않는다는 기존 계약 불변). 어휘는 `LEGACY_DELIVERY_VALUES` 단일 출처(`$12` 파라미터 — SQL 에 사본 금지). 회귀가드 `tests/acceptReviewTypeRepair.test.js`(**진짜 PG16 으로 네 경우·멱등·판정 연결 실행**, 변이시험 검출 확인).
  - ★★ **공고에 저장해 뒀는데도 검수가 못 보던 경로 둘**(SQL 단일 출처 `reviewTypeContext.CAMPAIGN_REVIEW_TYPE_LATERAL` — 단건·배치·`loadTabExpectations` 공용): ㉮ **탭 리네임** — `linked_tab_name` 이름 매칭뿐이라 이름이 바뀌면 공고를 못 찾아 설정이 조용히 풀렸다 → **gid 폴백**(★ 빈 gid 는 키를 만들지 않는다) ㉯ **차수 재발행** — 한 탭에 공고가 여러 개일 때 `ORDER BY created_at DESC LIMIT 1` 이 **최신 공고의 NULL** 을 집어 옛 공고의 `구매확정` 을 가렸다 → **값이 있는 최신 공고**(`COALESCE(review_type,'') <> ''`). '미지정'은 관리자가 **정하지 않은 것**이라 가리지 않는다(최신이 `포토` 처럼 **명시**돼 있으면 최신이 그대로 이긴다). 완화 방향이라 안전 — 반대가 이번 사고였다. ★ **채널 LATERAL 은 분리 유지**(`channel`+`channel_custom` 짝이 한 행에서 와야 한다).
  - ⚠ **여전히 자동이 아닌 경우**(문서화): ㉮ 작업오더에 리뷰타입 미기재 → 미지정(추측하지 않는다) ㉯ **혼합 오더**(`혼합(포토 10건, 구매확정 5건)`)는 어느 행이 무슨 유형인지 시트에만 있어 `mixed` → null(설계상 의도 — "혼합이니까 일단 리뷰"로 접으면 구매확정 행이 잘못 검수된다) ㉰ 접수를 거치지 않고 만든 탭. 이 셋은 **공고 모달에서 리뷰타입을 지정**해 해결한다.
  - **운영 조치**: ① 공고 모달(또는 탭 설정)에서 리뷰타입 = **구매확정** 저장 ② 그 작업에서 **[♻ 재검수]** — `reinspectTab` 이 fail/suspect/unverifiable 만 pending 으로 되돌려 새 기준으로 재판정한다(resolved·pass 는 보존).
  - 회귀가드 `tests/inspectReviewTypeHint.test.js`(판정 규칙·배치 조회·fail-soft **실행** + 헬퍼 vm 실행 + 배선, **변이시험 검출 확인**) + 브라우저 실행검증(카드 안내·상세 문장·구매확정 전환·구버전 침묵).
- ★★ **“무엇과 중복인지”는 카드에서 바로 읽혀야 한다 — 관계 줄 (시안 `frontend/docs/design-inspect-duplicate-relation.html`, 사용자 확정 2026-08-06)**: 종전엔 `같은 파일이 이미 제출됨 — 〈긴 작업명〉 A 리뷰어 31행` 처럼 **줄 번호가 문장 끝에 묻혀** 있어, 카드를 열어야 짝을 알 수 있었다.
  - ★★ **렌더러 한 벌 = `_riRelPairs`/`_riRelHtml`** — 카드 · `[🗑 중복파일 제거]` 확인 팝업 · 상세 팝업이 **같은 것**을 그린다(사본을 두면 화면마다 좌우가 뒤집히거나 한쪽만 안 바뀐다).
  - ★ **왼쪽 = 지금 보고 있는 건 / 오른쪽 = 이미 있던 것**(방향 고정). 같은 작업·같은 리뷰어면 **이름·작업명을 반복하지 않는다**(카드 제목에 이미 있다) · 다른 리뷰어면 양쪽 이름 + 🔴 도용 신호 · 다른 작업이면 오른쪽에 작업명 + “한 화면에 여러 리뷰일 수 있음”.
  - ★★ **같은 짝을 가리키는 사유는 한 줄로 합친다**(파일 중복 + 본문 겹침이 둘 다 31행 → 관계 줄 1개 + `본문 100% 겹침` 설명). **짝이 다르면 두 줄**(합치면 거짓말이 된다).
  - ★★ **짝의 줄 번호를 모르면 “줄 번호 없음”이라고 적는다** — 빈칸으로 두면 “같은 줄”로 오해된다(양쪽 모두 적용).
  - ★★ **`_riReasons(c, {skipRel:true})` 는 카드 전용** — 관계 줄이 말하는 두 사유를 빼서 같은 정보를 두 번 적지 않는다. **반려 사유 프리필은 옵션 없이 부른다**(리뷰어에게 가는 문장에는 관계 줄이 없어 문장으로 다 말해야 한다 — 옵션이 리뷰어 문구를 갉아먹지 않게 회귀가드가 고정).
  - ★ 확인 팝업은 사진 위에 `제거 40행 ▶ 보존 31행` + 조치 문장·사진 캡션에도 줄 번호. ★ **줄 번호는 관리자 화면 전용** — 리뷰어 채팅 카드·안내 문구에는 넣지 않는다(참여 순번·상품·제출일로 가리킨다).
  - 회귀가드 `tests/inspectRelationLine.test.js`(재료·렌더를 **vm 으로 실제 실행**(네 경우 + 줄 번호 없음 + XSS) + 배선, **변이시험 검출 확인**) + 브라우저 실행검증(카드 4종 실렌더·관계 줄 높이 실측·확인/상세 팝업). ⚠ 이 변경으로 `inspectTypeActions` 가드 2건(_riReasons 시그니처·팝업 캡션)을 함께 갱신(검사 의미 불변).
