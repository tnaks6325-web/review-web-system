# ★★★ 리뷰 캡처 1차 필터가 배포 이래 한 번도 실행되지 않던 사고 — `_preCtx` 프리변수 (2026-09-22 실측)

- **신고**: 리뷰어가 "리뷰 업로드가 너무 안 된다". 실측 결과 `POST /api/image/review-upload` 가 **p50 8.5초 · p90 13~15초 · p95~p99 30초(타임아웃)** 였다. 제출 기록(`/api/submit/review`)은 p50 0.1초로 멀쩡했다 — **느린 곳은 업로드 한 단계뿐**.
- **단계별 실측**(로그 타임스탬프 차): 폴더 확보 0.2~2.7초 · **Drive 업로드 2.9~8.7초** · **AI 검수(classify) 4.1~5.6초** · 원장/2차검수 0.1초. 즉 8초의 대부분이 Drive 업로드와 AI 검수 둘이다.
- ★★★ **근본 원인 = `_preCtx()` 의 프리변수 한 줄**: 그 함수가 `authSession` 을 **맨몸으로** 참조했는데, 그 이름은 구매양식 모드 함수(`initOrderFormMode`)의 **지역 변수**(`let authSession`)라 이 스코프에는 없다. 전역 선언은 프론트 어디에도 없어 **호출할 때마다 ReferenceError**.
  - 첨부 경로 3곳(`addFiles`·`_mrAddFiles`·`_csAddFiles`)이 전부 `_preCheckFiles(..., { ..._preCtx(idx) })` 로 부르므로 **인자 평가 단계에서** 터진다.
  - 셋 다 `async` 인데 호출부가 `await` 도 `.catch()` 도 하지 않아 **rejected promise 가 조용히 사라진다**.
  - 파일 첨부·미리보기는 그 **앞에서 이미 끝나** 화면은 완전히 정상으로 보인다. 서버 로그에도 흔적이 0이다.
  - ⇒ **2026-08-06(#579 「같은 사진 두 번 제출 방지」) ~ 2026-09-22, 약 7주간 서버 요청 0건.**
- ★★ **그동안 함께 죽어 있던 것 3가지**: ① 첨부 즉시 "리뷰 화면이 아닙니다" 차단 ② 첨부 즉시 **"이미 제출한 사진입니다" 중복 안내**(리뷰어가 되돌릴 수 있는 **유일한 시점**) ③ classify 캐시 워밍. **③ 때문에 AI 검수 5초가 통째로 제출 순간으로 밀렸다** — 123 이 적어 둔 "AI 콜 순증 0"의 전제(1차 판정이 제출 때 캐시 히트)가 깨져 있었다.
- **수정 2곳**(`frontend/js/search-app.js`, 서버·스키마 변경 0):
  - `_preCtx` 는 세션을 **이 자리에서 다시 읽는다**(`_loadAuthSession` 단일 출처). ★ **다른 함수의 지역 변수에 기대지 않는다** — 리뷰검수 화면의 프리변수 무한로딩(123)과 **같은 계열**이다. ★ 세션 조회 실패는 빈 값으로 접는다(fail-open — 이름·연락처는 중복 대조용 보조값이다).
  - `_preCheckFiles` 를 **fail-open 래퍼**로 감싼다. ★★ 내부 예외에도 **`checking` 을 반드시 내린다** — 그 플래그가 true 로 남으면 `_preHasBlock()` 이 **제출을 영구 차단**한다(막으려던 것보다 큰 사고).
- ★★★ **문자열 grep 은 이 사고를 통과시킨다** — 배선도 함수도 멀쩡히 "있다". 회귀가드 `tests/precheckWiringRuntime.test.js`(12)는 **sandbox 에 `authSession` 을 일부러 두지 않고 `_preCtx` 를 vm 으로 실제 실행**한다(프리변수가 다시 들어오면 그 자리에서 터진다). **변이시험 5종 검출 확인**(프리변수 되돌리기 · checking 미해제 · 래퍼 재throw · 호출 제거 · fail-open 상실).
- **검증**: 실제 오리진(pages.dev)에 수정본을 주입해 **첨부 3경로 + 세션 부재 + 내부 예외** 5갈래 실행 — 수정 전 요청 0건 → 수정 후 각 1건, `checking` 잔류 0, JS 오류 0. search-app.js 를 보는 기존 가드 **33개 전부 통과**(드리프트 0).
- ⚠ **배포 직후 운영 함의**: 7주간 없던 **첨부 즉시 게이트가 되살아난다**. ① 형식 차단(`kind='other'` + 확신 0.9 이상)은 2회 잠기면 "제가 올린 게 맞습니다" 우회 체크가 열린다(123 규율) ② **중복 차단은 우회 불가**(반영 완료된 같은 사진). 오탐이 잦으면 킬스위치 — `REVIEW_PRECHECK_BLOCK=0`(판정만 하고 잠그지 않는 관측 모드) · `REVIEW_PRECHECK=0`(1차 필터 전체 정지).
- ⚠ **남은 느림**: 이 수정으로 AI 검수 약 5초가 제출 경로에서 빠지지만 **Drive 업로드 3~9초는 그대로**다. 원인은 사진 크기 — `compressImage` 가 **가로 폭만** 보고 리사이즈를 판단해(`width > maxWidth`), 세로로 긴 리뷰 스크린샷(예 1080×2400)은 **줄이기가 한 번도 걸리지 않는다**. 긴 변 기준으로 고치면 −1~4초. ★ 다만 캡처는 **AI 가 글자를 읽어야 하고 검수 증거로 쓰이므로** 화질 하한은 사용자 확정 사항이다(긴 변 1600px 권장).
