# ★★ 입금관리 보류 사유 보완 — 사유를 그 자리에서 고친다 (마이그레이션 0, 사용자 확정 2026-08-10)
- **배경(실측)**: 입금대상 405건이 보류인데 화면은 **사유만 말하고 고칠 곳은 다른 화면**이라 아무도 안 치웠다. 사용자 확정 4건 = ① 범위 = 이체은행·통장표시 + 리뷰어 계좌 + 결제금액 ② 결제금액은 **시트 결제금액 칸에서 자동 보완** ③ 화면 = **상단 묶음 목록 + 행별 [보완] 둘 다** ④ 연결 공고가 없으면 **탭 설정에 저장**.
- ★★ **원인 1 = 판정이 공고만 봤다**: `tab_configs.transfer_bank`·`deposit_name` 은 **001 스키마부터 있고 관리자 대시보드 탭설정 팝오버가 예전부터 채워 온 칸**인데 입금관리 M1(086)이 `recruit_campaigns` 만 봐서, **공고 없는 옛 작업이 전부 '이체은행 미지정'으로 잠겨** 있었다 → 판정 순서를 **공고 → 탭 → 작업오더 물건비 자동**으로 넓혔다(`bankSource` 로 출처를 화면에 드러낸다). **마이그레이션 0**(기존 컬럼 재사용).
  - ★★ **표기가 저장소마다 다르다** — 공고는 코드값(`kbank`/`hana`), **탭은 한글 라벨**(`케이뱅크`/`하나은행`). `index-app.js` 가 `t.transferBank === '케이뱅크'` 로 **문자열을 그대로 비교해 배지를 그리므로** 탭에 되쓸 때는 반드시 한글 라벨(`tabBankLabel`) — 코드값으로 통일하면 남의 화면 배지가 조용히 죽는다.
  - ★★ **해석은 `normalizeBankChoice` 단일 출처이고 정확일치만** — 부분일치로 완화하면 `하나`→`하나증권`·`케이뱅크`→`케이뱅크증권` 이 걸려 **남의 계좌로 송금**된다. 모르는 값은 **null**(추측 금지). ⚠ 회귀가드는 **두 은행 양쪽**을 검사할 것 — 한쪽만 보면 반대쪽 완화를 통과시킨다(변이시험 실측).
- ★★ **원인 2 = 주문 원장 없는 행의 상품비 0원**: 옛 작업·직원 수기 입력 행은 `order_submissions` 가 없어 `no_price`+`zero_amount` 로 영구 보류였다 → **시트 결제금액 칸 폴백**(`priceSource:'order'|'sheet'`, 화면에 `시트` 배지). ★ 판정은 **`utils/paymentAmount.js` 단일 출처**로 이관하고 레거시 `GET /api/payment/targets`(GAS 이관분)의 사본 `_extractAmount` 를 제거 — 사본을 두면 "레거시 화면은 22,000원인데 입금관리는 0원"으로 갈린다. ★ **정확일치(`결제금액`) → 부분일치(`금액` 포함)** 순서(뒤집으면 `옵션금액`·`소계금액` 이 이긴다), **마이너스는 0**(시트의 `-` 표기가 음수가 되면 합계가 조용히 줄어 입금 사고). ★ `row_json` 을 통째로 SELECT 하지 않는다 — SQL 은 `jsonb_object_agg` 로 '금액' 후보 칸만 남기고(상위집합) **최종 판정은 JS 함수**가 한다(SQL 에 판정 사본 금지).
- **보완 저장 = `POST /api/trackb/payment/transfer-setting` · `/reviewer-account`(둘 다 adminOrMaster)**. **쓰기 표면 = 공고 이체설정 2칸 · 탭 이체설정 2칸 · 리뷰어 계좌뿐**(주문 원장·회차·검색인덱스·시트 무접촉).
  - ★ **저장처는 서버가 정한다**(화면이 고른 값 불신) — 공고가 있으면 공고(판정에서 공고가 이기므로 탭에 쓰면 화면이 안 바뀐다), 없으면 탭. **그 탭에 연결된 공고인지 재검증**(`campaign_mismatch`) — 낡은 화면이 남의 공고 이체설정을 덮는 것 차단(계약 오링크와 같은 규율).
  - ★★ **리뷰어는 `reviewers.id` 로 지목** — `phone8` 은 GENERATED·비유니크라 키로 쓰면 **같은 뒤8자리 타인 계좌를 덮는다**(외부모집 수동제출에서 이미 밟은 함정). 타계정은 `sub_accounts` 의 **그 명의 항목만** 병합(소유자 공통계좌 보존). 빈 칸은 덮지 않는다(부분 보완).
  - ★ **인식 못 하는 은행명은 저장 시점에 막는다**(`resolveBank` 미해석 = 거부) — 저장은 됐는데 다음 회차에도 '은행명 인식불가'로 남는 **막다른 길** 방지.
  - ★ 검증 실패는 **400대**로 매핑(`_PAY_FIX_STATUS`) — errorHandler 500 마스킹이면 담당자가 무엇을 고칠지 모른다. 42P01/42703 은 `not_ready`.
- **화면**(`workdesk.html`): 상단 **"보완하면 풀리는 보류"** 묶음 + 표 행별 **[보완]**. ★ 묶음은 **서버 issues/warnings 를 그대로 쓰고 "고치는 곳"으로 묶기만** 한다(판정 사본 0) — 그래서 이체은행 하나 저장에 그 작업 N건이 함께 풀린다. ★ **고칠 수 없는 사유**(리뷰어 미등록 · 시트에도 금액 없음)는 **버튼 대신 조치 문장**(죽은 버튼 금지). ★ 팝업은 **body 직속** · **바깥클릭으로 안 닫힘**(입력해 둔 계좌·통장표시 보호) · Esc/[취소]만 · Esc 리스너는 닫을 때 해제.
  - ★★ **묶는 단위는 작업(탭) 하나 — 사유별로 흩어 놓지 않는다**(사용자 요청 2026-08-10): 종전엔 같은 작업이 `🏦 이체은행`·`🏷 통장표시`·`💳 계좌` 카드 3곳에 나뉘어 몇 번을 오갔다. 지금은 **카드 1개 = 작업 1개**이고 그 안에 이체은행 · 통장표시 · 계좌(리뷰어별) · 리뷰어 미등록 · 결제금액 없음이 줄로 들어간다. 카드는 건수 많은 순, `_PM_FIX_CARD_CAP`(8) 초과분은 **"외 N개 작업"으로 고지**(조용히 자르지 않는다).
  - ★★★ **작업 단위 값(이체은행·통장표시·리뷰비)을 정하는 창구는 「작업보드 › 작업 조건」 하나다 (사용자 확정 2026-08-24)**: 종전엔 입금관리 전용 팝업(`_pmFixWork` — 3칸 직접 입력)과 작업 조건 카드 두 곳에서 같은 값을 정해 **창구가 갈렸다**(문구·권한이 한쪽만 바뀌면 어긋난다). 전용 팝업을 **없애고** 입금관리는 **[📋 작업보드]로 보내기만** 한다(`_pmOpenBoard` — 보완 카드 줄 · 표의 [보완](이체은행) · 표의 통장표시 [미설정] 세 자리 모두). ★ **저장 API(`/payment/transfer-setting`)는 그대로** — 작업 조건 카드가 그것을 쓴다(신규 경로 0). ★ **리뷰어 계좌(`_pmFixAcct`)는 옮기지 않는다** — 작업 조건이 아니라 **그 사람의 정보**라 작업보드에 고칠 자리가 없다.
    - **작업 조건 카드에 「이체은행」 줄 신설** — ★★ 판정은 **`payment.service.normalizeBankChoice`/`bankFromGoodsCostType` 재사용**(사본 금지 — 여기서 규칙을 새로 세우면 "작업 조건은 하나은행인데 이체 파일은 케이뱅크"로 갈린다). 순서 = 공고 → 탭 → 작업오더 물건비 자동. ★ `auto` 는 **사람이 정한 값이 아니라는 사실을 칩으로 말한다**(조용한 추정 금지). ★ **근거가 없으면 null = [미설정]**(0·빈 값으로 꾸미지 않는다). ★ **광고주에게는 안 나간다** — `_condAdvertiserLens` 가 화이트리스트 재구성이라 자동으로 빠지고, 화면의 광고주 표시 목록에도 없다. ★ 공고가 없는 작업은 `_cndBankModal`(3버튼 · 기존 `transfer-setting` 저장 · 관리자만 — 리뷰비·입금명과 같은 게이트).
  - ★★★ **리뷰비 0 = 리뷰비 없는 작업 (사용자 확정 2026-08-24 · 완화 금지)**: `no_review_fee` 경고를 **없앴다**. 종전 규칙("근거(`feeSource`)를 못 찾으면 경고")은 **공고가 없는 옛 작업이 구조적으로 근거를 못 갖기 때문에** 보완 목록을 상시 뒤덮었고(실측: 37개 작업 대부분), 그 소음이 진짜 신호(계좌·은행 미비)를 묻었다. 이 계정은 **상품비만 주는 작업이 다수**라 0 이 정상값이다. ★ 그래도 **정하는 창구는 그대로**(작업 조건 카드 › 리뷰비 — 0 의 뜻과 출처(스냅샷·구간)를 그곳이 말한다). ★ `feeSource` 계약(0 vs null 구분)은 **유지**한다 — 조건 카드가 [미설정]과 `0원`을 구분해 그린다. ★ 표의 리뷰비 `0` 도 이제 **평범한 숫자**다(설정 유도 버튼 제거). 회귀가드 `paymentIssueFix.test.js` 7c2b(서버·화면 양쪽에서 그 사유의 부활을 막는다).
  - ★★ **입금 대상 표는 같은 작업을 줄마다 되풀이하지 않는다 (사용자 확정 2026-08-24)**: 50건짜리 작업이면 작업명·이체은행·통장표시가 **50번 반복**돼 정작 줄마다 다른 값(리뷰어·금액·계좌)이 묻혔다. 작업이 바뀌는 자리에 **그룹 머리줄** 한 줄(`tr.pmgrp`)을 넣고, 그 작업에서 **모든 줄이 같은 값인 칸만** 머리줄로 올린다. ★★ **값이 하나라도 다르면 그 칸은 종전대로 줄마다 그린다**(정보 손실 0 — 조용히 숨기지 않는다). ★ 그룹 판정은 `_pmWorkKey` 단일 출처(작업 선택 패널·필터와 같은 키)이고 **순서는 서버가 준 그대로**(`ORDER BY ri.sheet_id, ri.tab_name, ri.row_index` 라 이미 작업별로 이어져 온다 — 화면에서 정렬하지 않는다). ★★ **접기(`_pmFoldWork`)는 표시 전용** — 접어도 체크 상태·이체 대상은 그대로이고(머리줄이 `선택 N/M건 · 합계`를 말한다) **줄을 지우지 않고 감추기만** 한다(`pmhide`). 전체 재렌더 대신 그 그룹 줄만 토글해 **보던 스크롤 자리를 잃지 않는다**. ★ 머리줄 버튼 onclick 도 **인덱스만**(작업명은 시트발 문자열) · **머리줄 `colspan` ≡ 표 머리 칸 수 ≡ 데이터 줄 칸 수**(작업 칸을 지우지 않고 **비운다**). 회귀가드 `paymentIssueFix.test.js` §7B(표 렌더러를 vm 으로 **실제 실행** — 되풀이 0·다른 값 보존·접기 무접촉·colspan 계수, **변이시험 8종 검출 확인**).
  - ★★ **작업 단위 3종(이체은행·통장표시·리뷰비)은 카드 안에서 한 줄이다 (사용자 확정 2026-08-24)**: 셋 다 **같은 팝업(`_pmFixWork`)에서 한 번에 입력**하고 저장하면 그 작업의 보류가 함께 풀리는데, 사유별로 줄을 나누면 **같은 [보완] 버튼이 세 번 반복**돼 "세 번 해야 하나"로 읽힌다. 줄에 적는 건수는 **합집합 `w.setupRows`**(그 작업에서 작업 설정 보완이 필요한 행 수 — 계좌·결제금액 없음은 안 센다)이고, **사유별 건수는 title 로 남긴다**(조용한 누락 금지). ★ **리뷰비가 비면 "지금은 상품비만 이체됩니다"를 묶은 줄에도 남긴다**(묶느라 경고를 잃지 않는다). ★ **필요한 항목만 적는다** — 멀쩡한 칸을 나열하면 담당자가 값을 덮어쓴다. ★ **계좌(💳)는 묶지 않는다**(명의마다 계좌가 달라 사람별로 고쳐야 한다). ★ 좁은 카드에서 **말줄임 대신 접는다**(`.pmfixrow.work .nm{white-space:normal}` — 잘리면 무엇을 입력할지 알 수 없다).
  - ★★ **`accts` 는 여전히 평면 배열 하나** — 한 리뷰어가 여러 작업에 걸치므로 원장은 하나로 두고 작업 카드는 **인덱스를 참조만** 한다. 카드 안 순번을 넘기면 **남의 계좌 창이 열린다**(변이시험이 잡았다 — 회귀가드가 "버튼 인덱스가 가리키는 리뷰어 = 그 줄에 적힌 이름"을 실행으로 고정).
  - ★★★ **카드·팝업이 가리키는 것은 "리뷰어"가 아니라 계좌 명의다 (실사고 2026-08-10 · 소유자 A / 타계정 명의 A′)**: 한 소유자가 **본인 명의 1건 + 타계정 명의 1건**으로 참여하면 두 항목이 **둘 다 "A"**(소유자 이름) 로 보여 어느 계좌를 보완하라는 건지 알 수 없었다(본인은 A은행, 타계정 A′는 B은행으로 **서로 다른 계좌**). 항목 자체는 `reviewerId||subPhone8` 로 이미 갈려 있었고 **표기만 구분을 못 했다**.
    - ★★ **명의 이름은 계좌 원장 값**(`accountName` = 본계정 `reviewers.name` / 타계정 `sub_accounts[].name`) — **시트 이름 칸(`reviewerName`)은 명의로 쓸 수 없다**(탭마다 주문자/수취인 중 무엇이 잡혔는지 달라 타계정 줄이 소유자 이름으로 뜬다). `accountOwner`(소유자 이름)를 함께 실어 "**A의** 타계정"을 말한다.
    - ★★ **표기 단일 출처 = `_pmAcctLabel`(HTML) / `_pmAcctPlain`(평문)** — 카드 줄·팝업 제목·형제 목록·title 이 같은 함수를 쓴다(사본을 두면 "카드는 A인데 팝업은 A′"). **연락처 뒤 4자리**가 이름이 같을 때(동명이인·본인/타계정) 구분되는 **유일한 단서**라 라벨에 상시 포함.
    - ★ 팝업 맨 위 **명의 확인 블록**(`.pmwho`)이 `A′ 명의 계좌 ·0101` + "A 님이 등록한 타계정 — 본인 명의 계좌와 별도로 저장됩니다"를 못박고, **같은 소유자의 다른 명의를 목록으로 보여주며 바로 건너뛴다**(`_pmFixAcct(idx)` — onclick 은 인덱스만). 부제에 그 명의가 걸린 **작업·건수**를 적어 "어느 건인지" 확인할 근거를 준다.
    - ★ 명의가 하나뿐이면 형제 안내를 **띄우지 않는다**(없는 혼동을 만들지 않는다). 회귀가드 `paymentIssueFix` §7C(실사고를 vm 으로 재현, **변이시험 검출 확인**).
  - ★★ **작업별 [📄 시트] 바로가기**(카드 머리 + 작업 보완 팝업): 아직 구글시트를 직접 봐야 하는 값(결제금액 칸 등)이 있어 그 탭을 `#gid=` 까지 바로 연다. ★ 링크 조립은 `payment.service.tabSheetUrl` 한 곳이고 `_loadTabMeta` SELECT 에 `tab_gid`·`sheetless` 를 합류시켜 **쿼리 순증 0**. ★ 프론트는 **서버가 준 `sheetUrl` 만** 쓰고(sheetId 로 재조립 금지) `docs.google.com` 접두 검증 후 `window.open(…,'_blank','noopener')`, **onclick 에는 인덱스만**(URL 을 화면에 심지 않는다).
  - ★★ **이관된 작업(sheetless)도 연다 — 막는 것은 "시트가 아예 없는 경우"뿐 (사용자 확정 2026-08-10)**: 초도 보완에는 아직 시트를 봐야 하는 값이 있다. **"시스템 표가 진실원본"은 링크를 막는 근거가 아니라 경고문의 근거** → 클릭하면 팝업이 사용자 확정 문구(`탈구글시트를 진행했습니다 / 구글시트에 입력한 내용은 시스템에 반영되지 않으므로 / 시트를 편집하지 마시고 참고용으로만 사용하세요`)를 먼저 보이고 **[확인하고 시트로 이동하기]** 로만 이동한다(버튼에도 `참고` 표기). 일반 작업은 종전대로 곧바로 열린다(불필요한 마찰 금지).
  - ★★ **빈 링크는 "시스템이 만든 무시트 작업"(가상 시트ID)뿐** — 구글 URL 로 조립하면 **죽은 링크**가 된다(반영 점검 `_gidOut` 과 같은 규율: 빈 링크 > 죽은 링크). 판정은 **`sheetlessAccept.isVirtualSheetId` 단일 출처**(접두 사본 금지 — 이 접두는 스코프 게이트가 아니라 **표시용 판정**이라 여기 쓰는 것이 맞다). ★ **"시트가 없음"과 "링크 미상(미등록·조회 실패)"을 구분해** 비활성 버튼 tooltip 으로 사유를 말한다(뭉뚱그리면 담당자가 원인을 엉뚱한 데서 찾는다).
  - ★ **`window.open` 은 onOk 동기 구간에서** — `onOk` 를 async 로 만들거나 `await` 뒤에서 열면 **브라우저가 팝업을 막는다**(회귀가드가 형태를 고정).
  - ★★ **저장 성공 뒤 순서 = 닫기 → 안내 → 재조회**(`_pmAfterFix` 한 벌): 후처리(toast·재조회)가 실패해도 팝업이 남으면 **성공이 실패처럼 보인다**(브라우저 실측으로 잡았다). `onOk` 예외도 삼키지 않고 사유를 말한다.
  - ★★ **표 `onclick` 은 인덱스만** — 종전 `_pmToggle('${esc(k).replace(/'/g,"&#39;")}')` 은 **HTML 속성이 엔티티 디코드 후 JS 로 파싱**되므로 `&#39;` 가 `'` 로 되살아나 시트발 탭명으로 문자열 탈출이 가능했다(기존 XSS 경로 — 이번에 제거).
- ⚠ **운영 함의**: 탭 설정에 이미 들어 있던 이체은행 값이 **이제 판정에 쓰인다**(확실히 해석되는 값만). 배포 후 화면의 출처 표기(`자동`/`수동`)로 한 번 훑을 것.
- 회귀가드 `tests/paymentIssueFix.test.js`(순수함수 실행 · 스텁 pool 로 서비스/라우트 **실제 실행** · 묶음 판정·시트 버튼 **vm 실행** · 라우터 스택 실검사 · XSS · 팝업 계약, **변이시험 12+15종 검출 확인**) + 브라우저 실행검증(카드 구성·버튼 4상태·클릭 noopener·경고 팝업 문구·팝업 저장). ⚠ **스텁은 SQL 을 해석하지 않는다** — `_loadTabMeta` 가 `sheetless`·`tab_gid` 를 실제로 **읽는지**는 SELECT 문자열을 따로 고정한다(변이시험이 이 구멍을 잡았다).
- ⚠ **곁다리로 고친 빨간 가드**: `paymentBatch` 의 "리뷰어앱 스코프 토큰은 이체 설정을 못 바꾼다"(권한 상승 검사)가 **배포 전부터 실패 중**이었다 — `_scopedCampaignEdit` 를 **고정 길이(i+3000)로 잘라** 함수가 자라자 UPDATE 뒷부분이 잘려 정규식이 못 맞췄다(실제 보안 속성은 살아 있었다). **함수 끝까지 보도록** 고쳤다. ★ **고정 길이 슬라이스로 함수 본문을 자르지 말 것** — 그 함수가 커지는 순간 가드가 조용히 빨개지고, 빨간 가드는 새 변경도 못 지킨다. ★ 쓰기 표면 가드는 **M1 본체 / 보완 경로 두 구역으로 분리** — 합쳐 검사하면 M1 이 조용히 넓어져도 통과한다.
