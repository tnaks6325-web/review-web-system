# ★★ 작업보드 리뷰제출 칸 = [📎 리뷰 대신 제출] (셀 옆 인라인 팝오버 · 붙여넣기 전용, 사용자 확정 2026-08-21)
- **신고**: 리뷰제출 칸을 우클릭해도 [이 셀 편집]만 흐리게 나오고 수동 제출 항목이 없다.
- ★★ **원인 = 상태 칸 판정이 이름 목록 사본이었다**: `_SUBMIT_HEADERS`(`리뷰제출`·`리뷰제출여부`·`리뷰제출완료`·`제출` **정확일치**)에 헤더가 그냥 **`리뷰`** 인 탭이 없었다. 그런 탭은 `columnResolver` 3단계가 정상적으로 채택하는 실재 형태라 **시스템은 그 칸에 제출 시각을 쓰는데 화면만 "평범한 칸"으로 봐서** ① 직접 타이핑이 열려 있고 ② [📎 수동 리뷰제출]은 안 떴다.
- ★★ **판정 단일 출처 = 그 탭의 `submit_col`**(= `campaign_participants.submit_col/2`, columnResolver 가 정한 값): 서버가 내부 응답에 **`statusCols {submit,paid}`** 로 실어 주고, 화면(`_workdeskStatusKind`)과 **편집 잠금(`_statusToggleForRow`)** 이 그것을 본다. 이름 목록은 **그 값을 못 받았을 때(구버전 백엔드·미감지)의 폴백**일 뿐 — **목록을 늘려 때우지 말 것**(탭마다 헤더가 다르다). `statusCols` 미동봉이면 종전 동작 100%(무회귀).
- ★★ **메뉴 주 항목 교체**: 리뷰제출 칸에서는 [이 셀 편집]을 **그리지 않고** 그 자리에 [📎 리뷰 대신 제출]을 둔다 — 그 칸은 어차피 상태 칸이라 흐린 편집 항목만 남으면 **할 수 있는 일이 없는 메뉴**가 된다. ★ 못 누르는 사유(**이미 제출됨 / 내부 담당자 아님**)는 숨기지 않고 흐린 항목 + 툴팁으로 말한다.
- ★★ **팝오버는 셀 옆 인라인**(`#wdRevPop` · **body 직속** — 표는 가로 스크롤 컨테이너라 안에 그리면 잘린다 · 앵커 배치·Esc·스크롤/리사이즈 닫기는 구매일자 달력과 **같은 규율** · 전역 리스너는 **최상위 1회** · onclick 은 **인덱스만**). 중앙 모달로 되돌리지 않는다.
- ★★ **첨부 창구는 붙여넣기(+드롭)뿐 — 파일 탐색기 창구를 두지 않는다**(사용자 확정): 카톡방에서 복사한 이미지를 **Ctrl+V** 로 바로 붙인다. 두 경로는 **`_manualReviewTake` 한 함수로 수렴**(사본을 두면 한쪽만 장수·용량 제한이 풀린다 — 1:1문의 첨부 3경로 규율). 붙여넣기는 **캡처 단계**에서 가로챈다(그리드 셀 범위 붙여넣기(text/plain)와 섞이지 않게).
- ★★ **붙여넣기만으로 자동 제출하지 않는다**: 제출은 작업표·`review_index`·누적금액까지 확정하는데 **되돌릴 창구가 없다** — 오붙여넣기 한 번이 그대로 사고가 된다. 붙이면 즉시 미리보기가 뜨고 **[제출 처리] 한 번**으로 끝난다(회귀가드가 `_manualReviewTake` 안에 제출 호출이 **없음**을 고정).
- ★ **직접 편집을 잠근 대신 있던 길을 없애지 않는다** — 캡처가 아예 없는 건(외부모집 사후 등록)은 팝오버 아래 **[날짜만 기록]**(기존 `review-submit-date` 백필 라우트, **adminOrMaster + 무시트일 때만 렌더** = 서버 게이트와 1:1).
- ★ **처리 실행부·서버 계약은 무변경**: `/api/image/review-upload`(원장 기록) → `/api/trackb/workdesk/manual-review-submit`(그 행 소유 파일 대조 후 `campaign_participants.is_submitted` + 리뷰제출 열 시각 + `review_index.is_submitted` 를 **한 트랜잭션**에서 확정) — 리뷰어 참여내역·제출완료 탭·누적금액 로직이 그대로 적용된다.
- ★★ **상태 칸은 줄이 아니라 탭 단위다 (2026-08-21 실측 `submit_column_missing`)**: `campaign_participants.submit_col` 은 **`review_index` 복제 경로(`importTabFromIndex`)에서만** 채워져, `addParticipant`(수동 추가)·`prepareRosterSlots`·`appendSlot`·`createWorktableSlots` 로 만든 줄은 그 칸이 **NULL** 로 남는다 → 화면은 잠그고 메뉴도 띄우는데 제출만 거부되는 상태가 됐다. **줄에 값이 없으면 그 탭의 감지값으로 보완**하되(해석기는 무시트 상태 기록과 같은 `sheetlessStatus.statusHeaderForTab` — 사본 0, 잠근 tx 라 **`client`** 로 조회), **줄이 들고 있으면 그 값이 이긴다**(무회귀) · **탭에도 없으면 여전히 거부**(fail-closed — 추측해서 아무 칸에나 시각을 박으면 담당자가 적어 둔 값을 덮는다).
- ★ **화면은 오류 코드가 아니라 다음 행동을 말한다**(`_MR_ERR`) — 모르는 코드는 **원문을 남긴다**(뭉뚱그리면 원인이 사라진다).
- ★★ **업로드가 먼저면 실패할 때 파일만 쌓인다 (2026-08-21 실사고 — 캡처는 드라이브에 있는데 제출 시각은 안 찍힘, 한 줄에 5장)**: 화면이 캡처를 올린 뒤 제출을 요청하던 순서라, 제출이 거부되면 **되돌릴 수 없는 외부 저장만 남았다**(재시도마다 한 장씩). → **사전 확인**(`manualWorkdeskReviewSubmit({preflight:true})` · **쓰기 0** · 파일 대조 **앞**에서 ROLLBACK)을 붙여넣기 **전에** 부르고, 못 낼 상태면 **첨부조차 받지 않는다**. ★ 게이트는 실제 제출과 **같은 함수**를 지난다(사본 0 — 따로 만들면 "확인은 통과인데 제출은 거부"). ★ 확인 실패(구버전 백엔드·네트워크)는 **막지 않는다**(fail-open — 서버가 최종 방어).
- ★★ **이미 쌓인 캡처가 곧 근거다** — 그 줄에 올라온 리뷰 캡처를 팝오버가 보여 주고 **업로드 없이** 제출할 수 있다(`_manualReviewSubmitExisting`). 실행부는 붙여넣기 경로와 **한 벌**(`_manualReviewFinish`). 서버 파일 수 상한(5)에 맞춰 **최근 5장**만 쓰고 그 사실을 말한다.
- ★★ **리뷰제출 칸이 없으면 작업표 생성이 만들어 붙인다**(`worktablePlan.ensureReviewColumn` — 택배송장·블로그 3열과 같은 규율). 자리는 **공통(core) 열 바로 뒤**(상태 칸 자리 — 맨 뒤에 붙이면 "back 유형 열이 맨 뒤"라는 열 순서 계약이 깨진다). ★ **열이 0개인 계획(표준 열 미설정)에는 넣지 않는다** — 넣으면 "먼저 설정하라"는 잠금이 풀려 리뷰 한 칸짜리 표가 만들어진다(회귀가드가 잡았다).
- ★★★ **"이 열이 제출 칸인가" 판정 단일 출처 = `columnResolver.findSubmitColumnIndex`**(인덱스 빌드의 인라인 3단계 판정을 그대로 추출·export). 작업표 생성이 **같은 함수**로 확인하므로 **"만든 칸 ≠ 읽는 칸"이 구조적으로 불가능**하다. ★ 문자열 grep 가드로는 `includes('리뷰')` 같은 **사본 규칙**을 못 잡는다 — 읽는 쪽이 거부하는 헤더(`주문자리뷰`)로 **실제로 돌려 보는** 단언을 함께 둔다(변이시험 실측).
- ★★ **구매양식 [주문자] = 로그인한 리뷰어 이름으로 자동 채움 (사용자 확정 2026-08-24 · "가" 안)** — 위 규율의 짝. 리뷰어에게 주문자를 한 번 더 치게 하지 않고 **서버가 채운다**: `submit.routes` 의 `_orderer = orderer || loginName` **한 값**을 필수 검증·`orderData`·SSE 세 소비처가 함께 쓴다(한 곳만 쓰면 "검증은 통과인데 원장은 빈 주문자"로 갈린다). ★★ **값이 오면 그 값이 이긴다**(빈 값일 때만 채운다) — 두 경로가 이 규칙에 기댄다: ㉮ **인애드명단이 있는 탭**은 리뷰어가 고른 이름이 **행 배정 근거**다(`orderLedger.buildCandidateRows` 가 인애드 열(`인애드·카톡·카카오·닉네임`) 값 == 제출 주문자인 행을 우선 배정) ㉯ **타계정 다건**은 카드마다 그 명의 이름이 프리필된다(`_batchDecorateCard`). ★ **로그인 이름이 없으면 채우지 않는다**(빈 값 그대로) — ⚠ 그 경로는 **애초에 주문자 필수 검증 대상이 아니다**(검증 블록 전체가 `if (_idPhone8.length === 8)` = 리뷰어 제출 안에 있다). 즉 레거시·관리자 경유 제출의 동작은 이 변경 전과 **완전히 같다**(가상테스트 실측). ★★ 화면은 **요소를 지우지 않고 감춘다** — `of_orderer` 를 참조하는 코드가 6곳(`_setOrdererDisabled`·`selectAcItem`·`onOrdererInput`·`_syncSharedInfoToCard`·`_lockSharedFields`·`_embedRestoreForm`)이라 지우면 그쪽이 null 을 만난다. ★ **인애드명단 후보가 1건이라도 있으면 도로 보여준다**(`_ordererPickerOn` 단일 판정 — 화면과 검증이 갈리면 "안 보이는데 필수"). ★ 조용한 자동 채움 금지(`주문자는 로그인한 본인 이름으로 자동 기록됩니다`) — 단 **배치에서는 그 안내를 감춘다**(그 카드는 명의가 주문자라 문구가 거짓이 된다). ⚠ 무시트 작업표는 인애드명단 후보가 **구조적으로 0건**이라(가상 시트ID를 못 읽고 표준 열에 인애드 열이 없다) 현 운영에서는 칸이 늘 감춰진다. 회귀가드 `tests/ordererAutoFill.test.js`(22 — vm 실행 4갈래, **변이시험 8종 검출 확인**).
- ★★★ **템플릿 리터럴 안 HTML 주석에 백틱을 쓰지 말 것 (실측 사고 2026-08-24)** — 카드 마크업 주석에 `` `of-orderer-on` `` 이라고 적자 **템플릿 리터럴이 그 자리에서 끊겨** 뒷부분이 코드로 파싱됐고, 카드 생성이 런타임 `ReferenceError: orderer is not defined` 로 죽었다. **`node --check` 는 통과한다**(구문은 여전히 유효하다) — 실브라우저가 잡았다. CSS `:root` 삼킴·`*/` 조기 종료와 같은 무신호 계열. ★ 회귀가드는 **주석 블록 전체**를 봐야 한다 — 줄 단위로 보면 **여러 줄 주석의 가운데 줄**(`<!--` 도 `-->` 도 없는 줄)을 놓친다(변이시험이 실제로 뚫었다).
- ★★ **`주문자` 열은 표준 열에 두지 않는다 (사용자 확정 2026-08-24)** — 리뷰웹시스템[3버전]에서는 **작업보드의 참여자 칸이 그 역할을 대체**한다. 근거 = 무시트 주문 기록이 참여자 이름을 `loginName || orderData.orderer || orderData.recipient` 로 정한다(`sheetlessOrder.service`) — 즉 주문자는 이미 참여자 칸의 폴백이고, 로그인 없는 경로(외부모집 수동제출)에서는 그 값이 그대로 참여자 칸에 들어간다. 주문자 원문은 **주문 원장 `order_submissions.orderer` 에 그대로 남는다**(칸이 없다고 값이 사라지지 않는다). ★ 작업표 생성기의 경고·차단 목록에 주문자 관련 항목이 없어 **조용한 누락 계열이 아니다**(구매일자·옵션 칸과 다른 성질). ★ 그래서 **프리셋(`WT_PRESET_CORE`)에서도 뺐다** — 버튼 한 번에 되살아나면 이 확정이 조용히 뒤집힌다(회귀가드 `worktableTemplate` 가 부재를 고정, 변이시험 검출 확인). ⚠ **열 이름·역할로서의 `주문자`(orderer)는 그대로 유효하다** — 시트 기반 탭·기존 표에 그 칸이 있으면 매퍼가 계속 기입하고 분류기도 core 로 인정한다(분류 케이스 불변). ⚠ 타계정 참여 건은 참여자 칸에 **로그인 본계정 이름**이 뜬다(구매양식에 적은 명의와 다를 수 있다 — 확인은 수취인 칸·참여횟수 배지).
- ★★ **표준 이름 = `리뷰`**(사용자 확정 2026-08-21): 표준 열 프리셋(`WT_PRESET_CORE`) · 시스템이 만드는 칸 이름 · 화면/서버 폴백 목록(`_WD_SUBMIT_HEADERS`/`_SUBMIT_HEADERS`)에 합류. **정확일치라** `리뷰옵션`·`리뷰비`·`리뷰타입`·`리뷰가이드`는 걸리지 않는다. ★ 기존 탭의 열 이름은 **바꾸지 않는다**(`리뷰제출`·`리뷰완료` 등은 그대로 인정 — 시트 기반 탭은 시트가 진실원본이라 이름을 바꾸면 다음 빌드에 되돌아온다).
- 회귀가드 `tests/workdeskManualReviewSubmit.test.js`(판정 단일 출처·메뉴 교체·파일 input 부재·붙여넣기·인라인 앵커·자동 제출 금지, **변이시험 8종 검출 확인**) + 실 http 오리진 브라우저 실측(메뉴 교체·팝오버 body 직속/앵커·붙여넣기 첨부·업로드→제출 왕복·차단 상태 2종·Esc·JS 오류 0). ⚠ 이 변경으로 착수 시점에 **이미 빨간 상태였던** `emptySlotCellEdit`(메뉴 항목이 조건부가 된 뒤 패턴 드리프트)과 `workboardTopC` ②(**고정 길이 슬라이스** `i-700` 이라 그 사이에 줄이 늘자 빨개졌다 → 바로 앞 `if (showEdits) {` 를 찾는 방식으로)를 함께 갱신했다(검사 의미 불변).
