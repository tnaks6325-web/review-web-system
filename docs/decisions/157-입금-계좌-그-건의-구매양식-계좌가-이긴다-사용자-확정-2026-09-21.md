# ★★★ 입금 계좌 = 그 건의 구매양식 계좌가 이긴다 (사용자 확정 2026-09-21 · 112 규율을 뒤집음)

- **사고(모기위키 모기기피제 439/440 · 2026-09-14 회차 41)**: 김솔지(1건)와 백운(1건)이 각각 참여했고 **구매양식에는 두 건 다 김솔지 계좌**(국민 `…9824`)가 적혀 있었다. 그런데 백운은 **정재석의 타계정**(등록DB에 "백운"이라는 리뷰어는 없고, 정재석의 `sub_accounts` 에 "백운"이 있으며 **그 타계정에는 전용 계좌가 비어 있다**)이라, 규율상 주인 계좌(신한 `…9898` 정재석)로 나갔다. 김솔지는 **2건을 기대했는데 1건만** 받았다.
- ★★ **시스템 오작동이 아니었다** — 같은 phone8 에 등록 리뷰어가 둘 이상인 건 **0건**이었고, 회차 결과 파일에서도 두 건이 **서로 다른 줄**(84·91번째)에 정상 매칭됐다. 문제는 **양식에 적힌 계좌를 보지도 않는 구조** 자체였다(112 의 "등록 계좌가 언제나 이긴다").
- **전수 점검(2026-09-21)**: 이체 완료 7,805건 중 양식 계좌와 **대조 가능한 3,229건**에서 **불일치 40건**(그중 **예금주까지 다른 건 23건 · 746,800원** · 2026-08-12~09-20). ★ 그중 **31건은 리뷰어가 직접 제출한 건**이다 = 양식에서 계좌를 바꿔 적어도 반영되지 않았다(관리자 수동등록은 9건). ★ 같은 계좌를 여러 리뷰어가 등록 계좌로 쓰는 묶음이 **111개 · 281명**(최대 9명)이라는 사실도 함께 드러났다 — 그 정당성은 별건 조사 대상.
- ★★★ **확정 = 그 건의 구매양식(작업보드) 계좌가 이긴다**(사용자 확정 2026-09-21). 해석 순서가 **① 그 건의 구매양식 → ② 등록DB(소유자 링크 → 연락처)** 로 뒤집혔다. ⚠ 112 의 "등록 계좌가 언제나 이긴다"는 **이 항목으로 대체됐다** — 되살리면 이 사고가 그대로 재현된다.
- ★★ **완화 금지 5종**:
  - **반쪽 값은 인정하지 않는다** — `_orderAccount` 가 은행·계좌·예금주 **셋이 다 있을 때만** 객체를 돌려준다(반쪽으로 만든 이체 파일은 은행이 통째로 거부한다). 반쪽이면 종전대로 등록DB.
  - **`reviewerId` 를 만들지 않는다**(112 규율 유지) — 양식 계좌는 지목할 등록 리뷰어가 없어 회차 스냅샷 대조에서 `unverifiable` 로 빠져야 **다운로드가 막히지 않는다**.
  - **신원 추적 필드는 등록DB 기준을 유지한다**(`ownerReviewerId`·`participantIdentityId`) — "누가 참여했나"는 "어디로 보내나"와 별개다. 여기까지 비우면 회차 항목의 소유자 추적이 끊긴다.
  - ★★ **두 계좌가 같으면 아무 말도 하지 않는다** — 양식 계좌가 기본 경로가 되면서 `accountSource==='order'` 가 대부분의 행에 붙는다. 그때마다 배지를 띄우면 **진짜 신호(불일치)가 묻힌다**(늑대소년). 서버가 `accountMismatch` 를 **다를 때만** 싣고, 화면은 그 값을 **그리기만** 한다(판정 사본 금지).
  - **비교는 숫자만**(`normalizeAccount`) — `725602-00-129824` ↔ `72560200129824` 를 다름으로 오판 금지.
- ★★ **보류시키지 않는다(사용자 확정) — 체크박스로 사람이 정한다**: 불일치는 `issues` 가 아니라 **`warnings`(`account_form_override`)** 라 `payable` 은 true 이고 **체크박스는 기본 체크**다(병목 방지). 행 바로 아래 안내줄이 **두 계좌를 나란히** 보여주고 마지막 줄이 `체크박스를 풀면 해당 건 입금은 보류됩니다` 를 말한다. 체크를 풀면 그 건만 회차에서 빠진다.
  - 안내는 **세 경우를 구분**한다: 예금주까지 다름(빨강 태그) / 계좌번호만 다름 / **등록 계좌가 아예 없음**(`registered: null` — 없는 계좌를 지어내지 않는다).
- ★ **되돌리기 = env `PAYMENT_FORM_ACCOUNT_FIRST=0`**(코드 변경 0). 판정은 `_formAccountFirst()` 가 **호출 시점에** 읽으므로 재배포 없이 즉시 종전 동작으로 돌아간다.
- ★ **쓰기 표면 0** — 이체 파일 생성·결과 반영·회차 구조·신규 테이블 전부 무접촉. 어느 계좌로 보냈는지는 `payment_batch_items` 에 이미 박제된다.
- ⚠ **잔여(문서화)**: ㉮ **과거 40건은 손대지 않았다**(사용자 확정 "앞으로만 막기") ㉯ 양식 계좌 우선 = **리뷰어가 적은 값이 곧 송금처**다 — 등록DB 방어벽이 사라지므로 안내줄을 놓치면 그대로 나간다 ㉰ 관리자 [외부모집 수동제출]로 등록한 건은 **카톡 원문이 저장되지 않아**(파싱 결과만 저장) 잘못 입력인지 요청대로인지 시스템으로는 구분할 수 없다.
- 회귀가드 `tests/paymentSubAccountOwner.test.js`(33 — §2-2 우선순위 뒤집힘·같으면 `null`·`registered: null`·신원 추적 유지·반쪽 값 거부·**env 스위치 왕복** + §5 화면(안내줄 렌더 3갈래·escape·배지 조건·**표 칸 수 ≡ 안내줄 colspan**)). ⚠ 가드 함정: 헤더 칸을 `/<th/` 로 세면 **`<thead>` 가 함께 잡혀 한 칸이 늘어난다**(실측) → `/<th[\s>]/`.
