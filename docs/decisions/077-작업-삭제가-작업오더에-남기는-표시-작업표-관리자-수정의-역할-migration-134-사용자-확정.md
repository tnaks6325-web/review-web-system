# ★★ 작업 삭제가 작업오더에 남기는 표시 + [작업표]·[관리자 수정]의 역할 (migration 134, 사용자 확정 2026-08-21)
- **신고 3건**: ① 작업을 삭제해도 **보낸오더에 아무 흔적이 없다** ② 보낸오더 행의 **[📋 작업표] 버튼이 의미가 있나** ③ **[✏ 관리자 수정]이 모집공고 설정과 전부 중복 아닌가**.
- ★★ **① 삭제 표시**: 홈 작업목록 [작업 삭제]는 연결 작업오더의 `linked_tab_*` 를 **비우기만** 하고 오더는 남긴다(같은 오더를 다시 접수할 수 있게) → 그 오더가 목록에서 **"아직 접수 안 한 오더"와 똑같이** 보였다. 134 = `work_orders.tab_deleted_at/_by/_tab`(**컬럼 추가만·백필 0**)를 **링크 해제와 같은 UPDATE 문장**에서 새긴다(따로 쓰면 한쪽만 실패한다). ★ **재접수하면 accept 가 세 칸을 비운다** — 다시 연결됐는데 삭제 배지가 남으면 화면이 거짓을 말한다(이력은 `memo_log` `kind:'tab_deleted'` 에 남는다). ★ `REQUIRED_SCHEMA` 등록(컬럼 없으면 삭제가 전면 42703).
- ★★ **인트라넷 "보낸 오더" 알림 실행부 = `services/intranetMemo.service.js` 한 벌**(처리메모 전송·관리자 수정 알림·작업 삭제 알림 공용 — order.routes 의 인라인 사본을 이관, 사본 금지). 값 자체는 `intake/list` 응답에 `tab_deleted_at`·`tab_deleted_tab` 을 실어 보내되(**인트라넷이 그 필드를 읽도록 고친 뒤에야 보인다**), **지금 있는 채널**인 처리메모 webhook 으로 push 해 인트라넷 배포 없이 주황 메모로 바로 뜬다. ★ **커밋 뒤에만** 부르고 **절대 throw 없음**(삭제는 이미 끝났다) · 알릴 대상은 링크 해제 UPDATE 의 `RETURNING` 으로 받는다(오더 재조회 0).
- ★ **화면**: 목록 행 `🗑 작업 삭제됨 · MM.DD HH:mm` 배지(작업명·삭제자는 **툴팁**으로 — 행이 길어지지 않게) + 펼침 한 줄 안내(다음 행동 = **재접수**). ★ **값이 없으면 아무것도 그리지 않는다**(구버전 백엔드·삭제 이력 없음을 0/미상으로 꾸미지 않는다) · 시트발 문자열 escape.
- ★★ **② [📋 작업표] = 접수 전 창구다(유지)** — 조사 결과 **모집공고 저장은 작업표 줄을 만들지 않는다**(`campaign.routes` create/update 는 `ensureCampaignWorktable`·`createWorktableSlots`·`buildWorktablePlan` 을 한 번도 부르지 않는다. `ensureCampaignWorktable` 은 `submit.routes` **한 곳**에서 = 첫 주문 제출 시점의 안전망이고 **열 구성만 만들고 줄은 안 만든다**). 줄을 만드는 것은 **접수** 하나뿐이고, 그 모달의 **[✅ 이 구성으로 접수하기]가 곧 접수 창구**다(총건수·일건수·시작일·주말·휴무일·채널·작업유형을 조정해 접수). ★ 접수 **뒤**에는 같은 모달이 "지금 만들면 이렇게 된다"는 **가정 계산**이라 실제 표(주문 유입·번호 정리·중복 정리로 달라진 표)와 어긋난다 → 접수된 오더에는 **[📊 작업보드]**(관측 뷰·업체관리와 **같은 `pendingTab` 계약**)로 바꿔 **실제 표**로 보낸다. 연결이 없으면 사유를 말한다(막다른 길 금지).
- ★★ **③ [✏ 관리자 수정] = 발주서 원문이라 유지 — 다만 화면이 그 사실을 말한다**: `ADMIN_EDIT_FIELDS` 22칸 중 **19칸이 모집공고와 겹치지만 역할이 다르다** — 이 화면 = **발주서 원문**(인트라넷 보낸 오더 카드가 실시간 조회) / 모집공고 = **리뷰어에게 보이는 값과 참여 정원**. 겹치지 않는 **3칸 = 담당AE(`manager_name`) · 물건비(`goods_cost_type` — 입금 이체은행 자동분류) · 택배대행(`courier_proxy` — 작업표 열 자동 판정)**. 게다가 **작업표 계획(열·행 수·날짜 분배)은 오직 `work_orders` 만 본다**(`buildWorktablePlan({workOrder, template})`)라 접수 전에는 여기가 유일한 창구이고, **발주 정원 폴백**(공고 총인원·일건수 0이면 오더 값이 정원)·`review_type_mix` 프리필도 살아 있다. → 모달 상단 배너가 공고 연결 여부로 갈려 말하고(연결 = "리뷰어 화면·정원은 공고가 정한다" + 여기서만 정하는 3종 / 미연결 = "이 값이 작업표 계획·정원의 기준"), 겹치는 칸(총모집·일건수·리뷰비·리뷰타입)에는 **"공고 우선"을 한 줄로** 덧붙인다(조용한 no-op 오해 방지). ★ 미연결이면 그 줄을 **그리지 않는다**.
- 회귀가드 `tests/workOrderTabDeleted.test.js`(39 — 스텁 pool 로 `deleteTask` **실제 실행**(파라미터·알림·memo_log·fail-soft·Drive 무접촉) + 배지 렌더 vm 실행(값 부재·XSS) + 배선, **변이시험 14종 검출 확인**) + 실 http 오리진 브라우저로 모달 배너 2갈래 실측(JS 오류 0).
