# ★★ 작업보드 우클릭 → 리뷰어에게 메시지 (관리자가 문의방을 먼저 연다 · 2026-08-21, 스키마 변경 0)
- **발단**: 문의방은 **리뷰어가 먼저 보내야만** 생겼다 — 관리자가 먼저 말을 걸 창구가 없어 카톡·전화로 새어 나갔다. 이제 작업보드 표에서 그 줄을 우클릭해 바로 보낸다. 기획·유저플로우 = `frontend/docs/prd-workboard-reviewer-message.html`.
- ★★ **실행부는 기존 `csBridge.postAdminNotice` 한 벌**(리뷰검수 반려·입금 실패 안내가 쓰는 그 함수) — 방 생성·`admin_unread` 0·닉네임 fail-closed 치환·SSE 푸시·**절대 throw 없음**이 그대로 상속된다. **마이그레이션 0 · 신규 저장소 0**(쓰기 표면 = `cs_threads`/`cs_messages` 두 곳뿐).
- ★★★ **받는 사람은 표의 명의가 아니라 로그인 본계정이다(완화 금지)** — 리뷰어 문의함은 `reviewer.routes` 의 `WHERE reviewer_phone8 = $1`(**로그인 phone8 하나**)로만 조회하므로, 명의(타계정) 번호로 방을 열면 **리뷰어 화면에 영영 안 보이는 유령 방**이 되고 관리자는 보냈다고 믿는다. 그래서 이 판정은 편의가 아니라 **동작 조건**이다.
- ★★ **판정 단일 출처 = `services/csRecipient.service.js`**(미리보기·전송이 **같은 함수** — 사본을 두면 "미리보기엔 A인데 실제로는 B에게 갔다"). 순서 = ① `campaign_applications.owner_phone8`(주문 id 로 그 줄에 결속 · 홀드 생성 시 명의 검증을 거친 값) → ② `participation_links.phone8`(제출 성공 후에만 남는 로그인 번호, ★ **그 소유자의 타계정 목록에 그 줄 이름·번호가 있을 때만** — 재배정된 줄의 stale 링크 차단) → ③ 명의가 곧 등록 리뷰어 → ④ `sub_accounts` **번호 역조회**(소유자 유일할 때만).
- ★★ **완화 금지 4종**: **이름만으로 소유자를 추측하지 않는다**(동명이인 한 번이면 남의 채팅방에 그 사람 작업 얘기가 들어간다) · **모호하면 안 보낸다**(후보 2명 이상·근거 0 = 쓰기 0건 + 사유를 문장으로) · **등록 리뷰어가 아니면 안 보낸다**(로그인 못 하는 방은 아무도 못 읽는다) · **보내기 전에 받는 사람을 화면이 말한다**(타계정이면 "명의 ○○○ · 본계정 △△△" 를 밝힌다 — 조용한 전송 금지).
- ★ **판정은 읽기 전용**(SELECT 만) · 대상 줄은 **표와 같은 조건**(`deleted_at IS NULL AND active AND held_at IS NULL` — 숨김·분리된 줄은 대상 아님) · 표 이름 ≠ 등록 이름은 **막지 않고 경고만**(수취인 이름이 적힌 정상 케이스가 있다).
- **API** = `GET /api/trackb/cs/participant-recipients`(**쓰기 0** 미리보기) · `POST /api/trackb/cs/notify-participants`, 둘 다 `authMiddleware + internalMiddleware` = **광고주를 제외한 내부인 전원**(사용자 확정 2026-08-21). ⚠ C/S 본문 조회 8경로는 여전히 master/admin 전용이라 **AE 는 보낼 수는 있어도 그 방의 답장은 못 본다** — 팝업이 그 사실을 문장으로 말하고 **[문의방 열기] 버튼은 AE 에게 안 그린다**(눌러도 403 인 막다른 길 금지).
- ★★ **같은 본계정은 한 번만 보낸다** — 한 리뷰어가 그 작업에 2회 참여했으면 줄은 둘인데 **방은 하나**다(`UNIQUE(reviewer_phone8, campaign_key)`). 합친 건수를 `merged` 로 **말한다**(조용히 버리지 않는다). ★ **전송 시 서버가 다시 판정**하고 미리보기 때와 대상이 달라졌으면(`expect` 불일치) **그 건은 보내지 않는다**(TOCTOU — 주문 취소·재배정).
- ★ **방 키는 `csBridge.campaignKeyOf`(`sheetId||tabName`) 단일 출처** — 리뷰어 UI·교체요청·검수 반려와 같은 규칙이라 **이미 열려 있는 그 작업 방에 이어 붙는다**(다른 규칙을 쓰면 같은 작업에 방이 두 개 생긴다). 라벨은 탭 표시명(**시트 제목 미노출**), 기존 방의 라벨은 **덮지 않는다**.
- **화면**(`workdesk.html` 우클릭 메뉴 → 전용 팝업, 사용자 확정): 여러 줄을 잡으면 `리뷰어 N명에게 메시지`(다건 일괄 발송) · 팝업은 **body 직속** · **바깥 클릭으로 닫지 않는다**(입력 보호, Esc·[취소]만) · Esc 리스너 **최상위 1회** · **onclick 은 인자 없음**(줄은 선택 상태에서 읽는다 — 시트발 문자열 보간 0) · **재렌더가 textarea 를 다시 만들지 않는다**(목록·푸터만 갈아끼운다 — 한글 IME 조합 파괴 금지) · 전송 실패 시 **입력 내용을 지우지 않는다** · 받는 사람은 **등록된 전체 연락처**를 보여준다(사용자 확정 2026-08-21 — 뒤 4자리로는 누구인지 확인이 안 된다). ★ 번호는 **등록 원장(`reviewers.phone`) 값**이고 `phone8` 앞에 `010` 을 **지어내지 않는다** — 못 받았거나 같은 phone8 을 쓰는 행이 둘이면 뒤 4자리로 접는다(표기 단일 출처 `_rmPhone` + 표와 같은 `_fmtPhone`).
- ★ **[문의방 열기]는 C/S 목록의 그 행을 눌러서 연다**(`.cs-room-row[data-tid=]`.click()) — 이름·연락처를 화면에 한 번 더 싣지 않기 위해서다.
- ⚠ 잔여(문서화): 리뷰어는 **본계정으로 로그인해야** 본다(타계정 세션은 자기 phone8 방만 조회) · 사진 첨부는 방을 연 뒤 C/S 대화창에서(기존 3경로) · **메시지 회수·삭제는 없다**(팝업이 "보내면 즉시 도착 · 회수 불가"를 말한다).
- ★★★ **`cs_messages.meta` 에 맨 `null` 을 넘기면 안 된다 — 카드 없는 안내가 조용히 전멸해 있었다 (실사고 2026-08-21)**: `meta` 는 **NOT NULL DEFAULT `'{}'`**(080)인데 **기본값은 컬럼을 생략했을 때만** 적용된다. `postInspectionReject` 가 카드 없을 때 `null` 을 넘겨 **23502 로 전건 실패**했고, 이 함수는 계약상 **절대 throw 하지 않아** warn 로그만 남고 화면은 `메시지를 보내지 못했습니다` 한 줄이었다 → **리뷰검수 [✕ 불량] 반려 본문 · 입금 실패 안내 · 작업보드 선톡 3종이 조용히 안 나가고 있었다**. 수정 = 컬럼 목록은 그대로 두고 **SQL 에서 `COALESCE($5::jsonb,'{}'::jsonb)`** 로 접는다(카드 경로 불변). ★★ **스텁 pool 로는 절대 못 잡는다**(스텁은 제약조건을 모른다) → 회귀가드 `tests/csBridgeMetaPg.test.js`(**진짜 PG16** — 카드 없는 안내가 실제로 저장되는지·`meta={}`·`admin_unread` 0·카드 경로 보존, 변이시험 검출 확인). ★ 같은 계열(무신호 DB 제약 위반)은 **한 번 넣어 봐야** 잡힌다 — 123 트리거 사고와 같은 규율.
- ★★ **방 생성·메시지·방 갱신은 한 트랜잭션이다 (실측 신고 2026-08-21 — "보냈는데 채팅방이 비어 있다")**: 종전 구조는 방을 먼저 만들며 `last_message_at`·`last_message_preview` 까지 채우고 메시지를 **따로** 넣어서, 메시지 INSERT 가 실패하면 **목록에는 보낸 것처럼 보이는데 열면 "아직 메시지가 없습니다" 인 빈 방**이 남았다(위 23502 사고의 화면 증상). 지금은 `pool.connect()` + BEGIN/COMMIT 으로 묶어 실패 시 **그 방이 아예 안 생긴다**. ★ SSE 푸시는 **커밋 뒤**(후처리 실패가 저장을 되돌리지 않는다) · 계약(절대 throw 없음 · 성공 객체|null)은 불변. ★ 회귀가드는 **진짜 PG 에 트리거로 실패를 주입**해 빈 방 0을 확인한다(스텁으로는 만들 수 없는 상황). ⚠ 이 변경으로 스텁 pool 을 쓰는 가드 2종(`inspectRejectNotify`·`inspectMessageSettings`)에 **`connect()` 스텁**을 추가했다(검사 의미 불변) — 그리고 `inspectMessageSettings` 의 meta 블록 경계를 **선언 형태 대신 SQL 문**(`INSERT INTO cs_messages`)으로 잡도록 고쳤다: 선언에 기대면 `slice` 가 −1 로 무너져 **검사가 조용히 무의미해진다**(이번에 실제로 밟았다).
- ★★ **실패 사유를 호출자에게 흘려보낸다** — `postAdminNotice`/`postInspectionReject` 에 **가산 옵션 `onError`**(반환 계약은 성공 객체 | `null` 그대로 = 기존 호출부의 `if (!out)` 판정 불변, 안 넘기면 동작 불변). 라우트가 그 사유를 `failed[].reason` 에 실어 **화면이 건별로 그대로** 보여준다(에러 코드 포함) — "보내지 못했습니다" 한 줄로 뭉개면 담당자가 손쓸 수 없다(이번 사고의 절반이 이것이었다).
- ★★ **팝업 전용 클래스는 전부 `rm` 접두** — `.rmrow` 는 이미 **명단 × 버튼**(22×22 고정)이 쓰던 전역 클래스라 팝업 줄이 그 상자에 갇혀 **글자가 세로로 한 글자씩 접히고 겹쳤다**(실측 신고). 맨몸 `.why`·`.num`·`.tag` 도 다른 화면이 쓴다(레포 `ad*`·`wbl-` 접두 규율과 같은 자리). 배치는 `[이름] ···뒤4 [타계정 참여]` 한 줄 + **사유는 아랫줄 블록**(줄바꿈 허용).
- ★★ **화면은 중앙 팝업이 아니라 "누른 셀 옆 인라인 팝오버"다 (사용자 확정 2026-08-21 2차)**: 표를 훑다가 그 줄에 바로 말을 거는 일이라, 화면 한가운데로 튀면 어느 줄에 보내는지 눈으로 잃는다. 배치는 **구매일자 달력·[📎 리뷰 대신 제출] 과 같은 앵커 규율**(`_rmPlace` — 셀 왼쪽 정렬 + 바로 아래, 아래가 좁으면 위로 뒤집음).
  - ★★ **양쪽 다 좁으면 "더 넓은 쪽에 붙이고 안에서 스크롤"** — 화면 위(`top:8`)로 클램프하면 그건 셀 옆이 아니라 그냥 뜬 창이라 요청이 무너진다(실브라우저가 잡았다). `max-height` 를 남은 공간에 맞춰 준다.
  - ★★ **겉면은 `pointer-events:none`** — 팝오버가 떠 있어도 **표를 계속 보고 조작**할 수 있다(전면을 흡수하면 모달이다). 그래도 **바깥 클릭으로 닫지 않는다**(본문·첨부 보호) — 닫는 길은 [취소]·Esc 둘.
  - ★★ **스크롤·리사이즈에 앵커를 다시 잰다**(`anchorEl` 을 들고 다닌다) — 좌표만 저장하면 표를 조금만 굴려도 엉뚱한 자리에 남는다. **달력은 그럴 때 닫지만 여기는 입력 중이라 닫을 수 없어 따라간다.** 셀이 재렌더로 사라지면 마지막 좌표를 유지한다(위치를 잃었다고 팝오버를 지우지 않는다).
- ★★ **사진은 붙여넣기(Ctrl+V)·드래그로만 받는다 — 파일 탐색기 창구를 두지 않는다**(사용자 확정 · [📎 리뷰 대신 제출] 과 같은 규율): 카톡방에서 복사한 이미지를 그대로 붙인다. 두 경로는 **`_rmTake` 한 함수로 수렴**(사본을 두면 한쪽만 장수·용량 제한이 풀린다) · 붙여넣기는 **이미지일 때만** 가로채고(텍스트 붙여넣기 기본 동작 보존) · 드래그오버는 **파일 드래그일 때만** 반응 · 리스너는 **최상위 1회**.
  - ★ **붙이는 즉시 업로드하고 끝난 것만 전송**(`_rmSendable` 이 인플라이트 중 전송 차단) · 상한 5장 · 한 장 8MB · **초과는 조용히 버리지 않고 사유를 말한다** · 첨부 빼기 onclick 은 **인덱스만** · 닫을 때 objectURL 반납.
  - ★★ **업로드 실행부는 `/cs/upload` 와 같은 핸들러**(Drive `[문의첨부]` 폴더·8MB·프록시 URL — 규칙 복제 0). 창구만 `POST /api/trackb/cs/notify-upload`(**internalMiddleware**) 로 연다 — 메시지를 보낼 수 있는 사람은 사진도 붙일 수 있어야 한다(눌러도 403 인 막다른 길 금지). `/cs/upload`(관리자 대화창)의 종전 `adminOrMaster` 는 그대로.
  - ★★ **첨부 URL 화이트리스트는 `utils/csImageUrls` 단일 출처** — 종전엔 같은 정규식이 `cs.routes`·`reviewer.routes` 두 곳에 복사돼 있었다(한쪽만 넓히면 그 경로로 임의 URL 이 리뷰어 화면 `<img src>` 로 들어온다). `csBridge` 도 저장 전에 이 함수를 통과시키고, **`image_urls` 도 `COALESCE($6::jsonb,'[]'::jsonb)`** 로 접는다(meta 23502 와 같은 계열).
  - ★ **본문이 비어도 사진만으로 보낼 수 있다**(리뷰어 답장과 같은 규칙) — 그때 방 미리보기는 `사진 N장`(빈 줄로 남기지 않는다). **본문도 없고 통과한 사진도 없으면 보내지 않는다.** SSE 푸시에도 첨부를 싣는다(목록 API 만 고치면 실시간 푸시에서 사진이 사라진다).
- 회귀가드 `tests/workboardReviewerMessage.test.js`(80 — 스텁 pool 로 판정 4갈래 **실제 실행**(우선순위·stale 링크 미채택·모호 미채택·읽기 전용) + 라우터 스택 실검사 + **핸들러 실제 호출**(중복 합치기·TOCTOU·빈 본문/과길이 시 발송 0) + 화면 배선 + **인라인 팝오버·첨부 규율**(앵커·pointer-events·한 함수 수렴·파일 탐색기 부재·인플라이트 차단·사본 0)) + `tests/csBridgeMetaPg.test.js`(**진짜 PG16** — 첨부 저장·화이트리스트·"사진 N장" 미리보기까지) + **실 http 오리진 브라우저 실측 18항목**(셀 옆 배치·아래가 좁으면 위로 뒤집되 붙어 있음·표 조작 가능·스크롤 추적·붙여넣기 첨부·업로드 중 잠금·payload `imageUrls`·IME 보호·Esc·JS 오류 0).
