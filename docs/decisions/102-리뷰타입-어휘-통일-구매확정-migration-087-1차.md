# ★★ 리뷰타입 — 어휘 통일 + 구매확정 (migration 087) · 1차
- **와이어프레임** `frontend/docs/리뷰타입_구매확정_정합_와이어프레임.html`(맞춰야 하는 항목 26개 = A 용어·값 5 / B 통로 5 / C 화면 4 / D 슬롯 6 / E AI 6, 칸별 지금·맞출 값·안 맞추면 생기는 일).
- ★★ **어휘 충돌 정리(사용자 확정)**: 같은 `review_type` 칸을 두 목록이 나눠 쓰고 겹치는 값이 `구매확정` 하나뿐이었다 — 인트라넷 발주 폼(`포토·텍스트·구매확정·별점·혼합` = 리뷰 결과물 형태) vs 옛 탭 팝오버(`실배송·빈박스·구매확정·믹스` = 앞 둘은 사실 배송유형). **인트라넷 목록으로 통일**하고 이름도 **리뷰타입** 하나로. 실배송/빈박스는 `tab_configs.delivery_type`(이미 있고 접수가 자동으로 채움 — 팝오버에서 배송유형은 *"진행차수/배송유형 제거됨 (자동화)"* 로 이미 빠져 있었다)로, `믹스`→`혼합`.
- ★★ **판정 단일 출처 = `utils/reviewType.js`**(`normalizeReviewType`·`resolveReviewType`). 우선순위 = **시트 작업옵션 칸(행별) > 공고 `recruit_campaigns.review_type` > 탭 `tab_configs.review_type`**. ★ **혼합뿐이면 null** — 어느 행이 무슨 유형인지는 시트에만 있어, "혼합이니까 일단 리뷰"로 접으면 구매확정 행이 리뷰로 잘못 검수된다. ★ **판정 실패는 전부 null = 오늘 동작 그대로**(opt-in). ★ 혼합 판정을 **포토보다 먼저** — `혼합(포토 10건, …)` 문자열에 '포토'가 들어 있어 순서가 뒤면 오판한다.
- ★★ **1차 필터 안전핀(완화 금지)**: 구매확정 완료 화면은 AI 가 아직 아는 종류가 없어 `kind='other'` 로 판정하고 `precheckPolicy` 가 확신 0.9 이상에서 **첨부를 잠근다** = 정상 제출 전면 차단(참여 소각). → `reviewType==='confirm'` 이면 `skip('purchase_confirm_tab')`. **구매확정 화면을 판별하는 층(kind `purchase_confirm`)이 붙기 전까지 이 핀을 빼지 말 것.** `loadTabExpectations` 가 채널과 **같은 LATERAL 행**에서 리뷰타입을 읽는다(따로 조회하면 다른 공고 값이 섞인다).
- **저장**: 공고 create INSERT·update **CASE 센티널**(null=유지 / `''`=해제) + **버튼군 UI 있는 화면에서만 전송**(옵션표·이체설정과 같은 원칙 — 리뷰타입 칸 없는 축약 화면이 저장해도 설정이 안 풀린다). ★ `_scopedCampaignEdit` 화이트리스트 **미포함**(리뷰어앱 스코프 토큰이 검수 정책을 못 바꾼다). ★ `REQUIRED_SCHEMA` 등록(컬럼 없으면 공고 발행·수정 전면 42703).
- ★ **`_rfPickBtn` 은 `rf_<group>` 으로 hidden 을 찾는다** — channel/manager 만 알던 삼항을 일반화했다(그룹이 늘 때마다 삼항을 덧대면 새 그룹이 조용히 빠진다). 프론트 프리필용 라벨 표(`RF_REVIEW_TYPE_LABELS`)는 최소 사본이고 **회귀가드가 서버 `REVIEW_TYPES` 와의 일치를 고정**(workManager 사본 규율).
- **정리 화면** = 설정 탭 **[✅ 리뷰타입 정리]**(`admin-settings.js` `reviewtype` 패널, 관리자 대시보드·리뷰웹시스템[3버전] 공용) — [미리보기]로 건수를 본 뒤에만 [적용하기]가 나타난다. ★ 경로는 **재기준하지 않는다**(`RTC_EP = /api/trackb/settings/review-type-cleanup`) — `authMiddleware` 는 admin 토큰을 받고 `via:'intranet'` 격리는 인트라넷 토큰을 trackb 로 **한정**할 뿐이라 양쪽 호스트가 그대로 닿는다(작업표 표준열과 같은 판단, `EP_DEFAULT`/`EP_SUFFIX` 키 개수 불변). ★ 펼칠 때 자동 실행하지 않는다.
- ★ **옛 값은 화면에서 지우지 않는다** — 선택지에서 빠진 `실배송·빈박스·믹스` 도 배지 색 맵·CSS 를 남겨 그대로 표시한다(무엇이 설정돼 있었는지 알아야 한다). 정리는 **자동 마이그레이션이 아니라** `POST /api/diag/review-type-cleanup`(dryRun 기본, admin/master) — ★★ 배송유형 이관은 **blank-only**(접수가 채운 값을 덮지 않는다). 안 돌려도 안전(판정에서 null 로 떨어진다).
- ★ migration 087 은 **컬럼 추가만**(백필 0) · **TEXT**(082 의 42804 계열 회피) · **DB CHECK 없음**(판정을 코드와 DB 에 이중화하면 어휘 확장 때마다 저장이 실패한다).
- 회귀가드 `tests/reviewType.test.js`(순수함수·`precheckPolicy` **실행** + 배선 + 권한 경계 + 사본 일치. 변이시험 검출 확인). 테마 없는 호스트에서 모달 **실브라우저 렌더** 확인.
- 회귀가드 `tests/reviewType.test.js`, 변이시험 검출 확인.
