# ★★ 고아 캡처 정리 — A 자동 · B·C 수동 (2026-08-21 A / 2026-08-24 B·C · migration 134)
- **발단(사용자 질문)**: "행삭제/구매기록 취소하면 Drive 캡처도 같이 지워지나?" → **아니다.** `orderCancellation.service`(전체 67줄)도 `workdeskOrderDelete` 도 Drive 참조가 **0건**이고, 되돌릴 수 없는 **작업 통째 삭제(`workTabDelete`)조차** `review_submissions`·`order_submissions` 행은 DELETE 하면서 **Drive 파일은 남긴다**. 그래서 지울수록 "폴더엔 캡처가 있는데 화면엔 리뷰 이미지 미등록"인 고아가 쌓이는데, 치우는 자동 경로가 **어디에도 없었다**(중복 정리 도구 `fileRoute`/`dedupe` 는 SHA-256 지문이 같은 **사본**만 잡는다).
- **고아는 3종** — ㉮ **A 링크 끊김**(파일·원장은 있는데 그 파일을 붙들던 주문/명단 행이 사라짐) ㉯ **B 원장 없음**(폴더엔 있는데 원장 어디에도 없음) ㉰ **C 작업 소멸**(작업이 통째 삭제돼 폴더만 남음).
- ★★ **자동 대상은 지금도 A 하나뿐**(사용자 확정 2026-08-21: 2단계 · DB 기준만 · 유예 7일). 2026-08-24 에 **B·C 를 수동 창구로 추가**했지만 **크론은 여전히 A 만 돈다** — 회귀가드가 크론 소스에 `trashTombstonedCaptures`·`trashFolderOrphans` 가 **없음**을 고정한다.
- ⚠ **2026-08-24 main 병합 이후**: `workTabDelete` 에 **Drive 폴더 휴지통 이동**(`deleteDrive` 옵트인)이 생겼다. 켜고 지우면 그 작업의 폴더가 통째로 휴지통으로 가므로 C 후보가 애초에 안 생긴다. 묘비는 **그 옵션을 끄고 지웠을 때**(기본값)의 경로다 — 이미 휴지통에 있는 파일을 다시 보내는 것은 무해하다(Drive 멱등).
- ★★ **C(작업 소멸) 해결책 = 묘비**(migration 134 `orphan_capture_tombstones`) — C 가 탐지 불가였던 이유는 `workTabDelete` 가 원장 행을 **실제 DELETE** 해 DB 에 흔적이 0이 되기 때문이다. 그래서 **지우기 직전에 파일 좌표만** 따로 남긴다. ★★★ **순서가 계약** — 묘비 INSERT 가 `DELETE_TABLES` 루프보다 **앞**이어야 한다(뒤면 읽을 원장이 이미 없다). ★ **fail-soft** — 묘비 기록 실패가 삭제를 막지 않는다(묘비는 청소 편의지 삭제의 전제가 아니다). 다만 조용히 넘기지 않고 사유를 로그로 남기고 `recordedTombstones` 로 건수를 보고한다. ★ `ON CONFLICT (file_id) DO NOTHING` — 재실행해도 같은 파일이 두 번 안 남는다. ★ 남기는 것은 **좌표뿐**(파일ID·이름·어느 작업·왜) — 구매양식 내용·리뷰 내용은 담지 않는다(원장이 아니라 청소 목록이다). ★ 처리해도 **지우지 않고** `resolved_at` 을 찍는다.
- ★★★ **B(원장 없음)는 일괄 삭제 표면을 두지 않는다** — "원장 어디에도 없다"에는 **업로드는 됐는데 기록만 실패한 정상 캡처**가 섞인다. 그걸 자동으로 지우면 리뷰어가 낸 증빙이 사라진다. 그래서 ① 크론이 **절대** 부르지 않고 ② 실행도 **`fileIds` 필수**(없이 부르면 후보가 있어도 `trashed:0` + `fileIds 필수 —…` 사유) ③ 응답에 오판 위험을 **문장으로** 실어 보낸다(`원장 기록만 실패한 정상 캡처가 섞일 수 있습니다 — 반드시 파일을 열어 확인한 뒤 고르세요`). ★ 스캔은 읽기 전용(폴더 목록 + 원장 대조 1회 왕복) · 폴더 해석은 업로드가 쓰는 그 경로(`ensureReviewFolderPath`/`ensureCaptureFolderPath`)를 **그대로** 쓴다(사본 금지) · 이미 묘비에 있는 파일은 B 후보가 아니다(C 와 중복 취급 금지) · **폴더 조회 실패는 사유를 말한다**(조용한 0건 금지).
- ★ **B·C 는 원장을 고치지 않는다**(`markLedger:false`) — 가리키던 원장이 애초에 없거나 사라졌다. 대신 묘비에 `resolved_at`/`resolved_by` 를 찍는다. A 만 `slot_key='trashed'` 표기를 한다.
- ★★ **표 분류 전수 가드에 세 번째 칸을 열었다**(`workTabDelete.KEEP_TABLES`) — `(sheet_id, tab_name)` 을 가진 표는 전부 "지운다/돈이라 막는다" 둘 중 하나여야 했는데 묘비는 **그 둘 다 아니다**(작업이 사라진 뒤에도 남아야 뜻이 있다). 세 번째 칸이 **도피처가 되지 않도록** 가드가 ① 세 목록 겹침 금지 ② **사유(`why`) 필수** ③ 삭제 경로에 `DELETE FROM <그 표>` 문장 부재까지 함께 고정한다.
- ★★★ **판정 근거는 `file_id` / `review_index_id` 뿐 — 위치키(`row_index`) 금지(완화 금지)**: `review_submissions.row_index` 는 위치키라 번호 정리·재배정·차수 변경으로 수시로 깨진다. 그것을 고아 근거로 쓰면 **멀쩡한 캡처를 지운다**(리뷰어 홈 dedup·관제 대조가 같은 함정을 이미 밟았다 — 좌표는 이름이 아니고 근거도 아니다). 좌표는 **사람이 읽을 표시용으로만** 싣는다. 회귀가드가 고아 근거 블록에 `row_index` 가 없음을 고정한다.
- **고아 근거(OR 2종)**: ㉠ `order_submissions.capture_file_id = rs.file_id AND deleted_at IS NOT NULL`(그 파일을 붙들던 주문이 취소됨 = 행삭제의 결과) ㉡ `rs.review_index_id` 가 가리키는 `review_index` 행이 없음. ★ `review_index_id` 는 **결정적 매칭 시에만** 채워지므로(032) NULL 인 행은 근거가 없어 후보가 아니다(추측으로 지우지 않는다).
- **제외 7종(fail-closed — 하나라도 걸리면 후보 아님)**: 이미 `slot_key='trashed'` · 유예 미경과 · **살아있는 주문**이 그 파일 참조(같은 파일의 재사용) · **살아있는 명단 행**이 대표로 참조(`review_index.review_file_id`) · **리뷰검수 이력**(사람이 이미 본 파일) · **리뷰 수정요청**(old/new 양쪽 — 진행 중 분쟁 증거) · 그 좌표에 **활성 작업표 줄**.
- ★★ **삭제는 휴지통만**(`trashFiles`) — 영구삭제 API 호출 표면이 없다(`fileRoute.service.js:11` 규율 그대로, 30일 복구창). 원장 표기도 **같은 칸**을 쓴다(`slot_key='trashed'` + `routed_from_slot`/`routed_at`/`routed_by='orphan:<주체>'`) — 사본을 두면 "자동 정리분만 되돌리기가 안 되는" 드리프트가 생긴다. **표기는 휴지통 성공 뒤에만**(실패 시 원장 무변경 → 다음 회차 재시도로 수렴).
- ★★ **TOCTOU 재검사** — 조회 직후 그 파일이 다시 쓰이기 시작했을 수 있다(재링크·수정요청·검수). 휴지통으로 보내기 **직전에** 같은 조건으로 그 한 건을 다시 확인하고, 풀렸으면 건너뛰며 **건수를 보고한다**(`skippedRecheck` — 조용한 no-op 금지). ★ 실행은 **후보를 서버가 다시 골라 교집합**만 처리한다(화면이 보낸 목록 불신 — `sheetlessOrphanCleanup` 과 같은 규율).
- **크론** `40 4 * * *`(KST) + `withJobLock('orphan_capture_clean')`(기존 6개 락 이름과 해시 비충돌 확인) · 킬스위치 `ORPHAN_CAPTURE_CLEAN=0` · 유예 `ORPHAN_CAPTURE_GRACE_DAYS`(기본 7) · 회차 상한 `ORPHAN_CAPTURE_CLEAN_CAP`(기본 200) · **정리 실패가 크론을 죽이지 않는다**.
- **수동 창구** `POST /api/drive/orphan-capture-cleanup`(adminOrMaster · **dryRun 기본**, 실행은 `dryRun:false` 명시) — `kind` 로 세 종류를 나눈다: `'linked'`(기본, A — **크론과 같은 함수**를 부른다/사본 금지) · `'tombstoned'`(C) · `'folder'`(B, `sheetId`+`tabName` 필수). ★ **미지정은 종전 동작(A)** 그대로 · ★ **모르는 kind 는 400 으로 거부**한다(오타가 조용히 A 를 실행하면 안 된다) · ★ 세 갈래가 **같은 `dryRun`** 을 쓴다(한쪽만 바로 실행되는 일 금지) · ★ 휴지통 이동 실행부(`_trashFiles`)도 셋이 **공유**한다(삭제 메커니즘 사본 금지). 화면 버튼은 아직 없다(UI 시안 미확정 — 넣는다면 B 는 **파일을 열어 보고 고르는** 화면이어야 한다).
- ★★★ **Codex 리뷰가 잡은 3종 — 전부 실측 재현 후 수정 (2026-08-24 · 완화 금지)**:
  - **㉠ 아카이브된 차수를 "행이 사라졌다"로 읽었다(P1 · 실데이터 삭제)** — 차수 아카이브는 `review_index` 행을 옮기고 **원본을 지운다**. 그러면 ㉮ `rs.review_index_id` 가 dangling 이 되어 A 근거가 곧바로 참이 되고, ㉯ 재생성 뒤 `review_file_id` 를 되붙이는 **스냅샷 복원(`sheetlessLedger` ②-1)이 그 행을 못 찾아** `review_file_id` 링크 제외까지 함께 무너진다 — 남는 방어가 위치키 기반 작업표 줄 하나뿐이라 **정상 리뷰 이미지가 유예 7일 뒤 휴지통으로 간다**. ★ 이 위험은 **이 PR이 만든 상호작용**이다(차수 마감 집행 ②가 고아 판정 ①에 먹이를 준다). → `review_index_archive` 좌표 제외를 추가. ⚠ **정확한 키로 못 잇는다** — `review_index_archive`(005)에는 `review_file_id` 도 원본 id 도 없고, 아카이브 INSERT 4곳 전부 그 링크를 버린다. 좌표 대조는 **과거 아카이브분까지** 덮고 빗나가면 **지우지 않는 쪽**으로 접힌다.
  - **㉡ B 폴더 스캔이 언제나 0건이었다(P1 · 기능 무음)** — `listFolderContents(id, mime)` 는 `mimeType = '<값>'` **완전일치**로 질의하는데 `'image/'` 를 넘겼다. 실제 파일은 `image/jpeg`·`image/png` 라 **어느 것과도 안 맞는다**. → 필터 없이 받아 **접두사로 거른다**(공용 래퍼를 고치면 다른 호출부가 흔들린다). ★ `mimeType` 을 모르는 파일은 후보로 삼지 않는다(모르면 건드리지 않는다).
  - **㉢ 묘비 fail-soft 가 성립하지 않았다(P2)** — 열린 트랜잭션 안에서 INSERT 가 실패하면(배포 중 134 미적용이 대표 사례) PG 가 tx 를 **abort** 로 표시해 뒤따르는 DELETE 가 전부 **25P02** 로 죽는다. try/catch 로 로그만 남기면 "삭제는 계속"이 아니라 **작업 삭제 전체가 롤백**된다. → **`SAVEPOINT` 격리 + 실패 시 `ROLLBACK TO`**. ★ CLAUDE.md 의 082 apply·번호 매기기와 **같은 함정을 세 번째로 밟았다** — 열린 tx 안의 부수 작업은 예외 없이 SAVEPOINT 로 감쌀 것.
- 회귀가드 `tests/orphanCaptureCleanup.test.js`(79 — 위치키 금지 · 미리보기 무쓰기 · 교집합 · TOCTOU · 영구삭제 표면 부재 · **폴더 스캔은 B 전용**(A 경로 본문에 부재) · 원장 표기 순서 · 크론/라우트 배선 · **묘비 순서 계약** · **B 일괄 삭제 금지** · **크론은 A만**, **변이시험 6종 + Codex 3종 회귀 6종 검출 확인**) + `tests/workTabDelete.test.js` 갱신(세 번째 분류 `KEEP_TABLES` — 겹침·사유·삭제문 부재, **변이시험 3종 검출 확인**). ⚠ B 가드는 **스캔이 진짜 후보를 뱉는 상태**로 만들어 놓고 부른다 — 폴더 조회가 실패해 조기 반환되면 "0건"이 나와도 가드를 시험한 게 아니다(그 스텁으로는 `fileIds` 가드를 지워도 통과해 검출력이 0이었다).
