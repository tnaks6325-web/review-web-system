# ★★ C/S 문의방 제목 → 그 작업의 작업보드 (2026-08-25 · 스키마 변경 0 · 서버 변경 0)
- **발단**: 문의를 받은 담당자가 그 작업의 표를 보려면 **검색 탭에서 작업을 다시 찾아야** 했다. 대화창 작업명을 누르면 새 탭으로 그 작업보드가 열리고 **그 리뷰어의 행을 찾아 스크롤+강조**한다.
- ★★ **실행부 사본 0** — 리뷰어 로그의 작업보드 딥링크와 **같은 계약**(`#go={s,t,g,p,n,st,r}` → `_consumeGo` → `pendingTab`/`pendingFocus` → `_applyPendingFocus`). 행 찾기·강조·"못 찾음" 토스트가 전부 기존 한 벌이고 **여기서는 좌표만 보낸다**(회귀가드가 발신부↔수신부 키 일치를 대조). 재료(`campaignKey`=`시트ID||작업명`·연락처·이름)는 이미 스레드 응답에 있다.
- ★★★ **행 찾기 3순위 — 참여자 칸이 본계정이라 타계정 건이 잡힌다 (사용자 확정 2026-08-25)**: ① **줄 번호**(`data-seq`, 주문 원장 `sheetRow` → 명단 `rowIndex`) ② 연락처(`data-p8`) ③ **참여자 이름**(`data-nm`). 근거 = `sheetlessOrder.service` 의 **`reviewer_name = loginName || orderer || recipient`**(로그인 본계정) · **`recipient_name = 명의`** · **`phone8 = 주문 연락처`** ⇒ **타계정 참여는 표의 연락처가 명의 번호라 ②가 빗나가고 ③이 잡는다.** 이 계약이 깨지면 타계정 문의가 행을 영영 못 찾는다.
- ★★ **①은 신원을 확인하고 채택한다** — 그 줄의 연락처 또는 이름이 어긋나면 **채택하지 않고 ②③으로 내려간다**(재배정·줄 정리로 번호가 밀린 표에서 남의 행을 짚지 않는다). 신원 재료가 둘 다 없을 때만 줄 번호를 단독 근거로 인정.
- ★ **가산적** — 줄 번호(`r`)가 없으면 **종전 동작 100%**(로그 딥링크는 `r` 을 안 보낸다). 그리드 `<tr>` 의 `data-seq` 는 표시 무영향.
- ★★ **완화 금지**: **권한이 넓어지지 않는다**(새 탭도 `canAccessTab` 을 그대로 거친다 — 토큰 동봉은 **자기 새 탭용**, 사람에게 건네는 주소가 아니다) · **정보는 프래그먼트(#)로만**(서버 로그·Referer 에 연락처 미유출) · **작업이 지정된 문의만 링크**(일반 문의는 갈 곳이 없다) · **onclick 은 `event` 만**(작업명은 시트발 문자열) · 헤더 전체에 접기/펼치기가 걸려 있어 **`stopPropagation` 으로 분리** · 목적지 경로는 **확장자 유무 유지**(Pages `/workdesk` · 테섭 `/workdesk.html`).
- ★ 주문정보 도착 시 줄 번호를 **문맥에 덧붙이기만** 한다(문맥 자체를 거기서 만들면 작업 미지정 문의에 링크가 생긴다).
- ⚠ **잔여**: 타계정 건은 **주문정보 카드 자체가 안 뜬다**(그 조회도 본계정 번호로 찾는다) → 줄 번호가 없고 이름 폴백이 담당한다(같은 이름 행이 여럿이면 첫 행 = 같은 사람). 그 조회를 넓히면 남의 주문이 섞일 위험이 있어 손대지 않았다.
- 회귀가드 `tests/csCampaignWorkboardLink.test.js`(27 — 스텁 위에서 `csOpenWorkboard`·`_applyPendingFocus` **실제 실행**: 우선순위 4갈래·신원 확인 폴백·타계정·미발견 안내 + 수신부 계약 대조 + 사본 금지 + 화면 배선, **변이시험 11종 검출 확인**) + 실 http 오리진 브라우저 실측. ⚠ **가드 함정**: 페이로드 검사를 **키 존재(`hasOwnProperty`)로만 하면 값을 통째로 안 실어도 통과한다**(변이시험이 실제로 뚫었다) → **값 대조**를 함께 둘 것. ⚠ 이 변경으로 `reviewerLogDeeplink` 기대값 1건 갱신(`pendingFocus.row` 추가 — 검사 의미 불변).
