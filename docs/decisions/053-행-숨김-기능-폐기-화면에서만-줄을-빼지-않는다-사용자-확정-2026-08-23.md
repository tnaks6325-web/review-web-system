# ★★★ 행 숨김 기능 폐기 — 화면에서만 줄을 빼지 않는다 (사용자 확정 2026-08-23)
- **사고(「0807(올리브영)블랑카우 바디로션 100건」)**: 작업표에 85줄이 채워져 있는데 상단 진행 현황이 `참여자 82/100`. 원인은 옛 **`_hidden`(행 숨김 오버레이) 3줄** — 줄을 지우지 않고 **화면에서만** 빼는 기능이라 **표의 줄 수 · 진행 현황 게이지 · 마감자료(CSV)가 서로 다른 사실**을 말했다. 그 오버레이는 `participant_edits` 한 줄이라 **아무 흔적 없이** 생겼다.
- ★★ **제거한 표면 4종(되살리지 말 것)**: ① 편집 화이트리스트 `_EDIT_FIELD_KIND._hidden`(= 저장 자체가 `field_not_editable` · 쓰기 0건) ② 작업보드 합성의 `ov._hidden === true → continue` ③ 마감자료 CSV 의 같은 제외 ④ 응답 `hiddenRows`·`counts.hidden` 과 화면 `숨김 N` 칩·복원 목록(`showHidden`).
- ★★ **레거시 방어 2종은 유지** — 남아 있는 옛 레코드가 **시트로 나가면 안 되므로** write-back 픽업 SQL 의 `field <> '_hidden'` 제외와 통계 FILTER 는 그대로 두고, 편집 이력 화면의 `(행 숨김)` 라벨도 남긴다(과거 기록을 읽을 수 있어야 한다). `editedFields` 에서도 계속 걸러 **편집 배지로 세지 않는다**.
- ★ **줄을 내리는 창구는 그대로다** — [행 삭제](`/workdesk/hide` → 실제 DELETE + **보충 슬롯 생성**으로 총 모집수 불변) · [🧹 줄 정리] · [♻ 중복 줄 정리]. 이번에 없앤 것은 "지우지 않고 감추는" 경로 하나뿐이다.
- **운영 조치(2026-08-23)**: 본섭 전수 스캔으로 숨김이 남아 있던 **5작업 10줄을 전부 복원**했다(블랑카우 참여자 82 → 85 확인).
- 회귀가드 `tests/rowHideRemoved.test.js`(15 — 쓰기·읽기·응답·화면 4표면 부재 + 레거시 방어 생존 + **스텁 pool 로 실제 실행**(옛 오버레이가 있어도 줄이 남고 `filled` 가 맞는다), **변이시험 4종 검출 확인**). ⚠ 이 변경으로 기대값이 뒤집힌 가드 4종을 갱신했다(`participantEdits`·`trackBCloseout`·`participantVisitBadge`·`workboardFilledGauge`) — **검사 의미 = 새 사용자 확정**.
