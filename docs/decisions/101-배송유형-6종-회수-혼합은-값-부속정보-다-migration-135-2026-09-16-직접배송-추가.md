# ★★★ 배송유형 6종 — 회수·혼합은 "값 + 부속정보"다 (migration 135, 2026-09-16 직접배송 추가)
- **어휘 = 실배송 · 빈박스 · 택배발송대행 · 직접배송(가구 등) · 회수 · 혼합**(`utils/deliveryType.DELIVERY_TYPES` 단일 출처).
- ★★★ **인트라넷은 배송유형을 `delivery_type` 한 칸에 문장으로 접어 보낸다** — `회수(회수택배사: CJ대한통운, 회수상품명칭: OO선크림 30ml)` · `혼합(실배송 20건, 빈박스 80건)`. 리뷰웹 판정은 전부 **정확일치**(맵 조회·앵커 정규식·select value)라 문장이 오면 통째로 빗나갔다 → 회수·혼합 오더는 **접수만 되고** ① 작업표 열이 하나도 안 켜지고 ② 발행 프리필이 `WO_DELIVERY_MAP[…] || ""` 로 값을 통째로 버리고 ③ 회수택배사·회수상품명칭이 리뷰어에게 도달하는 경로가 **0** 이었다(2026-08-24 조사).
- ★★ **저장은 원문 그대로, 판정만 기본형(base)으로 접는다**(`parseDeliveryType`/`deliveryBaseType`/`canonicalDeliveryValue`) — ① 파싱이라 **이미 저장된 과거 오더에도 소급 적용** ② **인트라넷 배포를 기다리지 않아도 즉시 동작**(양방향 가산적 = 배포 순서 무관) ③ 마이그레이션·데이터 변환 0. ★ **부속정보가 붙어 있으면 원문을 보존한다** — 기본형만 남기면 회수택배사·혼합 건수가 그 자리에서 증발한다. **맨 토큰만** 표준형으로 접는다(`빈택배`→`빈박스`·`회수건`→`회수`), 판정 불가값은 **원문 통과**(추측 금지).
- ★★★ **`LEGACY_DELIVERY_VALUES` 와 절대 합치지 말 것(완화 금지)** — 그 배열은 어휘 목록이 아니라 **"리뷰타입 칸에 잘못 들어간 배송유형 판별 목록"**(`order.routes` 접수 업서트 `$12`)이고, **`혼합` 은 정상 리뷰타입 값**(`normalizeReviewType('혼합') → 'mixed'`)이다. 넣는 순간 접수 업서트가 **멀쩡한 혼합 리뷰 탭의 리뷰타입을 갈아치우고 `delivery_type` 으로 이관**한다(2026-08-06 이노크아든 사고의 **거울상**). 두 목록은 **분리**돼 있고 회귀가드가 그 사실을 고정한다.
- **작업표**: 혼합은 **행마다 `배송구분` 칸**에 `실배송`/`빈박스` 를 배분한다(리뷰 종류 혼합과 같은 규율 — 혼합 오더는 "어느 줄이 실배송인가"의 답이 행에만 있다). ★★ 배분기는 **같은 `_allocByDateRatio`**(라벨만 주입 — 인자 생략 시 종전 동작, **사본 0**) · 칸이 없으면 **자동 추가**(리뷰옵션·택배송장번호와 같은 규율) · **2종 미만이면 행에 안 적는다** · 합계가 총 건수와 다르면 비율 유지 + `delivery_mix_scaled` **경고**(조용한 보정 금지). 회수는 `회수택배사`·`회수상품명칭` 열을 자동 추가하고 **줄마다 채운다**(표는 정렬·필터로 흩어지므로 줄이 스스로를 설명해야 한다).
- ★★ **트리거는 기본형 기준**(`delivery_real`/`delivery_empty`/신설 `delivery_recall`) — 종전 정규식은 문장에 글자가 있다는 이유로 걸려 `혼합(실배송 0건, 빈박스 100건)` 처럼 **0건인 유형의 열까지 만들었다**. ★ **조합을 못 읽으면 둘 다 켠다**(모른다고 열을 빼면 배분이 갈 곳을 잃는다).
- ★★ **배송유형을 바꾸면 부속정보도 다시 세운다**(수정 3경로 = 인트라넷 intake · AE · 관리자) — 안 하면 혼합→실배송으로 바꿔도 **옛 조합이 남아 작업표가 유령 배분을 돈다**(`_reviewTypeMixJson` 의 "단일 타입이면 빈 배열" 규율과 같다).
- **135** = `work_orders`·`recruit_campaigns` 에 `delivery_type_mix`·`recall_courier`·`recall_product`(**컬럼 추가만 · 백필 0 · CHECK 0** = 배포 즉시 동작 불변) + `REQUIRED_SCHEMA` 등록. 재료 우선순위 = **구조화 컬럼 → 문장 파싱 폴백**(과거 오더 구제).
- **인트라넷(inadd-webapp)**: `REVIEW_ORDER_FIELDS` 에 세 필드 합류(★ **문장 요약 `delivery_type` 은 그대로 유지** — 사람 읽기·구버전 호환) + `reviewOrderDeliveryMixError` **근본 차단**(혼합 합계 = 모집인원 · 회수 택배사·상품명칭 필수 · 그 외 유형은 무검사 · 수정은 직전 payload 와 병합 · **재전송(retry)에는 게이트를 걸지 않는다**). ★ 규칙은 리뷰웹 `utils/deliveryTypeMix.validateDeliveryTypeMix` 와 **같아야 한다** — 어긋나면 "인트라넷은 통과인데 리뷰웹은 거부"라는 막다른 길이 되살아난다(레포가 갈라져 코드 공유가 불가능하므로 **양쪽 가드가 각각 규칙을 고정**한다).
- ★★ **옛 어휘 `회수건`·`빈택배` 는 화면 선택지에서 제거**(회귀가드가 세 파일에서 부재를 고정) — 그 값이 저장돼 있으면 현행 모집공고 모달 select 에 option 이 없어 **`el.value` 가 빈 문자열이 되고**(브라우저 실측 확인), 아무것도 안 건드리고 저장만 눌러도 `COALESCE($7,…)` 가 빈 문자열은 안 막아 **배송유형이 조용히 지워진다**. 저장값 정리 = 설정 › **🚚 배송유형 표기 정리**(`POST /api/diag/delivery-type-cleanup` + `/api/trackb/settings/delivery-type-cleanup`, adminOrMaster · **미리보기 기본** · 쓰기 표면 `delivery_type` **한 칸**(`updated_at` 도 무접촉) · **접히는 값만** 바꾸고 부속 문장·모르는 값은 무접촉).
- ★ **리뷰어 배지는 기본형만** 그린다(문장이 카드를 덮는다) — ★ **아는 어휘일 때만 접는다**(모르는 값 `기타배송(박스)` 을 괄호 앞에서 자르면 다듬기가 아니라 **정보 삭제**다).
- ★★★ **부속정보 3칸이 "바뀐 칸 판정"에서 빠져 접수된 오더에서 조용히 사라졌다 (2026-08-25 실측)**: `delivery_type_mix`·`recall_courier`·`recall_product` 는 전체 저장 UPDATE 에는 있는데 `order.routes._sourceContentNextValues`(원본 수정의 바뀐 칸 판정)에는 없었다 → 접수된 오더에서 그 칸만 고치면 **"바뀐 게 없다"로 읽혀 계약 전용 저장으로 흘러가고 값은 버려진 채 200 ok** 로 응답했다(담당자는 반영된 줄 안다). ★ **판정에 넣되 `SOURCE_EDIT_AFTER_ACCEPT`(접수 후 허용 목록)에는 넣지 않는다** — 이 세 칸은 작업표의 **열 구성과 줄마다 채우는 값**을 정하고(`worktablePlan`) 이미 깔린 표는 따라오지 않는다 = 정원·일정과 같은 부류. 이제 409 + `회수 택배사` 처럼 사람이 읽는 이름으로 말한다(쓰기 0건). ★★ **보내지 않은 요청은 비교하지 않는다(fail-open)** — 이 칸을 안 보내던 옛 인트라넷 payload 가 문장 파싱 폴백 때문에 "바뀐 것"으로 읽히면 **계약 후속 매칭이 새로 막힌다**(2026-08-20 실장애와 같은 자리). ★ 저장소 기본값이 `NOT NULL DEFAULT '[]'`/`''` 라 값 비교가 헛되이 뒤집히지 않는다. ★★ **재발 방지 본체 = 드리프트 가드** — 전체 저장 UPDATE 의 SET 칸(계약·광고주·기록 칸 제외) ≡ 판정 목록 키. 앞으로 칸이 늘 때 한쪽에만 들어가면 그 자리에서 빨개진다. 회귀가드 `tests/reviewOrderRecallMixEditDetect.test.js`(15, **변이시험 7종 검출 확인**). ⚠ 가드 함정: SET 절만 잘라 볼 것(`WHERE id = $1 AND source_review_order_id = $40` 이 "쓰는 칸"으로 잡힌다) · 끝 표시는 **시작점 뒤에서** 찾을 것(앞쪽 동일 문구를 먼저 잡으면 조각이 비어 검사가 조용히 무의미해진다 — 둘 다 개발 중 실측).
- 회귀가드 `tests/deliveryTypeVocabulary.test.js`(42 — 파싱·배분·열·**무회귀**(실배송/빈박스 오더는 열·행·판정 그대로)·원장 배선·**INSERT/UPDATE 파라미터 계수**·화면 어휘·정리 도구, **변이시험 19종 검출 확인**) + 인트라넷 `test/review-order-delivery-hard-block.test.ts`(14 — 판정 블록을 **글자 그대로 잘라 실제 실행**, 변이시험 7종) + 실 http 오리진 브라우저 실측. ⚠ 이 변경으로 드리프트한 가드 5종을 갱신했다(`courierProxyDeliveryContract`·`woCampaignMapDoc`·`workOrderAutoFill`·`workdeskOrderActions`·매핑 문서) — 검사 의미는 불변이거나 새 사용자 확정.
- ★★ **모집공고도 부속정보를 저장한다 (사용자 확정 2026-08-24 2차)**: 발행·수정 모달에 **회수택배사·회수상품명칭 / 실배송·빈박스 건수** 칸이 있고(해당 유형일 때만 펼침 · 합계 = 총 건수를 화면이 말한다) 값이 `recruit_campaigns` 세 칸에 저장된다. ★ **미전송 = 유지**(CASE 센티널 — 부속 칸 없는 화면이 저장해도 조용히 안 지워진다) · ★ **기본형을 벗어나면 비운다**(혼합→실배송 전환 시 옛 조합 잔류 = 유령 배분) · ★ **스코프 편집(리뷰어앱)은 못 바꾼다**(권한 상승 차단) · ★ 프리필은 **공고 저장값 → 작업오더** 순(발행은 오더 값을 채우고 사람이 확인 후 저장).
- ★★ **공고 저장이 연결 작업표에 부속 열을 보장한다**(`worktableDeliveryColumn.service` — 옵션 칸 훅과 같은 자리·같은 규율): 접수는 **작업오더만** 보고 열을 정하므로 "오더엔 없었는데 공고에서 혼합·회수로 정한" 작업은 칸 없이 만들어진다. 저장 시 `배송구분`(자동 열 뒤) 또는 `회수택배사`·`회수상품명칭`(맨 뒤)을 만들고 **소급 기입**한다(혼합은 `distributeDeliveryTypes` 로 날짜 비율 배분). ★ **열 이름·자리·배분은 `worktablePlan` 단일 출처**(사본 0) · ★ **쓰기 표면 = `raw_sheet_tabs.headers/detected_headers` + `campaign_participants.row_json` 두 곳뿐** · ★ **blank-only**(담당자가 적어 둔 값을 덮지 않는다) · ★ **무시트 탭만**(fail-closed) · ★ **절대 throw 없음**(열 보장 실패가 공고 저장을 죽이면 안 된다).
- ⚠ **잔여**: ① 이 배포 이전에 인트라넷에 저장된 어긋난 혼합 오더는 그대로 남는다(수정하려면 조합을 먼저 바로잡아야 한다) ② **접수 시점 작업표는 여전히 작업오더 값이 정한다** — 공고 값은 그 뒤에 열을 보태고 빈 칸을 채울 뿐, 이미 채워진 줄의 배분을 다시 나누지 않는다 ③ `courier_proxy` boolean 이 true 면 종전 계약대로 배송유형보다 우선한다.
- 회귀가드 `tests/campaignDeliveryDetailSave.test.js`(25 — INSERT 칸≡자리≡파라미터 · SET 센티널 · 유형 전환 시 비움 · 훅 fail-soft · 서비스 사본 0/쓰기 표면/fail-closed/blank-only · 권한 경계 · 화면 배선, **변이시험 13종 검출 확인**). ⚠ 이 변경으로 `campaignCashReceiptContract` 의 INSERT 패턴을 갱신했다 — 종전 패턴이 `cash_receipt_required` 가 **마지막 칸**임을 전제해 칸이 하나만 늘어도 조용히 빨개졌다(**위치가 아니라 소속으로** 고정).
