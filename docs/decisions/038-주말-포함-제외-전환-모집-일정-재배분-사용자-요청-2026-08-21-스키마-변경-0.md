# ★★ 주말 포함/제외 전환 = 모집 일정 재배분 (사용자 요청 2026-08-21 ①②, 스키마 변경 0)
- **발단**: 주말 토글(104)은 `recruit_campaigns.skip_weekends` **한 칸만** 바꿔 그날 신청을 막을 뿐, **날짜별 계획(095)도 작업표도 손대지 않았다** — 주말에 잡혀 있던 자리가 그대로 남아(그 날은 아무도 못 들어온다) 총량만 뒤로 밀렸고, 되돌려도 뒤에 붙어 있던 날이 그대로였다.
- ★★ **모델 = "여는 날에 기본 일건수씩, 총량을 다 채울 때까지"**(`planWeekendSpread` 순수함수): 오늘(또는 시작일)부터 정책이 여는 날에 `defaultDaily` 씩 깔고 마지막 날은 남은 만큼만. **총량이 구조적으로 불변**이고 주말을 닫으면 종료일이 뒤로, 열면 앞으로 — **양방향 대칭**이라 ①②를 한 규칙이 처리한다.
- ★★ **제안까지만 — 반영은 사람이 [확정 저장]**: 재배분은 조절 표에 펼쳐 보여주기만 하고 **서버를 한 번도 부르지 않는다**(회귀가드가 저장 API 부재를 고정). 저장은 기존 경로(`savePlans` → `rebuildAdjustedPlansToWorktable`)라 확인창·상한·이월 회계·작업표 재구성이 전부 그대로다.
- ★★ **하한 = 오늘 확정·진행 인원 + 그날 이미 채워진 작업표 줄** — 그 아래 계획은 서버 재구성이 `worktable_rebuild_below_used` 로 **통째로 거부**하므로 애초에 만들지 않는다("조절은 저장됐는데 표는 안 바뀐다"는 막다른 길 차단). 판정은 **`minFor` 하나**로 모아 드래그·−/＋·[자동 맞춤]·재배분이 같은 값을 본다. 닫지 못한 날은 배너가 **사실대로 말한다**.
- ★★ **구간 뒤에 남는 자리는 해제(remove)가 아니라 명시 0** — 해제하면 `baseFor` 가 작업표 행 수로 돌아가 그 날이 계속 열린다(= 종료일이 안 당겨진다).
- ★★★ **재배분 저장은 구간 전체를 명시 계획으로 확정한다(실브라우저가 잡은 결함)**: 균형 모드의 종전 규칙("손대지 않은 값은 안 보낸다")이면 **주말이 통째로 빠진다** — 주말 제외 공고의 토요일은 `baseFor` 가 정책상 0 이라 "0 으로 바꿔도 변경 없음"이 되는데 **작업표에는 그날 빈 줄이 그대로 있다**. 그 날짜가 저장 계획에 없으면 재구성의 관리 풀(`managed`)에 못 들어가 줄이 옮겨지지 않고 뒤에 새 줄만 생겨 **총 줄 수가 늘어난다**. 목적지 날짜도 같은 이유로 필요하다 → `payload()` 의 `pinAll` 에 `S.rebalanced` 합류. ★ 재배분 대상에서 시트 일정 공고를 뺐으므로 "시트 우선권 상실" 우려는 없다.
- ★ **주말 판정 사본 0 = `policyClosed`**(`baseFor` 와 공유) · **대상 아님은 사유를 말한다**(시트 일정·무제한·하루 전량(블로그)·총원 충족·오늘 초과·구간이 저장 상한 초과).
- **창구 둘**: ㉮ 공고 수정에서 주말 설정을 **바꿔 저장하면** [📅 인원]이 재배분안을 펼친 채 열린다(참여형 · 편집 모드 · 값이 실제로 바뀐 경우만 · 모듈 없으면 무동작 · 저장 안내를 먼저 읽히려 1.2초 뒤) ㉯ [📅 인원]을 그냥 열었을 때 **주말 제외인데 주말에 인원이 남아 있으면** 배너 + [주말 기준으로 재배분]. ★ **주말 포함 쪽은 감지하지 않는다** — 토/일 0 이 관리자의 의도일 수 있어 구분할 수 없다(그래서 ㉮ 만 자동).
- **서버는 재료만**(정원 판정 변경 0): `readWorktableDates` 가 날짜별 **채워진 줄 수**를 함께 돌려주고(판정 = `utils/rowNumbering.isFilledRow` 단일 출처 — 작업보드 게이지·번호 정리와 같은 네 칸) `getPlanOverview` 가 `worktableDates[].filled` 로 싣는다. 값이 없으면(구버전) 0 = 종전 동작이고 서버가 최종 방어한다.
- ⚠ **잔여**: ① 재배분은 **기본 일건수 기준**이라 사람이 손으로 넣어 둔 다른 조절도 함께 다시 깔린다(표에서 확인하고 저장) ② 구간이 저장 상한(120일)을 넘으면 재배분하지 않고 사유를 말한다 ③ 주말 제외 공고의 토/일 정원은 여전히 계산되고 소진되지 않아 **자동 이월(066)로 다음 진행일에 얹힌다**(상한 2배) — 재배분해 두면 명시 조절일이 되어 그 가산이 얹히지 않는다.
- 회귀가드 `tests/campaignWeekendRebalance.test.js`(35 — 순수 배분 함수 · **전 흐름 vm 실행**(open→재배분→[확정 저장])으로 저장 payload 검증 · 단일 출처 · 서버 재료 · 배선, **변이시험 11종 검출 확인**) + 실 http 오리진 브라우저 4시나리오 실측. ⚠ **vm sandbox 함정**: 모듈은 bare `sessionStorage` 를 읽으므로(`_headers`) sandbox 전역에 없으면 GET 이 ReferenceError 로 죽고 `open()` 이 조용히 catch 해 **단언이 통째로 무의미**해진다(실측). ⚠ 파일 끝의 **동기 `process.exit`** 는 async 블록보다 먼저 돈다 — 비동기 절은 그 안에서 끝낼 것. ⚠ 이 변경으로 `planWorktableSeqReuse` 의 SELECT 패턴을 갱신했다(검사 의미 불변 — 기준은 WHERE 절).
