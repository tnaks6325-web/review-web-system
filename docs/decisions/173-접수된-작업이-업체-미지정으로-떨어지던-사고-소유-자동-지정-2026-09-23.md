# ★★ 접수된 작업이 업체 「미지정」으로 떨어지던 사고 — 접수 시 소유 자동 지정 (2026-09-23 · migration 165)
- **사고(「고양이사료」)**: 인트라넷 리뷰오더에서 광고주(주식회사수진코리아)를 고르고 계약·견적서까지 붙여 접수했는데 작업이 업체관리·작업바에서 **「미지정」** 으로 떨어졌다. 같은 상태가 4건(수진코리아 2 · 버즈업 1 · 풍성에프엔비 1).
- **원인**: 접수(`/order/admin/accept`)는 `projectIntranetAdvertiser` 로 `work_orders.advertiser_id`·포털 작업을 채우고 계약을 `trackb_settlement_links` 에 자동 연결하지만, 업체관리·작업바의 업체 묶음이 읽는 **`advertiser_campaigns`(작업 소유)는 어디서도 만들지 않았다** — 사람이 업체관리에서 손으로 지정해야만 풀렸다.
- **수정**: 접수 8c) 가 `advertiserProjection.ensureTabOwnership` 을 부른다(응답 `ownershipAssigned`). 과거 4건은 migration 165 가 소급.
- ★★ **완화 금지**: ① **작업(탭) 단위만**(gid 없으면 지정 안 함 — 결정 082, 시트 전체 소유 금지) ② **이미 누가 소유(탭 지정·시트 전체)하면 덮지 않는다**(`kept_existing` — 사람이 정한 소유를 재접수 한 번으로 바꾸지 않는다, 정산 계약 자동 연결과 같은 규율) ③ **사람이 [×]로 해제한 같은 업체 행은 되살리지 않는다**(`ON CONFLICT DO NOTHING` → `kept_removed`) ④ 종료 거래처 제외 ⑤ **fail-soft · 절대 throw 없음**(접수는 이미 끝났다).
- 판정과 쓰기는 **한 문장**(`INSERT … WHERE NOT EXISTS`).
- 회귀가드 `tests/acceptAdvertiserOwnership.test.js`(스텁 pool 로 서비스 실제 실행 7갈래 + 라우트 배선 + 소급 SQL 규율). 소급 SQL 은 운영 DB 롤백 트랜잭션으로 대상 4건 정확히 일치 확인.
