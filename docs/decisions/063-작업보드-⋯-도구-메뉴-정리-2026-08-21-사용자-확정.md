# ★★ 작업보드 [⋯] 도구 메뉴 정리 (2026-08-21 · 사용자 확정)
- ⚠️ **이 절은 2026-08-23 확정으로 갱신됐다 — 위 「작업보드 [⋯] 정리 도구 4종 → 1종」 절이 현재 상태다.** 아래는 그 결정에 이르는 경위이고, 표시된 대목은 그 뒤 뒤집혔다. 되살리기 전에 최신 절을 먼저 볼 것.
- **배경**: [⋯] 메뉴의 5개 버튼이 **전부 "할 일이 있는지"를 보지 않고** 역할·무시트 여부만으로 상시 노출돼, 대부분의 작업에서 5개가 할 일 없이 떠 있었다. 자동화가 이미 대신하는 것부터 걷어낸다.
- **제거 2종**:
  - **[🔢 번호 정리]** — `sweepNumbering` 크론이 `3-59/5 * * * *` 로 **빈칸·중복 번호·pairedBlank** 를 이미 잡는다. ~~전체 작업 진입점은 남긴다~~ → **2026-08-23 에 모달·전용 CSS·서버 라우트 3종까지 전부 제거**됐다(자동 경로만 남는다).
  - **[🧩 옵션 열]** — `worktablePlan` 이 옵션 배분이 있는데 칸이 없으면 **자동으로 덧붙이고**(`option_column_added`) 공고 저장도 연결 작업표에 칸을 보장하므로(2026-08-20 재발 방지 2곳), 지금 이후 만들어지는 작업에는 창구가 필요 없다. ~~서버 창구는 그대로 남긴다~~ → **2026-08-23 에 그 라우트도 제거**됐다(실행부 `ensureOptionColumn` 은 작업표 생성·공고 저장 훅이 계속 쓴다). 화면 코드(모달 88줄)는 **죽은 코드를 남기지 않으려고 통째로 제거**했다.
- ★★ **권한 ≠ 노출**: 서버 = adminOrMaster · **화면 버튼 = master 전용**. 화면이 서버보다 **일부러 좁다** — admin 이 써야 하면 API 로 쓴다. (⚠ 08-23 에 화면 ≡ 서버로 잠시 넓혔다가 **08-24 사용자 확정으로 다시 master 전용**. 두 번 뒤집혔으니 바꾸기 전에 반드시 물을 것.) ★ 여전히 유효한 규율은 **화면이 서버보다 넓어지면 안 된다**는 방향이다. 회귀가드가 그 방향을 고정한다.
- ~~줄 정리 게이트를 adminOrMaster 로 좁혔다~~ → 그 라우트 자체가 제거됐다(위 절).
- **남은 2종**: [♻ 중복 정리](**master 노출** · 서버 admin/master · 할 일 있을 때만 — `worktableDupWatch` 감시망이 **읽기 전용**이라 고칠 경로가 이것뿐) · [＋ 블로거 추가](정리 도구가 아니라 **업무 입력** 창구).
- ★★ **조건부 노출 — 할 일이 있을 때만 띄운다 (2026-08-21 사용자 확정, 2차)**: 종전에는 역할·무시트 여부만 보고 상시 노출돼 **눌러서 미리보기를 봐야 "대상 0건"을 알 수 있었다**.
  - **판정 재료 = 그 도구가 실제로 쓰는 것과 같은 것(사본 금지)** — ♻ 중복 정리는 모달 미리보기와 **같은 호출**(`POST /worktable/dedupe-rows` `dryRun:true` → `removeRows`), ~~🧹 줄 정리는 `STATE.wd.roster` 차수 그룹~~(그 도구는 제거됨). 갈라지면 "배지는 0인데 열어 보면 대상이 있다"가 된다.
  - ★★★ **모르면 숨기지 않는다(fail-open · 완화 금지)** — 조회 실패·미도착·응답 형태 불일치·**다른 작업의 값이 남아 있는 경우**(키 불일치)는 전부 `null`(모름)로 접고 **종전대로 노출**한다. 숨기면 "정리하고 싶은데 버튼이 없다"는 조용한 봉쇄가 된다.
  - **서버는 [⋯] 를 열 때만 부른다** · 같은 작업은 1회만(`_MHB.key`) · 건수가 있으면 배지(`♻ 중복 정리 3`).
  - ★ **메뉴 내용 생성기는 `_mhMenuHtml()` 한 곳** — 헤더 첫 렌더와 배지 도착 후 재렌더가 같은 함수를 쓴다(사본을 두면 두 화면이 갈린다). 재렌더는 **껍데기(`#mhMenuBox`)만** 갈아끼운다(헤더를 다시 그리면 열린 메뉴가 닫힌다). 진실원천 전환 버튼은 렌더 지역변수에서 `STATE._flipBtnHtml` 로 옮겼다(재렌더 시 못 읽는다).
  - ★ **조건부 노출이 권한을 넓히지 않는다** — 서버가 막는 역할(staff·advertiser)에게는 할 일이 있어도 안 띄운다. 반대로 **역할이 맞아도 할 일이 0건이면 안 띄운다**(조건부 노출은 역할과 무관하게 적용된다).
  - 회귀가드 `tests/workdeskToolMenuConditional.test.js`(27 — **vm 실제 실행**: fail-open 4종 · 0건 숨김 · 배지 · 차수 판정 · 권한 불변 · 배선, **변이시험 6종 검출 확인**). ⚠ 이 변경으로 `blogRegister`·`workdeskWidthCap` 의 정적 배선 패턴을 새 구조에 맞춰 갱신했다(검사 의미 불변).
- **잔여(문서화)**: 고아 캡처 정리 버튼은 아직 [⋯] 에 없다(A·B·C 전부) — 넣는다면 위 조건부 노출 규율(같은 재료 · fail-open · 0건 숨김)을 그대로 따를 것.
- 회귀가드 갱신 `tests/sheetlessRetireRows.test.js`(게이트 adminOrMaster · 화면 master · **서버보다 넓지 않음**) · `tests/worktableDuplicateGuard.test.js`(같은 축) · `tests/rowNumbering.test.js`([⋯] 부재 고정 · 전체 진입점 존재 유지) · `tests/worktableOptionColumn.test.js`(화면 부재 + 서버 존재로 **계약을 뒤집어** 고정).
