name: oneoff-ph9
on:
  push:
    branches: [oneoff/ph9]
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - env:
          PGURL: ${{ secrets.BACKUP_DATABASE_URL }}
        run: psql "$PGURL" -X -A -F $'\t' -P pager=off -f q.sql
