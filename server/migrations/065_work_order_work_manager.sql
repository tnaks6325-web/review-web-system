-- ═══════════════════════════════════════════════════════════
-- 065: 작업오더에 "작업담당"(리뷰웹 실무자) 추가
--
-- 배경: 인트라넷 작업오더에는 담당AE(예: 이만수)와 작업담당(박세희 / 박은비 / 랜덤)이
--   함께 적힌다. 리뷰웹은 지금까지 담당AE(manager_name·created_by)만 받아서,
--   모집공고의 담당자(만두/망고)와 캠페인 탭의 담당자를 관리자가 매번 손으로 골라야 했다.
--
-- 매핑(단일 출처 = server/src/utils/workManager.js):
--   박세희 → 만두 · 박은비 → 망고 · 랜덤 → (직접결정, 자동선택 안 함)
--
-- 담당AE는 신규 컬럼을 만들지 않는다 — 기존 manager_name(인트라넷 표기 실명)과
--   created_by(제출 계정)로 이미 표현되며, 화면은 manager_name 우선 폴백으로 표시한다.
--
-- 비파괴적: 컬럼 추가만. 기존 오더는 빈 값 = 종전처럼 관리자가 직접 선택.
-- ═══════════════════════════════════════════════════════════
ALTER TABLE work_orders ADD COLUMN IF NOT EXISTS work_manager TEXT DEFAULT '';
