-- 164: 리뷰 캡처 파일명 소급 정정(주문자 → 수취인)의 되돌리기 재료.
--
-- 왜: 과거 캡처는 Drive 에 **주문자(로그인 본계정)** 이름으로 쌓여 타계정 건을 구분할 수 없다
--     (결정 009 후속 · 2026-09-22 신고). 앞으로의 저장은 고쳤고, 과거분은 rename 으로 정정한다.
--     ★ Drive rename 은 되돌리기 어려운 외부 쓰기라 **바꾸기 전 이름을 반드시 남긴다** —
--       이 칸이 없으면 잘못 바꿨을 때 원래 이름을 아무도 모른다.
--
-- ★ 컬럼 추가만 · 백필 0 · 제약 0 = 배포 즉시 동작 불변(아무도 읽지 않으면 없는 것과 같다).
ALTER TABLE review_submissions ADD COLUMN IF NOT EXISTS renamed_from TEXT;
