-- ═══════════════════════════════════════════════════════════════════════════
-- 101 — 블로그체험단 M5: 블로그URL (블로그 주소)
--
-- 사용자 확정(2026-08-10):
--   · 블로그URL = **그 사람의 블로그 주소**(예 blog.naver.com/아이디) — 모집·선정 단계 값.
--     작업이 끝난 뒤 제출하는 **포스팅URL(쓴 글 주소)과 다른 칸**이다(그쪽은 M4-1 memo 열).
--   · 입력 주체는 **둘** — ① 공고 참여 시 블로거 본인 ② 별도 채널로 모집해 온 블로거를
--     관리자가 사전등록(작업보드 [＋ 블로거 추가], M5 후속).
--
-- ★ 컬럼 추가만 · 백필 0 · DB CHECK 없음(087 리뷰타입·099 work_kind 와 같은 규율 —
--   판정을 코드와 DB 에 이중화하면 형식 규칙이 바뀔 때마다 저장이 실패한다).
-- ★ 빈 값 = "블로그 작업이 아니거나 아직 안 받음" = 오늘 동작 그대로(opt-in).
-- ═══════════════════════════════════════════════════════════════════════════

-- ① 참여(홀드) 시점에 새긴다 — 공고 참여 게이트가 여기에 저장한다.
ALTER TABLE campaign_applications ADD COLUMN IF NOT EXISTS blog_url TEXT;

-- ② 주문 원장으로 전파 — 시트/작업표 '블로그URL' 칸을 채우는 값의 출처.
--    (홀드 확정 시 COALESCE 로 전파 = 이미 있는 값은 안 덮는다. 082 리뷰비 스냅샷과 같은 자리.)
ALTER TABLE order_submissions ADD COLUMN IF NOT EXISTS blog_url TEXT;
