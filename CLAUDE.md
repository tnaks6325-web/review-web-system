# CLAUDE.md

## Shared UI naming

### UI style proposal gate

- UI 변경·신규 화면·시안 요청을 받으면, 구현 전에 반드시 **A / B / C 스타일 중 어떤 방향이 적합한지 먼저 제안**하고 사용자의 선택을 받는다. 사용자가 이미 특정 스타일을 지목한 경우에는 그 스타일을 바로 적용한다.
- **A·B스타일은 아직 정의되지 않았다** — 임의로 정의하지 말고 기존 화면의 근거를 찾거나 사용자에게 기준을 확인한다.
- **C스타일**: Track B 입금관리의 담당자·작업 선택 UI를 뜻한다. 담당자는 `전체 / 망고 / 만두` 등 단일 선택 토글로 고르고, 작업은 펼쳐진 목록에서 행 전체를 눌러 복수 선택·해제한다. 체크박스나 드롭다운 작업 선택은 사용하지 않는다. 선택 행은 배경·테두리·확인 표시로 구분하며, 합계 금액과 다운로드 대상은 선택된 작업들의 체크된 입금 항목만 기준으로 계산한다.
- C스타일 영역의 담당자 표기는 `박은비`가 아니라 항상 `망고`로 정규화한다.

이 파일은 Claude Code가 이 저장소에서 작업할 때 따르는 지침입니다.

## 프로젝트 개요
리뷰(체험단) 관리 시스템.
- **백엔드**: Node.js + Express + PostgreSQL → Railway 배포
- **프론트엔드**: Vanilla JS + HTML → Cloudflare Pages 배포 (https://review-web-system.pages.dev)
- **인증**: JWT (`/api/admin/login` 발급, `Authorization: Bearer` 헤더)

## 디렉터리 구조
- `frontend/` — 정적 페이지(`admin.html` 관리자 대시보드, `portal.html` 업무포털, `search.html` 리뷰제출, `staff.html` AE담당자 등)와 `js/`, `css/`. `docs/prd-userflow.html` = 시스템 소개서(PRD·유저플로우·와이어프레임, 관리자 헤더 "시스템 소개서" 버튼에서 새창 열람)
- `frontend/api.js` — GAS 호환 API 래퍼(`gasGet`/`gasPost`), 토큰은 `sessionStorage.admin_token`
- `server/src/routes/` — Express 라우트(`*.routes.js`), `server/src/middleware/auth.middleware.js`에 역할별 미들웨어(master/admin/staff)
- `server/migrations/` — DB 마이그레이션
- `.github/workflows/` — DB/Drive 백업·복구 리허설 (앱 빌드/배포 워크플로 아님)

### 관리자 시스템공지 필수열람 게이트
- 관리자 공지(`admin_notices`, 마스터 작성 → 로그인 팝업 + `admin_notice_reads` 계정별 확인 기록)에서 **본문에 `[필수열람]` 마커 + 문서 URL**을 넣으면 팝업이 URL을 자동 링크화하고 **링크 열람(클릭) 전까지 [확인] 비활성**(`_showAdminNoticePopup`/`_noticeDocOpened`, opt-in — 마커 없는 공지는 동작 불변, 링크 없는 필수열람은 게이트 미적용=잠금사고 방지). 링크화는 escape 후 따옴표·꺾쇠 미포함 매칭(href breakout 방지). 회귀가드 `tests/adminNoticeGate.test.js`. 기능소개서류 문서(`frontend/docs/*.html`)를 전 관리자 필수열람시킬 때 이 경로 사용.

### 작업탭 등록 단일경로 (작업 중인 탭 목록 = tab_configs)
- 관리자 대시보드 "작업 중인 탭 목록"은 `tab_configs`를 그대로 표시(`GET /api/tab/dashboard`, 아카이브 탭만 제외). **신규 시트/탭이 `tab_configs`·`campaigns`에 등록되는 경로는 작업오더 접수(`POST /api/order/accept`) 하나로 일원화**(`utils/tabRegistration.js`, env `TAB_REGISTRATION_MODE`: `order` 기본 | `manual`=+관리자 수동 버튼 | `auto`=레거시 전체 허용·롤백용).
- **게이트 적용점**: ① smartBuild·전체빌드(indexBuilder)는 `tab_configs` 등록 탭(**이름 또는 gid 일치** — gid 일치=리네임이라 빌드 유지)만 읽기/빌드 — **미등록 탭은 인덱스 자체가 안 생겨 목록·검색·제출 모두 접수 후 활성화**(스킵은 `tabsSkippedUnregistered` 로그). ② 동기화(`syncTabListToDB`)는 기본 신규 campaigns/tabs/index INSERT 스킵(gid 보정·리네임만, `registrationGate` 보고) — **`manual` 모드도 자동추가는 차단**(`auto`만 허용). ③ 수동 등록 `diag/add-tab`·`add-campaign`은 order 모드에서 안내 메시지 반환(`registrationLocked`), 프론트 '작업시트추가' 버튼은 `tabRegistrationMode`가 order면 숨김. ④ '새 시트생성'(`create-campaign-sheet`, admin+AE)은 **시트/탭 생성·SA권한부여는 유지, DB 등록만 접수 시점으로 이연**(`registrationDeferred`).
- **예외(복원 성격, 모드 무관)**: 아카이브 복구, DB 재구축(`syncTabListToDB({allowNewTabs:true})` — 재해복구는 시트 현황 복원이라 게이트 우회), 탭명·gid 교정(`sync-tab-names`/fix-swap). migration 057 = 전환 시 `index_master(active)`에만 있던 탭 1회 백필(app_settings `migration_057_tab_backfill_done` 가드로 재배포 시 재실행 안 함). 회귀가드 `tests/tabRegistrationGate.test.js`.
- 운영 함의: 접수 전 탭은 리뷰어 검색이 안 되므로 **모집 시작 전에 접수 먼저**. 긴급 복원은 Railway `TAB_REGISTRATION_MODE=auto`.

### 작업오더 접수 gid 미발견 → 탭 골라 교정 재접수
- **사고(2026-08-05)**: 작업시트탭URL의 gid가 그 시트에 없어(탭 삭제 후 재생성 = gid 변경 / 다른 시트의 gid) 접수가 404 막다른 길. `getSpreadsheetMeta`는 무캐시 실시간 조회라 이 404 = **시트에 그 gid 탭이 진짜 없음**이다.
- **해결**: `/api/order/admin/accept` 404에 `gidNotFound`+`availableTabs`(실제 탭 목록) 동봉 → 공유 팝업 `woAcceptTabPicker`(work-order-detail.js, admin·workdesk 공용)에서 **사람이 탭을 골라** `body.gid`로 재접수. 서버는 고른 gid를 URL gid보다 우선하고 **`work_sheet_url`도 함께 교정**(COALESCE — 교정 시에만, 안 고치면 프리필·재접수가 계속 죽은 gid를 본다). ★ **자동 이름매칭 폴백 금지**(잘못된 탭 연결이 빈칸보다 나쁨) ★ 탭명은 시트발 외부 문자열 — 팝업은 textContent+addEventListener만(onclick 보간 금지). 회귀가드 `tests/orderAcceptTabPicker.test.js`(팝업 vm 실행 XSS 포함).

### ★★ 접수 시 "동일 이름의 기존 광고주" — 자동 병합 금지 / 사람이 확인하면 연결 (2026-08-19)
- **사고**: 인트라넷 오더에 실려 오는 `intranet_advertiser_id` 로 업체를 연결하는데(103), **업체관리에서 먼저 만든 업체는 그 칸이 빈 값**이라 이름이 겹치는 순간 접수가 `409 광고주 작업 연결을 완료하지 못했습니다`로 **영구히 막혔다**. 그 칸을 채울 창구가 화면 어디에도 없어(쓰는 곳이 접수 경로 하나뿐) 오류 문구가 시키는 "원본 연결 확인"을 할 방법 자체가 없었다.
- ★★ **이름으로 자동 병합하지 않는다(완화 금지)** — 업체는 작업 소유·정산 계약·**광고주 접속 링크(로그인 없이 열리는 URL)** 가 매달린 단위라, 동명 오판 한 번이 남의 작업·연락처·정산 금액을 통째로 여는 사고다. 판정 근거는 여전히 `intranet_advertiser_id` 하나(103 규율).
- **해결 = 확인 후 연결**: 충돌 시 409 에 `advertiserNameConflict{ name, businessNumber, contact, candidates[] }`(후보의 사업자번호·연락처·담당AE·소유 작업 수)를 실어 주고, 공유 팝업 `woAdvertiserLinkPicker`(work-order-detail.js — 관리자 대시보드·리뷰웹시스템[3버전] 공용)에서 사람이 [같은 업체입니다 · 연결하고 접수]를 누르면 `body.linkAdvertiserId` 로 재접수한다. 서버는 그때만 그 업체에 원본 ID 를 **blank-only 백필**한다. gid 미발견 재접수(`woAcceptTabPicker`)와 같은 패턴.
- ★ **fail-closed 3종**: 이미 다른 인트라넷 광고주에 연결된 업체는 거부(남의 원본 탈취 금지 — 팝업에서도 **비활성 + 사유**) · 그 사이 업체명이 바뀌면 거부(판단 근거가 달라졌다) · 백필 UPDATE 0행이면 실패(경합을 성공으로 읽지 않는다). 충돌은 **ROLLBACK** 이라 advertisers 에 INSERT/UPDATE 가 한 줄도 남지 않는다.
- ★ **원본 ID 매칭이 최우선** — 확인값이 실려 와도 그 인트라넷 광고주가 이미 다른 업체에 연결돼 있으면 그 업체가 이긴다. **인트라넷 원본 ID 가 없는 오더는 무동작**(기존 동작 불변).
- ★ **연결 로직은 서비스 한 곳** `advertiserProjection.service.js`(`projectIntranetAdvertiser`) — order.routes 의 인라인 사본을 이관(테스트 seam 확보 + 사본 금지). ★ 이름이 정말 다른 회사면 연결하지 말고 **업무포털 거래처 관리에서 기존 업체명을 구분되게 바꾼 뒤 재접수**(`advertisers.name` 이 UNIQUE 라 같은 이름으로는 새로 만들 수 없다 — 팝업이 이 사실을 문장으로 안내).
- ★ **후보의 사업자번호는 "없음"이 아니라 "미등록"이라고 말한다**(`_woAdvBizLine` — 사용자 지적 2026-08-19): 리뷰웹 업체는 업체관리에서 만들면 그 칸이 **애초에 비어 있는데**(103 이전 개념·업체 추가 폼에 입력칸 없음) `사업자번호 없음` 으로 적으면 **대조 실패(= 다른 회사)** 로 읽혀 판단을 방해한다 → `사업자번호 미등록 · 연결하면 원본에서 채워집니다`. 값이 있는데 원본과 **다르면** 빨강 ⚠(경고만 — 막지 않는다), 같으면 초록 `원본과 일치`. **비교는 숫자만**(`365-87-02833` ↔ `3658702833` 를 다름으로 오판 금지).
- 회귀가드 `tests/advertiserNameLinkConfirm.test.js`(스텁 pool 로 서비스 **실제 실행**(충돌 시 쓰기 0·blank-only·fail-closed 3종·원본 우선) + 라우트/화면 배선) + 실 http 오리진 브라우저 실측(body 직속·후보 렌더·비활성 후보·확인 후 id 반환). ⚠ 이 변경으로 `orderAcceptTabPicker` 의 재접수 배선 패턴(인자 추가)과 `reviewOrderAdvertiserProjection` 의 대상 파일을 함께 갱신했다(검사 의미 불변).

### 작업오더 관리자 수동 수정 (인트라넷 보낸오더카드 동기화)
- **시안** `frontend/docs/작업오더_관리자수정_와이어프레임.html`(2026-08-05 확정). `PUT /api/order/admin/edit`(adminOrMaster) + `PUT /api/trackb/work-orders/edit`(`_delegate` 위임, internal+**editorOnly** — 접수·상태변경과 같은 2단 권한). 화이트리스트 `ADMIN_EDIT_FIELDS` 부분수정(PATCH), **빈 값 = "지움"이 아니라 "변경 없음"**(칸 비우기 사고 방지), 같은 값 = no-op(메모 도배 방지).
- ★ **편집 불가 3종(완화 금지)**: `status`(전용 [상태 변경] 흐름) · 계약건 `sales_id/contract_number/quote_id`(인트라넷 발주 확정값·정산 매칭 보호) · `work_sheet_url`(접수·작업표 생성·gid 교정 재접수가 관리). `guide_images`는 1차 제외(업로드 UI 필요).
- **동기화 = 신규 채널 0**: 인트라넷 "보낸 오더" 목록은 리뷰웹 `work_orders`를 실시간 조회(`/api/review-order/list` → intake/list 프록시, 캐시 병합 시 리뷰웹 값 우선)라 **값은 자동 반영** — 수정 diff 요약만 기존 처리메모 webhook(`_pushIntranetMemo` 공용 헬퍼, send-memo와 한 벌)으로 push해 카드 주황 메모 블록에 표시(인트라넷 수신부 무변경, fail-soft). diff는 `memo_log`에 `kind:'admin_edit'`로 누적(수정 이력, `admin_memo`는 안 덮음). 긴 텍스트 필드는 "내용 수정"으로 요약.
- **모달은 공유 한 벌**(`work-order-detail.js` `woAdminEditModal`) — 호스트가 `opts.save`로 자기 경로 주입(admin=`orderAdminEdit` / workdesk=trackb, workdesk 버튼은 `_woEditActions` 안 = 허용명단 게이트 상속). ★ 오더 값은 전부 `.value`/`textContent`로만 주입(innerHTML 보간 금지). 바뀐 필드만 전송. 회귀가드 `tests/workOrderAdminEdit.test.js`(스텁 pool 핸들러 실행 + 모달 vm 실행 XSS·diff 전송).

### 작업오더 도착 알림 (관리자 실시간)
- 신규 제출(`POST /api/order/submit`·`/intake`)·보완 재제출(revision→submitted)은 SSE `work_order_new` 발신(`sse.emitWorkOrderNew`, 페이로드 최소 — 가이드 본문 미포함). 관리자 대시보드는 SSE(실시간)+2분 폴링(폴백)을 `_woCheckNewOrders` **단일 진입점**으로 수렴해 **우측하단 미확인 알림 카드**(자동 소멸 없음 — 클릭해야만 닫힘, 최신 5장+"외 N건" 요약)와 **OS 알림**(`requireInteraction:true`, 오더별 tag, 4건+는 요약 1건)을 띄운다. 알림 클릭 → `openWoDetailModal`(내용 전체 = `_woDetailHtml`+메모로그, [작업오더 탭에서 처리]).
- 확인 여부는 `localStorage.wo_notif_seen_v1`(프루닝 = 현재 submitted 목록과의 생존 교집합)에 기록 — **미확인 카드는 새로고침·재접속에도 재표시**, 타 관리자가 접수/삭제하면 reconcile로 자동 회수. 스택/모달은 JS 동적 생성이라 admin.html·admin-siand.html 공용(HTML/CSS 무수정). 한계: SSE 단절 중 재제출은 폴링만으로 구별 불가(updated_at 비교는 메모 전송 오탐이라 미사용). 회귀가드 `tests/workOrderNotify.test.js`.

### 리뷰 이미지 ↔ 인덱스 연결 (데이터 모델)
- 리뷰 캡처는 `AI_REVIEW_FOLDER → {시트제목} → {탭명} → [리뷰]` 폴더에 저장(`drive.service.js`의 `ensureReviewFolderPath`). 업로드 시 폴더ID를 못 잡으면 루트로 새므로 `uploadFileBase64`가 빈 `parentFolderId`를 차단한다.
- `review_index.review_file_*`(031, A-1): 제출 행당 대표 리뷰 이미지 1장(파일ID/URL/이름/개수/시각). `review-upload`가 업로드 즉시 기록.
- `review_submissions`(032, A-2): 리뷰 이미지 파일 단위 원장(탭/행/리뷰어/파일ID/제출시각/출처). `file_id` 유니크 업서트로 재실행 안전.
- 과거에 루트로 샌 캡처는 관리자 "리뷰 캡처 정리"(`POST /api/drive/relocate-orphan-reviews`)로 `[리뷰]` 폴더 이동 + 파일명 이름↔행 결정적 링크 백필(모호하면 링크 안 함).
- **탭 `[리뷰]` 폴더 스캔 백필**(`POST /api/drive/review-folder-backfill {sheetId, tabName, folderUrl?, dryRun?}`): 폴더에는 캡처가 있는데 원장·대표 이미지가 비어 업체 뷰어 미리보기가 "리뷰 이미지 미등록"으로 뜨는 탭용(031/032 이전 제출분·직원 수동 업로드분). 폴더 내 이미지를 나열해 파일명 이름↔행 결정적 매칭으로 백필 — 폴더 미연결 탭은 `ensureReviewFolderPath`로 자동 매핑(기존 폴더 탐색·없으면 생성) 후 `tab_configs.folder_url` 연결. relocate와의 차이 = OCR 키워드 검색·이동 없음(폴더 자체가 스코프). ★★ **링크 규칙 단일 출처 = `reviewFileLink.service.js`(`linkReviewFilesToRows`)** — relocate·백필 두 소비처가 같은 헬퍼를 쓴다(원조 relocate의 인라인 블록을 이관, 사본 금지). dryRun 기본 true. 관리자 UI = "리뷰 캡처 정리" 모달의 [선택 탭 폴더 이미지 연결] 버튼(미리보기→실행 2단계). 회귀가드 `tests/reviewFolderBackfill.test.js`(스텁 db 실행 + 사본 부재).

### 리뷰어 홈 "리뷰 내역" 제출대기/제출완료 탭
- `index.html` 내정보/현황의 리뷰 내역은 `GET /api/search?includeSubmitted=1`로 대기+완료를 한 번에 받아 `isSubmitted`로 좌우 서브탭(기본 [제출대기]) 분리. 완료 카드는 클릭/이동 없음(초록 배지) + `review_index.review_file_at` 기반 제출일 표시 + **입금 데이터가 채워진 행은 "입금완료" 남색 배지 병기**(`isPaid` = `is_submitted2='PAID'` 또는 row_json 입금 키워드 컬럼 값 존재 — 대시보드 집계와 동일 판정, row 비우기 전 서버에서 계산). 다중 캡처 슬롯 부분 제출은 대기 탭에서 "n/m 제출" 배지. bfcache 복귀(pageshow persisted) 시 재조회.
- **보안 가드**(`/api/search`는 무인증): 제출완료 행은 **phone8/participation_links 정확 일치 매칭에만 포함**(이름 단독·이름+전화근접 약한 키엔 미개방 — 이름만으로 타인 제출이력 스크래핑 차단), 완료 행의 `row`(행 전체 JSON)는 비워 반환(데이터 최소화). includeSubmitted 시 LIMIT 400 + `is_submitted ASC` 정렬(대기 건 절단 보호, 절단 시 완료 탭 하단 고지). 회귀가드 `tests/searchSubmittedGuard.test.js`.
- ★★ **주문원장 병합 카드의 짝짓기 키 = 주문 id (2026-08-19 "제목 수정하자마자 참여가 두 장으로 쪼개짐")**: 병합(`_mergeOrderSubmissions`)은 위치키(`시트ID||탭명||행번호`)로 중복을 걸렀는데, **참여형(무시트) 주문의 원장 좌표는 `campaign:<공고ID>`**(`submit.routes._resolveCampaignOrderScope`)라 작업표(`review_index`) 좌표와 **영원히 일치하지 않는다** → 중복이 구조적으로 안 걸러졌다. 보완으로 넣었던 제목 매칭(`ri.tab_name = rc.title`, 59f936c·migration 120)은 **제목을 고치는 순간 풀린다**. → **`campaign_participants.order_submission_id`** 로 짝짓는다(제목·탭명·행번호가 바뀌어도 불변). ★ `review_index` 와 조인해 **"이 리뷰어에게 실제로 보이는 행"만** dedup 근거로 인정(위치키 dedup 과 같은 보장 — 안 그러면 남의 작업표 행 때문에 참여가 화면에서 사라진다) · **조회 실패는 fail-soft**(중복 노출 > 참여 실종) · 제목 매칭에는 **연결 작업표 좌표 매칭을 병기**(가산적).
- ★★ **카드에 보이는 작업 이름 = 지금 적용된 공고 제목(사용자 확정 2026-08-19)**: 종전엔 색인행 카드가 `tab_configs.display_name`(접수 시점 고정 — 업서트가 **blank-only** 라 제목을 절대 안 따라간다), 병합 카드가 `rc.title`(실시간)이라 **같은 참여가 옛 제목·새 제목 두 이름으로 갈렸다**. 지금은 **양쪽 다 공고 제목**을 쓴다 — 색인행은 `campaignTitleContext.service.campaignTitlesForTabs`(리뷰타입·체험단 종류와 **같은 배치·같은 60초 캐시·같은 fail-soft**), 병합 카드는 `COALESCE(NULLIF(rc.title,''), NULLIF(wt.display_name,''), NULLIF(wt.tab_name,''), tc.display_name)`. ★ 조회 실패·미연결 탭은 **종전 탭 표시명으로 접는다**(이름이 비면 안 된다). ★ **본검색·폴백 두 경로 모두** 적용(한쪽만 고치면 화면이 갈린다 — 회귀가드가 호출 2회를 센다). ★ 이름 규칙(gid 폴백·값이 있는 최신 공고)은 `utils/campaignTabLateral` 공유 조각이 소유(`ALLOWED_COLUMNS` 에 `title` 합류). ⚠ **제목 수정이 리뷰어 화면에 반영되는 데 최대 60초**(`CAMPAIGN_TITLE_CACHE_MS`). ⚠ 이 표기는 **주문 id dedup 위에서만 안전하다** — 이름이 달라져도 카드가 늘지 않는 것은 dedup 이 보장한다.
- 회귀가드 `tests/reviewerHistoryTitleSplit.test.js`(스텁 pool 로 병합 실제 실행, **변이시험 검출 확인**) + `tests/reviewerHistoryTitleSplitPg.test.js`(**진짜 PG16** — 수정 전 코드에서 신고 증상이 그대로 재현되는 것까지 확인). ⚠ 착수 시점에 이미 빨간 상태였던 `tests/reviewerPurchaseFormStatus.test.js` 의 배지 패턴을 현재 UI(7768c3d)에 맞춰 갱신(검사 의미 불변).
- ★★ **리뷰어 화면에 내부 키를 그리지 않는다 (2026-08-19 신고 — `campaign:camp_0df34a3b8664`)**: 참여형(무시트) 주문의 원장 좌표(`campaign:<공고ID>`)·가상 시트ID(`wt_…`)는 **좌표이지 이름이 아니다**. 구매 캡처 보완 카드가 `tabName` 을 이름 폴백으로 써서 그대로 노출됐다. → 서버(`/my-missing-captures`)가 **리뷰 내역 카드와 같은 우선순위**(공고 제목 → 연결 작업표 탭 이름 → 원장 탭 설정)로 이름을 확정하고, 프론트는 **`_taskLabel` 단일 출처**로 그린다(내부 키는 걸러내고, 이름을 못 찾으면 빈 문자열이 아니라 `참여 작업`). ★ `tabName`·`sheetId` 는 **첨부 업로드의 폴더 좌표**라 계속 내려보내되 **그리지 않는다**. ★ 이름 폴백에 `|| item.tabName` 사본을 되살리지 말 것(회귀가드가 부재를 고정).
- ★★ **받을 예정 금액 이중 집계 — 여기도 위치키가 참여형에서 성립하지 않는다**: `/review-earnings` 의 무시트 주문 합류가 `NOT EXISTS(ri.sheet_id=os.sheet_id AND … row_index=os.sheet_row)` 로 중복을 걸렀는데, 참여형 주문은 원장 좌표가 `campaign:*`·`sheet_row` 가 작업표 seq 라 **절대 안 걸러져** 같은 참여가 명단과 원장 양쪽에서 세어졌다(실측: 카드 2장 35,900원인데 배지 `참여중 3건 · 49,800원` — **건수·금액이 함께 부풀었다**). → 리뷰 내역 dedup 과 **같은 키**(작업표 줄 = `campaign_participants.order_submission_id` 링크 좌표)로도 짝짓는다. ★ 소프트삭제된 작업표 줄은 근거가 아니다 · 명단에 아직 없는 주문은 종전대로 합계에 든다(무회귀).
- 회귀가드 `tests/reviewerTaskLabel.test.js`(라벨 순수함수 vm 실행·사본 금지·서버 계약) + `tests/reviewerMissingCapturePg.test.js`(**진짜 PG16** — 수정 전 코드에서 이름 노출·이중 계수가 그대로 재현되는 것까지 확인). ⚠ **스텁 매칭 순서 함정**(`tests/csEntryRework.test.js`) — 이중집계 방지 `NOT EXISTS` 안의 `FROM review_index ri` 를 명단 fixture 분기가 가로챘다(코드 버그 아님). **스텁 분기는 더 좁은 조건을 먼저** 둘 것.
- 한계: 완료 탭은 활성 `review_index`의 뷰 — 차수/탭 아카이브 시 해당 완료 내역은 화면에서 사라진다(archive 테이블 미조회). 과거 오염 `participation_links`가 완료 탭에 재부상할 수 있으므로 필요 시 `POST /api/diag/participation-cleanup`(dryRun 먼저) 권장.
- ★ **카드는 두 탭 공통 한 벌**(`renderReviewSubTab` 단건 분기): 썸네일 + 3줄(날짜·이름 / 작업명 / 상품비·리뷰비). 완료 전용 2줄 카드(초록 체크 아바타 + 하단 통짜 [이미지 확인·수정요청])는 **제거** — 사본을 두면 두 탭이 계속 어긋난다(작업내용 렌더러 공용화와 같은 규율). 제출일은 첫 줄 꼬리(`· 제출 MM.DD`, 지난 해면 `YY.MM.DD`)로 붙여 줄 수를 늘리지 않고, 입금완료 배지는 상태 배지 아랫줄(`.result-right.rr-col`)로 내려 제목 잘림을 줄인다. **완료 카드도 클릭 대상**(참여상품 정보 팝업 진입, 완료는 그 행 1건만 전달).
- **상단 금액 블록**(`renderReviewSummary`, `GET /api/reviewer/review-earnings`): 참여중 = `💰 받을 예정 금액`(미제출 행 합계, 종전과 동일) / 제출완료 = `🏆 누적 금액`(초록). ★ **누적은 입금완료된 건만 합산**(사용자 확정 기준) — 제외한 미입금 건수를 `unpaidCount`로 함께 반환해 화면에 고지한다(조용한 누락 금지). 입금 판정은 `search.service`의 `PAYMENT_COL_KEYWORDS`를 **그대로 ILIKE 패턴으로 파생**해 SQL에서 계산(`is_submitted2='PAID' OR jsonb_each_text(row_json) 입금열 비어있지 않음`) — 배지(`_isPaid`)와 합계가 갈라지지 않고 `row_json`을 서버로 끌어오지 않아 메모리도 안전. 완료 행도 `items`에 실려 카드 상품비/리뷰비 배지가 채워진다. 실패 폴백도 `doneTotals` 형태를 유지(프론트 undefined 접근 방지).

### ★★ 캠페인 기간별 리뷰비 (migration 082) — 리뷰비는 "그 건의 참여 시점" 금액이다
- **사고(2026-08)**: 같은 작업의 리뷰비를 7월 1,000원 → 8월 1,500원으로 올리자 **7월 참여자의 리뷰 내역 카드·누적 금액까지 1,500원**이 되어 리뷰어 문의가 발생했다. 원인 = 리뷰비가 `recruit_campaigns.review_fee` **한 칸**뿐이고 리뷰어 화면이 조회할 때마다 **현재 값**을 다시 읽는 구조(참여 당시 금액을 아무 데도 안 남겼다).
- **모델(두 겹, 하나만으론 안 됨)**: ① **구간표 `campaign_fee_schedules`** = 앞으로 줄 금액(`7/1부터 1,000 · 8/1부터 1,500`) ② **스냅샷** `campaign_applications/order_submissions.review_fee_snapshot` = 이미 준 금액(참여 시점에 새김). 구간표만 두면 구간을 고칠 때 과거 표기가 또 흔들리고, 스냅샷만 두면 예약·과거분 구제가 안 된다.
- ★★ **종료일은 저장하지 않는다** — 시작일만 받으면 "다음 구간 시작 전날까지"가 자동이라 **빈틈(그날 금액 없음)·겹침(어느 쪽인지 모름)이 구조적으로 불가능**. 종료일을 받는 순간 `7/31`↔`8/1` 사이 하루가 비는 입력이 가능해진다. 같은 날 두 금액은 `UNIQUE(campaign_id, effective_from)`가 최종 방어.
- ★★ **판정 단일 출처 = `utils/campaignFee.js`**(`resolveReviewFee`). 순서 = **스냅샷 → 주문 제출일 → 시트 구매일자(`review_index.start_date`) → 오늘 → 기존 `review_fee` 폴백**. 화면마다 규칙을 만들면 "카드는 1,000원인데 합계는 1,500원"으로 갈라진다. 기준일 = **참여(구매)일**(사용자 확정 — 리뷰어가 화면에서 본 금액과 일치).
- ★ **스냅샷이 있으면 무조건 스냅샷**(완화 금지) — "구간표가 더 최신이니까"로 덮으면 막으려던 사고가 재현된다. 반대로 **배포 이전 과거 건은 스냅샷이 없어** 구간만 등록하면 자동 정정된다(이번 7월 건 수습 경로 = 수기 정정 0).
- ★ **구간 0개 = 기존 동작 100%**(opt-in). 킬스위치 `CAMPAIGN_FEE_SCHEDULE=0`이면 전건 기존 `review_fee`로 즉시 복귀.
- ★★ **`_int('')`·`_int(null)`을 0으로 접지 말 것** — `Number(null)===0`·`Number('')===0`이라 가드가 없으면 **"스냅샷 없음(DB NULL)"과 "빈 금액 칸"이 조용히 0원**(무상 처리)이 된다. 개발 중 회귀가드가 실제로 잡았다.
- ★★ **apply 안의 구간 조회는 SAVEPOINT 로 격리** — 트랜잭션에서는 **실패한 쿼리 하나가 tx 전체를 abort(25P02)** 시키므로(로컬 PG16 실증), 격리가 없으면 구간 테이블 부재·조회 실패가 **참여 INSERT 를 통째로 죽인다**. 주문원장 홀드확정 SAVEPOINT 와 같은 규율.
- **소비처**: `/api/reviewer/review-earnings`(카드 배지·받을예정·누적 — **탭 일괄 → 건별 판정**), `/api/reviewer/applications`(`COALESCE(snapshot, review_fee)`), 공고 목록·상세는 `_applyCurrentFee`로 **오늘 구간** 금액을 `review_fee` 자리에 실어 보낸다(프론트 무수정). 홀드 확정 시 신청→주문으로 스냅샷 전파(`COALESCE`로 기존 값 미덮음, 소유권 3조건 유지).
- **화면**: 모집공고 모달(`recruit-modal.js` `rf_fee_sched_on`/`rf_fee_rows`) 구간표 + 자동점검(같은 시작일 중복만 하드블록 — 어느 금액인지 정할 수 없다 / 첫 구간이 공고 시작일보다 늦으면 경고). **구간표 UI 있는 화면에서만 전송**(없으면 미전송=기존 구간 유지 — 옵션표와 같은 원칙), 스위치 끄면 빈 배열=구간 제거. **인상 예고는 표시하지 않는다**(사용자 확정).
- ★★ **FK 타입 불일치 = 그 마이그레이션 파일 전체가 영영 안 붙는다 (2026-08 실측 사고)**: 082 가 `campaign_id`를 **UUID**로 선언했는데 `recruit_campaigns.id`는 **TEXT**(018:9)라 PG 가 **42804**(`Key columns … are of incompatible types: uuid and text`)로 FK 생성을 거부 → 러너가 파일 다중문장을 **한 암묵 트랜잭션**으로 돌리므로 **파일 전체 롤백**(뒤의 `ADD COLUMN review_fee_snapshot` 2개까지 증발) → 42804 는 `DUP_OBJECT_CODES` 밖이라 `_migrations` **미기록** → 배포마다 재시도하고 매번 같은 자리에서 실패. 그런데 **서버는 정상 부팅**해서(그 컬럼이 `REQUIRED_SCHEMA`에 없었다) `/health` 도 통과 → **리뷰어 [참여하기]가 전면 42703**("column review_fee_snapshot … does not exist"), `/reviewer/applications` 500, `/review-earnings`는 catch 폴백으로 **금액이 조용히 0원**, 홀드확정 실패로 지각(late) 증가. 킬스위치 `CAMPAIGN_FEE_SCHEDULE=0`은 **구제하지 못한다**(판정만 끄고 INSERT 컬럼 목록은 그대로).
- ★ **재발 방지 3종**: ① `recruit_campaigns(id)` 참조 컬럼은 전부 참조 대상과 **같은 타입**(회귀가드가 018 에서 타입을 읽어 전 마이그레이션과 대조 — 문자열 `REFERENCES` 존재만 보던 옛 가드는 이 사고를 통과시켰다) ② 두 `review_fee_snapshot` 컬럼을 **`REQUIRED_SCHEMA`에 등록**(없으면 부팅 거부 = 직전 배포 유지, "서버는 멀쩡한데 참여만 조용히 안 되는" 상태 차단) ③ 프리플라이트는 `information_schema.columns`만 보므로 **테이블 부재는 못 잡는다**(구간표 조회는 fail-soft라 무해하지만 "컬럼 있으니 완전 적용" 오판 금지).
- 회귀가드 `tests/campaignFeeSchedule.test.js`(판정 순수함수 실행 + 배선 + **FK 타입 정합·프리필라이트 등록** + **`created_at` 재발 차단**: `order_submissions` 에는 created_at 컬럼이 없다는 전제를 마이그레이션에서 읽어 고정하고, 그 테이블을 지목하는 SQL 전체를 스캔한다 — 옛 가드가 **버그 쪽 컬럼명을 고정**하고 있어 #488 수정 후 계속 실패했고, 그대로 두면 "테스트를 통과시키려고" 코드를 버그로 되돌리게 된다). 검증은 **진짜 PG16**으로 마이그레이션 idempotent·`<> ALL($2::date[])` 바인딩·UNIQUE/CHECK·전파 소유권·SAVEPOINT 격리 + **42804 재현 → 수정본으로 apply INSERT·스냅샷 전파·CASCADE 실행 확인**, 모달은 **테마 없는 호스트(리뷰웹시스템[3버전] 조건)에서 실제 렌더**로 확인. 설계·시안 문서 = `frontend/docs/design-campaign-fee-schedule.html`, 비개발자용 조치 안내 = `frontend/docs/리뷰비칸_누락_참여불가_조치_유저플로우.html`.
- ★★ **트리거·함수를 정의·변경하는 마이그레이션은 실제 DML 을 돌려보는 회귀가드를 함께 둔다 (2026-08-19 실측 사고)**: 함수 본문은 `CREATE` 시점에 **파싱되지 않아** 마이그레이션이 `_migrations` 에 **'성공'으로 기록**되고 **실행 시점에만** 터진다 — 배포 직후 무신호. 실제로 123 이 PL/pgSQL 변수 `phone8` 과 서브쿼리 별칭 `phone8` 을 겹쳐 **42702**(ambiguous)를 냈고, 그 트리거가 `AFTER UPDATE OF bank_name, bank_account, account_holder, sub_accounts ON reviewers` 라 **리뷰어 계좌 등록·타계정 저장·구매양식 제출 중 계좌 보강·관리자 계좌 보완이 전부 실패**했다(화면엔 마스킹된 `서버 오류가 발생했습니다.` 만). 같은 무신호 계열 = 확장 함수 부재(42883)·감사 테이블/컬럼 오타(42P01·42703)·타입 불일치 — **전부 정적 검사로는 못 잡고 한 번 돌려봐야 잡힌다**. ★ **일반 DDL(컬럼 추가 등)에는 적용하지 않는다** — 그쪽은 `REQUIRED_SCHEMA` 프리플라이트와 "INSERT 컬럼 수 ≡ 자리표시자 수" 계수 가드가 이미 담당하고, 전면 적용은 마찰만 늘린다(레포 134개 마이그레이션 중 함수·트리거 정의는 3개뿐이라 이 규칙의 비용은 사실상 0). 회귀가드 `tests/reviewerAccountAuditTrigger.test.js`(최신 정의 파일을 **자동 탐색**해 선언 변수 ↔ 쿼리 별칭 충돌 0 을 고정 + `PGTEST_URL` 있으면 **진짜 PG 로 계좌 저장·타계정 저장 → 감사 2행** 실행 확인, 변이시험 검출 확인). 수정 = migration 125.

### 리뷰어 1:1문의 — 시작=리뷰내역 팝업 / 확인=내 문의함(인라인)
- **문의 시작 창구는 하나**: 리뷰 내역 카드 → 참여상품 정보 팝업(`_partInfoRenderBtns`)의 **[🎧 1:1 문의하기]**(참여중·제출완료 공통, 그 방 미확인 수 뱃지). 완료 건의 **[🖼 제출 이미지 확인·수정요청]도 같은 팝업 안**(단건일 때만 — 행 단위 작업이라 다건 그룹은 목록 줄별 미니버튼 유지). 문의 문맥 = 진행한 탭(`sheetId||tabName`), 라벨은 상품명→탭 표시명(**시트제목 미노출 원칙 유지**).
- **1:1문의 탭 = 내 문의함**(`#sectionCsInbox`, `switchTab('cs')`): 팝업 없이 인라인, **이미 문의한 방만** 표시(진행 중 / 종료된 문의 분리, 마지막 메시지·시각·미확인 숫자). 목록은 기존 무인증 phone8 스코프 `GET /api/reviewer/cs/threads` 재사용(**신규 엔드포인트 0**), 방 열기는 배열 인덱스 전달(따옴표 이스케이프 사고 차단). 빈 상태는 [리뷰 내역으로 가기 →]로 다음 행동 안내. 문의함 화면에서는 구매 캡처 보완 블록을 숨긴다.
- **캠페인 선택 팝업(피커)은 제거**(`reviewer-cs.js`) — 남겨두면 "문의 시작"과 "문의 확인"이 한 창에 다시 섞이고 진입 경로가 둘이 된다. 대화창·SSE·탭 뱃지는 종전대로 `reviewer-cs.js` 담당이며, 전송·뒤로가기 시 `window.loadCsInbox`로 목록을 갱신한다.
- 회귀가드 `tests/csEntryRework.test.js`(배선 grep + **스텁 pool로 `/review-earnings` 실제 실행**해 누적 집계 검증).
- **관리자 답장 사진 첨부**: 리뷰어와 대칭으로 관리자도 첨부 가능. `POST /api/cs/upload`(리뷰어측 `POST /api/reviewer/cs/upload`와 **동일 인프라 재사용** — guide-image Drive `[문의첨부]` 폴더 + 무인증 프록시 URL, mime/8MB/5장 제한 동일, 인증 계층만 다름: `authMiddleware`+`adminOrMaster`). 첨부 3경로(**파일선택 버튼 · Ctrl+V 붙여넣기 · 드래그앤드롭**)가 전부 `_csUploadFile` 한 함수로 수렴(사본 두면 한쪽만 제한이 풀리는 드리프트가 난다) — 붙여넣기는 이미지일 때만 가로채(텍스트 붙여넣기 기본 동작 보존), 드래그오버는 파일 드래그일 때만 오버레이 표시. 전송은 업로드 완료된 첨부만 대상(인플라이트 중 전송 차단), 실패 시 첨부 복구, 방 전환 시 이전 첨부 폐기. 회귀가드 `tests/csAdminImageUpload.test.js`.

### 관리자 닉네임 — 1:1문의 실명 노출 차단 (migration 078)
- **문제**: 답장은 `cs_messages.sender_name`에 **로그인 계정명(=관리자 실명)** 을 기록해 왔고 그 값이 리뷰어 대화창에 그대로 나갔다(박세희·박은비·master).
- **모델**: 계정 테이블에 컬럼을 붙이지 않고 **`admin_nicknames(login_name PK, nickname)`** 매핑 하나로 둔다 — 마스터(env 계정)·staff·인트라넷 SSO는 `admin_users` 행이 없어 로그인명 키만이 전 로그인 경로를 덮는다. 시드 = `박세희→만두`·`박은비→망고`(`utils/workManager.js`의 작업담당 닉네임과 같은 값, `ON CONFLICT DO NOTHING`으로 기존 값 보존).
- ★★ **표시 규칙(완화 금지)**: 리뷰어 화면 = `닉네임 || '관리자'` **fail-closed**(맵에 없는 값은 실명인지 판단할 수 없으므로 무조건 가림 — "설정 전까지 실명 노출"로 두면 막으려던 사고가 그대로 남는다) / 관리자 화면 = `닉네임 || 로그인명`(내부 책임추적 유지).
- ★ **저장은 로그인명 그대로, 치환은 읽는 시점**(`adminNickname.service.maskMessages`) — 그래서 **닉네임을 바꾸면 이미 보낸 답장의 이름까지 함께 바뀐다**(이미 노출된 이름을 되돌릴 유일한 방법). 소비처 3곳: `/api/cs/messages`(admin) · `/api/reviewer/cs/messages`(reviewer) · **`emitCsReplyToReviewer` SSE 푸시**(목록 API만 막으면 실시간 푸시로 샌다).
- **설정**: 관리자 설정탭 "내 닉네임" → `GET/POST /api/admin/my-nickname`(authMiddleware, **대상은 항상 `req.admin.name` = 본인**, body로 남의 닉네임 변경 불가). 로그인명과 같은 닉네임은 거부(실명 우회 저장 차단), 20자 상한, 빈 값 저장 = 해제(리뷰어에겐 '관리자'). `/api/admin/*` 경로라 `via:'intranet'`·`'reviewer_campaign'` 스코프 토큰은 도달 불가. 맵은 60초 캐시(저장 시 무효화), 조회 실패는 fail-soft(대화는 계속 뜬다).
- 회귀가드 `tests/adminNickname.test.js`.

### 리뷰어 직접참여 캠페인 (participation_mode) — M1 백엔드 (PRD: `frontend/docs/prd-reviewer-participation.html` v1.2)
- **모델**: `recruit_campaigns.participation_mode=true` 공고만 신규 경로. **레거시(false)는 현행(카톡 신청·current_slots·시트 행추가) 완전 유지**(분기). migration 045: 시간창(`window_start/end` TIME·KST, `close_buffer_min` 10, `hold_ttl_min` 15)·`daily_limit`·`recruit_total`(0=무제한)·`thumbnail_url`·`landing_url`·`work_detail`(JSONB 스냅샷, 신청 후 공개) + `campaign_applications` 홀드 라이프사이클(`applied→submitted/expired/cancelled`, `phone8` 서버파생, `expires_at`, `hold_token`, `order_submission_id`/`late_order_id`는 **UUID**) + 구 UNIQUE DROP·레거시 보호 부분유니크(`uq_campaign_apps_legacy` confirmed 전용)·`uq_campaign_apps_active_hold`(applied/submitted).
- **상태엔진**(`campaignState.service.js`, 무저장 계산): legacy/preopen/open/cutoff/daily_done/soft_full + **closed만 영속**(제출확정 도달 시 status 저장 — 비단조 플립 방지). **자율주문 = 시간창 양쪽 미설정 → 종일 open**(`allDay` 신호, preopen·cutoff·시간 daily_done 없음 — 일일한도·총모집·closed는 동일; 홀드 만료는 TTL만). 한쪽만 설정/역전은 여전히 `window_invalid` 차단. 활성화 게이트(서버 `_participationActivationErrors`·프론트 자동점검 동일 규칙): gid·daily_limit 필수, 시간창은 "양쪽 설정(start<end)" 또는 "양쪽 비움(자율주문)". **시작일(migration 062 `recruit_campaigns.start_date`, 발행폼 프리필=작업오더 `work_orders.start_date`)**: KST 오늘<시작일이면 **날짜 preopen**(opensAt=시작일의 window_start/자율주문은 자정, 카드 D-일수 카운트다운 `_fmtOpenLabel`) — 미리 게시해도 시작일까지 "오픈 예정". NULL=즉시 오픈(기존 불변). ⚠ 시작일/시간창은 공고 상태를 참조할 뿐, **시트의 구매일자 컬럼은 오픈 판정에 미참조**(행별 `review_index.start_date`는 원문 텍스트 저장용). PUT 편집은 `''`=제거·null=유지 CASE 센티널(시간창 포함 — 자율주문 전환 가능, `auto_order=true`도 메인 라우트에서 강제 비움). `dailyQuota = min(daily_limit, recruit_total−전일까지 누적확정)` **KST 일시작 고정**(이중차감 금지). 유효홀드 = `applied AND expires_at>now` **시각 기준**(스윕은 정리용 — 크론 죽어도 카운터 무오염). 회귀가드 `tests/campaignState.test.js`.
- **시트 일정 자동 인식(063 · `campaignSchedule.service.js`)**: 연결 탭의 **날짜 컬럼(구매일자·시작일·주문일·배정일·진행일)** 에서 **시작일=최소 날짜 · 마감일=최대 날짜 · 그날 정원=그 날짜의 행 수 · 총건수=총 행 수**를 파생해 발행폼 값(start_date/daily_limit/recruit_total)보다 **우선** 적용(날짜 2종 이상 + 값 있는 칸의 60% 이상 파싱될 때만 = opt-in 아님, 자동). **소스는 RAW 미러(`raw_sheet_rows`, 5분 cron) — `review_index`는 이름 없는 행을 버려(`columnResolver` `if (!name) return null`) 미래의 빈 슬롯이 없어 못 씀.** 라이브 시트 읽기 0(쿼터 무영향), 탭당 1분 캐시. 헤더 탐지 후 **날짜 컬럼 한 칸만**(`cells->>idx`) 읽어 넓은 시트에서도 전송량 일정.
  - **날짜 파서**(`utils/koreanDate.js`): 실측상 한 컬럼에 `7 / 23 (목)`(연도 없음)와 `26.7.28(화)`(2자리 연도)가 **혼재** → 토큰 파싱과 연도 추론을 분리. 연도 없는 값은 **인접한 연도 확정 값을 앵커**로 추론(±6개월 넘으면 연말연초 경계 보정). `YYYY-MM-DD`·`YYYY.M.D`·`2026년 7월 24일`·시트 일련번호(40000~60000)도 지원. 판정 실패는 전부 null(=미적용 폴백).
  - **이월**: 그날 정원 = `min(오늘까지 누적 계획, 총건수) − 전일까지 확정` → 못 채운 물량이 사라지지 않고 다음 진행일에 합산. **휴무일(그 날짜 행 0개)은 열지 않되**(`stateReason:'rest_day'` + `nextWorkDate` + 다음 진행일 카운트다운 `opensAt`) 이월분은 보존(누적 계획이 휴무일엔 증가하지 않으므로 자동).
  - **자동 재개(핵심)**: 마감일 경과는 `closed`+`stateReason:'schedule_ended'`로 **계산만 하고 status에 영속하지 않는다**. 그래서 시트에 날짜를 추가하면 최대 5분 뒤 마감일이 연장되고 종료된 캠페인이 자동으로 다시 열린다. 이를 위해 `maybePersistClosed`·스윕 백스톱 ③이 **시트 일정 캠페인을 영속 대상에서 제외**(`_isScheduleDriven`). 관리자 수동 마감(`status!=='active'`)은 여전히 최우선 존중(자동 재개 안 함).
  - **일관성**: 목록·상세·**참여(apply) 게이트**·옵션 뷰가 전부 같은 일정을 본다(카드는 열렸는데 참여는 거부되는 불일치 차단). apply는 잠금 커넥션(`client`)으로 캐시 조회 — 커넥션 고갈 교착 회피. 회귀가드 `tests/campaignSchedule.test.js`(파서·요약·상태 케이스, 사용자 시나리오 "마감 후 새 날짜 추가 → 재개" 포함).
- **작업내용 렌더러 공용화(`frontend/js/campaign-workdetail.js`)**: 리뷰어가 참여 후 보는 작업내용 4카드(상품·옵션·결제금액 / 유입가이드(+상품페이지 열기) / 리뷰 가이드 / 특이사항)를 **한 모듈**이 그린다. **실제 리뷰어 페이지(`campaign.html`)와 관리자 미리보기가 같은 코드를 쓰므로 "미리보기 ≠ 실제 화면"이 구조적으로 불가능**(과거엔 관리자 모달이 별도 모형 카드를 그려 실물과 계속 어긋났다). 색상은 호스트 CSS 변수 미의존(리터럴 고정)이라 admin.html에 얹혀도 동일 렌더. 정규화 로직(`fmtProduct` 금액 합치기·`pickReviewOnly` [리뷰등록 가이드] 섹션 추출·`extractGuideImages` 첨부이미지 병합/중복제거, guide-image 호스트는 신뢰 베이스로 재구성)이 전부 여기로 이관 — campaign.html의 인라인 사본은 제거됐다. 상품/리뷰/특이사항은 escape, 유입가이드만 서버 sanitize본 HTML 그대로. 소비처: campaign.html·admin.html·index.html·recruit.html. 회귀가드 `tests/campWorkDetailShared.test.js`(렌더 10 + 배선 4 = 사본 부활·모형 회귀 차단).
  - **관리자 모집공고 모달 미리보기**(`admin.html` `#rf_preview_area` + `index-recruit.js` `_renderPreview`) = 모형 카드가 아니라 **참여 후 실제 화면**(홀드 타이머 바 + 작업내용 4카드 + 구매양식 자리 안내). 작업내용 필드(`rf_wd_*`·랜딩·TTL) 입력 시 실시간 재렌더.
  - **리뷰어앱 인라인 공고수정 모달**(`campaign-cards.js` `openAdminEdit`): 썸네일은 **URL 입력만**(파일 업로드 창구 제거 — 관리자 대시보드의 직접 업로드는 유지), 모달 세로 확대(88vh→94vh·480→520px), 썸네일 아래 **연결된 작업내용 읽기전용 미리보기**(`_caeRenderWorkDetail`, 공용 렌더러) = 관리자가 유입가이드 설정 상태를 육안 확인.
    - **`_scopedEditorView`에 `work_detail` 포함**(읽기 전용 프리필). 보상통제 = `_scopedCampaignEdit`의 UPDATE SET절에 여전히 `work_detail` 없음(**쓰기 표면 불변**) + 이미 홀드 보유 리뷰어에게 공개되는 sanitize된 값. `chat_url`·`notes`·`linked_*`·`source_work_order_id` 등 연결·구조 필드는 종전대로 미노출(회귀가드가 주석 제외 후 판정). 미표시 사유는 **"필드 자체가 없음(권한/구버전)" vs "비어있음(미등록)"** 을 구분해 안내.
    - 모달 우상단 **[👁 리뷰어 화면]은 진짜 admin 토큰(`sessionStorage/localStorage.admin_token`)일 때만 `preview=1#tok=`** 미리보기로 연결 → **마감·오픈전 공고도 참여 전→작업가이드→제출완료 전 과정 확인**(일반 링크로 가면 잠금 화면만 보임). 스코프 토큰(`rapp_camp_edit_token`)은 preview 엔드포인트가 adminOrMaster라 403 막다른 길이므로 **제외하고 기존 일반 링크 유지**(무토큰 동작 불변). 토큰은 프래그먼트 전달(서버 로그·Referer 미유출).
- **홀드 파이프라인**(`campaignHold.service.js`, 레드-블루-심판 산출): 잠금 계층 고정 = **recruit_campaigns 행 FOR UPDATE → 신청 행**(apply·주문확정·수동확정 공통 — write-skew·교착 동시 제거). apply는 잠금 후 재집계→게이트(등록 리뷰어 검증, **당일 재신청 0회**(상태 무관 이력), 활성홀드 상한 2, 당일 신청총량, holdToken 발급). 제출확정은 `POST /api/submit/order`의 `campaignHold` 문맥 → `createOrderLedgerEntry` **단일 tx + SAVEPOINT 격리**(홀드확정 실패해도 **주문 손실 0**) → 소유권 3중검증(applied·grace 30s·phone8·연결탭 gid우선) 통과 시만 submitted. 만료 후 도착 = `late_order_id` 표기 → **관제 수동확정(`POST /api/campaign/admin/:id/confirm`)이 유일 구제**(유예 정책 없음). 스윕 cron 매분(`withJobLock('campaign_hold_sweep')`, SKIP LOCKED). 주문취소(diag order-cancel)는 확정 홀드를 cancelled로 반환(closed 자동 재오픈 금지). 회귀가드 `tests/campaignHoldGuards.test.js`.
- **보안/부하**: `GET /api/campaign/list·/:id` 공개 화이트리스트(참여형은 chat_url·notes·총원계열 미반환; admin JWT는 전체), `/:id/applications` count만, campaign admin 라우트 adminOrMaster. 작업내용은 `GET /:id/work-detail?phone8&holdToken`(이중 열쇠, sanitize-html 정화) 게이트 뒤에만. `app.set('trust proxy',1)` + 전역 리미터 skip은 `baseUrl+path` 판정(마운트 스트리핑 함정 — 구 `/api/index/` skip은 dead code였음) + list 5초 캐시 + apply/detail 리미터 phone8 키. 참여형 활성화 게이트: gid·시간창·daily_limit 없이 active 불가(status·COALESCE 편집 양 라우트).
- **M2 완료**: ① apply에 **내정보 완비 게이트**(`profileMissing` #272 재사용, 홀드 생성 전 403 `profile_missing`+missing 목록 = 자리 미점유 — 15분 홀드 내 제출차단 사고 예방) ② 공고 create/update에 참여형 11필드 편집(+work_detail 저장 시 sanitize 1차, undefined=유지) ③ my-status에 유효홀드 `stage:'applied'` 병합. ④ 프론트: `js/campaign-cards.js`(공용 카드 — **모바일 2열 그리드**(`pcards-grid`, 호출부가 partHtml을 이 클래스로 감쌈)·**정사각 썸네일 전체노출**(aspect-ratio 1/1·object-fit contain, 비잘림)·채널/배송 **우상단 배지**·바로참여는 시간 오른쪽·참여인원 **게이지**(pgauge, 시안 A)·**모집중 시간창은 썸네일 중앙 "오늘 구매마감까지" 카운트다운**(cutoffAt, 라이브 톤)·오픈전 회색 "오픈까지"(opensAt)·cutoff/daily_done/soft_full/closed 상태별 리본·푸터, 서버시간 오프셋 보정, 카드 탭→`campaign.html?id=`; **admin_token 보유 관리자 또는 리뷰어앱 공고수정 허용자는 카드 탭=인라인 수정 모달**(`openAdminEdit` — GET /:id admin 전체조회 프리필→기존 PUT /admin/:id COALESCE 부분수정 = 대시보드와 동일 원장 즉시 동기화·서버 무변경, 서버가 미전송을 0으로 강제하는 review_fee/max_slots/sort_order는 로드값 재전송, 썸네일=guide-image imageUrl·파일 재사용, 공개뷰 응답(sort_order 부재)=만료토큰으로 판정해 차단, 리뷰어 화면은 모달 내 👁 링크; 무토큰 동작 불변, 회귀가드 `tests/campCardAdminEdit.test.js`)), `campaign.html`(참여 전/내정보안내/참여 후(홀드타이머+작업내용+**동일오리진 iframe 인라인 제출**)/만료/완료, `camp_hold_<id>` localStorage 복원, postMessage 오리진 검증, 10s 폴링+visibilitychange), `search-app.js` **embed=1 모드**(전부 가산적 — 크롬 숨김·높이 보고·입력값 sessionStorage 복원·세션부재 시 부모 위임·제출 body에 campaignHold 문맥·완료 시 order-submitted postMessage), 관리자 공고 모달 **⚡참여형 스위치**(시간창·일일한도·총모집·고급(TTL/버퍼)·랜딩·작업내용 + `_parsePurchaseTime` 프리필 + 게시 전 자동점검=서버 게이트 동일 3항목). 회귀가드 `tests/campaignM2Guards.test.js`·`tests/campaignM2FrontGuards.test.js`.
- **M3 완료**: ① **작업오더→발행 자동 프리필**(`woCreateCampaign` prefill 확장 — daily_count/recruit_count/purchase_time(파서로 시간창 변환)/작업내용 스냅샷/링크유입 첫 URL→landing_url; **상품명·결제금액 기본 프리필**(`_woFirstProductInfo` — JSON·텍스트 양형 파싱) + 모달 오픈 시 상품정보 자동수집 1회(`fetchProductInfo({auto:true})`) — **성공 항목만 덮어쓰고**(비클로버) 실패(쿠팡 봇차단 등) 시 작업오더 값 유지·미리보기 미숨김; **유입가이드 원본 HTML(이미지 포함) 보존** = textarea엔 `_htmlToPlainPreview` 미리보기만, 미수정 시 원본 전송(`dataset.rawHtml` 라이프사이클), 수정하면 평문 escape 모드 전환) ② **관제 패널**(참여형 카드 [📡 관제] 버튼 → `openCampControl` — 오늘 진행중/제출/만료 KST 집계(유효홀드 시각 기준), 기구매(late_order_id) 배지, **수동확정 버튼**(`POST /api/campaign/admin/:id/confirm`, submitted 제외)) ③ **썸네일 직접 업로드**(`uploadCampThumb` — guide-image Drive+무인증 프록시 인프라 재사용, **절대 프록시 URL** 저장(pages↔railway 교차 오리진), 5MB 상한, 미입력 시 자동추출 폴백) + **쿠팡 CDN 이미지주소 붙여넣기 수집**(`fetchCampThumbFromUrl` → guide-image `imageUrl` 분기, `utils/thumbFetch.js` — 쿠팡 상품 HTML은 Akamai 봇차단이라 서버 스크래핑 전멸(데이터센터 IP 즉시 403, Firecrawl stealth 프록시도 403 실측 2026-07)이지만 **이미지 CDN(coupangcdn.com)은 미차단**: 관리자가 브라우저에서 "이미지 주소 복사"한 URL만 서버가 받아 Drive 재저장=핫링크 아님. SSRF 가드 = https 전용·호스트 허용목록(기본 coupangcdn.com, env `THUMB_URL_HOST_ALLOW` csv 확장)·리다이렉트 거부·image/* 검증·5MB·10s, 회귀가드 `tests/campThumbUrl.test.js`). 회귀가드 `tests/campaignM3Guards.test.js`.

### ★★ 제출 시 옵션 칸 지워짐 — 빈 값은 "안 씀"이 아니라 "지움" (7/31 포토리뷰 건)
- **증상**: 리뷰어가 구매양식을 제출하면 그 행의 **옵션 컬럼 값('포토리뷰')이 사라진다**. 미제출 행은 그대로.
- **메커니즘**: `mapOrderToSheetRow`는 옵션 칸에 `''`를 반환하고 `buildBatchUpdateData`는 **`null`만** 걸러낸다 → 빈 옵션이 **그 칸을 지우는 쓰기**로 나간다. 시트 옵션 피커는 `reviewer-options`의 `matchedZero`(이름 매칭 0건)일 때만 렌더되므로, **로스터에 이름이 이미 있는 정상 케이스에서 `_selectedOptKey`가 null** → 전부 지워졌다.
- **수정**: `createOrderLedgerEntry`가 행 배정 직후 **선택값이 비어 있을 때만** 배정 행의 기존 옵션값을 되쓴다(`existingOptionKeyAt`). `orderData`는 호출부가 큐 페이로드로 그대로 넘기는 **같은 객체**라 reconcile 재기록까지 일치. 값이 있으면 **절대 건드리지 않는다**(무회귀).
- ⚠ **공유 매퍼는 고치지 않는다** — `order_cancel`의 칸 비우기와 Track B write-back의 컬럼 disjoint 마스크가 `''` 반환에 의존한다. 그래서 **호출부에서 되쓴다**(외부모집 수동제출이 이미 쓰던 방식과 동일).
- ★ `optionColIndexes`/`existingOptionKeyAt`는 **orderLedger가 단일 출처** — manualOrder의 사본 제거(사본을 두면 "리뷰어 제출은 보존, 수동제출은 삭제" 드리프트). 회귀가드 `tests/optionColumnPreserve.test.js`(지우는 쓰기 메커니즘 자체를 고정 + 보존 시뮬레이션).

### ★★ 옵션 칸은 두 종류다 — 시스템은 **빈 칸에만** 쓴다 (C′ blank-only, 8/3 상품명 오기입 건)
- **사고**: 리뷰어가 구매양식을 제출하자 시트 `리뷰옵션` 칸의 `텍스트`(관리자 작업지시)가 **상품명으로 덮였다**. 원인 두 겹 = ① 작업오더→공고 발행 프리필이 **상품명을 옵션명(`opt_key`)으로 승격**(옵션 없는 상품 / 다상품 접두) ② 제출 시 매퍼가 헤더에 '옵션'만 있으면 **값이 있어도 덮어썼다**.
- ★★ **모델**: 시트 '옵션' 칸은 ㉮ **상품옵션**(리뷰어 선택값 기입 대상) / ㉯ **작업옵션=리뷰형태**(`텍스트`·`포토리뷰` — 관리자가 모집 전 적어두는 **작업지시**, 시스템이 절대 쓰면 안 됨)의 두 종류인데 **헤더 문자열로는 구분되지 않는다**. 그래서 구분하지 말고 **"값이 있으면 안 쓴다"**(blank-only)로 해결한다.
- **적용 지점 = 시트 쓰기 직전**(`syncQueue` 단건·배치 양쪽). `orderLedger.filterOptionWritesBlankOnly`가 `buildBatchUpdateData` 결과에서 **옵션 칸 range만** 골라 라이브 셀이 비어있지 않으면 그 쓰기를 버린다. **확인 불가(probe 없음)=쓰지 않음**(fail-closed). 킬스위치 `ORDER_OPTION_BLANK_ONLY=0`.
- ★★ **원장에 시트값을 역주입하지 말 것**(옛 방식 폐기): `selected_opt_key`에 시트값을 되쓰면 ① 큐가 실행 시 `order_submissions`를 **재조회**하므로(`_osRowToOrderData`) 그 값이 시트로 나가고 ② **재배정(reconcile)이 옛 행의 작업지시값을 새 행으로 전이**시키며 ③ CS 표시·Track B `identityKey`·홀드 옵션검증이 그것을 "리뷰어 선택"으로 오독한다. **원장은 리뷰어가 고른 값(없으면 빈 값)이 진실.** manualOrder의 사본도 같은 이유로 제거.
- ★★ **옵션 칸 판정은 `optionWriteColumns`(매퍼 파생)로** — `optionColIndexes`(헤더에 '옵션' 포함)를 쓰면 **`옵션금액`(=결제금액 칸)·`비고(옵션확인)`(=비고 칸)을 오분류**해 금액·비고 쓰기가 조용히 삭제된다(막으려던 것보다 큰 손실). 센티널 주입으로 "매퍼가 실제로 optParts를 넣는 열"만 뽑는다. 센티널에 **공백·NUL 금지**(매퍼가 `trim`하고, NUL은 git이 바이너리 취급 — 둘 다 실측으로 밟음).
- 라이브 판정 비용 0: **기존 다중컬럼 가드 사각형에 옵션 열을 합류**시켜 읽는다(콜 순증 0, RAW 미러 stale 판정 문제도 함께 해소). probe 오프셋은 가드와 **같은 minCol** 기준(어긋나면 가드 오판).
- **취소(order_cancel)도 옵션 칸은 비우지 않는다** — 작업지시일 수 있다. **경고는 `logAbnormal(warn)`만**(`reviewer_event_logs` 금지 — 선기입 탭은 모든 제출이 조건 참이라 알림 도배 = 늑대소년).
- **발행 단계 방어**: 상품명이 옵션명이 되던 프리필 교정(`_woOptionRows`·`parseProductLinesToRows`) + 게시 전 **경고 전용** 자동점검(`GET /api/tab/option-column-audit`, RAW 미러만 조회·시트 재읽기 0). ★ **하드블록 금지** — ㉯ 탭은 정상이어도 옵션명이 절대 일치하지 않아 멀쩡한 공고 발행이 전부 막힌다. `tab_configs.option_columns`로 ㉮ 지정된 칸에서만 값 대조.
- 잔여(문서화): ㉯ 선기입 탭에서는 **리뷰어 선택 옵션이 시트에 안 나타난다**(원장·관제·CSV엔 남고 warn으로 노출) / 취소해도 옵션 칸은 잔류 / `order_update`(관리자 명시 편집)는 여전히 옵션 칸에 쓸 수 있다(의도적 교정 수단, `cur===''||cur===wantOld` 가드로 클로버 불가).
- 회귀가드 `tests/optionColumnPreserve.test.js`(역주입 부재 + 필터 실행 + 오분류 차단 + 배선)·`tests/optionColumnAudit.test.js`.
- **리뷰어에게 리뷰 형태 표시(D)**: 그 행의 작업 옵션을 리뷰 내역 → **참여상품 정보 팝업 맨 위**(상품정보보다 먼저 — "어떻게 써야 하는가"가 우선)에 파란 블록으로 표시. 값은 **기존 `GET /api/review-edit/participation-brief`에 `workOptions[{label,value}]`로 동봉**(신규 엔드포인트 0 — 그 라우트가 이미 행 소유권 강한-키 게이트를 통과시킨다). 소스는 `review_index.row_json`(헤더명→값 맵) **읽기만**, 실패해도 나머지 brief는 나간다. ★ 라벨은 **시트 헤더명 그대로**(탭마다 '리뷰옵션'·'작업옵션'이라 우리가 고쳐 부르면 어긋난다). ★ 어떤 칸을 보여줄지는 **`optionWriteColumns` 단일 출처** — 표시 규칙을 따로 만들면 "쓰는 칸"과 "보여주는 칸"이 갈리고, `옵션금액`(결제금액)·`비고(옵션확인)`(내부메모)이 리뷰어 화면으로 샌다. 공고 미연결 탭도 작업 옵션만은 반환(공고 필드는 여전히 미노출). 한계: 헤더에 '상품'이 들어간 옵션열은 매퍼가 관리자 보호열로 취급해 표시에서도 빠진다. 회귀가드 `tests/reviewFormatDisplay.test.js`.

### ★★ jsonb 배열 인덱싱 — `cells->>$n::int` 캐스트 필수 (063이 무음으로 꺼져 있던 원인)
- **실측 사고**: `SELECT cells->>$3`(파라미터=텍스트)는 PostgreSQL이 `jsonb ->> text`(**객체 키 조회**)로 해석한다. `raw_sheet_rows.cells`는 **JSONB 배열**이라 객체 키 조회는 **에러가 아니라 전 행 NULL**을 돌려준다 → 날짜 컬럼이 통째로 빈 값 → `summarizeDates`가 null → **시트 일정 자동 인식(063)이 배포 이래 한 번도 작동하지 않았다.** 모든 캠페인이 조용히 발행폼 `daily_limit`/`recruit_total` 경로로만 돌아갔다.
- **수정**: `cells->>$3::int`(`::`가 `->>`보다 강하게 결합 → `jsonb ->> integer` = 배열 원소). 로컬 PG16으로 재현·검증.
- ★ **스텁 DB 테스트로는 절대 못 잡는다**(스텁은 SQL을 해석하지 않음). 회귀가드 `tests/jsonbCellsIndex.test.js` = 정적(주석 제외 후 모든 `cells->>$n`에 `::int`) + `PGTEST_URL` 있으면 **진짜 PG로 NULL 재현/수정 확인**.
- **되살아나는 배포라 킬스위치** `CAMPAIGN_SHEET_SCHEDULE`(`deriveSchedules`가 빈 맵 반환 = 전부 미적용). ⚠ **2026-08-07 사용자 확정으로 기본값이 뒤집혔다 — 아래 "모집 정원 기준 = 시스템표" 참조**(이 절의 서술은 스위치를 `1`로 켰을 때의 동작이다).

### ★★ 모집 정원 기준 = 시스템표 (사용자 확정 2026-08-07 — "기준을 구글시트에서 시스템표로 변경했어")
- **정원·마감일을 정하는 것은 시스템 값이다**: 그날 정원 = **일 모집인원 + 날짜별 조절(095) + 이월(066/098)**, 총량 = **차수 합계(`recruit_total`)**. 시트 구매일자 컬럼(063)은 **더 이상 정원·마감일을 정하지 않는다**. 시트는 계속 읽지만(주문 행배정·검색·리뷰·RAW 미러) **정원은 안 정한다**.
- **적용점은 스위치 기본값 하나** — `campaignSchedule.service.SHEET_SCHEDULE_ENABLED = process.env.CAMPAIGN_SHEET_SCHEDULE === '1'`(종전 `!== '0'` = 기본 켬에서 뒤집음). `deriveSchedules` 가 **빈 맵**을 돌려주므로 `computeCampaignState`·`getPlanOverview`·apply 게이트가 **전부 같은 시스템표 경로**를 탄다(counts 깔때기 규율 그대로 — 화면마다 기준이 갈릴 수 없다). ★ **되돌리기 = Railway `CAMPAIGN_SHEET_SCHEDULE=1`**(코드 변경 0).
- ★★ **관제 진단은 "적용 중"이라고 말하지 않는다** — `describeTabDates` 가 날짜를 **읽어 보여주되**(시트 현황 대조는 계속 필요) `applied=false` + `reason:'system_basis'` 로 내려준다. 엔진은 무시하는데 화면만 "시트 일정 적용 중"인 상태가 **가장 나쁜 거짓 표시**다. ★ 그 사유는 **경고(노란 박스)로 칠하지 않는다** — 정상 기준이라 전 공고가 상시 경고가 되면 진짜 불일치 신호(확정 수 ≠ 시트 행 수)가 묻힌다.
- **화면은 이미 분기를 갖고 있어 프론트 수정이 거의 없다**: [📅 인원] 모달의 `기본 시트 구매일자 기준 → 기본 일건수 N명`, `총량 기준 시트 행 수 → 차수 합계`, 시트 안내문·`baseFor`(시트 계획 기준선)가 모두 `scheduleDriven` 한 값으로 갈린다. 그래서 시트 계획이 배분 목표보다 적어 생기던 **`부족(파랑)` 상태도 자연히 사라진다**(총량 = 차수 합계라 구간이 정확히 맞는다).
- ⚠ **운영 함의(문서화)**: ① 시트 행 수(예 800)와 `recruit_total` 이 다르면 **총량이 `recruit_total` 로 바뀐다** — 차수 원장으로 맞춰 둘 것(차수 있는 공고는 서버가 `recruit_total` 직접 수정을 무시한다). ② **휴무일(rest_day)·시트 마감일(schedule_ended) 판정이 사라진다** — 총량이 찰 때까지 매일 열린다. 특정 날짜를 닫으려면 [📅 인원]에서 **그 날 0명**으로 조절한다. ③ 시트에 날짜를 추가해도 **자동 재개는 없다**(그건 시트 일정 기능이었다).
- 회귀가드 `tests/campaignQuotaBasis.test.js`(기본값·빈 맵·`system_basis` 를 **자식 프로세스로 켬/끔 두 경우 실제 실행**해 대조 + 화면 배선, 변이시험 검출 확인). ⚠ 시트 일정 **엔진 자체**를 검증하는 `campaignFullState`·`sheetlessDailyPlan` 은 파일 맨 위에서 `CAMPAIGN_SHEET_SCHEDULE='1'` 을 세운다(env 는 require 시점에 읽힌다).
- **교훈**: 조용히 빈 값을 돌려주는 SQL은 기능을 무음으로 끈다. 관측(관제 시트 대조)이 없었으면 계속 몰랐다.

### 총원 충족 판정 우선 + 관제 시트 대조 (7/31 "다 모집했는데 내일 다시 오픈" 건)
- **증상**: 100명 캠페인이 오늘 20/20을 채우자 카드가 "8/1 다시 오픈"으로 표시. 원인 2개.
- ★★ **판정 순서 고정**: `computeCampaignState`에서 **총원 충족(soft_full)을 일일 마감(daily_done)보다 먼저** 본다. 반대면 총원이 차는 순간 `dailyQuota=0` → `todayCount(0) >= quota(0)`이 먼저 참이 되어 **다 모집한 캠페인이 매일 "내일 다시 오픈"** 으로 보였다. **apply 게이트는 두 상태 모두 차단이라 참여 동작은 순서와 무관**(바뀌는 건 표시 문구뿐) — 그래서 안전하게 순서만 바꿨다. 회귀가드 `tests/campaignFullState.test.js` + 기존 `campaignState.test.js`의 옛 기대값(daily_done) 정정.
- **두 번째 원인 = 확정 99 ≠ 시트 100행**(홀드 없이 결제한 지각 건·직원 수기 입력 등). 총원 미달로 판정되어 남은 1명분이 열린 것 — 데이터 불일치이지 상태 버그가 아니다.
- ★ **하루 완결 캠페인은 시트 일정이 구조적으로 안 잡힌다**(날짜 1종 < `MIN_DISTINCT_DATES`=2) → 시스템이 마감일을 모른다. **자동 종료로 바꾸지 않았다** — 날짜가 일부 행만 채워진 준비 중 시트를 조기 종료시키는 위험이 더 크다(닫는 판단은 사람이). 대신 **관제에서 사유를 설명**한다.
- **관제 시트 대조**(`GET /admin/:id/applications` → `sheetInfo`): ① 연결 탭 **로스터 행 수(`review_index`) vs 확정 수**와 차이 ② **시트 일정 적용 여부와 미적용 사유**(`describeTabDates` — `single_date`/`no_mirror`/`no_date_column`/`low_parse_ratio`). ★ **읽기 전용·fail-soft·시트 재읽기 0**(RAW 미러만 조회), 상태 계산에 일절 미참조. `describeTabDates`는 `deriveSchedules`와 **일부러 분리** — 저쪽은 게이트 통과분만 반환해 "왜 미적용인지"를 설명할 수 없다.

### ★★ 관제 "확정으로 안 잡힌 작업표 줄" — 대조는 세 키, 종류별로 할 일이 다르다 (2026-08-19 위프 800건 실측)
- **사고**: 관제 대조가 **명의 `phone8` 정확일치 하나**로만 짝을 지어, 505줄·확정 309 인 작업에서 **196건**이 "확정 안 잡힘"으로 떴다. 실데이터로 갈라 보니 **조치 대상은 16건뿐**이었다(만료·취소 10 · 지각 6). 나머지는 전부 오표시였다.
- ★★ **대조 키 3종(완화 금지)**: ㉮ 명의 `phone8` ㉯ **소유자 `owner_phone8`** — 타계정 참여는 **작업표 연락처 칸에 소유자 번호**가 적히는 게 정상이다(홀드 명의 ≠ 구매양식 연락처) ㉰ ★ **주문 링크**(그 줄의 `campaign_participants.order_submission_id` = 확정 홀드의 `order_submission_id`). ㉰ 가 결정적이다 — **연락처 오타(끝 4자리 한 글자)·수취인 번호 기입은 번호로는 영원히 못 짝짓는데** 주문 id 는 이름·연락처·줄 번호가 바뀌어도 불변이다(리뷰어 홈 dedup 과 같은 규율). ★ **소프트삭제된 줄의 링크는 근거가 아니다.**
- ★★ **같은 오류가 양쪽에서 두 번 세어졌다**: 번호가 어긋난 건은 목록에선 "확정 안 잡힘", 반대편에선 "확정인데 작업표 줄이 없음"으로 **동시에** 잡혔다(실측 18건 중 14건이 주문 링크로 즉시 찾아졌다). 한쪽만 고치면 반대편이 남는다.
- ★★ **남은 미확정을 한 덩어리로 보여주지 않는다**: 홀드 이력이 **아예 없는 줄**은 공고를 거치지 않은 제출(외부모집·직접 제출)이라 **만료·취소 목록에 애초에 없다** — 종전 화면은 그것까지 "[수동확정]하세요"로 안내해 **찾을 수 없는 건을 찾게 만들었다**(196건 중 163건). `unmatchedCounts{total,hold,orderOnly,neither}` 로 갈라 세고, 목록도 `ORDER BY has_hold DESC, has_order ASC` 로 **조치 대상을 앞에** 낸다(목록은 30건 상한이라 뒤로 밀리면 안 보인다).
- ★ **건수는 서버가 센다** — 목록이 30건 상한이라 화면에서 세면 항상 30에서 멈춘다. **못 받으면(구버전 백엔드) 요약 줄을 아예 안 그린다**(0으로 꾸미지 않는다). ★ 머리줄 "차이 N건"은 **줄 수 − 확정 수**라는 산수라 그대로 두되, 대조로 풀린 만큼을 **문장으로 밝힌다**(두 숫자가 말없이 다르면 "왜 196인데 179만 나오나"가 된다).
- ★ 여전히 **관측 전용**이다 — 여기서 무엇이 나오든 캠페인 상태·정원을 바꾸지 않는다. 쿼리는 `rosterRows > confirmed` 일 때만 1회(평상시 0).
- ⚠ **곁다리로 드러난 잔존 오염**: 그 탭에 **한 주문을 여러 줄이 가리키는 링크 오염 2묶음 5줄**이 남아 있다(8/19 "링크는 표 주문번호가 정한다" 수정 **이전에 박힌 값** — 그 수정은 앞으로 채울 때의 규칙이라 기존 값은 안 고친다). 중복 줄 감시망(표 주문번호+명의)에는 **0건**으로 안 잡힌다(줄이 는 게 아니라 링크만 틀렸다).
- 회귀가드 `tests/campaignUnmatchedKeysPg.test.js`(**라우트에 박힌 SQL 을 꺼내 진짜 PG16 으로 실행** — 세 키·삭제 줄 배제·window FILTER 집계·정렬, **변이시험 7종 검출 확인**) + `campaignFullState`(배선·화면 문구). ⚠ 이 변경으로 `sheetVocabulary` 의 `_campUnmatchedRows` 호출 패턴을 함께 갱신했다(검사 의미 불변).

### 일 모집한도 미달분 자동 이월 (migration 066)
- **배경**: 일건수 20인데 15명만 모이면 관리자가 다음날 공고수정으로 25로 올리고, 25가 차면 다시 20으로 되돌리는 일을 손으로 했다. **시트 일정이 인식되는 캠페인은 063이 이미 이월**하므로, 이 기능은 **`daily_limit` 경로(시트 일정 없는 캠페인)** 전용이다.
- **계산**(`dailyQuota(c, submittedBeforeToday, carry)`): 기준일 = `max(캠페인 start_date, 이월 기준선)` → `그날 정원 = min(daily_limit × 경과일수, daily_limit × CAP) − 기준일 이후 누적확정`. 어제 5 부족 → 오늘 25, 오늘 25 다 차면 내일 20(닫힌 형태로 관리자 수작업과 동일).
- ★★ **완화 금지 불변식**: ① **절대 축소 없음** — 계산값이 `daily_limit` 미만이어도 `daily_limit`을 쓴다(이월은 더 여는 기능이지 조이는 기능이 아니다 = 기존 대비 회귀 금지). ② **총원 초과 불가** — 마지막 `min(q, recruit_total − 전일까지확정)` clamp 유지. ③ **소급 금지** — `app_settings.campaign_carry_start`(066이 배포 시각 KST 날짜로 1회 시드, `ON CONFLICT DO NOTHING`) 이전은 세지 않는다. 없으면 몇 주째 진행 중인 캠페인의 과거 미달분이 배포 당일 한꺼번에 열린다(062 캡처 컷오프와 같은 장치). ④ **기준선을 모르면 이월 안 함**(조회 실패·미설정 = 종전 동작 — 모르면 안 늘린다).
- **상한** `CAMPAIGN_DAILY_CARRY_CAP`(기본 2배): 오래 미달한 캠페인이 어느 날 수십 건을 한꺼번에 여는 버스트 차단. 킬스위치 `CAMPAIGN_DAILY_CARRY=0`.
- **전달 경로**: 이월 데이터는 **`fetchCampaignCounts` 반환값(`counts.carry`)에 실어** 보낸다 — `computeCampaignState` 호출부가 11곳이라 별도 인자로 두면 목록/상세/apply가 서로 다른 정원을 보게 된다(“카드는 열렸는데 참여는 거부”). 쿼리는 **FILTER 1개 추가**(기준선 이후~오늘 이전 확정), 캠페인별 시작일은 JS에서 계산 — 캠페인은 자기 시작일 전에 확정이 있을 수 없어 전역 기준선 하나로 세도 결과가 같다. 기준선은 5분 프로세스 캐시.
- **관리자 표시**: 상태 payload `carryAdded` + 관리자 카드 게이지에 **`+N 이월` 칩** — 없으면 “일건수를 20으로 저장했는데 25로 뜬다”가 버그로 오해된다. 회귀가드 `tests/campaignDailyCarry.test.js`.

### ★★ 날짜별 모집인원 조절 + 차수(물량 추가) — 조절 = 계획 변경 (migration 095)
- **배경(실사고)**: 업체 요청 "오늘은 20명만"으로 줄이면 시스템이 미달로 오해해 **다음날 60명 자동 이월**, "오늘만 +10"은 반영 수단 자체가 없었다. 시안 = `frontend/docs/모집인원조절_이월차수_와이어프레임.html`(사용자 확정 3건: 축소 시 [종료일 연장/남은 날 분산] 선택 · 차수별 집계 표시 · **1단계는 일건수 캠페인만**).
- **모델(두 겹)**: ① `campaign_daily_plans`(캠페인×날짜→인원, UNIQUE — 값 있는 날만 저장, 없는 날 = 기본 일건수) ② `campaign_rounds`(차수 원장 — **총량을 늘리는 유일한 창구**, 첫 추가 때 기존 recruit_total을 1차(초도)로 흡수, `recruit_total`=차수 합계로 동기화). 이력은 `campaign_plan_events`. FK 전부 TEXT(082 규율).
- ★★ **명시 조절일 = 그 값이 그날의 전부**(`dailyQuota` planCtx): 자연 이월을 얹지도(축소 무력화) 빼지도 않는다 — "오늘은 20명만"의 의미. 전일 자연 미달분은 사라지지 않고 **다음 조절 없는 날**의 계획누적(planned = dl×경과일 + 조절 증감분)으로 살아난다. 비조절일 이월 상한·하한(dl)·**총량 clamp 불변식은 그대로**. 계획·차수 0개 = 기존 동작 100%(옵트인). 킬스위치 `CAMPAIGN_DAILY_PLAN=0`.
- ★★ **전달은 `fetchCampaignCounts`(단일 깔때기)의 `counts.plans`** — 목록/상세/apply 게이트가 같은 계획을 본다(따로 실으면 "카드는 열렸는데 참여 거부"). 로더 `_loadPlanMaps`는 **fail-open**(테이블 부재·실패 = 계획 미적용 = 기존 동작, 42P01 5분 네거티브 캐시) + **client(잠금 tx)면 SAVEPOINT 격리**(082 — 실패 쿼리 하나가 apply tx를 abort). ★ 로더는 **카운트 쿼리 뒤**에 둔다(쿼리 순서를 보는 066 가드 계약 유지).
- **표시**: 조절일은 `carryAdded=0` + `todayPlanned`/`planAdjusted`(admin/list 동봉) — 조절을 "+N 이월"로 위장하면 관리자가 이월 사고로 오해한다. 카드 = 노란 `조절` 칩(`pg-plan`) + 차수 줄(`_roundsLine` — 2차 이상일 때만, 확정 순서대로 1차부터 차오름).
- **API = `/api/trackb/campaigns/:id/daily-plan`(GET/POST) · `/rounds`(POST/DELETE), 전부 adminOrMaster**(정원·총량 변경 = 관제 수동확정 급, 경로는 양쪽 호스트 공용 — 리뷰어 게이트와 같은 판단, 스코프 토큰 도달 불가). 42P01=`not_ready`(095 안내), 검증 코드는 400대 매핑(errorHandler 마스킹 방지). `savePlans`는 **캠페인 행 FOR UPDATE**(축소↔apply write-skew 차단) + **오늘 하한 = 오늘 확정+유효홀드**(`below_used`+floor — 참여 취소는 이 기능의 일이 아님) + 과거 날짜 거부.
- ★★ **조절을 저장하면 오늘 이후 전체가 자동 재구성된다 (사용자 확정 2026-08-19 · 위프 800건 `26.8.26` 잔존 신고)**: 종전 증분 동기화(`syncAdjustedPlansToWorktable`)는 **이번에 저장한 날짜만** 손대서, 작업표 생성 때 깔린 과거·꼬인 미래 빈 자리가 그대로 남았다("8/20부터여야 하는데 26.8.26으로 스케줄링"). 이제 `savePlans` 가 증분 동기화 뒤 **관리자 [작업표 재구성] 버튼과 같은 함수**(`rebuildAdjustedPlansToWorktable`, 사본 0)를 **오늘 이후 저장된 계획 전부**로 같은 트랜잭션에서 실행한다. ★★ **SAVEPOINT `cp_auto_rebuild` 격리 + 절대 throw 없음** — 재구성 실패(계획 없음·확정분 초과)로 **계획 저장 자체가 죽으면 안 된다**(082 apply 규율과 같은 자리). 실패는 `worktableSync.rebuild` 로 올려 **화면이 사유를 말한다**(조용한 누락 금지, `worktable_rebuild_empty` = 오늘 이후 계획 0건이라 정상 무동작). 킬스위치 `CAMPAIGN_PLAN_AUTO_REBUILD=0` = 종전 동작(증분만). ⚠ 재구성 풀은 종전 규칙 그대로 **빈 줄 + (날짜 없음 · 과거 · 조절한 날짜)** 라, **계획에 없는 미래 날짜의 준비 행은 여전히 손대지 않는다**(별도 계획일 수 있다 — 그 줄을 옮기려면 그 날짜를 조절 대상에 넣는다).
- ★★ **시트 일정 캠페인(063)도 조절 가능 — 조절한 날짜만 리뷰웹이 이긴다 (사용자 확정 2026-08-07, 종전 `schedule_driven` 거부 폐기)**: 계기 = 휴무일·일정종료로 **오늘 정원이 0인 공고의 이월을 어디서도 조절할 수 없었다**(사용자 신고). 규칙 = `computeCampaignState`의 시트 경로에도 `planOverrideFor` 적용 — **저장한 날짜만** 그 값이 그날의 전부이고, **조절하지 않은 날은 종전대로 시트(plannedThrough)가 정한다**(조절 해제 = 즉시 시트 값 복귀 → 진실원본이 통째로 넘어오지 않아 시트 운영과 공존). ★ **총량 clamp 유지**(`min(ov, totalSlots − 전일까지확정)` — 시트 총건수 초과 불가). ★★ **조절값 ≥1이면 `rest_day`·`schedule_ended` 판정을 우회해 그날을 연다** — 사람이 인원을 정했는데 화면이 무시하면 "저장했는데 안 열린다"(조용한 no-op)가 된다. **0명 조절은 '안 연다'는 뜻**이라 휴무·종료 표기를 유지한다. ★ `savePlans`의 일정 판정 실패(`schedule_unknown`)도 더 이상 저장을 막지 않는다(조절은 저장한 날짜에 그대로 적용되므로 fail-closed 근거가 사라졌다 — 잠그면 조절할 방법이 없는 막다른 길만 남는다). 화면은 "기본" 표시가 부정확할 수 있음을 고지. ★ 시트 일정 공고의 **해제(remove)** 는 `below_used` 하한 검사를 건너뛴다(복귀값=시트 계획을 서버가 모르고, "시트 값 < 사용량"은 이 기능 없이도 도달 가능한 상태).
- ★★★ **시트 경로에도 조절 증감분(`planDeltaThrough`)을 계획 누적에 반영해야 한다 — 빠뜨리면 095가 존재하는 이유가 무너진다(코드리뷰 Blocker, 실측)**: `plannedThrough`는 시트 값 그대로 누적되는데 확정만 적으니, "오늘은 5명만"으로 줄인 15명이 **다음 날 자동 이월**되어 시트 20인 날이 **35로 열렸다**(반대로 늘린 날은 다음 날에서 그만큼 빼앗는다). 일건수 경로의 `planned += ov − dl`과 같은 항을 **시트 계획을 기준선으로** 계산한 것. ★ 회귀가드는 **조절한 "다음 날"** 을 봐야 잡는다 — 조절 당일만 보면 이 회귀를 그대로 통과시킨다(변이시험으로 검출 확인).
- ★★ **모달은 시트 계획을 기준선으로 보여준다**(`campaign-daily-plan.js` `sheetFor`/`baseFor` 단일 출처 — 게이지·"기본 N"·눈금선·축소 질문·[기본으로]·저장 확인문이 **전부** 같은 값을 본다): 읽기 전용 표는 제거되고 그 자리에 "여기서 조절한 날짜만 시스템 값이 우선" 안내 + 앞으로의 시트 계획 요약. 시트에 없는 날은 기본 0으로 떠서 올리면 열린다. ★★ **`defaultDaily` 사본을 남기지 말 것**(코드리뷰 #3): `setPlan`이 그걸로 비교하면 시트 15인 날을 의도적으로 20(= `daily_limit`)으로 올릴 때 **조절이 조용히 삭제**되어 드래그가 되돌아가고 저장해도 아무 일이 없다(이번 변경이 곳곳에서 막으려던 "조용한 no-op" 그 자체), `settle`이 그걸로 비교하면 시트 30인 날을 22로 줄여도 **「빠진 인원 처리」 질문이 아예 안 뜬다**.
- ★ **총량도 시트 기준**(코드리뷰 #4): 서버가 시트 공고의 총량을 `sch.totalSlots`로 판정하므로 `getPlanOverview`가 `scheduleTotal`·`scheduleLastDate`를 함께 내려주고 모달의 `totalFor`가 그것을 쓴다(`recruit_total`을 쓰면 화면 숫자가 서버와 갈리고 예상 종료일·분산 범위가 어긋난다). ★ 시트 공고의 **차수(총량 추가)는 여전히 시트 행 수가 기준**이라 화면이 "차수 등록은 이 공고 정원에 영향 없음"을 말한다.
- ★ **`carryAdded`·"기본 N" 툴팁의 기준선도 시트 그날 행 수**(코드리뷰 #5 — `payload.todayBaseline`, admin/list 동봉): `quota − daily_limit`으로 두면 시트 공고에서 둘이 아무 관계가 없어 **근거 없는 "+15 이월" 칩**이 뜬다.
- ★ **`heldCarry` 호출 3곳이 모두 일정을 넘겨야 한다**(코드리뷰 #2 — `getPlanOverview`·admin/list·**`savePlans`의 carryApply 재검증**): 한 곳만 빠져도 표시(null)와 게이트(양수)가 갈려, 개념이 없는 공고에 `carry_apply` 원장 행이 영구히 남고 나중에 일건수 경로로 떨어지면 **이월 물량이 조용히 사라진다**.
- ★★ **시트 일정 공고에는 이월 보류(098)가 적용되지 않는다** — 정원을 시트 계획이 정하므로 '자동 이월 가산 멈춤' 자체가 없다 = 잔량 개념도 없다. `heldCarry(c, counts, todayStr, appliedSum, schedule)`가 **일정이 있으면 null**을 돌려주고(호출부 2곳이 일정을 넘긴다: `getPlanOverview`·admin/list), 모달·원클릭은 **"조회 실패"로 뭉뚱그리지 않고** "시트 일정 공고에는 이월 보류가 적용되지 않습니다 — 날짜별로 조절하세요"라고 사유를 말한다(효과 없는 ⏸ 칩 = 막다른 길 금지). `getPlanOverview`는 일정 판정 실패(`unknown`) 시에도 잔량을 계산하지 않는다(fail-closed).
- ★★ **오늘 정원이 0인 카드도 칩(이월·조절·보류)을 그린다**(`campaign-cards.js`): 종전엔 게이지 자리가 통째로 `게시 전 / 게시하면 집계가 시작됩니다`로 대체되며 칩이 함께 사라졌고, **게시된 공고를 미게시로 오해**하게 만들었다(사용자 신고). 칩 계산을 게이지 분기 **밖으로** 빼고(`chips`), 사유는 `_zeroQuotaNote`가 사실대로 말한다(오늘 휴무+다음 진행일 / 일정 종료 / 총원 충족 / 모집 종료 / 오늘 0명 조절 / 오늘 정원 0). ★ **오픈 전·게시 전(draft)은 칩 미표시**(`showChips` — 오늘 집계 자체가 없는데 숫자를 띄우지 않는다).
- ★★ **차수 있는 공고의 recruit_total 직접 수정은 서버가 무시**(`roundsLockRecruitTotal` — PUT `/admin/:id` + `_scopedCampaignEdit` 양쪽): 수정 모달의 진행상품 표 파생 hidden 재전송이 차수 추가분(300)을 낡은 값(200)으로 되돌리는 footgun 차단. fail-soft(테이블 부재 = 잠금 없음). 드리프트는 GET이 `roundsDrift`로 고지. ★ **무시는 조용하지 않다**(코드리뷰 M2) — PUT 응답 `recruitTotalLocked:true` → 발행 모달이 "총모집은 차수 원장이 관리" 토스트로 고지.
- ★★ **총원 충족으로 closed 가 영속된 공고는 차수를 추가해도 자동 재오픈하지 않는다**(코드리뷰 M1 — 수동 마감과 auto-persist 를 status 값만으로 구분할 수 없어 자동 재오픈은 위험): addRound 응답에 `status` 동봉 → 모달 배너·차수 추가 토스트가 **"게시 토글을 켜야 모집이 재개됩니다"** 를 말한다(조용한 누락 금지). 재개 = 카드 게시 토글(사람이).
- **모달 = `js/campaign-daily-plan.js` 공유 한 벌**(admin.html·workdesk.html, 카드 [📅 인원] 버튼 — 참여형+모듈 존재 게이트): 게이지 드래그+−/＋, ★ **질문(빠진 인원 처리)은 조절 한 묶음당 한 번**(0.7s 디바운스 — 클릭마다 팝업이면 연속 조절 불가, 시안 실측) ★ **분산 범위 = 축소 전 종료일까지의 남은 진행일**(밖까지 나누면 종료일이 밀림, 시안 실측), 저장은 [확정 저장] confirm 경유(연장/분산 결과는 저장 시점에 계획 행으로 풀려 set에 실림 — 서버 런타임 분기 0), 차수는 건별 confirm 즉시 반영. body 직속 마운트·var 폴백·onclick 보간 0.
- 잔여(문서화): 관제(📡) 패널의 날짜별 계획 대비 실적 표시는 후속 / 수정 모달의 총모집 칸 읽기 전용 전환(현재는 서버 무시 + 드리프트 경고)은 후속 / 시트 일정 공고의 **차수(총량)는 여전히 시트 행 수가 기준**(조절은 날짜별 정원만 바꾼다) / 조절한 날짜 이후 시트에서 그 날 행 수를 바꿔도 **조절값이 계속 이긴다**(해제해야 시트로 복귀 — 화면이 문장으로 안내). 회귀가드 `tests/campaignDailyPlan.test.js`(순수함수·스텁 pool 서비스 실행·라우터 스택 실검사·배선 + PGTEST_URL 시 진짜 PG, **변이시험 검출 확인**) + 실브라우저 시나리오(시트 일정 조절·정원 0 카드 표시·baseFor 단일 출처·시트 총량).

### ★★ 이월 보류·수동 반영 (carry_mode, migration 098) — 이월의 반영 시점을 사람이 고른다
- **배경(사용자 요청)**: 자연 미달분(이월)이 무조건 다음날 자동으로 얹혀(066), 업체가 "미달분은 나중에 몰아서"를 원하면 매일 아침 손으로 줄여야 했다. 시안 = `frontend/docs/이월보류_수동반영_와이어프레임.html`(사용자 확정 3건: 기본=자동·옵트인 보류 / 반영 창구=카드 원클릭+[📅 인원] 팝업 / 홈 인라인 수정은 허용명단 리뷰어도 설정 가능).
- **모델**: `recruit_campaigns.carry_mode` TEXT 기본 `'auto'`(백필 0 = 기존 공고 현행 불변, DB CHECK 없음 — 087 규율). `'hold'` = **자동 이월 가산만 멈춤**(`dailyQuota`의 carry 분기 앞 `isCarryHold` 게이트) — 계획표(095)·조절일 규칙·총량 clamp 불변이라 **물량 소실 0**(반영 안 하면 종료일만 뒤로). 킬스위치 `CAMPAIGN_CARRY_HOLD=0` = 전건 자동 복귀.
- ★★ **판정 단일 출처 = `isCarryHold` / 잔량 = `heldCarry`**(campaignState.service — 게이트·잔량·목록 칩이 같은 판정): 잔량 = 어제까지 계획누적(095 계획 반영) − 확정(`counts.hold` 창) − 반영누적(`campaign_plan_events` `carry_apply` 합 — `fetchCarryAppliedSums` fail-soft). **null = 계산 불가**(기준선 없음·보류 아님) — 화면은 칩 미표시·"조회 실패"로 말하고 **0으로 꾸미지 않는다**.
- ★ **기준선 2종(066 carry + 098 hold, `campaign_carry_hold_start` 시드 = 소급 차단)은 한 쿼리**(`_baselines`) — 따로 읽으면 fetchCampaignCounts 쿼리 순서가 밀려 **"카운트 쿼리는 두 번째"라는 066 가드 계약이 깨진다**(실측 — 스텁 행에 `key` 컬럼 필요).
- **반영 = 날짜별 계획(095)에 얹기(신규 정원 경로 0)**: `savePlans`의 `carryApply`(hold 공고만 — auto면 `carry_not_hold`)가 `carry_apply` 이력을 남겨 잔량에서 차감. 창구 둘 = ① 카드 **⏸ 보류 N 칩**(`pg-hold` 보라 — 클릭 = `CampaignDailyPlan.quickApplyHeld` 확인창 3택[오늘 반영/세부 선택/그대로 두기] 후 즉시 저장) ② [📅 인원] 팝업 상단 **보류 블록**(오늘/내일/분산 스테이징 → [확정 저장]).
- ★ **설정 세그먼트 = 공고 수정 모달(`rf_carry_mode`) + 홈 인라인 모달(`cae_carry_mode`)**: UI 있는 화면에서만 전송(미전송 = COALESCE 유지 — $40 센티널, auto/hold 외 값 무시). **보류를 직접 고른 순간에만** 고지문 펼침(silent 프리필) — "오늘 정원 복귀 + 물량 불소실"을 문장으로(원칙 ⑤). ★ **스코프 편집(`_scopedCampaignEdit`)도 carry_mode 만 추가**(확정 ③ — 일건수를 이미 수정 가능한 신원) — **반영(carryApply)은 adminOrMaster 전용 API라 스코프 토큰이 보류분을 열 수는 없다**(인라인 모달이 "반영은 관리자"를 문장으로 안내).
- ★★ **코드리뷰 반영 5종(완화 금지)**: ① **B1** — 공개 `GET /list` SELECT에 `carry_mode` 명시 동봉(빠지면 목록·apply가 서로 다른 정원을 봐 "카드는 열렸는데 참여 거부" — counts 깔때기 규율의 SELECT판) ② **M1** — 생성(create) INSERT에도 carry_mode(발행 시 선택이 조용히 auto로 낙하 방지) ③ **M2** — `savePlans`의 carryApply는 **캠페인 행 잠금 안에서 잔량을 재계산**(SAVEPOINT `cap_check`): 초과 = `carry_stale`(409, e.held 동봉 — 동시 원클릭 이중 반영 차단), 재계산 실패 = `carry_unknown`(503, **fail-closed** — 모르는 채 원장에 기록하지 않는다) ④ **M3** — `fetchCarryAppliedSums` 실패 = **null**(빈 Map로 접으면 반영분이 0으로 세어져 잔량이 부풀고 이중 반영을 유도) → admin/list도 합계 null이면 carryHeld null ⑤ **M4** — `REQUIRED_SCHEMA`에 `recruit_campaigns.carry_mode` 등록(082 규율 — 없으면 발행·수정·목록 42703).
- ★ **carryApply 단독(계획 미동반)은 `empty` 거부가 맞다** — 정원을 안 늘리고 반영 이력만 남기면 그 인원이 어디에도 안 열려 **물량이 소실**된다(원클릭은 항상 오늘 계획 set을 동반).
- ★ 원클릭 확인창은 **시트 일정 공고면 "이월 보류가 적용되지 않습니다 — [📅 인원]에서 날짜별로 조절하세요"**(2026-08-07 — 종전 "시트에서 조절" 문구 대체)와 **게시 꺼짐 고지**("반영 후 게시 토글을 켜야 재개" — 차수 추가 M1과 같은 규율)를 문장으로 말한다. 판정 실패(`unknown`)는 서버가 잔량을 null 로 내려 잔량 검사에서 걸린다.
- ⚠ **병합 유실 실측**: main 병합 과정에서 `repairRecruitTotalFromRounds`가 module.exports에서 빠져 **경합 자가치유가 TypeError→fail-soft로 조용히 무력화**돼 있었다(이번에 복구 + 가드가 export 존재를 고정). **여러 세션이 같은 파일을 병합할 때 exports 목록은 유실 단골**이다.
- ★ **시트 일정 공고에는 보류가 적용되지 않는다**(2026-08-07 — 위 095 항목 참조): `heldCarry`가 일정 인자를 받아 null 을 돌려주고 화면이 사유를 말한다. 그 공고의 이월 조절 창구는 [📅 인원]의 날짜별 조절.
- 잔여(문서화): 카드 ⏸ 칩은 admin/list 응답 재료라 **스코프 토큰(리뷰어앱) 화면에는 잔량이 없다**(설정 세그먼트만 — 반영은 어차피 adminOrMaster 전용이라 막다른 길 아님, 모달이 "반영은 관리자"를 안내). 킬스위치 복귀(`CAMPAIGN_CARRY_HOLD=0`)는 보류 중이던 이월이 **한꺼번에 자동 이월로 되살아나므로**(066 상한 2×dl가 버스트를 억제) 낮은 트래픽 시간대에 끄는 것을 권장. 잔량 기준선은 전역 1개(`campaign_carry_hold_start`) — 배포 이전 미달분은 세지 않는다(066 소급 금지와 동일).
- 회귀가드 `tests/campaignCarryHold.test.js`(hold 게이트·잔량 순수함수·carryApply 스텁 실행(carry_stale/carry_unknown/empty 포함)·배선 + 진짜 PG) + 실브라우저 실행검증(칩·원클릭 POST·스테이징 저장·잔량 null 잠금).

### ★★ 모집인원 조절 = 총량 균형 모드 (요구 ①~⑥, 시안 `frontend/docs/design-recruit-quota-balance.html`)
- **배경**: [📅 인원]은 "오늘 20명" 같은 **점 조절**만 가능했고, 조절한 결과가 총량과 맞는지·종료일이 언제가 되는지를 화면이 말하지 않았다. 이월(미달분)은 카드 칩으로만 보였고 **어느 날에 얹을지 고를 수단이 없었다**. 사용자 확정 요구 = ① 총원까지 남은 건수 안에서만 조절 ② 조절 시점의 현재모집인원/총건수 표기 ③ 종료일 표기 ④ 이월 처리 토글(종료일 고정 조절 ↔ 일일인원 유지+종료일 연장) ⑥ 배분 합계 초과=빨강·부족=파랑·일치=초록, **일치일 때만 저장**.
- ★★ **모델 = "구간을 펼쳐 합계를 맞춘다"**: 모달을 열면 **오늘(또는 시작일) ~ 예상 종료일** 구간의 날짜별 인원을 실제 값으로 펼치고(`buildHorizon`), 그 합계가 **배분해야 할 인원 = 총량 − 어제까지 확정**(`targetTotal`)과 **정확히 같을 때만** 저장한다. 마지막 날은 남은 만큼만 열어 합계가 구조적으로 목표와 일치한다. 저장은 그 구간을 **명시 계획(`set`)** 으로 보내므로 화면에서 본 대로 열린다.
- ★★ **저장하면 그 구간에는 자동 이월(066)이 얹히지 않는다 — 확인창이 그 사실을 말한다**(조용한 의미 변경 금지). 못 채운 몫은 사라지지 않고 **구간 이후 첫 무계획일**의 계획누적으로 되살아난다(095 규율 그대로 = 물량 소실 0, 종료일만 밀린다).
- ★★ **이월 보충 투입 방식 3종**(요구 ④, `CARRY_MODES`) — ★★ **기본 = `spread`(남은 날에 나눠 담기, 사용자 확정 2026-08-19 — 종전 `next` 에서 뒤집혔다)**: 계기 = 위프 800건에서 이월이 **625명**까지 쌓였는데 기본값이 `next` 라 그대로 배치하면 **다음 진행일 하루에 625명**이 열린다(066 하루 상한 2×일건수를 훌쩍 넘고 리뷰어 화면에도 그 숫자가 그대로 나간다). ★ `extend` 를 기본으로 두지 않는 이유 = 그건 **종료일이 조용히 밀리는** 선택이라 사람이 명시로 고를 일이다. ★ 기본값은 `DEFAULT_CARRY_MODE` **단일 출처**(폴백·되돌리기·'기본' 배지가 전부 이것을 본다 — 회귀가드가 `'next'` 하드코딩 부활을 막는다). 세 방식의 뜻은 그대로: `next` 첫 진행일에 전액 / `spread` 진행일에 고르게 / `extend` 얹지 않음(종료일이 밀린다). 전환은 이월만 재배치하고 **총량은 어느 방식에서도 불변**, 화면이 "방식별 종료일"을 나란히 보여준다.
  - ★ **나눌 대상(`slots`) = 이월 없이도 온전히 채워지는 진행일**(마지막 부분일 제외) — 부분일까지 세면 그 몫이 총량 맞추기에서 잘려 **"이월 30명인데 29명만 얹혔다"** 가 된다(시안 실측).
  - ★★ **`extend` 의 부족분은 기존 날에 얹지 않고 새 진행일로 흘려보낸다**(`autoFit` 의 `continue`) — 마지막 날에 쌓으면 그건 "마지막 날에 몰아주기"라 **고른 방식과 정반대**이고, 사람이 방금 줄여 둔 날을 되돌려 놓기까지 한다(변이시험이 잡은 실제 버그).
- ★★ **이월 표시는 배정량으로만**(`carryOn = min(배정, plan − effBase)`) — `plan − base` 로 되계산하면 **사람이 손으로 올린 인원까지 "이월"로 부른다**(시안 실측: 40→46 으로 올린 줄에 `＋이월 6`). 주황 막대 폭도 같은 값을 쓴다.
- ★★ **저장된 조절은 출발선이다**(`effBase = S.base[d] ?? baseFor(d)`) — `baseFor` 로 펼치면 관리자가 저장해 둔 조절("8/12 는 5명만")이 **모달을 여는 것만으로 조용히 기본값으로 덮인다**. 구간 밖(휴무일·종료일 이후)에 저장된 계획도 `S.plan` 에 살려 합계에 넣는다(안 살리면 저장 시 `remove` 로 나가 사라진다).
- ★ **오늘 하한 = 확정·진행 인원**(`minFor`) — 펼칠 때(`buildHorizon`)와 자동 맞춤 양쪽에 건다. 저장된 조절이 그 사이 늘어난 확정보다 작을 수 있고, 그대로 보내면 서버가 `below_used` 로 거절해 막다른 길이 된다.
- ★ **한 날 최대 = 남은 배분수**(요구 ①, `maxFor`) — 게이지·−/＋·직접 입력 세 창구 모두. 직접 입력은 **`change`(확정/blur)에서만** 반영한다(입력마다 재렌더하면 입력칸 DOM 이 다시 만들어져 **한글 IME 조합이 파괴**된다 — 홈 목록 검색 `ㅁ며면면` 실측 계열).
- ★★ **균형 바 높이는 "일치(초록)" 기준 41px 고정**(사용자 확정 · `box-sizing:border-box` 명시 — 호스트 리셋에 기대지 않는다): 상태마다 바가 커졌다 작아지면 아래 표가 위아래로 흔들려 **조절하던 줄을 놓친다**. 문구는 사용자가 글자 그대로 확정한 한 줄(`총량이 750명보다 6명 초과입니다. — 저장불가` / `… 부족입니다. — 저장불가` / `총량 750명과 딱 맞습니다. — 저장가능`). 초과/부족은 **합계의 성질**이라 행을 빨갛게 칠하지 않는다(어느 줄이 초과인지 정할 수 없다 — 판정 단일 출처 = 균형 바 하나).
- ★★ **펼칠 수 없으면 균형 모드를 끄고 종전 동작(14일 성긴 표)으로 폴백**(무제한 총량 · 총량 충족 · 구간 > 120일 · 오늘 하한 초과 · 서버 기준값 없음 · **앞으로 진행일이 0개**) — **반쪽짜리 균형 표시로 잘못된 확신을 주지 않는다**. 저장 상한은 서버 `MAX_PLAN_ENTRIES`(120)와 **같은 값**이고, 넘으면 사유를 말하고 잠근다.
- ★★★ **"시트 계획이 배분해야 할 인원보다 적다"는 기능 불가가 아니라 `부족(파랑) — 저장불가` 그 자체다 (사용자 신고 2026-08-07 · 시트 800명 공고)**: 종전엔 이 경우 균형 모드를 **통째로 끄고** 노란 안내문만 `앞으로 열 수 있는 자리(650명)가 …적어 배분 표를 만들 수 없습니다 — 아래에서 어느 날의 인원을 늘려주세요` 로 띄웠다 → **시키는 대로 인원을 늘려도 균형 바가 영영 안 뜨는 막다른 길**(균형 판정은 모달을 열 때 한 번만 하므로 조절해도 재평가되지 않는다). 지금은 `walkDays` 가 목표를 못 채워도 **모아 둔 진행일을 그대로 돌려주고**(`{days,sum,short}`), 구간을 그 날들로 펼친 뒤 균형 바가 부족분을 말한다 — 사람이 올리거나 **[자동 맞춤]** 을 누르면 그 자리에서 초록(저장가능)이 된다. 시트 일정 공고는 과거에 하루라도 미달하면 항상 이 상태가 되므로(= 조절이 가장 필요한 상태) **이 경로가 열려 있어야 한다**.
- ★ **부족 안내는 "지금도 모자랄 때만"**(`bal && S.shortBy > 0 && diffPlan < 0`) — 사람이 채워 합계가 맞았는데 `130명이 모자랍니다` 가 남으면 균형 바(`딱 맞습니다`)와 **정면으로 어긋난다**(실브라우저가 잡았다). ★ 부족분은 **적용한 방식의 결과에서 읽는다**(`S.shortBy = h.shortBy`) — 렌더 중 방식별 종료일 비교(`em`)가 `buildHorizon` 을 다시 부르므로 전역에 흘려 두면 다른 방식 값으로 덮인다.
- ★ **균형 모드에서는 창구를 하나로**: 보류 이월 블록(098)을 그리지 않고(반영 창구가 둘이면 이중 반영), 「빠진 인원 처리」 팝업도 띄우지 않는다(부족/초과는 균형 바가 말하고 [자동 맞춤]이 고른 방식대로 되돌린다). 098 회계는 `carryApply = carryPlaced`(**보류 공고일 때만**) — 얹지 않은 `extend` 는 미전송.
- ★ **서버는 재료만 추가**(정원 계산 무변경): `getPlanOverview` 가 `carryPending`(자동 공고 = `pendingCarry`, 보류 공고 = `heldCarry`) · `todaySubmitted` 를 **읽기 전용**으로 내려준다. `pendingCarry` 는 `heldCarry` 에서 추출한 단일 출처이고 `heldCarry` 는 그 얇은 래퍼(반영 누계 차감만 추가) — 사본을 두면 카드 칩과 모달의 이월 숫자가 갈린다. **null = 계산 불가**(0 으로 꾸미지 않는다 → 화면은 `↩ 이월 ?`).
- ★ **「조절」 배지 기준 = 저장된 조절 또는 "이 방식의 배치에서 사람이 더 바꾼 날"**(`S.modePlan`) — 열었을 때(`openPlan`)로 재면 **방식을 바꾸는 순간 전 줄에 배지가 붙어**(시스템 배치인데 사람이 손댄 것처럼) 신호가 죽는다(실측). 닫기 확인만 `openPlan` 기준(펼침 자체는 변경이 아니다).
- ★★ **코드리뷰 반영 6종(완화 금지)**: ① **오늘 하한(확정·진행 인원) > 배분 목표면 균형 모드를 켜지 않는다** — 총량 초과 확정·소진 직전 홀드에서 도달하는데, 켜면 균형 바가 영원히 "초과 — 저장불가"이고 [자동 맞춤]은 하한에 막혀 **아무 일도 안 하는 죽은 버튼**이 된다(조절 자체가 봉쇄되는 막다른 길). ② **손대지 않은 날은 저장하지 않는다**(`payload` = `plan[d] !== naturalFor(d)` 인 날만) — 구간을 통째로 저장하면 아무것도 안 건드리고 [확정 저장]만 눌러도 앞으로의 전 날짜가 리뷰웹 값으로 굳어 **시트 일정 공고의 시트 우선권이 통째로 사라진다**. ③ **자동 이월 공고의 "다음날에 더하기" = 서버(066)가 이미 하는 일이라 보낼 것이 없다**(`naturalFor(오늘) = 기본 + 자동이월`) — 명시 계획으로 박으면 `planned` 가 같이 올라가 **이월이 영영 정산되지 않는다**(유령 이월: 이월 30을 얹고 30명이 다 채워도 다음날 또 30). 반대로 `extend`·`spread` 는 그 자동 가산을 **억제해야** 하므로 오늘을 명시 저장한다. ④ **한 날 상한은 서버 `MAX_DAY_COUNT`(9999)를 넘지 않는다** — 넘기면 `savePlans` 가 `bad_count` 로 **저장 전체를 거부**한다(차수 누적 총량 9999+ 공고에서 도달). ⑤ **보존 기준은 `baseDate` 가 아니라 오늘** — 시작일이 미래인 공고에서 `today ≤ d < 시작일` 의 저장된 계획이 화면에서 빠져 **`remove` 로 조용히 삭제**된다. ⑥ **시트 일정 공고에는 "이월 ?" 칩을 띄우지 않는다** — `pendingCarry` 가 의도적으로 null 인 것(개념 미적용)을 "조회 실패"로 말하면 담당자가 원인을 엉뚱한 데서 찾는다(`heldBlk` '해당 없음'과 같은 규율).
- ★★★ **"손대지 않으면 오늘 몇 명이 열리는가"는 서버가 준 `todayNaturalQuota` 하나**(2차 코드리뷰 🟡1) — `getPlanOverview` 가 **정원 판정 단일 출처인 `computeCampaignState` 를 그대로 태워** 값을 실어 보낸다. 프론트가 `기본 + 이월` 로 되계산하면 066 의 **이월 상한(`CARRY_CAP_MULT` 2배)·킬스위치(`CAMPAIGN_DAILY_CARRY=0`)·095 계획·총량 clamp** 를 모르므로, **이월이 하루치를 넘는 흔한 경우부터** 화면 숫자가 서버와 갈리고(예: dl 40·이월 200 → 화면 240 vs 서버 80) 저장 판정·예상 종료일·토스트 문구가 전부 그 위에서 계산된다. ★ **기준값을 못 받으면(구버전 백엔드·계산 실패) 균형 모드를 켜지 않는다** — 잘못된 기준으로 "손 안 댔다"를 판정하면 조용히 아무것도 저장하지 않는 화면이 된다.
- ★ **이월은 서버값 그대로 표시한다**(저장해 둔 앞 날짜 계획을 빼지 않는다, 🟡2) — "이월 배치"와 "무관한 상향 조절"을 구분할 수 없어, 무관한 +N 이 **실제로 남아 있는 이월을 0으로 숨겨 배치 창구가 사라진다**. 재제안 걱정은 `naturalFor` 가 서버값이라 이미 막혀 있다.
- ★★★ **이월을 얹지 않는 방식(`extend`·`spread`)은 "저장하지 않으면 성립하지 않는다" — 구간을 명시 고정한다 (사용자 신고 2026-08-07 "종료일 뒤에 붙이기 선택하였는데 저장이안됨")**: `naturalFor` 는 **오늘만** 서버 판정값(`todayNaturalQuota`)을 알고 앞날은 기본 일건수로 본다. 그런데 서버 자동 이월(066)은 **내일도 모레도** 그날 정원에 미달분을 얹으므로, 오늘만 고정하면 사람이 고른 "각 날 인원은 그대로"가 **다음 날 그대로 무너진다**. 게다가 오늘이 이미 그 값으로 저장돼 있으면 고정할 것조차 없어 **저장 버튼이 잠긴다**(신고 화면). → `payload` 의 `pinAll` = `방식 ≠ next && 이월 > 0 && 공고가 auto`. ★ **고정 중에는 `remove` 로 내보내지 않는다** — 해제 = 자동 이월 복귀 = 고른 방식 무효. ★ `next` 는 종전대로 보낼 것 없음(서버가 이미 하는 일 · 유령 이월 방지), **이월 0·보류(hold) 공고는 억제할 대상이 없어 고정하지 않는다**. ★ 이월이 없어 방식이 무의미할 때는 그 사실을 문장으로 말한다(`지금은 이월 인원이 없어 방식별로 달라지는 것이 없습니다`).
- ★ **`payload` 의 "마지막 부분일" 예외는 시스템이 깐 값일 때만**(`v === S.modePlan[d]`, 🟡3) — 균형이 맞은 상태의 마지막 날은 **항상** residual 이라 그 조건만 보면 **사람이 의도적으로 줄인 마지막 날이 조용히 누락**된다. ★ **확인창은 실제로 보내는 날 수를 말한다**(🟡4) — "구간이 확정된다"는 손댄 날만 보내는 지금 동작을 과장한 거짓 고지. ★ **마지막 부분일(총량에 맞춰 남은 만큼만 연 날)은 고정하지 않는다** — 서버도 같은 총량 clamp 를 걸고, 고정하지 않아야 앞 날이 미달했을 때 그날이 온전히 열린다. ★ **기본값으로 되돌린 저장분은 `set` 이 아니라 `remove`**(시트·일건수 우선권 복귀).
- ★★ **구간 밖(시작일 이전·예상 종료일 이후)에 저장된 계획은 합계에 넣지도, 지우지도 않는다** — 넣으면 모달을 열자마자 **사람이 만들지 않은 "초과 N명"** 이 뜨고 [자동 맞춤]이 그 계획을 0으로 깎는다. 총량 clamp 때문에 종료일 이후 계획은 어차피 열리지 않는 값이고, `S.plan` 에 남겨 두므로 `remove` 로도 안 나간다. **존재 사실은 표 위에 문장으로 고지**(조용히 숨기지 않는다).
- ★★ **균형 모드를 못 켠 이유를 화면이 말한다**(`S.balanceOff` — 3차 코드리뷰): 무제한·총량 충족은 조용히 넘기되, **오늘 확정이 남은 배분수보다 많음 / 서버 기준값 없음 / 앞으로 열 자리 부족 / 구간이 저장 상한 초과**는 사유 + 다음 행동을 적는다. ★ 특히 **시트 일정 공고는 과거에 하루라도 미달하면 남은 시트 자리 < 배분해야 할 인원이 되어 균형 표를 못 만든다**(= 이월 조절이 가장 필요한 상태인데 조용히 종전 화면으로 떨어졌다) → `앞으로 열 수 있는 자리(100명)가 배분해야 할 인원(300명)보다 적어…시트에 진행 날짜를 더 넣거나` 로 숫자와 조치를 말한다. ⚠ 이 문구는 **`render` 아래에서 `var` 선언되는 `target` 을 참조하면 호이스팅으로 `undefined명`이 찍힌다** — 실브라우저가 잡았고(정적 검사로는 못 잡는다) 회귀가드가 `targetTotal` 호출을 고정한다.
- ★★ **한 날에 066 하루 상한을 넘겨 여는 배치는 경고한다(막지 않는다)**: 서버 자동 이월은 `min(미달분, 일건수 × CARRY_CAP_MULT)` 로 잘리는데, 명시 계획으로 저장하면 095 규율상 그 값이 그날의 전부라 **상한을 넘겨 열린다**(dl 40·이월 200 → 240). 066 이 막으려던 것이 정확히 이 하루 버스트이므로 이월 블록이 `자동 이월이었다면 오늘은 N명까지만 열립니다 … [남은 날에 나눠 담기]를 고르세요` 로 사실과 대안을 말한다 — **저장 게이트는 건드리지 않는다**(사용자가 고른 방식을 막지 않는다).
- ★★ **고른 이월 방식은 저장·재조회·재오픈에도 유지한다**(사용자 신고 2026-08-07 "종료일 뒤에 붙이기로 저장이 됐는데 게이지가 조절 전으로 되돌아간다"): 저장하면 `applyOverview` 가 다시 도는데 **무조건 `applyCarryMode('next')`** 로 깔아, 이월이 오늘에 다시 얹힌 배치가 그려져 **저장이 되돌아간 것처럼 보였다**(저장값 자체는 정상 — 화면만 다른 방식으로 재제안). → `S.carryMode || _loadMode || 'next'`(못 펼치면 next 폴백). 기억은 **화면 상태일 뿐**이라 `sessionStorage`(캠페인별 `cdp_carry_mode_<id>`)에 담고 **실패해도 무시**(사생활 보호 모드), **모르는 값은 쓰지 않는다**. ★ 고정 모드라도 **이미 같은 값으로 저장된 날은 다시 보내지 않는다** — 안 걸러내면 저장 직후에도 [확정 저장]이 열려 있어 "저장이 안 됐나?"로 오독된다.
- ★★ **표 머리는 고정, 스크롤은 날짜 목록부터**(사용자 확정 2026-08-07 "행고정하듯이"): 본문(`.cdp-bd`)은 스크롤하지 않고 안에 **고정 영역 `.cdp-fix`**(안내·모집 현황·이월 방식·균형 바·표 머리)와 **스크롤 영역 `.cdp-sc`**(날짜 목록·예상 종료일·차수·이력)를 둔다. ★ **flex 자식은 기본 `min-height:auto`** 라 `min-height:0` 이 없으면 **스크롤이 안 생기고 내용만큼 늘어난다**(업체관리 도구줄 sticky 가 죽었던 것과 같은 함정). ★★ **스크롤 위치를 기억했다 복원한다**(`S._scrollTop`) — `render` 가 본문을 통째로 갈아치워 스크롤 컨테이너가 새로 만들어지므로, 복원하지 않으면 −/＋ 한 번에 목록이 맨 위로 튀어 **조절하던 줄을 놓친다**(종전에는 본문 자체가 스크롤 컨테이너라 없던 문제).
- ★★ **날짜 색 = 법정공휴일·일요일 빨강 / 토요일 파랑**(`dayKind`·`holidayName`, 공휴일 이름 배지 + title): **양력 고정일만 계산으로 뽑고 음력(설·추석·부처님오신날)·대체공휴일은 연도별 확정값 표**(`LUNAR_HOLIDAYS`)로 둔다 — 음력은 계산식으로 만들 수 없고 대체공휴일은 해마다 정부가 확정하므로, 추측으로 칠하면 **"빨간 날이 아닌데 빨갛다"** 가 되어 배분 계획을 잘못 세운다(틀린 값 > 빈 값). ★ **표에 없는 연도는 고정일만 칠하고 음력 공휴일은 표시하지 않는다** — 연도가 넘어가면 표에 줄을 추가한다(회귀가드가 추측 부활을 막는다). 색은 리터럴(테마 없는 호스트).
- ★★ **부족(`shortBy > 0`)일 때 "방식별 종료일"에 날짜를 단정하지 않는다 (위프 800건 실측 2026-08-19)**: `walkDays` 는 목표를 못 채워도 모아 둔 날을 그대로 돌려주고 탐색은 **`finitePlanLimit()`(작업표/시트 마지막 날 + 13일)** 에서 끊긴다 — 그 컷오프를 종료일로 찍으면 ① 헤더의 `예상 종료일 계산 불가`(`endDate()` 가 400일을 돌아도 총량을 못 채워 `null`)와 **정면으로 어긋나고** ② 세 방식이 전부 같은 날로 보여 **"뒤에 붙여도 종료일이 안 밀린다"** 는 잘못된 판단 근거가 된다. `em(mode)` 은 `h.shortBy > 0` 이면 **`계산 불가`** 를 돌려준다. ⚠ `buildHorizon` 의 필드는 `short` 가 아니라 **`shortBy`**(모자란 인원) — 이름을 헷갈리면 조건이 항상 거짓이 되어 조용히 종전 동작으로 돌아간다(개발 중 회귀가드가 실제로 잡았다).
- ★ **무시트 작업표 공고의 "예상 종료일 계산 불가" 는 버그가 아니라 부족의 다른 표현**: `baseFor` 가 `worktableFor` 로 떨어져 **작업표에 줄이 없는 날짜는 0명**이므로, 계획 합이 배분 목표보다 적으면 `endDate()` 는 영원히 0에 도달하지 못한다. 부족분을 채우면(주말 열기·하루 인원 상향·종료일 연장) 즉시 계산된다.
- ★ **[자동 맞춤]이 차이를 다 못 메우면 말한다** — 눌러도 아무 일 없는 버튼을 두지 않는다(하한·상한 사유를 토스트로). ★ 자동 공고 + 다음날 방식의 "저장할 것 없음"은 **"이미 오늘 정원에 반영되어 있습니다"** 로 말한다 — 화면에 이월이 얹혀 보이는데 저장이 잠겨 있으면 "미반영"으로 오독된다.
- 회귀가드 `tests/campaignDailyPlan.test.js` §7·§7A(균형 엔진·저장 payload 를 **vm 으로 꺼내 실제 실행** — 합계·이월 배정·방식 충실도·하한·상한·폴백·코드리뷰 6종 + 부족 상태에서 늘리면 초록이 되는지 + 사용자 확정 문구/높이 고정, **변이시험 검출 확인**) + 브라우저 실행검증(테마 있는 호스트·없는 호스트 양쪽 · **서버가 기준값을 못 준 경우의 폴백이 막다른 길이 아님**까지 확인). ⚠ 이 변경으로 `campaignCarryHold` 의 carryApply 배선 패턴을 함께 갱신했다(검사 의미 불변 — 저장 본문 동봉·보류 공고 한정).

### 작업오더 → 모집공고 자동 채움 (연결탭·구매채널·담당자, migration 065)
- **와이어프레임 문서** `frontend/docs/작업오더-모집공고_자동반영_와이어프레임.html`(28항목 = 자동 18 / 확인 4 / 직접 6, 칸별 출처·이유). 회귀가드 `tests/woCampaignMapDoc.test.js`가 **문서가 '자동'이라 한 항목은 프리필 배선이 있어야, '직접 입력'이라 한 항목은 없어야** 한다를 고정.
- **① 연결 탭**: `work_sheet_url`은 **제출 단계에선 선택**(2026-08 작업표 생성 도입으로 필수 해제)이지만 **접수(`/admin/accept`)가 URL+gid를 여전히 강제**하므로 접수된 오더는 `linked_tab_sheet_id/name/gid`가 항상 있다(불변식 유지) → 프리필이 그대로 선택(`_prefillLinkedTab`). ★ 이 게이트를 풀면 프리필·모집공고 자동채움 전제가 함께 깨진다. ★ **gid 우선 재매칭**(운영 중 리네임 대비), 목록에 없는 탭이면 **선택 안 함**(잘못된 탭 연결이 빈칸보다 나쁨). 인박스 목록이 `SELECT *`라 서버 변경 0. 미접수 오더는 값이 없어 종전대로 수동.
- **② 구매채널**: 작업오더에 채널 필드가 **없어서** 상품 URL의 **호스트**로 판정(`WO_CHANNEL_HOSTS` — coupang/naver/oliveyoung/**makers.kakao→카카오메이커스**, 없으면 유입가이드 첫 URL). ★ **호스트만 본다** — 전체 URL 매칭이면 `coupang.com/...?src=naver_ad`를 네이버로 오판하고 `coupang.com.evil.kr`에 속는다. ★ **카카오는 `makers.kakao.com`만** — `kakao.com` 전체를 잡으면 톡스토어·선물하기 등 진행 방식이 다른 서비스까지 메이커스로 오판한다. 판정 실패 = 빈 값(채널이 틀리면 현금영수증 발행방법 이미지가 잘못 나감). 관리자 화면 2종에 **올리브영·카카오메이커스 버튼**, 버튼 없는 채널은 `직접입력`+custom으로 흡수 — ★ 회귀가드가 **판정하는 채널은 전부 버튼이 있어야** 함을 고정(직접입력 흡수에 기대면 관리자가 매번 손으로 확인해야 한다).
- **현금영수증 발행방법 이미지도 같은 4채널**(아래 항목 참조) — 채널 판정과 발행방법 안내가 갈리지 않는다.
- **③ 담당자**: 인트라넷이 보내는 **작업담당**(박세희/박은비/랜덤)을 `work_orders.work_manager`(065)로 받아 **박세희→만두 · 박은비→망고**로 매핑. ★ **랜덤·미매핑은 빈 값**(자동으로 아무나 배정 금지). 매핑 단일 출처 = `utils/workManager.js`(서버) + `index-app.js` 사본(프론트) — **회귀가드가 두 사본의 일치를 고정**. 인트라넷 수신은 표준키 `work_manager` + 별칭(`worker`/`work_staff`/`작업담당`) + 본문 `작업담당 : X` 폴백. 담당AE는 신규 컬럼 없이 `manager_name || created_by`로 표시.
- ★ **접수 시 `tab_configs.manager`도 매핑 닉네임으로 변경**(종전 `manager_name`=담당AE 실명 → 탭 설정 담당자 옵션이 만두/망고라 어긋났음). 매핑 실패 시 빈 값(틀린 값보다 빈 값).
- ★★ **grep 가드 맹점 실증**: 이 변경 때 기존 `woCampaignMapDoc` 가드는 옛 패턴(`rf_manager").value = prefill`)만 봐서 `_rfPickBtn` 배선 추가를 **놓치고 통과**했다 → 가드를 실제 호출 형태로 갱신. 배선 방식이 바뀌는 변경엔 패턴을 함께 갱신할 것. 회귀가드 `tests/workOrderAutoFill.test.js`(매핑·URL 판정 순수함수 실행 + 배선).
- ★★ **4칸 정리(유입/리뷰/특이/유의) 1차**: ① **유의사항(notes) 프리필 폐지** — 종전엔 "유입방식: ○○"+`review_guide` 원문 전체를 주입해 **공고 카드(참여 전 공개)에 상품번호·옵션:결제금액·내부 라벨까지 노출**됐다(실측: 장수돌침대 공고). 공개용 안내는 관리자 직접 작성(placeholder가 용도 안내). ② **리뷰가이드 메타 스트립** — `_woStripReviewMeta`(work-order-detail.js)가 `_woPickSections` 결과에서 유입 라벨·첨부 파일명/저장 URL·상품번호·`▶옵션:결제금액◀` **줄 단위** 제거(전문 폴백 케이스 포함). ★ **확실한 패턴만** — 서술형 문장('상품번호를 확인')·일반 URL·번호 목록은 보존(지우는 오탐 > 놓침). 관리자 상세 표시(`_woDetailHtml`)는 무접촉(원문 보존). 진단·개선안 = `docs/design-campaign-field-cleanup.html`·`docs/모집공고_4칸정리_개선_와이어프레임.html`, **근본 해결 = 인트라넷 리뷰오더와의 칸=칸 매핑**(`docs/prd-order-field-mapping.html` — 섞는 곳은 인트라넷 폼 `reviewOrderBuildReviewGuide` 한 곳, API·DB는 이미 1:1). **매핑 1단계(인트라넷 #267) = `review_guide` 순수화 완료**(조립 함수 폐기 — `pickSections`는 라벨 없으면 전문 그대로라 배포 순서 무관 가산적, 과거 blob 호환용으로 유지). **2단계(migration 090) = 첨부 이미지 `work_orders.guide_images`(TEXT·JSON 배열) 구조화**: 인트라넷이 URL 배열을 함께 보내고(`REVIEW_ORDER_FIELDS`+`guide_images`), 리뷰웹은 `_guideImagesJson`(https만·8장 상한)으로 정규화 저장 + `_woGuideImages`→`_woBuildInflowHtml`/`_woDetailHtml`이 **토큰 중복제거로 소비**(과도기 = 텍스트 블록·배열 동시 전송에도 이중 렌더 0). `REQUIRED_SCHEMA` 등록(없으면 접수 전면 42703). **3단계(인트라넷) = 리뷰오더 폼 칸별 목적지 힌트**("이 칸이 리뷰어 화면 어디에 나가는지") + 유입가이드 혼입 감지(경고 전용). 회귀가드 `tests/campaignFieldPrefill.test.js`(실측 원문 픽스처로 `_woCampaignPrefill` vm 실행 + INSERT 컬럼수≡파라미터수 카운트).
- ★★ **4칸 정리 개선 ③·④ = 🧹 정리 도우미**: 발행·수정 모달이 열릴 때/입력 중 오염을 감지해 칸 아래 ⚠ 경고(`rf_clean_inflow/review/notes` 컨테이너, `renderRecruitFieldCleanup`) — **게시는 절대 막지 않는다**(경고 전용). 정리는 항상 **전/후 미리보기(`rfCleanPreview`) → 사람 [적용](`rfCleanApply`)**(조용한 자동수정 금지). ★ **감지 = "정리 함수를 돌려보고 달라지는가"** — 정리 규칙 단일 출처는 `_woStripReviewMeta`/`_woPickSections`(work-order-detail.js)이고 관리자 카드 `🧹 본문 정리` 배지(`campaign-cards.js` `_campNeedsFieldCleanup`, `CampCards.needsFieldCleanup`)도 같은 함수를 쓴다(정리 함수 없는 리뷰어 페이지는 무조건 false = fail-soft). ★ **유입가이드 원본 HTML 모드(rawHtml)는 값 수정 제안 없음**(이미지 유실 방지 — 안내만). 유입→특이사항 이동은 키워드(주소·연락처·입금류) 조각만 분리(`_cleanInflowSplit`, 줄·`/` 단위). 수정 모달을 열면 같은 감지가 돌아 **기발행 공고 소급**이 클릭 한 번이 된다. 회귀가드 `tests/campaignFieldCleanupUI.test.js`(감지·분리 vm 실행 + 미리보기 게이트·비차단·rawHtml 보호 고정).

### 리뷰어 앱 공고수정 권한 (마스터 허용명단 · 스코프 토큰)
- 리뷰어 홈(`index.html`)은 무비밀번호 로그인(이름+전화 뒤8자리, `verifyReviewer`)이라 관리자 로그인보다 약하다. 그래서 리뷰어 화면에서 공고 카드를 눌러 인라인 수정(`campaign-cards.js` `openAdminEdit`)할 수 있는 사람을 **마스터가 phone8 허용명단(`reviewer_campaign_editors`, migration 059)으로 명시**한 사람만으로 제한한다.
- **발급 이중 게이트**(`reviewerCampaignEditor.service.js` `issueCampaignToken`): ① `verifyReviewer(name,phone8)` 실 리뷰어 신원 재검증 AND ② active 명단 → 통과 시 **`{role:'admin', via:'reviewer_campaign'}` 2h 토큰** 발급(role은 마스터라도 admin으로 하향=최소권한). `POST /api/reviewer/campaign-token`(무인증 공개, `campaignTokenLimiter` 20/분, 미허용=401 동일문구로 열거 방지).
- **격리(핵심)**: `authMiddleware`가 `via:'reviewer_campaign'` 토큰을 **`PUT /api/campaign/admin/:id`(공고 수정) + `POST /api/product/preview`(상품정보 자동수집·읽기전용) 로만** 허용(정규식, `via:'intranet'` 격리 선례와 동일 폐쇄기본) — 공고 생성/삭제/확정·`/status`·`/api/admin/*`(명단 자기증식 불가)·Track B·주문원장 전부 차단. 프리필용 `GET /api/campaign/:id`는 authMiddleware 미경유 공개라 무관(단 via='reviewer_campaign'은 `_scopedEditorView` 축약뷰만). product/preview는 임의 URL 서버 fetch라 `_isBlockedHost`(사설·루프백·링크로컬 IP 리터럴·localhost 차단) SSRF 가드 보강.
- **관리 UI**: 관리자 설정탭 "리뷰어 앱 공고수정 허용"(마스터 전용, `GET/POST /api/admin/campaign-editors` = authMiddleware+masterOnly). 프론트 = 로그인 시 `maybeFetchCampEditToken`로 토큰 받아 `sessionStorage.rapp_camp_edit_token`(로그아웃 시 제거), `campaign-cards.js` `_adminTok`가 병합. 회귀가드 `tests/reviewerCampaignEditor.test.js`.

### 참여형 캠페인 상품옵션 (옵션별 정원·일일한도·리뷰어 자율선택) — 1단계 백엔드 (PRD: `frontend/docs/prd-campaign-options.html` v1.1)
- **모델**: 상품이 여러 옵션(예: 힙스 골라담기/콰이어트/어나더)일 때, 리뷰어가 **참여 시 옵션을 자율선택**하고 옵션별로 정원·하루한도를 관리. **참여형(participation_mode) + 옵션 2개 이상 등록 시에만 활성화(opt-in)** — 옵션 미등록·레거시·기존 구매양식 시트 옵션 피커는 동작 불변. migration 061: `campaign_options`(캠페인 1:N — `opt_key`=옵션명 겸 시트 옵션열 기입값, `pay_amount`·`recruit_total`(0=무제한)·`daily_limit`(0=옵션 일일제한 없음)·`status` active|closed) + `campaign_applications.option_key`(NULL=옵션 없는 공고).
- **자리 계산**(`campaignState.service.js`, 순수함수 `computeOptionView`): 옵션 잔여 = `recruit_total − (제출확정 + 유효홀드)`, 오늘 잔여 = `daily_limit − (오늘 제출 + 오늘 유효홀드)`. 유효홀드 = `applied AND expires_at>now`(시각 기준). 상태 = open/soldout/today_done/closed, `selectable`=옵션 open **AND** 캠페인 open. `fetchOptionCounts`는 `option_key` GROUP BY(`option_key IS NOT NULL`). 회귀가드 `tests/campaignOptions.test.js`.
- **동시성(핵심)**: apply·change-option·옵션저장이 **모두 `recruit_campaigns` 행 FOR UPDATE 안에서** 옵션 카운트를 재집계 → 옵션별 소진 경합(write-skew) 원천 차단(캠페인 단위 직렬화). 옵션소비 증가 writer는 apply·change-option 둘뿐(제출확정은 순증0, 스윕은 반환).
- **참여(apply)**: 옵션 등록 캠페인은 **선택 필수**(전부 마감이어도 `option_key=NULL` 우회 금지 → `option_unavailable`), 미선택=`option_required`, 소진=`option_soldout`/`option_today_done`. 홀드 INSERT에 `option_key` 기록.
- **옵션 변경**(`POST /:id/change-option`, phone8+holdToken 이중열쇠): 구매(제출) 전까지 잔여 남은 다른 옵션으로 변경. **만료시각 미연장**(자리 갈아타기 악용 방지), 새 옵션 자리 확보 실패 시 기존 유지(원자 조건부 UPDATE `status='applied' AND expires_at>NOW`). 전환 대상 잔여는 자기 홀드 제외 계산(자기 홀드는 기존 옵션에 계수).
- **제출 서버권위**(`submit.routes.js`): 홀드에 저장된 `option_key`를 `selectedOptKey`로 override → 행배정·시트기입·dedup 모두 이 값 기준(화면 값 조작 방지). fail-open(옵션없는 홀드·조회실패=클라값 유지). 시트 기입은 기존 `selectedOptKey` 경로 그대로 재사용(옵션열에 opt_key 기입, 같은 옵션 빈 행 우선 배정).
- **노출**: 공개 `GET /:id`는 **옵션명+잔여만**(`_publicOptionView` — payAmount 등 상세 제외, "금액은 참여 후 공개" 원칙), work-detail(홀드 게이트 뒤)은 옵션 금액+선택옵션+`canChangeOption` 반환. 공고 저장 `_saveCampaignOptions`는 **자체 트랜잭션+캠페인 행 FOR UPDATE**(apply와 상호배제 → 참여자 있는 옵션 삭제 TOCTOU 봉합), 참여자 있는 옵션은 삭제 대신 `closed`. 리뷰어앱 공고수정 스코프 토큰(`via:'reviewer_campaign'`)은 PUT 진입 즉시 `_scopedCampaignEdit`로 분기 → **옵션 편집 불가**(정원/금액 변조 차단). 회귀가드 `tests/campaignOptionsGuards.test.js`.
- **운영 잔여(문서화된 한계)**: ① 옵션명 변경은 "신규옵션+기존closed"로 처리 → **모집 중 옵션명 변경 지양**(진짜 rename은 후속). ② `opt_key`는 시트 옵션열 값과 **정확 일치**해야 시트 기입/로스터 매칭됨(내부 공백 유지, 불일치 시 주문 유실 없이 빈 행 폴백 + 옵션 표시만 소실) — 게시 전 자동점검 대조는 후속(3단계). ③ 다단계 조합옵션(색상×사이즈, `|` 다중열)은 범위 밖(`_normOptKey`가 `|` 제거=단일선택). ④ 모집 중 옵션 추가 시 기존 `option_key=NULL` 홀드는 옵션 정원 미계수 → 옵션 추가는 모집 시작 전 권장. ⑤ change-option↔제출 동시 TOCTOU는 **2단계 UI로 봉합됨**(옵션변경 성공 시 `enterJoined` 재조회가 구매양식 iframe을 재로드 → 인플라이트 제출 파기, 같은 페이지라 동시 진행 불가).
- **2단계 완료(리뷰어 화면)**: `campaign.html` — 참여 전 옵션명+잔여 읽기전용 표시(`renderPreOptions`, `GET /:id`.options), 참여 시 옵션 선택 바텀시트(`openOptSheet`→`_doApply(optionKey)`, 옵션 1개=자동·2개+=선택 필수·소진 사유 재선택 유도·전부 마감 시 참여버튼 "옵션 마감"), 참여 후 "내가 참여한 옵션" 카드+변경(`renderJoinedOption`·`openChangeOptSheet`→`_doChangeOption`→`enterJoined` 재조회), work-detail의 `selectedOption`/`canChangeOption`/`application.optionKey` 사용, iframe URL에 `optionKey` 전달. `search-app.js` embed = `_EMBED_CTX.optionKey`면 `_lockEmbedOption`(시트 옵션 피커 무시·"참여한 옵션" 잠금표시·`_selectedOptKey` 설정으로 제출검증 통과)·참여형 카드추가 버튼 숨김(참여 1건=제출 1건). `campaign-cards.js` 카드 옵션 요약칩(`_optChip` — 옵션 N종·마감/임박). `GET /list` 배치 옵션(`_fetchOptionsForCampaigns` 2쿼리, 5초 캐시 상각) → 카드에 옵션명+잔여(`_publicOptionView` 금액 은닉). 회귀가드 `tests/campaignOptionsFrontGuards.test.js`.
- **3단계 완료(관리자 화면)**: 공고 모달(`admin.html` `#rf_opt_rows`) 옵션표(옵션명·금액·정원·하루건수) — `index-recruit.js` `addOptRow`/`renderOptRows`/`readOptRows`(파이프 제거·빈옵션 제외)·`_optSummary`(정원합/하루합/중복 자동점검), 저장 시 `payload.options`(**옵션표 UI 있는 페이지에서만 전송** — 없으면 미전송=기존 옵션 유지, 옵션 소실 방지)+중복 하드블록, 편집 프리필 `renderOptRows(json.options)`(관리자 `GET /:id`가 원본 옵션 반환). 작업오더→발행 프리필: `index-app.js` `_woOptionRows`(`product_options_json`→옵션행, **2개 이상만**·정원/하루는 관리자 입력)→`prefill.options`. 관제: `GET /admin/:id/applications`가 `option_key`+옵션 뷰(`_loadOptionViews`, 금액 포함=관리자 전용) 반환 → `_campOptionTable`(옵션별 오늘 진행중/제출/누적확정/정원/상태, 신청행 기반 오늘 집계). 회귀가드 `tests/campaignOptionsAdminGuards.test.js`.
- **다음 단계(선택)**: 게시 전 옵션명↔시트 옵션열 대조(불일치 경고), 다단계 조합옵션, 진짜 옵션 rename 엔드포인트.

### ★★ 옵션 없는 작업이 "옵션 공고"가 되어 잠기는 사고 — 원인 확정 + 입력칸 분리 계획 (2026-08-07 우레온 건)
- **증상**: 작업오더는 옵션 없음인데 발행된 공고에 옵션 1종(`우레온 모이스처 바디` = 상품명 조각)이 생겼고, 그 옵션이 소진·마감되자 apply 게이트가 `option_unavailable`로 **공고 전체를 영구 잠금**(오늘 정원 5명 중 3명에서 정지). 관리자가 옵션명을 비워 저장해도 안 풀림.
- **원인 3겹(전부 실행 재현으로 확정)**: ① **인트라넷 리뷰오더는 옵션유무를 신호로 안 보낸다** — `product_options_json`을 **전송하지 않아 항상 빈 값**(리뷰웹 `_woOptionRows`의 JSON 경로는 인트라넷 오더에서 dead path), 옵션 정보는 `reviewOrderProductSummary`가 만든 **요약 문장**(`product_option`)뿐 → 리뷰웹은 문장을 구분자(`- – / | ·`)로 쪼개 **추측**(`parseProductLinesToRows` — 상품명 속 하이픈·빗금·가운뎃점이면 뒷조각이 옵션명으로 승격. 8/3 보강은 "상품명과 같음/접두"만 차단이라 자동수집 짧은 상품명 vs 문장 긴 상품명처럼 **표기가 다르면 여전히 뚫림**). ② **`_woProductLines`가 옵션 없는 줄을 오독** — 인트라넷 옵션 없는 상품 줄 `- 결제금액 22,000원 / 5명 / 소계…`의 첫 `/`-조각(`결제금액 22,000원`)을 옵션 라벨로 읽어 **`상품명 결제금액 22,000원 - 결제금액 22,000원`** 오염 문장을 생성(실측 화면과 바이트 일치 재현). ③ **한 번 생긴 옵션은 화면에서 제거 불가** — `readOptRows`가 빈 옵션명 행을 미전송(index-recruit.js:987) + `_saveCampaignOptions`가 참여자 있는 옵션을 closed로 보존 + apply 게이트(campaign.routes.js:1065)가 **closed 포함 `allOpts.length`로 "옵션 공고" 판정** → closed-only 공고 = 참여 전면 거부.
- **계획 문서 = `frontend/docs/모집공고_옵션입력_분리_와이어프레임.html`**(비개발자용, 모호점 ㉠~㉥ 대조표 포함). 구현 4순: ① `_woProductLines` 옵션없는줄 오독 수리(결제금액 라벨 차단) ② **apply 게이트 — closed-only = 옵션 없는 공고로 참여 허용**(판정을 `allOpts.length` → active 기준으로. ★ soldout(active인데 만석)은 종전대로 차단 — "전부 마감이어도 우회 금지" 원칙의 '마감'은 soldout이고, closed는 관리자가 옵션 구조를 정리한 상태라 의미가 다름. 카드 옵션칩·`renderPreOptions`도 같은 기준) ③ **진행상품 입력 2모드 분리** — [옵션 없는 작업] = 상품명/결제금액/총인원/일건수(옵션명 칸 자체가 없음 = 사고 입구 제거) / [옵션 있는 작업] = 상품행(상품명/총인원=자동합계) + 옵션행(옵션명/결제금액/옵션인원/일건수), 모드는 자동 제안(오더 옵션 신호·옵션 원장) + 사람 전환 자유, 전환 시 값 이월·confirm ④ **인트라넷이 `product_options_json` 동봉**(reviewOrderCollectProducts 구조 그대로 — 옵션별 count → 옵션인원 자동 프리필, 양방향 가산적이라 배포 순서 무관).
- **즉시 조치(배포 전 우회)**: 수정 모달 → 흐린 마감 옵션 행 [↩ 재개] → 옵션인원 0(무제한)·일건수 0 → 저장 → 목록 캐시 5초 후 반영(옵션 1종 = 리뷰어 자동 선택).
- ★ 구현 시 유지할 기존 원칙: 옵션표 없는 화면 미전송=유지 · 참여자 있는 옵션 closed 보존 · soldout 우회 금지 · 기존 정상 옵션 공고 무변화.
- **구현 완료(4순 전부)**:
  - ① **`_woProductLines` 오독 수리** — 옵션 없는 줄의 첫 `/`-조각에서 `결제금액 N원`·`소계 N원`을 걷어내고 **남는 글자가 있을 때만** 옵션 라벨로 인정(숫자만 남은 조각도 제외). 오염 문장이 발행 표의 재료가 되던 경로 차단.
  - ② ★★ **옵션 공고 판정 = `campaignState.liveOptions` 단일 출처**(`status !== 'closed'`): apply 게이트가 `allOpts.length` → `liveOptions(allOpts).length` 로 바뀌어 **closed-only 공고는 옵션 없이 참여**(`option_unavailable` 제거). ★ **soldout·today_done 은 liveOptions 에 남아 종전대로 차단**(완화 아님 — "전부 마감이어도 우회 금지"의 '마감'은 자리 소진이고, closed 는 관리자가 옵션 구조를 정리한 상태). 공개 목록·상세도 **같은 판정으로 옵션 미노출**(카드 `_optChip`·`renderPreOptions` 는 서버 응답만 따르므로 프론트 무수정 = 사본 0). 관리자 뷰(`_loadOptionsRaw`)는 무변경(재개 경로 보존).
  - ③ **진행상품 입력 2모드**(`rf_prod_mode` = `none`|`opt`): ★★ **모드가 옵션 유무의 단일 출처** — `readOptRows`·`_readProdRows` 둘 다 모드를 보고 판단해 숨은 칸의 잔여값이 옵션·작업내용 원문으로 새지 않는다. 행 DOM(`.rf-opt-row` + 5칸)은 **두 모드 공통**이라 저장 계약 무변경, 보이는 칸만 CSS(`.rf-pm-none/.rf-pm-opt`)로 바뀐다. `opt` 모드는 상품 그룹(`.rf-gp` 머리 = 상품명 + **옵션인원 자동합계**) + 들여쓴 옵션 행. 자동 판정 = 살아있는 옵션 유무(②와 같은 기준) + **사유 문구 표기**(조용한 자동선택 금지) + 사람 전환 자유, `opt→none` 전환은 **정리될 옵션 수를 말하는 confirm**. `_readProdRowsRaw`(모드 무시)로 전환 시 값 이월.
  - ★★ **옵션 원장이 비면 텍스트 추측을 옵션으로 승격시키지 않는다** — `renderOptRowsWithProduct([], …)`·`applyProductRowsFromOrder`(옵션 배열 없음)는 `parseProductLinesToRows` 가 쪼갠 조각을 **상품명으로 되붙이고** `mode:'none'` 강제. 상품명 속 하이픈·빗금이 옵션이 되던 마지막 구멍(㉣)이 닫힌다.
  - ④ **인트라넷 옵션 구조 신호**(`reviewOrderOptionsPayload` → `product_options_json`, `REVIEW_ORDER_FIELDS` 합류): `[{name,url,base:{pay,count},options:[{label,pay,count}]}]` — **`options:[]` 가 곧 "옵션 없음" 확정 신호**. 시트참조 모드·값 0건은 **미전송**(빈 구조로 "옵션 없음"을 단정하지 않는다 = 종전 경로). 요약 문장(`product_option`)은 그대로 유지(사람 읽기·구버전 호환) → **양방향 가산적, 배포 순서 무관**. 리뷰웹 `_woOptionRows` 가 옵션별 `count` 를 **옵션 정원(`recruitTotal`)으로 프리필**(하루건수는 오더에 없어 종전대로 관리자 입력).
- 회귀가드 `tests/campaignProdModeSplit.test.js`(**가짜 DOM 위에서 표 함수 vm 실행**: 모드별 저장 페이로드·원문 파생·자동판정·전환 이월/확인창 + 인트라넷 구조 조립 실행 + 배선). ⚠ 이 변경으로 `campaignOptionsGuards`(옛 `option_unavailable` 기대) · `campaignOptionsFrontGuards`(list 옵션 노출 패턴) 2종을 **사용자 확정 규칙으로 갱신**했다 — 되돌리면 우레온 잠금이 재현된다.

### 타계정 추가참여 (§09 확정 2026-07-27) — 1단계 서버 규칙 완료 (migration 063)
- 참여형 공고에서 로그인 리뷰어가 자기 타계정(`reviewers.sub_accounts`) 명의로 같은 공고에 추가 참여하는 기능. PRD·유저플로우·와이어프레임 = `frontend/docs/prd-multiaccount-participation.html`(v1.1 확정, 일반인용 무개발용어). **확정 정책**: 공고별 [가능/불가] 토글(`recruit_campaigns.multi_account_mode` **기본 FALSE**), 명의당 1건, 타계정 단독참여 허용, 소유자 합산 동시홀드 10건(`CAMPAIGN_OWNER_HOLD_CAP`), 타계정 건 TTL 10분(`sub_hold_ttl_min`, 본계정은 `hold_ttl_min` 15분), 타계정 하루한도(`multi_daily_limit` 0=무제한), 카드 "타계정 가능" 배지, 설정=공고 발행 때.
- **★ 핵심 설계(레드-블루-심판)**: 홀드는 계속 **"명의 phone8"로 키잉** — `uq_campaign_apps_active_hold(campaign_id, phone8)` **무변경**(§02 "누가 등록한 명의든 한 번만")이라 work-detail·cancel·change-option·`confirmHoldInTx`·수동확정 5개 경로가 무수정 정합. 소유자 귀속은 신설 `campaign_applications.owner_phone8`(NULL=레거시 → 판정은 `COALESCE(owner_phone8, phone8)`). 같은번호 공유 타계정(A안)은 `sub_shares_owner_phone`으로 거부(phone8=검색·행배정·입금대사 신원키 보호).
- **잠금 계층(고정·교착 불가)**: `recruit_campaigns` 행 FOR UPDATE → **owner advisory**(`camp_hold_owner:`) → **명의 advisory**(`camp_hold_phone:`) → INSERT. 클래스 순서 전역 고정 + tx당 클래스별 1락 = 순환대기 불가. apply 사유코드: `multi_disabled`·`sub_invalid`·`sub_shares_owner_phone`·`sub_not_registered`·`owner_hold_cap`·`sub_daily_limit`. 명의 검증 = `identity.service.findSubAccount`(SUB 판별과 동일 정규화 — apply↔submit 드리프트 금지), 홀드 표기값은 **서버 저장 sub_accounts 값 우선**(요청 오타 무시). 기존 per-명의 `CAMPAIGN_HOLD_CAP=2` **불변**(레거시 완화 없음). 타계정 하루한도는 **상태 무관 집계**(취소 리트라이 파밍 차단, `already_today`와 동일 시맨틱).
- **apply 응답 계약(레드 #4)**: `phone8` = **명의 p8**(프론트 `h.phone8` → work-detail/cancel/change-option/embed `holdPhone8` 전 경로 무수정), `ownerPhone8`·`participant{type,name}` 추가. submit은 `_campaignHoldCtx(b, loginPhone8)` **단일 출처**로 통일(인라인 중복 제거) + **owner 폴백**(서브 로그인 세션이라 `reviewers` 행이 없으면 홀드 `owner_phone8`로 소유자 행 역조회 → 신원게이트 조용한 스킵 봉합, SUB 자동보강도 게이트 기준 행에 기록). `confirmHoldInTx`는 `expectedOptKey` 받아 **옵션 드리프트 warn만**(비차단 백스톱). 곁다리 봉합: `_saveCampaignOptions`가 status 미지정 시 `COALESCE($7, campaign_options.status)`로 **closed 옵션 조용한 재오픈 차단**. my-status는 `phone8 OR owner_phone8` 병합 + `isSub`만 반환(**무인증 API라 ownerPhone8 원문 미노출**). 회귀가드 `tests/campaignMultiAccount.test.js`.
- **2단계 완료(리뷰어 화면)**: `campaign.html` 홀드 저장 **v2 = 명의별 맵**(`camp_holds_<id>` = {명의p8: hold} + 활성 포인터 `camp_active_<id>`). ★**구키 `camp_hold_<id>`는 읽어서 승격만 하고 `_allHolds`가 절대 지우지 않는다**(당일 재참여 0회라 토큰 유실=참여권 소각) — v2 파싱 실패 시에도 구키로 복구. `getHold(p8?)`(무인자=활성)·`clearHold(p8?)`(활성/지정 **1건만** 제거, 나머지 명의 보존)로 기존 호출부 8곳 무수정. 어제 홀드 폐기는 맵 전체 순회. PREVIEW 격리는 쓰기 경로 4곳(`_saveHolds`·`setHold`·`clearHold`·`setActiveP8`) 전부 유지.
  - **UI**: 명의 선택 시트(`#acctSheet` — 본계정+타계정, 상태=참여중/같은번호/오늘불가 클라 추정 + **서버 409 사유를 그 명의에 인라인 잠금**(`_acctBlocked`) 후 재선택 유도, 자리 미점유), 미등록 시 `#acctEmpty` → `index.html?returnCamp=<id>#my`(형식검증 `^[A-Za-z0-9_-]{1,64}$` = 오픈 리다이렉트 차단, index가 내정보 상단에 복귀 배너). 참여 후 `#acctBox`(명의 카드 + 2건 이상이면 전환 칩) + `#addSubBtn`(＋타계정으로 추가참여하기). 제출 완료 시 **그 명의만** `clearHold(p8)` 후 남은 명의 "이어서 진행" CTA.
  - **★ 단일 `#orderFrame` 불변식 유지**: 명의 전환(`switchAcct`)도 활성 포인터만 바꾸고 `enterJoined` 재실행 = iframe 재로드로 인플라이트 제출 파기(옵션 TOCTOU 봉합과 동일 장치). iframe은 여전히 1개(가드가 고정).
  - **무영향 보장**: `multi_account_mode=false`거나 타계정 0개면 `_joinWithOption`이 `_doApply` 즉시 호출 = 기존 경로 그대로(시트·버튼 미표시). 타계정 목록은 기존 무인증 `POST /api/reviewer/profile{action:'get'}` 재사용(**신규 엔드포인트 0**). 회귀가드 `tests/campaignMultiAccountFront.test.js` + 홀드 v2 저장 로직 브라우저 실행검증 시나리오(구키 파기 금지·다중홀드·PREVIEW 격리·파싱실패 복구·타 캠페인 미오염).
- **보안·안정성 방어(2차 레드-블루-심판, 스키마 변경 0)**: ① **사칭 차단** — `sub_accounts`는 무인증 `POST /api/reviewer/profile{saveSubAccounts}`로 덮어쓰는 자기신고 배열(번호 소유 증명 없음)이라, **이미 리뷰어로 등록된 번호는 타계정 명의로 사용 불가**(409 `sub_is_registered_reviewer`, 그 번호 본인이 직접 로그인해 참여). `reviewers.phone8`은 GENERATED·비유니크라 행 동일성 아닌 **EXISTS** 판정. 완화 스위치 `CAMPAIGN_SUB_REGISTERED_POLICY`=block(기본)|warn|allow. ② **이력 선검사 1쿼리화**(done+hist 병합, 왕복 순증 0) + 신규 사유 `blocked_by_other_owner`(타 소유자 선점=사칭 신호 warn)·`same_phone_other_name`(같은번호 다른명의가 phone8 키잉으로 접힌 원인 설명 — **이름 공개는 요청자가 근거 보유 시에만**). ③ **명의 드리프트 경고**(`detectIdentityDrift` 순수함수 — 타계정 홀드인데 주문 연락처가 다르면 warn, **확정은 절대 비차단**; 자기참여·레거시는 무신호=오탐 0). ④ **홀드 서버권위**(`_authoritativeHold` — 클라 `holdPhone8`은 힌트, 명의는 `hold_token`(24B)+id+campaign_id로 서버가 확정·보정. 구버전 프론트/회귀로 phone8이 어긋나면 확정·provenance 링크가 동시에 실패해 "결제했는데 관제에 흔적 0"인 고아 주문이 되던 경로 봉합. 옵션 권위 쿼리를 흡수해 **왕복 순증 0**, fail-open). ⑤ **마이그레이션 오분류 차단**(`err.message.includes('duplicate')` → **PG 에러코드 5종**만 '적용됨' 기록; 23505 데이터 실패를 영구 적용됨으로 오기록하던 함정 제거) + **필수 스키마 프리플라이트**(063 컬럼 4종 없으면 `process.exit(1)` — Railway가 직전 배포 유지 = 진짜 fail-closed. **DB 조회 실패는 부팅 계속**(크래시루프 방지), 탈출구 `ALLOW_SCHEMA_DRIFT=1`) + **마이그레이션 `lock_timeout`/`statement_timeout`**(`set_config` 바인딩, 55P03·57014는 미기록 재시도 — 오픈 러시 중 ALTER가 락 큐 선두에서 전 요청을 정지시키는 것 방지, 실패 커넥션은 `release(true)` 폐기). ⑥ 관리자 폼 **타계정 하루한도 기본값 1**(0=무제한은 여전히 저장 가능하나 자동점검이 ⚠경고 — 가장 약한 값이 기본이 되는 footgun 제거). 회귀가드 `tests/campaignMultiAccountDefense.test.js`(, 스텁 pool로 실제 핸들러 호출)·`tests/campaignRouteRuntime.test.js`(구조분해 누락=500 재발 방지).
- ★ **grep 회귀가드의 맹점**: 문자열 존재만 보는 테스트는 **스코프·선언·실행경로를 못 본다**. 실제로 063이 admin update 라우트의 `req.body` 구조분해에 3필드를 빠뜨려 **공고 수정 저장이 전부 500**이었는데 SQL grep 가드는 통과했다(핫픽스). 라우트 파라미터·핸들러 스코프가 걸린 변경은 **런타임 실행 가드**를 함께 둘 것.
- **잔여위험(문서화)**: ① 소유자가 **미등록** 타인 번호를 서브 등록하면 그 명의를 선점 가능(등록번호는 ①로 차단) — PRD §02 "누가 등록한 명의든 동일 적용"의 귀결, 남용 시 관제 `owner_phone8`로 추적. ② 무인증 `/api/reviewer/profile{action:'get'}`이 phone8만으로 주민번호·계좌·sub_accounts 전체를 반환하는 구조적 문제는 **별건**(my-status 마스킹은 보안연극이라 미적용). ③ 소유자 당일 총량 상한은 명의별이라 명의를 늘리면 우회 가능(동시 10건·타계정 하루한도로 억제).
### 타계정 다건 일괄 제출 (batch) — 5번 반복 → 한 화면 (스키마 변경 0)
- **배경(실사고)**: 리뷰어가 타계정 5건 참여를 시도하다 **30분 걸려 포기**(카톡 피드백 실물). 원인 ① 명의당 [참여→구매→캡처1장→양식→제출] 5회 반복 ② 작성 중 새로고침하면 화면이 되돌아가고 **첨부 캡처가 소실**. 개선 제안서·목업 = `frontend/docs/타계정_다건참여_일괄제출_개선안.html`.
- ★★ **묶는 것은 "제출"이지 "구매"가 아니다**: 홀드 TTL(본계정 `hold_ttl_min` 15분 / 타계정 `sub_hold_ttl_min` 10분)은 **그대로**다. "5건 다 사고 와서 한 번에"를 UI가 부추기면 후행 건이 전부 지각(`late_order_id`)이 되어 관제 수동확정이 폭증한다 → **만료 홀드는 스냅샷에서 제외 + 카드에서 잠금 + 페이로드에서 제외**, 참여가 2건 이상이 되는 순간 "구매를 먼저 끝내고 마지막에 한 번에 제출하세요" 경고. **TTL 상향은 하지 않았다**(서버 정책 = 사용자 결정 사항, 아래 잔여위험 ①).
- **서버 변경은 멱등 게이트 1개뿐**(스키마 0, 신규 엔드포인트 0): 요청 1건 = 홀드 1건이라는 기존 계약(`_campaignHoldCtx`+`_authoritativeHold`)을 그대로 쓰고 **카드마다 자기 홀드 문맥을 실어 순차 전송**한다. `_authoritativeHold`가 같은 왕복에서 `status`·`order_submission_id`·`late_order_id`(+주문 생존 LEFT JOIN)를 함께 읽어 **이미 접수된 홀드면 `{ok:true, alreadySubmitted:true, campaignHold:'confirmed'|'late'}`로 즉시 반환**(왕복 순증 0).
  - ★★ **`late` 경로를 반드시 포함**: `confirmHoldInTx`의 late UPDATE는 `late_order_id IS NULL` 조건이라, 지각 건을 두 번째로 재제출하면 **링크조차 못 남기고 주문만 새로 생긴다**(관제에 흔적 0인 고아 주문). `status='submitted'`만 보면 이 경로가 뚫린다.
  - ★ **`ok:true`로 돌려주는 게 핵심** — "실패"로 보이면 리뷰어가 또 누른다. ★ **fail-open 유지**(조회 실패 시 `doneKind` 없음 = 기존 경로). ★ 레거시·비참여형·nc모드·관리자 경유는 `holdCtx`가 null이라 게이트에 도달조차 안 함.
- **부팅은 fail-closed 7조건 AND**(`_batchBoot`, `search-app.js`): embed && **미리보기 아님** && **nc모드 아님** && `batch=1` && 스냅샷 존재 && `2 ≤ 홀드수 ≤ MAX_ORDER_CARDS(5)` && 전 홀드 `app`·`holdToken`·8자리 `phone8` 보유 && 활성 app이 스냅샷에 포함(stale 거부). 하나라도 어긋나면 `null` = **기존 단건 동작 100% 불변**. 어떤 예외도 단건으로 수렴.
  - ★ **배치 여부는 iframe이 보내는 `batch-mode` ack로만 인정**(campaign.html). 부모 HTML만 새 버전이고 iframe JS가 캐시된 구버전인 **배포 스큐**에서 부모가 혼자 "5건 냈겠지"로 화면을 정하면 리뷰어가 1건만 낸 채 이탈한다(= 이 사고의 재현).
  - 스냅샷 = `sessionStorage.camp_batch_<campId>`(부모가 iframe src 세팅 직전 기록, **만료 임박 오름차순**). 저장 실패 = batch 미설정 = 단건. **옵션 공고인데 `optionKey`를 모르는 홀드**(이 배포 이전 생성)가 섞이면 배치 금지(틀린 옵션을 사게 하느니 단건).
- ★★ **자동배정(순서 폴백)을 리뷰어 화면에 옮기지 말 것**: 관리자 `manual-order.js`의 `distributeStaged`는 이름/뒤4자리 실패 시 **붙인 순서**로 배정하는데, 그건 **표를 눈으로 대조할 사람이 있어서** 안전했다. 리뷰어 화면엔 없다. 캡처가 남의 홀드로 가면 **"남의 명의로 주문 접수"**이고 **서버는 이를 막지 않는다**(`resolveOrderIdentity`는 "내 계정 중 하나인가"만 봐서 같은 소유자의 타계정 A↔B 교차는 SUB로 통과, `detectIdentityDrift`는 warn만). → 일괄 첨부는 **고른 순서대로 위 카드부터 부착(화면에 명시)** 하고, AI 추출 수취인 ≠ 그 카드 명의면 카드에 경고 + [○○ 카드로 옮기기]/[제가 확인했어요] 중 **사람이 고르게** 한다. 확인 안 된 불일치가 남으면 `_batchIdentityGuard`가 제출을 막는다. `manual-order.js`의 순서 폴백은 **그대로 유지**(가드가 생존을 고정).
- **배치 전용 카드 보정 3종**(전부 실제 사고 예방): ① **삭제 버튼 제거** — 카드가 사라지면 홀드↔카드 매핑이 어긋나 그 명의가 조용히 누락된다. ② **"1번과 동일" 기본 해제** — 기본 체크면 2~5번 카드의 **주문자**가 1번 값으로 잠기는데 서버 신원게이트는 `orderer`를 검증하지 않아 잘못된 명의가 그대로 시트에 기록된다(대신 제출 직전 **빈 계좌칸만** 1번에서 복사 = 타계정 리뷰비를 본인 공통계좌로 받는 정상 흐름 보존). ③ **옵션 잠금 전용 경로**(`_lockEmbedOptionBatch`) — 전역 피커를 숨기는 것만으로는 부족하고 `_selectedOptKey`가 null이면 `submitOrderForm`이 **제출을 통째로 차단**한다.
- **결과는 명의별 배열**: `_embedLastCampaignHold`(루프마다 덮어써지는 단수 값) **삭제** — 5건 중 4건이 지각이어도 "참여 확정!"이 뜨던 경로다. `_embedPost({results})` → 부모 `renderDoneBatch`가 명의별 한 줄로 표시하고 **성공한 명의만 `clearHold`**(실패분만 [이어서 진행] CTA 대상 = 재제출 유도 차단). 부분 실패는 "실패한 N건 다시 제출" 버튼 + `_BATCH.done[app]`으로 성공분을 건너뛴다(서버 멱등이 최종 방어 — `done`은 sessionStorage라 탭을 닫으면 사라진다).
- **새로고침 복원은 명의(phone8) 앵커**: 기존 `_embedSaveForm/_embedRestoreForm`은 **배열 인덱스 위치** 기반이라 홀드 1건이 만료돼 카드 수가 줄면 **2번 카드 자리에 3번 명의의 수취인·주소가 들어간다**(= 남의 명의 주문). → 배치는 `_batchSaveForm/_batchRestoreForm`(키 `embedBatchForm_<campId>`)이 담당하고 **위치 기반은 배치에서 조기 반환**으로 끈다(단건 경로는 무수정·무변경). **텍스트와 사진은 다른 키**(`embedBatchCap_<campId>`) — 한 키에 담으면 사진 용량 초과가 텍스트 저장까지 죽인다. 사진은 첨부 시점에 **1회만** 축소(canvas 1080px/JPEG0.6)하고 2.5MB 예산 초과분은 **저장 시점에 `dropped`로 세어** 배너가 "사진 N장은 용량 때문에 복원하지 못했어요"라고 정직하게 말한다(`_ssWrite`가 저장 후 되읽어 조용한 실패도 잡는다).
- **find-slot 왕복은 배치에서 생략** — 서버가 항상 `{mode:'append'}`를 즉시 반환하는 no-op인데 카드당 최대 15초 타임아웃이라 총 제출시간이 홀드 TTL을 그대로 갉아먹는다.
- 회귀가드 `tests/campaignBatchSubmit.test.js`(**스텁 pool로 멱등 게이트 실제 실행**(확정/지각/soft-delete/applied/조회실패/레거시 6분기 + 원장 미호출 스파이) + 배선 + 자동배정 부재 + manual-order 무회귀) + **실제 http 오리진 브라우저 실행검증 시나리오**(카드↔홀드 결속·페이로드 홀드 분리·fail-closed 6조건·만료 제외·재제출 스킵·명의 앵커 복원). ⚠ 이 변경으로 기존 grep 가드 5종의 패턴이 드리프트해 함께 갱신했다(검사 의미는 불변) — **배선 형태가 바뀌는 변경엔 패턴을 함께 갱신할 것**.
- **잔여위험(문서화)**: ① 🔴 **TTL vs 일괄은 해결되지 않았다** — 10~15분 안에 5건 결제는 물리적으로 불가하다. 이 기능은 **이미 유효한 홀드들의 제출을 묶는 것**까지만 하고 만료 건은 잠근다 → 지각은 줄지만 0이 아니다. 근본 해결은 `sub_hold_ttl_min` 상향 또는 "구매 완료 신고 시 TTL 연장"이며 **서버 정책 변경이라 사용자 결정이 필요**하다. ② 새 창 폴백(`openFormWindow`)은 `sessionStorage`가 복제돼 batch가 켜질 수 있으나 `_embedPost`가 부모에 닿지 않는다(폴링→`renderDone`→CTA로 자가치유하되 그 사이 화면은 1건만 확정된 것처럼 보인다). ③ **일괄 참여(홀드 5개 한 번에 생성)는 만들지 않았다** — TTL 폭발기라 기각. ④ `orderer`는 서버 신원게이트의 검증 대상이 아니라 사용자가 명시적으로 잘못 입력하면 그대로 나간다(단건과 동일한 한계).

### 리뷰어 미노출(비공개·테스트) 공고 (migration 085)
- **목적**: 대량구매(타계정 다건 일괄 제출) 같은 흐름을 **실제 참여·제출까지** 검증하려면 진짜로 열려 있는 공고가 필요한데, 그게 리뷰어 목록에 뜨면 안 된다. 그래서 **노출과 상태를 분리**했다 — `status`는 그대로 `active`(참여 가능), 목록 노출만 `recruit_campaigns.reviewer_hidden`으로 끈다.
- ★★ **`status`로 숨기면 안 되는 이유**: `computeCampaignState`는 `status !== 'active'`면 **무조건 `closed`**를 반환(`campaignState.service.js`)하므로 draft로 두면 목록에선 빠지지만 **참여 자체가 막혀** 테스트가 불가능하다. 관리자 미리보기(`preview=1`)도 제출 4중 차단 + `_batchBoot`가 preview에서 꺼져 **일괄 제출 테스트에 못 쓴다**.
- **적용점은 딱 하나** — 공개 목록 `GET /api/campaign/list`의 WHERE에 `AND COALESCE(reviewer_hidden, FALSE) = FALSE`. 그 API가 **리뷰어 홈 미리보기·공고 목록(recruit.html)·인기상품 게이트 모달(`openPopGate`)의 유일한 출처**라 한 줄이 리뷰어 노출 경로 전체를 덮는다. **관리자 목록(`/admin/list`)·상세(`/:id`)·참여(apply)·work-detail은 무변경** — 그래야 링크로 들어가 실제 참여·제출을 테스트할 수 있다(회귀가드가 그 라우트들에 필터가 **없음**을 고정).
- ★ **스코프 토큰(리뷰어앱 공고수정, `via:'reviewer_campaign'`)은 이 플래그를 못 바꾼다** — `_scopedCampaignEdit` 화이트리스트 미포함(노출 권한 상승 차단).
- ★ **부팅 프리플라이트 `REQUIRED_SCHEMA`에 등록** — 컬럼이 없으면 `/list`가 42703으로 죽어 **리뷰어 공고목록 전면 장애(무신호)** 가 된다(083과 같은 규율).
- **관리자 화면**: 공고 모달 **상태(rf_status) 묶음**에 `rf_reviewer_hidden` 토글 + 관리자 카드에 `🧪 리뷰어 숨김` 배지(`admin` 분기 — 리뷰어 응답엔 필드 자체가 없다). ★ 토글을 **참여형 전용 섹션 안에 두지 말 것** — 평소 접혀 있어 존재를 모르고 레거시 공고는 숨길 수도 없다(브라우저 렌더로 실측해 잡았고, 회귀가드가 `<div>` 깊이로 위치를 고정한다).
- **[🧪 테스트 공고] 버튼** — ★ **창구는 리뷰웹시스템[3버전] 모집공고 탭 하나**(사용자 확정: "관리자대시보드가 아닌 리뷰웹시스템[3버전] 기준"). Track B가 관리자 대시보드를 대체하는 방향이라 양쪽에 두면 창구가 둘이 된다(회귀가드가 admin.html 부재를 고정). 모달 토글(`rf_reviewer_hidden`)은 공유 모듈이라 양쪽에서 수정 가능 — 단일화한 것은 **생성 창구**뿐. (`openTestCampaignModal`): 기존 발행 모달을 그대로 열고 값만 채운다(신규 엔드포인트·모달 사본 0). 프리셋 = `status=active` + 리뷰어 숨김 ON + 참여형 ON + 타계정 허용 ON(하루한도 5) + 자리 유효시간 30분 + 자율주문 + 진행상품 1줄(총 100·일 20). **연결 탭만 사람이 고른다**(어느 시트에 테스트 행을 쓸지는 시스템이 정할 수 없다).
- ⚠⚠ **회귀 실측(2026-08-19)**: 모집공고 모달 v2 개편 때 이 토글 마크업이 **보관용 template 태그 안으로만 남아 화면에서 통째로 사라져 있었다**(브라우저는 그 안을 inert 로 다뤄 `document.getElementById('rf_reviewer_hidden')` 가 null). 저장 경로는 `if (document.getElementById(...))` 가드가 있어 **기존 값이 조용히 꺼지지는 않았지만**, 켤 수단이 없어 **[🧪 테스트 공고]로 만든 공고가 리뷰어 목록에 그대로 노출**됐다. 되살린 자리 = 라이브 마크업의 제목·상태 바 바로 아래. ★ 회귀가드는 이제 **살아 있는 마크업만** 본다(보관 조각이 대신 잡히면 "없다"가 "위치가 이상하다"로 둔갑해 원인을 가린다). ★ 소스 주석에 `template` 태그를 **글자 그대로 적지 말 것** — 그 계수기가 어긋나 멀쩡한 마크업이 보관 조각으로 오분류된다(이 수정 중 실제로 밟았다).
- ★★ **모달의 [🧪 리뷰어에게 숨김] 토글은 제거했다 (사용자 확정 2026-08-19 오후)** — 숨기는 일은 **게시(모집중) 토글을 내리는 것**으로 갈음한다(창구 단일화). ★ **서버 기능(085 컬럼·공개 `/list` 필터·`🧪 리뷰어 숨김` 배지)은 그대로** — 이미 숨김으로 저장된 공고는 계속 숨겨지고, 토글이 없으니 저장 payload 에 `reviewer_hidden` 이 실리지 않아(요소 존재 게이트) 서버 COALESCE 가 기존 값을 지킨다. ⚠ **[🧪 테스트 공고]로 만든 공고는 모집중이면 리뷰어 목록에 뜬다** — 테스트가 끝나면 게시 토글을 내린다(다시 숨기려면 DB 또는 토글 복원).
- ★★ **총인원 · 일건수는 수정 화면에서도 고칠 수 있다 (사용자 확정 2026-08-19 오후 — 오전의 읽기 전용 잠금을 되돌림)**: 초도 세팅이 잘못 들어온 공고를 여기서 바로잡아야 한다. 대신 **경고 전용**(막지 않는다) — `rf_quota_lock` 안내가 사용자 확정 문구 그대로 `총건수과 일건수는 초기작업세팅값이므로 함부로 수정하지마세요, 필요시 모집인원조절 기능을 사용하세요` 를 말한다. ★★ **0 리셋 방지 장치 2겹은 유지**: ① 프리필이 캠페인 원장 값을 **첫 행**에 싣는다 ② **수정 모드(옵션 없는 작업)에서는 표의 합계가 아니라 첫 행 값을 그대로 정원으로 쓴다**(`_rfQuotaNotice`) — 합계 규칙은 "하나라도 0이면 무제한"이라 상품 줄이 둘 이상이면 총량이 통째로 무제한이 됐다(실사고). 신규 발행은 종전 합계 규칙 그대로. ★ 차수 있는 공고의 총모집은 서버가 여전히 무시한다(`roundsLockRecruitTotal`). 회귀가드 `tests/recruitQuotaLock.test.js`.
- ⚠ **한계(화면에도 고지)**: **목록에서만 숨긴다.** 공고 URL(`campaign.html?id=...`)을 아는 사람은 들어올 수 있다 — "링크를 아는 사람만 접근하는 비공개 공고"이지 인증 게이트가 아니다.
- 회귀가드 `tests/campaignReviewerHidden.test.js`(`PGTEST_URL` 있으면 진짜 PG로 필터 실행 확인) + 브라우저 실행검증 시나리오(모달 렌더 6 = 테마 없는 호스트에서 토글 가시성·`.rf-split` 생존 / **프리셋 13 = Track B 재기준(`CAMPAIGN_ADMIN_API=/api/trackb/campaigns`) 조건에서 `openTestCampaignModal` 실제 실행** — 상태 active·숨김 ON·참여형/타계정 섹션 실제 펼침·자율주문·진행상품 표에서 정원 파생 확인). 직원 안내서 `frontend/docs/대량구매_테스트공고_만들기.html`(리뷰웹시스템[3버전] 기준).
- ★ **리뷰웹시스템[3버전] 경로 확인**: 공고 생성은 `/api/trackb/campaigns/create`(`_delegate` 프록시, `editorOnlyMiddleware`)로 나가고 목록은 `/campaigns/list`가 같은 `/admin/list` 핸들러(`SELECT *`)를 태우므로 `reviewer_hidden`이 그대로 실려 배지가 뜬다 — 별도 배선 불필요.

### ★★ 모집공고 보관(폐기) — 끝난 공고를 목록에서 내린다 (migration 130, 사용자 확정 2026-08-19)
- **발단(실측)**: 게시/비활성은 있는데 **폐기 수단이 하드 삭제뿐**이었다. 그 `DELETE /admin/:id` 는 FK CASCADE 로 `campaign_applications`(참여 이력)·`campaign_options`·`campaign_fee_schedules`(참여 시점 리뷰비 스냅샷)·`campaign_rounds`·`campaign_daily_plans`·`campaign_reviewer_gates` 를 **함께 지우고** `payment_batch_items.campaign_id` 를 SET NULL 로 끊는다 → 아무도 못 치웠고, 공개 `/list` 가 `status IN ('active','closed')` 라 **마감 공고가 리뷰어 목록에 영구히 남았다**(운영 실측: 공고 32건 중 closed 5 + `[TEST]` draft 3 = **25%가 "끝났는데 남아 있는" 상태**).
- ★★ **status 에 새 값을 넣지 않는다** — `computeCampaignState` 는 `status !== 'active'` 를 무조건 closed 로 접고, apply·목록 정렬 등 십수 곳이 그 값을 본다. **보관은 노출 축**이지 상태 축이 아니다(085 `reviewer_hidden` 과 같은 판단 — 그쪽은 "리뷰어에게만 숨김", 이쪽은 "일이 끝나 목록에서 내림"). 130 = **컬럼 추가만·백필 0·CHECK 0**(`archived_at`/`archived_by`) = 배포 즉시 동작 불변.
- **적용점 3곳**: ① 공개 `/list` `AND archived_at IS NULL`(리뷰어 노출의 유일한 출처) ② 관리자 `/admin/list` 기본 제외 + **`?archived=1` = 보관함**(★ 서버가 통째로 거르면 보관함이 영원히 빈다 — 088 마감 보관함과 같은 규율) ③ **apply 게이트**(레거시·참여형 분기 **앞** — 목록엔 없는데 링크로는 참여되는 상태를 만들지 않는다).
- ★★ **fail-closed 2종(완화 금지)**: **유효 홀드**(`applied` + 만료 전)나 **블로그 승인 대기**(`blog_pending`)가 있으면 **보관 거부**. 보관하면 목록에서 빠지고 apply 가 막혀 **결제 중인 리뷰어가 제출 화면을 못 찾는다**(그 참여가 통째로 소각). 거부 시 **쓰기 0건** + 사유를 문장으로. 해제는 게이트 없음(비상구).
- ★ **쓰기 표면 = `archived_at`/`archived_by` 두 칸뿐**(다른 테이블 무접촉) · 보관 UPDATE 는 `archived_at IS NULL` 조건(경합) · 이미 보관됨은 **멱등 성공** · **해제는 `archived_by` 를 지우지 않는다**(이력 테이블이 없어 "누가 언제 보관했나"의 유일한 기록).
- ★★ **자동 보관은 없다 — 제안까지만**(사용자 확정): 조건 = **작업보드 표(`campaign_participants`)의 모든 줄이 채워진 경우**("투입 총량을 모집해 모든 행을 채웠다"). 판정은 `campaignArchive.archiveSuggestions`(총 줄 수 vs 이름/연락처가 채워진 줄 수, soft-delete·비활성 제외). ★ **못 세면 제안하지 않는다**(fail-soft `null`) — **0/0 을 "다 찼다"로 읽으면 아직 시작도 안 한 작업에 보관 제안이 붙는다**. ★ 화면은 `[📦 보관 제안]` 배지까지이고 **실행은 사람이 [⋯] → [📦 보관]** 을 누른다(조용히 사라지는 공고 금지).
- ★★ **삭제는 좁힌다** — 참여 이력이 **1건이라도** 있으면 409 로 거부하고 **보관으로 안내**한다(`force` 우회 없음). ★ **세지 못하면 지우지 않는다**(fail-closed 503 — 모르는 채로 파괴하지 않는다). 삭제는 **발행 직후 오발행분**(참여 0건)에만 남는다.
- **화면**: 카드 `[⋯]` 메뉴에 [📦 보관]/[↩ 보관 해제](**주 줄은 3개 + [⋯] 고정 규율** 그대로 — 버튼이 늘면 메뉴로), `📦 보관됨`·`📦 보관 제안` 배지는 **admin 분기에서만**, **보관 중에는 게시 토글 대신 「보관」 표기**(목록엔 없는데 `active` 가 되는 상태 방지), 헤더 `[📦 보관함 N]` 토글(관리자 대시보드·리뷰웹시스템[3버전] 공용). ★ 건수는 **서버가 준 값만**(구버전 백엔드·조회 실패 = 숫자 미표시, 0 위장 금지) ★ onclick 은 **id 만** ★ 확인창이 **되돌릴 수 있다는 사실**을 말한다 ★ 경로는 호스트가 재기준(`_campAdminBase` — 인트라넷 SSO 토큰은 `/api/trackb/*` 로만 닿는다) ★ workdesk 는 머리말을 매번 새로 그리므로 **재렌더 뒤 버튼 상태를 다시 입힌다**(보관함을 보는 중인데 버튼은 "보관함"으로 어긋나는 것 방지).
- **API** = `POST /api/campaign/admin/:id/archive {archived:true|false}`(adminOrMaster) + `POST /api/trackb/campaigns/:id/archive`(내부인 + 편집 허용명단 — 같은 파일의 status·delete·create 프록시와 같은 계약). 42703 = `not_ready`(130 미적용 안내). 보관 성공 시 `/list` 5초 캐시 즉시 무효화. `REQUIRED_SCHEMA` 등록(컬럼 없으면 **리뷰어 공고목록·관리자 모집공고 탭 전면 42703**).
- ★★ **보관은 "새 참여 신청"만 닫는다 — 이미 참여한 리뷰어는 무접촉**(사용자 신고 2026-08-19): 확인창의 종전 문구 `참여가 닫힙니다` 가 **이미 참여한 리뷰어의 참여까지 끊긴다**로 읽혔다. 실제로 보관이 건드리는 곳은 **공개 목록 · 관리자 목록 · apply 게이트 셋뿐**이고, 리뷰 내역·리뷰 캡처 제출·받을 예정/누적 금액·입금(페이백) 대상 추출·이미지 교체요청은 `review_index`·`order_submissions`·`campaign_participants` 에서 나오므로 **보관 필터가 한 곳도 없다**(`search.service`·`reviewer.routes`·`payment.service`·`submit.routes`·`reviewEdit.routes` — 회귀가드가 그 5개 파일에 `archived_at` 0을 고정한다). ★ **그쪽에 필터를 추가하지 말 것** — 리뷰 미제출·페이백 미결 리뷰어의 화면이 통째로 사라진다. ★ 확인창도 그 사실을 그대로 말하고(가드가 **확인창을 실제로 띄워** 문구를 읽는다), 취소하면 요청이 나가지 않는다.
- ⚠ **보관해도 삭제가 아니다** — 리뷰어의 리뷰 내역·누적 금액·입금 회차 스냅샷은 그대로 살아 있다(그게 이 기능의 요점). 되돌리기는 `archived_at = NULL` 한 번.
- 회귀가드 `tests/campaignArchive.test.js`(81케이스 — 스텁 pool 로 서비스 **실제 실행**(거부 시 쓰기 0·멱등·제안 4갈래·fail-soft) + 라우터 스택 실검사 + **카드 렌더러·확인창 vm 실행**(배지 6상태 + 실문구) + 리뷰어 경로 무필터 + **리터럴 NUL 0** + 배선). ⚠ 이 변경으로 `campaignCardActions` 의 메뉴 항목 목록 패턴을 갱신했다(검사 의미 불변 — 주 줄 3개 + [⋯] 규율은 그대로).

### 모집공고별 참여가능 리뷰어 게이트 — 블랙리스트 건별 관리 (migration 091)
- **목적(직원 아이디어, 2026-08-05 확정)**: 공고별로 "참여 불가 리뷰어"를 건별 지정 + 차단 시 안내 = 고정 2종(**마감 위장** `공고가 마감되었습니다` / **사유 고지** `이전 리뷰 미작성 또는 리뷰 기한 경과…`). 시안 = `frontend/docs/design-campaign-reviewer-gate.html`.
- ★★ **deny-list — 예외만 저장**: 기본 = 전원 참여가능이라 `campaign_reviewer_gates`에는 차단(`block`)/허용(`allow`) 예외 행만 쌓인다(2000여명 전체 로드·저장 없음). `allow` = 전역 블랙리스트여도 **이 공고는 참여시킴**(안 구해지는 공고 구제 — "건별"의 핵심). 해제는 소프트(`released_at`) = 관리 이력 보존, 활성 부분유니크 `(campaign_id, phone8) WHERE released_at IS NULL`. FK는 **TEXT**(082의 42804 규율).
- ★★ **판정 단일 출처 = `utils/reviewerGate.js`**(`decideReviewerGate`): 공고별 `allow` > 공고별 `block` > **전역 `blacklist`(기존 001 테이블)는 `CAMPAIGN_REVIEWER_GATE_GLOBAL=1` 옵트인일 때만**(지금까지 apply를 안 막았으므로 기본 ON = 기존 참여자 조용한 차단 회귀 — 사용자 확정 Q3: 테스트 후 결정). 고정 문구도 `GATE_MESSAGES` 한 곳(라우트·화면 사본 금지). 킬스위치 `CAMPAIGN_REVIEWER_GATE=0` = 전건 통과.
- **적용점 = apply 게이트 1곳**(`campaign.routes.js`, 등록 리뷰어 검증 직후·홀드 INSERT 전 = **자리 미점유**): 403 `reviewer_blocked` + `gate:'closed'|'policy'`. ★ **SAVEPOINT `rg_gate` 격리 + fail-open**(테이블 부재·조회 실패가 참여를 죽이면 안 됨 — 막는 기능의 오류 > 못 막는 오류). ★ **기존 홀드·확정 건은 건드리지 않는다**(사용자 확정 Q1 — 신규 신청만 차단). 키는 소유자(로그인) phone8 — **타계정 명의 차단은 후속**(문서화된 한계).
- **마감 위장의 규율**(`campaign.html` `_renderGateBlocked`/`_gateMask`): 위장형은 **실제 마감과 같은 renderPre 경로**로 그린다(전용 화면을 만들면 미묘한 차이로 들통). `loadCampaign` 재조회·폴링이 상태를 '모집중'으로 되돌리지 않게 `_gateMask`가 계속 덮는다. ⚠ **목록 카드는 위장되지 않는다**(무인증·개인 무관 렌더) — [참여하기] 시점에만 마감 안내.
- **관리 화면 = 공유 모달 `js/campaign-reviewer-gate.js` 한 벌**(admin.html·workdesk.html, body 직속·var 폴백·onclick 인덱스만): 참여형 카드 **[🚫 리뷰어]** 버튼(모듈 존재+참여형일 때만). 기본 화면 = 예외 목록만, 검색(이름/전화 뒤4/**계좌번호** — 노란 메모 반영, 닉네임은 컬럼 부재로 제외) LIMIT 50 + "좁혀주세요" 고지. 체크=참여가능/해제=차단, 변경분만 confirm 후 저장. 예외 CSV는 클라 생성(신규 PII 엔드포인트 0).
- **API = `/api/trackb/campaigns/:id/reviewer-gate*` 3종 + `/api/trackb/settings/reviewer-gate-criteria` 2종, 전부 adminOrMaster**(계좌 검색이라 등록리뷰어DB와 같은 판단 — AE·광고주 차단, 응답은 뒤4자리만). 경로는 재기준 없이 양쪽 호스트 공용(RTC_EP 판단). 42P01 = `not_ready` 반환(088 무신호 규율).
- **블랙리스트 관리기준**(설정탭 `gatecriteria` 패널): `app_settings.reviewer_gate_criteria` JSON(1..365 클램프, **빈 값≠0일** — 082 `_int` 규율). ★★ **판정 재정의(2026-08-05 사용자 확정)**: 두 값 모두 **미제출 행**의 구매 후 경과일수 — `overdueDays`(기본 **14**) = 기한경과 / `nowriteDays`(기본 **30**) = 리뷰미작성. **30일을 넘으면 기한경과→미작성으로 승격 = 이중 계수 없음**(SQL이 상하한 양쪽을 건다 — 진짜 PG16 실행 확인). 옛 정의("기한경과 = 제출했지만 늦게")는 폐기. 판정 재료 = `review_index`(제출 여부) LEFT JOIN `order_submissions`(구매 시점, `submitted_at` — `created_at` 아님!), 구매 시점 모르는 행은 건수 제외(누적참여에만 포함). 기준 변경 = 표시·판단 보조일 뿐 **자동 차단 없음**(차단은 항상 사람이).

### 등록리뷰어DB 자동 블랙리뷰어 (091-2, 시안 `frontend/docs/design-reviewerdb-blacklist.html`)
- **표에 3열 추가**(타계정 오른쪽 누적참여·이전 리뷰 / 삭제 바로 왼쪽 참여설정 토글, `RV_COLSPAN` 13→16): **누적참여 = 본인+타계정 phone8 합산**(사용자 지시 "타계정 포함"), 이전 리뷰 = `리뷰미작성 N건`(빨강)·`기한경과 N건`(주황). **미작성 1건↑ = 자동 블랙리스트 후보**(빨간 건수 텍스트 + 🚨 후보 필터 칩이 표시의 전부 — ★ **행 배경색·이름 배지 없음**, 사용자 확정). 집계는 저장 없이 **페이지 행(≤200)만 조회 시 파생**(`annotateReviewerRows`), 실패 = `statsUnavailable` → **"집계 실패 ?"**(0으로 꾸미지 않음).
- **참여설정 토글**(빨간 사각 스위치) = 기존 전역 `blacklist` 테이블 등록/해제(`POST /api/trackb/reviewers/blacklist`, adminOrMaster). ★ **확인창 없이 즉시 적용**(사용자 확정 — 낙관 반영 + 실패 롤백), 사유는 현재 집계를 자동 기록. ★★ **효력 = Q1-B**: 전 공고 자동 차단이 아니라 **공고별 [🚫 리뷰어] 팝업 맨 위에 자동 표시**(`listGlobalBlacklist` — reviewer-gate GET에 동봉, 조회 실패는 **필드 미동봉** = 프론트가 "0명"으로 위장하지 않고 "조회 실패" 고지) + [🚫 블랙리스트 모두 차단] 버튼(스테이징 → confirm 경유, ★ **사람이 정한 허용 예외(allow)는 덮지 않는다**). 전역 자동 차단은 여전히 `CAMPAIGN_REVIEWER_GATE_GLOBAL=1` 옵트인 뒤에만.
- 필터 칩(전체/참여가능/블랙리스트/🚨 후보/기한경과만 — "기한경과만"은 승격분 제외) + 검색에 **등록 계좌번호** 부분일치 추가. ★ **엑셀 다운로드 없음**(사용자 확정 Q4 — 팝업의 예외 CSV도 제거). candidate/overdue 필터는 **본인 명의 phone8 기준**(타계정 미작성분은 건수 표시에만 합산 — 문서화된 한계).
- 회귀가드 `tests/reviewerDbBlacklist.test.js`(집계 시맨틱·서비스 스텁 실행·라우터·프론트 배선) + 진짜 PG16으로 승격/이중계수 검증. ⚠ 이 변경으로 `reviewerDbTab.ux`(컬럼 순서 16열)·`campaignReviewerGate`(기본값 14/30) 가드를 갱신(검사 의미 = 새 사용자 확정).
- 회귀가드 `tests/campaignReviewerGate.test.js`(판정 순수함수·스텁 db 서비스 실행(자리표시자 정합·PII 마스킹·tx)·라우터 스택 실검사·apply 배선 위치·프론트 배선) + **진짜 PG16으로 마이그레이션 idempotent·부분유니크·release 재등록·CASCADE·이력 집계 SQL 실행 확인**. ⚠ 이 변경으로 `workdeskSettingsTab` 가드의 패널 목록 패턴을 갱신(검사 의미 불변).

### ★★★ 작업보드 줄이 "중복"으로 잘못 지워진 사고 — 주문 링크는 표 주문번호가 정한다 (2026-08-19)
- **증상**: 「0721)장수돌침대…」 8/19 구간에서 다섯 줄이 사라졌다(직원 송장 출력 37명 → 화면 32명). 구매양식 제출은 전부 정상 접수(`written`)였고 주문도 취소되지 않았다 — **줄만** 소프트삭제됐다. 전 작업으로 넓히니 같은 사유로 **21개 작업 174줄**.
- ★★ **원인 = 투영(`trackB._enrichTab`)이 주문 링크를 phone8 하나로만 정했다**: `byPhone` 은 한 연락처당 주문을 **하나만** 남기므로, 링크가 비어 있던 줄마다 그 한 주문이 붙는다. 같은 작업에 여러 번 참여한 리뷰어는 **모든 줄이 같은 주문을 가리키게** 되고, 중복 정리(`dedupeRows`)의 ㉯ 축("같은 주문 기록을 여러 줄이 씀")이 그 링크만 믿고 뒤 줄을 내렸다. 실측: 8/19 줄(seq 863)의 표 주문번호는 `16102387707847`(8/19)인데 링크는 `16102062667987`(8/4)이었다. ★ 복구 직후 dry-run 이 같은 다섯 줄을 **그대로 다시 지목**해 인과가 확정됐다(복구만 하고 링크를 안 고치면 다음 정리에서 또 지워진다).
- ★★ **링크는 표 주문번호가 정한다(완화 금지)**: 표에 적힌 주문번호와 원장 주문번호가 **정확히 같을 때만** 링크 · 같은 번호의 주문이 둘이면 **모호로 두고 링크하지 않는다** · 표 번호가 없을 때만 phone8 폴백이고 그마저 **그 사람 주문이 그 탭에 유일할 때만**. ★ `identity_key` 계산은 **종전 그대로** — 그 값은 편집 오버레이의 앵커라 좁히면 사람이 적어 둔 값이 조용히 끊긴다(좁히는 것은 링크뿐). 링크 blank-only(`COALESCE`)도 불변.
- ★★ **중복 정리는 표 주문번호가 다르면 묶지 않는다(사용자 확정)**: ㉯ 축 키에 표 주문번호를 합류시켰다 — 담당자가 표에서 눈으로 보는 값이 다르면 그건 다른 구매다, 링크보다 그 값을 믿는다. ★★ **6자리 미만(비번호 주문·빈 칸)은 ㉯ 축에서 아예 제외한다 (2026-08-19 2차 확정 · 완화 금지)** — 종전에는 빈 값끼리 링크로 묶었는데, **링크는 투영이 연락처만으로 추측해 채우는 값**이라 표에 주문번호가 없는 줄들은 *같은 사람의 서로 다른 참여*가 한 주문에 함께 매달린다(진행 중 무시트 작업 실측 82줄). 그 상태에서 묶으면 **별개 참여가 중복으로 지워진다**. 판정 근거가 없으면 판정하지 않는다 — 그 갈래는 사람이 [♻ 중복 줄 정리]·[🧹 줄 정리]로 직접 고른다. ★ 표 주문번호가 있는 줄끼리는 종전 그대로(무회귀). ⚠ 그래서 **링크 비우기로는 이 갈래가 해결되지 않는다** — 투영(`_enrichTab`)이 blank-only 로 다시 채우기 때문(실측: 비운 14줄 중 4줄이 10분 만에 재부착). 링크 비우기가 영구적인 것은 **남의 주문을 가리키던 줄**(그 사람 이름의 주문이 그 탭에 없어 추측 근거가 없는 경우)뿐이다.
- ★★ **"진짜 중복"과 "오삭제"를 가르는 기준**: 내려간 줄의 표 주문번호와 **같은 번호를 쓰는 살아 있는 줄이 그 탭에 있으면** 정상 정리(한 주문이 여러 줄에 기록된 것을 접은 것), 없으면 오삭제다. 같은 번호가 여러 줄이면 가장 이른 seq 하나만 되살린다. 이 기준으로 174줄 중 **145줄 복구 · 29줄은 진짜 중복이라 그대로** 뒀다(링크 정정 292줄).
- ★★ **줄을 되살려도 장부를 다시 만들지 않으면 반쪽이다** — 검색 명단(`review_index`)·리뷰비 입금 대상은 `rebuildLedgers` 로만 갱신되는데 그것을 부를 창구가 **어디에도 없었다**(재생성은 정리·이관·접수의 부수효과로만 일어났다 = 복구의 막다른 길). `POST /api/trackb/worktable/rebuild-ledgers`(adminOrMaster · dryRun 기본 · 새 판정 0) 신설.
- ★ 회귀가드 `tests/dedupeOrderLinkAxis.test.js` 는 **두 규칙을 함께** 고정한다 — ㉯ 축 본래의 교훈(원장 주문번호가 없어도 잡는다)은 **표 주문번호가 같은** 줄로, 장수산업 규율(표 주문번호가 다르면 안 묶는다)은 별도 블록으로. ⚠ 그 가드의 옛 픽스처는 **표 주문번호를 줄마다 다르게** 두고 한 덩어리를 기대해, 새 규율이 들어온 뒤 계속 빨간 상태였다(코드가 아니라 픽스처가 옛 규칙이었다).
- 회귀가드 `tests/dedupeLinkCorruption.test.js`(스텁 pool 로 `_enrichTab`·`dedupeRows` **실제 실행**: 863↔8/19 링크 · 두 줄이 같은 주문을 안 가리킴 · 표 번호 다르면 정리 대상 0 · 표 번호까지 같으면 종전대로 정리) · `tests/worktableRebuildLedgersRoute.test.js`.
- 잔여(문서화): ① 링크가 **비어 있게 되는 경로**는 그대로다(투영이 새 seq 를 INSERT 할 때 링크 컬럼이 없어 NULL · 주문 기록 append 가 `ON CONFLICT DO NOTHING` 으로 스킵된 경우 · 이관 전 시트 시절에 만들어진 줄). 지금 수정은 **그 빈칸을 틀린 주문으로 채우지 않게** 한 것이다 ② 표 주문번호가 없는 작업(비번호 주문·공영쇼핑 등)은 여전히 phone8 유일 조건에만 기댄다 ③ "한 주문 = 한 줄" 을 DB 제약으로 강제하지는 않았다(부분 유니크 후보 — 정당한 다중 기록 케이스를 먼저 조사할 것).

### ★ 작업보드 참여자 칸 = 참여횟수 배지(명의 기준) — 사용자 확정 2026-08-19
- 종전 `주문` 배지를 **참여횟수 배지**로 교체했다: `[1회]` 파랑 · `[2회]` 노랑 · `[3회]` 이상 빨강. **색은 3단계로 끝내고 숫자가 실제 횟수를 말한다**(4회·5회도 빨강 — 색을 늘리면 한눈에 읽히는 신호가 약해진다).
- ★★ **세는 단위는 리뷰어(소유자)가 아니라 계정 명의(`phone8`)** — 한 사람이 본계정으로 1회·타계정으로 1회면 **둘 다 [1회]** 이고, 같은 타계정으로 또 참여해야 [2회]. 집계 범위는 **이 작업(탭) 안**(전 작업 누적 아님 — 다른 작업 이력이 이 표에 섞이지 않는다).
- ★★ **판정은 서버 한 곳**(`trackB.workdeskTab` 의 `visitSeen`) — 화면은 `visitNo` 를 **그리기만** 한다. 프론트에서 다시 세면 표를 필터·정렬할 때 숫자가 흔들린다.
- ★★ **마스킹 전 원본 phone8 로 센다** — 광고주 렌즈의 `_mask` 를 거친 뒤 세면 전 줄이 같은 값이 되어 1,2,3… 으로 무너진다(회귀가드가 코드 순서를 고정).
- ★ **모르는 것을 1회로 꾸미지 않는다** — 명의 미상(빈 슬롯·연락처 없음)은 `visitNo` 자체를 안 싣고 화면도 배지를 안 그린다. 구버전 백엔드(필드 부재)도 같다.
- ★ **화면에서 뺀 줄(`_hidden` 오버레이)은 번호를 먹지 않는다** — 표에 없는 줄이 번호를 차지하면 뒤 줄이 밀린다(카운트는 `continue` 뒤에서 한다).
- ★★ **렌더러는 한 벌 `_visitBadge(r)`**(workdesk.html) — 시트 그리드와 기본 표가 같은 것을 쓴다(종전에는 같은 배지 문자열이 두 곳에 복사돼 있었다).
- ★ **`수동` 배지와 🔒(편집 잠금)는 유지**(사용자 확정) — 성격이 다르다. 사라진 `주문` 배지의 정보(주문 원장 연결 여부)는 **툴팁으로 남긴다**(정보 손실 0).
- ★ **복사값에서 배지를 뺀다**(`.visit` 를 `_gridCopyText` 제거 목록에 합류) — 안 그러면 표를 복사할 때 셀 값에 `2회` 가 섞인다.
- 회귀가드 `tests/participantVisitBadge.test.js`(스텁 pool 로 `workdeskTab` **실제 실행**(명의별 순번·제거 줄 제외·마스킹 순서) + 렌더러 **vm 실행**(색 3단계·값 부재·툴팁) + 배선·CSS, **변이시험 검출 확인**).

### ★★★ 같은 주문이 10분마다 새 줄로 늘어난 사고 — 중복 방어를 링크에서 떼어낸다 (2026-08-19)
- **증상**: 「8/3(쿠팡)위프_블랙 탈취제 800건」에서 A 리뷰어 한 사람에게 **16:40~18:20 사이 10분마다 한 줄씩 11줄**이 생겼다(전부 같은 표 주문번호·같은 구매일자). 줄 자체는 8/7 에 만들어 둔 **빈 슬롯**이었고 8/19 에 하나씩 채워졌다.
- **원인**: `sheetless_worktable_recover` 크론(`*/10`)이 미반영 주문을 재기록하는데, 그 앞의 중복 방어가 **`cp.order_submission_id → order_submissions` 링크를 타고** "이미 반영됐나"를 판정했다. **그 링크가 오염돼 있으면**(다른 주문을 가리키면) 이미 반영된 줄을 못 보고 **새 빈 슬롯을 먹는다**. 즉 **링크 오염 하나가 줄 오삭제(중복 정리)와 줄 중복 생성(복구 크론) 두 방향으로 동시에 사고**를 냈다.
- ★★ **수정 = 판정을 링크에서 떼어낸다**: 1차(링크 조인)가 못 잡으면 **표에 적힌 주문번호 + 명의(phone8)** 로 한 번 더 본다. 담당자가 화면에서 보는 값으로 판정하므로 **링크 상태와 무관**해진다(중복 정리의 표 주문번호 축과 같은 원칙).
- ★★ **판정 SQL 단일 출처 = `utils/tableOrderNum.js`(`tableOrderNumSql(alias)`)** — 소비처 둘(`sheetlessLedger.dedupeRows` · `sheetlessOrder.writeOrderToWorktable`)이 같은 조각을 쓴다. 서로 다른 규칙으로 "표 주문번호"를 뽑으면 **한쪽은 중복이라 지우고 다른 쪽은 중복이 아니라며 또 만드는** 상태가 된다(이번 사고가 정확히 그 모양이다). 별칭은 정규식으로 검증(주입 차단), 추출 시 **원본 SQL 과 바이트 동일**함을 확인했다.
- ★★ **완화 금지 4종**: ① **명의(phone8)까지 같을 때만** 중복으로 본다 — 표 주문번호만 보면 담당자가 옆 줄에 같은 번호를 적어 둔 경우까지 막아 **정상 주문이 표에 못 들어간다** ② **삭제된 줄은 근거가 아니다** ③ **자기 줄은 제외**(`order_submission_id <> $3`, 링크 없는 줄도 후보) ④ **모르면 막지 않는다** — 주문번호 6자리 미만·연락처 8자리 미만이면 검사를 **건너뛴다**(fail-open, 종전 동작 그대로).
- ★ **링크 없는 줄(담당자 수기 입력)도 대상** — 그 줄이 곧 그 구매다. 이때 `duplicateOf` 는 `String(null)`(문자열 `"null"`)이 아니라 **`null`** 이어야 한다(로그·응답이 거짓을 말하지 않는다).
- ★ 기존 줄의 `order_submission_id` 는 **여전히 바꾸지 않는다** — 링크를 빼앗으면 원래 주문이 "미반영"으로 되살아나 복구 잡이 다시 줄을 만든다(막으려던 사고의 재현).
- ⚠ 남은 한계(문서화): `duplicate_row` 는 주문을 `written` 으로 **찍지 않으므로** 그 주문은 복구 스캔 창(`SHEETLESS_RECOVER_WINDOW_HOURS` 48시간) 동안 매 주기 재시도되며 warn 을 남긴다(줄은 더 안 생긴다). 종전부터 있던 성질이다.
- 회귀가드 `tests/sheetlessDupRowGuard.test.js`(스텁 pool 로 `writeOrderToWorktable` **실제 실행**: 1차 무회귀 · 2차 차단 · 링크 없는 줄 · fail-open 3갈래 · 사본 부재, **변이시험 7종 검출 확인**).

### ★ 작업표 줄 ↔ 주문 링크 교정 도구 (2026-08-19 · 스키마 변경 0)
- **왜**: "링크는 표 주문번호가 정한다"(같은 날 수정)는 **앞으로 채울 때의 규칙**이라 blank-only 다 — **이미 박힌 오염은 안 고쳐진다**. 위프 800건에 2묶음 5줄이 남아 있었고, 그 오염이 ㉮ 중복 정리로 **멀쩡한 주문을 취소**시키고(실측: `canceled_by='dedupe:…'`) ㉯ 관제 대조를 오표시했다. 사람이 지목해 고치는 창구가 필요했다.
- **API** `POST /api/trackb/worktable/repair-link`(adminOrMaster) — `fixes:[{seq, action:'relink'|'unlink', expectOrderSubmissionId, orderNum}]`. **dryRun 기본** · 실행은 `confirm:true`.
- ★★ **완화 금지 6종**: ① **자동 스캔·일괄 적용 없음**(줄과 **지금 값**을 명시 — 그 사이 바뀌었으면 `changed` 거부) ② **표 주문번호가 링크를 정한다**(원장에 없거나 둘 이상이면 **채우지 않는다** — 추측이 빈 링크보다 나쁘다) ③ **줄 번호가 일치할 때만**(`order.sheet_row === seq`) ④ **한 주문은 한 줄**(다른 살아있는 줄이 가리키면 거부 — 오염을 새로 만들지 않는다) ⑤ **전부 아니면 전무**(한 건이라도 실패하면 통째 ROLLBACK) ⑥ **취소 되돌리기는 중복 정리가 취소한 건에만**(`canceled_by` 가 `dedupe*`) — 사람이 [주문취소]한 건을 되살리면 정원·시트가 어긋난다(응답이 `orderStillCanceled` 로 사실을 말한다).
- ★ **쓰기 표면 = `campaign_participants.order_submission_id` + (되돌리기 시) `order_submissions.deleted_at/canceled_by/mirror_status` 뿐** — 시트·장부·정원·홀드 무접촉, 하드삭제 0. **미리보기 ≡ 실행**(같은 코드 경로를 돌린 뒤 ROLLBACK).
- ★ **취소 내력 진단**: `GET /api/diag/order-stuck-export` JSON 에 `canceledBy`·`deletedAt` 동봉(취소 경로가 넷인데 누가 취소했는지 볼 창구가 없었다). **CSV 열 구성은 불변**(head 배열이 칸을 정한다 — 회귀가드가 15칸 고정).
- 회귀가드 `tests/worktableLinkRepair.test.js`(**진짜 PG16** — 미리보기 무쓰기·재링크·되돌리기·낙관적 동시성·전부아니면전무·사람취소 보존, **변이시험 6종 검출 확인**) · `tests/orderCancelProvenance.test.js`.

### ★ 작업표 중복 줄 감시망 (2026-08-19)
- **왜**: 같은 구매가 표에 여러 줄로 늘어난 사고(권정현 11줄)를 **아무도 몰랐다** — 사람이 표를 보다 우연히 발견했다. 기록 경로의 2차 중복 판정이 최종 방어지만, **그것이 또 뚫려도 알 길이 없는 상태**를 없앤다.
- **무엇을 보나**: 같은 **표 주문번호 + 명의(phone8)** 로 살아있는 줄이 2개 이상인 묶음. ★ 판정을 새로 만들지 않는다 — 번호는 `utils/tableOrderNum` 공유 조각(중복 정리·주문 기록과 같은 값), 키도 중복 방어와 같다.
- ★★ **읽기 전용** — 줄을 내리지도 주문을 취소하지도 않는다. 정리는 사람이 [♻ 중복 줄 정리]로. 쓰기는 **스냅샷 한 곳**(`app_settings.worktable_dup_watch`)뿐이고 회귀가드가 그 개수를 센다.
- ★ **무시트 탭만** — 시트 기반 탭은 시트가 진실원본이라 같은 번호 여러 줄이 담당자 의도일 수 있고, 이 사고의 발생 경로(빈 슬롯 소비)도 무시트 전용이다. ★ **모르는 것은 세지 않는다**(번호 6자리 미만·명의 없음·삭제된 줄) — 빈 값끼리 뭉쳐 거짓 경보가 되는 것을 막는다.
- ★★ **늑대소년 방지 = 달라졌을 때만 알린다** — 직전 스냅샷과 지문이 같으면 침묵한다. ★ 지문에 **줄 수를 포함**해야 "줄이 하나 더 늘어난 것"을 다시 알린다(빼면 증식이 조용해진다). ★ **스냅샷을 못 읽으면 알린다**(놓치는 쪽보다 한 번 더).
- ★ 알림은 **묶음마다가 아니라 한 건**으로(도배 방지) · `logAbnormal(severity:'warn')` = **시스템 오류로그**(리뷰어 개인 문제가 아니라 데이터 정합 문제라 `reviewer_event_logs` 아님, 관리자 빨간 알림도 아님).
- **크론** `17 * * * *` + `withJobLock('worktable_dup_watch')` · 킬스위치 `WORKTABLE_DUP_WATCH=0` · **크론은 감시망 실패로 죽지 않는다**. **조회 창구** `POST /api/trackb/worktable/dup-watch`(adminOrMaster) — ★ **`record:false`** 라 사람이 눌러 본 것 때문에 다음 크론 알림이 조용해지지 않는다.
- ⚠ **감시망은 임시 대책이다** — 근본은 `(sheet_id, tab_name, order_submission_id)` 부분 유니크로 DB가 막는 것이고, 그건 남은 어긋난 링크를 먼저 정리해야 가능하다.
- 회귀가드 `tests/worktableDupWatch.test.js`(30케이스 — 스텁 pool 로 **실제 실행**: 처음 발견·동일 침묵·증식 재알림·소멸·스냅샷 실패·조회 실패 + 라우터 스택 실검사, **변이시험 8종 검출 확인**).

### ★★ 등록리뷰어DB 로그인 번호 변경 — 덮어쓰기가 아니라 "승격" (migration 126)
- **배경**: `reviewers.phone` 을 바꾸면 파생 컬럼 `phone8`(GENERATED, 001:99)이 통째로 바뀌는데, 이 시스템의 참여·주문·문의·정산·차단이 전부 phone8 을 신원키로 쓴다. 그래서 번호 변경은 "칸 하나 수정"이 아니라 **신원키 교체**다. 종전에는 창구가 아예 없었고(등록리뷰어DB 는 `admin_memo` 만 수정), DB 직수정은 **그 사람의 참여 내역이 화면에서 통째로 사라지는** 조작이었다.
- ★★ **채택 방식 = 승격**: 새 번호 → `reviewers.phone`(본계정) / 옛 번호 → `sub_accounts` 항목(같은 이름). 그러면 아래 세 경로가 **기존 코드 그대로** 과거를 계속 본다(분기·사본 0) — 리뷰 내역·검색 `search.service._getReviewerPhoneList`(본인+타계정 phone8) · `/api/reviewer/review-earnings`(같은 phoneList) · 리뷰비 계좌 `payment.service._loadAccounts`(타계정 매핑 + 빈 계좌는 소유자 계좌 COALESCE).
- ★★ **방향이 계약이다(비대칭 함정)** — `_getReviewerPhoneList(p8)` 는 `reviewers WHERE phone8=$1` 로 소유자 행을 찾는다. **옛 번호를 본계정으로 남기면** 새 번호 세션이 소유자 행을 못 찾아(sub 로그인은 세션 phone8 이 타계정 값) 자기 번호 하나만 스코프에 담긴다. 그래서 반드시 **새 번호가 본계정**.
- ★★ **일부러 건드리지 않는 것**: `review_index`(시트 연락처 원문 = 진실원본, stale-pl 보안 게이트가 그 전제 위에 있다) · `participation_links` · `order_submissions` · `campaign_applications` · `payment_batch_items`(회차 스냅샷은 박제값). 승격이 조회를 살리므로 옮길 이유가 없다.
- ★ **이관하는 키**(옛 phone8 에만 매달려 승격으로 안 따라오는 것): `cs_threads.reviewer_phone8` · `campaign_reviewer_gates.phone8` · `reviewer_campaign_editors.phone8`. 셋 다 유니크 제약이 있어 **대상에 이미 행이 있으면 옮기지 않고 건수를 보고**한다(조용한 실패 금지 — 화면이 "옮기지 못한 N건"을 말한다). ★★ **블랙리스트는 옮기지 않고 새 번호를 추가**한다(`blacklist.phone` = 전체 번호 키) — 번호를 갈아 차단을 회피하는 길을 열지 않는다.
- ★★ **fail-closed 차단 4종(force 우회 없음)**: ① 새 번호가 다른 reviewers 행(UNIQUE 충돌 = 계정 병합 사고) ② **뒤 8자리가 타인과 겹침** — `phone8` 은 비유니크라 로그인·검색·계좌 매칭이 섞인다 ③ 새 번호가 **남의 타계정**으로 등록됨 ④ **유효 홀드 보유** — 홀드는 phone8+hold_token 이중 열쇠라 번호가 바뀌면 제출 확정이 막혀 참여가 소각된다(10~15분 뒤 저절로 풀리므로 막다른 길이 아니다). ★ 대상 지목은 언제나 **`reviewers.id`** — `WHERE phone8 =` 는 타인 행까지 함께 바꾼다.
- ★★ **리뷰어 내정보(무인증)에 두지 않는다** — `/api/reviewer/profile` 은 이름+뒤8자리가 전부이고 서버가 `WHERE phone8 = $1` 만 본다. 거기에 번호 변경을 열면 **뒤 8자리만 아는 제3자가 남의 계정을 자기 번호로 가져간다**(주민번호·계좌·리뷰비까지). 창구는 `POST /api/trackb/reviewers/phone`(**adminOrMaster** — 등록리뷰어DB 와 같은 게이트) 하나이고, confirm 없이 부르면 **쓰기 0 미리보기**(차단 사유 + 영향 건수)를 돌려준다.
- ★★ **집계·이관 프로브는 SAVEPOINT 격리**(진짜 PG16 으로 실측) — 프로브 하나가 42703/42P01 로 실패하면 그 뒤 전부가 **25P02(current transaction is aborted)** 로 죽어, "세지 못했을 뿐"이어야 할 fail-soft 가 **변경 전체를 무력화**한다(082 apply 규율과 같은 자리). ★ 변이시험이 이 자리를 뚫었다 — `previewPhoneChange`(pool 경로)만 검사하면 tx 경로 회귀를 못 잡는다.
- ⚠ **곁다리로 고친 무신호 버그**: `/reviewers/delete` 의 주문 집계가 **없는 컬럼**(`order_submissions.phone8` — 그 테이블엔 전체 번호 `phone` 뿐)을 봐 **항상 실패**했고, 확인창이 "구매양식 제출 0건"으로 조용히 속이고 있었다(진짜 PG 로 실행해야만 보인다).
- **화면** = 등록리뷰어DB 연락처 칸 [변경] → 팝업(**body 직속** · **바깥 클릭으로 닫지 않는다**(입력한 번호 보호) · Esc 리스너는 닫을 때 해제 · onclick 은 **인덱스만**). ★ **프론트 재판정 0** — 서버 blockers 를 그대로 그리고, 차단이 있으면 [이대로 변경] 버튼 자체를 만들지 않는다.
- 회귀가드 `tests/reviewerPhoneChange.test.js`(순수함수·스텁 db 차단 4종·라우터 스택 실검사·프론트 배선 + **진짜 PG16 end-to-end**: 승격 후 옛 번호 참여 2건이 실제로 조회되는지(`_getReviewerPhoneList` 실행) · 이관 충돌 보존 · 블랙리스트 승계 · 감사 이력 · 계좌 감사 트리거 통과 · 차단 시 쓰기 0 · 왕복 복구, **변이시험 검출 확인**) + 브라우저 실행검증.
- 잔여(문서화): ① **1:1문의방은 새 번호로 이미 방이 있으면 옮기지 않는다**(둘로 남는다 — 관리자 화면에서 확인) ② 리뷰어에게 "새 번호로 로그인" 안내는 **사람이** 한다(자동 통지 없음) ③ 옛 번호로도 로그인은 되지만(타계정 매칭) 그 세션은 새 번호 참여분을 못 본다 ④ `my-status`·`/cs/threads` 는 단일 phone8 스코프라 옛 번호 건이 그 화면에는 안 뜬다(타계정까지 넓히면 타인 명의 문의방 노출 위험 — 별건).

### 모집공고 별표 고정(우선노출) · 인기상품 선행참여 게이트 (migration 064)
- **노출순서 제거**: `rf_sort_order` UI(admin·admin-siand)와 `/list`·`/admin/list` 정렬에서 sort_order 제거(**컬럼은 보존** — 서버 `||0` 강제라 편집 저장은 `window._recruitEditLoaded` 로드값 재전송으로 0-덮어쓰기 방지). 우선노출 = 관리자 공고 카드 **우측상단 ⭐ 별표**(`recruit_campaigns.pinned_at`, `POST /api/campaign/admin/:id/flags {pinned,popular}` adminOrMaster — **먼저 별표한 순서대로 최상단**(pinned_at ASC), 재별표에도 `COALESCE(pinned_at,NOW)`로 원래 자리 유지, 스코프 토큰은 PUT 끝앵커 격리라 도달 불가).
- ★★ **별표 요청 경로는 호스트가 재기준한다**(`campaign-cards.js` `_flagsUrl` → `window.CAMPAIGN_ADMIN_API`, index-recruit.js `_campApi`와 같은 장치): 리뷰웹시스템[3버전]은 인트라넷 SSO 토큰(`via:'intranet'`)을 쓰는데 그 토큰은 `authMiddleware`에서 **`/api/trackb/*` 로만 도달 가능**하다 → `/api/campaign/admin/:id/flags` 하드코딩은 **별표만** 403(`인트라넷 연동 계정은 …Track B…에서만`)으로 죽는다(실측 2026-08-07 — 수정·보기·게시토글은 이미 재기준돼 있어 별표 하나만 갈라져 있었다). 서버 프록시(`POST /api/trackb/campaigns/:id/flags`)는 이미 존재. 전역 미설정 화면(관리자 대시보드·리뷰어 홈)은 종전 경로 = 동작 불변. 회귀가드 `tests/campaignPinnedPopular.test.js`(하드코딩 부활 시 실패, 변이시험 검출 확인).
- **행으로 보기(시안 C 티켓형)**: 리뷰어 홈 ≡ 토글(`applyRecruitView`가 `.rc-list`에 `rc-view-row` 부여)이 참여형 카드에도 적용된다. ★**렌더 경로는 `cardHtml` 하나뿐 — 행 보기는 CSS 표현만 전환**(`.rc-view-row .pcard{flex-direction:row}` + 썸네일 92px 좌측고정·`position:static`으로 낮춰 배지/리본 절대배치를 `.pcard` 기준으로 재기준화·게이지 1줄 압축). 행 전용 HTML 빌더를 만들면 상태 규칙(카운트다운·리본·인기배지·버튼 문구)이 두 벌이 되어 드리프트하므로 **금지**(회귀가드가 `rowHtml`류 부재를 고정). 함정: flex 자식 기본 `min-width:auto` 때문에 `.pbody{min-width:0}` 없으면 긴 제목이 행을 가로로 밀어내고 말줄임도 안 먹는다(실측 확인). 회귀가드 `tests/campaignRowView.test.js`. 시안 문서 = `frontend/docs/design-campaign-rowview.html`(모바일 390px 목업 A/B/C, `?v=A|B|C`로 단독 보기).
- **🔥 인기상품**(`is_popular`, 참여형 카드 토글): 리뷰어 카드 [인기!] 배지(`pt-pop`, 공개필드 레거시/참여형 양쪽) + **선행참여 게이트** — 명의(phone8)별 `일반(비인기) 참여형 submitted 수 > 인기 소비(제출확정+유효홀드)`일 때만 인기 apply 허용(403 `popular_locked`+normalDone/popularUsed, **1:1 교환**·만료/취소 자동 환불·타계정도 명의별 일반 1건씩 필요, 명의 advisory 락이 이중소비 차단). `GET /api/campaign/popular-status?phone8=`(무인증 phone8 스코프, **`/:id`보다 먼저 등록** — 라우트 삼킴 함정). campaign.html = 인기 공고 안내 카드(`renderPopNotice`) + 잠금 시 **일반모집 테이블 모달**(`openPopGate` — open·비인기·자기제외 필터, [참여하기] CTA→`pop_return_camp` 기록) + 일반 제출완료 화면 "인기 상품으로 돌아가기" 버튼(지각접수는 미노출=크레딧 아님). 회귀가드 `tests/campaignPinnedPopular.test.js`.

### ★ 작업보드 상단 3단 구성 (시안 B · 마감은 작업명 행에서)
- **시안** `frontend/docs/design-workboard-top-3section.html`(A/B 두 안, `?v=A|B` 단독 보기). **B 확정**(사용자 2026-08-06) + master 도구 [⋯] 접기 확정. 대상은 **작업 화면 상단만** — 시트 그리드(작업상세)·툴바·우측 레일 무접촉.
- **종전 = 세로로 쌓인 5블록**(제목 행 / 마감 전폭 띠 / 요약 스트립 8칸 / 정산 상세 / 발주 배너)이라 정작 일하는 표가 아래로 밀렸다. **지금 = 제목 행 + `[작업 조건 · 진행 현황 · 정산]` 3분할 카드**(`.tp3grid`, 광고주는 정산 열 없이 2열 `.n2`).
- ★★ **마감 안내 문구 + [🏁 마감]은 작업명과 같은 행**(사용자 확정): `_finBarHtml` 이 전폭 띠(`.wbl-finbar`, 폐기)가 아니라 **제목 행에 넣을 조각**(`.tp3fin`)을 돌려준다. ★ **루트 `id="finBar"` 유지 필수** — `_finRefresh` 가 `outerHTML` 로 통째 교체하는 계약이다. 상태 3종(진행중 / ✓마감 후보 / 🏁마감된 작업+복귀)은 **같은 자리**에서 표현하고 `openFinishCur`·`doReopenCur`·검수 게이트는 **무변경**.
- ★★ **제목 행은 `nowrap` 이라야 한다**(`.mh.mh-wb`): flex 는 **줄바꿈을 먼저 하고 축소는 그다음**이라(줄나눔 판정 = 축소 전 max-content) `min-width:0` 만 주면 좁은 폭에서 **[마감]이 아랫줄로 떨어진다** — 시안 검증에서 실측. `nowrap` + 제목·시트명·문구의 `min-width:0` 이 **한 벌**. 긴 문장 뒷부분은 `.opt` 로 감싸 1280px 미만에서 CSS 가 접고 `title` 툴팁으로 전문을 남긴다(지우지 않는다). ★ 스코프는 **`mh-wb` 한정** — 공유 클래스 `.mh` 를 건드리면 작업오더·모집공고 등 다른 뷰 헤더까지 nowrap 이 된다.
- ★ **master 도구 3종(Track B 전환·write-back 시뮬·그림자 투영)은 [⋯] 메뉴로**(`#mhTools`) — 주 행동인 [마감]이 오른쪽 끝을 갖는다. `sotBtn`/`projBtn` id 는 그대로(호출부가 참조). 바깥클릭·Esc 리스너는 **최상위 1회만**(`_mhToolsBound` — 열 때마다 걸면 겹쳐 쌓인다).
- ★ **발주 '미연결' 배너는 ① 작업 조건 카드 안쪽 줄**(`_woUnlinkedRow`, admin/master 만)로 흡수 — `renderWorkOrderSection` 은 `!d` 면 `''` 를 돌려준다(같은 말 두 번 금지). 연결된 경우의 작업세부 펼침(`.wodetail`)은 종전 그대로.
- ★ **정산 열은 `#setlCell` + `.setlsummary` 계약 유지**(클릭=상세 펼침·`outerHTML` 교체). 초기 상태도 **`setlSummaryHtml(null)` 한 곳**에서 만든다 — 따로 조립하면 로드 전/후 구조가 갈려 단계 막대가 flex 행에 갇힌다(실브라우저로 잡음). 4단계 표시(마감자료→견적서→계산서→입금)는 `_setlStage` 와 **같은 필드**를 읽는 표시 보조일 뿐 새 판정을 만들지 않는다.
- ★ 제목 행의 초록 배지 「참여자 N」은 뺐다 — 바로 아래 [진행 현황] 열이 같은 값을 `252/300 · 84%` 로 더 크게 보여준다(값이 사라진 게 아니라 자리를 옮긴 것).
- 회귀가드 `server/tests/workdeskWidthCap.test.js` 섹션 I~K + **실브라우저 실행검증**(역할 4종 × 폭 3종 — 같은 행 유지·열 수·상한·중복 배너 0·[⋯] 열고닫기·정산 2회 갱신). ⚠ 이 변경으로 `advertiserViewer` 의 `body.advm` 캡 규칙 패턴을 함께 갱신(`.stripA`→`.tp3grid`, 검사 의미 불변).

### 화면 폭 상한 — 한 원칙, 값은 코드가 진실원본
- **원칙**: 표·카드는 `width:100%` 로 두고 **감싸는 컨테이너 폭만** 좁혀 hug-left + 오른쪽 여백. 헤더 버튼바(`.mh`)에는 **아래 데이터 영역과 같은 값**을 걸어야 버튼이 그 데이터의 오른쪽 끝에 붙는다(캡이 없으면 `.sp{flex:1}` 이 남는 폭을 다 먹는다). 공유 클래스(`.mh`·`.lgwrap`·`.pcards-grid`) 자체는 건드리지 않고 **뷰 컨테이너 id 로 스코프**한다 — 뷰마다 필요 폭이 달라 공유 클래스에 걸면 값이 충돌한다.
- ★★ **한 화면의 헤더 캡과 본문 캡은 반드시 같은 숫자** — 두 곳에 따로 적혀 있어 한쪽만 바뀌면 조용히 어긋난다. **실제 픽셀 값은 CSS 가 진실원본이고 회귀가드 `server/tests/workdeskWidthCap.test.js`·`csConvWidthCap.test.js` 가 고정**한다(여기에 숫자를 다시 적지 않는다).
- ★ **카드 자체 크기는 안 건드린다** — 컨테이너 폭만 제한해 열 수를 고정한다(카드 렌더러는 admin·workdesk 공용이라 셀렉터를 고치면 양쪽이 같이 바뀐다).
- ★ **`.tb2`(작업 탭바)만은 `padding` 이 아니라 `margin-inline`** — 자기 자신이 가로 스크롤 컨테이너라 padding 으로 상한을 걸면 스크롤 내용이 그 영역을 **뚫고** 창 끝까지 그려진다(클리핑이 border-box 기준). sticky 배경 바는 요소에 `max-width` 를 걸지 말고 `padding-inline:max(...)` 로 안쪽만 좁힌다(배경 띠가 끊긴다).

### ★ 작업바 정렬 = 모집중 우선 (사용자 확정 2026-08-10)
- **규칙**: 업체(1단) = **미지정 마지막 → 업체 즐겨찾기(★) → 모집중 작업 수 desc → 리뷰 대기 작업 수 desc → 남은 인원 desc → 가나다** / 작업(2단) = **모집중 → 모집 종료+리뷰 대기(남은 건수 많은 순) → 나머지**. 마감 작업 제외(088)·`selTab(i)` 인덱스 계약은 **불변**(idxs 배열의 순서만 바뀐다).
- ★★ **공고 상태 판정 사본 0 = `_campRank`**(홈 [금일 진행] 필터와 같은 함수) — state 문자열 표를 여기서 새로 만들면 홈 필터와 작업바가 **서로 다른 "모집중"** 을 갖는다. ★ 다만 **`soft_full`(총원 충족)만 따로 뺀다**(`_wRecruiting`) — `_campRank` 는 그걸 1등급(살아있음)으로 보지만 **사용자 기준에서 총원이 다 찬 건 "모집 종료"** 이고 그게 곧 2순위(리뷰 대기)의 정의다.
- ★★ **모르는 것을 앞으로 끌어올리지 않는다**: `_wTabPending` 은 `total`·`submitted` 가 **둘 다 숫자일 때만** 계산한다. `+null||0` 이 0 이라 가드가 없으면 **"총원은 아는데 제출 수를 모름"이 전건 미제출로 세어져 모르는 작업이 최상단**에 온다(변이시험이 잡은 실제 빈틈).
- ★ **사람이 정한 것을 자동이 덮지 않는다** — 업체 즐겨찾기(★)는 담당자가 명시로 왼쪽에 고정한 것이라 모집중 수보다 우선한다.
- ★★ **재료는 이미 있는 것뿐(서버 변경 0)** — `campaigns`·`stats` 는 `?stats=1` 응답에만 실리므로 `loadTabs` 뒤에 **비차단으로 한 번** 받아 도착하면 다시 그린다(60초 클라 + 30초 서버 캐시 = **탭 전환마다 다시 안 나간다**, "작업바 로드엔 stats=1 을 붙이지 않는다"는 규율 유지). ★ **실패하면 전부 같은 등급이 되어 종전 정렬(즐겨찾기 → 가나다)로 자연 폴백**한다.
- ★ **왜 이 순서인지 화면이 말한다**: 업체 칩 툴팁(`모집중 5 · 리뷰 대기 1`, 조회 실패면 그 사유 — 둘 다 실패면 "이름순으로 표시") + 작업 칩 **점**(초록=모집중 · 앰버=리뷰 대기, **등급 2는 점 없음** = 신호 아닌 것에 색을 쓰지 않는다).
- ★ 같은 등급은 **원래 인덱스로 tiebreak**(안정 정렬 — 재렌더마다 칩이 뒤바뀌면 누르려던 칩이 움직인다).
- 회귀가드 `tests/workbarRecruitSort.test.js`(`_wGroups` 를 **vm 으로 실제 실행**해 순서를 대조, **변이시험 검출 확인**) + 실브라우저 실측(1단·2단 순서·점 색). ⚠ 이 변경으로 `workdeskTopbar` 가드의 추출 목록(`WANT`)에 새 의존 6종을 추가했다 — **그 목록은 `_wGroups`/`_renderTabList` 가 새 전역을 부르면 함께 늘려야 한다**(스텁을 두면 정렬 규칙이 거기서만 딴판이 된다).

### ★ 작업보드 표 — 오늘 참여현황 표기 (툴바 맨 앞 · 시안 C+B 믹스)
- **시안** `frontend/docs/design-grid-today-progress.html`(A/B/C + 상태 6종 + 결정사안, `?v=A|B|C`). 사용자 확정 = **C안 도넛 + B안 파란 날짜**. 시트 그리드 툴바 **맨 앞**(검색창 왼쪽)에 `◔ 8/10 (월) 참여현황 12/20명` — "오늘 몇 명을 채워야 하고 지금 몇 명이 들어왔나"를 표를 세지 않고 알게 한다.
- ★★ **정원 판정 사본 0 = `computeCampaignState.dailyQuota` 그대로**(`trackB.tabTodayProgress`): 여기서 "일건수 + 이월"을 다시 세우면 **066 이월 상한 · 095 날짜별 조절 · 098 이월 보류 · 총량 clamp** 를 모르는 두 번째 기준이 생겨 **툴바는 20명인데 카드·[📅 인원] 은 25명**으로 갈린다(`todayNaturalQuota` 를 서버가 실어 보내는 것과 같은 이유). 재료 3종(counts·시트 일정·now)도 `tabCampaignsMap`/`admin/list` 와 같은 것을 모은다. **프론트는 서버가 준 값을 그리기만** 한다.
- ★★ **분자 = 작업표 기준(사용자 확정 2026-08-10 B안 — 종전 "오늘 제출 확정"에서 변경)**: `tabFilled.service.todayFilledMap` 이 `review_index`(이름 있는 행만 = **채워진 줄**)에서 **구매일자가 오늘인 행 수**를 센다. 계기 = 장수돌침대 900건에서 표 27줄인데 표기는 8명(지각 참여·시트 수기 입력·외부모집분이 공고에 없어 안 세어짐, 진단 문서 `frontend/docs/design-participation-count-mismatch.html`).
  - ★★ **계산은 한 곳** — 모집공고 카드(`/campaign/admin/list` → `todayFilled`)와 작업보드 툴바(`tabTodayProgress` → `sheetFilled`)가 **같은 함수**를 소비한다. 각자 세면 "왜 카드와 표가 다르냐"가 방향만 바꿔 되돌아온다.
  - ★★ **`parseDateColumn` 에 `fallbackAnchor` 필수** — 시트 표기는 `8 / 10 (월)` 로 **연도가 없어** 안 넘기면 전부 null → "오늘 0명"으로 조용히 죽는다(W2-b F-2 와 같은 자리).
  - ★★ **중복 탭 키 제거** — 한 탭에 공고가 여럿(차수 재발행)이면 같은 키가 두 번 들어와 JOIN 이 행을 **두 번 매칭**해 숫자가 배가 된다("42/42 완료"로 위장).
  - ★★ **소비처는 셋** — 작업보드 툴바 · 관리자 모집공고 탭(`/campaign/admin/list`) · **홈 모집공고 카드(공개 목록 `/campaign/list`)**. 홈 카드가 빠지면 "작업보드는 맞는데 홈만 다르다"가 남는다(실제로 그렇게 신고됐다). 공개 목록은 **5초 캐시 안에서** 계산한다.
  - ★★ **표시는 운영자 게이트 뒤에만**(`admin || _adminTok`) — 일반 리뷰어에게는 종전(공고 기준) 그대로여야 한다. 참여 허용 판정이 공고 기준이라 리뷰어 화면까지 바꾸면 **"게이지는 찼는데 참여는 되는"** 상태가 생긴다. 재료(`todayFilled`)는 공개 응답에 실리되 **그리는 쪽이 게이트**를 건다. ★ 게이트는 **`✏️ 관리자 수정` 칩과 같은 범위**(`_adminTok` = 진짜 admin_token **또는** 공고수정 허용명단 스코프 토큰) — `_realAdminTok`(진짜 토큰만)로 좁혔더니 **스코프 토큰으로 홈을 보는 담당자에게 카드만 옛 숫자로 남았다**(실측 신고 2026-08-10: 서버는 40 을 내려주는데 화면은 13). 회귀가드가 두 게이트의 **동일성**을 고정한다.
  - ★ 표시 숫자를 바꾸면 **완료 판정·채움 폭도 같은 값**을 따라가야 한다(`pubFull`/`shownFull`) — 안 그러면 39/42 인데 카드가 "완료" 색으로 칠해진다.
  - ★ **못 세면 null → 종전 값으로 폴백하고 그 사실을 말한다**(0 위장 금지). ★ **표 ≠ 공고 확정의 차이는 툴팁으로 남긴다** — 그 차이가 곧 **지각 참여 확정 대기·수기 입력 신호**라, 표기를 바꿔도 신호를 잃지 않는다.
  - 진행 중(결제 중 홀드)은 `holds` 로 따로 — 합치면 홀드 만료 때 **숫자가 줄어든다**.
  - 회귀가드 `tests/sheetBasedParticipation.test.js`(, **변이시험 검출 확인**). ⚠ 가드 함정 2종: **스텁은 SQL 을 해석하지 않으므로 WHERE 절은 쿼리문으로 고정**할 것 · **`indexOf` 순서 단언은 `-1 < n` 이 항상 참**이라 존재 확인을 먼저 할 것(둘 다 변이시험이 실제로 뚫었다).
- ★★ **"없다"와 "모른다"를 다른 모양으로**: 정원 0(휴무·일정 종료·총원 충족·0명 조절) = 회색 `0/0명 · 오늘 모집 없음` / 공고 없음·게시 전 = 점선 `12 / ?명 · 모집 기준 없음`(**구매한 인원은 사실이라 그대로 보여주고 분모만 ?**) / 조회 실패 = 점선 `? / ?명 · 불러오지 못함`. **0/0 으로 위장하면 "오늘 할 일 없음"으로 읽힌다.** 초과는 감추지 않고 `+3`.
- ★★ **gid 는 서버가 `tab_configs` 에서 다시 구한다** — 클라이언트가 보낸 `tabGid` 를 믿으면 낡은 화면이 **같은 시트 다른 탭의 공고 정원**을 이 표에 띄운다(스코프 판정이 tabGid 를 안 믿는 것과 같은 규율). 매칭은 **이름 + gid 폴백**(리네임으로 연결이 조용히 풀리면 멀쩡한 공고를 두고 "모집 기준 없음"이 뜬다), ★ **빈 gid 는 절을 켜지 않는다**(`$3 <> ''` — 켜면 gid 없는 공고가 전부 매칭).
- ★ 살아있는 공고가 여럿(차수 재발행)이면 **합산하고 `campaignCount` 로 그 사실을 말한다**(하나만 고르면 나머지 물량이 조용히 빠진다). 참여형 + `status='active'` 만 대상(레거시는 정원 개념이 없다).
- ★★ **업체(광고주) 뷰어에도 같은 자리·같은 모양으로 표기한다**(사용자 확정 2026-08-10) — 단 **`_tpAdvertiserLens` 를 거친다**: 공고를 거친 확정 수·결제 중 홀드·합산 공고 수는 **응답에서 폐기**하고 `done` 을 표 기준과 같은 값으로 눕혀 **"차이 N명(지각 확정 대기·수기 입력)" 내부 문구가 아예 생성되지 않게** 한다. ★ **화이트리스트 재구성**(`{...tp}` 스프레드 금지 — 나중에 필드가 늘면 조용히 샌다). 화면은 무수정(`buildGrid` 가 광고주에게도 같은 툴바를 그린다). · **읽기 전용**(쓰기 쿼리 0 · 시트 무접촉 · 클릭 동작 없음) · **fail-soft**(어떤 실패도 throw 하지 않아 작업보드 로딩을 죽이지 않는다).
- ★ **칩 높이 28px = 툴바의 다른 컨트롤과 같다** → 툴바 높이 불변(실측 32px, 칩 없을 때와 동일 = 표가 아래로 안 밀린다). 도넛은 `conic-gradient` 한 겹(외부 이미지·SVG 0), 클래스는 **`tprog` 접두로 스코프**(맨몸 `.ring` 금지). 좁은 화면(≤720px)은 라벨만 접고 **숫자는 어떤 폭에서도 남긴다**. 날짜 표기는 `_fmtKDate` 단일 출처.
- 회귀가드 `tests/gridTodayProgress.test.js`(스텁 pool 로 서비스 **실제 실행**(총량 clamp 로 판정 사본 검출) + 렌더러 vm 실행 6상태 + 배선·CSS 계약, **변이시험 검출 확인**) + 실브라우저 검증상태 실측(툴바 높이 불변·칩이 검색창 왼쪽·JS 오류 0). ⚠ 이 변경으로 `gridSearch` 가드의 "검색창이 툴바 맨 앞" 패턴을 갱신했다(검사 의미 불변 — 앞에 올 수 있는 것은 **읽기 전용 표기뿐**).
- ⚠ **가드 함정 2종(변이시험이 실제로 뚫었다)**: ① 시그니처 검사를 파일 전체로 하면 **호출부 문자열**(`tabTodayProgress(db, { sheetId, tabName })`)이 대신 통과시킨다 → **선언부 destructuring 만** 볼 것. ② `title="${esc(tip)}"` 는 이 파일에 **5곳**이라 파일 전체 검사는 여기서 escape 를 빼도 통과한다 → **함수 본문으로 잘라서** 볼 것. ⚠ **변이시험은 커밋 후에** — `git checkout` 이 미커밋 변경(이번엔 인접 가드 갱신)을 통째로 되돌린다.

### ★★ 작업보드 표 — 클릭 편집 폐지 · 우클릭 편집 · 범위 복사/붙여넣기 (사용자 확정 2026-08-10)
- **바뀐 것 3가지**: ① **왼쪽 클릭은 더 이상 편집을 시작하지 않는다**(셀 렌더러의 `onclick="beginEditCell(this)"` 제거) — 표를 훑다 스친 클릭 한 번이 곧 편집 시작이라 값이 실수로 바뀌었고, 그 상태로는 드래그·복사가 매번 편집창을 열며 어긋났다 ② **편집 진입 = 셀 우클릭 메뉴**(`_openCellMenu` → [✏️ 이 셀 편집], 키보드 `F2`/`Enter` 도 같은 경로) — **선택한 그 셀 하나만** ③ 드래그 범위를 **Ctrl+C 복사 · Ctrl+V 붙여넣기**.
- ★★ **선택은 직사각형** — 세로로만 끌면 그 열만 잡히고 오른쪽 열(수취인·쿠팡id·연락처·주소)은 선택도 복사도 되지 않는다(사용자 확정 예: 참여자 이름 열만 세로 드래그). 4방향 자유(역방향 드래그도 같은 직사각형), 그룹 머리행은 제외.
- ★★ **Ctrl(⌘)+클릭·드래그 = 떨어진 범위 추가**(사용자 확정 2026-08-10 — 사이에 낀 열(id)을 빼고 `수취인+연락처+주소` 만 복사하는 실제 작업). 복사본은 **행 오름차순 · 그 행 안에서 화면 열 순서**로 이어 붙는다(**누른 순서가 아니다** — 붙여넣는 쪽 열 순서와 어긋나면 안 된다). 겹치는 칸은 1회.
- ★★ **떨어진 범위 붙여넣기는 그 칸들에만** — 밖으로 넓히지 않는다(어디까지가 대상인지 사람이 눈으로 정한 상태라 임의 확장은 사고). 단일 범위만 종전처럼 복사본이 크면 아래·오른쪽으로 넓힌다. ★ **복사본에 없는 칸은 건드리지 않는다**(빈 값으로 지우지 않는다).
- ★★ **그리기(`.gselrange`)와 논리 범위(`STATE.gSelRanges` 목록 / `gSelRange`=활성 범위)는 `_setCellSel`·`_updateActiveSel`→`_paintSel` 한 곳에서만** 만든다 — 복사·붙여넣기·배경색이 각자 범위를 다시 세면 "칠한 칸과 복사한 칸이 다른" 상태가 된다. 복사·붙여넣기·색칠의 재료는 전부 `_selectionGrid`(합집합을 격자로).
- ★★ **재렌더(필터·정렬·탭 전환) 시 `buildGrid` 가 선택을 비운다** — 범위는 행/열 **인덱스**라 남겨 두면 복사·붙여넣기가 **엉뚱한 칸**에 적용된다.
- ★★ **붙여넣기는 `.gedit` 셀에만** — 참여자·연락처(신원)·`#` 은 잠금이라 건너뛰고 **건수를 말한다**(조용한 누락 금지). 저장은 기존 **`commitCellEdit` 한 경로**(사본 0 = 낙관 반영·실패 롤백·이력이 그대로). 같은 값은 왕복을 만들지 않고, 한 칸만 복사해 여러 칸을 선택하면 **채운다**(엑셀식). 상한 `_PASTE_MAX`(500, 셀당 저장 왕복 1회)·20칸 초과는 confirm.
- ★ **복사는 열람 전용(광고주·명단 밖 AE)에서도 된다**(읽기라 안전) — 붙여넣기만 `STATE.canEdit` 게이트. ★ 페이지 어딘가의 텍스트를 직접 드래그해 복사하는 중이면 `copy` 를 **가로채지 않는다**, 표 밖을 누르면 선택 해제(Ctrl+C 를 계속 가로채지 않게).
- ★ 배경색(migration 080)은 드래그가 끝나면 자동으로 뜨던 팔레트 → **우클릭 메뉴 안 스와치 줄**로 옮겼다(드래그를 복사에 내주기 위해). 저장 경로(`_applyRangeColor` → `/workdesk/cell-color`)는 무변경. 메뉴는 **body 직속**(표는 가로 스크롤 컨테이너라 안에 그리면 잘린다)·onclick 에 시트 문자열 보간 없음·최상위 1회 바인딩.
- 회귀가드 `tests/gridCellRangeEdit.test.js`(배선 + **가짜 DOM 위에서 선택·복사·붙여넣기 실제 실행**: 세로 드래그가 옆 열을 안 물어오는지·잠긴 칸 건너뛰기·엑셀식 채우기 · Ctrl 다중 범위의 열 순서·붙여넣기 대상) + 실 http 오리진 브라우저 실측(클릭≠편집 · 드래그 TSV · 클립보드 · 우클릭 메뉴 렌더 · 메뉴 편집 · Ctrl+V 2×2).

### ★ 작업보드 표 검색 (실시간 · 노란 하이라이트 · 자동 이동) — 시안 C
- **시안** `frontend/docs/design-grid-search.html`(A/B/C 목업, `?v=A|B|C` 단독 보기 — **가짜 데이터로 실제 동작**). 사용자 확정 = **C안**(툴바 왼쪽 인라인 + 스크롤 위치 눈금) · **[일치하는 행만 보기] 제외** · **왼쪽 고정 3열(#·참여자·연락처) 검색 대상 포함**.
- 시트 그리드 툴바(`.gridbar`) **맨 앞** = 표 왼쪽 위에 검색창. 치는 즉시 찾아 **일치 셀 연한 노랑 + 일치한 글자 진한 노랑**, **현재 1건만 주황 테두리**. 가로로 밀려나도 남게 `#` 칸에 세로 막대, 표 오른쪽 바깥 띠(`.gsmap`)에 결과 분포 눈금.
- ★★ **매칭은 데이터(`STATE.gRows`)에서 센다 — DOM 훑기 금지**: 이 그리드는 첫 `_GRID_CHUNK`(60)행만 즉시 그리고 나머지는 rAF 로 이어 붙이므로, DOM 기준이면 **아직 안 그려진 행이 통째로 결과에서 빠진다**. 대신 **이동할 때** `_gsFlushAll` 로 남은 청크를 먼저 다 그린다. ★ 회귀가드는 "호출이 있나"가 아니라 **`_gsFlushAll` 이 첫 `_gsTr(` 조회보다 앞인지 순서를 본다** — 그룹 펼침 분기에도 같은 호출이 있어 존재 검사만으로는 첫 호출 제거를 통과시킨다(변이시험 실측).
- ★★ **하이라이트는 `!important`** — 셀 배경은 이미 세 갈래로 칠해진다: ① 담당자 지정 셀 배경색(migration 080)은 **인라인 style** 이라 클래스로는 못 이긴다 ② 편집 셀(`.gedit.edited`) 파란 배경 ③ 행 호버. 하나라도 못 이기면 "찾았다는데 노란 게 안 보이는 셀"이 생긴다. ★ 기존 앰버 `.rowhit`(리뷰어 로그 딥링크 행 강조 `#fef3c7`)와 **색을 분리**(검색 `#fff3b0`/`#ffe14d`, 현재 `#ff8a00`).
- ★★ **이동은 세로뿐 아니라 가로도** — 주소·계좌번호는 표 오른쪽 끝이라 세로만 맞추면 "찾았다는데 화면엔 안 보인다". `STATE.gFrozenW`(왼쪽 고정열 폭 합, buildGrid 가 기록)로 **고정열 뒤에 가려지지 않는 위치**까지 계산하고 sticky 헤더 높이도 뺀다. 고정열 셀(`.gfz`)은 가로 이동 안 함.
- ★★ **입력 중 그리드·툴바를 재렌더하지 말 것** — 입력칸이 든 툴바를 다시 그리면 **한글 IME 조합이 깨진다**(홈 작업목록 검색에서 실측된 `ㅁ며면면` 사고와 같은 자리). `_gsInput`/`_gsPaint`/`_gsStatus` 는 `#gbody` 셀과 카운터 `textContent` 만 건드린다(120ms 디바운스).
- ★ **`<mark>` 는 텍스트 노드만 훑어 삽입** — 셀 innerHTML 을 통째로 갈면 배지(주문/수동)·되돌리기(↩) 마크업이 깨진다. 해제 시 텍스트로 되돌리고 `normalize`.
- ★ **조용한 누락 금지**: 결과 없으면 입력칸 빨강 + "결과 없음", **필터로 숨겨진 행에 결과가 있으면 건수를 고지**, `n개 행 · m칸 일치`로 풀어서 표기. ★ 재렌더(필터·정렬·숨김) 후엔 하이라이트만 복원하고 **다시 스크롤하지 않는다**(보던 자리를 뺏지 않게). ★ **탭 전환 시 검색어 초기화**(남으면 "왜 여기만 노랗지"가 된다).
- 조작: `Enter` 다음 · `Shift+Enter` 이전 · `↑↓` · `Esc` 지우기, 마지막 다음은 처음으로 **순환**. 4자리 이상 숫자는 "숫자만" 비교도 함께 봐 `010-3498-2400` 을 `34982400` 으로 찾는다(3자리 이하는 폴백 미적용 — 짧은 숫자로 표가 통째로 노래지는 것 방지).
- 회귀가드 `tests/gridSearch.test.js`(정적 배선 + **`_gsCompute` vm 실행**, 변이시험 검출 확인) + 실브라우저 실행검증(청크 밖 행 탐색·가로/세로 이동·IME 조합 실측·필터 고지·탭 전환·광고주 화면). ⚠ 이 변경으로 `advertiserViewer` 가드의 `rerenderGrid` 패턴을 함께 갱신(검사 의미 불변).

### ★★ 작업보드·업체 공유 링크 — 메신저로 그 화면 주소를 바로 전달 (migration 131, 사용자 확정 2026-08-19)
- **발단(직원 요청)**: 택배·문의 인수인계 때 "그 업체 그 작업"을 상대가 **검색 탭에서 매번 찾아** 들어가야 했다. 작업보드에는 고유 주소라는 개념이 아예 없었다(리뷰어 로그 → 작업보드 딥링크 `#go=` 는 있었지만 **복사해 공유하는 창구가 어디에도 없었다**). 이제 작업보드 상단 **[🔗 링크 복사]** 한 번이면 `…/workdesk#w=<code>` 가 복사되고, 카톡으로 받은 사람은 열자마자 그 작업보드로 직행한다. 업체관리 업체 패널의 **[🔗 업체 링크 복사]** 는 그 업체의 작업 목록으로 직행한다.
- ★★ **링크는 권한이 아니다(완화 금지)** — `code` 는 **어디로 갈지만** 말한다: ① 여는 사람은 평소대로 **로그인해야 하고**(비로그인 진입 없음) ② 되찾기 게이트는 **작업보드 본문과 같은 단일 출처 `_ensureEditScope`**(내부 직원 허용 · **광고주는 탭 이름조차 못 받는 403**, 라우터 `internalMiddleware` 와 이중). 그래서 링크가 카톡방에 굴러다녀도 권한이 넓어지지 않는다. ★★ **여기에만 담당 스코프를 걸지 말 것**(2026-08-19 확정) — 내부 직원은 이미 모든 작업보드를 열므로(`allowAllStaff`) 링크만 좁히면 **목록엔 보이는데 링크로는 안 열리는 막다른 길**이 된다. 좁힐 때는 `/workdesk`·`/tabs` 와 **함께**. **광고주 전용 접속 링크(058)와 정반대의 물건** — 그쪽은 로그인을 대신하는 열쇠, 이쪽은 로그인한 사람에게 길만 알려 주는 이정표다.
- ★★ **공유 주소에 토큰을 싣지 않는다** — 리뷰어 로그의 `_logOpenWorkdesk` 는 **자기 새 탭용**이라 `&sso=<토큰>` 을 붙이지만, 사람에게 건네는 주소에 그러면 **세션을 통째로 넘기는 링크**가 된다(회귀가드가 `_shareLinkCopy` 본문에 토큰 문자열 부재를 고정).
- ★ **대상마다 코드 1개**(부분 유니크 `(sheet_id, tab_name) WHERE kind='tab'` · `(advertiser_id) WHERE kind='advertiser'`) — 언제 눌러도 **같은 주소**가 나온다. 이미 공유해 둔 링크가 다음에 눌렀다고 죽으면 "고유 링크"가 성립하지 않는다. 발급은 `ON CONFLICT DO NOTHING` + 재조회(경합 안전), `tab_gid` 는 **blank-only 보정**.
- ★ **쓰기 표면 = `trackb_share_links` 한 곳**(운영 테이블·시트·인트라넷 무접촉) · 업체 삭제 시 CASCADE · 되돌리기 = 라우트 제거 + DROP TABLE. **131 은 신규 테이블이라 `REQUIRED_SCHEMA`(컬럼만 검사)로는 못 막는다** → 42P01/42703 은 `not_ready` 로 마이그레이션 번호를 말한다(088 무신호 규율).
- ★★ **로그인 전에 열어도 살아남는다** — `#w=` 는 `initToken`(해시 제거)보다 **먼저** 읽어 `_SHARE` 모듈 변수에 담는다. `boot` 이 로그인 화면으로 되돌아가도 값이 남아 **로그인 직후 boot 재실행 때 소비**된다(실브라우저로 왕복 확인). 소비 시 주소창에서 해시를 지운다(새로고침 재발동 방지).
- ★ **실패는 조용히 홈으로 떨어뜨리지 않는다** — 폐기·담당 밖·업체 삭제를 각각 사유로 말한다(`_SHARE_ERR` → alert / 업체 미발견 → 토스트).
- ★★ **클릭 = 즉시 복사, 확인 팝업 없음**(사용자 확정 2026-08-19 — 주소를 눈으로 확인할 일이 없다): 성공하면 **화면 정중앙에 「링크가 복사되었습니다」가 천천히 페이드아웃**(`_shareLinkOk` — body 직속 · `pointer-events:none` 이라 화면 조작을 막지 않음 · **연타해도 겹치지 않음** · 애니메이션 이벤트가 안 와도 `setTimeout` 백스톱으로 반드시 사라짐 · 문구는 `textContent`). ★ **복사가 막힌 경우에만** 주소 팝업을 띄운다(조용한 실패 금지 — 복사된 줄 알고 빈 메시지를 보내는 것이 가장 나쁘다). 복사는 **`_copyText` 단일 출처**(Clipboard API → 비보안 오리진·구브라우저는 임시 textarea + `execCommand` 폴백). 팝업은 **body 직속** · Esc 리스너는 **최상위 1회**(`_shlKeyBound`).
- ★★ **창구 3곳 · 실행부 한 벌(`_shareLinkCopy`)**: ① 작업보드 상단 ② **홈 작업목록 줄마다 `[🔗 링크]`**(작업표 열 바로 뒤 `공유` 열 — 목록을 훑다가 보드를 열지 않고 바로 복사) ③ 업체관리 업체 패널. ★ 홈 목록 버튼은 **줄 클릭(작업 열기)과 겹치지 않게 `stopPropagation`** · **인덱스만** 넘긴다 · **마감 보관함에도 같은 칸을 둔다**(열 수가 모드마다 달라지면 눈이 열을 다시 찾는다 — v3 규율, 헤더 칸 수 ≡ 행 칸 수 11).
- ★ 업체 패널의 기존 **[🔗 접속 링크]**(광고주가 로그인 없이 여는 058 매직 링크)와 이름이 겹치므로, 새 버튼은 **[🔗 업체 링크 복사]** 로 두고 툴팁이 "광고주 접속 링크와 다르다"를 말한다.
- **API** = `POST /api/trackb/share-link`(발급·재사용) · `GET /api/trackb/share-link/:code`(되찾기), **둘 다 `authMiddleware + internalMiddleware`**(광고주·리뷰어 도달 불가 — 광고주에게는 058 링크가 따로 있다).
- 회귀가드 `tests/shareLink.test.js`(58케이스 — 스텁 pool 로 서비스·되찾기 라우트 **실제 실행**(같은 대상=같은 코드·재발급 0·blank-only·거부 시 쓰기 0) + 라우터 스택 실검사 + 화면 배선) + **진짜 PG16**(idempotent·부분 유니크·CASCADE) + **실 http 오리진 브라우저 실측**(공유 링크로 보드 직행 · 로그인 후 직행 · 담당 밖 사유 고지 · 팝업 body 직속 · 주소에 토큰 0).

### 리뷰웹시스템[3버전] 뒤로/앞으로 (SPA 히스토리)
- **문제**: 업체관리에서 업체→작업을 클릭하면 화면이 통째로 작업보드로 바뀌는데 히스토리에 아무것도 안 쌓여 **브라우저 뒤로가기가 리뷰웹시스템[3버전]을 벗어났다**. 뷰 전환(nav 탭)·작업 열기(`selTab`)·업체 선택(`selAdv`) 3종을 히스토리 항목으로 쌓아 뒤로/앞으로가 화면 전환을 되짚는다.
- ★★ **URL 은 건드리지 않는다** — `history.pushState(state,'',pathname+search)`(같은 URL 로도 별개 항목이 생긴다). 해시를 쓰면 `#sso=`·`#adv=`·`#go=` 프래그먼트 계약과 충돌하고, 주소 공유로 딥링크·PII 가 샌다(그 코드들이 도착 즉시 해시를 지우는 이유와 같다). 대신 새로고침으로는 복원되지 않는다(종전과 동일).
- ★ **첫 항목은 replace**(`_navBooted`) — 브라우저 최초 항목(state:null)을 낭비하면 뒤로가기 한 번이 "아무 일도 안 일어나는" 헛클릭이 된다. 같은 화면 재적재(`_navSame`)도 replace(nav 버튼 연타 = 뒤로가기 낭비 방지).
- ★★ **중간 항목 금지**: 업체관리 연결탭 클릭·관측 '열기'·`#go=` 딥링크는 `pendingTab`/`pendingAdv` 를 예약한 뒤 `switchView` 하므로, **switchView 는 예약이 있으면 쌓지 않고** 곧 이어지는 `selTab`/`selAdv` 가 최종 상태 1건만 쌓는다. 둘 다 쌓으면 "탭 없는 작업보드"를 한 번 더 거쳐 **뒤로가기를 두 번 눌러야** 업체관리로 돌아간다(신고된 증상의 재발).
- ★ **복원 중 재적재 금지**: `_navApply` 가 `_navLock` 을 잡고, 비동기로 이어지는 `selTab`/`selAdv` 에는 `pendingTab.__nav`/`pendingAdv.__nav` 표식으로 `{nav:true}` 를 전달한다(락은 동기 구간만 덮는다).
- ★ **항목 ↔ 화면 일치**: `renderWorkdeskView` 는 예약 없으면 `STATE.cur=null`, `renderOwnershipView` 는 복원이 아니면 `STATE.advCur=null` 로 **첫 await 전에** 비운다 — 안 그러면 "본문은 작업 선택 안내인데 항목엔 옛 작업"이 남아 뒤로/앞으로가 없던 화면을 되살린다.
- 복원 범위 = **어느 화면·어느 작업·어느 업체**까지. 그리드 필터·숨김열 등 표 상태는 탭 전환 시 초기화되는 클라이언트 상태라 대상 아님(`selTab` 기존 계약 유지). 회귀가드 `server/tests/workdeskHistoryNav.test.js`(배선 + **나브 함수를 vm 으로 꺼내 실제 실행**해 적재 순서·중복 억제·lock 검증). 실제 http 오리진 + 스텁 API 브라우저 실행으로 업체관리→작업→뒤로×3→앞으로×3 왕복 확인.

### ★ 업체관리 화면 리디자인 (시안 A · 업체 작업대) — 기능 삭제 0 · 재배치
- **시안·결정** `frontend/docs/design-vendor-management.html`(A/B 2안 + 기능 보존 대조표 18항목, `?v=A|B` 단독 보기, 가짜 데이터로 실제 동작). 사용자 확정 4건 = ① **시안 A**(지금 업체 중심 흐름 유지) ② 표 기본 열 = **정산상세 접힘 + 브라우저 기억**(`localStorage.wd_own_cols`) ③ **'처리필요' 배지 = 마감자료 검수 대기만**(계약 미매칭·미입금은 표의 필터 칩으로) ④ 개요 표 **집계 추가**.
- **바뀐 것은 배치와 조작뿐** — 기존 함수명·id·계약을 한 줄도 바꾸지 않았다(`#ownPanel`·`#owntabsSect`·`#advs`·`.advitem[data-i]`·`.ct`·`.pmn`·`#addSheet`·`#advSheet`~`#advCreateBtn`·`openOwnTab(i)`·`editTabMemo(event,i)`·`ownMatchContract(event,i)`·`_contractMatchApplied`·`refreshAdvCounts`·`saveAe`). ★ 신규 엔드포인트 0 · 마이그레이션 0.
- **기본 화면 = 전체 개요 표**(업체 미선택): 종전엔 빈 **거래처 추가 폼**이 뜨고 오른쪽 절반이 비어 있었다. 이제 업체별 작업수·계약 미매칭·마감자료 검수·접속링크·계정을 한 표로 보여 **어느 업체를 봐야 하는지**부터 말한다. 거래처 추가는 사이드바 [＋ 업체]·미지정 시트에서 열리는 **우측 패널**로 이동(`showAddAdvertiser`는 이름·검증·필드 id 그대로, 그리는 곳만 패널).
- **설정은 우측 슬라이드 패널**(접속링크·계정·소유 시트·소유 추가·거래처 등록·미지정 시트): 매일 보는 연결탭 표가 화면 위를 차지한다. ★ 패널은 **뷰 루트 직속 + `position:fixed`** — 스크롤 컨테이너 안에 두면 오버레이가 화면 흐름에 섞인다(모집공고 모달 실측 사고). ★ **ESC 리스너는 `document`에 1회** — 컨테이너에만 붙이면 포커스가 밖일 때 닫히지 않는 오버레이가 된다(시안 목업에서 실측).
- ★★ **마감자료 검수 대기 판정 = `trackB.service.finishCandidate(stats)` 단일 출처**: 인원 충족 + 전건 제출 + 전건 입금(088 마감 후보와 **같은 규칙**), 재료도 **홈과 같은 `tabStatsMap`**(index_master + review_index PAID). 업체관리가 `campaign_participants`(bTotal/bSub/bPaid)로 따로 세면 **같은 작업이 홈에서는 후보, 업체관리에서는 아님**으로 갈린다. ★ **통계 없으면 판정 안 함**(`total` 0·null 을 충족으로 접지 않는다 — 회귀가드가 실행으로 고정). ★ 서버가 행마다 `finishCand`/`finished` 불리언을 실어 주고 **프론트는 재계산하지 않는다**(배지·칩·표 숫자가 구조적으로 일치).
- ★★ **통계 주석은 opt-in(`annotate`)** — `ownedTabsForAdvertiser` 에 `tabStatsMap`(=`review_index` 전체 GROUP BY)을 무조건 붙였다가, 이 함수의 소비처 4곳 중 **광고주 경로**(`advertiserWorkSummary` → `/my-work-summary`, 무로그인 공개 링크로도 도달)·정산 요약·브랜드 배정에까지 그 비용이 매달렸다(CLAUDE.md "통계는 홈 전용, 작업바 로드엔 붙이지 않는다"와 정면 충돌). **켜는 곳은 `/ownership/tabs` 하나**. ★ 광고주 렌즈는 화이트리스트 재구성이라 `finishCand`/`finished` 가 광고주에게 나가지 않는다.
- ★ **`?overview=1` 의 접속링크 상태는 admin/master 에만 싣는다** — 링크를 다루는 다른 모든 라우트가 `adminOrMaster` 인데 목록만 staff 에게 열면 **서버가 프론트보다 넓어진다**(프론트는 이미 admin 분기). 작업수·미매칭·검수 집계는 staff 도 받는다(담당 판단에 필요).
- **개요 집계** `advertiserOverview` + `GET /api/trackb/advertisers?overview=1`(같은 라우트에 얹음 — 미지정 시 응답 불변): ★★ **로컬 DB만·인트라넷 무접촉**(17업체분 sales 프록시 금지) · **쓰기 쿼리 0**(접속링크를 여기서 생성하지 않는다). ★ 그래서 **개요에 미입금 열을 두지 않는다** — 입금액은 인트라넷 `sales.matched_bank_amount` 에만 있어 여기서 셀 수 없고, 0 으로 꾸미는 대신 "업체를 고르면 표의 미입금 필터로" 라고 화면이 말한다.
- ★★ **fail-soft 는 조용하지 않다 — 그리고 플래그를 "싣는 것"과 "소비하는 것"은 다른 일이다**(코드리뷰가 잡은 blocker): `advertiserOverview` 는 소스별 `statsUnavailable`/`finishedUnavailable`/`contractsUnavailable`/`linksUnavailable`, `ownedTabsForAdvertiser` 는 `{rows, statsUnavailable, finishedUnavailable}`(배열 반환에서 변경 — 호출부 **4곳** 갱신)로 올린다. 처음 구현은 서버가 이 4개를 정성껏 실어 보내면서 **화면이 `overview.ok` 만 봐서**, `ok:true` 인데 통계만 죽은 경우가 **"마감자료 검수 0건"으로 조용히 위장**됐다. 판정은 `_ovmCandKnown`/`_ovmMatchKnown`/`_ovmLinkKnown` **한 곳**으로 모아 배지·사이드바 칩·개요 표 3곳이 **열별로** `?`(사유 tooltip)를 띄우고, 판정 불가 칩은 **클릭도 막는다**(누르면 "조건에 맞는 업체가 없습니다"라는 확정 문장이 나와 다시 오독된다). ★ 특히 접속링크 열의 "모름"을 **"미생성 — 열면 자동 생성됩니다"** 로 적으면 폐기·로그인필요 링크를 **적극적으로 틀리게** 안내한다.
- ★★ **이 뷰는 내부 스크롤이다**(`.own-wrap{height:calc(100vh - var(--toph,57px))}`): 종전엔 `.own-panel{overflow:auto}` 가 있는데도 높이가 콘텐츠만큼 늘어나 **스크롤이 안 일어나 도구줄 sticky 가 영영 죽었다**(홈 목록 헤더행에서 겪은 것과 같은 함정 — 실측). 덤으로 사이드바·[미지정 시트] 버튼이 항상 보인다. ★ 좁은 화면(≤780px)은 1열로 접히므로 `height:auto` 로 문서 스크롤 복귀(고정 높이면 아래가 잘린다).
- ★★ **검색 중 입력칸을 재렌더하지 말 것**: 도구줄과 행을 분리해 검색은 `#ownRows` 만 갈아치우고, **정산 요약이 타이핑 중에 도착해도**(`_rerenderOwnTabs` → `#ownRows` 있으면 rows only) 입력칸 DOM 을 건드리지 않는다. 섹션을 통째로 innerHTML 교체하면 한글 IME 조합이 파괴된다(`ㅁ며면면` 실측 계열). 비고 편집 중 재렌더 연기(`.omemoin`)는 그대로.
- **열 4묶음 접기**(작업정보·진척·자료·정산상세): 열 폭·`min-width` 를 **JS(`_ovmGrid`)가 인라인으로** 정한다(CSS 고정값을 되돌리면 접어도 가로 스크롤이 남는다). 기본 13칸(자료 켬·정산상세 접힘)에서 FHD 가로 스크롤 없음, 정산상세를 펼치면 17칸(그때만 스크롤). ★ **헤더 칸 수 ≡ 행 칸 수 ≡ grid 열 수**는 회귀가드가 **16조합 전부 실행**해 대조한다(열을 끼워 넣을 때 가장 흔히 깨지는 자리).
- 브랜드 칩은 **상위 5개 + 드롭다운**(40개가 3줄로 넘쳐 필터 구실을 못 했다). 상태 필터 5종(전체·계약 미매칭·마감자료 검수·미입금·마감)의 **숫자와 목록은 `_ovmRowMatch` 한 판정**을 쓴다(따로 세면 "칩은 12인데 표는 3줄"). 마감된 작업은 표에 `🏁 마감` 배지로 구분(서브라인 **앞** — 뒤에 두면 시트명 말줄임에 잘려 사라진다, 실측).
- ⚠ **곁다리로 고친 라이브 버그**: `workdesk.html` 주석의 별표+빗금(`qd*/qv*/idc*`)이 주석을 조기 종료시켜 바로 아래 `.bb.docv{cursor:pointer;text-decoration:underline dotted}` 가 **브라우저에서 통째로 삼켜져** 있었다(견적서·계산서 [문서 보기] 배지가 클릭 가능해 보이지 않음). `workboardFinish` 의 CSS 균형 가드가 그 때문에 계속 빨간 상태였다 — **빨간 가드는 새 변경도 못 지킨다**.
- ★ **로딩 자리표시자를 깐 함수는 어떤 예외에도 화면을 종결시킨다** — 목록 조회가 reject 되면 사이드바·패널 두 칸이 영원히 '불러오는 중…'에 매달렸다(리뷰검수 무한로딩 사고와 같은 자리). `_ovmLoadFailed` 가 사유 + [다시 시도]로 끝낸다. 연결탭 조회 실패도 **"연결된 탭 없음 — 시트를 지정하세요"라고 행동을 지시하지 않는다**(실패인데 미지정으로 오독시킨다).
- 회귀가드 `tests/vendorManagementRedesign.test.js`(판정 순수함수·개요 집계 스텁 실행(인트라넷 무접촉·쓰기 0)·라우터 스택 실검사+핸들러 실행·프론트 배선·동적 열 8조합·**사이드바 배지 규칙을 가짜 DOM 에 올려 실행**, 변이시험 검출 확인) + 브라우저 실행검증. ⚠ 이 변경으로 `contractMatch` 가드 9d/9e 를 정적 문자열 → **빌더 실행 대조**로 갱신(검사 의미 동일·더 강함).
### ★ 업체관리 [자료] 열 + 작업보드 상단 폴더 바로가기 (시안 A + D · 사용자 확정 2026-08-06)
- **시안** `frontend/docs/design-vendor-folder-buttons.html`(배치 후보 6곳 판정표 + 3안 목업 + D 작업보드 상단, `?v=A|B|C`). 확정 4건 = ① **A안**(연결된 탭 표에 `[자료]` 열) ② 기본 **켬** ③ **3버튼**(구매·리뷰·현영) ④ **작업보드 상단도 같이**. 업체용 뷰어에는 이미 있던 폴더 바로가기가 내부 화면(업체관리)에는 없어 시트를 열어야 폴더에 닿던 것을 없앤다.
- ★★ **렌더러·열기 동작은 한 벌** — `_folBtnsInner`/`_folMat`/`_folRow`/`openTabFolder` 를 홈 작업목록·업체관리 표·작업보드 상단이 함께 쓰고, 다른 것은 **재료가 담긴 자리와 버튼 크기뿐**이라 `src('home'|'own'|'cur')` 로 그것만 가른다. 사본을 두면 한쪽만 `_folUrlOk`(Drive 호스트 검증)가 풀리거나 현영 판정이 갈라진다.
- ★★ **현영 판정 단일 규칙 = `captureSlots.hasCashReceiptSlot`**(`effectiveCaptureSlots` 파생 — 규칙 사본 0): 소비처 3곳 = `tabStatsMap`(홈 버튼) · `ownedTabsForAdvertiser`(업체관리 표) · `/tab-folders`(서버 폴더 해석). 종전에는 홈만 `income_type` 을 보고 라우트는 `capture_slots` 까지 봐서, 수동 슬롯 탭이 **"버튼은 비활성인데 서버는 허용"** 으로 갈라져 있었다. ★ 슬롯을 `[review]` 로 명시한 탭은 업로드가 현영 서브폴더를 만들지 않으므로 '대상 아님'이 사실이다(업로드 경로와 같은 판정).
- ⚠ **아직 통일되지 않은 소비처 1곳(의도)**: 리뷰어 공개 카드의 `🧾 현금영수증` 배지(`campaign.routes._cashReceiptFlags`)는 여전히 `isCashReceiptIncome`(income_type) 만 본다 — 그건 **리뷰어에게 참여 전 안내**를 켜는 다른 기능이고(현금영수증 3시점 D안), 바꾸면 리뷰어 화면 배지가 사라지는 탭이 생긴다. 폴더 바로가기 3곳만 `hasCashReceiptSlot` 으로 묶었다.
- **재료 공급**: 업체관리는 연결탭 응답에 `cashReceipt` 를 파생해 싣는다(`tab_configs` 를 **이미 조인**하고 있어 쿼리 순증 0, 판정 원재료 `capture_slots`·`income_type` 는 **응답에서 버린다** — 316행 × JSONB 전송 낭비 + 프론트 재판정 금지). 작업보드는 **`GET /api/trackb/tab-folders?kind=info` 가산 분기**(그 탭 **한 줄**만 읽음 · 신규 엔드포인트 0 · 같은 스코프 게이트 · 60초 캐시) — ★ **홈처럼 `?stats=1`(review_index 전체 GROUP BY)을 붙이지 않는다**(탭 전환마다 무거운 집계 금지), 홈을 거쳐 왔으면 `t.stats` 를 재사용해 **요청 0**.
- ★★ **"모른다"와 "없다"를 구분한다**(조용한 거짓 안내 금지): 통계 미도착·조회 실패는 비활성 + **그 사유**(폴더가 없다고 말하지 않는다), `cashReceipt` 가 **undefined**(구버전 백엔드)면 '대상 아님'으로 단정하지 않고 "알 수 없습니다". 비활성은 **숨기지 않고 옅은색**(줄마다 버튼 수가 같아야 눈이 열을 다시 찾지 않는다 — 홈과 같은 규율).
- ★ **onclick 은 인덱스만**(`openTabFolder(0,'capture','own')`) · 폴더 버튼은 `stopPropagation`(행 클릭 = 작업보드 열기로 튀지 않게) · 새 창은 **await 전에** 열어 둔다(현영은 서버에 물어본 뒤 이동 — 비동기 뒤 `window.open` 은 팝업 차단).
- ★ **자료 열 폭 136px** — `.wbl-fol` 은 `3×40px + gap 3px×2 = 126px` 고정 그리드다. 좁히면 버튼이 칸 경계를 넘어 옆 열(계약) 위에 겹쳐 그려진다(grid 트랙 이탈은 `scrollWidth` 로 안 잡혀 **실측**으로만 보인다).
- ★★ **표 폭 상한 1400 → 1560px**(`.ovm-hd`·`.ovm-tools`·`#owntabsSect` **세 곳 같은 값**) — 열이 늘어난 만큼 넓혔다(사용자 지시). 한 곳만 넓히면 버튼이 데이터 오른쪽 끝에서 어긋난다(`#wohead .mh` 규칙과 같은 이유). FHD(1920)에서 가로 스크롤·작업명 잘림 0. ⚠ 노트북(≈1440)은 **[자료] 없이도 이미** 스크롤이 나는 폭이고(필요 1222 vs 여유 1152), [자료]를 끄면 종전 폭으로 되돌아간다(열 묶음 토글이 탈출구).
- ★ **광고주에게는 작업보드 상단 버튼을 그리지 않는다** — 광고주 화면엔 이미 자기 폴더 링크가 있고 `/tab-folders` 는 `internalMiddleware` 라 누르면 403 막다른 길이 된다. **staff(AE)는 담당이 아니어도 전부 연다**(사용자 확정 2026-08-19 — `/workdesk`·`/tabs` 가 이미 `allowAllStaff` 라 폴더 버튼만 막는 것은 반쪽 규칙이었다. 좁히기로 되돌린다면 그 둘·공유 링크와 **함께** 좁혀야 한다).
- ★ 도착 후에는 **버튼 묶음(`#folBar`)만** 갈아치운다(그리드 전체 재렌더 금지 — 편집 중 셀·검색 하이라이트를 잃는다) + `STATE.cur===t` 확인(다른 작업으로 옮긴 뒤 도착한 응답 미반영).
- ★★★ **캐시는 스코프 게이트 뒤에 둔다 (코드리뷰가 잡은 블로커)**: `/tab-folders` 가 캐시 히트를 `canAccessTab` **앞에서** 반환해, 누군가 그 탭을 최근에 열어 둔 동안(info 60초 · 현영 10분) **담당 밖 AE 가 Drive 링크를 그대로 받았다** — 이 라우트가 스스로 적어 둔 불변식이 캐시 한 줄로 무력화된 상태였다. 순서 고정 = **파싱 → 스코프 → 캐시 → 작업**. ⚠ **기존 403 가드가 이 구멍을 통과시켰다** — 한 번도 캐시되지 않은 탭명을 썼기 때문. **스코프 검사는 캐시가 채워진 키로도 확인할 것**(admin 으로 데운 뒤 staff 로 호출).
- ★★ **현영 슬롯은 key 가 아니라 라벨로도 찾는다**: `capture_slots` 의 **유일한 writer**(`tabconfig.routes` 캡처 슬롯 저장)는 key 를 **위치로** 부여한다(`0번=review`, 그 외 `slot2`…) — 즉 **`receipt` 라는 key 는 영영 저장되지 않는다**. key 만 보면 관리자가 `[리뷰, 현금영수증]` 으로 설정한 현영 탭의 버튼이 "대상 아님"으로 죽는다(업로드는 그 라벨로 폴더를 실제로 만들어 둔 상태). 판정 = `cashReceiptSlotInfo`(key `receipt` **또는** 라벨에 현금영수증·현영·지출증빙). ★ **Drive 폴더 이름도 그 슬롯의 실제 `label`** — 종전 `slotLabel(...,'receipt')` 은 수동 슬롯 탭에서 문자열 `receipt` 폴더를 뒤졌다(함께 고침). ★ 현영인데 슬롯에 그 칸이 없으면 **'대상 아님'으로 뭉개지 않고** 고칠 곳을 말한다(`CR_MISCONFIG_NOTE` 단일 문구, 서버·툴팁 공용).
- ★★ **배포 스큐 표식 `kind:'info'`**: 구버전 백엔드는 `kind` 를 모르고 **현영 분기로 답한다** — 표식이 없으면 프론트가 그 `{ok:true,url}` 을 info 로 오독해 폴더가 멀쩡히 있는데도 "아직 폴더가 생성되지 않았습니다"로, `{ok:false,'대상 아님'}` 이면 **구매·리뷰 버튼까지** 그 문구로 위장한다(실측 시나리오). 표식 없는 응답은 '모른다'로 두고 사유를 말한다. ★ 표식은 **응답 3경로(신규·캐시·미등록) 전부**에 실려야 한다(한 곳만 붙이면 샌다 — 변이시험이 실제로 통과시켰다).
- ★ **실패한 `_fol` 이 나중에 도착한 `stats` 를 덮지 않는다**: `_finEnsureStats` 는 같은 탭 객체에 stats 를 **병합**하므로(identity 보존), 직행 진입 → info 일시 실패 → 홈 방문 → 재진입 이면 버튼이 **세션 내내** 죽어 있었다. `const f=(t._fol&&!t._fol.err)?t._fol:(t.stats||t._fol)`. ★ 실패는 **재진입 시 재시도**(한 번의 장애로 되살릴 방법이 없어지지 않게).
- ★★ **업체관리 목록의 staff 폴더 스코프**: 이 화면은 AE 가 **남의 업체도 골라 볼 수 있는데** 응답에 Drive 링크가 실려 있으면 [자료] 버튼이 곧 담당 밖 폴더 접근 수단이 된다(`/tab-folders` 가 담당 밖을 막던 시절의 비대칭 — 그 서버 게이트는 2026-08-19 확정으로 폐지됐고, **이 목록 쪽 마스킹만 남아 있다**). `/ownership/tabs` 가 staff 일 때 `staffOwnsAdvertiser` 한 쿼리로 판정해 **폴더 URL 2개와 현영 판정을 비우고 `folderScoped:false` 로 고지**한다(조용히 비우지 않는다 — 표 상단 안내 + 버튼 툴팁). admin/master 응답은 불변.
- ⚠ **테스트 도구 함정**: "그 호출이 코드에 없다"를 보려고 **블록 주석 정규식(`/\*…\*/`)으로 주석을 지우면** 이 레포의 정규식 리터럴을 물어 **41KB(코드 3분의 1)가 통째로 사라진다**(실측). **줄 주석만** 지운다 — 지우려는 것보다 큰 것을 지우는 도구는 쓰지 않는다.
- 회귀가드 `tests/vendorFolderButtons.test.js`(판정 순수함수·서비스·라우트 **실제 실행**(캐시↔스코프 순서·Drive 무접촉·쿼리 순증 0·staff 스코프 3역할) + 렌더러 vm 실행(상태 10종) + 배선, **변이시험 검출 확인**) + 브라우저 실행검증(FHD 스크롤 0·칸 이탈 실측·새 창·토글 기억·좁은 폭 열화). ⚠ 이 변경으로 열 수 계약 가드를 **16조합**으로 확장(`vendorManagementRedesign`·`contractMatch`)하고 `homeTasklistFilters` 의 현영 판정 패턴을 갱신했다(검사 의미 불변).


### ★★ 작업(소유) 이관 — 다른 거래처로 옮기기 (2026-08-10 위프코리아 건 · 스키마 변경 0)
- **사고**: 「위프」 작업이 `(주)위드프렌즈` 소유 시트 안에 있는데 실제 광고주는 **별개 등록된 `주식회사 위프코리아`**였다. 종전엔 옮길 창구가 없었다 — 거래처 등록 폼은 **미지정 시트만** 고를 수 있어(`_ovmAddAdvHtml` 의 `freeSheets`) **이미 소유된 시트의 일부 탭만 새 업체로 넘기는 것이 구조적으로 불가능**했고, [소유 추가]로 겹쳐 지정하면 두 업체가 같은 작업을 갖는 상태가 됐다.
- **모델 = 기존 `advertiser_campaigns` 만**(신규 테이블·마이그레이션 0). 이관 = **같은 범위의 기존 소유 소프트 해제 + 새 업체 업서트**를 `transferOwnership` 이 **한 트랜잭션**으로 한다 — 중간 실패로 "주인 없는 작업"이 남지 않는다.
- ★★ **범위(scope)가 계약**: `tabGid` 있으면 **그 탭만**, 없으면 **시트전체 행만**. 탭 이관은 시트전체 행을 **건드리지 않아** 나머지 탭이 옛 업체에 그대로 남고, 시트 이관은 **타 업체의 탭지정 세분 소유를 보존**해 `keptTabOverrides` 로 보고한다(조용한 삭제 금지 — 세분 소유는 사람이 명시한 더 강한 결정이다).
- ★★★ **판정 우선순위(탭지정 > 시트전체)를 전개 SQL 에도 반영해야 한다** — `ownedTabsForAdvertiser`·`advertiserOverview` 의 시트전체 전개에서 **타 업체가 탭지정으로 가져간 탭을 `NOT EXISTS` 로 배제**한다. 빼면 이관한 작업이 **옛 업체 표·작업 수에 그대로 남아** "이관했는데 아무것도 안 바뀐" 화면이 된다(`advertiserForTab`·`scopedActiveTabs` 가 이미 쓰는 규칙을 같은 자리에 맞춘 것 — 사본이 아니라 정합).
- ★★ **`advertiserOverview` 는 `SELECT DISTINCT` 필수 (진짜 PG16 실측으로 발견)**: 한 업체가 같은 시트의 **시트전체 + 탭지정을 동시에** 가지면 그 탭이 두 own 행에 매칭돼 **작업 수가 이중 계수**된다(탭 이관 → 시트 이관 순으로 두 번 하면 즉시 도달). 연결탭 표는 `tabs` CTE 의 `DISTINCT ON` 이 접어 주므로 여기만 두면 **개요 숫자 ≠ 연결탭 표**로 갈린다.
- **API = `POST /api/trackb/ownership/transfer`(adminOrMaster)** — 업체 간 재배치는 staff 초기매핑 게이트가 이미 "재배치는 관리자에게 요청"으로 못박은 admin 소관이다. 대상 검증은 **fail-closed**(업체 미존재 404 · **종료(ended) 거래처 거부** · 필수값 없으면 쓰기 0 + ROLLBACK).
- **화면 = 업체관리 › 소유 캠페인 줄 [이관]**(admin/master 에만 렌더 = 서버 게이트와 1:1) → 팝업에서 **범위**(시트 전체 / 특정 탭 다중선택)와 **대상 거래처**를 고른다. ★ 대상에 **[＋ 새 거래처 등록]** 경로를 둔 것이 이번 사고의 직접 해소 — 인트라넷 광고주DB **정확일치 검증**(`_trChk`, 등록 게이트 동일 · 서버 `createAdvertiserScoped` 가 최종 방어)을 통과해야 실행되고, **이미 등록된 이름이면 새로 만들지 않고 그 업체로 이관**한다(409 예방).
- ★ 팝업은 **body 직속**(드로어 스크롤 컨테이너에 두면 흐름에 섞인다) · **바깥 클릭으로 닫지 않는다**(고른 값 보호, Esc·[취소]만) · **Esc 리스너는 드로어보다 먼저 등록 + `stopImmediatePropagation`**(팝업과 드로어가 한 번에 같이 닫히지 않게) · onclick 은 **인덱스만**(탭명·업체명은 시트/외부발 문자열) · 새 거래처명 입력 중에는 **팝업을 재렌더하지 않는다**(제안 목록·검증 문구만 갱신 — IME 조합 보호).
- ★ **부분 실패를 "완료"로 꾸미지 않는다** — 탭 여러 개를 고르면 건별로 전송하므로 `일부만 이관됨 (1/2) — 사유` 로 **몇 건이 옮겨졌는지 사실대로** 말한다.
- ⚠ **이관해도 따라오지 않는 것(문서화)**: 계약·정산 링크(`trackb_settlement_links`)는 탭에 매달려 있어 **그대로 유지**된다 — 계약이 옛 업체 것이면 [계약 매칭]에서 다시 매칭해야 한다(팝업이 문장으로 고지). 브랜드 배정·광고주 접속 링크도 업체별 원장이라 별개.
- 회귀가드 `tests/ownershipTransfer.test.js`(서비스 스텁 실행·라우터 스택 실검사·**`ownTransferGo` vm 실행**(범위별 전송 · 부분 실패 고지 · 미검증 새 거래처 차단 · 중복 이름 재사용)) + `tests/ownershipTransferPg.test.js`(**진짜 PG16** — ★ 쿼리 사본이 아니라 **서비스를 그대로 호출**한다: 사본을 검증하면 서비스 SQL 을 망가뜨려도 통과한다(변이시험이 실제로 잡아 이렇게 바꿨다) · 이중 계수 · 멱등 · 세분 소유 보존), **변이시험 검출 확인** + 실 http 오리진 브라우저 실행검증(팝업 body 직속·클릭 가능·역할별 버튼·탭 칩·자동완성·Esc).

### ★ 담당 AE 후보에서 퇴사자 제외 (2026-08-10 · 스키마 변경 0)
- **문제**: 담당AE 자동완성(`GET /api/trackb/intranet/users`)이 인트라넷 users 를 그대로 내려줘 **퇴사자가 계속 후보로 떴다**.
- ★★ **판정 기준은 인트라넷 자신의 규칙 그대로** — `is_active === 0 || resigned_at`(`inadd-webapp/src/routes/api.ts` 220·2064·2275 과 동일, **빈 문자열 `resigned_at` 은 재직**). 리뷰웹에서 새 규칙을 만들면 인트라넷 인사 화면과 갈린다. 두 값은 범용 테이블 API 응답에 실려 온다(`redactSensitive` 가 지우는 건 `password` 뿐)라 **인트라넷 변경 0**.
- ★★ **필드가 아예 없으면(구버전 인트라넷 배포) 판정 불가 = 포함(fail-open)** — 모른다고 전 직원을 지우면 **담당AE 지정이 통째로 막히는 막다른 길**이 된다. `is_active` 가 `null`/`undefined` 인 것도 "모름"이라 제외하지 않는다(`!v` 로 접으면 fail-closed 회귀).
- ★ **거르는 곳은 서비스 한 곳**(`intranetStaffUsers`) — 소비처 3곳(업체추가 폼 `#aelist2` · 업체 [변경] 박스 `#aelist` · 편집 허용명단 후보 `_edSuggest`)이 전부 이 프록시를 타므로 **프론트 판정 사본 0**이고 명단 후보에서도 자동으로 빠진다.
- ★★ **빼기만 하고 침묵하지 않는다** — 이미 퇴사자가 지정돼 있던 업체는 목록에서 빠지는 순간 `직접 입력값 (인트라넷 AE 목록에 없음)` 으로 보여 **재지정 신호가 사라진다**. 그래서 응답에 `resignedNames`(그 스코프의 퇴사자 이름, 상한 200)를 함께 실어 화면이 **⚠ 퇴사한 직원**으로 사유를 구분해 말한다. **저장은 막지 않는다**(자유입력 허용 계약 유지 — 경고 전용).
- ★ **응답 shape 은 3필드(`name`/`username`/`department`) 그대로** — `resigned` 는 내부 캐시 판정용이라 내보내지 않는다(items 는 어차피 재직자만이라 항상 false + 데이터 최소화 계약 유지, 기존 `trackBAeMatch` 가드의 shape 단언 불변).
- ★ 프론트 수신은 **단일 지점 `_aeApply(r)`**(`_aeItems`·`_aeResigned` 를 한 곳에서 세팅 — 사본을 두면 한쪽 화면만 퇴사자를 계속 그린다) + `_aeIsResigned` 판정 한 벌. **`resignedNames` 미동봉(구버전 백엔드)이면 빈 배열 = 종전 문구 그대로**(모르는 것을 퇴사자로 단정하지 않는다). [변경] 박스는 **입력 중·목록 도착 후 양쪽에서 재판정**(이미 지정된 퇴사자는 목록이 와야 알 수 있다).
- 회귀가드 `tests/aeResignedFilter.test.js`(판정 3갈래·fail-open·shape 불변·dept 스코프가 `resignedNames` 에도 적용·q 검색이 퇴사자를 되살리지 않음·프론트 배선 vm 실행, **변이시험 41종 검출 확인** — ★ 순서 계약은 **정적 인덱스 비교만으로는 '정리를 아예 안 부르게' 만든 변이를 놓친다**(실측) → 스윕을 실행해 호출 순서를 스파이로 본다 — ★ 가드 함정 2종을 변이시험이 잡았다: **변경 0건일 때 "담당자 필드 없음"은 공허하게 참**(번호가 실제로 바뀌는 계획으로 볼 것) · **표 열 추가는 헤더 칸 수 ≡ 행 칸 수 ≡ colspan 계수**로만 잡힌다) + 실 http 오리진 브라우저로 세 상태(확인됨/퇴사자/미등록) 문구 실측.

### ★★ 구글시트 주소로 작업 가져오기 (업체관리 · 탈 구글시트 잔재 처리 · 마이그레이션 0)
- **발단(실측 2026-08-10 · 0720수진코리아 고양이캔 4종)**: 시트에 108명이 적혀 있고 맨 윗줄엔 이미 `⛔ 리뷰웹시스템으로 이관되었습니다`가 붙어 있는데 **시스템에는 그 작업이 없었다**. 시트를 본 사람은 시스템으로 오고, 못 찾아 시트로 돌아가면 "여기 보지 마세요"가 적혀 있다 — **어느 쪽에서도 일할 수 없는 상태**. 원인 3겹 = ① 접수를 안 거쳐 `tab_configs` 미등록 ② 업체 소유 미지정 ③ 내용이 시스템 표로 옮겨진 적 없음. 기획서 `frontend/docs/prd-sheet-import.html` · 시안 `design-sheet-import-wizard.html`.
- ★★ **출발점이 "목록에서 고르기"가 아니라 "사람이 주소를 가져오기"인 이유** — **시스템이 모르는 시트는 시스템이 셀 수도 없다**. 업체관리의 "업체 미지정 시트" 목록조차 `listActiveTabs`(raw∩index) 기반이라 미등록 시트는 거기에도 안 뜬다. 그래서 남은 358건 중 같은 상태가 몇 건인지는 지금도 셀 수 없다(드라이브 폴더 전수 스캔은 별건).
- **서비스 `sheetImport.service.js` · 라우트 `/api/trackb/sheet-import/{preview,run,revert}`(adminOrMaster) · 화면 = 업체관리 진입점 2곳(업체 패널 · 사이드바) + 4단계 마법사(`si-` 접두)**. 사용자 확정 D1~D6 = 즉시 끊기 / 공고 미생성 / 업체 필수 / 제출·입금 그대로 이월 / 이미 등록된 탭 차단 / 가져온 직후 되돌리기.
- ★★ **판정 사본 0 — 이 서비스가 새로 만드는 판정은 "어느 줄을 가져오는가" 하나뿐**: 헤더 탐지 `utils/sheetHeader` · 열 역할 `worktableTemplate.classifyHeaders`(매퍼 파생) · **명단 수는 `indexBuilder.parseTabRows` 에게 직접 묻는다**(여기서 세면 "미리보기 107명인데 가져오니 92명") · 줄 생성 `participants.createSlotsFromSheetRows` · 장부 `sheetlessLedger.rebuildLedgers` · 안내문 `sheetNotice.applySheetNotice`.
- ★★ **등록 창구가 하나 는다(명시적 예외)** — `TAB_REGISTRATION_MODE='order'` 규율상 등록 관문은 접수 하나였다. 이 경로는 **복원 성격**(아카이브 복구와 같은 분류)이라 우회하되 조건을 건다: adminOrMaster 전용 · **업체 지정 필수** · 자동 발견·일괄 실행 없음 · 수행자를 `sheetless_by` 와 로그에 기록. **`allowManualRegister` 를 완화하지 말 것**(그 스위치는 다른 창구의 것이다).
- ★★ **접수 업서트는 재사용할 수 없다** — `order.routes` 의 `tab_configs` 업서트는 작업오더 행(`review_type`·`delivery_type`·`work_manager`…)에 강하게 결속돼 있다. 최소 컬럼 자체 업서트를 두되 **두 불변식**을 회귀가드가 고정한다: ① `sheetless = COALESCE(...) **OR** EXCLUDED.sheetless`(이관을 되돌리지 않는다 — 되돌리면 크론이 옛 시트 값으로 장부를 덮는다) ② 메타는 **blank-only**(`COALESCE(NULLIF(...))`).
- ★★ **미리보기는 DB 쓰기 0 — 그래서 미러를 부르지 않는다**: `mirrorOneSheet` 는 그 시트의 **모든 탭**을 저장하므로 약속과 어긋난다. 시트를 직접 1회 읽는다(`batchReadSheet` + **`FORMATTED_VALUE`** — 미러와 같은 옵션이라야 `7 / 20 (월)` 표기가 보존된다, 시트 lane throttle 경유). **실행 단계에서도 미러는 안 부른다**(`rebuildLedgers` 가 `raw_sheet_*` 를 작업표에서 만든다).
- ★★ **실행 순서가 계약**: 시트 재읽기(화면이 보낸 줄 목록 불신) → `campaigns`/`tab_configs` 등록 **이때 `sheetless=TRUE`**(장부 생성기가 무시트 탭에서만 동작하므로 표식이 **먼저** 켜져야 한다) → 업체 소유(**탭 지정**) → 작업표 줄 → 장부 3권 → 시트 안내문(best-effort — 실패해도 되돌리지 않고 사유를 응답에 싣는다).
- ★★ **fail-closed 3종**: **연락처 칸 미검출 = 거부**(리뷰어가 영영 자기 참여 내역을 못 찾는 명단이 된다 — 숫자만 채우고 "옮겼다"고 말하는 쪽이 훨씬 나쁘다) · 헤더 줄 미검출 = 거부 · **이미 등록된 탭은 미리보기와 실행 양쪽에서 차단**(낡은 화면이 preview 를 건너뛰고 run 을 직접 부르는 경로가 있다 — 변이시험이 잡았다).
- ★ **줄 판정은 좁게**: 번호가 없어도 내용이 있으면 **가져온다**(실측 8줄 = 번호를 안 매겼을 뿐 실제 진행 건) · 연습으로 보이는 줄(이름 1글자·연락처 해독 불가)은 **체크만 해제하고 목록에 남긴다**(시스템이 마음대로 버리면 사람이 사라진다) · `seq` = **시트 실제 행 번호**(어긋나면 주문 유입 시 표가 두 겹).
- ★ **표에 보여줄 값(구매일자·금액·제출·입금)은 서버가 역할 인덱스로 골라 싣는다** — 화면이 `row_json['리뷰제출']` 처럼 이름을 찍으면 그 이름이 다른 탭에서 통째로 빈 칸이 된다.
- ★★ **되돌리기(D6)는 등록까지 내린다** — 표식만 끄면 등록이 남아 크론이 시트를 다시 읽어 "되돌렸는데 또 나타난다". `campaign_participants`(→ `participants.purgeImportedRows`, **쓰기 소유자 규율** · `client` 를 받아 같은 트랜잭션) · 장부 3권 · 소유 · `tab_configs` 를 한 트랜잭션으로. ★ **주문이 한 건이라도 들어왔으면 거부**(등록을 지우면 그 주문이 갈 곳을 잃는다 — [줄 정리]로 안내) ★ **`campaigns` 는 남긴다**(같은 시트의 다른 탭이 공유) ★ **소유 해제는 탭 지정만** — 빈 gid 를 NULL 로 넘기면 `tab_gid IS NULL` = **시트 전체 소유**가 걸려 같은 시트의 다른 탭까지 업체에서 떨어진다(자체 점검으로 잡음).
- 잔여(문서화): 사람이 주소를 가져와야 한다 · 리뷰 캡처 이미지는 함께 오지 않는다(기존 [리뷰 캡처 정리]로 별도 연결) · 계약 매칭은 자동으로 안 붙는다 · 한 번에 한 탭.
- 회귀가드 `tests/sheetImport.test.js`(순수함수 실행 · 스텁 pool 로 preview/import/revert **실제 실행** · 라우터 스택 실검사 · 화면 배선 · 헤더 칸 수 ≡ 행 칸 수, **변이시험 검출 확인**) + 실 http 오리진 브라우저 4단계 실측. ⚠ 변이시험이 잡은 가드 함정 2종: **호출부 테스트는 스텁이 대신 응답해 SQL 조건 회귀를 통과시킨다**(실제 함수를 따로 실행할 것) · **미리보기 blocker 만 검사하면 실행 단계 게이트 제거를 놓친다**.

### 리뷰웹시스템[3버전] 첫 진입 홈 (시안 B 타일 런처형 · ① nav 상시 유지)
- 내부인(master/admin/AE) 첫 진입 기본 뷰 = **홈**(`renderHomeView`) — 그라데이션 히어로(인사 + 오늘 요약 4칸: 진행 중 작업·모집중 공고·오늘 참여 확정·확인 필요) + **메뉴 타일 그리드**(nav 의 카드화, 역할별 접힘 = nav 정책 1:1) + 우측 "오늘의 브리핑" 레일. 시안 = `frontend/docs/design-workdesk-home.html`(B안 + B-2 ①). nav 맨 앞 **🏠 홈** 버튼(admin·staff 두 nav — 타일=학습용 큰 입구, nav=빠른 입구 역할 분담), **광고주는 홈 미도달**(nav 자체가 없음 · 전용 대시보드 유지). `STATE.view` 초기값 `''` — 기본 뷰 결정은 renderShell 의 `isAdv?'workdesk':'home'` 한 곳, 딥링크(`#go=`·`#view=`)는 boot 이 view 를 먼저 세팅해 종전대로 직행. logout 리셋에 `view:''` 포함(재로그인 = 홈).
- ★★ **홈은 읽기 전용 + 이동만** — 타일·브리핑 줄 클릭 = `switchView` 뿐, 처리 로직 사본 0(교체요청 승인·오더 접수는 각 탭의 기존 코드로 이동만 한다).
- ★★ **숫자 = nav 뱃지 재료 그대로**(홈 전용 집계 금지): `_NAVCS`(C/S 문의+교체요청) · `_NAVRI`(리뷰검수 — 이번에 신설, `_riSyncNavBadge`가 값을 먼저 기억. **DOM `#riNavBadge` 되읽기 금지** — ' 🔴4' 문자열 파싱이 되어 표기 형식에 결합) · `_NAVLG`(로그) · `STATE.woCounts`(작업오더 — **`#woNavBadge` 표기는 `_woSyncNavBadge` 한 곳뿐**, 작업오더 뷰/홈 어느 쪽이 갱신하든 같은 함수). 각 `*SyncNavBadge` 가 `_homeSyncBadges` 를 불러 수렴(홈 미표시 = no-op). ★ 훅 호출은 `typeof _homeSyncBadges==='function'` 가드 — 런타임엔 항상 있지만, **블록 단위 vm 추출 회귀가드**(workdeskLogTabs 등)의 sandbox 에는 없어 가드 없이는 그 테스트들이 ReferenceError 로 죽는다(실측).
- ★ 히어로 숫자도 **기존 엔드포인트 3개 재사용**(신규 API 0): `/api/trackb/tabs` · `/campaigns/list`(참여형 state==='open' + 레거시 status==='active', ops.todaySubmitted 합산) · `/work-orders/list?status=submitted`(counts 를 `STATE.woCounts` 에 실어 `_woSyncNavBadge` 경유). ★ **실패/미도착 칸은 '?'·'–'** — 0 으로 꾸미지 않는다(조용한 정상 위장 금지, 관측 뷰 배포 스큐 규칙과 동일). AE 에겐 "미확인 문의" 브리핑 줄 자체를 안 그린다(AE 는 문의 미조회라 영원한 '–' 가 된다).
- CSS 는 전부 `hm-` 접두 + `#hmwrap{max-width:1380px}`(모집공고 카드 컨테이너와 같은 값). 회귀가드 `server/tests/workdeskHome.test.js`(정적 배선 + **vm 런타임 실행**(sandbox 에 isAdmin 전역을 일부러 두지 않음), 변이시험 검출 확인) + 실브라우저 http 오리진 검증(렌더·타일 이동·🏠 복귀·히스토리 뒤로가기·성공/실패 경로 숫자).

### ★★ 작업 "마감"(전사 공통) + 홈 작업 목록 — 시트 탭 워크플로우 이관 M1 (migration 088)
- **배경**: 직원은 구글시트를 브라우저 탭으로 여러 개 열어두고 **진행 중인 것은 앞, 끝난 것은 뒤로** 옮기며 일해 왔다(실제 화면 관찰). 작업보드에는 순서·완료 개념이 전혀 없어(서버 고정 정렬) 끝난 작업이 계속 섞여 나왔다. 직원 인터뷰: "관리자 대시보드에서 **작업 목록이 나열되어** 보이던 게 좋았다". PRD·와이어프레임 = `frontend/docs/prd-workboard-worktabs.html`(v1.2, 결정 9건 확정).
- ★★ **"오늘 완료"와 "마감"은 다른 것이다**(사용자 확정): ㉮ **오늘 완료** = 그 작업의 오늘 할 일을 끝냄(뒤로+회색, **다음날 자동 해제**, 매일 반복) ㉯ **마감** = 작업 자체의 종료(보드에서 빠져 보관함으로, 영구). 구분하지 않으면 "오늘 몫 끝낸 작업"이 모두의 보드에서 사라지거나, 끝난 작업이 매일 아침 되살아난다. **M1 은 ㉯ 마감만** — ㉮ 오늘 완료는 M2(`trackb_tab_daily_done`, `done_date=KST 오늘` 판정이라 자정 리셋 크론 불필요).
- **모델**: `trackb_tab_finished(sheet_id, tab_name)` — 활성 마감 1건 **부분 유니크**(`WHERE deleted_at IS NULL`) → 코드 실수로도 중복 마감 불가. 복귀 = 소프트 삭제(이력 보존, `reopened_by`). `inspect_confirmed_at` = 검수 확인 시각 박제.
- ★★ **마감 확정에는 "리뷰폴더 마감자료 검수"의 사람 확인이 필수**(사용자 확정 ㉠): 확인창 체크 전 [마감] 비활성 **+ 서버도 `inspected:true` 없으면 400**(프론트만 믿지 않는다 — 필수열람 게이트와 같은 규율). 검수는 캡처 품질 판단이라 시스템 수치로 대신할 수 없다. **마감자료 미생성은 경고만**(하드블록 금지 — 마감자료를 안 쓰는 작업 유형까지 막지 않는다), 표시 재료는 기존 `trackb_tab_closeouts` LATERAL LIMIT 1 재사용(신규 저장소 0).
- ★ **마감 후보 배지 = 인원 충족 + 전건 제출 + 전건 입금** — **제안 전용**. 후보가 아니어도 마감할 수 있고(조기 종료), 후보여도 **자동 마감하지 않는다**. 통계가 없으면 판정 자체를 안 한다(모르면 제안하지 않는다).
- ★★ **서버는 목록에서 마감 탭을 거르지 않고 주석만 단다** — 거르는 순간 홈 "마감 보관함"이 **영원히 빈다**(같은 응답을 쓴다). 거르는 것은 프론트 `_wGroups`(작업보드 1·2단에서 제외, **인덱스는 원본 유지** = `selTab(i)` 계약 불변). 검색(`wSearch`)은 마감도 찾아주되 `🏁 마감`이라고 말한다 — 작업바에서 빠진 작업을 되찾는 유일한 길.
- ★ **스코프 단일 출처 = `scopedActiveTabs`** — 홈 목록·작업보드·보관함이 전부 이 한 경로를 태운다(AE 담당 범위가 갈라지지 않는다). 관리자 대시보드 `/api/tab/dashboard` 를 프록시하지 **않는다**: 그 응답은 스코프가 없어 staff 에게 담당 밖 데이터가 새고, 폐기 예정 표면에 새 의존이 생긴다.
- ★ **광고주에겐 마감 주석·통계를 아예 안 보낸다**(마감자·담당자는 내부 정보 — 데이터 최소화). 권한 = master/admin 전체 · staff 담당 탭만(`_ensureEditScope`) · 광고주 차단(사용자 확정 ㉣).
- ★ **통계(`?stats=1`)는 홈 전용** — `review_index` 전체 GROUP BY 라 작업바 로드(`loadTabs`)엔 붙이지 않고, 서버 30초 + 클라 60초 캐시. 클라는 **배열 교체가 아니라 병합**(`_finEnsureStats`) — 교체하면 인덱스(`selTab` 계약)와 `STATE.cur` 객체 동일성이 깨진다. `loadTabs` 는 이전 통계를 이월해 후보 배지가 화면을 오갈 때마다 사라지지 않게 한다.
- ★★ **히어로 "진행 중 작업" writer 는 `_finRenderList` 한 곳** — 두 곳에서 세면 마감 직후 히어로 3 · 목록 [진행 중] 2 로 갈린다(**브라우저 검증이 실제로 잡은 회귀**).
- ★★ **onclick 에 시트에서 온 문자열을 넣지 말 것 — 인덱스만 넘긴다**(이 작업에서 **실제 XSS 재현**): HTML 속성은 엔티티 디코드 후 JS 로 파싱되므로 `esc`(따옴표 미이스케이프)로는 문자열 탈출을 못 막는다. 탭명 `x'),alert(1),String('` 하나로 **임의 JS 실행 + 서버엔 잘린 탭명 전송**이 재현됐다. 레포 관용구(worktable 후보 클릭 `wtPickAdd(key, idx)`·교체요청 카드 `_idAttr`)대로 `openFinishModal(i)`/`doReopen(i)`, 보드 헤더는 인자 없는 `openFinishCur`/`doReopenCur`.
- ★ **Track A 무접촉** — `tab_configs.is_closed`를 재사용하지 않는다(그 플래그는 `/api/tab/options` 등 **기존 소비처 동작을 바꿔** "화면 분류일 뿐"이라는 계약과 충돌). 시트 쓰기 0 · 검색·인덱스·주문 경로 참조 0. 관리자 대시보드의 아카이브도 끌어오지 않는다(폐기 예정 — 사용자 확정). 되돌리기 = 라우트 미마운트 + DROP TABLE.
- ★★ **fail-soft 는 "조용히"가 아니다**(코드리뷰가 잡은 무신호 사고): 조회 실패를 **빈 맵만** 돌려주면 프론트가 그것을 "아무것도 마감 안 됨"으로 읽어 **마감 작업 전부가 작업보드로 되살아나고 보관함이 비는데 화면엔 아무 신호가 없다**(다음 성공 조회까지 유지). → `finishedTabsMap`/`tabStatsMap` 은 **`{ok, map}`** 을 돌려주고, 라우트가 `finishedUnavailable`/`statsUnavailable` 을 응답에 실으며, 프론트는 그 플래그가 있으면 **기존 주석을 덮지 않고** 목록 상단에 사유를 표기한다. 같은 이유로 통계도 **값이 있을 때만 대입**(undefined 로 덮으면 후보 배지가 통째로 사라진다). 실브라우저로 DB 블립을 흉내 내 유지·고지·복구를 확인.
- ★ **쓰기 경로는 fail-soft 가 아니다** — 다만 `42P01`(마이그레이션 미적용)은 그대로 올리면 `/api/trackb/*` 가 `isAdminApi` 밖이라 **마스킹된 200 "서버 오류가 발생했습니다."** 로 나가 원인을 알 길이 없다(읽기는 fail-soft 라 화면이 정상으로 보이므로 이 경로가 유일한 신호) → `code:'not_ready'` + migration 번호를 담은 문구로 반환. 신규 **테이블**이라 `REQUIRED_SCHEMA`(컬럼만 검사)로는 못 막는다.
- ★ **탭 리네임 대비 gid 폴백**: 목록 소스 `listActiveTabs` 는 `tab_gid OR tab_name` 으로 리네임에 관대한데 마감 매칭이 이름뿐이면 **리네임 순간 마감이 조용히 풀리고 옛 이름 행이 영구 고아**가 된다(레포 규율 = "gid 우선 재매칭"). 맵에 `sheetId\tgid:<gid>` 키를 함께 싣고 라우트가 이름 → gid 순으로 찾는다. **빈 gid 는 키를 만들지 않는다**(전부 매칭되는 사고 방지).
- ★ **목록 잘림 고지**: 마감 탭도 `limit` 을 차지하므로(서버가 안 거른다) 마감이 쌓이면 **진행 중 작업이 조용히 밀려난다** → 응답 `truncated` + 화면 안내(투영 커버리지 `truncated` 선례). ★ 입금 집계는 `FILTER` 대신 **`WHERE is_submitted2='PAID'`** — 결과는 같고 대상만 준다(이 쿼리는 관리자 화면 하나가 아니라 **모든 내부 사용자의 홈 진입 경로**에 붙는다).
- ★ **인덱스 전달의 짝 = 목록 재렌더**: `loadTabs` 가 `STATE.tabs` 를 교체하고 홈 목록을 다시 안 그리면 **목록 DOM 이 stale 인덱스를 들고 있어 옆 작업이 마감/복귀된다** → 교체 직후 `wblMount` 있으면 `_finRenderList`. 복귀 confirm 에는 **작업 이름을 넣는다**(어긋났을 때 사용자가 알아챌 유일한 장치 — 마감 확인창은 이름이 보이지만 복귀는 안 보였다). 0행 복귀는 "되돌렸습니다"로 꾸미지 않는다.
- ★ `tabStatsMap` 맵은 **전 탭 무스코프** — 절대 응답에 통째로 싣지 말 것(`res.json({..., stats})` 를 추가하는 순간 전 업체 담당자·캠페인명이 staff 에게 샌다). 스코프는 tabs 루프가 유일한 방어이고 회귀가드가 이를 고정. 복귀 명시 파싱(`finish:'false'`·`0` 을 마감으로 오해 금지)도 같은 가드에 있다.
- CSS 는 전부 `wbl-` 접두(홈 CSS 스코프 가드가 `#hmwrap`·`.hm-`·`.wbl-` 만 허용 — 컨테이너도 `.wbl-box`). 확인창은 **body 직속**(뷰 스크롤 컨테이너에 넣으면 오버레이가 화면 흐름에 섞인다).
- 회귀가드 `server/tests/workboardFinish.test.js`(라우터 스택 실검사 + **스텁 pool 로 서비스·라우트 실제 호출**(검수 게이트 거부 시 **DB 쿼리 0건**까지) + Track A 무접촉 + 프론트 배선 + CSS/스크립트 무결성, **변이시험 검출 확인**) + `workdeskTopbar` 에 마감 제외 케이스 추가(`isFinished` 를 **스텁이 아니라 구현 추출**) + **진짜 PG16 으로 마이그레이션 idempotent·부분 유니크·복귀 후 재마감·조인 실행 확인** + **브라우저 실행검증**(마감→보관함→복귀 왕복·검수 게이트·XSS 재현/수정). ⚠ 이 변경으로 기존 가드 3종(`workdeskHome` 2건·`worktablePlan` 1건)의 패턴이 드리프트해 함께 갱신했다(검사 의미는 불변 — 엔드포인트 개수 3 · 히어로 단일 writer · 응답 키는 `tabs`). **배선 형태가 바뀌는 변경엔 패턴을 함께 갱신할 것.**
### ★★ 열린 작업 줄(개인별) + 오늘 완료(전사 공통) — 시트 탭 워크플로우 이관 M2 (migration 089)
- **완성된 그림**: 0단 = 내가 열어둔 작업(브라우저 탭) · 1·2단 = 업체 세그먼트/그룹 탭(기존) · 마감(088) = 보드에서 제거 · 오늘 완료(089) = 뒤로+회색. 이제 **업체를 거치지 않고 0단 클릭만으로 작업을 오간다**(직원이 지적한 불편의 직접 해소).
- ★★ **"오늘 완료"와 "마감"을 절대 합치지 말 것**(사용자 확정): 오늘 완료는 **목록에서 사라지지 않는다**(뒤로 밀고 회색만 — 사라지면 오늘 몫을 끝낸 작업을 다시 못 연다), 마감은 **영구**(보드에서 제거·보관함으로). 합치면 "오늘 몫 끝낸 작업이 모두의 보드에서 사라지거나, 끝난 작업이 매일 아침 되살아난다".
- ★★ **자정 리셋 크론이 없다** — 판정 = `done_date = KST 오늘`이라 날짜가 바뀌면 어제 행이 **저절로** 빠진다. boolean 컬럼으로 바꾸는 순간 어제 체크가 영원히 남고 그걸 지울 배치가 필요해진다. **해제는 오늘 행만 DELETE**(어제 이력을 지우면 "언제 처리했나"가 사라진다). KST 파생은 `campaignState.kstTodayStr` **재사용**(사본 금지 — 날짜 규칙이 두 벌이면 갈린다).
- ★★ **열린 줄은 즐겨찾기 원장에 얹지 않는다** — 그쪽은 클라가 **Set 으로 재직렬화해 순서가 사라진다**. 여기서 순서 = 사용자가 드래그로 정한 탭 배치라 신규 테이블(계정당 1행 + JSONB 배열). 키 규약(`_favKey`)은 재사용(두 벌이면 같은 탭이 다른 키가 된다).
- ★★ **부팅 경합**(실브라우저가 잡은 실측 사고): 부팅 조회(`_wtSyncFromServer`)가 **사용자의 첫 클릭보다 늦게 도착하면 방금 연 탭을 덮어쓴다**. → 변경은 **단일 커밋 지점 `_wtCommit`** 에서만 하고 거기서 `_wtDirty` 를 세운다. dirty 면 서버 값을 버리고 **서버를 내 상태로 맞춘다**. 커밋 사본을 두면 어떤 경로는 dirty 를 안 세워 경합이 되살아난다.
- ★ **작업을 여는 모든 경로가 `selTab` 으로 수렴** → 거기서 `_wtOpen(t)` 한 번(그룹 탭·검색·홈 목록·딥링크 공통). 사본을 두면 어떤 경로로 연 작업만 줄에 안 남는다.
- ★ **마감된 작업은 열린 줄에서 자동 정리**(전사 공통이라 남이 마감해도 빠진다). ★★ 단 **목록이 아직 안 왔을 때(`STATE.tabs` 비어 있음)는 정리하지 않는다** — 안 하면 부팅 때 열린 줄이 통째로 지워진다.
- ★ **상한 12**(사용자 확정 ㉤): 서버는 **잘라 담을 뿐 요청을 튕기지 않는다**(튕기면 화면이 멈춘다), 화면은 **안내만** 하고 자동으로 닫지 않는다.
- ★ **onclick 문자열 보간 금지는 여기도 동일**(M1 실측 XSS): `wtPick(this.dataset.k)` — 그리고 **닫기(×)에도 `data-k` 를 붙인다**(부모 `.wt0` 에만 두면 `this.dataset.k` 가 undefined 라 닫기가 죽는다 — 실브라우저가 잡음).
- ★ 조회 실패는 `dailyUnavailable` 로 고지하고 **기존 체크를 덮지 않는다**(088 의 무신호 규율과 동일). 토글 실패 시 낙관적 반영을 되돌린다. 권한은 마감과 같다(master/admin 전체 · staff 담당 탭 · 광고주 차단·주석 미동봉). 단 **열린 줄은 개인 데이터라 스코프 게이트가 없다**(자기 것만 읽고 쓴다 — 즐겨찾기와 같은 계약).
- ★★ **계정 전환 시 메모리 상태를 반드시 비운다**(코드리뷰 blocker): localStorage 키가 계정별이어도 `_wtLoaded`/`_wtDirty`/`worktabs` 를 안 비우면 **키 분리가 무의미**하다 — 새 계정 캐시를 읽지 않고 이전 계정 배열을 그리고(스코프 밖 탭명 노출), dirty 가 남으면 `_wtSyncFromServer` 가 새 계정 GET 을 버리고 **이전 계정 목록을 새 계정 서버 행에 저장**한다. `logout` **과 `_dropSession`(401 재로그인)** 둘 다.
- ★★ **`getWorkdeskWorktabs` 도 `{ok, tabs}`**(코드리뷰 blocker): 조회 실패를 빈 배열로 접으면 프론트가 그것을 서버 원본으로 신뢰해 로컬을 덮고, 다음 탭 하나를 여는 순간 **서버 행이 1건짜리로 영구 대체**된다. 라우트가 `worktabsUnavailable` 을 실어 주고 프론트는 로컬 유지 + 고지. 저장의 42P01 도 `not_ready` 로(프론트 `catch` 가 삼켜 무신호가 되던 경로).
- ★★ **부팅 경합 가드는 1회 한정(`_wtBootSynced`)** — `_wtDirty` 를 영구 깃발로 쓰면 세션 중 탭을 한 번 연 뒤로 서버 값을 **영영 안 받아** 다른 창·다른 PC 의 변경이 한 방향으로만 지워진다("기기 무관 유지" 약속과 정면 충돌). push 성공 시 dirty 해제. **자동 정리(`_wtPruneFinished`)는 dirty 를 세우지 않는다**(시스템 정리를 사용자 편집으로 치면 구식 로컬이 최신 서버를 이긴다).
- ★ **정리는 데이터 도착 지점에서, 렌더는 순수하게** — 렌더 함수가 서버에 쓰면 부수효과가 렌더 타이밍에 묶여 0단이 없는 화면(홈)에선 정리가 안 도는 비대칭이 생긴다.
- ★ **상한에서 열기를 막는다** — 서버가 뒤를 자르는데 클라가 끝에 추가하면 **방금 연 탭만 저장되지 않아** 새로고침 시 이해 불가한 동작이 된다. ★ **드래그는 오른쪽=대상 뒤**(항상 앞이면 끝자리로 옮길 수가 없다).
- ★ **오늘 완료에는 gid 폴백이 없다(의도)** — `trackb_tab_daily_done` 에 `tab_gid` 가 없고 하루짜리 상태라 리네임으로 풀려도 다음날 초기화된다(마감은 영구라 폴백이 필요). 주석으로 명시 — 없으면 다음 사람이 버그로 읽는다.
- ★ **밤새 띄워 둔 화면**은 `visibilitychange` 복귀 시 날짜를 확인해 자가치유(토글 시점 자가치유만으론 조작이 없으면 어제 체크가 계속 보인다).
- CSS 는 `tb0`·`wt0` 접두 신설. 회귀가드 `server/tests/workboardWorktabs.test.js`(라우터 스택 실검사 + 스텁 pool 로 서비스·라우트 실행 + 격리 + 프론트 배선, **변이시험 검출 확인** — 그중 1종은 가드가 통과시켜 단언을 강화했다: 실패 경로가 둘(응답 실패·네트워크 예외)이면 **둘 다** 검사할 것) + **진짜 PG16**(순서 보존·상한·멱등·**어제 행이 저절로 빠지는지**·해제가 오늘 행만 지우는지) + **실브라우저 실행검증**(열기/닫기/드래그/전환·오늘 완료 뒤로 밀기·마감 시 자동 정리·부팅 경합·새로고침 유지·**계정 전환 누수**·**조회 실패 시 데이터 보존**·상한 절단·자정 자가치유). ⚠ M2 도입으로 `workboardFinish`(블록 추출 경계)·`workdeskTopbar`(selTab 새 의존) 가드를 함께 갱신했다.

### ★ 홈 작업목록 → [공고] 버튼 (연결된 모집공고를 팝업으로) · 버튼 열 고정 · 오늘 완료 색표시
- **시안** `frontend/docs/design-home-campaign-popup.html`. 홈 작업 목록 줄마다 **[● 공고]** — 그 작업에 연결된 모집공고를 **그 자리에서 팝업**으로 연다(화면 이동 0). 종전엔 상단 [모집공고] 탭으로 옮겨 카드 더미에서 찾아야 했다.
- ★★ **새 화면을 만들지 않는다** — 모집공고 탭에서 쓰는 그 모달(`js/recruit-modal.js`)을 `openRecruitModal(공고id)` 로 그대로 연다. 축약 폼을 새로 만들면 필드가 빠져 **저장 시 기존 설정이 빈 값으로 덮인다**(리뷰어앱 축약 편집에서 겪은 함정). 저장·검증·미리보기는 전부 기존 기능에 위임하고 홈은 **여는 일만** 한다.
- ★★ **상태 점은 카드·리뷰어 목록과 같은 계산**(`tabCampaignsMap` 이 `computeCampaignState` + `deriveSchedules` 를 그대로 태운다) — status 컬럼으로 흉내 내면 "목록엔 모집중인데 참여는 거부"가 된다. 점 = 모집중(초록)·오픈예정/오늘마감(주황)·마감(회색).
- ★ **키는 이름 + gid 폴백**(마감 주석과 같은 규칙 — 탭 리네임으로 연결이 조용히 풀리면 담당자가 **이미 있는 공고를 또 발행**한다). **빈 gid 는 키를 만들지 않는다**(만들면 gid 없는 탭이 한 키로 뭉친다).
- ★★ **조회 실패는 "공고 없음"이 아니다** — 서버가 `campaignsUnavailable` 로 말하고, 버튼은 비활성 + 목록 상단 고지, 프론트는 그 응답으로 **기존 주석을 덮지 않는다**(088 무신호 규율). 빈 값으로 접으면 중복 발행이 난다.
- ★ **한 작업에 공고가 2개 이상**(차수 재발행)이면 **생성일과 함께 보여주고 사람이 고른다**(사용자 확정 — 최신을 말없이 열지 않는다). 버튼에 건수 표기(`공고 2`).
- ★ 미발행 = **＋공고발행**(편집 허용명단만) → 같은 모달을 발행 모드로 열고 **그 작업의 탭을 프리필**(`linked_sheet_id/name/gid`). 권한 없으면 비활성(눌러도 아무 일 없는 버튼 금지). 판정은 **홈 전용 플래그 `STATE.campEdit`** — `STATE.canEdit` 은 작업보드 그리드 편집에도 쓰여 작업 탭을 한 번 열면 덮어써진다.
- ★ 주석은 **`?stats=1` 일 때만** 조회(작업바 로드는 가볍게) + 30초 캐시, **`fresh=1`** 로 캐시 우회(공고 저장 직후 — 없으면 발행하고도 [＋공고발행]이 한동안 남아 "저장이 안 된 것"으로 보인다). 저장 후 갱신은 **`window.CAMP_ON_SAVED` 훅**(훅 미설정 = 관리자 대시보드 동작 불변). `loadRecruitList` 는 컨테이너가 없으면 조기 반환 — 없으면 홈 팝업 저장이 **성공했는데도 "저장 오류"** 로 보인다.
- ★★ **맵은 전 탭 무스코프** — 응답에 통째로 싣지 말 것(tabs 루프가 유일한 스코프). **광고주에겐 미동봉**(마감 주석과 같은 판단).
- ★★ **버튼은 줄마다 자기 열을 지킨다**: `.wbl-act` = **고정 폭 grid**(진행 중 4칸 92/46/88/64px · 보관함 3칸 · 열람전용 2칸) + 버튼 `width:100%`. flex(내용 폭)로 되돌리면 같은 버튼이 줄마다 다른 x 좌표에 온다(사용자 신고). [공고] 버튼은 **연결됨·미발행·조회실패가 전부 같은 칸** — 그래서 미발행 문구도 `＋공고발행`(띄어쓰기 없음).
- ★★ **[오늘 완료]는 글자를 바꾸지 않고 색으로 말한다**(`.wbl-b.today.on` 파란 채움) — 종전 `☑ 오늘 완료 ↔ 해제` 라벨 스왑은 ① 폭이 달라져 열이 어긋나고 ② "해제"가 무엇을 해제하는지 모호했다. **동작·API 는 그대로**(`toggleTodayDone`), 바뀐 것은 표시뿐.
- ★ onclick 에는 **인덱스만**(`openTabCampaign(i)`·`_campPick(i,ci)`) — 시트/공고에서 온 문자열을 넣으면 따옴표 하나로 탈출된다(M1 실측 XSS).
- 회귀가드 `tests/homeCampaignButton.test.js`(스텁 pool 로 서비스·라우트 **실제 실행** + 버튼 렌더러 vm 실행 + 배선, **변이시험 검출 확인**) + 실 http 오리진 브라우저로 **줄별 버튼 x 좌표가 전부 동일**함과 선택 팝업(생성일 표기)을 실측.

### ★ 홈 작업목록 — 헤더행 고정 · 저장폴더 바로가기 · 필터 토글 스위치 (시안 `frontend/docs/design-home-tasklist-improvements.html` v3)
- ★★ **헤더행 고정이 안 되던 원인 = 전역 `table{overflow:hidden}`(workdesk.html 상단)**: sticky 선언(`table.wbl-t th`)이 있어도 조상 표에 overflow:hidden 이 끼면 sticky 기준이 스크롤 컨테이너(.wbl-tw)가 아니라 **표 자신**이 되어 영영 발동하지 않는다(실브라우저 재현으로 확정). 수정 = `table.wbl-t{overflow:visible}` 상쇄(시트그리드가 이미 쓰는 관용구 — **전역 규칙 자체는 무수정**, 다른 화면의 둥근 모서리가 의존) + `border-collapse:separate`(collapse 는 th 경계선이 본문과 함께 밀림 → box-shadow 로 경계선). 목록 높이 640px(사용자 확정).
- **열 구조 v3(사용자 확정)**: 통합 "폴더/처리" 칸 폐지 → 작업명 · **작업표(열기)** · 담당 · 인원/제출 · 입금 · 상태 · **저장폴더** · **모집공고** · **오늘완료** · **마감** 독립 헤더열. `.wbl-act` 고정폭 grid 폐기(열 정렬 = 표 구조 승격), 저장폴더 칸 내부만 3분할 고정 grid. ★ 열람전용·보관함도 **열 수는 같다**(빈 칸 유지 — 사람·모드마다 열이 달라지면 눈이 헤맨다). 회귀가드가 **헤더 칸 수 ≡ 행 칸 수**를 렌더 실행으로 센다.
- **저장폴더 [구매][리뷰][현영]**: 구매/리뷰 = `tab_configs.capture_folder_url/folder_url` 새창(`tabStatsMap` SELECT 에 3컬럼 합류 — **쿼리 순증 0**, stats 는 광고주 미동봉이라 자동 격리). **현영 = `[리뷰]/현금영수증` 서브폴더로 URL 미저장** → `GET /api/trackb/tab-folders`(**internalMiddleware** — 광고주 차단, staff 담당 스코프는 2026-08-19 확정으로 폐지)가 클릭 시 해석. ★★ **find-only**(생성 금지 — 폴더 생성 경로는 업로드·스마트빌드 하나, 사본 경로를 두면 라벨·소유권 규칙이 갈라진다), 발견 10분/미발견 60초 캐시, fail-soft(500 금지). ★ 현영 여부 판정 = `captureSlots.isCashReceiptIncome` **재사용**(사본 금지). ★ 비해당 작업은 버튼을 **숨기지 않고 옅은색 비활성**(사용자 확정 — 숨기면 줄마다 버튼 수가 달라진다). ★ 새창은 **await 전에** 열어 둔다(비동기 후 window.open 은 팝업 차단). URL 은 `drive.google.com` 호스트 검증 후에만.
- **필터 3종 = 사각형 3분할 토글 스위치**(왼쪽 금일 진행 / 중앙 입금 체크 / 오른쪽 마감 체크, 같은 칸 재클릭 = 해제): ★★ **판정 단일 출처 = `_finMatchFilter`**(목록·스위치 건수 공용 — 갈라짐 구조적 차단). 금일 진행 = 오늘 완료 미체크 ∧ **연결 공고 모집중**(`_campRank 0` — 서버 상태엔진 값 소비, 사용자 확정 Q1. 공고 없는 작업은 정의상 미포함이라 **필터 중 하단에 N건 고지**). 입금 체크 = 제출>입금(미입금 큰 순 정렬 + 빨간 병기). 마감 체크 = `isFinishCandidate` **그대로**(기존 "마감 후보" 배지가 이 스위치로 승격·제거). ★ 조회 실패는 칸 **비활성+사유 툴팁**(무신호 금지 — statsUnavailable→입금·마감, campsUnavailable→금일 진행). 탭 전환·로그아웃(`_dropSession` 포함)이 `finFilter` 리셋.
- ★★ **홈 목록 검색이 한글을 쪼개던 버그(실측)**: 검색칸에 '면'을 치면 `ㅁ며면면`이 됐다. 원인 = `oninput` 마다 `_finRenderList` 가 헤더까지 `innerHTML` 로 **통째 교체** → **입력칸 DOM 이 재생성**되며 브라우저 IME 조합 상태가 파괴(자모가 낱개로 확정). 재생성 뒤의 `focus`/`setSelectionRange` 복구는 **조합을 되살리지 못한다**(그래서 함께 제거 — 부활 금지). 수정 = 표 본문을 `_finBodyHtml` 조각으로 분리해 `<div id="wblBody">` 로 감싸고 **검색은 그 조각만 갱신**. 헤더 숫자(진행 중·오늘 완료·스위치 건수·담당자 칩)는 전부 `all` 기준이라 검색으로 바뀌지 않아 안전하다. ★ 조각은 **한 벌**(전체 렌더·검색 공용 — 사본을 두면 검색 결과 행만 열 구성이 어긋난다). ★ **grep 으로는 못 잡는다**(호출은 여전히 있다) → 가드가 **실행해서 host DOM 이 안 갈리는지**를 본다. 검증은 실브라우저 CDP(`Input.imeSetComposition`)로 **수정 전 `ㅁ며면면` 재현 → 수정 후 `면` 확정**을 대조.
- ⚠ **변이시험은 커밋 후에** — 뮤테이션 되돌리기의 `git checkout` 이 미커밋 구현까지 통째로 되돌린다(이 작업에서 실제로 밟아 재작업). 회귀가드 `tests/homeTasklistFilters.test.js`(판정·렌더 vm 실행 + tabStatsMap·tab-folders 스텁 실행 + find-only + 칸 수 계수, **변이시험 검출 확인**) + 실브라우저 실행검증(sticky 실측·열 x좌표 동일·새창·스위치 왕복). ⚠ `homeCampaignButton` 가드의 .wbl-act 패턴을 함께 갱신(검사 의미 불변).

### ★★ 홈 작업목록 [⋯] → 작업 삭제 (admin/master 전용, 사용자 확정 2026-08-19)
- **발단**: 접수 재시도·오발행으로 **같은 이름의 작업이 두 줄** 생기고 한 줄이 고아로 남았다(실측 `[TEST]…` 3종 · 「웰스앤헬스 …500건」 0/500). 종전 수단은 [🏁 마감](목록엔 남고 보관함으로)과 무시트 잔재 정리(`sheetlessOrphanCleanup` — **빈 가상 탭만**, 작업오더·공고·계약 연결이 하나라도 있으면 제외)뿐이라 **"연결은 있는데 실체가 없는 고아"를 치울 창구가 없었다**.
- ★★ **되돌릴 수 없다(사용자 확정: DB 행까지 실제 삭제)** — 그래서 방어 셋: ① **미리보기 → 체크 → 실행** 2단계(`confirm !== true` 면 서버가 거부 · 문자열 `'true'` 도 확인이 아니다) ② **돈 기록은 두 번째 확인으로만**(아래) ③ **표 분류 전수 고정**.
- ★★ **입금 기록이 걸린 작업도 지울 수 있다 — 단 별도 확인(사용자 확정 2026-08-19, 종전 절대 거부에서 완화)**: 일반 요청(`forcePayment` 없음)은 **쓰기 0건으로 거부**하고, 화면이 무엇이 걸렸는지(`payment_batch_items`·`payment_records`·`manual_payment_marks`·`unconfirmed_transfer_reviews`) 말한 뒤 **별도 체크 + [🗑 입금 기록까지 삭제]** 버튼을 누른 요청만 그 기록까지 지운다. ★ 한 단계를 남긴 이유 = 지우면 그 리뷰어 줄이 **미입금으로 보여 다음 회차에 다시 담기고 이중 송금**이 날 수 있다(129 "표에서만 빼기"가 존재하는 이유) — 막지는 않되 실수로 눌리지 않게 한다. ★★ **삭제 순서가 계약**: `payment_account_mismatch_reconciliations.batch_item_id` 가 회차 항목을 **ON DELETE RESTRICT**(119)로 잡아 그 대조 이력을 **먼저** 지워야 FK 로 튕기지 않는다. ★★ **입금 회차(`payment_batches`) 자체는 남긴다** — 한 회차가 여러 작업에 걸쳐 있어 지우면 **남의 작업 입금 기록까지** 사라진다. ★ 미리보기 이후 회차에 담긴 경우(TOCTOU)는 트랜잭션 안 재검사가 잡고 화면이 확인창을 다시 그린다.
- ★★ **표 분류는 전수 — 새 표가 생기면 가드가 빨개진다**: `(sheet_id, tab_name)` 을 가진 표는 전부 `workTabDelete.service` 의 `DELETE_TABLES`(36) ∪ `MONEY_TABLES`(4) 중 하나에 있어야 한다(회귀가드가 마이그레이션을 읽어 대조 — "지워야 하는데 남는 잔재"도 "지우면 안 되는데 지워지는 사고"도 이 자리에서 막는다). 삭제 순서는 자식 → 부모, **등록행 `tab_configs` 가 마지막**.
- ★ **곁다리 정리 4종(같은 트랜잭션)**: 연결 작업오더는 **링크만 해제**(삭제 금지 — 같은 오더를 **다시 접수**할 수 있어야 한다) · **연결 모집공고는 함께 삭제**(사용자 확정 2026-08-19 — 종전 보관 처리에서 변경. 작업이 사라졌는데 그 작업을 가리키는 공고만 남으면 **갈 곳 없는 공고**가 목록에 뜬다. 참여 신청·옵션·리뷰비 구간·차수·날짜별 계획·리뷰어 게이트는 **FK CASCADE** 로 함께 사라지고 — 018·061·082·091·095·112·115 전부 CASCADE·RESTRICT 없음 — 입금 회차 항목의 `campaign_id` 는 **SET NULL**(086)이라 회차 자체는 무사하다. 화면이 "공고도 함께 삭제된다"를 문장으로 말한다) · 업체 소유(`advertiser_campaigns`)는 **탭 지정 행만**(gid 는 **지우기 전에** 읽는다, ★ **모르면 손대지 않는다** — 빈 값 매칭은 시트 전체 소유·남의 탭을 지운다) · 시트 등록행(`campaigns`)은 그 시트를 쓰는 탭이 하나도 없을 때만.
- ★ **구글시트는 읽지도 쓰지도 않는다**(사용자 확정 — 시트 탭 삭제는 이 기능의 일이 아니다). 등록이 사라져 등록 게이트(`TAB_REGISTRATION_MODE=order`)가 인덱스를 다시 만들지 않으므로 목록에도 돌아오지 않는다. 시트 API·큐 호출 0(회귀가드가 부재를 고정).
- **API** = `GET /api/trackb/work-tab/delete-preview`(**쓰기 0건**) · `POST /api/trackb/work-tab/delete`, 둘 다 **adminOrMaster**(AE·광고주 차단 — 화면도 `_taskDelAllowed()` 로 **버튼 자체를 안 그린다**, 서버 게이트와 1:1). 거부는 400/409 로 매핑(500 마스킹에 사유가 묻히면 담당자가 손쓸 수 없다). 돈 기록 재검사는 **트랜잭션 안에서**(미리보기 이후 회차에 담긴 TOCTOU) + `pg_advisory_xact_lock`.
- **화면** = 홈 작업목록 줄 끝 **[⋯]**(주 줄 버튼은 늘리지 않는다 — 공고 카드 [⋯] 규율): 메뉴·확인창 모두 **body 직속**(표는 가로 스크롤 컨테이너라 안에 그리면 잘린다) · 바깥 클릭 닫기는 **캡처 단계**(항목 onclick 의 `stopPropagation` 을 넘어야 한다) · 전역 리스너 **1회만** · onclick 은 **인덱스만** · 확인창은 **바깥 클릭으로 안 닫힘**(Esc·[취소]만) · 체크 전 실행 버튼 잠금 · 미리보기 실패는 **"삭제 가능"으로 위장하지 않는다**. 열은 12칸(비권한자는 빈 칸 — 모드·사람마다 열 수가 달라지지 않는다).
- ★★ **지워진 작업의 남은 공고 정리**(모집공고 탭 헤더 [🧹 지워진 작업의 공고], adminOrMaster): 공고 함께 삭제가 들어오기 **전에** 지운 작업들의 공고가 남아 있어 만든 창구. ★★ 판정은 **fail-closed** — 연결 시트·탭이 **둘 다 적혀 있고**(빈 값 매칭은 멀쩡한 미연결 공고를 지운다) `tab_configs`·`index_master`·`raw_sheet_tabs` **셋 다 없을 때만**(하나라도 있으면 그 작업은 살아 있다), **gid 로도 찾아** 리네임을 삭제로 오인하지 않는다. ★ 판정 SQL 은 `ORPHAN_CAMPAIGN_WHERE` **한 곳**(목록·삭제 공용)이고 **실행이 그 시점에 다시 판정**한다(낡은 화면이 보낸 id 라도 살아 있으면 건너뛰고 건수를 보고 — 조용한 파괴 금지). 미리보기 → 체크 → 실행 2단계는 작업 삭제와 같다.
- ⚠ **모집공고 탭 머리말은 뷰 진입 때 먼저 그린다** — 종전엔 카드 렌더 훅에서만 그려서 **공고 0건이면 머리말이 통째로 없었다**(＋새 공고·보관함·정리 버튼이 전부 사라지는 막다른 길). 건수는 목록 도착 후 그 훅이 채운다.
- 회귀가드 `tests/workTabDelete.test.js`(표 분류 전수 · 스텁 pool 로 서비스 **실제 실행**(돈 게이트 시 쓰기 0·강제 시 FK 순서·회차 미삭제·confirm 게이트 시 쿼리 0·표별 DELETE·링크 해제/보관 방향·gid 미상) + 라우터 스택 실검사 + 화면 배선, **변이시험 6종 검출 확인**) + 실 http 오리진 브라우저 실측(12열·[⋯] body 직속·확인창·역할별 노출). ⚠ 이 변경으로 `homeTasklistFilters` 의 칸 수 기대값을 11 → 12 로 갱신했다(검사 의미 불변 — 헤더 칸 수 ≡ 행 칸 수).
- ⚠ 잔여(문서화): ① **백업 말고는 복구 수단이 없다**(확인창이 그 사실을 말한다) ② 리뷰어 1:1문의방(`cs_threads`)·아카이브 이력은 지우지 않는다(그 작업 데이터가 아니다) — 공고는 지우지만 그 공고로 참여했던 리뷰어의 문의방은 남는다 ③ 시트 파일·탭은 그대로 남는다 ④ 입금 기록까지 지우면 그 회차의 이체 이력이 사라진다(회차 자체는 남는다) — 지우지 않고 목록에서만 내리려면 [🏁 마감].

### 리뷰웹시스템[3버전] 화면 폭 상한 (FHD/QHD 토글) · 열 너비 고정
- **문제 2개**: ① 창을 넓힐수록 헤더·업체 칩바·탭바·본문이 끝없이 따라 늘어났다. ② 시트 그리드가 컨테이너를 꽉 채우려고 **주소 열이 잔여폭을 흡수**해, 같은 열이 창 크기·숨긴 열 수마다 다른 너비가 됐다(작업을 오갈 때마다 열 위치를 다시 찾음).
- **상한**: `--app-max` 토큰 하나(`:root` 기본 1920 + `body[data-vw="fhd"|"qhd"]` = 1920/2560), 우측 상단 좌우 슬라이딩 스위치(`.vwsw` `#vwToggle`, `localStorage.wd_viewport_mode`, **기본 FHD**=좁은 쪽). 적용은 `.top`·`.tb1`·`.tb2`·`.wrap`·`.ovwrap` 5곳.
- ★ **작업보드 상단(`.tp3grid`)은 `max-width:1380px`**(광고주 2열 `.n2` 는 920px) — 넓은 화면에서 남는 폭을 카드가 나눠 먹어 "카드가 화면 끝까지 늘어나는" 신고를 막는다. 남는 폭은 우측 여백(모집공고 카드 컨테이너와 같은 원리), 긴 상품명은 `.tp3kv dd` ellipsis+title 툴팁이 흡수. ★ `body.advm .tp3grid`(0,2,1)가 `.tp3grid.n2`(0,2,0)를 이기므로 **광고주 2열 상한은 `body.advm .tp3grid.n2` 로 다시 걸어야** 한다(실측). 종전 8칸 스트립(`.stripA .mcell`)은 아래 3단 구성으로 대체됐다.
- ★ **전체화면(widemode)은 상한 해제**(`--app-max:100vw`) — 표에 최대 폭을 주는 모드라 상한이 오히려 방해. `none` 은 `calc` 안에서 무효라 쓰면 상한이 통째로 죽는다.
- ★★ **표폭 = 보이는 열 고정폭의 합**(`_fitGrid` 가 `tbl.style.width` 로 못박음 · `table.sheetgrid{min-width:0}`): 창을 아무리 넓혀도 열 너비 불변, 남는 공간은 우측 여백. **열이 많은 작업일수록 합이 커져 표만 그만큼 넓어진다.** 유동 열(`_isFlexCol`=주소) 개념은 제거 — 부활시키면 열 너비가 다시 창 크기를 탄다. `_fitGrid` 는 역할 분기 없음(광고주=내부 동일 규칙, 종전 `advsnug` 동작이 기본이 됨).
- ★★ **실측 사고(조용한 파싱 포기)**: `:root{--tbh:…}` 위 주석에 닫는 표시가 하나 더 있어 주석이 일찍 닫혔고, 뒤 설명 텍스트가 top-level 셀렉터로 파싱되며 **`:root` 규칙을 통째로 삼켰다** → `--app-max` 기본값이 아예 적용 안 됨(FHD 모드 상한 없음). 브라우저는 에러 없이 넘어가고 **선언은 멀쩡히 '있어서' grep 가드로는 못 잡는다** → 회귀가드가 주석/중괄호 **토큰을 센다**. 회귀가드 `server/tests/workdeskWidthCap.test.js`.
- **미적용(문서화)**: 업체관리 연결탭 표(`.othead`/`.owntab`)는 여전히 `fr` 스트레치 — 상한 안에서만 늘어나므로 방치. 필요해지면 fr→고정폭.

### 리뷰이미지 교체요청 ↔ C/S 문의창구 연동 (migration 080)
- **목적**: 교체요청의 승인·반려 결과가 리뷰어에게 **전혀 통지되지 않던 것**(SSE 는 관리자 위젯용뿐, 리뷰어는 '내 요청함'을 다시 열어야 알았다)을 메운다. 요청이 오면 그 리뷰어의 C/S 스레드에 **카드 메시지**가 자동으로 생기고(스레드 없으면 생성 — 사용자 확정), 관리자가 대화창·전용탭·기존 관리자페이지 어디서 처리하든 리뷰어 채팅에 **"승인/반려되었습니다"가 자동 전송**된다(반려는 사유 필수, 그 사유가 그대로 전달).
- **데이터**: `cs_messages.msg_type`(`text`|`review_edit`) + `meta jsonb`. 요청 1건 = 카드 1장(`uq_cs_msg_review_edit` 부분유니크), 승인/반려는 **제자리 갱신**(meta 병합) + 별도 text 통지. 스레드 키는 리뷰어 UI 와 같은 `sheetId||tabName`(다르면 같은 탭에 방이 두 개 생긴다). CHECK 는 **NOT VALID**(러너가 파일을 암묵 트랜잭션으로 돌려 스캔 중 ACCESS EXCLUSIVE 가 유지된다).
- **단일 출처** `csBridge.service.js` — 라우트가 아니라 **서비스 레벨**에서 부르므로 어느 화면에서 눌러도 결과가 같다. ★★ **절대 throw 하지 않는다**: 승인은 Drive 이동·rename·`review_index` 갱신까지 끝난 뒤의 후처리라, 통지 실패로 예외가 오르면 "파일은 바뀌었는데 요청은 pending"이 된다. 반려도 **커넥션 반납 뒤** 호출(같은 풀에서 새 커넥션을 얻으므로 쥔 채 부르면 자기교착).
- **권한**: 전용 탭 `/api/trackb/review-edit/*` 는 **AE(staff)도 담당 탭만**(목록=결과 필터, 승인/반려=위임 **전** `canAccessTab`), 광고주·리뷰어 차단. **C/S 문의 탭은 여전히 master/admin 전용** — 문의 본문에 리뷰어 실명·연락처·주소·주문정보가 그대로 실려 담당 스코프로 나눌 수 없다. 원본 `/api/review-edit/*` 정책은 무변경.
- ★★ **리뷰어에게 나가는 값의 규율**(코드리뷰로 잡힌 것): ① `meta` 는 `/api/reviewer/cs/messages` 응답에 **그대로 실린다** — `adminNickname.maskMessages` 는 `senderName` 만 치환하므로 **관리자 실명을 meta 에 넣으면 안 된다**(화면에서만 감추는 건 devtools 에 보이는 보안연극). ② SSE 푸시도 페이로드로 새므로 `toReviewerName` 으로 치환해 보낸다. ③ 기존 스레드의 `campaign_label` 을 **덮어쓰지 않는다** — 그 값 출처에 **시트 제목**이 섞이는데 시트 제목은 리뷰어 응답에 넣지 않는다는 규칙이 `reviewer.routes` 에 있다.
- ★ **이미지는 파일ID로 넘긴다** — `image_urls`(guide-image 만 허용하는 sanitize) 를 넓히지 않고 `meta.oldFileId/newFileId` 만 저장, 프론트가 `/api/drive/image/<id>` 를 **신뢰 베이스로 재구성**한다. 반려 시 `newFileId` 를 비운다(그 파일은 휴지통으로 가 썸네일이 깨진다). 취소도 카드를 `cancelled` 로 내린다(안 그러면 [승인]/[반려] 가 살아 있는 좀비 카드).
- ★★ **`window.API_BASE_URL` 은 존재하지 않는다**(실측 블로커): `api.js` 의 `const API_BASE_URL` 은 최상위 const 라 window 프로퍼티가 **아니다**. `window.` 로 읽으면 항상 undefined → 상대경로가 되어 프론트(Pages)에서 API(Railway) 이미지를 전부 못 부른다. 레포 관용구는 bare 식별자(`campaign-cards.js` `_apiBase`). ⚠ **`file://` 로 여는 브라우저 테스트는 이 버그를 가린다** — 상대경로도 스텁에 걸린다. 이런 검증은 **실제 http 오리진**에서 할 것.
- ★ **Express 4 는 async 핸들러 rejection 을 안 잡는다** — `_reCanTouch` 가 UUID 아닌 id 로 22P02 를 맞으면 **응답이 영영 안 나간다**(클라이언트는 서버 타임아웃까지 대기). 형식 선검사 + try/catch 로 전부 fail-closed 응답.
- 카드 렌더러는 `js/cs-review-edit-card.js` **한 벌** — 관리자 대화창·전용 탭·리뷰어 채팅이 같은 것을 그린다(리뷰어는 `admin:false` 로 버튼 없음).
- ★ **썸네일 클릭 = 기존↔변경 요청 둘 다 크게 보기**(`zoomPair`): 교체요청 처리는 "무엇이 어떻게 바뀌었나"를 대조하는 일이라 클릭한 한 장만 띄우면 오버레이를 닫았다 여는 왕복이 매번 생긴다. **호스트 라이트박스(`woImageModal`)로 위임하지 않는다** — URL 한 장짜리라 위임하는 순간 원래 동작으로 되돌아간다(단일 보기용 `zoom`은 그대로 남겨 한쪽 이미지만 있을 때의 폴백으로 쓴다). 오버레이도 `zoom`과 **분리**(`csReCardZoomPair`) — 그쪽 리스너는 "아무 데나 누르면 닫힘"이라 공유하면 확대한 이미지를 누르는 순간 닫힌다. ★★ **onclick 은 HTML 속성이라 엔티티가 디코드된 뒤 JS 로 파싱**되므로 `_esc`(따옴표→`&quot;`)만으로는 문자열 탈출을 못 막는다 → 파일ID를 `[-\w]` 밖 전부 제거해 넣는다(`_idAttr`). 회귀가드 = `tests/reviewEditCsBridge.test.js` 8절(두 썸네일 모두 zoomPair·양쪽 ID 동봉·단일 위임 부재·탈출 차단). 회귀가드 `tests/reviewEditCsBridge.test.js`(정적 + **런타임 실행**: hang 재발·실명 미노출·절대 URL·리뷰어 버튼 없음).

### 관리자 공고 수정 모달 — v2 레일 배치 · 참여형 기본 (시안 C, `frontend/docs/design-recruit-modal-v2.html`)
- **레이아웃**: 데스크톱 폭 **1124px(사용자 확정 2026-08-19)** — 값은 모달 CSS 가 갖고 `tests/recruitModalCompactTemplateParity` 의 `RF_DESKTOP_WIDTH` 가 고정한다. ★ 바꿀 땐 **둘을 함께** 고친다(그동안 문서 1280 · 가드 1020 · 코드 1124 로 셋이 갈려 가드가 상시 빨간 상태였고, 그 더미에 진짜 회귀가 묻혔다). `.rf-split` = **레일(`.rf-rail` 176px · 목차+배치 편집) / 입력 카드 / 미리보기(`.rf-side` 340px)** 3단. 900px 이하 세로 복귀 + 레일 접힘(배치 편집은 넓은 화면 전용). ★★ **레이아웃 CSS는 `@media` 밖 최상위**(안에 넣어 특정 폭에서만 먹은 실측 버그).
- ★★ **참여형이 기본**: 참여형 스위치(녹색 박스) UI 제거 — `rf_participation`은 **hidden 체크박스**로 유지(저장 페이로드·서버 분기·`onParticipationToggle` 계약 무변경). 신규 공고 = 초기화가 `checked=true`, **레거시(일반) 공고 편집 = 프리필 else 분기가 끔** → 참여형 전용 카드가 숨고 `rf_legacy_note`(레거시 안내 + [⚡참여형으로 전환]=`rfLegacyConvert`)가 뜬다. 전용 카드 토글은 기존 `rf_part_section`/`rf_work_section` 두 계약 + **신규 `[data-part-only]` 일괄 토글**(진행상품 카드·자동점검 블록).
- **통일 카드 문법**: 색 박스 4종(참여형 녹·기간별리뷰비 녹·타계정 보라·현영 주황) 폐기 → **흰 카드(`.rf-card`) + 회색 헤더(`.rf-ch`) 한 벌**, 라벨은 `.rf-hrow` **82px 우측정렬 한 종**(종전 64/76px 혼재 제거). 카드 7장(`data-sec`) = **고정 2**(`link` 맨 위 — 시트·탭 연결이 모든 자동값의 전제 / `pub` 맨 아래 — 자동점검이 저장 버튼 직전) + **자유 5**(`prod`/`cond`/`fee`/`info`/`work`).
- ★ **레일 = 배치 편집기**: 레일 ⠿ 드래그(핸들 mousedown 때만 draggable — 입력 텍스트 선택과 무충돌)로 본문 카드가 같은 순서로 재배열, `localStorage.rf_layout_v1`에 관리자별 저장(**서버 변경 0**, 실패 시 기본 순서 폴백, 모르는 키 무시·빠진 키 기본 보충 = 버전업 안전). 프리셋 [기본 순서(발행 흐름)]·[수정 최적화]. 레일은 **본문 DOM이 단일 출처**(카드에서 다시 그림 — 레거시로 숨은 카드는 목차에서도 제외)이며 상태판 겸용(● 필수 미입력 · ⚠ 자동점검 경고 수 · ✓ 입력됨) + 클릭 = 해당 카드로 스크롤(+스크롤 스파이).
- **필드 ID 불변** — 기존 프리필·저장 로직 계약 유지(회귀가드가 36개 ID 생존 확인). 리뷰타입(087)·이체은행(086)·기간별 리뷰비(082)·리뷰어 숨김(085)은 상시 카드(`info`/`fee`/`pub`)에 있어 레거시 공고에서도 보인다.
- **진행상품 표**(`rf_opt_rows`, 5열 = 상품명·옵션명·결제금액·총인원·일건수): 옵션 하나가 한 줄. 옵션 추가 시 상품명이 위 행에서 따라옴(`_lastOptProductName`, 같은 값은 `.rf-dup` 회색). **표가 진실원본** — `_syncPreviewFromOptRows`가 ① 작업내용 상품 원문(`rf_wd_product`) ② 캠페인 정원(`rf_daily_limit`/`rf_recruit_total`, **hidden**)을 파생한다. ★ 하나라도 0이면 합계도 0(무제한) — 부분합을 상한으로 쓰면 조기 마감 사고. 마감 옵션은 합계 제외. 그래서 "총모집≠정원합" 경고는 성립하지 않아 제거(항상 일치).
- **작업오더 자동 적용**(`applyProductRowsFromOrder`): 옵션 배열(`product_options_json`)이 있으면 그대로, 없으면 **상품정보 텍스트를 줄 단위로 분해**(`parseProductLinesToRows`). 실측 형식 `A - 옵1 - 결제금액 28,900원` / `옵2 - 결제금액 28,900원`(상품명 생략 시 앞 줄 계승) / `상품A / 레드 / 12,000원` / `힙스 31400원` 지원. ★ **"옵션 없음·단일·해당없음"은 옵션명이 아니라 서술**이라 빈 값으로(시트 옵션열 오염·가짜 선택지 방지). 분해 실패는 상품명 칸에 통째로(값 유실 0).
- **종료일**(`rf_deadline` → `recruit_campaigns.deadline`): 시트 일정 파생 마감일(`endDate`)과 다르면 **경고만 띄우고 시트를 따른다**(`onRecruitDatesChange`, 사용자 확정 ③). 입력값은 참고로 보관. 시작일·종료일은 왼쪽에 붙이고 구매시간대가 같은 행 오른쪽.
- **리뷰어앱 모달과의 관계**: 그쪽 13개 필드는 서버 `_scopedCampaignEdit` UPDATE 화이트리스트 12컬럼과 1:1 → **필드를 늘리면 "저장했는데 반영 안 되는" 창**이 된다. 통일 대상은 필드 개수가 아니라 같은 필드의 동작(리뷰비 `step="100"`·[바로가기 ↗]·자율주문 자동 감지를 관리자로 가져옴).
- **마크업·CSS·배치 로직은 `js/recruit-modal.js` 공유 모듈**(admin.html 인라인에서 이관 — 리뷰웹시스템[3버전]과 한 벌). `admin.html`·`workdesk.html` 둘 다 `#recruitModalMount`(**body 직속**)에 마운트하고, 모듈이 껍데기 CSS(`.modal-overlay`/`.modal-box`/`.rform-*`)까지 `#recruitModal`로 스코프해 동봉하므로 admin 테마 없는 화면에서도 같은 모양으로 뜬다(모듈 CSS 선택자는 `#recruitModal`·`.rf-*`만 — 회귀가드가 토큰을 센다). 회귀가드 `tests/recruitModalLayout.test.js`(레이아웃 + 참여형 기본 + 레일 배치 + CSS 무결성) + 실브라우저 검증(테마 없는 http 호스트에서 마운트·토글·프리셋·localStorage 복원·반응형 21항목).

### ★ 작업내용 3칸 첨부 이미지 — 썸네일 미리보기 · 확대 팝업 · 그 자리에서 첨부 (시안 `frontend/docs/design-workdetail-images.html`)
- **배경**: 유입가이드에 이미지가 붙어 있어도 관리자 화면엔 `[이미지]` 라는 **글자만** 남았고, **글을 한 글자만 고쳐도 사진이 통째로 사라졌다**("원본에 있던 이미지가 빠진 평문으로 게시됩니다" 확인창 = 실제로 사라졌다는 뜻). 관리자가 사진을 추가할 창구도 없었다. 사용자 확정 6건 = ①입력창 **오른쪽** 배치 ②삭제 허용 ③**순서 바꾸기 포함** ④칸당 **4장** ⑤**유입가이드·리뷰가이드·특이사항 3칸 모두** ⑥세 칸 **높이 통일**(rows 3 · 82px).
- ★★ **세 칸의 저장 형태가 다르다(합치지 말 것)**: 유입가이드는 이미 HTML(`work_detail.inflowGuideHtml`)이라 **그 안의 `<img>`** 로 두고(기존 저장분 호환 — 새 칸을 만들면 옛 공고 사진을 못 읽는다), 리뷰가이드·특이사항은 **평문 필드**라 텍스트는 그대로 두고 **사진만 새 배열**(`reviewGuideImages`/`specialNotesImages`)로 옆에 둔다. ★ **평문 칸을 HTML 로 승격하지 않는다** — 기존 저장분의 `<옵션>` 같은 꺾쇠 글자가 태그로 오인돼 **통째로 삭제**된다(이 레포가 이미 겪어 "평문은 escape 후 `<br>`"로 굳혀 둔 자리). 신규 테이블·컬럼·엔드포인트 **0**.
- ★★ **유입가이드 조립 단일 출처 = `_igComposeInflow`**(저장·미리보기 공용 — 갈라지면 "미리보기 ≠ 저장본"): 모달을 열 때 `_igLoadInflowHtml` 이 원본을 **이미지부/텍스트부로 분해**하고, 저장 때 **글 미수정 + 사진 구성 그대로면 원본 HTML 을 그대로**(바이트 동일 — 단락·링크·사진 위치 보존 = 종전 동작) 돌려준다. 그 외에는 `텍스트 + <img>들`로 재조립 → **글을 고쳐도 사진이 살아남는다**. 확인창은 없애지 않고 **문구만 사실에 맞게**("사진 N장은 그대로 유지되고 글자만 다시 게시 · 줄 서식·링크는 사라지고 사진은 글 아래로 모입니다").
- ★ **넣는 길 3가지(파일선택 · Ctrl+V · 드래그앤드롭)가 `_igUpload` 한 함수로 수렴** — 사본을 두면 한쪽만 장수·용량 제한이 풀린다(1:1문의 첨부와 같은 규율). 붙여넣기는 **이미지일 때만** 가로채고(텍스트 붙여넣기 기본 동작 보존), **바깥에서 온 파일 드래그**(`dataTransfer.types` 에 `Files`)만 업로드로 받아 **안에서 끄는 순서 변경**(`application/x-ig-reorder`)과 구분한다. 순서 드롭은 **오른쪽 = 대상 뒤**(항상 앞이면 끝자리로 옮길 수가 없다 — 작업탭 M2 규율). 업로드는 기존 `POST /api/order/guide-image` 재사용.
- ★ **확대 팝업은 body 직속**(모달 스크롤 컨테이너 안에 두면 오버레이가 화면 흐름에 섞인다) · **그 칸의 사진 안에서만** ←→ 이동(칸이 섞이면 지금 뭘 보는지 잃는다 — 그래서 팝업에 칸 이름 표기) · **끝에서 순환하지 않는다** · 키 리스너는 **최상위 1회**(`window._igKeyBound`) · onclick 없이 위임하고 **넘기는 값은 인덱스뿐**(주소 문자열 보간은 따옴표 하나로 탈출된다 — 실측 XSS 선례).
- ★★ **서버 검증이 이번 건의 유일한 서버 변경**: `work_detail` 은 JSONB 통째 저장이라 배열을 검증 없이 넣으면 **임의 주소가 리뷰어 화면의 `<img src>` 로 그대로 나간다** → `sanitizeGuideImages`(우리 프록시 주소 정확일치 · 파일ID 중복 제거 · **칸당 4장 재확인** — 화면만 믿지 않는다)를 **저장(`_prepWorkDetail`)·응답(`sanitizeWorkDetail`) 양쪽**에 건다(§03-E 이중 적용과 같은 규율). ★ **미전송 필드는 만들지 않는다**(옛 스냅샷에 빈 배열을 심지 않기 위해). 규칙 모양은 리뷰어 1:1문의·관리자 답장 첨부와 동일(`https?` 허용 — 여기서만 좁히면 `PUBLIC_API_URL` 이 http 인 배포에서 첨부가 조용히 사라진다).
- ★ **리뷰어 화면(`campaign-workdetail.js`)은 사진을 그 카드 안에** 그린다 — 리뷰가이드 사진이 유입가이드 밑에 나오면 리뷰어가 "구매 경로 안내"로 읽는다. **글이 비고 사진만 있어도 카드를 낸다**(안 그리면 넣은 사진이 통째로 사라진다). 배열 토큰을 `extractGuideImages` 의 "이미 있음" 목록에 넘겨 **이중 노출을 접는다**. 호스트는 **신뢰 베이스로 재구성**(배열의 임의 호스트를 그대로 쓰지 않는다). 레거시 경로(리뷰가이드 **평문**에 섞여 온 주소 → 유입가이드 카드)는 **무회귀로 유지**.
- ★ **저장 payload 는 위젯 상태를 항상 싣는다** — `work_detail` 은 통째 교체 저장이라 **"미전송 = 유지"가 성립하지 않는다**(옵션표·리뷰타입과 다른 점). 프리필로 실린 값이 그대로 되돌아가므로 스트립 없는 화면에서도 사진이 지워지지 않는다. 로드 실패(`_recruitEditLoadFailed`) 시엔 그 위의 기존 가드가 work_detail 자체를 미전송으로 만든다.
- ★ **4장 초과로 들어와 있는 공고는 자르지 않는다** — 작업오더 쪽 첨부는 8장 상한이라 5장 이상이 올 수 있다. 전부 보여주고 **[＋]만 숨기고 안내**한다(조용히 자르면 관리자가 모르는 사이 사진이 사라진다 · 무엇을 뺄지는 사람이 고른다). 저장은 막지 않는다(경고 전용).
- ★ **`_igResetAll` 이 배선도 다시 잡는다**(멱등) — 리뷰웹시스템[3버전]은 모달을 **뷰 진입 시** 마운트하므로 스크립트 로드 시점 배선만 두면 그 화면에서 스트립이 통째로 죽는다. ★ CSS 는 `#recruitModal` 스코프 + `var` 폴백 + **`box-sizing:border-box` 를 호스트 리셋에 기대지 않는다**(content-box 화면에서 스트립이 padding·border 만큼 높아져 입력창과 어긋난다 — 실측).
- 회귀가드 `tests/workDetailImages.test.js`(위젯 블록을 **vm 으로 꺼내 실제 실행**(왕복 바이트 동일·글 수정 시 사진 생존·순서 반영) + 서버 정화 실행 + 리뷰어 렌더 실행 + 배선, **변이시험 검출 확인**) + 실 http 오리진 브라우저 검증(업로드 3장·상한·확대 팝업·삭제·실패·비이미지 거부·세 칸 높이 82px). ⚠ 이 변경으로 기존 가드 2종을 함께 갱신했다 — `recruitModalLayout`(팝업이 body 직속이라 `#igLightbox` 를 CSS 스코프 허용목록에 추가) · `campaignM3Guards`(조립이 `_igComposeInflow` 로 모임). **검사 의미는 불변.**
- 잔여(문서화): ① 글을 고치면 본문 중간 사진이 **맨 뒤로 모이고** 링크·굵기는 평문이 된다(확인창이 고지) ② 순서 바꾸기는 **마우스 전용** ③ **리뷰어 화면에는 확대가 없다**(지금과 같음) ④ 작업오더 쪽 8장 상한은 그대로 ⑤ 리뷰가이드 **글**은 종전대로 `[리뷰등록 가이드]` 섹션만 리뷰어에게 나가고, **사진은 섹션과 무관하게** 그 카드에 나온다.

### 작업표 생성 (리뷰웹시스템[3버전] 업체별 작업생성) — M1 완료 (PRD: `frontend/docs/prd-worktable-generation.html` v1.1)
- **목표**: 100건 작업이면 **리뷰웹시스템[3버전]이 100행짜리 작업표를 미리 만든다** — "시트에 100줄을 손으로 준비해 작업오더에 URL을 붙이는" 선행작업 제거.
- **확정 방향(2026-08-04, 사용자 확정 4종)**: ① 출시 모드 = **A 하이브리드**(작업표 생성 + 동일 구성 시트 자동 생성·병행 → 주문원장·행배정·사후검증 등 **라이브 경로 무변경**) ② 시트 URL 필수 **즉시 해제** ③ 템플릿 소유 단위 = **채널별 기본**(업체별 원장 미생성) ④ 광고주 열람 = **광고주 링크 즉시 전환**(하이브리드 기간에도 시트 신규 공유 안 함).
- ★★ **제출은 선택, 접수는 유지**: `/order/submit`·`/intake`·`/my/update`·intake update의 `work_sheet_url` 필수 검증을 제거했지만 **`/order/admin/accept`는 URL+gid를 계속 강제**(`needsSheet:true`). 접수는 그 탭을 `tab_configs`·`campaigns`에 등록하는 단일 관문이라 등록할 탭이 없으면 성립하지 않고, 이 게이트가 **"접수된 오더 = `linked_tab_*` 보유"** 불변식(모집공고 자동채움 전제)을 지킨다. M2 전까지 시트 미첨부 오더는 관리자가 URL을 채워 접수.
- **M1 = 헤더 학습 리포트 + 표준 열 확정**. ★ 위치는 **설정 탭 안**(사용자 확정 — 상단 탭 아님): 관리자 대시보드 설정 · 리뷰웹시스템[3버전] 설정 **양쪽에 같은 패널**(`js/admin-settings.js` 공유 모듈의 `worktable` 패널, `PANELS`/`LOADERS`에 등록). 노출은 adminOrMaster(AE는 패널 자체가 안 그려짐 = 서버 게이트와 1:1).
  - **리포트**(`GET /api/trackb/worktable/header-stats`): 활성 탭들의 감지 헤더를 집계해 **코어 후보 / 작업별·채널별 / 미분류** 3묶음 + 탭 비율(고정 ≥80% · 일부만 <30%) + 헤더 이름 변형 분포 + **상태칸 겹침 경고**. **읽기 전용·시트 재읽기 0**(RAW 미러만)·조회 실패는 빈 리포트(fail-soft). ★ **펼칠 때 1회만 로드** — 설정 화면을 열 때마다 무거운 집계를 돌리지 않는다.
  - **표준 열 커스터마이징**(`GET/POST /api/trackb/worktable/template`): 저장은 `app_settings.worktable_template` **JSON 1키**(신규 테이블·마이그레이션 0). ★ **저장하는 것은 역할이 아니라 열 이름** — 작업표에 만들 열의 이름이 곧 매퍼 판정을 결정한다('연락처'로 만들어야 제출이 전화번호를 쓴다). 서버가 정규화(트림·대소문자 무시 중복제거·40자·60열 상한)하고 **분류·경고를 되돌려준다**(같은 역할 2개 = 제출이 두 칸에 같은 값을 씀).
  - **화면 = "열 구성" 블록 한 곳**(사용자 확정): 맨 위 **🌐 공통(모든 채널)** 행 + 채널 행(기본 4 + 직접 추가) + 작업유형 행. 작업표의 열 = **공통 + 그 채널 행**이라 채널마다 같은 15열을 반복해 넣지 않는다. ★ **공통 = 코어 열 그 자체**(별도 개념 없음 — `_wtTpl.core` 한 벌을 미리보기와 아래 "공통 열 상세" 목록이 함께 본다. 두 뷰·한 데이터라 드리프트 불가). ★ **공통·채널 행은 같은 빌더 `_wtRowHtml`** — 사본을 두면 한쪽에만 [▼]가 생기거나 버튼 동작이 갈린다. 편집은 전부 `wtChAdd/Del/Move(key)` → `_wtAfterEdit(key)` 로 수렴(`wtAddCol`/`wtDelCol`/`wtMoveCol`은 `key='core'` 위임 별칭 — 이름은 회귀가드·호출부가 묶여 있어 유지). ★ **공통에 있는 열을 채널에 또 넣지 못한다**(작업표에 같은 열 2번 생성 차단).
  - ★★ **공통 열의 순서·빼기 창구는 "작업표 미리보기" 하나**(사용자 확정 2026-08-04): 종전엔 공통 행 칩(◀▶✕)·미리보기·상세 목록(↑↓✕) 세 곳이 같은 배열을 그렸고 둘이 편집까지 돼 창구가 갈렸다 → 미리보기 칸 클릭 = 선택 → 툴바(◀▶✕, `wtPvSel/wtPvMove/wtPvDel` — 편집 자체는 기존 `wtChMove/wtChDel('core')` 단일 경로 위임, onclick 은 인덱스만). **공통 행은 추가 창구([＋]/[▼]) + 개수 요약만**(칩 미렌더), **"공통 열 상세"는 읽기 전용 설명표**. ★ onclick 함수는 `window` 노출 필수(IIFE — 빠지면 클릭이 조용히 ReferenceError).
  - ★ **상세 각 행에 구매양식 제출 매칭 설명(`fill`)**: "리뷰어가 구매양식을 제출하면 이 칸에 무엇이 들어가는가"를 행마다 문장으로 표기 — **배지 오른쪽 같은 줄**(행 높이 한 줄 유지, 넘치면 말줄임+title 전체 문장, 640px 이하만 아랫줄 전폭). 문장은 **서버 `ROLE_META`(utils/worktableTemplate.js) 단일 출처** — `classifyHeaders`→`_annotate`가 실어 나르고 프론트는 표시만(역할별 설명 사본 금지). 역할 없는 열은 "매칭되지 않습니다"를 말하고(조용히 비우지 않음), 저장 전 임시 열은 `pending`으로 "저장하면 판정" 안내(매칭 없음과 구분).
  - ★★ **화면 = 시안 B**(사용자 확정 2026-08-04, 시안 `frontend/docs/design-worktable-column-ui.html`): 채널·작업유형은 **알약 줄 한 줄씩**(오른쪽 끝에 [⚙ 관리]·[＋ 추가]) · 열 담기는 **미리보기 오른쪽 끝 [＋ 열 추가] → 팝오버**(입력 + "지금 쓰는 열" 후보가 한 창, 후보 클릭 = 즉시 담기·창 유지) · 순서·빼기는 미리보기 칸 클릭. **채널마다 있던 행은 제거**(알약과 중복) — 그래서 삭제·이름변경은 [⚙ 관리] 안에서만 한다(평소 화면에 파괴 버튼 미노출). ★ 작업유형 만들기·고치기는 **전용 모달**(`wtOpenTypeModal` — 브라우저 `prompt` 금지, **body 직속 마운트**), 입력값은 `_wtTypeSync` 한 벌로 읽는다.
  - ★★ **작업유형 자동 선택 조건**(사용자 확정 — "작업오더 내용 보고 자동 반영"): 유형마다 조건을 고른다(`workTypes[].autoTrigger`). 목록·라벨 = `worktable.service.WORK_TYPE_TRIGGERS`(서버가 화면에 내려 줌 — **프론트 사본 금지**), 판정 = `worktablePlan.evalWorkTypeTrigger` **한 곳**. 조건은 전부 `work_orders` 실제 칸에서 파생: `options_2plus`(옵션 2가지↑) · `courier_proxy`(택배대행 예) · `delivery_real`/`delivery_empty`(배송유형) · `review_type_set`(리뷰유형 기재) · `always`/`never`. ★ **값 없음·모르는 값 = `auto` = 종전 동작**(옵션 열 보유 + 옵션 2종 이상) — 옛 저장분이 조용히 안 달라진다. ★ 옵션 개수는 **작업오더가 말한 종류 수**로 센다(배분 결과로 세면 총 건수 미정 오더에서 제안이 죽는다). ★ **제안은 체크를 미리 켜는 것까지** — 만들기 버튼은 사람이 누른다. 화면이 "작업오더를 보고 N종이 자동 선택됨" + 유형별 근거(`suggestReason`)를 **말한다**(조용한 자동 체크 금지). 예: 쿠팡 URL + 옵션 2가지 작업오더 → 채널 쿠팡 + 상품옵션 유형이 켜진 채로 미리보기가 열린다.
  - ★★ **조건 목록을 못 받았을 때 빈 드롭다운을 그리지 말 것**(실측 사고): 목록은 서버가 내려 주는데(`_wtTpl.triggers`) **백엔드 배포가 프론트보다 늦으면 옵션 0개짜리 셀렉트**가 그대로 그려져 화면이 고장난 것처럼 보였다. → 못 받으면 **지금 설정을 옵션으로 남기고 셀렉트를 잠근 뒤 사유 + [다시 불러오기]** 를 띄운다(나머지 항목은 그대로 저장 가능 = 기능 전체가 막히지 않는다). 잠긴 상태에서는 그 값을 읽지 않는다. 회귀가드가 폴백 3요소(값 유지·잠금·안내)를 고정 — **프론트에 조건 목록 사본을 두는 것으로 때우지 않는다**(사본은 조건이 늘 때 조용히 어긋난다).
  - ★ **저장 페이로드에 `autoTrigger` 를 빠뜨리지 말 것**(브라우저 검증으로 잡은 실측 버그) — 모달에서 고른 조건이 서버까지 못 가고 **저장은 되는데 자동 반영만 조용히 `auto` 로 되돌아간다**. 회귀가드가 `wtSaveTemplate` **본문 안**을 지목해 고정한다.
  - ★★ **확장 3종(B·C·D) 구현 완료** — 안내서 `frontend/docs/작업표_열구성_확장_와이어프레임.html`(v1.1).
    - **B 미리보기 채널·유형 알약**: [기본(공통만)/채널…] 1택 + [작업유형] 다중토글 → **공통+채널+유형이 합쳐진 완성 양식**을 그 자리에서 보고 칸 클릭으로 조절. 알약은 **보기·조절 전환일 뿐 저장과 무관**(저장은 [저장] 하나). ★★ 합성 순서(`_wtMergeCols`)는 서버 `worktablePlan.buildColumns`와 **같아야 한다** — 화면이 저장 전에도 즉시 그려야 해 프론트에서 계산하지만, **회귀가드가 두 구현을 vm으로 실제 실행해 열 순서를 대조**한다(사본 드리프트 차단, 변이시험으로 검출 확인).
    - **C 커스텀 작업채널**(지마켓 등): `worktable_template.customChannels[{key,label}]` — ★ **키는 서버가 발급**(`c1`…, 화면 임시키는 저장 응답으로 교체 = 열 고아 방지), 기본 4채널 키와 겹치면 거부. ★ **기본 4채널은 삭제 불가**(현영 발행방법 등과 짝) · **URL 자동 채널판정은 기본 4채널만**(커스텀은 작업표 만들 때 담당자가 목록에서 선택 — 틀린 자동 추측보다 안전) · **현영 이미지 슬롯은 따라 늘지 않는다**(별개 기능).
    - **D 작업유형별 열**(상품옵션·리뷰유형·택배발송대행 등): `workTypes[{key,label,desc,position,columns}]`. **작업표 최종 열 = 공통 + 채널 + 켠 유형**, 순서 = `[공통 자동 열] + [front 유형] + [나머지 공통] + [채널] + [back 유형]`. ★★ **켠 것만 반영**(미전송=없음) — `suggestedWorkTypes`는 화면이 체크를 미리 해 주는 재료일 뿐 서버가 조용히 적용하지 않는다. ★ **제안 근거는 열 역할 파생**(`role==='option'` + 작업오더 옵션 2종 이상) — 이름 매칭이 아니라 유형 이름을 바꿔도 따라온다. ★ 켠 유형의 열이 하나도 안 붙으면 `worktype_no_column` 경고(조용한 소실 금지).
    - ★ **담기·조절 창구는 미리보기 하나** — 🌐 공통 줄은 개수 요약만. 편집은 `wtChAdd/Del/Move(key)` 단일 경로(유형 키 = `wt:<유형키>`), 담을 곳은 **켠 알약들 중에서 고른다**(하나면 그것, 여럿이면 사람이 — 조용한 추측 금지).
    - 작업오더 [📋 작업표] 미리보기에 **구매채널 선택 + 작업유형 체크**가 붙고 `planOptions`로 전달된다(계획 재계산은 여전히 서버).
  - **담는 방법 3가지**: ① 직접 입력 + [＋] ② **[▼] 지금 쓰는 열에서 고르기**(후보 = 헤더 학습 리포트의 역할 열 변형 + 미분류 열, 사용 탭 수 내림차순·칩에 탭 수 표기, 입력칸 타이핑 = 검색어, 이미 담긴 열은 ✓ 비활성, 한 번에 한 목록만) ③ **[공통을 기본 15열로]**(`WT_PRESET_CORE` — 번호·구매일자·주문자·수취인·ID·연락처·주소·은행·계좌번호·예금주·결제금액·주문번호·리뷰제출·입금·비고) / **[통계에서 불러오기]**(서버 `suggestedCore`). ★ ③ 두 버튼은 **공통만** 바꾸고 채널 행은 보존. ★ **프리셋은 버튼으로만** — 저장값이 없을 때 조용히 적용하지 않는다(정하지 않았는데 정해진 상태 금지).
  - ★★ **후보 클릭은 인덱스로 넘긴다**(`wtPickAdd(key, idx)`) — onclick 문자열에 시트에서 온 열 이름을 넣지 않는다(따옴표 탈출 차단, 변이시험으로 가드 검출 확인).
  - ★ 통계는 `_wtEnsureStats` **지연 로드 + 캐시 한 벌** — 고르기·리포트·[통계에서 불러오기]가 같은 것을 쓴다(실측 호출 1회).
  - ★ **경로는 재기준하지 않는다**(`ADMIN_SETTINGS_API` 미사용): `/api/trackb/worktable/*` 는 관리자 대시보드(admin_token)와 리뷰웹시스템[3버전](인트라넷 SSO) **양쪽에서 그대로 닿는 유일한 경로**라 호스트별로 다를 이유가 없다 — 다르게 두면 두 화면이 서로 다른 설정을 본다. 그래서 `EP_DEFAULT`/`EP_SUFFIX` 표에 넣지 않았다(키 개수 불변).
  - ★ 채널 목록은 **현영 4채널 표(`CR_GUIDE_CHANNELS`)를 그대로 재사용** — 채널 사본을 또 만들면 한쪽만 늘어난다(이미 밟은 함정).
  - ★ **쓰기 표면**: 이 서비스의 쓰기는 `app_settings` 업서트 **한 곳뿐**(운영 테이블 무접촉). 회귀가드가 고정.
- ★★ **역할 분류기는 매퍼에서 파생한다(사본 금지)** — `utils/worktableTemplate.js`가 필드별 **센티널을 주입해 `mapOrderToSheetRow`를 실제 실행**하고 결과에서 역할을 되읽는다(`optionWriteColumns`가 쓰는 기법 재사용, 옵션 칸은 그 함수 그대로). 키워드 표를 복사하면 **"제출은 A열에 쓰는데 작업표는 B열을 만든다"** 드리프트가 조용히 생긴다. 센티널에 **공백·NUL 금지**(매퍼가 trim, NUL은 git이 바이너리 취급).
- **계층(tier)**: `core`(제출이 채움 — 생성 시 빈 칸) / `auto`(생성 시 시스템이 값까지: 번호·구매일자) / `channel`(구매채널 id 열) / `work`(옵션 등 작업지시) / `status`(리뷰제출·입금 — `review_index.submit_col/submit_col2`로 판정). ★ **번호(`seq`)는 매퍼에 추가하지 않는다** — 추가하면 제출이 번호를 덮어쓴다(생성 측 분류에만 존재). ★ **NC(쿠팡+네이버) 탭의 id 2열은 매퍼가 일부러 비우지만**(채널구분 불가 → 오기입 방지) 작업표에는 필요하므로 channel 분류만 한다 — **쓰기 표면은 불변**.
- ★★ **판정 우선순위(코드리뷰로 교정, 되돌리지 말 것)**: ① **상태 칸(탭 설정)이 매퍼 판정을 이긴다** — `submit_col2='입금일자'`는 매퍼의 `일자` 규칙에 걸려 dateStr로 잡히므로, 먼저 보지 않으면 **입금 상태 칸이 "자동 채움 구매일자"로 보고**된다. ② 그 겹침은 `conflict`로 남겨 리포트 상단에 **⚠ 경고 카드**로 노출 — 겹치는 열은 **제출이 상태 표시를 덮어쓸 수 있다**(Track B write-back의 컬럼 disjoint 전제 위반)는 실운영 신호라 조용히 한쪽을 고르지 않는다. ③ 채널 id 열 판정은 저수준 `_isIdHeader`가 아니라 **`_idColIndices`(id 규칙이 실제로 이기는 열)** 를 쓴다 — 전자를 쓰면 매퍼가 보호하는 `상품아이디`·`비고(아이디확인)`까지 채널 열로 오분류해 만들지 말아야 할 열을 제안한다.
- ★ **시트URL 선택 항목화의 필수 짝**: `_insertWorkOrder`의 `String(b.work_sheet_url || '').trim` — `|| ''` 폴백이 없으면 필드 생략 시 문자열 `'undefined'`가 저장되어 ① 목록에 시트가 붙은 것처럼 보이고 ② 접수가 "유효한 URL이 아닙니다"로 막혀 시트 미첨부 안내 흐름을 못 탄다.
- ★ **fail-soft catch 안의 logger는 `const { logger } = require(...)`** — 이 모듈은 `{ logger }`를 내보내므로 통째로 받으면 `logger.warn`이 undefined라 **방어하려던 자리에서 TypeError(500)** 가 난다.
- ★ **NUL 문자(0x00)를 주석에 글자 그대로 적지 말 것** — git이 파일 전체를 바이너리로 취급해 diff·grep이 죽고, **grep 기반 회귀가드가 그 파일에서 무력화**된다(이 작업에서 실제로 밟음). 참고: `search.service.js`·`campaign.routes.js`의 NUL은 **의도된 복합키 구분자**(런타임 동작)이므로 제거 금지 — 대신 그 두 파일엔 grep 가드를 걸지 말 것.
- ★ **학습은 제안까지만, 확정은 사람이**: 통계는 생성 미리보기의 **기본값**일 뿐이고 실제 표 생성은 담당자가 확인 후 누를 때만 일어난다(옵션 칸이 "상품옵션"인지 "작업지시"인지 헤더로 구분되지 않는다는 교훈과 같은 규율). 채널 추정 실패는 `unknown`(틀린 채널로 분류하지 않음).
### 작업표 생성 M2a — 계획 산출 + 미리보기 (읽기 전용)
- **`utils/worktablePlan.js` = 순수함수 단일 출처**: 작업오더 + 표준 열 템플릿 → 열 구성·행 수·날짜 분배·옵션 배분·경고를 계산한다. ★★ **미리보기와 실제 생성(M2b)이 같은 함수를 쓴다** — 사본을 두면 "미리보기 ≠ 실제 표"가 되고 그건 이 기능의 존재 이유를 무너뜨린다(campaign-workdetail.js 공용 렌더러와 같은 규율). DB·시트·`Date.now` 미접근(결정적).
- ★★ **구매일자 표기는 `M / D (요일)`** — 063 시트 일정 인식이 그 칸을 읽어 그날 모집 정원을 파생하므로 형식이 어긋나면 정원이 조용히 발행폼 값으로 되돌아간다. ★ **타임존 무관**: `Date` 산술이 아니라 Y-M-D 문자열 + `Date.UTC` 로만 다룬다(서버 TZ가 무엇이든 같은 결과).
- ★ **채널 판정은 상품 URL의 호스트만** — 전체 URL이면 `coupang.com/...?src=naver_ad`를 네이버로 오판하고 `coupang.com.evil.kr`에 속는다. 카카오는 `makers.kakao.com`만. 판정 실패 = `unknown`(채널 열이 안 붙을 뿐, 틀린 채널로 분류하지 않는다). 채널 목록은 `cashReceiptChannels` 한 벌.
- ★★ **막을 것(blockers) vs 알릴 것(warnings)**: 잠그는 것은 "그대로 만들면 반드시 잘못된 표가 되는" 경우만(건수 0 · 열 0 · 옵션 합 ≠ 총건수 · 상한 초과). 시작일 없음·채널 미상·역할 중복·상태칸 겹침·옵션열 없음은 **경고만**(오탐으로 정상 생성을 막지 않는다 — 옵션 칸 자동점검이 하드블록을 안 쓰는 것과 같은 판단).
- ★ 옵션은 **2종 이상일 때만** 배분(선택지가 하나면 기입 의미 없음), `"옵션 없음/단일/해당없음"`류는 서술이라 제외(시트 옵션 칸 오염 방지), 깨진 JSON은 옵션 없음으로 수렴.
- **제외 날짜(공휴일·업체 휴무)**: 작업오더에 날짜 필드가 없어(특이사항 자유 서술) 담당자가 미리보기에서 지정한다 — 뺀 만큼 뒤로 밀린다. 형식 검증·중복 제거·정렬은 **서버가 최종 판정**(프론트에 판정 사본 없음), 잘못된 값은 조용히 무시(날짜 분배가 통째로 깨지는 것보다 낫다). 진행 기간 밖 날짜는 `holiday_outside` 경고로 알린다(오타·잘못 고른 날 노출). 다른 칸을 고쳐도 유지(`_wtpSyncForm` 한 벌 — 값 읽기 사본 금지).
- **미리보기** `GET /api/trackb/worktable/plan?workOrderId=`(authMiddleware + internal + **editorOnly** — 표를 만들 사람이 본다): 읽기 전용, 조정값(total·daily·startDate·skipWeekends)은 **미전송 시 작업오더 값 유지**. 프론트(작업오더 펼침행 [📋 작업표])는 **서버 계획을 그대로 렌더**하고 조정 시 재조회한다(로컬 재계산 금지).
- ★★ **`getPool`은 `trackB.routes.js`에 없다** — 이 파일은 `const pool = require('../db/pool')`. 잘못 쓰면 정적 grep은 통과하고 **런타임 500**이 난다(개발 중 실제로 밟았고 런타임 실행 가드가 잡았다).
- 회귀가드 `tests/worktablePlan.test.js`(순수함수 실행 + 라우터 스택 실검사 + 프론트 배선). 실브라우저로 모달 렌더·조정 재조회·잠금 표시·XSS 이스케이프 확인.
### 작업표 생성 M2b-1 — 시트 만들기 (`worktableCreate.service.js`)
- 미리보기 [이 구성으로 만들기] → **템플릿 탭 복사(서식·공지문 유지) + 열 이름 줄·N행을 설정 구성으로 다시 쓰기** + `work_orders.work_sheet_url` 자동 연결. 건수는 **작업오더 요구 값 그대로**(10·100·170·400 — 고정 100 아님, 사용자 확정).
- ★★ **계획은 서버가 다시 계산한다** — 화면이 보낸 행 목록을 믿지 않는다(낡은 화면·조작 요청이 그대로 시트에 박히는 것 차단). 미리보기 잠금과 서버 게이트가 **같은 `canCreate` 판정**.
- ★★ **`clearSheetValues` 를 쓰지 말 것** — 그 함수는 **gid 를 받지 않아**(범위 문자열만) 방금 만든 탭이 아니라 **첫 번째 탭을 지울 수 있다**(개발 중 런타임 확인으로 잡은 위험). 대신 gid 를 지원하는 `writeSheet` 로 **빈 값을 덮어써** 지운다. 폭 = 우리 열 수와 템플릿 헤더 폭 중 넓은 쪽(옛 헤더 잔존 방지), 본문 아래 20줄만 정리(경계 있는 정리 — 무한정 삭제 금지).
- ★ **헤더 줄 위치는 가정하지 않고 탐지**(`utils/sheetHeader.detectSheetHeader`, **1-based 반환**) — 템플릿 상단 메타(A1 캠페인명·C1:R1 공지문)를 덮지 않는다. 못 찾으면 2행 폴백.
- ★ **시스템이 값을 넣는 칸은 번호·구매일자·옵션 셋뿐** — 나머지는 빈 칸으로 두고 구매양식 제출이 채운다(기존 경로 그대로). 구매일자는 `M / D (요일)` 그대로 써야 063 시트 일정 인식이 읽는다.
- ★★ **쓰기 표면 = 시트 + `work_orders.work_sheet_url` 뿐**. 주문원장·투영·큐·행배정 무접촉, **`tab_configs` 등록은 여전히 접수(accept)가 유일한 관문**(등록 게이트 불변식 유지 — 화면도 "접수는 확인 후 직접" 이라고 말한다). **시트 탭을 시스템이 지우지 않는다**(되돌리기 어려운 파괴는 사람 손에).
- 대상 시트 목록은 기존 `/api/trackb/tabs` 재사용(신규 엔드포인트 0, 1회 로드). `POST /worktable/create`(auth + internal + editorOnly).
- ★★ **템플릿 시트는 선택 항목**(프로덕션 테스트로 잡음): 종전 '새 시트생성'은 `templateSheetId`를 **관리자 브라우저 localStorage**(`rapp_template_sheet_id`)에서만 읽어 **서버는 알 수 없고 사람마다 값이 달랐다**. → 해석 순서 = **요청값 → 전사 설정(`app_settings.worktable_template.templateSheetId`) → env**, 그래도 없으면 **빈 탭으로 만든다**(서식·공지문만 없고 열·행은 동일). 하드 의존으로 두면 설정 안 된 조직에서 기능이 통째로 막힌다. ★ 빈 탭이면 **헤더는 1행**(덮을 메타·공지문이 없다), 템플릿 복사본만 헤더 행을 탐지한다. ★ 템플릿 없이 만들면 화면이 "서식 없이 만들었습니다"라고 **말한다**(조용한 누락 금지). 설정 화면(`설정 › 작업표 표준 열`)에 입력칸 — 시트 주소를 붙여넣으면 ID로 정규화(`normalizeSheetId`, 잘못된 값은 빈 값 = 추측 금지).
- 회귀가드 `tests/worktablePlan.test.js`(계획 재계산·잠금 게이트·gid 지정·라이브 무접촉·쓰기 표면 1곳·행 생성 실행). ⚠ 실제 구글시트 쓰기는 자격증명이 필요해 **배포 후 실물 1건으로 확인**해야 한다.
### 작업표 생성 M2b-2 — 작업대 표에 미리 보이게 (스켈레톤 행)
- 시트를 만든 직후 같은 줄을 `campaign_participants` 에 넣어 **주문 0건이어도 통합작업대 표에 N줄이 보인다**(`participants.createWorktableSlots`). `review_index` 는 이름 없는 행을 버려(`if (!name) return null`) 시트만으로는 빈 줄이 표에 안 나온다.
- ★★ **seq = 시트 실제 행 번호**(`headerRow + i + 1`). 어긋나면 주문이 들어올 때 `importTabFromIndex` 의 `ON CONFLICT (sheet_id, tab_name, seq)` 가 제자리 갱신을 못 하고 **새 행을 만들어** 빈 100줄 + 채워진 30줄 = **130줄로 표가 두 겹**이 된다. `prepareRosterSlots` 의 `_MANUAL_SEQ_BASE=900000` 대역은 **시트 행을 모를 때용**이라 여기선 쓰지 않는다.
- ★★ **`source='worktable'`** (CHECK 제약 없어 마이그레이션 0). 투영 업서트의 상태 CASE 를 `source IN ('import','worktable')` 로 넓혔다 — `'manual'` 로 두면 **리뷰어가 리뷰를 내도 리뷰제출·입금 표시가 영영 안 켜진다**(사람이 만든 행이 아니라 시스템이 자리만 잡아둔 줄이므로 '보존' 대상이 아니다). ★ `_reconcileSeen` 은 **건드리지 않는다**(`source='import'` 만 비활성화 → 빈 줄이 투영에 살아남는다). ★ parity 는 `filter(r => r.p8)` 로 phone8 없는 행을 걸러내므로 **진짜불일치(real)에 안 잡힌다**(cutover 게이트 무영향). ⚠ `overview.b_total` 은 늘어 카운트 일치가 깨질 수 있다(채워지면 해소 — `prepareRosterSlots` 와 같은 성질).
- ★ **멱등·비파괴**: `ON CONFLICT DO NOTHING` — 같은 작업표를 두 번 만들거나 이미 주문이 들어온 줄이 있어도 기존 행을 건드리지 않는다.
- ★★ **헤더 인식 불가 = 잠금**(경고 아님): 열 구성이 `isSheetHeaderRow`(인덱스 빌더와 **같은 함수**) 판정을 통과 못 하면 **그 탭이 통째로 파싱되지 않아** 검색·제출·행배정이 전부 죽고 행 번호도 어긋난다 → `header_unrecognizable` blocker.
- **되돌리기**(`POST /worktable/delete`, `deleteWorktableRows`): **작업대 표의 줄만** 내린다(소프트 `deleted_at`). 시트 탭·주문 원장 무접촉 — 시트는 사람이 지운다. ★ 주문·연락처가 들어온 줄이 있으면 **바로 지우지 않고 목록을 돌려주고**, 담당자가 "내부에서 진행한 테스트건" 임을 확인(`confirmed:true`)한 뒤에만 최종 삭제(사용자 확정).
- ★★ **만든 직후 그 시트를 즉시 미러한다**(`mirrorOneSheet(force)`) — **프로덕션 실측으로 잡은 결함**. 행 배정(`claimRow`)은 RAW 미러 스냅샷에서 빈 줄을 찾는데 방금 만든 탭은 아직 미러에 없다(주기 5분). 그 사이 주문이 들어오면 **준비된 빈 줄을 못 보고 표 아래에 새로 붙는다**(실측: 1~5번 빈 줄을 두고 6번째에 append) — 작업표를 미리 만든 의미가 사라진다. best-effort(실패해도 되돌리지 않음, 다음 주기가 메운다).
- **시트 탭 삭제**(`POST /worktable/delete-tab`, `deleteWorktableTab`): 시스템이 만든 것을 시스템이 못 지우면 정리할 방법이 없다는 지적을 받아 열었다(사용자 확정). ★★ **주문·참여자가 1건이라도 있으면 거부**(`campaign_participants` + `order_submissions` 양쪽 확인) — 시트 탭 삭제는 휴지통이 아니라 사라진다. ★ **확인 실패 = 삭제 안 함**(fail-closed, 모르면 파괴하지 않는다). ★★ **gid 는 서버가 이름으로 재조회** — 클라이언트 gid 를 믿으면 낡은 화면이 **엉뚱한 탭을 지운다**. ★ 마지막 남은 탭은 미리 거부(구글이 거부하는 동작), **파일(스프레드시트) 자체는 어디서도 지우지 않는다**.
- ★ **시트가 1순위 산출물** — 스켈레톤 생성이 실패해도 시트 생성을 되돌리지 않는다(되돌리면 사람이 다시 만들어 중복 탭이 생긴다). 사유를 응답에 실어 화면이 알린다. 킬스위치 `WORKTABLE_DB_ROWS=0` = 시트만 생성(종전 동작).
- 회귀가드 `tests/worktablePlan.test.js`. 기존 `tests/participants.test.js` 의 상태 CASE 단언도 함께 갱신(`manual` 보존은 그대로 고정).
- **다음**: 실제 생성 — 동일 구성 시트 생성(`create-campaign-sheet` 재사용) + DB 스켈레톤 행. ★★ 스켈레톤은 **seq를 시트 실제 행번호에 맞춰야** 주문이 들어올 때 `importTabFromIndex`의 `ON CONFLICT (sheet_id,tab_name,seq)`가 **제자리 갱신**한다(어긋나면 100행 + 유입행이 겹쳐 보인다). `prepareRosterSlots`는 `_MANUAL_SEQ_BASE=900000` 별도 대역이라 그대로는 못 쓴다. 또 `source='manual'` 이면 `is_submitted/is_paid`가 그 CASE에서 **동결**되므로 상태 칸이 영영 갱신 안 된다 — 새 source 값(예 `worktable`)을 그 CASE에 합류시키되 `_reconcileSeen`(source='import'만 비활성)은 건드리지 않는 설계가 필요하다.
- 회귀가드 `tests/worktableTemplate.test.js`(분류기 **실행** 검증(별칭 헤더 추종=사본이면 실패, 분류 ≡ 쓰기 표면 교차확인) + 집계 읽기전용/시트무접촉 + **라우터 스택 실검사** + 시트URL 해제·접수게이트 생존 + 프론트 배선).

### 업무가이드 메뉴 (리뷰웹시스템[3버전] — 직원용 안내서 모음)
- 리뷰웹시스템[3버전] nav **[업무가이드]** → 직원용 안내서를 분류별 카드로 모아 새 창으로 연다(`renderGuidesView`). 흩어져 있던 `frontend/docs/*.html` 안내서를 직원이 찾을 수 있게 하는 창구.
- ★ **목록 단일 출처 = `workdesk.html`의 `WORK_GUIDES` 배열** — 문서를 추가하면 여기 한 줄만 넣는다. 사본을 두면 "어떤 화면에서는 안 보이는 안내서"가 생긴다. 분류 3종(업무 절차 / 문제 해결 / 기능 이해).
- ★ **내부 설계문서(`prd-`·`design-` 접두)는 넣지 않는다** — 직원이 실제로 따라 하는 절차서·문제해결·기능이해만 담아야 목록이 쓸모를 유지한다. 예외는 `prd-userflow.html`(시스템 소개서, 관리자 화면에서도 쓰는 공식 안내서) 하나.
- ★ **광고주에게는 nav 자체가 없다**(내부 업무 문서) — 관리자·AE 두 nav 변형에만 버튼을 둔다. 한글 파일명은 `encodeURIComponent`, 링크는 `target=_blank rel=noopener`.
- ★★ **주석 안에 `*` + `/` 조합을 쓰지 말 것**(실측 사고): 설명문에 `prd-*/design-*`이라고 적었더니 그 `*/`가 **블록 주석을 조기 종료**시켜 뒤 문장이 JS로 파싱되며 `workdesk.html` 메인 스크립트 전체가 죽었다(CSS `:root` 삼킴 사고와 같은 계열 — 브라우저는 에러 없이 넘어가고 grep 가드로는 못 잡는다). 회귀가드가 **인라인 스크립트를 실제로 파싱**해 고정한다.
- 회귀가드 `tests/workGuides.test.js`(스크립트 파싱·죽은 링크·사본 금지·설계문서 배제·광고주 미노출. 변이시험으로 검출 확인).

### ★★ 리뷰타입 — 어휘 통일 + 구매확정 (migration 087) · 1차
- **와이어프레임** `frontend/docs/리뷰타입_구매확정_정합_와이어프레임.html`(맞춰야 하는 항목 26개 = A 용어·값 5 / B 통로 5 / C 화면 4 / D 슬롯 6 / E AI 6, 칸별 지금·맞출 값·안 맞추면 생기는 일).
- ★★ **어휘 충돌 정리(사용자 확정)**: 같은 `review_type` 칸을 두 목록이 나눠 쓰고 겹치는 값이 `구매확정` 하나뿐이었다 — 인트라넷 발주 폼(`포토·텍스트·구매확정·별점·혼합` = 리뷰 결과물 형태) vs 옛 탭 팝오버(`실배송·빈박스·구매확정·믹스` = 앞 둘은 사실 배송유형). **인트라넷 목록으로 통일**하고 이름도 **리뷰타입** 하나로. 실배송/빈박스는 `tab_configs.delivery_type`(이미 있고 접수가 자동으로 채움 — 팝오버에서 배송유형은 *"진행차수/배송유형 제거됨 (자동화)"* 로 이미 빠져 있었다)로, `믹스`→`혼합`.
- ★★ **판정 단일 출처 = `utils/reviewType.js`**(`normalizeReviewType`·`resolveReviewType`). 우선순위 = **시트 작업옵션 칸(행별) > 공고 `recruit_campaigns.review_type` > 탭 `tab_configs.review_type`**. ★ **혼합뿐이면 null** — 어느 행이 무슨 유형인지는 시트에만 있어, "혼합이니까 일단 리뷰"로 접으면 구매확정 행이 리뷰로 잘못 검수된다. ★ **판정 실패는 전부 null = 오늘 동작 그대로**(opt-in). ★ 혼합 판정을 **포토보다 먼저** — `혼합(포토 10건, …)` 문자열에 '포토'가 들어 있어 순서가 뒤면 오판한다.
- ★★ **1차 필터 안전핀(완화 금지)**: 구매확정 완료 화면은 AI 가 아직 아는 종류가 없어 `kind='other'` 로 판정하고 `precheckPolicy` 가 확신 0.9 이상에서 **첨부를 잠근다** = 정상 제출 전면 차단(참여 소각). → `reviewType==='confirm'` 이면 `skip('purchase_confirm_tab')`. **구매확정 화면을 판별하는 층(kind `purchase_confirm`)이 붙기 전까지 이 핀을 빼지 말 것.** `loadTabExpectations` 가 채널과 **같은 LATERAL 행**에서 리뷰타입을 읽는다(따로 조회하면 다른 공고 값이 섞인다).
- **저장**: 공고 create INSERT·update **CASE 센티널**(null=유지 / `''`=해제) + **버튼군 UI 있는 화면에서만 전송**(옵션표·이체설정과 같은 원칙 — 리뷰타입 칸 없는 축약 화면이 저장해도 설정이 안 풀린다). ★ `_scopedCampaignEdit` 화이트리스트 **미포함**(리뷰어앱 스코프 토큰이 검수 정책을 못 바꾼다). ★ `REQUIRED_SCHEMA` 등록(컬럼 없으면 공고 발행·수정 전면 42703).
- ★ **`_rfPickBtn` 은 `rf_<group>` 으로 hidden 을 찾는다** — channel/manager 만 알던 삼항을 일반화했다(그룹이 늘 때마다 삼항을 덧대면 새 그룹이 조용히 빠진다). 프론트 프리필용 라벨 표(`RF_REVIEW_TYPE_LABELS`)는 최소 사본이고 **회귀가드가 서버 `REVIEW_TYPES` 와의 일치를 고정**(workManager 사본 규율).
- **정리 화면** = 설정 탭 **[✅ 리뷰타입 정리]**(`admin-settings.js` `reviewtype` 패널, 관리자 대시보드·리뷰웹시스템[3버전] 공용) — [미리보기]로 건수를 본 뒤에만 [적용하기]가 나타난다. ★ 경로는 **재기준하지 않는다**(`RTC_EP = /api/trackb/settings/review-type-cleanup`) — `authMiddleware` 는 admin 토큰을 받고 `via:'intranet'` 격리는 인트라넷 토큰을 trackb 로 **한정**할 뿐이라 양쪽 호스트가 그대로 닿는다(작업표 표준열과 같은 판단, `EP_DEFAULT`/`EP_SUFFIX` 키 개수 불변). ★ 펼칠 때 자동 실행하지 않는다.
- ★ **옛 값은 화면에서 지우지 않는다** — 선택지에서 빠진 `실배송·빈박스·믹스` 도 배지 색 맵·CSS 를 남겨 그대로 표시한다(무엇이 설정돼 있었는지 알아야 한다). 정리는 **자동 마이그레이션이 아니라** `POST /api/diag/review-type-cleanup`(dryRun 기본, admin/master) — ★★ 배송유형 이관은 **blank-only**(접수가 채운 값을 덮지 않는다). 안 돌려도 안전(판정에서 null 로 떨어진다).
- ★ migration 087 은 **컬럼 추가만**(백필 0) · **TEXT**(082 의 42804 계열 회피) · **DB CHECK 없음**(판정을 코드와 DB 에 이중화하면 어휘 확장 때마다 저장이 실패한다).
- 회귀가드 `tests/reviewType.test.js`(순수함수·`precheckPolicy` **실행** + 배선 + 권한 경계 + 사본 일치. 변이시험 검출 확인). 테마 없는 호스트에서 모달 **실브라우저 렌더** 확인.
- 회귀가드 `tests/reviewType.test.js`, 변이시험 검출 확인.

### ★★ 리뷰타입 2차 — 구매확정 제출 슬롯 + AI 판별
- ★★ **구매확정 단독은 슬롯을 만들지 않는다(완화 금지 · 실측으로 잡은 함정)**: 프론트(`search-app.js:1841`)는 `captureSlots.length > 1` 일 때만 슬롯 UI 를 그리고 단일 첨부 경로는 slotKey 를 안 보내 서버에서 `'review'` 가 된다. 여기서 `[CONFIRM_SLOT]`(길이 1)을 돌려주면 `requiredSlotKeys` 는 `['confirm']` 을 요구하는데 실제 제출은 `'review'` 로 들어와 **완료 판정이 영영 안 된다**(구매확정 제출 전멸). → 슬롯은 종전대로 두고 **기대 화면 종류만** 리뷰타입에서 파생한다.
- ★★ **기대 종류 = `captureVerify._expectedKind(slotKey, reviewType)`** — 슬롯 key 만으로는 정해지지 않는다. `review`+confirm → `purchase_confirm` / `confirm` 슬롯(현영 2슬롯) → `purchase_confirm` / `receipt` 는 리뷰타입 무관. **reviewType 없으면 종전과 완전히 동일.** ★ 회귀가드가 **동기 호출**로 검증한다 — `verifyCapture`(async)로 간접 확인하면 단언이 `process.exit(0)` 뒤로 밀려 **조용히 실행되지 않는다**(변이시험으로 실측·수정).
- **현영과 겹칠 때만 2슬롯**: `[구매확정, 현금영수증]`(리뷰 자리를 치환). 관리자 `capture_slots` 명시는 여전히 최우선.
- ★★ **조회 단일 출처 = `reviewTypeContext.service.js`**(`reviewTypeForTab`, 탭당 60초 캐시) — 소비처 4곳(리뷰어 화면 슬롯 `search.service` · 완료 판정 `submit.routes` · 업로드 폴더 라벨 `diag review-upload` · 교체요청 라벨 `reviewEdit` 2곳)이 각자 SQL 을 쓰면 조용히 갈라진다. 판정 자체는 `utils/reviewType` 에 맡긴다(규칙 사본 0). ★ **조회 실패는 캐시에 넣지 않는다**(일시 장애를 60초 굳히지 않는다). ★ 행 단위(시트 작업옵션 칸)는 여기서 모르므로 **공고 > 탭**까지만 — 혼합 탭은 null 로 떨어져 오늘 동작 그대로.
- **AI**: `classifySubmissionImage` kind 에 `purchase_confirm` 추가 — ★★ **기존 3줄은 한 글자도 바꾸지 않고** 4번째 줄만 넣었다(오래 검증된 슬롯 검수가 그대로 쓴다). ★★ 캐시 접두 `classify2:`→`classify3:`(옛 캐시가 새 종류를 모른 채 히트하는 것 차단, 가드는 `classify[2-9]\d*:` 로 버전 상향 허용).
- ★★ **구매확정 갈래는 어떤 판정에도 잠그지 않는다(완화 금지)**: 예시이미지 등록 전에는 정확도가 낮아 오탐이 곧 정상 제출 차단(참여 소각)이다 → pass/warn 만. 킬스위치 `REVIEW_CONFIRM_VERIFY=0`.
- ★★ **역방향도 경고까지만**: 리뷰 작업 자리에 구매확정 화면이 오면 종전엔 `other` 로 떨어져 차단 대상이었다. 새 종류 도입만으로 **판정이 갑자기 세지면 안 되므로** `purchase_confirm_on_review` warn 으로 둔다 — 잠금은 여전히 `other` 한 갈래뿐(차단 범위를 넓히는 게 아니라 좁히는 방향이라 fail-open 원칙과 맞다).
- ⚠ **배선 형태가 바뀌어 기존 가드 2종의 패턴을 함께 갱신**했다(`captureSlotVerify`·`reviewInspect` — 검사 의미는 불변).
- 회귀가드 `tests/reviewType.test.js` (슬롯 파생·기대 종류·잠금 범위 **실행** + 소비처 4곳 배선), 변이시험 검출 확인.
- **다음(3차)**: 구매확정 판별 예시이미지 등록 화면(`utils/purchaseConfirmChannels.js` + 설정탭 — ★ 캐시 지문 `pc_` 접두 필수) · 리뷰어 단일 첨부 화면의 "구매확정 완료 화면을 올려주세요" 안내문(`search.service` 가 이미 `reviewType` 을 내려준다) · 행 단위(시트 작업옵션 칸) 판정.

### ★★ 리뷰비 입금 자동화 M1 — 입금대상 추출 · 은행별 분류 · 회차(다운로드) 잠금 (migration 086)
- **배경**: 매일 ① 구글시트에서 새로 리뷰 제출한 사람을 눈으로 찾고 ② 계좌를 다건이체 파일에 옮겨 적고 ③ 이체 후 시트 입금칸에 날짜를 적는 수작업. M1이 ①②를 없앤다(③ = M2).
- **확정 정책(사용자 확정, 완화 금지)**: ① 이체금액 = **상품비(결제금액)+리뷰비**(리뷰비는 082 참여시점 판정) ② 이체 단위 = **A안 건별**(시트 행 1개 = 이체 1줄, 합산 금지) ③ 대상 = 제출완료 ∧ 미입금 ∧ **다운로드 이력 없음**(재다운로드도 이력) ④ 입금칸 기록값 = **결과 파일의 실제 이체시점**(M2) ⑤ 이체 실패는 리뷰어에게 **"등록한 계좌 정보를 확인해 주세요"로 알린다**(M2).
- ★★ **이중입금 방지는 DB가 보장한다** — `uq_payment_items_active`(부분유니크, `(sheet_id,tab_name,row_index) WHERE status IN ('pending','paid')`). 코드 실수로도 뚫리지 않는다. 잠금 해제 경로는 **딱 둘** = 회차 취소(cancelled) · 이체 실패 판정(failed, M2). `createBatch`는 23505를 **그 건만 건너뛴다**(전체 실패로 만들면 매번 처음부터 다시 골라야 한다).
- ★★ **은행 자동분류 = 작업오더 물건비**(`work_orders.goods_cost_type`): **현금 → 하나은행 / 계산서·세금·수수료 → 케이뱅크**. 판정은 `payment.service.bankFromGoodsCostType` 단일 출처(프론트 힌트 `_rfTransferHint`가 **같은 정규식 문자열**을 쓰는지 회귀가드가 고정). 공고 `transfer_bank`가 있으면 **사람이 정한 값이 항상 우선**, NULL이면 작업오더를 계속 따라간다(`''` 저장 = 자동으로 되돌리기, start_date와 같은 CASE 센티널).
- ★★ **은행명 매칭은 정확 일치만**(`utils/bankCodes.js`) — 부분일치로 하면 `하나`가 `하나증권`(270)에 걸려 **남의 계좌로 송금**된다. 코드표 72개는 **케이뱅크 공식 대량이체 양식**('대량이체입력가이드' 시트)에서 그대로 옮겼고 하나은행도 같은 표준코드를 쓰므로 **표는 하나만 둔다**. 판정 실패는 추측하지 않고 null = 계좌미비로 화면에 드러남(틀린 값보다 빈 값).
- ★ **케이뱅크 서식 은행명은 코드에서 파생한 정식명**(`bankNameByCode`) — 리뷰어 원문('신한')을 그대로 넣으면 양식 가이드의 "형식에 맞는 은행/증권사명 혹은 코드만" 검증에서 거부될 수 있다. ★ **하나은행 은행코드는 `String`으로** — 숫자로 나가면 `'045'`의 앞 0이 사라진다(실제 운영 파일에 `45`로 찍힌 사례 있음).
- ★★★ **은행마다 파일 형식이 다르다 — 하나은행은 구형 `.xls`(BIFF8·OLE2) (실측 사고 2026-08-19)**: 하나 기업뱅킹 다건이체 업로드가 우리 `.xlsx` 를 **`해당 파일은 엑셀파일이 아닙니다. 다시 올려주십시오.`** 로 거부했다. **엑셀로 열어 다시 저장한 정상 xlsx 도 같은 거부** = 우리 파일이 깨진 게 아니라 **그쪽이 구형 형식만 읽는다**(하나가 돌려주는 이체결과 파일도 실측상 OLE2 `.xls` — 같은 계열). → 하나는 SheetJS(`@e965/xlsx`, 이미 결과 해석에 쓰는 의존성 = 신규 의존 0)로 `bookType:'biff8'` 생성, **케이뱅크는 종전 xlsx 그대로**(되는 것을 바꾸지 않는다). ★ **형식·확장자·MIME 는 `batchFileFormat` 단일 출처** — 내용은 `.xls` 인데 파일명·Content-Type 만 xlsx 로 나가면 은행 화면이 **확장자만 보고 다시 거부**한다(라우트가 하드코딩하지 않는 것을 회귀가드가 고정). ★ 문자열/숫자 구분과 텍스트 서식(`@`) 규칙은 두 형식이 **같다**(계좌 앞 0 보존은 BIFF8 왕복에서도 확인). ★ 되돌리기 = env `PAYMENT_HANA_XLS=0`. ⚠ **회귀가드의 되읽기도 형식을 따라가야 한다** — 종전 가드는 exceljs 로만 읽어 하나가 xls 로 바뀌면 그 자리에서 터진다(형식 인지 `readBack(bank, buf)` 로 갱신).
- ★★ **서식의 계좌·은행코드는 문자열 셀 + 텍스트 서식(`@`)** — 숫자로 해석되면 `0123…`의 **앞 0이 날아가거나** 긴 계좌가 지수표기(`1.23E+12`)가 되어 은행 업로드가 통째로 거부된다(담당자가 매번 `'0123` 처럼 손으로 고치던 지점). 금액만 숫자 그대로(은행 양식이 수치를 요구). ★ 계좌는 `buildWorkbook`에서도 **`normalizeAccount` 단일 출처**로 한 번 더 정규화한다 — 라이브 경로(`listPaymentTargets`)는 이미 정규화하지만 옛 원장 스냅샷에 `-`가 섞여 있으면 그대로 서식으로 나간다(정규화 사본 금지 — 회귀가드가 호출 2회·사본 부재를 고정).
- ⚠ **"버퍼가 비지 않고 `PK`로 시작"만 보는 가드는 이 회귀를 통과시킨다**(실측) — 계좌를 숫자 셀로 바꿔도 초록이었다. 지금은 **실제 xlsx를 만들어 되읽어** 값·타입(`string`)·`numFmt='@'`·하이픈 제거·금액 숫자를 대조한다(`paymentBatch` §8, DB 불필요 · 변이시험 검출 확인).
- ★ **계좌 보완 팝업(입금관리)의 입력칸에 "숫자만 입력" placeholder를 두지 않는다** — 서버가 `-`를 자동으로 빼므로 사람이 손으로 지울 이유가 없다. 대신 "`-` 는 자동으로 빠지고 앞자리 `0` 은 유지됩니다"를 안내로 말한다(`paymentIssueFix` 6i-2 가 고정).
- ★ **금액·통장표시·계좌는 회차 생성 시점 값을 박제**(`payment_batch_items`) — 화면에서 다시 계산하지 않는다. 리뷰비 규칙·통장표시를 나중에 바꿔도 과거 입금 표기가 흔들리지 않는다(기간별 리뷰비 사고와 같은 함정).
- ★ **쓰기 표면은 `payment_batches`/`payment_batch_items` 두 테이블뿐** — review_index·order_submissions·recruit_campaigns·**시트는 읽기만**. M1에 시트 API·큐 호출 0(회귀가드가 고정, exceljs `wb.worksheets`는 오탐이라 구글 API 패턴만 검사).
- ★ **미입금 판정은 `search.service.PAYMENT_COL_KEYWORDS` 재사용**(SQL판), **리뷰비는 `utils/campaignFee.resolveReviewFee`** — 사본을 만들면 화면과 파일 금액이 갈라진다.
- ★ **FK 타입**: `payment_batch_items.campaign_id`는 **TEXT**(`recruit_campaigns.id`가 TEXT). 082의 42804 사고(UUID로 선언 → 파일 전체 롤백) 재발 방지로 회귀가드가 018 원본과 대조.
- ★ **프리플라이트 등록**: `recruit_campaigns.transfer_bank`/`transfer_memo`는 공고 create/update의 INSERT·SET 목록에 들어가므로 컬럼이 없으면 **공고 발행·수정이 전면 42703**. `REQUIRED_SCHEMA`에 등록(083/085와 같은 규율).
- **화면**: 리뷰웹시스템[3버전] **[입금관리] 탭**(`/api/trackb/payment/*`, **adminOrMaster 전용** — 계좌번호 전체 노출이라 AE·광고주 차단, 등록리뷰어DB와 같은 판단). 은행별 다운로드 버튼(그 은행 건만) · 보류 사유 표기 · 통장표시 미설정 경고 · 회차 표(다시 받기/취소). 다운로드·취소는 confirm + 잠금/이중입금 안내 필수. 공고 모달에 **[이체은행] 3버튼(자동/하나/케이뱅크) + [통장표시](8자)** 추가.
- **문서**: PRD `frontend/docs/prd-payment-transfer.html` · 리뷰어 화면 시안 `design-payment-reviewer-view.html` · 직원 안내서 `입금관리_사용안내.html`.
- ★★ **`order_submissions` 의 제출 시각 컬럼은 `submitted_at` 이다(`created_at` 아님, 001:179)** — 배포 사고: 입금대상 조회가 42703 으로 죽어 [입금관리]가 전면 "서버오류"였다. **같은 오타가 `reviewer.routes` review-earnings 에도 있었고**(복사 원본), 그쪽은 fail-soft catch 에 삼켜져 **리뷰어 카드 상품비·누적 금액이 조용히 비어 있었다** — 함께 고쳤다.
- ★★ **빈 DB 로 서비스를 호출하는 테스트는 조인 쿼리를 한 줄도 실행하지 않는다** — `listPaymentTargets` 는 대상 0건이면 조기 반환이라 `_loadCampaigns`/`_loadOrderPrices`/`_loadAccounts`/`_loadTabLabels` 4개 쿼리가 안 돌고 컬럼명 오타가 통과한다. 회귀가드는 **대상 행을 실제로 만들어** 후속 조인까지 태우고, `information_schema` 로 컬럼 존재까지 대조한다(변이시험으로 검출 확인).
- 회귀가드 `tests/paymentBatch.test.js`(순수함수 실행 + 라우터 스택 실검사 + 배선 + **PGTEST_URL 시 진짜 PG로 부분유니크·CHECK·서식 생성·조인 실행·컬럼 존재 검증**). ⚠ `PGTEST_URL`은 **파일 최상단에서 `DATABASE_URL`로 옮겨야** 한다(pool은 require 시점에 읽는다 — 뒤늦게 설정하면 연결이 안 잡힌다).
### ★★ 리뷰비 입금 자동화 M2 — 이체결과 파일 반영 (migration 100)
- **흐름**: 은행 결과 파일 업로드 → 해석 → 회차 건과 짝 맞추기 → **확인 화면** → [이대로 반영] → 성공 건만 입금 기록(시스템 + 무시트 작업표 칸 / 시트 입금칸) + 실패 건 리뷰어 안내. 사용자 확정 4건 = 결과 파일 실물 제공 · **형식 3종(.xlsx/.xls/CSV) 모두 수용** · 같은 계좌·금액 다건은 **순서대로 배정**(+화면 고지) · 실패 안내 = **1:1문의 + 화면 표시**.
- ★★ **파일 형식은 확장자를 믿지 않는다**(`utils/spreadsheetLoad.js`): 실측 결과 하나은행 = **진짜 구형 .xls(OLE2)** · 케이뱅크 = **확장자만 .xls, 내용은 .xlsx**. 매직 바이트로 판정하고 CSV 는 UTF-8 실패 시 CP949 로 다시 읽는다(인코딩을 잘못 고르면 열 이름이 깨져 "은행 파일이 아니다"로 오판). `exceljs` 는 .xlsx 만 읽어 **SheetJS(`@e965/xlsx` — 0.18.5 의 CVE 수정 미러)** 를 추가했다.
- ★★ **해석 단일 출처 = `utils/paymentResultParse.js`**(`BANK_SPECS` 은행별 지문·열 정규식 · `parseResultAoa` · `matchResults`, 순수함수): 헤더 줄 위치를 **하드코딩하지 않고** 앞 30줄에서 지문으로 찾는다(하나 파일은 상단 메타 2줄, 케이뱅크는 선행 빈 열이 있다). 성공 판정은 **화이트리스트 정확일치**(하나 `처리완료` / 케이뱅크 `이체완료`) — 그 외는 전부 실패로 본다(모르는 문구를 성공으로 접으면 안 나간 돈이 나간 것으로 기록된다). 은행 불일치 파일은 명시적으로 거절, 필수 열 누락은 한국어로 무엇이 없는지 말한다.
- ★ **계좌 비교는 숫자만**(`accountDigits` — `iM뱅크(대구) 123-45` 처럼 은행명 접두가 붙어 오므로 공백 뒤 꼬리를 우선), 이체시각은 **KST 문자열 조립**(`Date` 산술 금지 — 서버 TZ 무관).
- ★★ **짝짓기 키 = (계좌 숫자, 금액)**. 같은 키가 여러 건이면 **파일 순서대로 배정**하고 그 사실을 화면이 고지한다 — 실측상 그 그룹은 **같은 사람·같은 작업**(예금주·통장표시까지 동일)이라 입금 결과는 같고 **기록상 어느 줄에 붙었는지만 임의**다. 리뷰어 손해는 없다.
- ★★★ **처리 대상은 `pending` 뿐 = 멱등·이중입금 0**: 이미 `paid` 인 건은 어떤 파일을 다시 올려도 건드리지 않고, 두 UPDATE 모두 `WHERE id=$1 AND status='pending'` 조건을 건다(스텁 DB 는 SQL 을 해석하지 않으므로 **회귀가드가 그 조건문을 문장으로 고정** — 변이시험이 잡은 구멍).
- ★★★ **결과 파일에 없는 건은 `pending` 그대로 둔다(실패로 내리지 않는다)** — 실패로 내리면 부분유니크에서 빠져 **다음 회차에 다시 담기는데**, 실제로는 이체됐고 파일만 일부일 수 있어 **이중입금**이 된다. 화면이 "대기 유지"를 말하고, 전체 결과 파일을 다시 받아 **같은 회차에 한 번 더 올리면** 그때 기록된다(재반영 경로가 열려 있다 — pending-only 규칙이 그것을 안전하게 만든다).
- ★ **은행이 실패로 답한 건만 `failed`** → 잠금이 풀려 다음 회차에 자동 재이체(086 이 적어 둔 "잠금 해제 경로 딱 둘" 중 하나).
- ★★ **미리보기는 쓰기 0 · 반영은 서버가 파일을 다시 해석·재매칭**(화면이 보낸 결과 목록 불신) + `confirm !== true` 면 400(업로드만으로 입금 기록이 되면 되돌릴 수 없다). 회차 행 `FOR UPDATE` 로 동시 업로드 경합 차단.
- ★★ **입금 기록 단일 출처 = `paymentApply.service`**(`recordDeposits` + `markDepositCells`) — 기존 수동 처리 `/api/payment/mark-done` 의 본문을 그대로 추출해 **M2 와 같은 함수를 쓴다**. 사본을 두면 "수동 처리는 무시트 작업표 칸에 남는데 자동 반영은 안 남는" 드리프트가 조용히 생긴다(무시트 탭에서는 다음 장부 재생성에 `review_index` 가 지워져 W3-a 작업표 칸이 유일한 생존 경로다). ★ **시점(stamp)은 호출자가 준다** — 수동은 '지금', M2 는 **결과 파일의 실제 이체시각**(사용자 확정 ④). `paid_at` 미지정이면 DEFAULT NOW(종전 동작 보존).
- **화면**(입금관리 회차 줄 [⬆ 결과 업로드]): 미리보기 → **확인 팝업**(입금완료/이체실패/결과없음/이미 입금 4분류 + 순서배정·결과없음·회차 밖 결과 줄 고지) → [이대로 반영]. ★ 팝업은 **body 직속** · 바깥 클릭으로 닫지 않고 Esc·[취소]만 · 리스너는 최상위 1회 · onclick 은 **인덱스만**. ★ **계좌는 뒤 4자리만** 내려온다(확인 화면에 전체 계좌를 늘리지 않는다).
- **실패 안내**: 확인 팝업의 [계좌 확인 안내 보내기] 체크(**기본 켬** — 검수 반려 팝업과 같은 규율) → `csBridge.postAdminNotice`(=`postInspectionReject` 실행부에 **이름만 하나 더 연 것**, 사본 0)로 1:1문의 전송 + `notified_at` 기록(재반영 시 도배 방지). ★ **문구는 `FAIL_NOTICE` 한 곳**이고 **은행 응답 원문을 리뷰어에게 보내지 않는다**(내부 표기라 오히려 헷갈린다). 전송 결과(`notified/notifyTargets`)를 토스트가 말한다(조용한 미전송 금지).
- **리뷰어 화면**: 참여상품 정보 팝업에 입금 결과 블록 — 완료(시각·입금자명·금액) / 실패(계좌 확인 + [내 계좌 정보 수정하기] → 내정보 이동). ★★ 재료는 기존 `participation-brief` 에 `payment` 로 동봉(**신규 엔드포인트 0**, 그 라우트가 이미 행 소유권 강한-키 게이트를 통과시킨다) — **계좌번호·은행 응답 원문·관리자 실명은 싣지 않는다**(080 규율).
- ★ migration 100 = **컬럼 추가·신규 테이블만**(`payment_batches.applied_at/by` · `payment_batch_items.result_status/result_seq/notified_at` · 감사 이력 `payment_result_uploads`, 백필 0). `REQUIRED_SCHEMA` 에 `payment_batch_items.result_status`·`payment_batches.applied_at` 등록(없으면 반영이 통째로 42703).
- ★ **픽스처는 실측 서식 그대로**(`tests/fixtures/paymentResultFixtures.js` — 마스킹만, 메타 줄·선행 빈 열·중복 그룹·`iM뱅크(대구)`·`KEB하나` 보존). 실패 갈래는 테스트가 **계좌+금액이 유일한 줄 하나만** '오류'로 바꾼 사본으로 만든다(픽스처 자체는 안 건드린다 — 실제 운영 파일은 195/195·207/207 전건 성공이었다).
- 회귀가드 `tests/paymentResultParse.test.js`(3형식 × 2은행 실제 바이너리 생성·거짓 확장자·헤더 이동·화이트리스트·중복 3그룹 순서배정) + `tests/paymentResultApply.test.js`(스텁 pool 로 미리보기·반영 **실제 실행**, **변이시험 검출 확인**). 직원 안내서 = `frontend/docs/입금관리_사용안내.html` §06.
- **잔여(문서화)**: ① 결과 파일에 이체시각이 없는 성공 건은 `NOW` 로 기록된다(파서가 경고로 드러낸다) ② 회차 전체를 되돌리는 창구는 없다(입금 기록은 되돌릴 수 없다는 전제 — 잘못 반영하면 DB) ③ 순서 배정된 그룹의 `result_seq` 는 남지만 화면에 표는 없다.

### ★★ 보류 보완에서 **리뷰비**도 정한다 (migration 128, 사용자 확정 2026-08-19)
- **배경**: 리뷰비는 `recruit_campaigns.review_fee`(+082 구간표) 한 곳에만 있어 **연결 공고가 없는 작업**(옛 작업·외부모집·수기 진행)은 넣을 칸이 아예 없었다 → 입금관리가 `no_review_fee` 경고로만 흘려보내고 **상품비만 이체**됐다. 보완 팝업에 통장표시와 **같은 자리**에서 리뷰비를 넣는다.
- ★★ **판정은 여전히 `utils/campaignFee.resolveReviewFee` 단일 출처** — 바뀐 것은 **폴백 순서**뿐이다: **스냅샷 → 구간표 → 공고 `review_fee` → 탭 `review_fee`(128)**. **스냅샷·구간표 우선은 완화 금지**(082) — 팝업이 `feeSource` 를 읽어 "이미 참여한 건은 안 바뀐다 / 구간이 우선한다"를 **문장으로 말한다**(저장했는데 안 바뀌는 조용한 no-op 금지).
- ★★ **탭 값은 NULL = 미설정**(0 = 무상 지정과 구분) — `|| 0` 으로 접으면 공고 폴백과 구분이 사라져 출처 표시가 거짓이 된다. 빈 값 저장 = 미설정으로 되돌림, **미전송 = 변경 없음**(리뷰비 칸 없는 화면이 저장해도 조용히 지워지지 않는다 — 이체설정과 같은 계약).
- ★ **저장처는 서버가 정한다**(공고 있으면 공고 `review_fee`, 없으면 탭) · 공고 UPDATE 는 **이체 2칸 + 리뷰비 3칸만**(축약 폼 클로버 금지 — 회귀가드가 SET 목록을 센다) · 숫자 아님·음수·과대는 **쓰기 0건으로 거부**(추측 저장 금지).
- ★ `REQUIRED_SCHEMA` 에 `tab_configs.review_fee` 등록 — `_loadTabMeta` 는 fail-soft 라 컬럼이 없으면 **이체은행·통장표시까지 함께 조용히 미설정**으로 보인다.
- 회귀가드 `tests/paymentIssueFix.test.js`(§3 폴백·스냅샷 우선·SELECT 실검사 / §4 저장 계약 / §6·§7 팝업 배선·문구 — 변이시험 검출 확인).
- ★★ **리뷰비 0원은 경고가 아니다 (사용자 확정 2026-08-19)**: 이 계정은 **상품비만 주는 작업이 다수**(실측 공고 32건 중 27건이 `review_fee=0`)라, 0 을 "미설정"으로 읽으면 입금관리가 상시 `no_review_fee` 로 뒤덮여 **진짜 신호(계좌·은행 미비)가 묻힌다**. → 경고는 **금액이 0이라서**가 아니라 **근거를 못 찾아서**(`!fee && !feeSource`) 띄운다.
- ★★ 종전 버그는 2겹이었다: 공고 로더가 `COALESCE(c.review_fee,0)` + `|| 0` 으로 **0 과 NULL 을 같은 값**으로 만들고, `campFee` 가 truthiness 로 0 을 걸러 **무상 작업 전건이 경고**를 달았다(위프 800건 24/24 · `campaignReviewFee` 가 0 인데도 null 로 나가 보완 팝업 프리필까지 비어 보였다) — 082 의 `_int()` 규율이 **거울상으로** 재발한 자리다. ★ **공고는 있는데 `review_fee` 가 NULL 이면 여전히 미설정**(0 으로 접지 않는다 — 회귀가드가 두 경우를 따로 고정). ⚠ **한계**: 발행 폼에서 리뷰비 칸을 비우면 서버가 0 으로 저장하므로 "0원으로 정함"과 "안 넣음"은 DB 에서 구분되지 않는다 — 사용자 확정으로 **0 = 무상**으로 읽는다. 회귀가드 `tests/paymentIssueFix.test.js` §3c6~3c8(변이시험 5종 검출 확인).
- ★★ **보완 창구의 바로가기는 [📋 작업보드] 하나 — 구글시트 버튼은 제거했다(사용자 확정 2026-08-19)**: 탈시트 이후 그 작업의 진실원본은 시스템 표라, 시트를 열어 두면 거기서 고친 값이 **시스템에 반영되지 않아** 사고가 난다(옛 "참고용 시트" 경고 팝업도 함께 제거). 이동은 관측 뷰·업체관리와 **같은 `pendingTab` 계약**(사본 금지)이고 **팝업을 먼저 닫는다**(오버레이가 작업보드를 가린다). 재료 `tabGid` 는 **서버가 실어 준다**(화면이 gid 를 추측하면 리네임 시 엉뚱한 탭이 열린다). 회귀가드가 `docs.google.com`·옛 함수(`_pmSheetBtn`·`_pmOpenSheet`) **부재를 고정**한다(창구가 둘이 되면 다시 시트에서 고치게 된다).

### ★★ 입금관리 보류 사유 보완 — 사유를 그 자리에서 고친다 (마이그레이션 0, 사용자 확정 2026-08-10)
- **배경(실측)**: 입금대상 405건이 보류인데 화면은 **사유만 말하고 고칠 곳은 다른 화면**이라 아무도 안 치웠다. 사용자 확정 4건 = ① 범위 = 이체은행·통장표시 + 리뷰어 계좌 + 결제금액 ② 결제금액은 **시트 결제금액 칸에서 자동 보완** ③ 화면 = **상단 묶음 목록 + 행별 [보완] 둘 다** ④ 연결 공고가 없으면 **탭 설정에 저장**.
- ★★ **원인 1 = 판정이 공고만 봤다**: `tab_configs.transfer_bank`·`deposit_name` 은 **001 스키마부터 있고 관리자 대시보드 탭설정 팝오버가 예전부터 채워 온 칸**인데 입금관리 M1(086)이 `recruit_campaigns` 만 봐서, **공고 없는 옛 작업이 전부 '이체은행 미지정'으로 잠겨** 있었다 → 판정 순서를 **공고 → 탭 → 작업오더 물건비 자동**으로 넓혔다(`bankSource` 로 출처를 화면에 드러낸다). **마이그레이션 0**(기존 컬럼 재사용).
  - ★★ **표기가 저장소마다 다르다** — 공고는 코드값(`kbank`/`hana`), **탭은 한글 라벨**(`케이뱅크`/`하나은행`). `index-app.js` 가 `t.transferBank === '케이뱅크'` 로 **문자열을 그대로 비교해 배지를 그리므로** 탭에 되쓸 때는 반드시 한글 라벨(`tabBankLabel`) — 코드값으로 통일하면 남의 화면 배지가 조용히 죽는다.
  - ★★ **해석은 `normalizeBankChoice` 단일 출처이고 정확일치만** — 부분일치로 완화하면 `하나`→`하나증권`·`케이뱅크`→`케이뱅크증권` 이 걸려 **남의 계좌로 송금**된다. 모르는 값은 **null**(추측 금지). ⚠ 회귀가드는 **두 은행 양쪽**을 검사할 것 — 한쪽만 보면 반대쪽 완화를 통과시킨다(변이시험 실측).
- ★★ **원인 2 = 주문 원장 없는 행의 상품비 0원**: 옛 작업·직원 수기 입력 행은 `order_submissions` 가 없어 `no_price`+`zero_amount` 로 영구 보류였다 → **시트 결제금액 칸 폴백**(`priceSource:'order'|'sheet'`, 화면에 `시트` 배지). ★ 판정은 **`utils/paymentAmount.js` 단일 출처**로 이관하고 레거시 `GET /api/payment/targets`(GAS 이관분)의 사본 `_extractAmount` 를 제거 — 사본을 두면 "레거시 화면은 22,000원인데 입금관리는 0원"으로 갈린다. ★ **정확일치(`결제금액`) → 부분일치(`금액` 포함)** 순서(뒤집으면 `옵션금액`·`소계금액` 이 이긴다), **마이너스는 0**(시트의 `-` 표기가 음수가 되면 합계가 조용히 줄어 입금 사고). ★ `row_json` 을 통째로 SELECT 하지 않는다 — SQL 은 `jsonb_object_agg` 로 '금액' 후보 칸만 남기고(상위집합) **최종 판정은 JS 함수**가 한다(SQL 에 판정 사본 금지).
- **보완 저장 = `POST /api/trackb/payment/transfer-setting` · `/reviewer-account`(둘 다 adminOrMaster)**. **쓰기 표면 = 공고 이체설정 2칸 · 탭 이체설정 2칸 · 리뷰어 계좌뿐**(주문 원장·회차·검색인덱스·시트 무접촉).
  - ★ **저장처는 서버가 정한다**(화면이 고른 값 불신) — 공고가 있으면 공고(판정에서 공고가 이기므로 탭에 쓰면 화면이 안 바뀐다), 없으면 탭. **그 탭에 연결된 공고인지 재검증**(`campaign_mismatch`) — 낡은 화면이 남의 공고 이체설정을 덮는 것 차단(계약 오링크와 같은 규율).
  - ★★ **리뷰어는 `reviewers.id` 로 지목** — `phone8` 은 GENERATED·비유니크라 키로 쓰면 **같은 뒤8자리 타인 계좌를 덮는다**(외부모집 수동제출에서 이미 밟은 함정). 타계정은 `sub_accounts` 의 **그 명의 항목만** 병합(소유자 공통계좌 보존). 빈 칸은 덮지 않는다(부분 보완).
  - ★ **인식 못 하는 은행명은 저장 시점에 막는다**(`resolveBank` 미해석 = 거부) — 저장은 됐는데 다음 회차에도 '은행명 인식불가'로 남는 **막다른 길** 방지.
  - ★ 검증 실패는 **400대**로 매핑(`_PAY_FIX_STATUS`) — errorHandler 500 마스킹이면 담당자가 무엇을 고칠지 모른다. 42P01/42703 은 `not_ready`.
- **화면**(`workdesk.html`): 상단 **"보완하면 풀리는 보류"** 묶음 + 표 행별 **[보완]**. ★ 묶음은 **서버 issues/warnings 를 그대로 쓰고 "고치는 곳"으로 묶기만** 한다(판정 사본 0) — 그래서 이체은행 하나 저장에 그 작업 N건이 함께 풀린다. ★ **고칠 수 없는 사유**(리뷰어 미등록 · 시트에도 금액 없음)는 **버튼 대신 조치 문장**(죽은 버튼 금지). ★ 팝업은 **body 직속** · **바깥클릭으로 안 닫힘**(입력해 둔 계좌·통장표시 보호) · Esc/[취소]만 · Esc 리스너는 닫을 때 해제.
  - ★★ **묶는 단위는 작업(탭) 하나 — 사유별로 흩어 놓지 않는다**(사용자 요청 2026-08-10): 종전엔 같은 작업이 `🏦 이체은행`·`🏷 통장표시`·`💳 계좌` 카드 3곳에 나뉘어 몇 번을 오갔다. 지금은 **카드 1개 = 작업 1개**이고 그 안에 이체은행 · 통장표시 · 계좌(리뷰어별) · 리뷰어 미등록 · 결제금액 없음이 줄로 들어간다. 카드는 건수 많은 순, `_PM_FIX_CARD_CAP`(8) 초과분은 **"외 N개 작업"으로 고지**(조용히 자르지 않는다).
  - ★★ **`accts` 는 여전히 평면 배열 하나** — 한 리뷰어가 여러 작업에 걸치므로 원장은 하나로 두고 작업 카드는 **인덱스를 참조만** 한다. 카드 안 순번을 넘기면 **남의 계좌 창이 열린다**(변이시험이 잡았다 — 회귀가드가 "버튼 인덱스가 가리키는 리뷰어 = 그 줄에 적힌 이름"을 실행으로 고정).
  - ★★★ **카드·팝업이 가리키는 것은 "리뷰어"가 아니라 계좌 명의다 (실사고 2026-08-10 · 소유자 A / 타계정 명의 A′)**: 한 소유자가 **본인 명의 1건 + 타계정 명의 1건**으로 참여하면 두 항목이 **둘 다 "A"**(소유자 이름) 로 보여 어느 계좌를 보완하라는 건지 알 수 없었다(본인은 A은행, 타계정 A′는 B은행으로 **서로 다른 계좌**). 항목 자체는 `reviewerId||subPhone8` 로 이미 갈려 있었고 **표기만 구분을 못 했다**.
    - ★★ **명의 이름은 계좌 원장 값**(`accountName` = 본계정 `reviewers.name` / 타계정 `sub_accounts[].name`) — **시트 이름 칸(`reviewerName`)은 명의로 쓸 수 없다**(탭마다 주문자/수취인 중 무엇이 잡혔는지 달라 타계정 줄이 소유자 이름으로 뜬다). `accountOwner`(소유자 이름)를 함께 실어 "**A의** 타계정"을 말한다.
    - ★★ **표기 단일 출처 = `_pmAcctLabel`(HTML) / `_pmAcctPlain`(평문)** — 카드 줄·팝업 제목·형제 목록·title 이 같은 함수를 쓴다(사본을 두면 "카드는 A인데 팝업은 A′"). **연락처 뒤 4자리**가 이름이 같을 때(동명이인·본인/타계정) 구분되는 **유일한 단서**라 라벨에 상시 포함.
    - ★ 팝업 맨 위 **명의 확인 블록**(`.pmwho`)이 `A′ 명의 계좌 ·0101` + "A 님이 등록한 타계정 — 본인 명의 계좌와 별도로 저장됩니다"를 못박고, **같은 소유자의 다른 명의를 목록으로 보여주며 바로 건너뛴다**(`_pmFixAcct(idx)` — onclick 은 인덱스만). 부제에 그 명의가 걸린 **작업·건수**를 적어 "어느 건인지" 확인할 근거를 준다.
    - ★ 명의가 하나뿐이면 형제 안내를 **띄우지 않는다**(없는 혼동을 만들지 않는다). 회귀가드 `paymentIssueFix` §7C(실사고를 vm 으로 재현, **변이시험 검출 확인**).
  - ★★ **작업별 [📄 시트] 바로가기**(카드 머리 + 작업 보완 팝업): 아직 구글시트를 직접 봐야 하는 값(결제금액 칸 등)이 있어 그 탭을 `#gid=` 까지 바로 연다. ★ 링크 조립은 `payment.service.tabSheetUrl` 한 곳이고 `_loadTabMeta` SELECT 에 `tab_gid`·`sheetless` 를 합류시켜 **쿼리 순증 0**. ★ 프론트는 **서버가 준 `sheetUrl` 만** 쓰고(sheetId 로 재조립 금지) `docs.google.com` 접두 검증 후 `window.open(…,'_blank','noopener')`, **onclick 에는 인덱스만**(URL 을 화면에 심지 않는다).
  - ★★ **이관된 작업(sheetless)도 연다 — 막는 것은 "시트가 아예 없는 경우"뿐 (사용자 확정 2026-08-10)**: 초도 보완에는 아직 시트를 봐야 하는 값이 있다. **"시스템 표가 진실원본"은 링크를 막는 근거가 아니라 경고문의 근거** → 클릭하면 팝업이 사용자 확정 문구(`탈구글시트를 진행했습니다 / 구글시트에 입력한 내용은 시스템에 반영되지 않으므로 / 시트를 편집하지 마시고 참고용으로만 사용하세요`)를 먼저 보이고 **[확인하고 시트로 이동하기]** 로만 이동한다(버튼에도 `참고` 표기). 일반 작업은 종전대로 곧바로 열린다(불필요한 마찰 금지).
  - ★★ **빈 링크는 "시스템이 만든 무시트 작업"(가상 시트ID)뿐** — 구글 URL 로 조립하면 **죽은 링크**가 된다(반영 점검 `_gidOut` 과 같은 규율: 빈 링크 > 죽은 링크). 판정은 **`sheetlessAccept.isVirtualSheetId` 단일 출처**(접두 사본 금지 — 이 접두는 스코프 게이트가 아니라 **표시용 판정**이라 여기 쓰는 것이 맞다). ★ **"시트가 없음"과 "링크 미상(미등록·조회 실패)"을 구분해** 비활성 버튼 tooltip 으로 사유를 말한다(뭉뚱그리면 담당자가 원인을 엉뚱한 데서 찾는다).
  - ★ **`window.open` 은 onOk 동기 구간에서** — `onOk` 를 async 로 만들거나 `await` 뒤에서 열면 **브라우저가 팝업을 막는다**(회귀가드가 형태를 고정).
  - ★★ **저장 성공 뒤 순서 = 닫기 → 안내 → 재조회**(`_pmAfterFix` 한 벌): 후처리(toast·재조회)가 실패해도 팝업이 남으면 **성공이 실패처럼 보인다**(브라우저 실측으로 잡았다). `onOk` 예외도 삼키지 않고 사유를 말한다.
  - ★★ **표 `onclick` 은 인덱스만** — 종전 `_pmToggle('${esc(k).replace(/'/g,"&#39;")}')` 은 **HTML 속성이 엔티티 디코드 후 JS 로 파싱**되므로 `&#39;` 가 `'` 로 되살아나 시트발 탭명으로 문자열 탈출이 가능했다(기존 XSS 경로 — 이번에 제거).
- ⚠ **운영 함의**: 탭 설정에 이미 들어 있던 이체은행 값이 **이제 판정에 쓰인다**(확실히 해석되는 값만). 배포 후 화면의 출처 표기(`자동`/`수동`)로 한 번 훑을 것.
- 회귀가드 `tests/paymentIssueFix.test.js`(순수함수 실행 · 스텁 pool 로 서비스/라우트 **실제 실행** · 묶음 판정·시트 버튼 **vm 실행** · 라우터 스택 실검사 · XSS · 팝업 계약, **변이시험 12+15종 검출 확인**) + 브라우저 실행검증(카드 구성·버튼 4상태·클릭 noopener·경고 팝업 문구·팝업 저장). ⚠ **스텁은 SQL 을 해석하지 않는다** — `_loadTabMeta` 가 `sheetless`·`tab_gid` 를 실제로 **읽는지**는 SELECT 문자열을 따로 고정한다(변이시험이 이 구멍을 잡았다).
- ⚠ **곁다리로 고친 빨간 가드**: `paymentBatch` 의 "리뷰어앱 스코프 토큰은 이체 설정을 못 바꾼다"(권한 상승 검사)가 **배포 전부터 실패 중**이었다 — `_scopedCampaignEdit` 를 **고정 길이(i+3000)로 잘라** 함수가 자라자 UPDATE 뒷부분이 잘려 정규식이 못 맞췄다(실제 보안 속성은 살아 있었다). **함수 끝까지 보도록** 고쳤다. ★ **고정 길이 슬라이스로 함수 본문을 자르지 말 것** — 그 함수가 커지는 순간 가드가 조용히 빨개지고, 빨간 가드는 새 변경도 못 지킨다. ★ 쓰기 표면 가드는 **M1 본체 / 보완 경로 두 구역으로 분리** — 합쳐 검사하면 M1 이 조용히 넓어져도 통과한다.

### ★★ 타계정 참여 건의 입금대상 통과 — 소유자 계좌 하드링크 폴백 (2026-08-19 · 소유자 A / 타계정 명의 A′, 스키마 변경 0)
- **사고**: 타계정 명의로 참여한 건이 입금대상에서 **`리뷰어 정보 없음`(no_reviewer)** 으로 영구 보류됐다. 원인은 이름·예금주 비교가 **아니다**(그런 규칙은 애초에 없다) — **타계정 매칭 키가 `sub_accounts[].phone` 하나뿐**이라, 소유자가 타계정을 **이름만 등록**했거나 번호를 다르게 적어 두면 소유자 계좌(A)가 멀쩡히 있어도 그 행을 지목할 길이 없었다.
- **해법 = 연락처로 못 찾은 행만** `_loadOwnerAccountsByRow` 로 한 번 더 찾는다(정상 건은 폴백 쿼리가 아예 안 나간다). 근거는 그 행에 이미 박제된 **하드 링크 둘**뿐: ① **`campaign_applications.owner_phone8`**(주문 id 로 그 행에 결속 — 홀드 생성 시 서버가 명의 검증을 거쳐 기록) ② **`participation_links.phone8`**(= 로그인 phone8). ★ **① 이 ② 를 이긴다**(원장이 더 강한 근거).
- ★★ **이름으로 소유자를 추측하지 않는다(완화 금지)** — 동명이인 한 번이면 남의 계좌로 송금이다. ② 는 명의 대조가 없으므로 **소유자의 `sub_accounts` 에 그 행 이름이 정확히 등록돼 있을 때만** 인정한다(재배정된 행의 stale 링크 차단 — 검색 게이트의 stale-pl 규율과 같다). ★ `reviewers.phone8` 은 GENERATED·비유니크라 **후보가 유일할 때만** 채택, 같은 이름이 타계정 목록에 둘이어도 미채택(모호 = 계속 보류 = fail-closed).
- ★ **이름 정규화는 `identity.service.normName` 재사용**(사본 금지 — 신원 판정과 갈리면 안 된다). ★ **조회 실패는 throw 하지 않는다**(폴백이 죽어도 입금대상 목록은 종전대로).
- ★ **미등록 명의를 `subPhone8` 로 지목하지 않는다** — 그러면 계좌 보완 저장이 `sub_not_found` 로 죽는 막다른 길이 된다(등록된 명의일 때만 타계정, 아니면 소유자 본계좌를 가리킨다). ★ 응답 `accountSource`(`self`/`sub`/`owner_order`/`owner_link`)를 화면이 **타계정 배지 툴팁으로 밝힌다** — "왜 이 사람 계좌로 나가지?"를 확인할 근거가 없으면 안 된다.
- ★★★ **계좌를 읽는 곳이 등록리뷰어DB 하나뿐이던 것이 더 큰 구멍이었다 (2026-08-19 · 리뷰어 A)** — 작업보드 표에는 은행·계좌번호·예금주가 멀쩡히 보이는데 입금관리만 `리뷰어 정보 없음`이었다. 그 값의 출처는 **`order_submissions.bank/account/depositor`**(035 — 그 건의 구매양식으로 리뷰어가 직접 적어 낸 계좌)인데 입금대상 추출이 이 칸을 **아예 안 봤다**. → 해석 순서 = **① 등록 계좌(연락처) → ② 소유자 링크 → ③ 그 건의 제출 계좌**. ★ **등록 계좌가 언제나 이긴다**(관리 원장이 진실원본 = 기존 동작 보존) ★ 재료는 `_loadOrderPrices` 가 이미 읽던 행에 컬럼만 더한 것이라 **쿼리 순증 0** ★ **은행·계좌·예금주 셋이 다 있을 때만** 인정(반쪽 값 이체 파일은 은행이 통째로 거부) ★ `reviewerId` 를 만들지 않는다(지목할 리뷰어가 없다 = 계좌 보완 팝업 대상 아님·회차 스냅샷 가드 밖) ★ 화면은 `양식계좌` 배지로 출처를 밝힌다.
- ★ **회차 스냅샷의 `account_source` 는 `subPhone8` 이 있을 때만 `'sub'`** — 없는 명의를 sub 로 박제하면 다음 대조(`reconcileAccountSnapshots`)가 그 명의를 못 찾아 **정상 건이 mismatch 경고**로 뜬다(값은 소유자 본계좌이므로 `'self'` 가 맞다).
- ⚠ 남은 한계: 등록 계좌·소유자 링크·제출 계좌가 **전부 없는** 건(옛 수기 입력분)은 여전히 `리뷰어 정보 없음` — 등록리뷰어DB에서 그 번호를 등록하면 풀린다.
- 회귀가드 `tests/paymentSubAccountOwner.test.js`(스텁 pool 로 `listPaymentTargets` **실제 실행**: 두 폴백 경로·우선순위·모호 미채택·불필요 쿼리 0·fail-soft·제출 계좌 폴백·해석 순서·반쪽 값 거부, **변이시험 검출 확인**).

### ★★ 은행명 인라인 관리 (입금관리 서브탭 · 마이그레이션 0)
- **발단**: 계좌 보완 팝업의 `은행명을 인식하지 못했습니다`(이체 서식은 **은행코드**로 나가므로 정식 명칭이어야 한다)를 **담당자가 그 자리에서 못 고쳤다** — `utils/bankCodes.js` 의 `BANKS`·`ALIASES` 가 코드에 박혀 있어 표기 하나 추가에 배포가 필요했다. 시안 = `frontend/docs/design-bank-name-admin.html`(비개발자용 · 외부 리소스 0 · 가짜 데이터로 실제 동작).
- ★★ **판정 단일 출처는 여전히 `utils/bankCodes.resolveBank`** — 이 기능은 그 표에 **"무엇이 들어 있는가"만 바꿔 넣는다**(오버레이). 규칙(4단계 순서·정확일치·모르면 null)은 한 글자도 안 바뀌었고, `_lookupName`/`_lookupAlias`/`_effName` 세 창구만 오버레이를 경유한다. **오버레이 미로드 = 기본 표 = 오늘 동작 100%**(fail-open).
- **저장 = `app_settings.bank_name_overrides` JSON 1키**(신규 테이블 0 — `inspect_messages`·`worktable_template` 과 같은 관용구). 모양 = `{banks:{코드:{name,add[],del[]}}, extra:[{code,name}], updatedBy, log[]}`. 서비스 `bankNameOverride.service.js`(60초 캐시 · single-flight · **조회 실패는 직전 표 유지 + `logger.warn`, 절대 throw 안 함**).
- ★★ **겹침(같은 표기가 두 은행)은 저장 자체를 막는다** — 유일한 하드블록(`validateBankOverrides` → 409 `conflict`). ★★★ **기본 별칭을 다른 은행에 추가하는 것을 "이동"으로 접지 말 것**: 조용히 옮겨 주면 오타 한 번('국민'을 신한에 추가)에 그 표기가 통째로 다른 은행을 가리켜 **남의 계좌로 돈이 간다**. 옮기려면 **원래 은행에서 먼저 지워야** 한다(겹침 검사에서 건너뛰는 조건은 `del` 하나뿐 — 완화 금지, 회귀가드 §3b 가 고정).
- ★ **이미 만든 회차는 안 바뀐다** — 금액·통장표시·계좌·은행명은 회차 생성 시점에 `payment_batch_items` 로 박제되므로 여기서 무엇을 고쳐도 과거 파일은 그대로다(**다음 회차부터** 적용).
- **소비처 3곳이 진입점에서 `await ensureBankOverrides`**: `listPaymentTargets`(보류 판정) · `buildWorkbook`(서식에 찍히는 정식명) · `saveReviewerAccount`(계좌 보완 검증 — 방금 등록한 표기를 검증이 알아야 막다른 길이 안 생긴다). `resolveBank` 가 sync 라 모듈 레벨 `_ov` + 명시 선호출 구조.
- **화면 = 입금관리 서브탭 2개**(`[입금 대상 · 회차]` / `[🏦 은행 이름]`, `STATE.pmSub` · `_pmPaint` 단일 분기). 앞 두 화면은 **쪼개지 않았다** — "담고 → 내려받고 → 회차 확인"이 한 흐름이라 나누면 작업이 늘어난다.
  - **미인식 큐** = `STATE.pmItems` 의 `bank_unknown` 항목에서 **원문 은행명을 집계**(신규 엔드포인트 0). [연결하고 풀기] 한 번 = 그 코드에 표기 추가 + 즉시 저장 + `/payment/targets` 재조회(보류가 실제로 풀리는 것까지). ★ **목록 미조회면 `null`** — 0건으로 위장하지 않는다.
  - ★ 이름 편집·표기 추가는 **그 칸만 input 으로 교체**(`replaceWith`) — 표를 재렌더하면 한글 IME 조합이 깨진다. 은행 추가도 **`prompt` 금지**(인라인 입력박스, 작업유형 모달과 같은 규율).
  - ★ `del` 은 "서버가 준 `baseAliases` 에 있는데 지금 `aliases` 에 없는 것"으로 계산 — 프론트에 정규화 사본을 두지 않는다(겹침 표시용 `_bnNorm` 만 서버와 **문자 그대로 같은 정규식**이고 회귀가드가 대조, 최종 판정은 서버).
  - ★ 낙관적 동시성 = `rev`(app_settings.updated_at) 불일치면 409 `stale`(다른 담당자가 방금 넣은 표기를 통째로 덮지 않는다). 변경 이력은 같은 키 안에 200건까지(`alias_add`/`alias_remove`/`base_off`/`rename`/`bank_add`…).
- **API = `GET/POST /api/trackb/payment/bank-names`(adminOrMaster)** — 계좌 보완과 같은 게이트. 경로는 재기준하지 않는다(양쪽 호스트 공용).
- **엑셀 대조 보강 13종**(2026-08 제공 표): `한국씨티`·`HSBC`·`도이치`·`BOA`·`BNP파리바`·`미래에셋(대우)`·`에스케이증권`·`한화증권`·`동부증권`·`메리츠종금`·`LIG투자증권`·`펀드온라인코리아` 등. ★ **같은 말이 두 코드에 걸리는 5건**(우리 020/022 · 국민 004/019 · 씨티 027/053 · NH투자증권 247/289 · IBK투자증권 225/271)은 **넣지 않았다** — 어느 코드로 보낼지 정할 수 없다(필요하면 화면에서 사람이 한쪽에만 등록).
- ★★ **입력칸이 열린 채 누른 [저장]이 조용히 안 나가던 문제 (2026-08-19 신고 "정식 명칭 수정이 저장되지 않음")**: 이름·표기 편집은 그 칸만 `<input>` 으로 바꾸는데, 커밋이 `_bnRender()` 로 표 전체를 다시 그린다 → **mousedown → blur → commit → 재렌더**가 [저장] 버튼 DOM 을 갈아치워 **click 이 아예 발생하지 않는다**(요청 0). 화면엔 "바뀐 내용이 있습니다" 바가 그대로라 눌렀는데 아무 일도 안 한 것처럼 보인다. → **방어 두 겹**: ① 바의 [저장]·[되돌리기]에 `onmousedown="event.preventDefault()"` 로 **blur 자체를 막아** click 을 살리고 ② `_bnSave`·`_bnLink` 가 **`_bnFlushEdit()`** 로 열린 편집을 먼저 확정한다(★ **여기서 재렌더 금지** — 재렌더가 곧 이 사고의 원인이다. `commit({silent:true})` 로 상태만 갱신). ★ 편집 진입점 두 곳(`_bnEditName`·`_bnAddKw`)이 `S._flush=commit` 로 **열린 편집을 등록**해야 flush 가 의미를 갖는다(등록이 빠지면 flush 는 no-op). ★ Esc·다른 곳 클릭(blur)의 종전 동작은 불변.
- 회귀가드 `tests/bankNameOverride.test.js`(판정 규칙 불변 · 오버레이 적용 · **겹침 저장 차단(조용한 이동 금지 포함)** · 스텁 pool 로 서비스 실제 실행(쓰기 표면 1곳·stale·이력 diff) · 라우터 스택 실검사 · 소비처 3곳 배선 · **프론트 함수 vm 실행**(payload/conflicts/queue·IME·onclick 인덱스·prompt 부재)) + 브라우저 실행검증. ⚠ **`withStubPool` 은 async 여야 한다** — sync try/finally 로 두면 `run` 의 첫 await 에서 finally 가 먼저 돌아 스텁·오버레이가 걷힌다(실측).

### ★★ 현금영수증 안내 D안(3시점) + 캡처 선택화 (사용자 확정 2026-08-05)
- **실제 워크플로우(사용자 확정)**: ① 공고 확인 시 현금영수증건 인지 → ② 결제 단계에서 지출증빙 발행 신청 → ③ **발행확정(일련번호)은 배송완료·구매확정 후 0~3일** — 그래서 발행 내역 캡처는 제출 시점에 존재할 수 없다.
- **안내 3시점(D안, 시안 `frontend/docs/design-cashreceipt-timing.html`)**: ① 참여 전 = 공개 목록/상세 `cashReceiptRequired` 불리언(campaign.routes `_cashReceiptFlags` 배치 파생, 목록 5초 캐시 상각) → 카드 `🧾 현금영수증` 배지(`pt-badge cr`) + 참여 전 상세 `renderCrNotice`(campaign.html). ★ **불리언만 공개** — 사업자번호·발행방법 이미지는 종전대로 참여 후 work-detail. ★ **조회 실패 = 필드 미동봉**(프론트는 `=== true`만 배지 — false로 꾸미지 않음, 필드 부재 = 구버전 백엔드). ② 참여 직후 = 기존 🧾 카드(위치 불변) + "발행확정 0~3일 · 캡처는 구매양식 제출 필수 아님" 문구. ③ 제출 화면 = 영수증 슬롯 재안내 + **[발행방법 다시 보기]**(`_csLoadCrGuides` — provider-info 신규 `cashReceiptGuideList`(채널 표 파생·라벨 동봉 = 프론트 채널 사본 0), https URL만 렌더).
- ★★ **현금영수증 캡처는 완료 판정에서 제외(선택 슬롯)**: `RECEIPT_SLOT.required=false` → `requiredSlotKeys`가 `required:false` 슬롯을 거른다(전부 선택인 병적 설정은 전체 필수 폴백). **화면 슬롯은 여전히 2개**(선택 표기 + 발행확정 후 제출 안내). ★★ `submit.routes`의 슬롯 모드 판정은 **required 개수가 아니라 `effectiveCaptureSlots`(화면 슬롯) 기준** — required로 판정하면 현영 탭이 fast-path를 타서 **영수증만 올려도 완료**가 된다(리뷰 캡처 0장). 관리자 명시 `capture_slots`는 required 미지정 = 전부 필수(무회귀), JSONB `"required": false`로 슬롯별 선택화 가능. 한계(문서화): 리뷰 캡처만으로 완료된 행은 대기 탭에서 빠지므로 영수증 늦은 제출은 완료 카드 경로/관리자 수집으로 — 필요 시 후속.
- **마감 공고 확인 = 카드 [👁 보기]**(`campaign-cards.js` `_adminActions`): 모집공고 카드(마감·투입완료·게시전 포함)에서 기존 `openReviewerPreview`(campaign.html `preview=1#tok=`, DB write 0, [모집중 가정] 토글)를 바로 연다 — 참여형만 활성(레거시는 비활성+사유 툴팁). 서버 변경 0.
- 회귀가드 `tests/cashReceiptTiming.test.js`. ⚠ 이 변경으로 `adminCardSync`(미리보기 버튼 부재 가드 → 재추가 반영)·`captureSlotVerify`·`reviewType`·`cashReceiptGuide(Doc)` 가드의 기대값을 함께 갱신했다(검사 의미는 사용자 확정 반영).

### 현금영수증 안내 (1단계 — 발행방법 노출)
- **판정의 진실원본은 탭 하나**: 연결 탭의 `tab_configs.income_type`에 '현영' 포함 → 발행 필요. 공고에는 컬럼을 만들지 않았고 모달은 **읽기 전용 표시**(`rf_cashrcpt_ro`, `refreshRecruitCashReceipt`)만 — 같은 값을 두 곳에서 관리하지 않는다.
- **서버**(`_cashReceiptInfo`): work-detail(`GET /:id/work-detail`)과 관리자 미리보기(`/admin/:id/preview`) 응답에 `cashReceipt{required, businessNo, guideImageUrl}` 동봉. 채널(쿠팡/네이버)에 맞는 발행방법 이미지를 `app_settings.cash_receipt_guide_*`에서 고른다. **현영이 아니면 null**(일반 공고 응답·화면 불변), 조회 실패도 null(fail-soft).
- **설정**: 관리자 설정탭에 채널별 발행방법 이미지 업로드 **4칸**(쿠팡·네이버·올리브영·카카오메이커스, `POST /api/tab/cash-receipt-guide`, adminOrMaster). guide-image Drive+프록시 인프라 재사용(신규 저장소 0). ★ `imageUrl`은 **https 절대 URL만** — 리뷰어 화면에 `<img src>`로 나가므로 자유 문자열 금지.
- ★★ **채널 목록 단일 출처 = `utils/cashReceiptChannels.js`**: 소비처 3곳(저장 라우트 화이트리스트 · `_cashReceiptInfo` 이미지 선택 · `provider-info` 조회키)이 전부 이 표를 본다. 과거엔 세 곳에 흩어져 있어 `WO_CHANNEL_HOSTS`에 올리브영·카카오메이커스를 추가하고도 **현영 안내 이미지는 2슬롯에 머물러 조용히 빈 값**이 나갔다. 프론트 사본(`index-app.js`의 `CR_GUIDE_CHANNELS`)·admin.html 슬롯(`crGuideFile<Cap>`)까지 **회귀가드가 일치를 고정**(서버 목록의 모든 채널에 등록 슬롯이 있어야 통과).
- ★ **`match` 정규식을 넓히지 말 것** — 발행방법은 채널마다 실제 화면이 달라 **틀린 안내가 없는 것보다 나쁘다**. 카카오는 `메이커스`를 요구(톡스토어·선물하기 제외), 미매칭·빈 값은 **이미지 없이 문구만**(fail-soft). 판정 우선순위는 배열 순서 = 쿠팡 우선(커스텀 채널명에 둘이 섞였을 때 기존 동작 보존).
- 회귀가드 `tests/cashReceiptGuide.test.js`(정적 + **스텁 pool로 라우트 실행**: 4채널 저장·목록 밖 400·provider-info 4키 반환·조회 SQL에 새 키 포함)·`tests/cashReceiptGuideDoc.test.js`(, 안내서↔화면 드리프트).
- **리뷰어 화면**: 공용 렌더러(`campaign-workdetail.js`)가 **상품 카드 바로 뒤**(결제 전 시점)에 🧾 안내 카드 — 지출증빙 + 사업자번호 + 발행방법 이미지 + "제출 때 발행 내역 캡처 필요" 예고. 렌더러가 공용이라 관리자 미리보기에도 자동 반영.
- **직원 안내서**: `frontend/docs/현금영수증_발행방법_이미지_등록_안내.html`(설정탭 등록 절차 4단계 + 리뷰어 화면·2슬롯 **HTML/CSS 목업**, 외부 리소스 0 = 오프라인 열람 가능). 화면 재현 목업은 `color-scheme:light` 고정 — 다크에서 색이 뒤집히면 "실물과 같다"가 깨진다. ★ 안내서가 가르치는 라벨·문구가 제품 소스에 실제로 있는지 `tests/cashReceiptGuideDoc.test.js`가 대조 — **화면을 고치면 이 테스트가 깨져 안내서 갱신을 상기**시킨다(문서↔제품 드리프트 가드).
- ★★ **역할 미들웨어 앞에 `authMiddleware` 필수**: `adminOrMasterMiddleware`는 `authMiddleware`가 세팅한 `req.admin`을 읽으므로, 빠뜨리면 **마스터 포함 전원 403**("관리자 권한이 필요합니다")이 된다. 실제로 `company-business-no`·`cash-receipt-guide` 두 라우트가 `authMiddleware` 없이 등록돼 **아무도 현금영수증 설정을 저장할 수 없었다**(토큰이 유효해도 거부되어 "권한 설정 문제"로 오인하기 쉬움). 전 라우트 파일 스캔 + 실제 서버 기동 권한별 검증 가드 `tests/authMiddlewareChain.test.js`. ⚠ 이 API의 errorHandler는 DB 오류를 **200 + `{error}`** 로 돌려주므로(GAS 호환) 상태코드만으로 성공을 판정하지 말 것.
- 회귀가드 `tests/cashReceiptGuide.test.js`.

### AI 판별 예시이미지 (현금영수증 · 리뷰) — 설정탭 한 창구
- **두 종류의 이미지를 구분한다**: ㉮ **발행방법**(`cash_receipt_guide_*`) = **리뷰어에게 보여주는 안내**(결제 전 "이렇게 발행하세요") ㉯ **판별 예시**(`cash_receipt_sample_*` · `review_inspect_sample_*`) = **AI 판정의 기준**(few-shot, **리뷰어에게 안 나간다**). 한 칸에 합치면 "리뷰어 안내를 바꿨더니 AI 판정이 흔들리는" 결합이 생긴다 — 그래서 채널 표는 하나(`utils/cashReceiptChannels.js`)로 두고 **키만 갈랐다**(채널을 늘리면 두 슬롯이 함께 따라온다).
- **현금영수증 예시의 소비처 = `captureVerify`(영수증 슬롯)**: `review-upload` 가 슬롯에 맞는 예시를 고른다(`slot==='review'` → `loadSamplesFor` / `'receipt'` → `loadReceiptSamplesFor`). ★ **슬롯 검수와 2차 검수가 같은 배열을 쓴다**(같은 이미지에 AI 콜 2번 금지 — 기존 규율 그대로). ★ 1차 필터(precheck)는 여전히 리뷰 슬롯 전용.
- ★★ **캐시 지문 충돌 금지** — classify 캐시 키는 예시 **슬롯 key** 로 만들어지므로 현금영수증 예시 key 에 **`receipt_` 접두**를 붙인다. 안 붙이면 `coupang`(영수증)과 리뷰 예시가 같은 지문을 만들어 **다른 예시로 낸 판정이 히트**한다.
- ★★ **예시 종류에 맞는 안내문** — 프롬프트 앞머리의 "아래는 정상 리뷰 화면의 예시입니다"를 현금영수증 예시에 그대로 쓰면 모델이 그 화면을 `review` 로 몬다(막으려던 것보다 큰 오판). `kind` 로 문장을 가르되 **리뷰 예시만 있을 때의 문장은 한 글자도 바꾸지 않는다**(기존 판정 보존). `kind` 판정 기준 3줄도 불변.
- **로더는 한 벌**(`_loadSampleSlots`) — 리뷰·현금영수증 예시가 같은 캐시·URL 검증을 쓴다(사본을 두면 한쪽만 TTL·https 검증이 달라진다). 채널 모르면 빈 배열(미동봉), 등록 전이면 빈 배열 = **오늘과 동작 동일**(fail-open).
- **창구 = `/api/trackb/review-inspect/samples` 하나**(GET 내부인 / POST adminOrMaster): GET 이 `samples`(리뷰 9) + `receiptSamples`(현영 4)를 함께 반환, POST 는 `kind:'receipt'` 면 채널 저장. ★ **kind 미지정 = 리뷰**라 리뷰웹시스템[3버전] 리뷰검수 탭의 [🖼 판별 예시] 모달은 동작 불변(같은 저장소라 어디서 올려도 결과가 같다).
- **크게 보기**: 설정탭 슬롯 썸네일(54×70)을 클릭하면 라이트박스로 원본 확인(←→ 이동·Esc 닫기·[이 장 제거]) — 40px 썸네일로는 "무엇을 기준으로 등록했는지" 검증이 불가능했다(실사용 신고). ★ URL 출처는 **서버 응답(`_smpLast`)** 하나(프론트 사본 금지) · onclick 은 **인덱스만** · 오버레이는 **body 직속** · 키 리스너는 **최상위 1회 + 입력 중 미가로채기** · 끝에서 순환 안 함.
- **화면 = 설정탭 `aisamples` 패널**(`js/admin-settings.js` 공유 모듈 — 관리자 대시보드·리뷰웹시스템[3버전] 한 벌, 관리자 전용). ★ **슬롯 목록의 프론트 사본을 만들지 않는다** — 서버 응답을 그대로 그리므로 채널을 늘려도 한 칸이 조용히 빠지지 않는다(현영 안내가 2슬롯에 머물렀던 실측 사고의 교훈). ★ 경로는 재기준(`ADMIN_SETTINGS_API`)하지 않는다 — 이 경로가 양쪽 호스트에서 닿는다(작업표 표준열과 같은 판단). 업로드는 guide-image Drive+프록시 재사용(신규 저장소 0).
- 회귀가드 `tests/aiSampleImages.test.js`(스텁 pool 로 서비스 실행 + 라우터 스택 실검사 + 배선 + 사본 부재) + **실제 http 오리진 브라우저 실행검증**(테마 없는 호스트 렌더·기존 등록분 프리필·업로드/제거 페이로드). 직원 안내서 = `frontend/docs/현금영수증_발행방법_이미지_등록_안내.html` 의 "AI 판별 예시이미지와 헷갈리지 마세요" 절.

### ★ 같은 사진 두 번 제출 방지 — 첨부 즉시 안내 (스키마 변경 0, 시안 `frontend/docs/design-duplicate-submit-prevention.html`)
- **원인(실측 28건)**: 한 사람이 **같은 작업의 다른 건**(또는 다른 작업)에 같은 캡처를 냈다. 구조적 사각지대 = ① 첨부 즉시 1차 필터는 형식·채널만 보고 **중복 대조 0** ② 업로드 시 중복 검사는 `findSlotDuplicate`(같은 행·같은 리뷰어·같은 칸)뿐 ③ 결국 **제출이 끝난 뒤** 2차 검수에서만 발견 → **리뷰어가 되돌릴 수 있는 시점에는 아무도 안 본다**. 게다가 리뷰어 화면엔 "내가 이미 낸 캡처"가 없다.
- ★★ **판정 = `findOwnDuplicate`**(첨부 즉시, 본인 것만): 지문 일치 + **이름 일치** + (알면) **phone8 일치**, ★ **같은 작업·같은 줄은 제외**(잘못 올려 다시 올리는 정상 재제출을 잡으면 멀쩡한 리뷰어가 갇힌다 — `findDuplicate` 와 같은 규율), 휴지통(`trashed`) 제외. 재료 부족·조회 실패는 **null = 경고 없음**(fail-open).
- ★★ **경고까지만, 차단 없음**(사용자 확정 ①): 우리가 잘못 알아봤을 때 **정상 제출이 막히는 쪽이 더 큰 피해**다(1차 필터 fail-open 규율). 대조 범위 = **본인이 낸 모든 작업**(확정 ② — 다른 상품 캡처 재사용까지 잡는다).
- ★ **응답은 가산 필드 `duplicate`**(`/api/image/review-precheck`) — 기존 `verdict`·`blocked` 계약 불변이라 구버전 프론트 무영향. **AI 판정과 독립**(형식이 통과여도 중복 안내가 나가고, 중복 조회가 죽어도 형식 판정은 나간다). 지문은 **서버가 계산**(클라 전달 금지).
- ★★ **리뷰어 화면에 줄 번호를 쓰지 않는다** — 관리용 번호라 리뷰어가 알 수 없다. 같은 작업이면 화면의 **참여 순번 ①②③**(`_dupOrdinal`), 못 찾으면 "다른 건", 다른 작업이면 **작업명 없이** "다른 작업"으로만 가리킨다(시트 제목 미노출 원칙).
- **제출 화면 "내 제출 현황"**(`_renderMySubmitStatus`): 그 작업의 내 참여 건을 순번·상품(옵션)·제출일·썸네일로 나열(정보 확인 그리드보다 먼저). **서버 목록(items)만 사용** — 썸네일 재료로 `search.service` 응답에 `reviewFileId`(그 행 대표 이미지 = 본인 것) 추가. 낸 것이 없으면 아예 안 그린다.
- **지나쳐 제출된 중복**: 업로드 직후 2차 검수 결과가 duplicate fail 이면 응답 `files[].duplicateNotice` 로 **그 자리에서 안내만** — ★ **파일을 지우거나 반려하지 않는다**(정당한 재제출 보호). 관리자 쪽은 검수 기록이 리뷰검수 탭에 바로 뜨므로 **알림을 새로 쌓지 않는다**(같은 사실로 두 번 울리면 늑대소년).
- 한계(문서화): 화면 개선이 없애는 것은 **실수**까지다 — 고의 재사용은 다른 사진으로 우회하므로 본문 겹침 검사·검수 카드가 계속 남는다. 회귀가드 `tests/duplicateSubmitPrevent.test.js`(스텁 pool 로 판정 실행 + SQL 불변식 + 라우트·화면 배선).

### ★★ 제출 이미지 자동 분류·이동 (파일 라우팅) — 오제출 이동 + 중복 반려 (migration 091)
- **목적**: 리뷰 칸에 현금영수증 / 영수증 칸에 리뷰 / 리뷰 칸에 구매캡처가 올라오면 **올바른 폴더로 자동 이동**하고, 이동 대상 칸에 **같은 파일이 이미 있으면 휴지통 + 반려**. 와이어프레임 = `frontend/docs/리뷰제출_이미지_자동분류_와이어프레임.html`. 기존 검수(verifyCapture)의 판정값을 그대로 재사용해 **AI 콜 순증 0**.
- ★★ **전이표 단일 출처 = `utils/captureRoute.js`**(`routeDecision` 순수함수). 정의된 4조합만 이동(review→receipt/order_capture, receipt→review/order_capture — receipt 이동은 그 탭에 영수증 슬롯이 있을 때만 = "폴더가 있는 경우"), **verdict.sure(확신≥0.9)만**, 표 밖(other·purchase_confirm·애매 0.7~0.9)은 전부 현행 경고 유지. 카카오메이커스 채널 전체 제외(피드형). 실행부 = `fileRoute.service.js`, 배선 = `diag.routes` `/review-upload`.
- ★★ **반려(제출 거절)의 유일한 근거 = SHA-256 정확 일치**(`findSlotDuplicate` — 같은 행·같은 리뷰어·**대상 슬롯**에 같은 지문의 다른 파일). **AI 판정만으로는 절대 반려하지 않는다**(완화 금지 — routeDecision 의 동작은 none/route 뿐임을 회귀가드가 매트릭스 실행으로 고정). ★ 2차 검수 `findDuplicate`(다른 사람·다른 자리)와 스코프가 정반대라 **재사용 금지**(합치면 정상 재제출이 반려된다). 판정 불가·쿼리 실패 = 중복 아님(fail-open).
- ★★ **삭제는 휴지통(trashFiles)만** — 영구삭제 API 금지(30일 복구창). 반려 파일은 원장 미기록·fileId 미반환, 응답 `files[].rejected` + 전부 반려면 최상위 `error`.
- **모드** env `AUTO_FILE_ROUTE`: `off`(0) | **`dry`(기본 = 관측)** | `auto`(1). 기본이 dry 인 이유 = 사용자 확정 "관측 1회 수행 후 출시결정" — 배포 즉시 관측(계획을 `capture_route_plan` info·resolved 로 기록)만 하고 이동 0. 출시는 Railway 에서 `AUTO_FILE_ROUTE=auto`. 반려만 끄는 스위치 `AUTO_FILE_ROUTE_REJECT=0`.
- ★★ **구매캡처(order_capture) 이동은 판별 예시 2장(구매캡처+구매확정)이 모두 등록돼야 발동**(사용자 확정 — 주문내역·구매확정 화면은 생김새가 비슷한 가장 약한 고리). 슬롯 단일 출처 = `utils/routeSampleKinds.js`(`route_` key 접두 = 캐시 지문 충돌 방지), 등록 = 설정탭 aisamples 패널(서버 응답 그대로 렌더, POST `kind:'route'`).
- ★★ **kind `order_capture` 추가는 줄 추가만**(gemini classify — 기존 review/receipt 판정 기준 줄 불변) + **캐시 접두 `classify3:`→`classify4:`**(옛 캐시가 새 종류를 모른 채 히트 차단, 087 2차와 같은 규율).
- ★★ **예시 조립 단일 지점 = `reviewInspect.submissionSamples`** — 1차 필터(precheck)·업로드 검수·2차 검수·소급 스윕이 전부 이걸 쓴다. 조립처가 갈리면 sampleSig(캐시 지문)가 갈려 **같은 이미지에 AI 콜 2번**(diag.routes 에 `loadSamplesFor` 직접 호출 0을 가드가 고정).
- **정합 3종**: ① A-2 원장은 라우팅 반영 **최종 슬롯**으로 기록 + `routed_from_slot/routed_at/routed_by`(091, fail-soft UPDATE — 마이그레이션 늦게 붙어도 업로드 불사) ② 대표 이미지는 라우팅 발생 시 `recomputePrimary` 가 원장 기준 재계산(영수증이 대표로 남지 않게, 0장이면 비움) ③ **완료 판정**: 슬롯 탭은 서버 원장 기준이라 자동 정합, 단일 첨부 탭은 프론트가 "남은 파일 0 = 제출 기록 안 함"(`search-app.js` `_rtStayed` — 리뷰 캡처 0장에 완료 찍힘 방지).
- **리뷰어 안내**: 이동 = 파란 정보 블록(`_csShowRouteNotice`) + 원래 칸 재첨부 유도 / 반려 = 빨간 안내(파일 미저장 명시). 관리자 알림 = `capture_routed`/`capture_dup_rejected`(warn, 리뷰웹시스템[3버전] 로그 탭) — 이동/반려 파일은 기존 `capture_mismatch` 를 남기지 않는다(도배 방지).
- **되돌리기**: 로그 탭 capture_routed 행의 [↩ 원위치](`POST /api/trackb/reviewer-logs/route-revert`, adminOrMaster) → `revertRoute` 가 이동 이력(routed_from_slot)으로만 원복(이력 없는 파일은 거부) + 원장·대표 재계산 + 관련 알림 자동 해결.
- **소급 정리**(사용자 확정 "소급정리 필요"): 설정탭 aisamples 패널 "오제출 소급 정리" — 탭 선택 → `POST /api/trackb/file-route/sweep`(adminOrMaster, **dryRun 기본**·limit 캡 60) 미리보기 → [실행]. 건당 Drive 다운로드+AI 1콜. `routed_from_slot IS NULL` 만 대상(핑퐁 방지), 중복은 휴지통+`slot_key='trashed'`(원장 보존). ★ **is_submitted 는 건드리지 않는다**(문서화된 한계 — 이동으로 리뷰가 빈 행은 알림으로 남아 사람이 재요청).
- **보완 3종(2026-08-05 실측 신고 — 이노크아든 구매확정 전환 건)**: ① ★★ **2차 검수도 리뷰타입을 본다** — `inspectSubmission` 형식 판정이 confirm 탭에서 `purchase_confirm`(+review)을 정상으로 인정(안 그러면 구매확정 탭 정상 제출 전건이 "리뷰 화면 아님" 불량 — 1차 필터 안전핀(087)만으론 2차가 새는 구멍이었다). **공고를 뒤늦게 구매확정으로 바꾼 탭은 리뷰검수 [♻ 재검수]로 소급**(`POST /api/trackb/review-inspect/reinspect` adminOrMaster → `reinspectTab`이 fail/suspect/unverifiable 만 pending 리셋 — **resolved·pass 보존**, attempts 0, 같은 잡 락으로 즉시 스윕 1회). ② **구매확정 예시 = 모바일·PC 2슬롯**(`purchase_confirm_pc` 추가, 기존 settingKey 보존 = 올린 이미지 유실 0, kind 동일이라 게이트는 한 장만 있어도 켜짐). ③ ★ **상품명 매칭 전처리 `_cleanProductForMatch`** — 캡처의 꼬리 말줄임(`…`)·기대값의 선행 번호(`1. `)·URL 꼬리(`(https:` 절단형 포함)를 벗기고 대조(같은 상품이 "다름"으로 나오던 실측 원문을 가드가 실행으로 고정 — 느슨해지는 방향만이라 정상 제출을 불량으로 몰지 않는다).
- 회귀가드 `tests/fileAutoRoute.test.js`(전이표 매트릭스·모드·중복 스코프·recompute 스텁 실행 + 배선 + 보완 3종 실측 원문 실행). ⚠ 이 변경으로 기존 가드 4종(`reviewInspect`·`captureVerifyNotify`·`reviewType`·`aiSampleImages`)의 패턴이 드리프트해 함께 갱신했다(검사 의미 불변 — 샘플 조립이 submissionSamples 로 수렴·kind/캐시 접두 추가).

### ★ 리뷰검수 수동 확인 = AI 판별 학습 루프 (migration 092)
- **목적(사용자 요청 2026-08-05)**: 수동 확인 백로그(실측 1632건)를 치우는 과정 자체가 **AI 판별의 기준을 키우는 학습 재료**가 되게 한다. Gemini는 파인튜닝이 아니라 few-shot이므로 학습 경로는 3갈래: ① **확인 2분화 원장**(`review_inspections.resolution` — `ok`=정상/오탐 · `bad`=불량 맞음, 오탐률 관측의 원천) ② **상품명 별칭 자동 학습** ③ **판별 예시 승격**(검수 카드의 실물 캡처 → few-shot 기준).
- ★★ **별칭은 가산만**(`tab_configs.inspect_product_aliases`, 기대 상품명과 **분리 저장**): [정상] 확인 시 **상품명 의심(product warn) 건만** 캡처 표기(`_cleanProductForMatch` 정리본, 4자 미만 제외)를 그 탭 인정 별칭으로 등록 → `loadTabExpectations`가 manual/work_order 어느 소스에든 **병합만** 한다. ★ `inspect_product_names`에 섞으면 안 되는 이유 = manual 값이 생기는 순간 작업오더 파생 후보가 꺼지는 우선순위 규칙이 있어 **후보가 좁아진다**(별칭은 소스 전환을 절대 만들지 않는다 — 가드 B1). 잡는 범위가 좁아질 뿐이라 정상 제출을 새로 불량으로 모는 회귀 없음.
- ★ **조용한 자동수정 금지**: 학습된 별칭은 [⚙ 기대 상품명] 창이 목록으로 보여주고 **사람이 편집·비울 수 있다**(`saveProductAliases`, POST product-names `aliases` 필드 — 미전송=유지).
- **확인 버튼 2분화**(workdesk 리뷰검수): 카드 [✓ 정상]/[✕ 불량], 상세 팝업 동일 + **[🖼 판별 예시로 등록]**(admin — 슬롯 목록은 GET samples 서버 응답 그대로, 이미지는 기존 무인증 프록시 `/api/drive/image/<id>` URL 재사용 = 신규 저장소 0, 선택은 인덱스만 전달). 구분 없이 확인된 옛 건은 resolution NULL(호환).
- **일괄 정상 처리**: 탭 선택 시 [✔ 일괄 정상] → `POST /api/trackb/review-inspect/resolve-bulk`(★ **adminOrMaster** — 대량 종결은 되돌리기 어렵다. 건별 확인은 종전대로 staff 담당 탭). 대상 = 미확인 suspect/fail 만, 별칭도 일괄 학습(중복 접힘·30건 캡). 백로그 권장 순서 = **[♻ 재검수](새 기준 자동 소거) → 실불량 건별 [✕ 불량] → [✔ 일괄 정상]**.
- ★ **REQUIRED_SCHEMA 등록 2종**(`review_inspections.resolution`·`tab_configs.inspect_product_aliases`) — resolution은 목록 SELECT·확인 UPDATE의 하드 의존이라 없으면 **리뷰검수 탭 전면 42703**.
- 회귀가드 `tests/reviewInspectLearn.test.js`(스텁 pool로 resolve/bulk 실제 실행 + 가산 병합·중복 접힘·화이트리스트 + 배선). ⚠ `workdeskInspectView` 가드의 상세 블록 추출 경계를 갱신(검사 의미 불변).

### ★ 리뷰검수 학습 루프 2차 — 예시 누적 · 오류유형 필터 · 수동 분류(스키마 변경 0, 와이어프레임 `frontend/docs/리뷰검수_예시누적_수동분류_와이어프레임.html`)
- ★★ **판별 예시는 누적(교체 아님)**: `app_settings` 값을 **JSON 배열**로 확장 — `parseSampleUrls`가 "문자열 = 1장짜리 배열"로 읽어 **기존 등록분 유실 0·마이그레이션 0**. 편집 단일 지점 `_mutateSampleList`(리뷰/현영/자동분류 3창구 공용): `mode:'add'`(누적·UI 기본) | `'remove'+index`(개별 삭제) | **미지정 = 종전 동작 100%**(교체/전체 제거 — 구화면 호환). 상한 = **슬롯당 5장**(`REVIEW_INSPECT_SAMPLE_SLOT_CAP`, 초과는 사유와 함께 거부) · **판정당 동봉 6장**(`_ATTACH_CAP`, `submissionSamples`에서 `_trimSamples` — ★ **슬롯별 최근 우선 라운드로빈**이라 자동분류 예시가 통째로 잘리지 않는다). 권장은 슬롯당 2~3장("많이"가 아니라 "다르게").
- ★★ **캐시 지문**: `_loadSampleSlots`가 샘플 key에 **URL 해시 접미(`#xxxxxxxx`)** — sampleSig(gemini)가 key 목록으로 만들어지므로 장수·구성이 바뀌면 지문이 바뀌어 옛 판정 캐시가 새 예시를 모른 채 히트하지 못한다. 설정 응답은 `imageUrls` 배열 + `imageUrl` 첫 장(구프론트 호환).
- ★★ **수동 분류(이동)** `POST /review-inspect/route-manual`(건별 확인과 같은 게이트): 검수 카드 [🧾 현금영수증으로 이동]/[🛒 구매캡처로 이동](receipt 칸은 [🖼 리뷰로]) — 실행은 `fileRoute.manualRoute`가 **자동분류와 같은 실행부**(폴더 해석·Drive 이동·원장·대표 재계산·로그, 사본 0) → 기존 [↩ 원위치]로 동일하게 되돌린다. ★ 사람이 판단하므로 확신도·예시 게이트 미적용, `AUTO_FILE_ROUTE` off/dry여도 동작(백로그는 사람이 치운다). ★ **대상 칸 같은 지문 = 휴지통이 아니라 거부**(수동 경로에 파일 삭제 없음). ★ `routed_from_slot`은 COALESCE(최초 출처 보존 — 되돌리기는 항상 원래 칸). 이동 성공 = 그 검수 건 `resolution='ok'` 자동 종결 + `routed_by='manual:<이름>'`(분류 원장).
- **오류유형 필터**(workdesk): 판정 근거(checks)가 이미 목록에 실려 와 **서버 변경 0** — `_riIssueTypes`(카드 근거 문장 `_riReasons`와 같은 분기) 기반 유형 칩(리뷰화면 아님/채널/상품명/같은파일/본문겹침/작성자)+건수. ★ 필터는 화면에서만 거르고 **onclick 인덱스는 원본 STATE.ri 기준 유지**, 상태·탭 전환 시 유형 초기화.
- **학습 결합**: 이동 성공 응답에 `promote` 제안(receipt→그 탭 채널 현영 예시 / order_capture→구매캡처 예시, **슬롯에 자리 있을 때만**) → 프론트 confirm 후 `mode:'add'` 등록(조용한 자동 등록 금지). **정확도 카드**(설정탭 aisamples, `GET /route-stats` adminOrMaster): 수동 분류(`routed_by LIKE 'manual:%'`) vs AI 관측 계획(`capture_route_plan`) 대조 = match/miss/differ — **95%+면 auto 전환 검토 안내(전환은 사람이)**, 구백엔드는 "배포 대기"(허위 0% 금지).
- 회귀가드 `tests/sampleAccumulateRoute.test.js`(파서·누적·상한·트림 실행 + manualRoute 스텁 drive/pool 실행 + 통계 + 배선). ⚠ `aiSampleImages` 가드 3건 갱신(저장 SELECT+업서트·배열 반환·mode add — 검사 의미 불변).
- **유형별 맞춤 조치 카드**(시안 `design-inspect-type-actions.html` + 카드 v2 `design-inspect-card-v2.html` A안 확정): 카드 = 유형 색 띠(`nc-type`, 필터 칩과 같은 6색) → **상단 전폭 썸네일 150px**(클릭=상세, 상태 배지는 썸네일 우상단 — 검수는 캡처를 보고 판단하는 일이라 카드에서 바로 읽혀야 한다) → 본문 클릭=상세([상세] 버튼 제거, 탭 링크는 stopPropagation) → **주 조치 1개 + 보조 2개 + [⋯]**. ★★ **주 조치에 채움(filled) 버튼을 쓰지 않는다** — 빨강 카드가 100장 늘어서면 화면이 벽이 되고 정작 급한 건이 안 튄다(실사용 신고). 강조는 **유형색 틴트 + 색 글자 + 테두리**로만(가드가 6종 전부 고정). ★★ **주 조치가 [✕ 불량]뿐인 유형(리뷰화면 아님·불명 등)은 전폭 버튼을 만들지 않고** `flat` 분기로 **[✓ 정상] | [✕ 불량] 좌우 반반** 한 줄(사용자 확정). 카드 문구는 `✕ 불량`까지만 — 반려 안내 전송 여부·내용은 전용 팝업이 설명한다. ★★ **매트릭스 단일 출처 = `_riCardActions`**(우선순위 `_riPrimaryIssue` = 같은파일 > 리뷰화면아님 > 상품명 > 본문겹침 > 채널 > 작성자): 중복=🗑 제거만(이동은 [⋯] — 어디로 옮겨도 중복은 남는다), 리뷰화면아님=AI got 종류의 이동(불명=✕ 불량), 상품명=✓ 별칭 학습, 채널=✓ 추정 오인, 본문겹침=👁 나란히 보기. ★ **[✓ 정상]/[✕ 불량]은 어느 유형이든 주/보조에 상시 존재**(맞춤 배치 = 빠른 길, 선택지 축소 아님 — 가드 A5가 전 유형 실행으로 고정).
- **[🗑 중복파일 제거]**(`fileRoute.dedupManual` + `POST /review-inspect/dedup-manual`, 건별 확인 게이트): ★ **duplicate fail 근거(matchFileId) 없는 파일은 서버가 거부**(휴지통 호출 0 — 버튼 없는 유형의 API 직접 호출 차단). 나중 제출본을 **휴지통만**(영구삭제 금지·30일 복구)으로 보내고 원장 `slot_key='trashed'`+`routed_by='dedup:<이름>'`(최초 출처 COALESCE 보존), 검수 건은 **불량 맞음(중복 제출)** 종결. 팝업이 제거/보존 두 장을 나란히 명시.
- **전용 확인 팝업 공용 골격 `_rcPopup`**(#562 시안): 이동·중복 제거가 시스템 confirm 대신 사용 — 대상 줄·동작 목록·되돌리기 색 박스·일반(파랑)/위험(빨강) 2변형, Esc·바깥 클릭=취소. 회귀가드 `tests/inspectTypeActions.test.js`(매트릭스 vm 실행 + dedup 스텁 실행 + 배선 + **채움 금지·flat 좌우 반반·썸네일 150px** 고정) + 실브라우저 검증(카드 4유형·버튼 배경 실측·팝업 2종·⋯ 메뉴·JS 오류 0). ⚠ `sampleAccumulateRoute` 되돌리기 문구 패턴 갱신(검사 의미 불변).
- ★★ **AI 판별 종류 판독은 `_riFmtKind` 한 곳**(`fm.got || fm.kind`): 원장(2차 검수 `inspectSubmission`)은 **`kind`**, 자동분류 판정값은 **`got`** 으로 이름이 갈려 있어 **현금영수증으로 판별된 건이 카드엔 '종류 불명'으로 떨어져 이동 버튼이 안 뜨던 실측 사고**가 났다. 프론트가 두 이름을 흡수하므로 **옛 레코드도 재검수 없이 소급 적용**된다. 유형 띠(`📵 리뷰화면 아님 — 현금영수증으로 보임`)·근거 문장(`현금영수증 화면으로 판별되었습니다 — 리뷰 화면이 아닙니다`)·상세·반려 사유 프리필이 전부 이 값을 쓴다. ★ 종류 불명은 종전대로 [✕ 불량](어디로 보낼지 모르면 이동 제안 금지).
- ★★ **채널 불일치 문구는 문장 한 줄**(`_riChanReason` — 카드·상세·반려 사유 프리필 공용): `X 작업에 Y 캡처가 제출되었습니다`. 종전 `작업 쿠팡 / 캡처 네이버` 병기는 **어느 쪽이 작업이고 어느 쪽이 제출물인지 순간적으로 뒤집혀 읽혔고**(실사용 신고 — 사용자가 실제로 반대로 읽었다), 그 문구가 **반려 사유로 리뷰어에게 그대로 나가서** 더 위험했다. 캡처 채널 불명도 문장으로(`X 작업인데 캡처의 채널을 알 수 없습니다`). 사본을 두면 화면마다 방향이 갈리므로 함수 한 벌만 둔다.
- **[✕ 불량] = 전용 팝업 + 반려 사유 리뷰어 통지**(시안 `frontend/docs/design-inspect-confirm-popup.html`): 카드·상세의 불량 처리는 `riBadPopup`(화면 중앙 전용 UI — 대상 줄·판정 근거 프리필 textarea·**전송 체크 기본 켬**) → resolve 라우트 `rejectMessage` → `notifyInspectionReject`(연락처 = **review_index.phone8만** — 소유권 규율) → `csBridge.postInspectionReject`가 리뷰어 1:1 문의 스레드에 자동 전송(교체요청 반려 통지와 같은 인프라 — 스레드 없으면 생성하되 ★ **관리자 발신이라 admin_unread 0**, SSE 발신자는 닉네임 치환 fail-closed, 절대 throw 없음). ★ **전송 실패는 응답 `notify`로 화면이 말한다**(조용한 미전송 금지 — 확인 자체는 유지). 일괄 정상 처리는 통지 무관(불량이 아님). 회귀가드 `tests/inspectRejectNotify.test.js`(스텁 pool/csBridge 실행 + admin_unread 0 + 배선).
- ★★ **리뷰어에게 나가는 문구는 설정에서 유형별로 고친다 (migration 093 · 시안 `frontend/docs/design-inspect-reviewer-messages.html`)**: [✕ 불량]·[🗑 중복파일 제거]·[이동] 세 팝업이 프리필하는 문장이 **관리자 설정 › 💬 리뷰어 안내문구**에서 편집·저장된다(유형 7종 = 같은파일·리뷰화면아님·상품명·채널·본문겹침·작성자·이동).
  - ★★ **문구 단일 출처 = `utils/inspectMessages.js`** — 유형표·기본문구·토큰(`{reason}` 판정 근거 / `{work}` 작업명 / `{to}` 옮긴 칸)이 여기 한 곳. **프론트에 사본을 두지 않는다**(설정 화면도 팝업도 서버 표를 그대로 받아 쓴다) — 사본을 두면 "설정에서 고쳤는데 실제로 나가는 문장은 그대로"가 된다(회귀가드가 두 프론트 파일에 문구 문자열이 **없음**을 고정).
  - ★★ **빈 값 = 그 유형만 기본 문구로 복귀**(빈 메시지가 리뷰어에게 나가지 않는다) · 모르는 키는 버린다 · 1000자 상한. ★ **값 없는 토큰은 지우지 않는다** — `{reason}`을 빈칸으로 지우면 `사유:` 만 남은 이상한 문장이 나간다(호출부가 근거를 채워 넘긴다).
  - **저장 = `app_settings.inspect_messages` JSON 1키**(신규 테이블 0). `GET /api/trackb/settings/inspect-messages`(**내부인** — AE도 검수 팝업을 쓴다) / `POST`(**adminOrMaster** — 전사 문구). ★ **조회 실패해도 기본 문구를 돌려준다**(팝업이 빈 채로 열리지 않게). 경로는 재기준하지 않는다(양쪽 호스트 공용).
  - ★★ **사진 카드(`msg_type='inspect_result'`)** — 리뷰어 채팅에 "어떤 작업의 어떤 사진"인지 함께 보낸다(중복은 **반려/이미 제출된** 두 장을 나란히, 이동은 옮긴 칸 표기). 카드는 `cs-review-edit-card.js` `inspectHtml` **한 벌**(리뷰어 채팅·관리자 대화창 공용, 파일ID로 넘겨 프론트가 신뢰 베이스로 URL 재구성). ★ **`meta` 는 리뷰어 응답에 그대로 실린다** — 관리자 실명·시트 제목 금지(080 규율), 파일ID 형식 밖 값은 버린다. ★ SSE 푸시에도 `msgType`/`meta` 동봉(목록 API만 고치면 실시간 푸시로 카드가 안 뜬다). ★ **줄 번호는 넣지 않는다** — 관리용 번호라 리뷰어는 모른다(참여 순번·상품·제출일로 가리킨다).
  - ★ CHECK 확장(093)은 **NOT VALID**(러너가 파일을 암묵 트랜잭션으로 돌린다 — 스캔 중 ACCESS EXCLUSIVE 방지). `card` 없으면 종전 `text` 그대로(구경로 무회귀).
  - 회귀가드 `tests/inspectMessageSettings.test.js`(정규화·토큰 순수함수 실행 + 스텁 pool 로 GET/POST 라우트 실제 호출 + 라우터 스택 실검사 + meta 위생 + 프론트 사본 0, **변이시험 검출 확인**) + 브라우저 실행검증(설정 패널 렌더·편집/저장 페이로드·[기본 문구로]·세 팝업 프리필·토큰 치환·카드 2장/1장).
- ★★ **반려 안내가 "리뷰어 연락처를 찾지 못함"으로 전멸하던 문제 (실사고 2026-08-06)**: 종전 해석은 `review_index` 를 **(시트,탭,행)으로만** 봤는데 ① 검수 건의 `row_index` 는 **NULL 일 수 있고**(업로드 시 이름↔행 매칭이 모호하면 `review_submissions.row_index` 가 비고 그대로 복제된다) ② 행이 있어도 **시트 연락처 칸이 비면** `phone8` 이 없다.
  - ★★ **해석 단일 출처 = `reviewInspect.resolveReviewerPhone8`** — **한 왕복**으로 4갈래를 함께 읽고 위에서부터 고른다: ① 시트 현재값(소유권의 최종 진실 — `search.service` 규율) ② 업로드 원장의 **결정적 링크**(`review_submissions.review_index_id`) ③ 확정 신원(`participation_links`) ★ **이름이 같을 때만**(재배정된 행의 옛 주인 차단, ①이 빈 경우에만 보는 것도 stale-pl 규율 그대로) ④ 같은 작업에서 **이름이 정확히 일치하는 리뷰어가 딱 한 명**일 때만(동명이인이면 포기 — 아무에게나 보내지 않는다).
  - ★ **못 찾으면 사유를 구분해 말한다**(행 미연결 / 그 행 연락처 칸 빔 / 동명이인 N명) — "왜 못 보냈는지"를 모르면 담당자가 손쓸 수가 없다.
  - **`GET /api/trackb/review-inspect/notify-check`**(건별 확인과 같은 게이트, **phone8 미반환** — 화면에 PII 를 늘리지 않는다): 팝업이 열릴 때 1회 확인해 못 보내는 건이면 **전송 체크를 끄고 사유를 표시**한다(누른 뒤에야 알던 것을 앞당김). ★ **확인 실패는 아무것도 바꾸지 않는다**(fail-open — 모른다고 전송을 끄면 보낼 수 있는 건까지 조용히 안 나간다).
- ★★ **"한 캡처에 여러 리뷰" — 같은 지문이라고 다 중복이 아니다 (실사고 2026-08-06)**: 리뷰어가 두 작업(노티드 두바이 쫀득쿠키 / 슈가베어 우유크림 폭탄떡)에 동시 참여했고 쿠팡 **리뷰관리 화면에 두 리뷰가 나란히** 보여 캡처 한 장으로 두 건을 냈다 — **리뷰어가 맞게 행동한 것**인데 검수가 "중복파일" 불량으로 처리했다.
  - ★★ **판정 = `classifyDuplicateContext` 세 갈래**: ㉮ **같은 작업** 안 재사용 = `fail` / ㉯ **다른 리뷰어** = `fail`(도용) — 둘 다 **완화 금지** / ㉰ **같은 리뷰어 + 다른 작업** = `warn`(사람이 본다), 그 캡처에서 **두 작업의 기대 상품명이 모두** 읽히고 **서로 다르면** `pass`(근거 있는 자동 통과).
  - ★ **자동 통과는 근거가 있을 때만** — 근거 없이 열면 "다른 캠페인에 같은 캡처 돌려쓰기"(진짜 재사용)까지 통과한다. **같은 상품(차수 재참여)은 근거로 인정하지 않는다.** ★ 근거를 못 찾아도 **`fail` 로 되돌리지 않는다**(정상 리뷰어를 불량으로 모는 쪽이 더 나쁘다). ★ 다른 작업 기대값 **조회 실패는 판정을 바꾸지 않는다**(모르면 완화하지 않는다).
  - ★★ **카드가 주 조치를 뒤집는다** — ㉰ 는 `[✓ 정상 — 한 캡처에 여러 리뷰]` 가 주 조치이고 `[🗑 중복파일 제거]`는 보조로 남는다(제거를 주 조치로 두면 정상 제출을 지우는 쪽으로 손이 간다). 유형 라벨도 `📄 같은 캡처 — 다른 작업에도 제출` 로 단정하지 않고, 근거 문장이 사정을 설명한다. ★ **제거 게이트는 `matchFileId`(근거) 하나** — 심각도(fail/warn)를 게이트로 쓰면 완화된 건을 사람이 못 지운다.
  - ★ **리뷰어 첨부 안내도 함께 고쳤다** — 다른 작업 건에는 "한 화면에 두 리뷰가 같이 보이는 캡처라면 그대로 제출해도 괜찮다"를 덧붙인다(맞게 한 리뷰어가 "다른 화면을 캡처하세요"만 보고 헤매지 않게).
  - ⚠ **이미 불량으로 기록된 과거 건은 자동으로 안 바뀐다** — 리뷰검수 `[♻ 재검수]`(`reinspectTab`)로 소급 재판정한다.
  - 회귀가드 `tests/duplicateMultiReview.test.js`(세 갈래·근거 한계·조회 실패 **실행** + 배선, **변이시험 검출 확인**)·`tests/inspectRejectNotify.test.js`(케이스 확장 — 4갈래 폴백·우선순위·사유 구분).
- **리뷰검수 화면 정리(사용자 확정 2026-08-06)**: ① **유형별 일괄 재검수** — 유형 칩(예 `📵 리뷰화면 아님`)을 고른 상태의 `[♻ …재검수]`는 **그 유형 건들만** 다시 검수한다(작업 선택 없이도). ★★ **유형 판정은 화면(`_riIssueTypes`) 단일 출처** — 서버에 유형 조건을 다시 만들면 "칩 건수 ≠ 재검수 대상"으로 갈라지므로 서버는 **파일ID 목록(`fileIds`)만** 받는다(중복제거·상한 500·빈 목록이면 쿼리 0). 되돌리는 대상은 어느 경로든 `fail/suspect/unverifiable` 뿐(확인됨·통과 보존). ★ 버튼 **라벨이 대상을 말한다**(무엇이 돌지 모르는 버튼 금지). ② `[↻ 과거분 검수]`·③ `[CSV]` **제거**(기능 불필요) — 화면에서만 없앴고 **서버 라우트·10분 크론은 그대로**(되살리기 쉽게). ④ **상태 칩은 `[의심＋불량]`·`[확인됨]` 둘뿐** — 이 화면의 목적이 "의심·불량만 모아 보기"라 통과·미검수·전체는 볼 것을 묻는다. ★ **`[확인됨]`은 남긴다** — 처리한 건을 되돌아보는 **유일한 경로**다(서버 status 필터는 무변경이라 필요해지면 칩만 되살리면 된다). ★ 재검수 진입은 셋(상단 버튼 · 카드 안내 · 카드 `[⋯]`)이지만 **실행부는 한 벌**(`riReinspectTab`). 회귀가드 `tests/inspectViewTrim.test.js`(스텁 pool 로 서비스 실행 + 배선 + 흔적 0) + 실브라우저 실행검증.
- **리뷰검수 화면 정렬·인터랙션(사용자 확정 2026-08-06)**: ★★ **헤더 버튼바(`#rihead .mh`) 캡 = 카드 영역(`.ricards`) 캡과 같은 값(1400px)** — 캡이 없으면 `.sp{flex:1}` 이 남는 폭을 다 먹어 버튼이 카드보다 한참 오른쪽에 뜬다(작업오더·모집공고와 같은 처리). 두 숫자를 따로 적어 두면 한쪽만 바뀌어 조용히 어긋나므로 **회귀가드가 값 일치와 5장/줄**(5×268+4×11=1384 < 1400 < 6장 문턱 1663)을 고정. `.ovnote` 도 같은 폭. ★ **버튼 인터랙션은 틴트 농도·테두리·1px 눌림만** — 변형별 색(m-red/m-green…)을 다시 적지 않고 `filter:brightness` 로 공통 처리하고, **호버에서 채움으로 바뀌지 않는다**(빨간 벽 방지 규율 유지). `prefers-reduced-motion` 이면 이동·전환만 끄고 색 변화는 남긴다. ★★ **확인 팝업·[⋯] 메뉴는 바깥 클릭으로 닫지 않는다** — 입력해 둔 반려 사유가 실수 클릭 한 번에 날아가던 것을 막는다. 대신 **닫는 길([취소]/[닫기])은 반드시 남긴다**(못 닫는 팝업 금지 — 회귀가드가 고정).
- ★★ **“구매확정 작업인데 리뷰검수가 불량” — 원인이 화면에 안 보이던 문제 (실사고 2026-08-06)**: 이노크아든_면마스크는 구매확정 작업이고 구매확정 캡처가 제출되는데 검수가 `리뷰 화면이 아닙니다` 불량을 냈다. 원인은 **리뷰타입 판정이 null** — `tab_configs.review_type` 에 **배송유형 `실배송`** 이 들어 있고(접수 업서트가 `COALESCE(NULLIF(...))` 라 기존 값을 보존하므로 뒤늦은 작업오더 값이 덮지 않는다) 공고에도 리뷰타입이 없었다. `resolveReviewType` 은 실배송/빈박스를 **배송유형이라 null 로 떨어뜨리므로** `_okKinds` 가 `['review']` 가 되어 정상 제출이 전건 불량이 됐다.
  - ★★ **판정을 바꾸지 않았다** — 고친 것은 **“왜 그렇게 판정했는지가 어디에도 안 보이던 것”**. `reviewTypeContext.reviewTypeDetailsForTabs` 가 판정 + **근거값(`tabRaw`/`campRaw`)** 을 **한 쿼리로**(N+1 금지, 단건 조회와 **같은 LATERAL 최신 1행** 모양) 돌려주고 `/review-inspect/list` 가 실어 보낸다. 판정 규칙 사본은 여전히 0(`utils/reviewType` 단일 출처).
  - **상세 팝업 ‘리뷰 화면·채널’ 줄**이 문장으로 말한다 — 구매확정 작업이면 `구매확정 완료 화면도 정상으로 봅니다` / 미지정이면 `리뷰 화면을 기대합니다` + 탭 값이 실배송·빈박스면 **`배송유형이라 리뷰타입으로 인정되지 않습니다`** 까지.
  - ★ **카드 조치 안내는 한 케이스에만** — `구매확정 캡처 + 리뷰타입 미설정 + 불량` 일 때만 `공고에서 구매확정으로 저장 → [♻ 재검수]`. 현금영수증 판별·중복 등 **조치가 다른 유형에는 뜨지 않는다**(늑대소년 방지).
  - ★★ **리뷰타입 정보가 없으면(구버전 백엔드·조회 실패) 아무 말도 하지 않는다** — 없는 것을 “미지정”으로 꾸미면 멀쩡한 구매확정 탭에도 잘못된 안내가 뜬다. 목록 조회는 fail-soft(표시만 생략).
  - ★★ **자동 인식 복구(접수 업서트)**: 신규 접수는 작업오더의 `구매확정` 이 `tab_configs.review_type` 에 그대로 들어가 **자동으로 통과**되지만, **이미 값이 있는 탭**은 `COALESCE(NULLIF(기존,''), 새값)` 이 무조건 보존해 영영 안 채워졌다 → **리뷰타입 칸에 든 값이 배송유형(`실배송`·`빈박스`)일 때만** 작업오더 값으로 **교체**하고 그 값은 `delivery_type` 으로 **이관**(★ blank-only — 이미 채운 배송유형은 안 덮는다). **유효한 리뷰타입·관리자 설정은 종전대로 보존**(2차 접수가 덮지 않는다는 기존 계약 불변). 어휘는 `LEGACY_DELIVERY_VALUES` 단일 출처(`$12` 파라미터 — SQL 에 사본 금지). 회귀가드 `tests/acceptReviewTypeRepair.test.js`(**진짜 PG16 으로 네 경우·멱등·판정 연결 실행**, 변이시험 검출 확인).
  - ★★ **공고에 저장해 뒀는데도 검수가 못 보던 경로 둘**(SQL 단일 출처 `reviewTypeContext.CAMPAIGN_REVIEW_TYPE_LATERAL` — 단건·배치·`loadTabExpectations` 공용): ㉮ **탭 리네임** — `linked_tab_name` 이름 매칭뿐이라 이름이 바뀌면 공고를 못 찾아 설정이 조용히 풀렸다 → **gid 폴백**(★ 빈 gid 는 키를 만들지 않는다) ㉯ **차수 재발행** — 한 탭에 공고가 여러 개일 때 `ORDER BY created_at DESC LIMIT 1` 이 **최신 공고의 NULL** 을 집어 옛 공고의 `구매확정` 을 가렸다 → **값이 있는 최신 공고**(`COALESCE(review_type,'') <> ''`). '미지정'은 관리자가 **정하지 않은 것**이라 가리지 않는다(최신이 `포토` 처럼 **명시**돼 있으면 최신이 그대로 이긴다). 완화 방향이라 안전 — 반대가 이번 사고였다. ★ **채널 LATERAL 은 분리 유지**(`channel`+`channel_custom` 짝이 한 행에서 와야 한다).
  - ⚠ **여전히 자동이 아닌 경우**(문서화): ㉮ 작업오더에 리뷰타입 미기재 → 미지정(추측하지 않는다) ㉯ **혼합 오더**(`혼합(포토 10건, 구매확정 5건)`)는 어느 행이 무슨 유형인지 시트에만 있어 `mixed` → null(설계상 의도 — "혼합이니까 일단 리뷰"로 접으면 구매확정 행이 잘못 검수된다) ㉰ 접수를 거치지 않고 만든 탭. 이 셋은 **공고 모달에서 리뷰타입을 지정**해 해결한다.
  - **운영 조치**: ① 공고 모달(또는 탭 설정)에서 리뷰타입 = **구매확정** 저장 ② 그 작업에서 **[♻ 재검수]** — `reinspectTab` 이 fail/suspect/unverifiable 만 pending 으로 되돌려 새 기준으로 재판정한다(resolved·pass 는 보존).
  - 회귀가드 `tests/inspectReviewTypeHint.test.js`(판정 규칙·배치 조회·fail-soft **실행** + 헬퍼 vm 실행 + 배선, **변이시험 검출 확인**) + 브라우저 실행검증(카드 안내·상세 문장·구매확정 전환·구버전 침묵).
- ★★ **“무엇과 중복인지”는 카드에서 바로 읽혀야 한다 — 관계 줄 (시안 `frontend/docs/design-inspect-duplicate-relation.html`, 사용자 확정 2026-08-06)**: 종전엔 `같은 파일이 이미 제출됨 — 〈긴 작업명〉 A 리뷰어 31행` 처럼 **줄 번호가 문장 끝에 묻혀** 있어, 카드를 열어야 짝을 알 수 있었다.
  - ★★ **렌더러 한 벌 = `_riRelPairs`/`_riRelHtml`** — 카드 · `[🗑 중복파일 제거]` 확인 팝업 · 상세 팝업이 **같은 것**을 그린다(사본을 두면 화면마다 좌우가 뒤집히거나 한쪽만 안 바뀐다).
  - ★ **왼쪽 = 지금 보고 있는 건 / 오른쪽 = 이미 있던 것**(방향 고정). 같은 작업·같은 리뷰어면 **이름·작업명을 반복하지 않는다**(카드 제목에 이미 있다) · 다른 리뷰어면 양쪽 이름 + 🔴 도용 신호 · 다른 작업이면 오른쪽에 작업명 + “한 화면에 여러 리뷰일 수 있음”.
  - ★★ **같은 짝을 가리키는 사유는 한 줄로 합친다**(파일 중복 + 본문 겹침이 둘 다 31행 → 관계 줄 1개 + `본문 100% 겹침` 설명). **짝이 다르면 두 줄**(합치면 거짓말이 된다).
  - ★★ **짝의 줄 번호를 모르면 “줄 번호 없음”이라고 적는다** — 빈칸으로 두면 “같은 줄”로 오해된다(양쪽 모두 적용).
  - ★★ **`_riReasons(c, {skipRel:true})` 는 카드 전용** — 관계 줄이 말하는 두 사유를 빼서 같은 정보를 두 번 적지 않는다. **반려 사유 프리필은 옵션 없이 부른다**(리뷰어에게 가는 문장에는 관계 줄이 없어 문장으로 다 말해야 한다 — 옵션이 리뷰어 문구를 갉아먹지 않게 회귀가드가 고정).
  - ★ 확인 팝업은 사진 위에 `제거 40행 ▶ 보존 31행` + 조치 문장·사진 캡션에도 줄 번호. ★ **줄 번호는 관리자 화면 전용** — 리뷰어 채팅 카드·안내 문구에는 넣지 않는다(참여 순번·상품·제출일로 가리킨다).
  - 회귀가드 `tests/inspectRelationLine.test.js`(재료·렌더를 **vm 으로 실제 실행**(네 경우 + 줄 번호 없음 + XSS) + 배선, **변이시험 검출 확인**) + 브라우저 실행검증(카드 4종 실렌더·관계 줄 높이 실측·확인/상세 팝업). ⚠ 이 변경으로 `inspectTypeActions` 가드 2건(_riReasons 시그니처·팝업 캡션)을 함께 갱신(검사 의미 불변).

### 현금영수증 2·3단계 — 이중 슬롯 + AI 검수
- **2단계(이중 슬롯)**: 신규 컬럼 없음 — migration 034의 **기존 캡처 슬롯 인프라를 재사용**한다(034 주석이 이미 "리뷰 1건 + 현금영수증 1건"을 예시로 설계). 달라진 건 **현영 탭은 `capture_slots` 수동 설정 없이도 자동 2슬롯**이 된다는 것.
- ★★ **슬롯 판정의 단일 출처 = `utils/captureSlots.js`**(`effectiveCaptureSlots`/`requiredSlotKeys`/`slotLabel`/`isCashReceiptIncome`). 규칙: `capture_slots` 설정 있으면 그대로(관리자 명시가 최우선) → 없고 `income_type`에 '현영' 있으면 리뷰+현금영수증 2슬롯 → 그 외 단일 `review`(기존 동작). **소비처 4곳이 전부 이 함수만 쓴다** — ① `search.service`(리뷰어가 그릴 슬롯) ② `submit.routes`(완료 판정 = 필요 슬롯 ⊆ 제출 슬롯) ③ `diag.routes` review-upload(슬롯 서브폴더 라벨) ④ `reviewEdit.routes`(교체요청 라벨 2곳). **하나만 어긋나도 "슬롯 2개인데 1장에 완료" 또는 파일이 다른 폴더로 흩어진다** → 각 쿼리가 `income_type`을 함께 읽어야 함(빠뜨리면 현영 자동 슬롯을 못 봄).
- **3단계(AI 검수)**: `gemini.service.classifySubmissionImage`(기존 캐시·타임아웃·라운드로빈 골격 재사용, 캐시키 `classify:` 접두로 extract와 분리) → `captureVerify.service`가 정책 담당. 리뷰 슬롯엔 리뷰 화면(별점·리뷰본문), 영수증 슬롯엔 영수증(국세청·승인번호·사업자번호)이 와야 통과. 영수증은 **사업자번호가 회사 번호와 다르면 경고**(발행 대상 오류).
- ★★ **fail-open (완화 금지)**: AI 미설정·오류·확신도 `< CAPTURE_VERIFY_MIN_CONFIDENCE`(0.7)는 **통과**. 오탐으로 정당한 제출이 막히는 쪽이 막으려던 사고보다 나쁘다(구매양식 신원게이트와 같은 규율). 검수 예외가 업로드 결과를 뒤집지 않고(`catch → verdict = null`), 알림 기록 실패도 무시. env `CAPTURE_VERIFY=0`으로 끌 수 있음.
- **불일치 처리**: 업로드는 **이미 완료된 상태**로 두고 슬롯 자리에 인라인 경고(`_csShowVerdict`) + 상태를 "⚠ 확인 필요"로. 다시 올리면 교체되고, 그대로 두면 `reviewer_event_logs`에 `capture_mismatch`로 남아 관리자가 확인한다. **제출 차단 없음**.
- **"확실히 아님" 2단 승격(정책 ① 유도+관리자알림)**: 확신 `≥ CAPTURE_VERIFY_SURE_CONFIDENCE`(0.9)면 `verdict.sure` → 로그 **`critical`** = 관리자 대시보드 좌측하단 빨간 알림 + SSE, 리뷰어 경고에 "담당자에게도 함께 전달했어요" 한 줄(재첨부 유도). 0.7~0.9는 **warn**(리뷰웹시스템[3버전] 로그에만) — 애매한 판정까지 빨간 알림을 띄우면 주문 소실 같은 급한 건이 묻힌다(늑대소년 방지). ★ **사업자번호 불일치는 sure로 안 올린다**(숫자 OCR 한 자리 오독으로 갈리는 판정). ★ **도배 방지**: 같은 `(sheet_id, tab_name, context->>'slotKey', context->>'row')`에 미해결 알림이 있으면 새로 안 쌓음(062의 부분유니크는 `order_submission_id` 기준이라 캡처 알림엔 안 걸림). ★ **자동 해결**: 같은 자리에 형식이 맞는 캡처가 오면 `auto:recaptured`로 닫음(옛 로그는 context에 행이 없어 수동 [확인]). 알림 경로는 **판정과 try 분리** — 알림 기록이 실패해도 리뷰어 화면의 재첨부 안내는 남는다. 신규 스키마 0(기존 `idx_reviewer_logs_tab` 사용).
- 회귀가드 `tests/captureSlotVerify.test.js`(슬롯 파생 8 + 소비처 배선 5 + 검수 정책 8(스텁 Gemini로 실제 핸들러 호출) + 배선 6)·`tests/captureVerifyNotify.test.js`(확신 구간·승격·도배·자동해결·fail-soft를 스텁 pool로 실제 핸들러 호출).

### 리뷰 자동검수 — 1차 필터(첨부 즉시) + 2차 검수(제출 후), migration 081
- **설계 문서**: `frontend/docs/prd-review-auto-inspection.html`(기술) · `리뷰자동검수_와이어프레임.html`(화면 9종·채널 4종 판별 단서).
- ★★ **검수는 두 층**: **1차 = 캡처를 첨부하는 순간**(리뷰 화면인가·어느 채널인가) / **2차 = 제출 이후**(상품명·같은 파일·본문 겹침). 1차를 첨부 시점에 두는 이유 = 그때는 **Drive에도 원장에도 아무것도 없어 파일만 바꾸면 끝**이지만, 제출 후엔 교체요청→리뷰어 재제출 왕복이 생긴다. 상품명·중복·유사도를 1차로 올리지 않는 이유 = 기준 미등록 탭이 많아 오탐 위험이 크고, 중복·겹침은 **저장된 다른 제출과 대조해야** 나와 첨부 시점엔 비교 대상 자체가 없다.
- ★★ **AI 콜 순증 0**: `classifySubmissionImage` 캐시 키가 **이미지 해시**라 1차 판정이 제출 시 캐시 히트된다. 게다가 `review-upload`는 기존 `verifyCapture`가 같은 이미지로 이미 classify를 돌려 **캐시를 데워 놓으므로**, 2차 검수의 classify는 항상 캐시 히트다. 프롬프트는 **필드 추가만**(kind 판정 3줄은 한 글자도 불변 — 기존 슬롯 검수가 그대로 쓴다). 캐시 접두는 `classify:`→`classify2:`(옛 캐시가 새 필드 없이 히트하는 것 차단, 회귀가드는 `classify\d*:`로 버전 상향 허용).
- ★★ **차단은 한 갈래뿐**(완화·확대 금지): `kind='other'` AND 확신 ≥ `REVIEW_PRECHECK_BLOCK_CONFIDENCE`(0.9). **채널 불일치·상품명 불일치·중복·유사도는 절대 차단하지 않는다**(경고 또는 사후 분류) — 탭 채널은 상품 URL 호스트 **추정값**이라 우리 쪽이 틀릴 수 있고, 우리 오류로 리뷰어를 막으면 안 된다. **카카오메이커스는 채널 전체가 잠금 제외**(`REVIEW_PRECHECK_EXEMPT`) — 별점 있는 후기 화면과 **별점 없는 피드형**이 공존해 리뷰어가 어느 쪽을 캡처할지 통제할 수 없다.
- ★★ **우회로 제거 금지**: 같은 슬롯에서 2회 잠기면 "제가 올린 게 맞습니다" 체크가 열린다(`_PRE_BLOCK_LIMIT`). AI 오판으로 리뷰어가 **제출 자체를 못 하는 상태 = 참여 소각**은 불량 1건을 놓치는 것과 비교가 안 되는 사고다. fail-open 전면 적용 — AI 미설정·오류·타임아웃·저확신·네트워크 실패는 전부 **무판정 통과**(화면에 아무것도 안 뜸), precheck 라우트는 어떤 예외에도 200 통과 응답.
- ★ **첨부 경로 3곳 전부**에 1차 필터를 건다(`search-app.js`): 단일 행(`addFiles`) · 다건(`_mrAddFiles`) · 캡처 슬롯(`_csAddFiles`). 한 곳만 걸면 **대부분의 탭이 필터 밖**에 남는다(슬롯 모드는 현영 2슬롯 탭 전용). 제출 게이트도 2곳(`submitReview`·`_submitReviewSlots`). 관리자 미리보기(`_PREVIEW_MODE`)에서는 미실행. 안내문에 `stopPropagation` 필수 — 드롭존 onclick이 파일 선택창을 열어 **우회 체크를 못 누르는** 실측 함정.
- ★★ **본문 유사도 판정식은 `similarity` 단독이면 무력하다**(로컬 PG16 실측): 가장 흔한 복붙 수법인 "원문 + 문장 한두 개 추가"가 **0.859 / 0.718** 로 90% 기준을 빠져나간다. → `GREATEST(similarity, word_similarity 양방향)`으로 잡되(둘 다 1.000), word_similarity만 쓰면 **짧은 문구가 긴 글에 통째로 포함될 때 1.000 오탐**이 나므로 **길이비 가드**(`REVIEW_INSPECT_SIM_LEN_RATIO` 0.5) — 길이가 비슷할 때만 포함관계를 본다(오탐 케이스 길이비 0.29 → 0.298로 하락). 짧은 글(`REVIEW_INSPECT_MIN_TEXT` 30자 미만)은 애초에 비교 안 함(관용구 오탐).
- **중복 판정**: 업로드 시 SHA-256을 `review_submissions.file_hash`에 **INSERT와 함께** 기록(나중 UPDATE로 미루면 그 사이 올라온 파일이 대조 대상을 놓친다). ★ **같은 (시트·탭·행·리뷰어) 재업로드는 중복이 아니다** — Drive는 재업로드마다 새 file_id를 만들어 이걸 잡으면 정상 재제출이 전부 불량이 된다.
- **제출자명 검수는 하지 않는다**(수집만): 리뷰 화면 작성자는 `김**`·닉네임이라 실명 대조가 구조적으로 불가능 — 억지로 판정하면 멀쩡한 제출이 무더기로 의심 처리된다.
- **킬스위치**: `REVIEW_PRECHECK=0`(1차 전체) · `REVIEW_PRECHECK_BLOCK=0`(**판정만 하고 잠그지 않는 관측 모드 — 운영 초기 권장**) · `REVIEW_INSPECT=0`(2차 전체).
- **기대 상품명 공급(조건 ②를 살리는 재료)**: 우선순위 = ① 탭에 직접 적어둔 `tab_configs.inspect_product_names` → ② 연결된 작업오더에서 파생(`productNamesFromWorkOrder` — `product_options_json`의 `name` + `product_option` 텍스트 줄 단위, 가격 꼬리 제거, **"옵션 없음/단일/해당없음"류 서술 제외**). ①이 있으면 ②는 안 본다(관리자 명시가 최우선, 빈 값 저장 = 해제 → ②로 복귀). **후보는 넉넉히** 모은다 — 하나라도 맞으면 통과라 후보가 많을수록 오탐이 줄고 미탐만 조금 는다(이 기능의 실패 선호와 일치). 기대값이 없으면 대조 자체를 skip 하고 **화면이 그 사실을 알려준다**(조건이 죽어 있는 걸 모르면 안 된다).
- ★★ **작업오더 링크는 `ORDER BY`로 우선순위를 명시**(실측 버그): Track B 링크(`trackb_work_order_links`)와 `work_orders.linked_tab_*` 폴백을 **`OR`로만 묶으면** 링크가 있어도 `created_at`이 더 최근인 폴백 오더가 이겨 **엉뚱한 상품명으로 대조**한다. `CASE WHEN EXISTS(링크) THEN 0 ELSE 1 END`을 정렬 첫 키로 둔다. `work_orders`는 **읽기만**(Track A 승인 흐름이 `linked_tab_*`를 보고 분기).
- **관리자 화면(M3)**: 리뷰웹시스템[3버전] **"리뷰검수" 탭**(`/api/trackb/review-inspect/*` — list·resolve·product-names GET/POST). 권한은 **이미지 교체요청 탭과 동일**(`_reInternal`: master/admin 전체 · staff 담당 탭만 · **광고주 차단** — 검수 근거에 리뷰어 실명·본문 OCR이 실린다). 탭 미지정 staff는 `scopedActiveTabs`로 거르고 집계도 같은 기준으로 다시 센다. 기본 필터 = **의심+불량**(통과까지 나열하면 볼 것이 묻힌다), 카드에 **판정 근거를 문장으로**(어느 탭 누구 것과 겹쳤는지 — 열어보지 않고 판단되게), 중복 건은 **나란히 보기**, [확인 처리]는 `status='resolved'`로 종결하되 **원판정 `checks`는 보존**(오탐 누적이 임계값 조정의 근거).
- **판별 예시이미지(few-shot, 9슬롯)**: `utils/reviewSampleChannels.js` 단일 출처(채널 4종 × PC/모바일 + **`kakao_feed`**). 관리자가 등록하면 판정 시 프롬프트에 실물 예시를 동봉해 채널 판별 정확도가 오른다. ★★ **캐시 키에 예시 지문(`sampleSig`)을 반드시 섞는다** — 안 그러면 예시를 등록·교체해도 옛 판정이 히트해 등록이 무의미해진다. ★★ **호출부가 모두 같은 samples를 넘겨야 한다** — `review-upload`의 `verifyCapture`와 2차 검수가 다른 값을 쓰면 캐시 키가 갈려 **같은 이미지에 AI 콜이 두 번** 나간다(순증 0 전제가 깨짐). 그래서 예시는 **파일 루프 밖에서 1회** 준비해 양쪽에 같이 준다. ★ 채널을 모르면 **미동봉**(9장을 다 보내면 토큰·지연이 커져 첨부 즉시 판별의 응답성을 해친다), 등록된 예시가 없으면 빈 배열 = **오늘과 동작 동일**. 저장은 adminOrMaster(전사 설정), 조회는 내부인.
- **M2 배치 스윕**(cron `4-59/10`, `withJobLock('review_inspect_sweep')`): ① `pending` 재시도 ② **검수 이력 없는 과거 제출분**(최근 `REVIEW_INSPECT_BACKFILL_DAYS` 30일) 따라잡기. 건당 Drive 다운로드 1회 + AI 1콜, **사이클 캡** `REVIEW_INSPECT_BATCH`(20). **시트 API 무접촉**. ★ 재시도 상한 `REVIEW_INSPECT_MAX_ATTEMPTS`(3) 초과는 `unverifiable`로 종결(무한 재시도 금지 — 스마트빌드 실패 시트와 같은 규율). ★ 과거분은 원장에 지문이 없으므로 이번에 계산해 채운다(이후 중복 대조의 재료). ★ **절대 throw 안 함**(cron이 죽으면 안 된다). 수동 실행 `POST /review-inspect/sweep`(adminOrMaster, 같은 락).
- **M4 확장**: ① **전역 유사도** `REVIEW_INSPECT_SIM_SCOPE=global`(기본 `tab`) — 다른 캠페인에 같은 글을 돌려쓰는 것까지 잡지만 비교 대상이 전체로 늘어 무거워진다. ② **작성자 표기 재사용**(`findAuthorReuse`) — 실명 대조가 아니라 **같은 마스킹 표기가 다른 리뷰어 제출에 반복 등장**하는 패턴만 본다(한 계정으로 여러 명 대행 신호). **경고 전용·차단 없음**, 같은 리뷰어 본인 제출은 제외, 2자 미만 표기는 비교 안 함. ③ **검수 결과 CSV**(`GET /review-inspect/export.csv`, UTF-8 BOM, 판정사유 문장화).
- ★ **지각 해시(dHash)는 구현하지 않았다 — 도메인 부적합**: 같은 캠페인의 리뷰 캡처는 **상품 썸네일과 화면 레이아웃을 공유**해 픽셀 해시가 서로 비슷하게 나온다(구조적 오탐). 게다가 잡으려던 "크롭·밝기 조정한 재사용"은 **텍스트가 같아 본문 유사도가 이미 잡는다**. 순증 가치가 낮고 이미지 디코더 의존성까지 필요해 **의도적으로 제외**했다(다시 검토할 땐 이 두 가지를 먼저 반박할 것).
- 회귀가드 `tests/reviewInspect.test.js`(97 + PGTEST_URL 시 11 = 케이스 — 정책 순수함수 실행 · 스텁 pool 대조 · **라우터 스택 실검사** · 배선 · **진짜 PG로 유사도 판정식·링크 우선순위 검증**). ★ 유사도·동적 WHERE·링크 우선순위는 **스텁으로 못 잡는다**: `PGTEST_URL=postgres://... node tests/reviewInspect.test.js`. ★ trackB.routes를 require하면 DB 풀 핸들이 열려 프로세스가 안 끝난다 → 끝에 `process.exit(0)`(레포 관용구).
- ★★ **화면 렌더의 프리변수 = 무한로딩 (실측 사고)**: 리뷰검수 메뉴가 **영원히 "불러오는 중…"** 이었는데 목록 API 는 **200 정상**이었다. 원인 = `_riRenderHead`(workdesk.html)가 자기 스코프에 없는 **`isAdmin`** 을 참조(그 const 는 `renderNav` 안에만 있다) → 렌더가 ReferenceError → `_loadInspect` 의 promise 가 reject → 아무도 안 잡아 **깔아 둔 자리표시자가 그대로 남는다**. 사용자에겐 서버 장애로 보이지만 서버 로그엔 아무 흔적이 없다(#441 에서 [🖼 판별 예시]·[↻ 과거분 검수] 버튼과 함께 들어옴). ★ **렌더 함수는 역할을 자기 스코프에서 다시 구한다** — 다른 함수의 const 에 기대지 않는다(레포의 다른 6곳도 전부 지역 const 방식). ★ **로딩 자리표시자를 까는 함수는 어떤 예외에도 화면을 종결**시킨다(`_loadInspect` try/catch + [다시 시도]) — 조용히 매달리는 화면 금지. ★ **grep 으로는 못 잡는다**(`isAdmin` 문자열은 멀쩡히 있고, 문제는 그 스코프에서 해석이 안 되는 것) → 회귀가드 `tests/workdeskInspectView.test.js`(렌더 블록을 **vm 으로 꺼내 실제 실행**, sandbox 에 `isAdmin` 전역을 **일부러 두지 않아** 프리변수가 다시 들어오면 그 자리에서 터진다 + "정상 응답에 오류 문구가 안 뜬다"까지 확인 = fail-soft catch 가 회귀를 삼키지 못하게). 변이시험 2종(선언 제거·런타임 전용 프리변수)으로 검출 확인.
- **상세 팝업 좌우 이동(← →)**: 검수는 여러 건을 연달아 훑는 일이라 "닫고 → 다음 카드 찾고 → 다시 클릭"의 왕복을 없앤다. 헤더에 `‹ n / m ›` + 방향키(`riStepDetail`), 순서는 화면의 카드 순서(`STATE.ri`) 그대로. ★ **끝에서 순환하지 않는다**(처음으로 튀면 어디까지 봤는지 잃는다) — 경계에서는 버튼도 `disabled`. ★★ **`#riModal` 은 [판별 예시] 모달과 같은 요소를 재사용**하므로 `dataset.riKind`(`detail`|`samples`)로 구분해 **상세일 때만** 방향키를 가로챈다(안 그러면 예시 모달의 입력칸 조작이 막힌다). ★★ **리스너는 최상위에 한 번만** 등록(팝업을 열 때마다 걸면 겹쳐 쌓여 한 번 눌러 여러 칸 건너뛴다 — 회귀가드가 등록 횟수와 최상위 여부를 고정). ★ 입력 중(input/textarea/select/contenteditable)·조합키(Ctrl/Alt/Cmd)는 가로채지 않는다(작업보드 그리드 방향키와 같은 규율). Esc = 닫기. 회귀가드는 같은 파일 C절(케이스 확장, 변이시험 검출 확인).

### ★ [상품 페이지 열기] = 링크유입 전용 (유입가이드 무력화 방지)
- 작업내용 카드의 `🔗 상품 페이지 열기` 버튼은 **링크유입일 때만** 노출한다. 가이드유입 공고에 이 버튼이 뜨면 리뷰어가 상품 페이지로 바로 들어가, 검색어·경유 경로를 지정한 **유입가이드 첨부자료가 통째로 무의미해진다**(유입 실패 = 캠페인 목적 훼손).
- 판정(`campaign-workdetail.js`): `inflowType === 'link'` **또는** (`inflowType` 불명 **AND** 유입가이드 내용 없음). `inflowType`은 연결된 `work_orders.inflow_type`에서 오므로 **작업오더 없이 수동 생성한 공고는 항상 불명** — 종전 규칙(`불명이면 노출`)에서 이 공고들이 전부 새어나갔다. 가이드 유무는 `inflowGuideHtml` + 리뷰가이드에서 추출한 첨부 이미지로 판정(안내문 치환 **전에** 계산).
- 관리자 카드의 유입방식 칩(`campaign-cards.js` `_adminSpec`)도 **같은 기준**으로 판정 — 카드는 링크유입인데 리뷰어 화면엔 버튼이 없는 불일치 차단. 회귀가드 `tests/campWorkDetailShared.test.js`·`tests/campaignGuideView.test.js` ④.

### 오늘 마감 카드 — 아래로 정렬 + 회색 썸네일 + 재오픈 카운트다운
- 리뷰어 목록에서 **오늘 마감된 공고가 진행 중인 공고 사이에 섞여 있으면** 참여 가능한 공고를 놓친다 → `campaign-cards.js` `sortByAvailability`(공용)로 등급 정렬: 0 지금 참여 가능(open) / 1 곧 열림·잔여 대기(preopen·soft_full) / 2 오늘 마감(cutoff·daily_done) / 3 완전 종료(closed). **안정 정렬**이라 같은 등급 안에서는 서버 순서(⭐ 별표 우선노출·최신순)가 그대로 유지된다. 소비처 = `index.html` 홈 미리보기 · `recruit.html` 목록 · `renderInto`/`gridHtml`. **관리자 모집공고 탭은 미적용**(별표 순서가 관리 의도라 그대로 둔다).
- **마감 카드 표시**: 썸네일 위에 `.pt-ovl.pre`(반투명 어둠 + `backdrop-filter:grayscale(1)`)를 덮고 가운데에 흰 글씨로 **다시 열릴 때까지 카운트다운**. 같은 말을 하는 `금일 모집완료` 리본은 오버레이가 뜨면 겹치지 않는다(`dailyOverlay`).
- ★★ **카운트다운 기준은 `reopensAt`이지 `opensAt`이 아니다**: `opensAt`은 **오늘의** window_start(=이미 지난 시각)이고 **자율주문은 아예 null**이라 그대로 쓰면 카운트다운이 0에 붙는다. `computeCampaignState`가 daily_done 세 분기에 `reopensAt`을 따로 실어 준다 — 시트 일정이 있으면 **다음 진행일**, 없으면 내일. 시각은 window_start(자율주문은 KST 자정). 휴무일(rest_day)은 기존 `opensAt`과 같은 값. `opensAt` 시맨틱은 **건드리지 않았다**(카드 시간표기·오픈전 카운트다운·`_fmtHM` 소비처 무영향). 공개 뷰·관리자 목록 양쪽 응답에 실린다.
- 회귀가드 `tests/manualOrderExternal.test.js` F섹션(상태엔진 실행 + 프론트 배선).

### 관리자 모집공고 카드 = 리뷰어 카드 (단일 렌더러 · 고정 슬롯)
- 관리자 모집공고 탭의 카드를 **리뷰어 홈과 같은 함수**(`campaign-cards.js` `cardHtml(c, {admin:true})`)로 그린다 — 사본을 두면 두 화면이 계속 어긋나므로 단일 출처로 묶었다(작업내용 렌더러 공용화와 같은 규율). 리뷰어 응답엔 관리 레이어가 **내려가지 않는다**.
- **배치**: 리뷰어는 2열 고정(`.pcards-grid`, 앱 폭 고정), 관리자는 `.pcards-grid.pc-admin`으로 `auto-fill minmax(258px,1fr)` = 너비만큼 채움.
- **고정 슬롯 6칸**(썸네일 / 제목 / 메타 / 게이지 / 스펙 / 액션): 내용이 없어도 자리를 지켜 카드마다 같은 줄에 같은 정보가 온다. ★ **제목 `min-height:2.6em`**(line-height 1.3 × 2) — 2.13em으로 뒀다가 한 줄 제목 카드만 짧아져 아래가 전부 어긋난 실측 버그. 같은 행 높이 통일은 `align-items:stretch` + `.pcard{height:100%}` + 액션 `margin-top:auto`. 오늘 집계가 없는 공고(임시저장·오픈 전)도 게이지 자리에 "게시 전/오픈 전"이 들어간다.
- **게이지 분해**(관리자만): 파랑=제출확정, 빗금 주황=진행중(유효 홀드, `todayCount − holdNow = 확정`). 리뷰어는 단색 1구간 유지.
- **관제 뱃지 = 지각(late)만**(C안 확정): `late_order_id IS NOT NULL AND status IN ('expired','cancelled')` — 결제했는데 자리를 못 받은 건으로, 수동확정이 유일 구제라 **반드시 눌러야 하는** 상태. 수동확정하면 `submitted`가 되어 자동 제외. ⚠ **만료·취소는 세지 않는다** — 신청만 하고 안 산 건이라 자리가 이미 반환돼 할 일이 없고, 매일 숫자가 떠 있으면 급한 지각 건이 묻힌다(늑대소년 방지). 세 값은 서로 겹쳐(수동확정 = 만료 ∪ 취소 ⊇ 지각) 동시 표기 시 같은 사람이 최대 3번 계수됨.
- **스펙 줄**(2줄 고정): 유입방식(`landing_url` 유무 → 링크유입/**가이드유입**)·결제방식(`badges`)·총 모집 / 연결 탭. **칩 접힘(+N) 미사용** — 넘치면 `ovf` 클래스로 좌우 왕복 애니메이션(`@keyframes pcMarquee`, 이동거리 `chips.scrollWidth − wrap.clientWidth` 실측, **넘칠 때만** 동작=평상시 정지). `prefers-reduced-motion`이면 애니메이션 대신 스크롤. 총 모집 숫자는 `flex-shrink:0`으로 절대 안 밀림.
- **별표(⭐)는 채널·배송 배지와 같은 줄**(`pt-badges` 안) — 따로 절대배치하면 배송 배지를 가린다(실측 버그).
- **삭제는 카드에서 제거** → 헤더 [🗑 삭제 모드] 토글 + 선택식 일괄삭제(`toggleRecruitDelMode`/`deleteRecruitPicked`). 카드가 조밀해 오클릭으로 공고가 사라지는 것 방지. 삭제 모드 중엔 카운트다운 자동 새로고침 보류(선택 유실 방지).
- **서버**: `GET /api/campaign/admin/list`가 원본 행 + **리뷰어 목록과 동일한 상태 계산**(`computeCampaignState`, 두 화면이 다른 계산을 쓰면 "카드는 모집중인데 참여는 거부" 불일치 발생) + `ops{holdNow, todayHold, todaySubmitted, totalConfirmed, late}` 반환. 집계 실패는 fail-soft(목록은 뜬다).
- ★ **`cardHtml`이 `_injectStyles`를 호출**한다 — HTML만 쓰는 호출부(관리자 목록)에서 CSS 없이 폭을 재면 칩 넘침 판정이 어긋난다(실측 버그). 회귀가드 `tests/adminCardSync.test.js`.

### ★ 카드 액션 줄 = 주 버튼 3개 + [⋯] 더보기 (시안 C · 사용자 확정 2026-08-07)
- **사고**: 액션 버튼이 6개로 늘자 라벨이 잘리고 마지막 버튼이 「게시」 토글과 겹쳤다(사용자 신고). 실측 = 카드 266px → 액션 줄 244px → **토글 57px 고정** → 버튼 하나에 **27px** 인데 라벨은 35~48px 필요. 시안 = `frontend/docs/design-campaign-card-actions.html`(현재/A/B/C, `?v=A|B|C` 단독 보기, 가짜 데이터로 실제 동작).
- **주 줄 = `✏️ 수정 · 👁 보기 · 📡 관제 · [⋯]`**, 나머지 3종(`📄 연결된 시트 열기` · `📅 날짜별 인원 조절` · `🚫 참여 리뷰어 관리`)은 `_ACT_MORE[c.id]` 로 모아 더보기 메뉴에. ★★ **버튼이 더 늘어도 [⋯] 안으로 들어간다** — 주 줄에 직접 추가하면 같은 방식으로 다시 깨진다(회귀가드가 주 줄 버튼 수를 고정). ★ 좁은 칸이 아니라 메뉴라 **이름을 온전히** 쓴다(줄임말이 필요 없어졌다).
- ★ **관제의 빨간 배지(지각 = 수동확정 필요)는 주 줄에 남긴다** — 목록을 훑다가 바로 보여야 하는 유일한 신호다. 라벨은 `<span class="lbl">` 로 감싸 말줄임·넘침 측정을 한 지점으로 모으고(`.bdg` 는 버튼 밖이라 측정에서 빠진다), `[⋯]` 는 `flex:0 0 auto;width:26px` 로 글자 폭을 안 먹는다.
- ★★ **메뉴는 body 직속**(`.pcmenu`) — `.pcard{overflow:hidden}` 안에 그리면 **통째로 잘린다**(오버레이를 스크롤 컨테이너 밖에 두는 레포 규율과 같은 이유). 색은 **리터럴**(테마 없는 화면에도 얹힌다), onclick 은 **id 만**(`CampCards._more('${id}',this)`).
- ★★ **항목을 고르면 닫는 리스너는 메뉴 자신에게 캡처 단계로** 건다 — 항목 onclick 이 `stopPropagation`(카드 클릭 차단)을 하므로 **document 리스너까지 못 간다**(정적으로는 "닫는 코드가 있다"로 보이지만 실제로는 안 닫힌다 — 실브라우저가 잡았다). 닫기는 `setTimeout 0` 으로 미뤄 항목 동작이 먼저 끝나게 한다.
- ★ **리스너는 최상위 1회만**(`_menuBound` — 열 때마다 걸면 겹쳐 쌓인다) · 스크롤·리사이즈는 닫는다(body 직속이라 카드를 안 따라간다) · **`renderInto` 가 `_closeMenu`**(목록을 다시 그리면 가리키던 카드가 사라진다) · 위가 좁으면 **아래로 뒤집는다**(화면 밖 금지).
- 회귀가드 `tests/campaignCardActions.test.js`(정적 배선 + **가짜 DOM 에 메뉴 기계장치를 vm 으로 올려 캡처/버블을 실제로 구분해 실행**, 변이시험 검출 확인) + 브라우저 실행검증(넘침 0·토글 겹침 0·body 직속·토글/바깥/Esc/다른 카드·좁은 화면). ⚠ 이 변경으로 `adminCardSync` 의 시트 버튼 패턴을 함께 갱신했다(검사 의미 불변).

### 관리자 "리뷰어 화면 미리보기" (마감 공고 포함 · 읽기 전용)
- 관리자가 **마감·오픈전 공고를 포함한 어떤 참여형 공고든** 리뷰어가 실제 보는 진행 화면(참여 전 → 작업가이드 → 제출 완료)을 확인. 진입 = 모집공고 카드 [👁 리뷰어 화면] + 수정 모달 미리보기의 [전체 화면으로 보기](`index-recruit.js` `openReviewerPreview`) → `campaign.html?id=&preview=1#tok=<admin_token>`.
- **격리(핵심)**: 무인증 리뷰어 게이트 `GET /:id/work-detail`은 **일절 미변경**(관리자 분기 없음). 대신 관리자 전용 `GET /api/campaign/admin/:id/preview`(authMiddleware+adminOrMaster)가 같은 shape을 **합성**해 반환 — `application`은 미저장 가짜 객체(`id:'preview'`), `canChangeOption:false`, `chatUrl`은 관리자라 그대로. **DB write 0**(campaign_applications INSERT/UPDATE 없음 = 홀드·정원·일일한도 카운터 무오염). 리뷰어앱 스코프 토큰(`via:'reviewer_campaign'`)은 `PUT /api/campaign/admin/:id` 끝앵커+PUT 한정이라 이 GET 경로에 도달 불가.
- **프론트 가드**(`campaign.html`, `PREVIEW = ?preview=1`): `getHold`는 preview면 항상 null·`setHold`/`clearHold`는 no-op(같은 브라우저의 실제 리뷰어 홀드 격리) / `onJoin`·`_doApply`·`_doChangeOption`·`onCancel` 전부 `_pvBlock` 차단 / 폴링 미실행 / 카운트다운 0 도달로 티커가 `loadCampaign`을 재호출해도 보고 있던 단계를 유지(`_pvStep !== 'pre'`면 화면 전환 생략). 참여 후 화면 렌더는 리뷰어와 **공용 `_renderJoinedView(j, hold)`** — 표시 내용 동일, 다른 건 iframe 파라미터·폴링뿐.
- **구매양식(iframe)**: 실제 폼을 그대로 렌더하되 **제출만 차단**. `search-app.js`의 `preview` 플래그는 `_EMBED_CTX`(=`embed=1`) 안에서만 존재(`_PREVIEW_MODE`) → 리뷰어 진입 URL엔 실리지 않아 **제출 동작 불변**. 차단은 `confirmOrderSubmit`+`submitOrderForm` 양쪽 조기 return(재제출 등 팝업 우회 호출까지 커버) + `body.embed-preview` CSS로 버튼 비활성. 미리보기는 리뷰어 로그인 게이트를 우회해 폼을 보여주되(`authSession` 자리표시자) **입력값 저장/복원은 안 함**(`_EMBED_FORM_KEY` 공란). 연결 탭 없는 공고는 폼 대신 안내(`#pvFormNote`).
- **토큰 전달**: 새 탭은 sessionStorage 미공유라 `#tok=` 프래그먼트로 넘기고(서버 로그·Referer 미유출) 도착 즉시 `history.replaceState`로 주소창·히스토리에서 제거(저장 실패해도 `finally`로 **정리는 항상** 수행). 저장은 **전용 키 `camp_preview_tok`**(`sessionStorage.admin_token`을 덮어쓰지 않아 타 모듈 동작 불변), 미리보기 새 창은 `noopener`(토큰 복제 차단).
- **레드팀 반영(재발 방지 — 이 가드들을 풀지 말 것)**: ① **`preview=1`만으로는 절대 미리보기가 켜지지 않는다**(`PREVIEW = PREVIEW_REQ && 토큰있음`) + 주소창에서 preview 파라미터 제거 — 관리자가 미리보기 주소를 공유해도 리뷰어는 평범한 참여 화면을 본다(그렇지 않으면 그 공고의 구매 시간창 전체가 마비). ② 미리보기 구매양식은 **잔여 리뷰어 세션을 절대 쓰지 않음**(`authSession` 무조건 자리표시자 + `_prefillBankFromProfile`/`_precheckProfileGateOnEntry` 스킵) — 관리자 브라우저에 남은 세션의 계좌번호·실명이 화면공유·스크린샷에 노출되는 것 차단. ③ 마감·게시전 공고는 실제 상태가 `closed`라 참여 화면이 잠기므로 **[모집중 가정] 토글**(`_pvForceOpen`, 기본 ON)로 진행 화면을 볼 수 있게 하되 클릭은 여전히 차단. ④ 단계 전환 시 **iframe URL이 같으면 재로드 안 함**(재로드마다 실 시트 읽기 = 시트 lane 소모). ⑤ 로드 실패 시 단계 버튼 제거(데이터 없이 ③을 눌러 가짜 "참여 확정!"을 정상으로 오판하는 것 방지). ⑥ **카운트다운 0 재조회가 화면을 되돌리지 않음** — `loadCampaign`은 참여 전 화면일 때만 `renderPre` (리뷰어가 구매 진행 중 cutoff를 맞으면 작업가이드·구매양식이 사라지던 **기존 버그도 함께 수정**). ⑦ 미리보기 옵션 고정표시는 `selectable`일 때만(리뷰어가 도달 불가한 마감 옵션 오표시 방지). ⑧ 수정 모달 버튼은 참여형일 때만(레거시 공고는 `campaign.html`이 리뷰어 홈으로 리다이렉트).
- ★ **향후 리팩터 금지선**: preview 판정을 sessionStorage/전역으로 옮기면 부모↔iframe·새 탭에 전파돼 **그 탭의 실제 리뷰어 제출이 영구 차단**된다. `_adminTok` 유무로 판정하는 것도 금지(`rapp_camp_edit_token` 보유자 = 실제 리뷰어라 조용히 제출 차단됨). 제출 차단 4중(`confirmOrderSubmit`·`submitOrderForm`·CSS·홀드문맥 미전달) 중 어느 하나도 제거 금지. 회귀가드 `tests/campaignAdminPreview.test.js`.

### ★★ 과거 작업 시트 읽기 정리 — "이관 안 함" ≠ "안 읽음" (2026-08-19)
- **발단(사용자 지시 「과거 358건 중 시트 읽는 건 정리해」)**: W5 에서 358건을 **이관하지 않기로** 했는데, 그건 **시트를 그만 읽는다는 뜻이 아니다** — `smartBuild` 는 매 주기 `campaigns ∪ tab_configs` 를 훑어 그 탭들을 A:Z 로 읽고 장부를 다시 만든다. `sheet_sync_ignored`(목록 제외)는 **표시용**이라 읽기를 못 멈춘다.
- **창구** = 이관 화면 `[🧹 과거 작업 시트 읽기 정리]` + `/api/trackb/past-tabs/{scan,close,reopen}`(adminOrMaster). **전부 수동**(크론 0) · **미리보기 기본** · 실행 시 **서버가 스캔을 다시 돌려 후보만** 닫는다(낡은 화면 방어).
- ★★ **`is_closed` 는 표시가 아니라 마감(아카이브) 예약이다** — 다음 전체 빌드의 `auto-clean-closed` 가 `review_index`·`index_master` 를 보관함으로 옮기고 **원본과 `tab_configs` 행까지 지운다** ⇒ 그 작업이 활성 목록에서 사라지고 **참여 리뷰어의 「제출완료」 내역도 화면에서 사라진다**. 화면·확인창은 **"데이터를 지우지 않는다"고 말하면 안 된다**(회귀가드가 그 문구 부활을 금지). 되돌리기는 **빌드 전이면** `reopen`, 그 뒤에는 **아카이브 복구**뿐.
- ★★ **판정 사본 0** — "과거인가"는 `utils/tabActivity` 한 곳(반영 점검·시트 우위 점검과 같은 판정). fail-closed 제외 = 연도 모름 · **근거가 시트 등록일뿐(`weak_signal`)** · 컷오프 이후 활동 · 미반영 주문 · 활성 공고 · 이미 무시트/아카이브/마감.
- ★★★ **"저쪽이 읽는가"를 판정할 때는 그 소비처와 글자 그대로 같은 규칙을 써야 한다(넓히는 쪽이 안전한 게 아니다)**: `smartBuild` 는 아카이브·마감을 **`sheet_id||tab_name` 이름만으로** 스킵한다(`tcMap`/`archivedSet`, gid 미참조 — indexBuilder:278·543·548). 여기에 레포 관용구인 **gid 폴백을 넣었더니** 이름이 바뀐 탭이 "이미 안 읽음"으로 접혀 **정리 대상이 조용히 사라졌다**(실측 「826개 중 0개」). 판정은 **이름 일치**로 좁히고, 차이는 `archivedByGidOnly` **건수로 말한다**.
- ★★ **반대 방향 구멍 = 탭 이름 어긋남(`name_drift`)**: 그 스킵이 **시트의 현재 탭 이름**으로 조회되는데 **탭명 교정(`syncTabListToDB`)은 크론이 없고 수동뿐**이라 등록명이 계속 어긋날 수 있다 ⇒ **마감·아카이브 표시가 있어도 새 이름으로 계속 읽힌다**(본섭 실측 5건). ★ 이 상태는 **여기서 마감해도 안 멈춘다**(표시가 옛 이름 행에 붙는다) → **후보로 올리지 않고**(죽은 조작 금지) 사유로 말하고 **등록명 → 시트의 현재 이름 목록**을 보여준 뒤 탭명 교정으로 안내한다. 재료는 **RAW 미러의 라이브 탭명**(gid 조인 · 시트 재읽기 0). ★ **무시트 판정만 gid 폴백이 유효**(`sheetlessScope`)라 이름이 바뀌어도 조용하다. ⚠ `sync-tab-names` 는 `tab_configs`·`index_master`·`review_index` 만 고치고 **`index_master_archive` 는 안 고친다** — 교정 뒤에도 읽히는 탭이 남을 수 있음을 화면이 말한다.
- ★ **연도 확인이 먼저** — 시트 표기에 연도가 없으면(`7 / 12 (금)`) 판정이 등록일로 폴백해 `weak_signal` 이 되어 후보에서 빠진다. 미확정 건수를 세어 기존 `/sheet-sync/year-probe`(**신규 엔드포인트 0**)를 상한 안에서 돌린 뒤 **재판정**한다.
- ★ **읽히는 것만 센다** — `archivedGidOnly` 를 세면서 `reads` 를 안 봐서 **무시트·마감이라 어차피 안 읽는 탭**까지 "여전히 읽습니다"로 말한 적이 있다(실측 6건). 후보 0인데 "정리 대상에 포함"이라고 말하는 것도 같은 계열.
- ★★ **빈 껍데기 행(`ghost_row`) — 시트의 현재 이름이 이미 별도 행으로 등록된 경우**: smartBuild 는 시트의 현재 이름으로 `tcMap` 을 찾으므로(indexBuilder:548) 읽기 여부는 **새 이름 행**이 정하고, 옛 이름 행은 gid 도 같은 값이라 `registeredGids` 에 보태는 것이 없다 ⇒ **아무 일도 하지 않는다**. 이것을 "지금도 읽습니다"에 세면 거짓(실측: 3건 중 1건). `name_drift` 판정 **앞**에 두고 `ghosts` **목록**으로 준다(건수만 주면 어느 행을 지울지 모른다). 삭제는 `deleteGhostRows` — ★ 이 서비스의 **유일한 비가역 조작**이라 fail-closed 3중(서버가 스캔 재실행해 ghost 만 · **장부 한 줄이라도 있으면 거부** · 미리보기 기본)이고 쓰기 표면은 **`DELETE tab_configs` 한 행**(주문은 참조가 아니라 과거 기록이므로 건수만 보고·미차단).
- ★★ **탭명 교정(`sync-tab-names`)이 마감·아카이브 탭을 못 고치던 것**: gid 를 `index_master` 에서만 읽는데 `auto-clean-closed` 가 그 행을 지워 gid 가 null → "GID 없음"으로 스킵된다(실측: 「탭 457개 → 변경 0건·스킵 27건」에 3건이 묻혔다). `COALESCE(NULLIF(im.tab_gid,''), NULLIF(tc.tab_gid,''))` 폴백(가산적). ⚠ 그 화면의 **머리말 카운터는 dryRun 에서 항상 0**(`renamed++` 가 `if (!dryRun)` 안) — 목록이 진실이다.
- ⚠ **가드 함정 3종(전부 이번에 실측)**: ① `t()` 가 동기 함수라 **async 콜백의 단언은 `process.exit(0)` 뒤로 밀려 실행되지 않는다** — 비동기는 `t()` 밖에서 끝내고 단언만 안에서 ② 쓰기 0 검사의 `/DELETE/i` 에 **`deleted_at IS NULL`** 이 걸린다(문장 형태로 판정) ③ **변이시험 검출기가 종료 코드를 안 보면** `TypeError` 로 죽는 변이를 "미검출"로 읽는다(대조군을 함께 돌릴 것).
- 회귀가드 `tests/pastSheetTabCleanup.test.js`(44 — 판정 순수함수 실행 · 쓰기 표면(`UPDATE` 대상 `tab_configs` · `SET` 대상 `is_closed`) · 시트/Drive 무접촉 · NUL 위생 · **smartBuild 의 스킵 키를 읽어 대조** · 화면 배선·escape·문구 금지) + `tests/pastSheetTabCleanupPg.test.js`(**진짜 PG16** 19 — 스냅샷 대조로 `is_closed` 만 변경 · 멱등 · 서버 재검증 거부 · **RAW 미러 조인은 스텁으로 못 잡는 자리** · 빈 행 삭제는 `tab_configs` 만 −1). ⚠ 가드 함정: **조용한 탭은 목록에 안 실리고 건수로만** 말하므로 `by[...]` 로 찾으면 undefined다.

### 시트 데이터 반영 점검(sheet-sync audit) — 등록 작업 전수 반영사슬 진단·수리
- **목적**: 등록된 작업의 베이스 시트 값이 "시트 → 검색인덱스(review_index) → 작업보드(campaign_participants)" 사슬 끝까지 반영됐는지 전수 진단(예: 7/20 이전 등록 작업 미반영 조사). 페이지 = `frontend/sheet-sync-audit.html`(관리자 로그인, 기준일 필터·[반영] 버튼).
- **API**: `GET /api/trackb/sheet-sync/audit?before=YYYY-MM-DD`(adminOrMaster, 읽기 전용·시트 API 무접촉) + `POST /api/trackb/sheet-sync/repair {sheetId,tabName}`. ★ **분모는 `tab_configs`(등록 작업 전체)** — `listActiveTabs`는 raw∩index에서 시작해 사슬이 끊긴 탭이 분모에서 빠진다(projectionCoverage와 같은 눈속임 구조). 등록일 = `campaigns.created_at` 시트 최솟값, **등록일 미상은 컷오프에서 제외하지 않고 `regUnknown`으로 동봉**(조용한 누락 금지).
- ★ **수리는 신규 쓰기 경로 0** — 기존 반영 함수 3개만 `mirrorOneSheet(force) → buildOneSheet → projectTab` 순서로 재사용(사본 금지). 한 단계 실패·빌드 락이 다음 단계를 죽이지 않고 단계별 결과를 그대로 보고. ★ `boardRows > indexRows`(수동/작업표 스켈레톤 행)는 플래그가 아니라 `boardExtra` 정보만(오탐 금지).
- ★★ **건수는 `dataRows`(헤더 아래 값 있는 행) — `raw_sheet_tabs.row_count` 를 화면 숫자로 쓰지 말 것**: 그 값은 상단 캠페인 정보(9행 남짓)·빈 줄·헤더까지 세서 **15건 작업이 33행**으로 보인다(실측 오해 신고). 헤더 탐지는 `utils/sheetHeader` 단일 출처, 세는 SQL 은 `jsonb_array_elements_text(cells)` + `btrim`. **문제 탭만** 계산(`_DETAIL_CAP`, 초과는 `detailCapped` 고지) · gid 없으면 계산하지 않고 null(숫자를 지어내지 않는다).
- ★★ **"검색인덱스에 없음"은 어디에 없는지까지 말한다** — 등록부 = `index_master`. 여기 없으면 ① 리뷰어 검색·구매양식 제출이 막히고 ② `listActiveTabs` 가 이 등록부를 게이트로 쓰므로 **리뷰웹시스템[3버전] 작업목록에서도 빠진다**. 원인 4분기: `index_archived`(아카이브) > `index_renamed`(시트 실제 탭명 ≠ 등록 탭명) > `index_sheet_never_built`(그 시트가 등록부에 한 줄도 없음) > `index_missing`(+같은 시트의 등록 탭 예시).
- ★★ **리네임 신호는 RAW 미러의 현재 탭 이름으로 판정**(index_master 에서 같은 gid 찾기 금지) — 본 쿼리가 `tab_name = … OR tab_gid = …` 로 **gid 우선 재매칭**을 하므로, 인덱스에 같은 gid 행이 있으면 애초에 `idxStatus` 가 채워져 미등록 분기에 오지 않는다. **진짜 PG 검증이 그 분기가 도달 불가임을 잡아냈다**(되살리지 말 것 — 회귀가드가 고정).
- ★★ **소스에 리터럴 NUL 금지(이 작업에서 또 밟음)**: Map 복합키 구분자를 문자 그대로 넣으면 git 이 파일을 **바이너리로 취급**해 grep·grep 기반 가드가 그 파일에서 통째로 무력화된다 → `'\u0000'` **이스케이프 표기**로 쓸 것.
- **아카이브 복구**: 감사 화면 [📦 복구+반영] → `POST /api/trackb/sheet-sync/unarchive` — ★ **복구 로직은 기존 `/api/archive/restore` 핸들러에 위임**(사본 금지: index_master_archive→index_master · review_index_archive→review_index · tab_configs 재생성(is_closed=FALSE) · 이력 기록이 이미 한 트랜잭션). Track B 경로에 두는 이유 = 인트라넷 SSO 토큰은 `/api/archive/*` 도달 불가. 게이트는 원본(authMiddleware)보다 **좁혀** adminOrMaster(프록시가 원본보다 넓어지면 안 됨). ★ 화면이 복구 뒤 곧바로 반영(repair)까지 이어 **작업보드에 다시 나타나는 상태로 끝낸다**. ★ 아카이브 탭은 tab_configs 에서 내려가 본 목록에 안 뜨므로 `includeArchived=1`(화면 체크박스)로 `index_master_archive` 항목을 합류시킨다(`archivedOnly`).
- **gid 해석·백필**(`resolveTabGid` 순수함수 + `POST /sheet-sync/gid-backfill`): 구글시트 URL 은 `#gid=` 가 있어야 그 탭이 열리는데 `tab_configs.tab_gid` 가 빈 작업이 많아 시트 첫 탭만 열렸다. 우선순위 = **등록값 → RAW 미러(동명 탭 1개일 때만) → index_master**. ★★ **동명 탭 2개 이상이면 확정하지 않는다**(`ambiguous` — 엉뚱한 탭 링크가 빈 링크보다 나쁘다, 행배정 "동명탭 보류"와 같은 규율). ★ 백필 **쓰기 표면 = `tab_configs.tab_gid` 한 칸 · `NULLIF(tab_gid,'') IS NULL` 일 때만**(이미 있는 gid 는 덮지 않는다 — 잘못된 gid 교정은 `sync-tab-names`/fix-swap 의 일), dryRun 기본. 화면은 작업명을 탭 직링크로 렌더하되 **`docs.google.com` 호스트 검증 후에만**(신뢰 베이스 재구성).
- ★★ **연도 기준 필터(기본 2026-01-01 이후)** — 등록 작업이 몇 년치 쌓여 목록이 과거 데이터로 뒤덮였다(사용자 신고). **탭 이름엔 연도가 없으므로**(`6/11퓨비아표백제…`) DB 신호로 판정한다. 판정 단일 출처 = `utils/tabActivity.js`(순수함수 + SQL 조각 상수 — 반영 점검·시트 우위 점검이 **같은 조각**을 쓴다, 사본 금지).
  - ★★ **판정 순서(사용자 확정)**: **구매일 컬럼의 마지막 날짜가 최우선** — 그 값이 있으면 그것으로 끝낸다(다른 신호로 덮지 않는다). 구매일이 곧 투입 시기이므로, 최댓값 합산으로 두면 "구매일은 2025년인데 시트 등록만 2026년"인 과거 작업이 살아남는다. 구매일을 모를 때만 나머지 신호의 **최댓값**(① `order_submissions.submitted_at` ② `recruit_campaigns.created_at` ③ `campaigns.created_at`)을 쓴다(2025년 등록 + 2026년 주문 = 현재 작업).
  - ★★ **실측 신고(2026-08-07): 표기에 연도가 없으면 등록일로 폴백해 과거 작업이 살아남았다** — 구매일 표기가 `7 / 12 (금)` 이면 구매일 신호가 null 이라 판정이 `campaigns.created_at`(시트 등록일)로 내려가는데, 그 값은 **DB 행 생성 시각이라 과거 작업도 2026 으로 찍힌다**. 게다가 그런 탭은 `yearUnknown=false` 라 **[연도 확인] 대상에서도 빠져** 영영 교정되지 않았다. → ① 확인 대상 판정을 `purchaseUnconfirmed`(구매일 연도 미확정) 로 바꾸고 ② 그 건수를 응답·화면에 **항상** 드러내며(등록일 임시 판정 중임을 명시) ③ 행마다 판정 근거(`activitySource`)를 표시한다. ★ 확인해도 연도를 알 수 없는 탭(글자로 친 날짜)은 `'none'` 으로 기록해 **재조회를 막되 신호로는 쓰지 않는다**(`force` 로만 재시도).
  - ★★ **연도 확인(`POST /sheet-sync/year-probe`)** — 실측: 셀에는 진짜 날짜값 `2024. 7. 12` 가 들어 있는데 **표시 형식이 `7 / 12 (금)`** 이라 미러(`FORMATTED_VALUE`)에는 연도가 없다. 그래서 **연도 미상 탭의 날짜 컬럼만** `UNFORMATTED_VALUE` 로 읽어(일련번호 → `parseDateToken` 이 이미 해석) 마지막 날짜를 확정하고 `app_settings.tab_year_probe` 에 캐시한다. ★★ **미러를 UNFORMATTED 로 바꾸면 안 된다** — 주문 행배정·다중컬럼 가드가 "시트 표기 그대로"를 전제로 비교하고 `orderLedger` 가 일부러 같은 옵션으로 읽는다(바꾸면 라이브 핫패스가 깨진다). ★ 이 도구에서 **시트 API 를 쓰는 유일한 경로** — 사람이 버튼을 누를 때만, 미상 탭만, 탭당 1콜(한 열), throttle 경유, `_PROBE_CAP` 상한·초과 고지. ★ 글자로 친 날짜는 일련번호가 아니라 여전히 미상(추측 금지).
  - ★★ **목록에서 제외(`POST /sheet-sync/ignore`)** — "복구할 필요 없는 작업"을 담당자가 치운다. **데이터를 지우지 않는다**(시트·인덱스·아카이브·작업보드 무접촉, `app_settings.sheet_sync_ignored` 한 키에 기록). 제외 건수 고지 + [제외된 작업 보기]로 언제든 복원. ★ 회귀가드는 문자열 존재가 아니라 **스텁 실행으로 `ignoredCount` 가 실제로 집계되는지**를 본다(변이시험이 약한 단언을 잡아 강화).
  - ★★ **`raw_sheet_tabs.mirrored_at`·`index_master.built_at` 을 신호로 쓰지 말 것** — 미러·빌드가 돌 때마다 `NOW` 로 갱신되어 **2025년 작업도 최신으로 보인다**(필터가 통째로 무력화). 회귀가드가 SQL 조각에서 이 두 컬럼 부재를 고정.
  - ★ **신호 0 = 과거가 아니라 "모름"** → `yearUnknown` 으로 분리해 **건수 고지 + [보기] 버튼**(`includeUnknown`)으로 연다(사용자 확정). 제외한 과거 건수도 `filteredOld` 로 고지 — 조용히 사라지지 않는다. ★ 잘못된 `since` 형식은 기본값으로 접지(형식 오류로 필터가 꺼지지 않게).
  - ★ 시트 우위 점검은 **연도 필터를 후보 선별보다 먼저** 적용 — 과거 작업이 정밀 스캔 상한(`SCAN_CAP`)을 잡아먹지 않게.
- 회귀가드 `tests/sheetSyncAudit.test.js`(, **변이시험 검출 확인**) + `tests/sheetSyncAuditPg.test.js`(**진짜 PG16** 케이스 — 미러 33행에서 작업 15건 판독 · gid 재매칭으로 미등록 분기 도달 불가 증명 · 리네임/아카이브 분기 · gid 백필 멱등 · 아카이브 전용 탭 합류 · 컷오프 날짜 비교).

### 탈 구글시트(무시트 운영) — 지금 지켜야 하는 규율
- **완료된 이관이다**(W1~W5 + 프로덕션 E2E 완주 2026-08-08). 진행 기록·사고 조사·의사결정 경위는 **`docs/history/탈구글시트-이관.md`**. 남은 358건(구매일 연도 미확정 과거 자료)은 **이관하지 않는다**(전환 화면이 건수를 고지).
- **모델 = 길 B**: 장부 3권(`raw_sheet_*` · `index_master` · `review_index`)은 그대로 두고 **채우는 손만** 시트 → 작업표(`campaign_participants`)로 바꾼다. 그래서 검색·행배정·리뷰내역·홈 통계·입금대상 등 소비처 수십 곳이 **무수정**이고 stale-pl 보안 게이트도 그대로 산다.
- ★★ **"구글에 물어야 하는가" 판정 단일 출처 = `utils/sheetlessScope.js`** — 시트 단위 제외는 **`BOOL_AND`(등록 탭이 전부 무시트일 때만)**, 탭 단위는 **이름 → gid 폴백**, **빈 gid 는 키를 만들지 않는다**, **조회 실패·컬럼 미적용은 빈 집합(fail-open)**. 게이트는 크론 **네 곳 전부**(`smartBuild`·`buildIndexSmart`·`checkDirtySheets`·`rawMirror`) — smartBuild 를 빠뜨리면 5분 크론이 **작업표로 만든 장부를 옛 시트 값으로 덮는다**.
- ★★ **장부 생성기 `sheetlessLedger.rebuildLedgers`** — 작업표를 시트 모양 2차원 배열로 재조립해 **기존 파서 `indexBuilder.parseTabRows` 를 그대로 통과**시킨다(판정 사본 0). 헤더는 **`COALESCE(detected_headers, headers)`**(`headers` 는 시트 A1 행이라 그것만 보면 전 탭이 빈 명단이 된다). **seq = 시트 실제 행 번호**, 탭 단위 `pg_advisory_xact_lock`. fail-closed 3종 = `not_sheetless`(시트 값 덮어쓰기 차단) · `tab_not_registered` · `no_headers`. **`preflight` 는 `dryRun` 과 함께일 때만** 게이트를 연다.
- ★★ **접수 = `sheetlessAccept.resolveAcceptMode` 단일 출처**(라우트·화면에 사본 0): 명시 요청 > **이미 무시트면 되돌리지 않는다** > 명시 시트 > URL 있음 > **URL 없음 = 무시트**(기본). URL 이 있는데 형식이 틀리면 **추측하지 않고 멈춘다**. 가상 시트ID `wt_`+hex20(23자)·숫자 gid, `sheet_url` 은 **빈 값**(죽은 링크 금지). 킬스위치 `SHEETLESS_ACCEPT_DEFAULT=0`. **접수 흐름에 구글시트 생성 창구를 두지 않는다**(두면 `work_sheet_url` 이 다시 붙어 시트 경로로 되돌아간다).
- ★★ **주문 즉시 완결 `sheetlessOrder`** — 큐를 안 타고 작업표 `row_json` 에 직접 기록 후 장부 재생성. **`mirror_status` 는 그대로 `'written'`**(새 상태를 만들면 소비처 여러 곳을 훑어야 하고 한 곳만 놓쳐도 조용히 미완결). **매핑 사본 0**(`mapOrderToSheetRow` · 옵션 칸은 `optionWriteColumns` · 옵션 blank-only). **시트 쓰기 차단 5중**(리뷰어 제출 · 외부모집 수동제출 · reconcile 은 큐 대신 작업표 재기록 · 큐 실행부 fail-closed 백스톱 · 리뷰제출 표시 배경작업). **완결 순서가 계약**: 작업표 기록 → **장부 재생성 성공** → `markOrderWritten` → 신원 링크(중간 실패에 링크를 남기면 교차노출).
- ★★ **상태·메모는 작업표 칸에 쓴다**(`sheetlessStatus.markStatusCell`/`markSheetlessMemo`) — 시트에도 칸이 있는 값(제출·입금)은 **보존이 아니라 기록**, 시트에 칸이 없는 값(대표 리뷰 이미지)은 **재생성 시 스냅샷 보존**. 어느 칸인지는 `review_index.submit_col/submit_col2`(파서가 감지해 둔 헤더명). **시트 기반 탭·판정 실패는 `{handled:false}`** → 호출부가 자기 기존 경로를 탄다(`true` 로 접으면 시트 탭의 기록이 조용히 사라진다).
- ★★ **정원은 달력이 정한다**(D3-a): 무시트 탭은 시트 일정(063) 판정에서 제외하고 발행 시 작업표 날짜를 `campaign_daily_plans` 로 프리필(`ON CONFLICT DO NOTHING`·과거 날짜 제외·fail-soft). ★ **`parseDateColumn` 에 `fallbackAnchor` 필수** — 작업표 구매일자는 `M / D (요일)` 라 연도가 없어 앵커가 없으면 **전 행 null 로 조용히 죽는다**. 두 조각(제외·프리필)은 **반드시 함께** 나간다.
- ★★ **표시는 서버 플래그 `sheetless` 하나로**(배지·죽은 링크 제거) — **ID 모양(`wt_`)으로 추측하지 않는다**(이관된 기존 작업은 진짜 시트 ID 를 그대로 쓴다). 필드가 없으면(구버전 백엔드) 아무것도 그리지 않는다.
- ★★ **표식을 켜는 유일한 창구 = 전환 화면**(`/api/trackb/sheetless/*`, adminOrMaster). 점검표 5항목은 **fail-closed**(`unknown` 도 통과 아님)이고 **경고 전용(advisory)은 ④ 시트 사본 최신 하나뿐** — 여기에 항목을 더하면 이관이 조용히 헐거워진다. 이관 **순서가 계약**: 마지막 반영(미러→빌드→**투영**) → **표식 켜기** → 비활성 import 줄 은퇴 승격 → 장부 재생성 → 시트 안내문(best-effort). 되돌리기([재연결])는 점검표 없이 언제든.
- ★★ **미반영 주문은 시트가 아니라 작업표로 인계한다** — 실행부는 `sheetlessOrder.writeOrderToWorktable` 한 벌이고 **표식을 켠 뒤에** 부른다. 다만 **배정된 줄이 없는 주문은 계속 잠근다**(옮길 자리가 없으면 진짜로 사라진다).
- ★ **폴더 1단 = 업체명**(`folderTitle.service`, D2-a) — **시트 기반 탭에는 `null` 을 돌려줘** 기존 경로를 바이트 불변으로 유지한다(소비처 3곳 공용).
- ★ **파생 규칙 2종**(이관 대조로 드러난 것): `campaignSchedule.DATE_HEADER_KEYWORDS` 에 **`구매날짜`** 포함(단 `날짜`·`일자` 로 넓히면 `입금일자`·`리뷰날짜` 를 구매일로 오인) · parity 짝짓기는 phone8 그룹 **안에서 줄 번호로**(`_pairByRow`) — 한 번호에 여러 줄인 리뷰어에서 허위 불일치가 났다.
- ⚠ **레거시 공고(participation_mode=false)의 참여 행추가**는 주문 큐와 별개의 시트 직접 쓰기라 `sheetlessApplicant.addApplicantRow` 가 분기한다 — **무시트인데 실패하면 `throw`**(조용히 false 를 돌려주면 아무도 안 읽는 시트에 참여자가 남는다).

### ★★ 작업표 `번호`·`담당자` 자동 채움 + 구매일자 기준 재번호 (2026-08-19, 스키마 변경 0)
- **사고**: 「0729)위드프렌즈_면마스크」에서 145번(8/18) 아래로 **번호·담당자가 빈 줄**이 쌓였다. 원인 = ① `번호`는 tier `auto`(`worktableTemplate.ROLE_META.seq`)라 **작업표를 만들 때 `planToSheetValues` 가 매기는 칸**인데, 준비된 줄이 동나면 주문이 `appendSlot`(`row_json = {}`)으로 줄을 이어붙여 그 칸이 영영 빈다 ② `담당자`는 **어느 코드도 쓰지 않는 칸**이다(매퍼가 `null` 반환) ③ 뒤늦게 도착한 지난 날짜(8/5) 주문이 맨 아래에 남아 구매일자 순서가 깨졌다.
- ★★ **DB `seq` 는 절대 바꾸지 않는다(완화 금지)** — `seq` 는 줄의 주소다: `order_submissions.sheet_row` · `review_index.row_index` · `review_submissions.row_index` · `participation_links.rowIndex` · `payment_batch_items(row_index)` 부분유니크 · `ON CONFLICT (sheet_id,tab_name,seq)` · 셀 색·편집 오버레이 앵커. 재배열하면 이 전부가 남의 줄을 가리킨다. **바꾸는 것은 `row_json` 의 표시 칸(`번호`·`담당자`)과 화면 정렬뿐**이고, 사용자가 말한 "145 → 146"은 이것으로 성립한다.
- ★★ **정렬 규칙 단일 출처 = `utils/rowNumbering`**(사용자 확정): ① 구매일자 asc ② 같은 날은 **주문 제출 시각** asc(주문 없는 빈 슬롯은 그 날짜의 뒤) ③ `seq`(안정 정렬 — 다시 돌려도 번호가 흔들리지 않는다) ④ **구매일자를 못 읽는 줄은 맨 아래**(맨 위로 두면 그 뒤 번호가 전부 밀린다) ⑤ **지나간 날짜의 빈 줄은 표 맨 아래로 민다**(사용자 확정 2026-08-19 — 든든푸드 쭈꾸미: 구매일자 < 오늘 + 주문·이름·수취인·연락처가 전부 빈 줄, **줄은 지우지 않고 밀기만**). 화면 정렬(`workdeskTab`)도 **같은 판정**을 쓴다 — 번호만 바꾸고 순서를 그대로 두면 "8/5 건이 146번인데 여전히 맨 아래"가 된다.
- ★★ **⑤ 는 `today` 를 받았을 때만 켜진다** — `utils/rowNumbering` 은 시계를 보지 않는다(결정적). 호출부(`renumberTab`)가 KST 오늘을 넘기고, **빈 줄 판정은 서비스의 `FILLED_SQL` 을 SELECT 에 그대로 실어** 짝 빈 줄 정리와 같은 기준을 쓴다(사본 0). `today` 미전달 = 종전 동작(무회귀). ★ **지나간 날짜라도 채워진 줄은 절대 밀지 않는다** · **오늘 날짜의 빈 줄도 밀지 않는다**(아직 지나가지 않았다) · **날짜 없는 줄이 계속 맨 끝**(④ 불변).
- ★★ **무시트 탭 전용(fail-closed)** — 시트 기반 탭은 10분 투영이 `row_json = EXCLUDED.row_json` 으로 통째 덮으므로(`participants.importTabFromIndex`) 여기서 고쳐도 조용히 되돌아간다. `not_sheetless` 사유를 **말하고 멈춘다**(고쳤는데 되돌아가는 상태를 만들지 않는다).
- ★★ **담당자 칸은 건드리지 않는다(사용자 확정 2026-08-19)** — 담당자는 작업보드 좌측 상단 [작업 조건]에 이미 표기되므로 줄마다 반복할 이유가 없다. 이미 적혀 있는 값도 그대로 둔다. ★ 판정(`MANAGER_KEY_RE`)·정규화(`workManager`)·스캔 집계까지 **전부 제거**했다 — 죽은 규칙을 남기면 다음 사람이 되살린다. 표에 없는 칸은 만들지 않는다(열 구성은 `설정 › 작업표 표준 열` 하나가 정한다).
- ★★ **자동 경로는 SAVEPOINT 격리 + 절대 throw 없음**(`renumberTabInTx`, 주문 기록 트랜잭션의 COMMIT 직전) — 번호 매기기 실패로 **주문 기록이 통째로 죽으면 안 된다**(082 apply 규율과 같은 자리). 번호는 나중에 [🔢 번호 정리]로 다시 맞출 수 있지만 주문은 못 되돌린다.
- ★★ **`fallbackAnchor` 필수** — 작업표 구매일자는 `M / D (요일)` 라 **연도가 한 칸도 없어** 앵커가 없으면 전 행 null 이 되고 재번호가 "전부 날짜 없음"으로 조용히 무너진다(W2-b F-2 와 같은 자리). 날짜 칸 찾기·파싱은 `campaignSchedule.findDateColumnIndex` + `utils/koreanDate.parseDateColumn` **재사용**(사본 0).
- ★ **쓰기 표면 = `campaign_participants.row_json` 한 곳**(시트·주문 원장·장부·정원 무접촉) · 바뀌는 줄만 · **UPDATE 한 문장**(줄 수만큼 왕복 금지). 킬스위치 `WORKTABLE_AUTO_NUMBER=0`.
- ★★ **주기 자동 스윕(크론 `3-59/5`)** — 사람이 누르지 않아도 정리된다(사용자 확정 2026-08-19). 주문 유입 시 즉시 재번호가 돌지만 **이미 비어 있던 줄**·**중복 줄 정리 뒤 남은 중복 번호**·그때 실패한 건은 남기 때문. ★ **한 쿼리 스캔으로 번호 빈칸이 있는 작업만 추려 그것만 연다**(전 탭 dry-run 금지 — 정리가 끝나면 매 사이클 쿼리 1번). ★ 사이클 상한(`WORKTABLE_RENUMBER_SWEEP_CAP` 기본 12탭, 남은 것은 다음 사이클) · 건별 독립 · **절대 throw 없음**(크론 보호) · **스캔 실패는 쓰기 0 으로 skip**(모르는 채로 쓰지 않는다) · `withJobLock('worktable_renumber_sweep')`(멀티 인스턴스가 같은 탭을 두 번 매기지 않는다). 킬스위치 두 겹 = `WORKTABLE_AUTO_NUMBER=0`(전체) · `WORKTABLE_RENUMBER_SWEEP=0`(스윕만). ★ 화면이 **"5분마다 자동"** 임을 말하고 버튼은 "지금 바로" 용도로 남긴다.
- ★★ **짝 빈 줄 정리(사용자 확정 2026-08-19)** — 조건은 **채워진 줄과 번호가 겹치는 빈 줄** 하나뿐이다. 짝이 있다는 것 자체가 "그 자리는 이미 채워졌다"는 근거라, **날짜·모집량을 보지 않으므로 아직 안 팔린 미래 자리를 지우지 않는다**(모집 계획 파괴 금지 — 사용자가 고른 가장 좁은 조건). "채워짐" 판정은 주문 링크·이름·수취인·연락처 중 하나라도 있으면(`FILLED_SQL` 단일 조각 — 스캔 집계와 대상 선정이 갈리면 "목록엔 N줄인데 다른 줄이 내려간다").
- ★★★ **정리는 재번호보다 먼저 돌아야 한다(완화 금지)** — 재번호가 번호를 1..N 로 **유일하게** 만들면 "번호가 겹친다"는 짝 신호가 **영영 사라져** 그 뒤로는 정리가 성립하지 않는다. 스윕·전체 정리·라우트 **세 경로 모두** 이 순서이고 회귀가드가 인덱스 비교로 고정한다. ⚠ 이 배포 **이전에 이미 재번호된 탭은 신호를 잃었다** — 그런 탭의 빈 줄은 [⋯] → [🧹 줄 정리]로 사람이 고른다.
- ★ **지우지 않고 내린다** — 실행은 `sheetlessLedger.retireRows` 위임(소프트 삭제 + 무시트 게이트 + "soft-delete → 장부 재생성" 순서 계약을 그대로 상속, 사본 0). 라우트는 **`cleanBlanks: true` 명시일 때만** 정리한다(줄을 내리는 일이 "번호 정리"라는 이름 뒤에 숨지 않게). 자동 스윕은 사용자 확정대로 항상 정리→재번호.
- **창구** = 작업보드 [⋯] → **[🔢 번호 정리]**(미리보기 = 쓰기 0 → confirm → 실행, `POST /api/trackb/worktable/renumber` internal) + **전체 소급 정리** `POST /api/trackb/worktable/renumber-all`(adminOrMaster). ★ 실행 confirm 이 **"이미 인쇄·공유한 번호와 달라질 수 있다"** 를 말한다.
- ⚠ 잔여: 기존 [번호 배정](`assignUnslottedOrderToOpenSlot`)은 `번호` 칸에 **`seq` 를 적어** 무시트 탭에서 이웃과 1 어긋난다(`seq = 번호 + 1`) — 재번호가 흡수하지만 그 함수 자체는 그대로다. 표의 `#` 열(seq)과 `번호` 는 서로 다른 값으로 보인다(원래도 1 차이).
- ★★ **전체 작업 조회는 dry-run 을 전 탭에 돌리지 않는다** — `renumberTab({dryRun})` 은 그 탭의 **모든 줄과 `row_json`** 을 읽으므로 무시트 작업 수백 개에 돌리면 조회 한 번이 수백 MB 를 끌어온다. 목록은 `scanNumbering` 이 **DB 안에서 세고 숫자만**(번호 빈칸 수 · **중복 번호 수**) 돌려준다. ★★ **중복을 함께 세지 않으면 안 된다** — 다른 세션의 중복 줄 정리 뒤 `566,566,567,567…` 처럼 **번호는 다 차 있는데 두 개씩**인 표가 남는데(실측 2026-08-19), 빈칸만 세면 그 표가 자동 스윕 대상에서 **통째로 빠져** 사람이 누를 때까지 정리되지 않는다. 판정은 `값 있는 줄 수 − 서로 다른 번호 수`(DISTINCT 차). ★ **칸 이름 후보는 `utils/rowNumbering` 의 목록을 SQL 파라미터로 넘긴다**(정규식도 그 목록에서 생성 — SQL 에 이름을 적으면 판정이 두 벌). ★ 한계: **번호가 다 차 있고 순서만 어긋난 작업은 이 숫자로 안 드러난다**(날짜 파싱이 필요해 비싸다) — 화면이 그 사실을 말하고 정확한 변경 줄 수는 작업별 미리보기가 보여준다.
- ★★ **팝업은 높이 상한 + 본문 스크롤**(실측 신고: 무시트 작업 **121개**가 세로로 무한히 이어져 하단 버튼에 닿지 못했다) — `#rnOv .wbl-dlg{max-height:86vh;display:flex;flex-direction:column}` + `.wbl-db{flex:1;min-height:0;overflow:auto}`(♻ 중복 정리 모달과 같은 관용구). ★ **`min-height:0` 이 없으면 flex 자식이 내용만큼 늘어나 스크롤이 안 생긴다**(레포 상습 함정). 폭은 모드별(목록 980px · 이 작업 560px).
- ★★★ **sticky 표머리가 죽는 함정 3종을 한 자리에서 다 밟았다(실브라우저로만 보인다 — 계산값은 `position:sticky` 인데 따라오지 않는다)**: ① **전역 `table{overflow:hidden}`** 이 sticky 기준을 표 자신으로 만든다 → `overflow:visible` 상쇄(홈 작업목록 헤더행과 같은 처방) ② **`border-collapse:collapse`** 에서는 sticky th 가 안 먹는다 → `separate` + 경계선은 box-shadow ③ **표를 감싸는 래퍼의 `overflow-x:auto`** — 한 축만 지정해도 다른 축이 `auto` 가 되어 그 래퍼가 스크롤 조상을 가로챈다 → 가로·세로 스크롤은 `.wbl-db` 한 곳이 맡는다.
- ★★ **기본은 [정리 대상만] 보기 + 작업 검색** — 121개를 다 늘어놓으면 그 자체가 조작 불가다. ★ 검색은 **행(`#rnRows`)만 갈아끼운다**(입력칸이 든 껍데기를 다시 그리면 한글 IME 조합이 깨진다 — `ㅁ며면면` 실측 계열). ★★ **필터·검색 중에도 실행은 원본 인덱스**(`all.indexOf(r)`) — 보이는 순번을 넘기면 **남의 작업을 정리한다**.
- **모달은 한 벌 · 모드만 둘**(`[이 작업]` / `[전체 작업]`) — 전체 모드는 **작업을 안 골라도** 열린다(진입 = 탈시트 전환 헤더 `[🔢 번호 정리]`). 목록에서 **행별 정리**와 **전체 정리** 둘 다 가능하고, 실행 후 **다시 세어** 보여 준다(줄었는지 눈으로 확인). ★ 행 실행은 **인덱스만** 넘긴다(작업명은 외부발 문자열).
- 회귀가드 `tests/rowNumbering.test.js`(정렬·계획 순수함수 실행 + 스텁 pool 로 서비스 실제 실행(무시트 게이트·미리보기 쓰기 0·**seq 변경 쓰기 부재**·스캔 읽기 전용) + SAVEPOINT 격리 + 배선, **변이시험 41종 검출 확인** — ★ 순서 계약은 **정적 인덱스 비교만으로는 '정리를 아예 안 부르게' 만든 변이를 놓친다**(실측) → 스윕을 실행해 호출 순서를 스파이로 본다 — ★ 가드 함정 2종을 변이시험이 잡았다: **변경 0건일 때 "담당자 필드 없음"은 공허하게 참**(번호가 실제로 바뀌는 계획으로 볼 것) · **표 열 추가는 헤더 칸 수 ≡ 행 칸 수 ≡ colspan 계수**로만 잡힌다 — ★ `confirm` 게이트는 **문자열 존재가 아니라 `if (!confirm(` 형태**를 고정할 것(`if (false && !confirm(...))` 이 통과했다)) + 실 http 오리진 브라우저 실측(모달 body 직속·미리보기 표·실행 페이로드·Esc·JS 오류 0). ⚠ 이 변경으로 `blogRegister` 가드의 [⋯] 메뉴 조건 패턴을 갱신했다(검사 의미 불변 — 도구가 늘 때마다 조건 문자열이 길어진다).

### 블로그체험단 (work_kind='blog') — 지금 지켜야 하는 규율
- **M1~M6 완료 + 프로덕션 E2E 완주(2026-08-19)**. 단계별 구현 기록·사고 경위는 **`docs/history/블로그체험단.md`**, 기획은 `frontend/docs/prd-blog-campaign.html`.
- ★★ **분류는 별도 축 `work_kind`(migration 099)** — 리뷰타입의 하위 값이 아니다(블로그엔 리뷰타입이 없다). 판정 단일 출처 `utils/workKind.js`(`resolveWorkKind` = 공고 > 탭), 조회는 `workKindContext.service`(60초 캐시·fail-soft). **빈 값 = review = 종전 동작 100%**. `workKindForStore` 는 `''` 를 `''` 로 남긴다(접수 blank-only 가 무력화된다).
- ★★ **준비 행 판정 = `readPreparedRows` 한 곳**(전수 점검·백필·이관 점검표·일괄 점검이 모두 통과): blog 는 **구매일자 무관·값이 하나라도 있는 행**, 총원(`recruit_count`)이 있으면 앞에서부터 그만큼. **총원이 실체 행보다 많아도 자리를 지어내지 않는다**(seq = 시트 실제 행 번호) — `shortOfTotal`/`beyondTotal` 로 고지. **판정 불가는 종전(날짜 기준)** — 모른다고 blog 로 단정하면 리뷰 작업 전체의 기준이 바뀐다.
- ★★ **블로그URL·포스팅URL 이 상품URL 을 하이재킹하지 않는다**(`columnResolver.URL_EXCLUDE_PATTERNS`) — `urlKeywords` 는 부분일치 + 첫 매칭 승이라 그 열이 왼쪽이면 `product_url` 이 통째로 블로그 주소가 된다. **확실한 두 낱말만** 막는다.
- ★★ **결과물 칸(비고/포스팅URL) 열 고르기 = `utils/memoColumn.js` 한 곳**(시트 배경 쓰기·큐 재시도·무시트 작업표 세 소비처 공용): blog 는 우선순위가 **`포스팅` → `비고`** 로 뒤집히고 리뷰는 종전 그대로. `isPostDateHeader` 가 URL 칸↔날짜 칸을 가른다(없으면 서로 뒤바뀐다). 큐 `review_submit` 페이로드에 `memo`·`blog`·`postDate` 를 실어야 재시도가 같은 칸에 쓴다.
- ★★ **포스팅URL 판정 = `utils/blogPostUrl.js`** — **도메인 화이트리스트 금지**(어느 플랫폼에 쓸지는 우리가 정하지 않는다). `http(s)` 절대 URL + 호스트에 점 1개 이상, 공백·따옴표·꺾쇠 거부. 프론트 사본은 회귀가드가 서버와 대조.
- ★★ **결과물 = 캡처 + 포스팅URL 둘 다**(M6 사용자 확정 — M4-2 의 "URL만 = 완료"를 뒤집음). 서버가 원장·대표 이미지로 캡처 존재를 최종 방어(`capture_required`, 조회 실패는 fail-open). 완료 시 **포스팅제출일**을 시스템이 KST 로 기록.
- ★★ **검수 격리**: 1차 필터는 blog 면 `skip('blog_tab')`(AI 가 블로그 화면을 몰라 `other` 로 잠그면 정상 제출이 전면 차단 = 참여 소각). 2차는 **형식·상품명·본문겹침만 skip 하고 같은 파일(중복) 검사는 그대로 둔다**.
- ★★ **승인제(migration 127)**: 블로그URL 제출 → `blog_pending`(TTL 없음·정원 미점유) → 관리자 [승인]/[반려] → 승인 = **`status='applied'` 전이**(별도 approved 상태 금지 — 유효홀드 판정 소비처가 십수 곳이라 한 곳만 놓쳐도 새거나 굳는다). 승인 후 24h 미구매 자동 취소, **반려는 즉시 재신청 가능**(부분유니크에서 `blog_rejected` 제외). 대기/반려는 '만료'가 아니라 전용 reason 으로 내려 화면이 구분해 그린다.
- ★ **작업표 필수 3열**(`블로그URL`·`포스팅결과URL`·`포스팅제출일`)은 `worktablePlan.ensureBlogColumns` 가 템플릿에 없어도 붙인다. **blog 작업표는 구매일자를 깔지 않는다**. blog 일건수는 비면 **총모집**(무제한 9999)으로 정규화 — 프론트는 `payload.daily_limit` 대입 **뒤**에서(앞이면 덮여서 무효).
- ★ **블로그 주소(`blog_url`, migration 101)는 포스팅URL과 다른 칸**: apply 게이트는 **홀드 INSERT 앞**에서 403(자리 미점유), 값의 출처는 **서버 홀드 하나**(요청 본문 불신), 주문 전파는 `COALESCE(NULLIF(os.blog_url,''), ca.blog_url)`(관리자 사전등록 값 미덮음). 매퍼는 `블로그` 규칙을 **주문자·주소보다 앞**에 두고 빈 값은 `''` 가 아니라 **`null`(=안 씀)**.
- ★ **[＋ 블로거 추가]**(작업보드 [⋯], 무시트 전용): 모집공고 밖에서 섭외한 블로거를 명단에 넣는 창구. 주소 칸은 **`orderLedger.blogUrlWriteColumns`(매퍼 파생)** 로 고르고, fail-closed 7종(시트 기반·미등록 탭·blog 아님·열 없음·중복 연락처·형식)으로 거부한다. 구매양식은 **그 블로거가 직접** 제출한다.
- ⚠ **곁다리로 고친 것**: `SUBMIT_EXCLUDE_PATTERNS` 에 `포스팅` 추가 — `포스팅제출일` 이 완료신호 '제출' 때문에 리뷰제출 열로 오탐돼 **장부 재생성마다 `is_submitted` 가 무너졌다**(E2E 가 잡음).

### ★★★ 등록리뷰어DB 오삭제 사고 + 복구 (2026-08-19 — E2E 정리 중)
- **사고**: 정리 스크립트가 `GET /api/trackb/reviewers?q=E2E블로거` 결과를 `force:true` 로 순회 삭제했는데, 그 검색이 q 에서 추출한 짧은 숫자(`'22'`)를 **연락처·계좌 부분일치로도 매칭**해 무관한 실제 리뷰어 수십 명이 결과에 섞였고 **39명이 하드삭제**됐다(reviewers 행만 — 참여·주문·문의 이력 테이블은 무손상). migration 120 의 `reviewer_id … ON DELETE RESTRICT`(계좌 provenance)가 걸린 11명은 삭제가 막혀 생존.
- **복구**: R2 백업(당일 03:47 KST)에서 reviewers 만 임시 PG 복원 → 대상 id 만 남김 → `pg_dump --column-inserts --on-conflict-do-nothing` 으로 운영에 **없는 행만** INSERT(일회성 워크플로, 실행 후 삭제). **28명 복구**, 1명은 같은 전화의 다른 행이 이미 있어 skip(그 행으로 정상), **7~10명은 백업 이후(당일) 가입자라 어느 백업에도 없어 미복구** — 재가입하면 phone8 기반 이력은 자동 재연결된다.
- ★★ **재발 방지 3종**: ① 검색 숫자 부분일치는 **추출 숫자 4자리 이상**일 때만(작업보드 표 검색과 같은 기준 — 회귀가드 `reviewerDbTab`) ② **일괄 삭제 스크립트에서 `force:true` 를 목록 전체에 자동으로 걸지 말 것** — needConfirm(이력 경고)이 마지막 방어선이었다 ③ **테스트 데이터 정리는 이름 정확일치로 걸러 한 건씩** — 검색 결과를 그대로 파괴 루프에 넣지 않는다.

### 컬럼감지 SoT (컬럼 판정 DB화 1단계) — 매핑 우선 · 키워드는 부트스트랩/폴백
- 리뷰 인덱스의 컬럼 감지(`columnResolver.parseTabRows`, 두 빌더 공용)는 **DB매핑(`tab_column_mappings`) 우선 → 키워드 폴백**. DB 오버라이드 6필드 = recipient/review_submit/product/phone/round/payment. **name은 PII 가드로 영구 키워드 전용**(테스트 케이스 9). 매핑은 재앵커(저장 헤더==현재 헤더)·범위가드 통과 시만 신뢰.
- **자동기록(detection snapshot)**: `COLUMN_MAPPING_AUTO_RECORD=1`이면 빌더가 매핑 없는 탭에서 "방금 키워드가 고른 컬럼"을 `recordDetectedMappings`로 기록(원자 `WHERE NOT EXISTS`+`ON CONFLICT DO NOTHING` = **수동 매핑 절대 미덮어씀**, `updated_by='auto:detect'`, **checksum 무효화 없음** — 기록≡키워드 결과라 재파싱 불필요). 전 탭 백필 = 플래그 켜고 `smart-build/run {force:true}` 1회(또는 04:00 전체 리빌드 대기). ★ `autoGuessField`(매핑 UI 추측) 기반 백필 금지 — resolver와 시맨틱 불일치(예: '입금자명').
- **관측(migration 044)**: 빌더가 `index_master.detect_source`(필드별 col/header/src)·`detect_drift`(매핑 거부 사유)·`detected_at` 기록 + 드리프트 `logger.warn`(무로그 폴백 해소). `GET /api/mapping/coverage`·`/drift`(admin/master), 관리자 설정탭 "컬럼매핑 현황" 카드, 캠페인탭관리 인원/제출 셀 ⚠배지. 교정은 raw-mirror.html 컬럼 매핑 에디터(`?sheetId=&gid=`로 탭 preselect+에디터 자동 오픈).
- **무변경 보장**: 기록된 매핑 ≡ 키워드 결과(회귀가드 `tests/columnResolver.test.js` 케이스 12 = 기록→재파싱 deepEqual, `tests/columnMappingRecord.test.js`). 2단계(집계 소스 DB 원장 전환, `tab_configs.source_of_truth` 활용)는 1단계 게이트 통과 후 별도 작업.

### 구매양식 신원게이트 (내정보 필수 · 제출정보 유사도검증 · 타계정 자동등록)
- **필수입력**: 구매양식 카드에서 **주문번호·비고 제외 전 필드 필수**(주문자/아이디/수취인/연락처/배송주소/은행/계좌/예금주/결제금액). "1번과 동일" 체크·nc모드 쿠팡카드 재사용값은 유효값으로 인정(`submitOrderForm` 검증).
- **내정보 게이트**: 리뷰어는 내정보 4종(**사용자명/전화번호/주소/계좌**)을 등록해야 제출 가능. 프론트(진입 배너 `#ofProfileGate` + 제출 차단, `index.html#my` 딥링크로 내정보 탭 오픈) + 서버(`POST /api/submit/order`가 `PROFILE_INCOMPLETE` 반환) 이중 강제. `loginPhone8` 없는 제출(레거시/관리자 경유)은 게이트 미적용.
- **유사도 검증**(`identity.service.js`): 제출정보(캡처 AI 추출값 `extractedRecipient/Phone/Address` 우선, 없으면 폼 입력값)를 본인 프로필·타계정(sub_accounts)과 대조. 이름=공백제거 정확일치, 전화=뒤8자리, 주소=**휴리스틱(호수·동 숫자 불일치=즉시 mismatch, 호수 일치+바이그램≥0.25=match — "서면다인로얄팰리스 728호"≈"서면팰리스 728호" 축약 허용) → 애매하면 Gemini(`verifyAddressMatch` 재사용) 보완, Gemini 실패시 소프트 처리**. 계좌는 SELF=본인계좌/예금주, SUB=타계정 전용계좌 **또는 본인 공통계좌**(타계정 참여 리뷰비를 본인 계좌로 받는 흐름 정상) 일치면 통과.
- **판정→동작**: `SELF`/`SUB` 통과(SUB는 빈 주소/계좌 자동보강 — 단 본인 공통계좌는 전용계좌로 오기록 안 함) / `NEED_CONFIRM`(이름·전화는 맞는데 주소·계좌 상이) → 프론트 확인 다이얼로그 → `identityConfirmed=true` 재제출시 통과+`logAbnormal` 기록 / `NEED_SUB_REGISTER`(본인·타계정 모두 불일치) → **"내 정보와 다른 정보가 감지되었습니다. 현재 입력값을 나의 타계정으로 등록할까요?"** 다이얼로그 → `saveSubAccounts`로 자동 등록(계좌 포함) 후 제출 계속.
- **사전검증**: 제출 직전 `POST /api/reviewer/identity-precheck`(무인증, phone8 스코프)로 프로필 완비+주문별 판정을 미리 받아 다이얼로그 처리 → 서버 게이트는 최종 방어. **fail-open 원칙**: 게이트 내부 오류·Gemini 장애는 주문 접수를 막지 않음(라이브 핫패스 보호).
- **타계정별 전용계좌**: `sub_accounts` JSON에 `bankName/bankAccount/accountHolder` 추가(마이그레이션 불필요). `index.html` 타계정 폼에서 편집(수정 시 기존 필드 보존 병합), 미입력=본인 공통계좌 사용. 회귀가드 `tests/identityMatch.test.js`.
- ★★ **제출 후 계좌 자동 저장은 blank-only (2026-08-19)**: `search-app.js` 는 제출 성공 후 **1번 카드**의 은행/계좌/예금주를 로그인 리뷰어의 마스터 계좌에 저장하는데, 1번 카드가 **타계정 명의면 본인 대표계좌가 타계정 계좌로 덮여** 입금 대상 추출(`payment.service._loadAccounts` — 본인 건은 `reviewers.bank_account` 를 그대로 쓴다)이 **본인 리뷰비를 타계정 계좌로 송금**한다. `submit.routes` 의 타계정 자동보강이 "본인 공통계좌와 **다른** 계좌일 때만" 타계정에 기록하는 것과 정면으로 어긋나던 경로(한쪽은 막고 한쪽은 뚫었다) — 게다가 **계좌는 제출 필수 게이트**(`profileMissing`)라 이 호출이 닿는 시점엔 이미 등록돼 있어 **사실상 덮어쓰기 전용**이었다. → 그 호출에만 `onlyIfEmpty:true` 를 실어 `saveBankInfo` 가 **칸 단위 blank-only**(빈 칸만 채움)로 동작한다(`manualOrder.service` 의 백필 규율과 같다). ★ **미전송 = 종전 덮어쓰기** 라 **내정보 화면의 계좌 '변경'은 동작 불변**(항상 blank-only 로 바꾸면 계좌를 영영 못 고친다 — 변이시험이 잡는다). 회귀가드 `tests/reviewerBankFillOnly.test.js`(정적 배선 + `PGTEST_URL` 시 진짜 PG 4갈래 실행, 변이시험 검출 확인).

### 외부모집 구매양식 관리자 수동제출 (슬래시양식 · 스키마 변경 0)
- **배경**: 리뷰어 페이지로 참여하지 못하는 외부 리뷰어를 카톡으로 모집해 **관리자가 구글시트에 직접 수기 입력**해 왔다. 그 수기 입력이 "유령 written"(DB=성공/시트=없음, 062 참조) 사고의 유발 조건이었고, 참여형 모집 정원에도 안 잡히며, 외부 리뷰어는 시스템에 없어 리뷰캡쳐를 우리 경로로 못 냈다 → **관리자가 슬래시양식을 붙여넣으면 리뷰어가 직접 제출한 것과 같은 길**로 접수한다. 신규 테이블·컬럼 0(전부 기존 원장·큐·업로드 인프라 재사용).
- **슬래시양식 파서**(`utils/slashForm.js`, 순수함수): `리뷰어/수취인/아이디/전화번호/주소/은행/계좌번호/예금주/결제금액`. ★★ **주소 슬래시 병합이 핵심 불변식** — 주소엔 `/`가 자연스럽게 들어가(`월드컵로 12/3, 302호`) 단순 `split('/')`면 칸이 통째로 밀려 은행 자리에 주소 조각이, 금액 자리에 예금주가 들어간다(`index-app.js`의 `opt.split("/")[0]` 전례가 같은 함정을 밟았다). **앞 4칸·뒤 4칸은 위치가 고정**이므로 가운데 조각을 전부 `/`로 재결합해 주소로 만든다. 10자리 전화는 오류가 아니라 **경고**(제출은 하되 `registerReviewer`가 11자리만 받으므로 자동등록만 생략).
- **슬래시 누락 자동 보정**(`repairSlashParts`): 실무에서 가장 흔한 오타는 칸 사이 슬래시 하나가 빠지는 것(`…/이시현15800` = 예금주+금액). 값의 생김새로 자리를 찾아 규칙으로 먼저 나눈다(즉시·무료·오프라인) — 예금주+금액 / 은행+계좌 / 계좌+예금주 / 아이디+전화 / 전화+주소 / 주소+은행 6종, 슬래시 2개 누락까지. ★★ **왼쪽부터 먼저 걸리는 자리를 자르면 안 된다** — `kirei223` 같은 아이디도 "이름+숫자"로 보여 `kirei`/`223`으로 갈리고 정작 붙은 칸은 그대로 남는다(실측) → **모든 자르기 조합을 만들어 `scoreParts`가 가장 높은 것**을 고른다(전화·금액이 제자리에 없으면 −1 = 채택 안 함). ⚠ **리뷰어+수취인이 붙은 경우는 시도하지 않는다**(한글 이름 둘의 경계는 알 수 없고, 잘못 자르면 남의 명의로 접수). 보정 못 하면 `diagnoseMissing`이 **무엇이 안 보이는지**(은행·주소·계좌…) 짚어 준다.
- **AI 폴백**(`gemini.service.repairSlashFormLine`, env `SLASHFORM_AI_REPAIR=0`으로 끔·`SLASHFORM_AI_REPAIR_MAX` 기본 10): 규칙이 못 푼 줄(순서 뒤섞임·라벨 혼입·목록 밖 은행명)만 보낸다. ★ **값을 지어내지 말라**고 프롬프트에 못박고, 이름·전화가 없으면 결과를 버린다(fail-open = 원래 오류 메시지 유지). ★★ **AI 출력도 9칸으로 재조립해 같은 `parseSlashLine`에 통과시킨다** — AI가 파서를 우회해 그대로 접수되는 경로를 만들지 않는다.
- **보정은 반드시 드러낸다**: 미리보기 표에 `🔧 자동보정`/`🤖 AI 보정` 배지 + 경고 문구 + 헤더 요약. 조용한 자동수정은 금지(관리자가 원문과 대조해야 한다).
- **제출 경로**(`manualOrder.service.js` `submitExternalOrder`): ① 리뷰어 등록·연결 → ② **`createOrderLedgerEntry` 단일 진입점 재사용**(diag의 `order-manual-add`처럼 행배정을 인라인 복제하지 않는다 — 그쪽은 이미 campaignHold 미처리로 드리프트했다) → ③ 참여형이면 정원 차감 → ④ `enqueue('order_append')`+`markOrderQueued`. **건별 독립 처리** — 5건 중 1건이 실패해도 나머지는 접수된다(카톡 재수집 비용 회피).
- ★ **리뷰어 등록/백필 규율**: 등록은 `registerReviewer` 단일 경로(ON CONFLICT 3분기 멱등성 상속). **주소·계좌 백필은 `CASE WHEN COALESCE(col,'')='' THEN` 으로 빈 칸만** 채우고 **대상은 `phone`(UNIQUE)** 으로 지목한다 — 기존 `saveBankInfo`/`saveAddress`는 값이 있어도 덮어써서 **이미 등록된 리뷰어의 계좌를 카톡 값으로 갈아치우는 정산 사고**가 나고, `phone8`은 GENERATED·비유니크라 같은 뒤8자리 타인 행을 건드린다. 타계정 건(리뷰어≠수취인)은 소유자를 이름으로 찾아 `sub_accounts`에 등록하되 **동명이인 2명 이상·미발견이면 연결하지 않고 경고**(틀린 사람에게 붙이느니 빈 값).
- **정원 차감**(`confirmExternalApplication`): 잠금 계층은 기존과 동일 **`recruit_campaigns` 행 FOR UPDATE → 신청 행**. 살아있는 `applied` 홀드가 있으면 그걸 확정하고 없으면 `status='submitted'` 신규 INSERT(중복 행 금지) → `maybePersistClosed`. 같은 (캠페인, 명의) 확정 참여가 이미 있으면 차단(이중 차감 금지). 정원 초과는 **기본 허용하되 경고**(외부모집은 이미 약속된 구매다). 출처는 `source='admin_external'` — ★ Track B가 `source==='manual'`을 "재투영 면역 물리행"으로 특수취급하므로 그 값과 겹치면 안 된다.
- **라우트** `/api/manual-order/*`(`preview`·`reviewer-search`·`submit`) — **전부 `authMiddleware + adminOrMasterMiddleware`**. ★ 무인증 `/api/submit/order`를 재사용하지 않은 이유: 그 경로는 `loginPhone8` 없으면 신원게이트를 통째로 건너뛰게 설계돼 있어(레거시 호환) 관리자 기능의 정식 문을 그 우회로에 두면 무인증 표면이 그대로 노출된다. `reviewer-search`는 PII라 **연락처 뒤 4자리만** 반환. `MAX_LINES=50`(오붙여넣기 사고 방지).
- **진입점 3곳 · 공용 모달 1개**(`frontend/js/manual-order.js`, `window.ManualOrder.open({sheetId,tabName,gid,campaignId,title})`): ① 관리자 대시보드 **작업 탭 관리 상세**(`openManualOrderForTab` — 탭 단위라 `campaignId:null`) ② **참여형 관제 패널(📡)** 헤더 `#ccMoBtn` ③ **리뷰어 홈 참여형 카드**의 🧾 칩. 연결 탭 문맥 해석은 **단일 렌더러 `campaign-cards.js`에만**(`_cacheMoCtx`/`openManualOrder`) — 진입점마다 따로 구하면 화면마다 다른 탭에 쓰는 사고가 난다. 공개 목록 응답엔 `linked_*`가 없어(공개 화이트리스트 제외) 캐시가 비면 `GET /api/campaign/:id`(admin JWT=전체 행)로 보충한다.
- ★ **③의 토큰 게이트 + 홈 관리자 로그인**: 리뷰어 홈 칩은 **`_realAdminTok`(진짜 admin_token)일 때만** 노출한다. `admin_token`은 `sessionStorage`(탭 단위)라 `/admin`을 거치지 않은 탭엔 없으므로, 공고수정 허용명단 배너(`#adminModeBanner`)에 **[🔑 관리자 로그인]**(`openHomeAdminLogin`/`doHomeAdminLogin`, 기존 `POST /api/admin/login` 재사용 = **서버 변경 0**)을 두어 그 자리에서 토큰을 받게 한다 — 성공 시 `loadRecruitPreview` 재호출로 ⭐·🧾가 함께 나타난다(둘 다 같은 게이트). 스코프 토큰을 `/api/manual-order/*`로 넓히지 **않는다**(이름+전화 뒤8자리 인증에 리뷰어 등록·원장 기록·정원 차감·시트 쓰기를 붙이지 않는다). `doLogout`이 `admin_token`도 제거(공용 PC 권한 잔류 방지).
- ★ **③의 토큰 게이트(상세)**:  리뷰어앱 공고수정 스코프 토큰(`via:'reviewer_campaign'`)은 `PUT /api/campaign/admin/:id`+`POST /api/product/preview`로만 격리돼 `/api/manual-order/*`에 도달할 수 없으므로(403) 버튼을 보여주면 막다른 길이 된다(⭐ 별표 칩과 같은 규율).
- **구매캡쳐**: **1단계(양식 붙여넣기) 화면에서 먼저 여러 장 받아 둔다**(`STAGED` — Ctrl+V·드래그·파일선택 3경로, 텍스트 칸에 이미지를 붙여도 캡처로 받는다). 줄이 나뉜 뒤 `distributeStaged`가 **캡처에서 읽은 수취인 이름 → 연락처 뒤 4자리 → 붙인 순서** 우선순위로 각 줄에 배정(이름을 못 읽어도 첨부가 버려지지 않는다, 이미 붙은 캡처는 미덮어씀). 2단계 표의 줄별 첨부는 보완 경로로 그대로 유지. `image-extract`로 주문번호 자동 인식 → 제출 성공 건만 `image-upload`+`orderSubmissionId`로 연결(신규 저장소 0). `imageApiLimiter`는 유효 JWT면 skip이라 일괄 처리에도 안 막힌다.
- ★★ **빈 값은 시트를 지운다(블로커였던 것)**: `mapOrderToSheetRow`는 날짜·옵션 칸에 `''`를 반환하고 `buildBatchUpdateData`는 `null`만 걸러내므로, `dateStr`/`selectedOptKey`를 비워 보내면 **그 칸을 지우는 쓰기**가 된다. 로스터 행엔 구매일자·옵션이 미리 적혀 있고 **063이 "그 날짜 칸의 값 개수"로 그날 정원을 파생**하므로, 한 건 제출할 때마다 그날 계획 물량이 1 줄어드는 이중 차감이 난다(마감일이 앞당겨져 `schedule_ended`로 뒤집힐 수도). → ① `dateStr`은 리뷰어 제출과 같은 `M / D (요일)`(`todayKstDateStr`) ② 옵션은 화면값→살아있는 홀드→옵션 1종이면 그것, 그래도 못 정하면 **배정된 행의 기존 옵션값을 되쓴다**(`existingOptionKeyAt` — 지우는 대신 원상 복귀, 원장·큐 페이로드 동시 갱신으로 reconcile 재기록까지 일치). ⚠ **공유 매퍼는 고치지 않는다** — `order_cancel`의 칸 비우기와 Track B write-back의 컬럼 disjoint 마스크(`_wbOrderMappedMask`)가 `''` 반환에 의존한다.
- **오배정·중복 방어**: ① 정원 차감 전 **`tabMatchesCampaign`으로 공고↔탭 결속 검증**(campaignId·sheetId가 독립 입력이라 낡은 화면이면 "A 탭에 쓰면서 B 공고 정원을 깎는다" — 캠페인 행을 이미 잠근 뒤라 비용 0) ② **24시간 내 같은 (시트,탭,연락처) 재접수 차단**(`duplicate`, `force`로만 우회) — 주문번호 없는 건이 대부분이라 dedupKey가 `osid:<uuid>` 폴백으로 매번 새 값이 되어 원장만으로는 못 막고, **재붙여넣기는 결과 화면이 안내하는 예상 복구 동작**이다 ③ 확정 불가한 참여형 건은 **원장 기록 전에** 걸러낸다(시트 행만 생기고 정원은 안 깎이는 어긋난 상태 방지) ④ 서버가 자릿수까지 재검증(phone8 8자리 미만이 원장·정원 키가 되는 것 차단).
- ★ **사칭 차단**: 타계정 소유자는 **붙여넣은 이름만으로** 찾으므로 이름 한 번 겹치면 실존 리뷰어 신원이 남의 계정에 매달린다 → 063과 같은 `CAMPAIGN_SUB_REGISTERED_POLICY`(block 기본)로 **이미 등록된 번호는 남의 `sub_accounts`에 붙이지 않는다**. 동명이인 2명 이상·미발견도 연결하지 않고 경고(틀린 사람에게 붙이느니 빈 값).
- **운영 배선**: 큐 등록 후 리뷰어 제출과 같은 `kickOrderBatch`/`kickQueuePump`(없으면 cron까지 시트에 안 뜨고 현황 위젯도 반응 안 함). 오류 메시지는 `/api/manual-order/`를 `isAdminApi`에 넣어 **마스킹하지 않는다**(관리자 도구는 실패 원인이 곧 조치 안내). 캡처는 업로드 전 1920px·JPEG로 축소(본문 10MB 상한 — 전체화면 캡처 413 방지)하고 **업로드 응답을 확인한 뒤에만** "첨부됨"으로 보고.
- **직원용 유저플로우 목업**: `frontend/docs/외부모집_구매양식_수동제출_안내.html`(개발용어 없는 6단계 와이어프레임).
- ★★ **일 정원(오늘 몫)은 확인을 받는다 — 막지는 않는다 (사용자 확정 2026-08-19 "나"안)**: 이 경로는 **총 정원(`recruit_total`)만 보고 일 정원(`daily_limit`)은 아예 보지 않아**, 오늘 몫이 찬 뒤에도 **아무 신호 없이** 들어갔다(운영 실측: 정원 15인 공고 두 개에 각 +2 → 확정 17, 카드가 `17/15명 완료`). 리뷰어 참여(apply)에는 일 정원 게이트가 있는데 이 경로만 통과했다.
  - ★★ **판정 사본 금지 = `dailyRemainingForCampaign`** 이 `computeCampaignState` 를 **그대로 태운다**(카드 게이지·apply 게이트·[📅 인원]과 같은 값). 여기서 `daily_limit` 을 직접 세면 066 이월·095 조절·098 보류·총량 clamp 를 모르는 두 번째 기준이 생겨 "화면은 15인데 서버는 20"으로 갈린다. **진행 중 홀드도 자리를 차지한 것으로 센다**(`st.todayCount`).
  - ★★ **판정은 두 곳, 성격이 다르다**: ① **라우트가 배치 시작 전에 1회**(`items.length > remaining`) → 초과면 `needConfirm:'over_daily'` + 숫자로 되돌린다 = **쓰기 0건**(건별로 막으면 "앞 3건은 들어가고 뒤 2건은 거절"이라는 부분 처리가 남아 담당자가 재붙여넣기를 하게 된다 = 중복 접수의 입구) ② **서비스가 건별로**(`남은 자리 0`) = 낡은 화면이 사전 판정을 건너뛰고 호출하는 경로의 최종 방어. **②는 원장 기록 전에** 둔다(뒤에서 거절하면 시트 행만 생기고 정원은 안 깎이는 어긋난 상태).
  - ★★ **확인값은 서비스까지 전달돼야 한다** — 라우트가 `allowOverDaily` 를 `submitExternalOrder` 에 안 넘기면 확인해도 ②가 다시 거절해 **"확인했는데 계속 막히는" 막다른 길**이 된다(변이시험이 실제로 뚫은 구멍 — 가드는 **정원이 꽉 찬 픽스처**로 봐야 잡는다. 남은 자리가 있으면 ②가 애초에 발동하지 않아 통과한다).
  - ★★ **외부모집은 "구매 전 예약"이 아니라 "이미 구매가 끝난 건의 사후 등록"이다(사용자 지적 2026-08-19)** — 그래서 확인창은 **허가를 구하는 질문이 아니라 고지**다. 종전 문구(`그대로 진행할까요?`)는 취소가 안전한 선택처럼 읽혔지만, **취소해도 구매는 되돌아가지 않고 기록만 빠진다**(되돌릴 수 없는 구매가 시스템에 없는 상태 = 초과보다 나쁘다). 지금 문구는 `오늘 모집인원을 넘겨 기록됩니다` + **취소의 실제 의미**(`이 건은 시스템에 남지 않습니다 — 나중에 다시 접수해야 합니다`)를 말한다. 어휘도 `제출` → `접수`(사후 등록).
  - ★ **모르면 막지 않는다** — 조회 실패·참여형 아님·일건수 0(무제한)은 `null` = 종전 동작(외부모집은 이미 약속된 구매라 우리 오류로 접수를 막는 쪽이 나쁘다). ★ **총 정원 검사는 무변경**(기본 허용 + 경고). ★ 확인 후 접수는 **결과 화면에 문구로 남긴다**(조용한 초과 금지 — 이번 사고가 정확히 "넘긴 줄 몰랐다"였다).
  - ⚠ **가드 함정 2종(둘 다 변이시험이 잡았다)**: 서비스가 `createOrderLedgerEntry` 를 **require 시점 구조분해로 캡처**해 export 스텁이 안 먹는다 → "스텁 호출 수" 대신 **실제 도달 여부**(DB 없는 테스트에서 원장에 닿으면 예외)로 고정 · 라우트 조건은 **핸들러를 실제 호출**해야 `if (false)` 변이를 잡는다(정적 검사는 통과시킨다).
- 회귀가드 `tests/manualOrderExternal.test.js`(파서 순수함수 실행 · 스텁 db/client로 **서비스 실제 호출** · 라우터 스택 실검사 · 프론트 배선 · 데이터 보전(E) · 마감 카드 표시(F) · **일 정원 확인 게이트(J — 라우트 실제 호출·변이시험 검출 확인)**).

### 구매양식 "제공정보" 추가안내 (제공정보 메모 · 회사 사업자번호)
- `tab_configs.provider_memo`(034): 탭별 자유 텍스트 "제공정보 메모"(진행방식 안내·특이사항 통합). 관리자 대시보드 탭설정 팝오버에서 편집. 구매양식 제출화면(`search.html`)의 "📦 제공정보" 카드에 표시되며 **공란이면 영역 미노출**.
- `app_settings.company_business_no`: 회사 공통 사업자번호 1개(관리자 "설정" 탭에서 편집, `POST /api/tab/company-business-no` = admin/master). 진행방식(`income_type`)이 **현영 포함(사업자현영)** 인 탭에서만 "지출증빙 현금영수증 발행 필수" 안내와 함께 노출.
- 폼은 진입 시 `GET /api/tab/provider-info?sheetId&tabName`(무인증, DB만 조회)로 `providerMemo/incomeType/companyBusinessNo`를 받아 렌더(`_loadProviderInfo`). 메모 URL은 `_linkifyMemo`로 링크화하되 href 속성 breakout XSS 방지(따옴표 제외 매칭).

### 폴더 소유권 & 자동 생성 (예방)
- 폴더/파일은 **OAuth(`DRIVE_OAUTH_REFRESH_TOKEN` = tnaks6325) 우선 → 실패 시 SA 폴백**으로 생성된다(`createFolder`/`uploadFileBase64`). `_normalizeOwner`가 생성 직후 소유자를 `DRIVE_OWNER_EMAIL`로 보정한다(SA→tnaks 이전은 구글이 막아 무시될 수 있음 — 그래서 OAuth 우선이 핵심).
- 폴더는 ① 첫 업로드 시 on-demand(`review-upload`), ② 배치(`POST /api/drive/sync-review`·`sync-capture`), ③ **스마트빌드 주기마다 자동**(`reviewFolders.service.js`의 `ensureReviewFoldersForActiveTabs`, `smartBuild` 말미 best-effort)로 생성된다. ③ 덕분에 **신규 활성 탭은 제출 0건이어도 tnaks 소유 `[리뷰]`/`[구매캡처]` 폴더가 미리 생성·연결**된다(비파괴·idempotent, `리뷰폼` 탭 제외, 1주기 최대 30탭).
- 관리자 점검: "리뷰폴더 현황 점검"(`POST /api/drive/folder-audit`)이 연결/정상/빈/미연결 + **폴더 소유자 집계**(tnaks/박세희/박은비/SA)를 보여주고, 미연결 탭은 현황 화면의 버튼으로 일괄 생성·연결한다. 파일 단위 소유자·용량은 "소유권"(`ownership-audit`/`transfer-ownership`)에서 확인·이관한다.

### 시트/Drive API throttle — lane 분리 (쿼터 근본해결)
- `utils/sheetsThrottle.js`는 **2-lane**: ① **sheets lane 45/분**(`throttledCall` — spreadsheets.* 전용, `SHEETS_ORDER_RESERVE`(8)·저우선 서브캡·`getThrottleStatus` 시맨틱 불변), ② **drive lane 120/분**(`driveThrottledCall` — `getSheetModifiedTime`(drive.files.get)/`shareSheetWithServiceAccount`(drive.permissions) 전용, 평평한 cap). **Drive API는 별도 쿼터인데 과거 시트 lane을 오점유**(smartBuild가 5분마다 시트 ~57개 modifiedTime 폴링 = 모니터 "리뷰인덱스빌드 33/분"의 정체)하던 것을 이관. env: `DRIVE_REQUESTS_PER_MINUTE`.
- **팬텀 슬롯 제거**: `concurrentMap`(무기록 동시성 map, allSettled 계약) — fn 내부가 스스로 `*ThrottledCall` 하는 호출처(rawMirror/indexBuilder/indexScan)는 map 레벨 슬롯 기록 없이 실 API콜만 계량. `throttledMap`은 레거시(내부 throttle 없는 fn 전용)로 유지.
- `getThrottleStatus`는 **시트 lane 전용 계약**(busy-gate 소비처들이 "시트 여유"로 해석) — drive 필드 추가 금지. 모니터(`getThrottleMonitor`/`throttle-monitor.html`)는 시트 lane 기존 필드 불변 + `drive:{…}` 하위객체(구백엔드 응답이면 프론트가 Drive 카드 자동 숨김).
- 부속 방어(레드-블루-심판): RAW미러 cron은 **부팅유예 2분**(`RAW_MIRROR_BOOT_GRACE_SEC`) + **`withJobLock('raw_mirror_all')` 인스턴스 직렬화**(rolling 배포 이중미러 차단) + bulk 사이클 중 시트 lane>`RAW_MIRROR_YIELD_THRESHOLD`(30)면 시트 단위 연기(`sheetsDeferred`, 단일미러/`_triggerSheetMirrorOnce`는 미적용=자가치유 보존). smartBuild·rawMirror의 Drive 변경감지 **연속실패 ≥`*_DRIVE_FAIL_SKIP_AFTER`(2)면 "변경 간주" 대신 그 시트 1사이클 skip**(drive 장애→풀리드 폭풍 전이 차단). 'RAW미러'도 저우선 라벨 기본 포함(주문 예약 8슬롯 보호). 회귀가드 `tests/throttleLanes.test.js`·`tests/driveLaneCallsites.test.js`.
- **정기 인덱스빌드 시트 lane 추가 감축**(smartBuild): ① **modifiedTime 캐시 영속화**(`app_settings.smart_build_modified_cache`, 사이클 말미 스냅샷·첫 실행 복원) — 재배포마다 "전 시트 변경 간주" 콜드스타트 스윕(~30콜/분 수 분) 제거. `resetSmartBuildCache`는 영속 키 삭제+1회성 복원스킵 플래그로 강제 전체갱신 의도 보존. ② **시트 lane 중간 양보** — 변경시트 처리 중 사용량>`SMARTBUILD_YIELD_THRESHOLD`(25)면 남은 시트 연기+캐시 무효화(다음 주기 재감지=유실 없음). 관리자 강제실행/DB재구축은 `runSmartBuild({noYield:true})`로 완주. ③ **실패 시트 캐시 무효화**(미반영 데이터가 "반영됨"으로 영속되는 것 방지, 연속 `SHEET_ERR_RETRY_CYCLES`(3) 초과 시 재시도 중단 — 백스톱 04시 전체빌드). ④ 주기 env `SMART_BUILD_INTERVAL_SEC`(기본 300). 제출 헤드룸을 env만으로 더 늘리려면 `SHEETS_ORDER_RESERVE` 상향(8→15 등). 회귀가드 `tests/smartBuildQuota.test.js`.

### 구매양식 제출 = DB-first 원장 + 시트 비동기 미러 (order-ledger)
- 구매양식 제출(`POST /api/submit/order`)은 **DB(`order_submissions`)에 먼저 확정**(`orderLedger.service.js`의 `createOrderLedgerEntry`) → RAW 미러(`raw_sheet_tabs.detected_headers`/`raw_sheet_rows`) 기반으로 행을 **원자적 배정**(`claimRow`, `sheet_row_claims` 행/dedup 2중 유니크 = 멱등·중복행 불가) → `enqueue('order_append')` → 큐 워커가 throttle(`sheetsThrottle`, 45/분)로 시트에 기록. 시트 통읽기 없음 = 쿼터 안전.
- 주문 상태는 `order_submissions.mirror_status`: `pending`→`queued`→`written`, 또는 행 배정 실패 시 `pending_no_row`, 쓰기 실패 시 `failed`.
- **데이터 손실 방어(레드/블루/심판 P0)**: ① **dedupKey osid 폴백**(`computeDedupKey`) — 주문번호 6자리 미만(쿠팡 비번호/분할주문)이면 `osid:<id>`로 별개 주문을 별개 행에 배정(같은 행 공유로 한 건 소실 차단). reconcile은 `row.dedup_key` 재사용으로 멱등 유지. ② **다중컬럼 가드**(`buildMirrorGuardRanges` — 연락처+주소+수취인) + 쓰기 직전 **그리드 캐시 무효화**(`invalidateSheetMeta`) — 한 칸만 보던 수동입력행 덮어쓰기 + 그리드밖 stale `[]`오판 차단. 차단 시 claim 해제 → 다음 reconcile이 더 아래 행 재배정. ③ **동명탭 보류** — gid 없이 동명 탭 다수면 오배정 대신 보류(`pending_no_row`, 미러로 gid 채워질 때까지 자가치유). ④ **42P01(claims 테이블 부재) 배정 금지** — 부팅 윈도우에 무검증 배정 대신 보류.
- **멀티인스턴스/동시성 방어(#1·#2)**: ⑤ **큐 클레임 원자화(#2)** — `processQueue`/`drainTabQueue`가 `UPDATE … WHERE id IN (SELECT … FOR UPDATE SKIP LOCKED) RETURNING *`로 batch를 한 번에 `processing` 점유(`syncQueue.service.js`). 멀티워커/멀티인스턴스(또는 rolling 배포 중 old+new 공존)에서 같은 큐항목 이중처리(중복행)·정체 차단. 클레임은 `attempts`를 올리지 않고 실행 직전에 +1(원본 시맨틱), `processed_at=NOW`로 stuck-detector(매시 `retryAllFailed`) 오리셋 방지. Quota 에러로 배치 중단 시 남은 점유분은 `pending` 복원. ⑥ **reconcile 직렬화(#1)** — `utils/jobLock.js`의 `withJobLock('order_reconcile', …)`(`pg_try_advisory_lock`, 전용 커넥션, busy면 즉시 양보)로 cron reconcile·인라인 `_triggerSheetMirrorOnce`·`order-reconcile`/`order-flush-tab` reconcile을 하나의 락으로 직렬화 → 동시 행배정 경합(churn/stall) 제거. `dryRun`은 락 불필요. unlock 실패 시 커넥션 폐기로 락 강제 해제(락 누수 방지).
- **막힌 주문 자동복구(reconcile)**: `reconcileStuckOrders`가 `pending_no_row`/`failed`/정체 `queued` 주문을 찾아 행 재배정 후 재큐잉. cron `*/2`(RAW 미러 `*/5` **뒤**에 둠 — 메타 채워진 뒤 복구, throttle busy면 양보) + 관리자 강제 `POST /api/diag/order-reconcile`(admin/master). **메타 없는 탭은 skip**(다음 RAW 미러까지 자가치유). **복구분은 시트 하단에 append + 비고란 표기**(`_markSystemMemo(memo, reason)`, 배경색 없음 — 일반 복구 `[시스템 재기록]` / **사후검증이 소실 판정한 사고 건은 `[시스템 재기록 · 확인요망]`**(reconcile이 `sheet_error ^= 'ghost written'`로 판별해 `recoverReason:'lost'` 전달), 기존 메모 보존·멱등·약한표기 승격)로 기록해 직원 수동입력분·중복과 구분. 평상시 실시간 주문은 제자리(in-place).
- **가드차단 자동 재배정(기본 ON) + 시트 변경 감지** (7/2 사고 재발방지, 레드-블루-심판 설계): 다중컬럼 가드가 쓰기를 차단하면(배정행이 타인 데이터 = 시트 수동 재배치 등) `_guardBlockDecision`이 defer/release/giveup 결정 — ① 제출 `ORDER_BLOCK_GRACE_SEC`(90s) 미경과 실시간건은 **defer**(일시 write 경합 오판 방지), ② 경과·복구분은 **release**(큐 done 먼저→claim 해제·`sheet_row` NULL·failed→reconcile이 하단 빈 행 재배정(비고 `system` 표기) = **남의 데이터 절대 안 덮고 빈 행 폴백**), ③ `reassign_count`(migration 043) ≥ `ORDER_MAX_REASSIGN`(5)이면 **giveup**→`mirror_status='stuck_manual'`(수동입력 대상, reconcile·스케줄러 제외 = 무한 append 종결). 끄려면 `ORDER_GUARD_REASSIGN=0`. `stuck_manual`은 현황 위젯 🚨배너+`needManual`+수동CSV(`order-stuck-export`)로 능동 노출. **차단 발생 = 시트 변경 신호** → `triggerSheetMirrorOnce`(60s/시트 debounce)로 그 시트 자동 재미러+리컨실 = 관리자가 시트를 손으로 편집해도 새로고침 클릭 불필요(백스톱: RAW 미러 cron `*/5`). throttle은 `SHEETS_ORDER_RESERVE`(8)로 리뷰인덱스빌드(저우선 서브캡 45-8=37)가 주문쓰기 슬롯을 못 굶김.
- 관리자 가시성: `GET /api/diag/order-mirror-status`(카운트+막힌탭+`hasRawMeta`+`tabGid`/`displayName`/`noRow`) + 대시보드 "구매주문 시트반영 현황" 위젯(원클릭 복구). 보조 진단: `GET /api/diag/order-written-sample?sheetId&tabName`(시트 반영 완료건을 `sheetRow`+주문자와 함께 — 복구행(비고 `system`) 육안 대조용). `GET /api/diag/order-stuck-export?sheetId&tabName`(admin/master, PII): 시트 미반영(written 아님) 주문의 전체 필드를 추출 — 자동복구가 수렴 못 하는 탭(예: 비연속 행 구조·가드 반복차단)에서 직원 수동 입력용 목록 확보.
- **탭 리네임 복구**: 시트 탭 이름이 바뀌면(예 `…100건`→`…81건`) gid 없이 저장된 옛 주문이 현재 탭과 매칭 안 돼 영구 적체됨. `POST /api/diag/order-relink {sheetId, fromTabName, toTabGid, toTabName?, dryRun?}`(admin/master)가 막힌 주문의 `tab_gid`/`tab_name`을 현재 탭으로 backfill + `sheet_row` 초기화 → 이후 reconcile이 현재 탭 하단 빈 행(비고 `system`)으로 복구. `loadRawTabContext`는 메타가 비어도 저장된 RAW 행에서 헤더 재탐지(자가치유)하므로 gid/이름만 맞으면 RAW 재미러 없이 복구된다(`detected_headers` NULL = 단순 stale 스냅샷).
- RAW 전체 미러(`POST /api/raw/mirror {force}`)는 `campaigns`∪`tab_configs`에 등록된 시트만 순회(`rawMirror.service.js`). 미등록 시트의 막힌 주문은 reconcile 자가치유로 처리(전체 미러 불필요).
- **단일 시트 미러(`POST /api/raw/mirror {sheetId, force?}`)**: `sheetId`를 주면 등록 전체를 순회하지 않고 **그 시트만** 재읽기(`mirrorOneSheet`) → 특정 캠페인 RAW만 빠르게 갱신(시간 절약). inline 동기 실행이라 `{tabsMirrored,tabsSkipped,rowsWritten,errors}` 결과를 즉시 반환(전체 미러는 비동기+폴링). 한 캠페인만 stale일 때(예: throttle busy로 cron이 오래 못 미러한 탭) 이걸로 그 시트만 새로고침 후 reconcile/flush가 실제 행 끝에 정상 append.
- **탭별 구매양식(DB) CSV 다운로드**: 관리자 **RAW 미러 데이터 뷰어(`raw-mirror.html`)** 에서 탭 선택 시 **"구매양식 CSV"** 버튼 노출 → 그 탭에 **실제 제출된 구매양식(서버 DB `order_submissions`)** 을 CSV로 받는다(시트 미러가 아니라 원장 원본 — 시트 붙여넣기 일괄적용용). 인증 헤더 fetch→blob. 백엔드는 `GET /api/diag/order-stuck-export?sheetId&tabName&includeWritten=true&format=csv`(admin/master, PII; 상태·시트행·주문필드·제출시각 컬럼, UTF-8 BOM). 시트 미러 자체(raw_sheet_rows)를 받으려면 별도 `GET /api/raw/rows.csv?sheetId&gid`.
- `order-relink`의 `toSheetId`: sheet_id 자체가 손상된 주문(단축URL 혼입 등)을 올바른 시트로 이전할 때 사용(시트+탭 동시 정정).
- **백로그 가속 드레인(선택적)**: 큐워커는 평상시 항목당 2초 안전대기(`processQueue`의 `interItemDelayMs` 기본 2000 — 쿼터 보수)로 ~20/분. `POST /api/diag/queue-drain {maxMillis?, batchSize?}`(admin/master)는 그 대기를 **0(지수백오프 포함 전부 제거)**으로 두고 `sheetsThrottle`(45/분)만 가드로 삼아 더 빨리 빼낸다. throttle로 한 배치가 길어질 수 있어 **백그라운드 실행 후 즉시 응답**(HTTP 타임아웃 방지) + `_queueDrainRunning` 중복방지; 진행은 `GET /api/diag/queue-drain`(running/last/remaining)·현황 위젯으로 관찰. 관리자 대시보드 "구매주문 시트반영 현황" 위젯의 **"빠른 반영(가속)"** 버튼으로 온디맨드 실행. **상시 켜는 게 아니라 백로그 있을 때만**(라이브 이벤트 중엔 자제 — 평상시 cron은 그대로 보수적).
- **그리드밖 행 가드읽기 처리**: 복구 append가 현재 시트 그리드보다 아래 행(예 행 268, 그리드 167행)을 가드 읽기하면 `_readSheetByGridData`가 빈 결과를 반환한다(이전엔 `endRowIndex<startRowIndex` 로 "endRowIndex cannot be before startRowIndex" 에러 → 복구 무한실패). 그 행은 데이터 없음=가드 통과가 맞고, 쓰기측 `_batchWriteByGrid`가 `appendDimension`으로 그리드를 확장하므로 정상 기록된다.
- 리뷰어 참여조회(`GET /api/reviewer/my-status?phone8=`)는 **`review_index`(시트빌드) + `order_submissions`(DB) 병합**(phone8=연락처 끝8자리, sheet_row로 중복제거). 시트 반영 전 주문도 `stage:'processing'`로 노출 → DB-first라 리뷰어가 제출 즉시 자기 참여 확인 가능.
- **신원링크(시트행↔phone8) 기록은 "쓰기 성공 후"에만**(리뷰어 교차노출 버그 방어): 리뷰어 대시보드 "리뷰 내역"은 `searchAll`(`/api/search`, `searchByName(name,phone8)`)로 조회한다. 매칭 통과키 = `review_index.phone8`(P0) **또는 (`review_index.phone8`이 공란일 때에 한해) `participation_links.phone8`(P5 확정신원)**. ★ **시트 현재값(연락처=`review_index.phone8`)이 그 행의 소유자를 말하면 그것이 최종 진실이며, 재배정된 행의 stale 확정신원(pl)은 그 행을 열지 못한다**(교차노출 차단·fail-closed). `pl.phone8`은 로그인 phone8, `ri.phone8`은 연락처 컬럼 값이라 서로 다를 수 있는데, 과거엔 게이트 없이 `ri.phone8 OR pl.phone8` 단독 통과라 **한 행에 stale pl(옛 주인)과 현재 ri(새 주인)이 공존하면 그 행이 두 사람 모두에게 노출**됐다(예: A 리뷰어의 옛 주문이 로스터 행에 written→pl=A, 이후 그 행을 B 리뷰어가 정상 사용→ri=B 인데 A 리뷰내역에 B 행이 뜸). **수정**: `search.service.js`(3분기+`searchByNameFallback`)와 `reviewEdit.routes.js`의 `_verifyRowOwnership`(무인증 리뷰이미지 열람/교체요청 게이트) **모두** pl 통과 조건을 `ri.phone8 IS NULL AND pl.phone8=ANY(...)`로 통일(시트 현재값 우선). 회귀가드 `tests/searchSubmittedGuard.test.js`(케이스 5=fallback 게이트, 6=stale pl 미개방/현재주인 개방 구조 고정). **트레이드오프**: 로그인번호≠주문연락처인 정당 케이스에서 그 연락처가 **미등록 타계정**이고 오직 pl로만 보이던 행은 이제 안 뜰 수 있음 → 타계정 등록으로 흡수(`my-status`는 원래 `ri.phone8`만 써 무영향). 상류(주문 행배정이 타 참여자 로스터 행을 first_available로 가져가는 `buildCandidateRows`/`isUnfilledOrderRow`)는 별도 후속 PR(라이브 쓰기 핫패스). 과거엔 구매양식 제출(`submit/order`)이 가드/시트쓰기 **전에** 낙관적 claim 행(`ledger.sheetRow`, RAW 미러 스냅샷의 "빈 행" 추정 — 미러 stale·로스터 선기입 시 *남의 행*)에 `recordParticipationLink`/`recordReviewIdentity`로 phone8을 선기입해 교차노출을 유발했다. **이제 제출 시점 선기입을 제거**하고, 신원기록은 **큐 워커(`syncQueue` `order_append`)가 다중컬럼 가드 통과 + 실제 시트쓰기 성공 후, 실제로 쓴 행에만** 수행한다(신뢰 가능한 링크만 남음). 과거 오염 정리: `POST /api/diag/participation-cleanup {sheetId?,tabName?,dryRun?}`(admin/master, 기본 dryRun=카운트만) — written 주문이 없는 `participation_links` 행(=가짜 신원링크) 삭제. `review_index.phone8` 오염은 강제 전체 재빌드(`POST /api/index/build {forceFullRebuild:true}`)로 시트 값 재유도.
- 운영 순서 규칙: **RAW 미러 → reconcile**(메타가 있어야 행 배정 가능). 메타 자동 공급은 3중: ① 탭 등록 시 `mirrorOneSheet`, ② RAW 미러 cron `*/5`, ③ **미러 안 된 탭에 주문이 오면 `_triggerSheetMirrorOnce`가 그 시트만 백그라운드 1회 자동미러(탭당 60초 debounce) + 즉시 리컨실** — per-제출 라이브 시트읽기를 없애 버스트에도 시트 쿼터 안전(관리자 수동미러 불필요, 근실시간 자동동기화).

### 시트 상단 강제 공지문 (신규 탭 자동 삽입 · `sheetNotice.service.js`)
- 직원이 시트에서 **줄(행)을 삭제하면 그 아래 모든 기록의 행번호가 밀리므로**, "행 삭제 금지·값만 삭제" 규칙을 시트 안에 상시 노출한다. 기본 문구(사유 포함형) = `⚠️ 구매취소·중복이 생겨도 줄(행)은 삭제하지 마시고 값만 지워주세요 · 줄을 지우면 아래 기록의 줄 번호가 전부 밀립니다`(운영 중 교체는 `app_settings.sheet_notice_text`).
- **위치 C1:R1 고정**(템플릿 메타영역 A1 캠페인명 오른쪽 여백, 병합+앰버 배경+굵게) — **행 삽입을 하지 않으므로 기존 기록의 행번호 무영향**(리스크 0). 회귀가드가 `insertDimension` 부재를 고정한다.
- ★★**안전 불변식(완화 금지)**: 공지문에 헤더 탐지 키워드(`HEADER_DETECT_KEYWORDS`)가 **2개 이상**이면 `detectSheetHeader`가 **공지 줄을 헤더로 오인**(공지는 헤더보다 위 + 위에서부터 첫 매칭 확정)해 그 탭 파싱이 통째로 깨진다 → `validateNoticeText`가 쓰기 전·문구 저장 전 양쪽에서 차단. 회귀가드 `tests/sheetNotice.test.js`가 실제 탐지기와 교차검증(위험 문구 주입 시 headerRowIndex가 1로 무너지는 것까지 고정).
- 삽입 시점: `create-campaign-sheet` 두 모드(new=복사 파일 전 탭 / existing=새 탭만) **best-effort**(실패해도 시트·탭 생성은 성공). env `SHEET_NOTICE_ON_CREATE=0`으로 비활성. 기존 탭은 `POST /api/diag/sheet-notice {sheetId, gid|tabName, dryRun?, force?}`(admin/master)로 수동 적용 — **이미 내용이 있으면 force 없이는 덮지 않고**(사람 메모 보호), 같은 문구면 no-op(멱등).

### written 사후검증 + 리뷰어 비정상 한글로그 (유령 written 재발방지, migration 062)
- **배경(7/24 유령 written 사건)**: 구매양식이 DB 접수·시트 기록까지 됐지만 같은 시각 시트 수기 작업과의 경합(가드차단→해제→재배정) 끝에 기록 행이 타인 데이터로 덮여 **"유령 written"(DB=성공, 시트=없음)** 으로 잠복 — written은 종결 상태라 reconcile이 다시 안 봤음.
- **사후검증**(`writtenVerify.service.js`, cron `3-59/5`(미러 `*/5` 3분 오프셋) + `withJobLock('order_written_verify')`, **전부 DB-only=시트 쿼터 0**): written 주문의 기록 행을 RAW 미러와 신원 대조. ① **정상=스탬프만**(`row_verified_at` — 같은 스냅샷 재작업 방지) ② **행 이동**(중간 행 삽입·정렬 — 7/27 #106 삽입 유형)은 유일 매치 시 `sheet_row` 포인터+자기 claim 자가보정(시트 무접촉, info 로그) ③ **소실**=critical 로그+`failed` 강등(claim 해제)→기존 reconcile이 하단 재기록(비고 `[시스템 재기록 · 확인요망]`), **같은 주문 소실 `ORDER_LOST_MAX`(2)회 반복 시 `stuck_manual`**(사람이 지운 행 무한 재기록 루프 차단) ④ 복수 매치=경고만(자동조치 없음).
- ★★**오탐 0 우선 5중 게이트**(오탐=정상 주문 강등→시트 중복 행+오경보 = 막으려던 사고와 동급 부작용이라 전부 fail-safe): ⓐ **판정가능성**(`_canVerify`) — 주문 쪽 강한 키(주문번호 6자리+ / 연락처 끝8자리)와 그 시트 열이 있어야만 판정, 없으면 `unverifiable` 무액션(레거시·관리자 경유 제출 보호) ⓑ **신선도** `mirrored_at > sheet_written_at + FRESH_SEC`(기본 **900s** — mirrored_at은 "읽은 시각"이 아니라 upsert 완료 시각이라 미러 1사이클 상한(15분) 고려) ⓒ **TOCTOU 순서** — `mirrored_at`을 행 데이터보다 **먼저** 읽어 "옛 스냅샷+새 시각" 조합 차단 ⓓ **인플라이트 배제** — pending/processing `order_append`·`order_update` 큐가 있거나 `updated_at > mirrored_at`(편집 반영 대기)이면 보류 ⓔ **커버리지 밖 보류** — 배정행이 미러 최대행보다 아래면 "지워짐"이 아니라 "미러가 못 봄"으로 처리. 판정은 강한 키 일치=mine / 강한 키 **충돌**=other(소실) / 충돌 없고 수취인만 일치=mine(사람이 일부 칸 지운 상태 보호). 대상 조회는 `row_verified_at ASC NULLS FIRST` 회전(고정 정렬의 LIMIT 뒤쪽 영구 미검증 방지). env: `ORDER_WRITTEN_VERIFY`(기본 ON)·`ORDER_WRITTEN_VERIFY_REASSIGN`(0=감지만)·`_DAYS`(14)·`_FRESH_SEC`(900)·`ORDER_LOST_MAX`(2)·`REVIEWER_LOG_NO_CAPTURE`(0=캡처 미첨부 감지 끔).
- **한글 자연어 로그**(`reviewer_event_logs`, `reviewerEventLog.service.js`): 캠페인별 문장 그대로 저장(예: "『탭』에 ○○○ 리뷰어가 구매양식을 제출했으나, 시트에 입력되지 못했습니다."). 유형 = order_lost/order_lost_manual/order_row_shifted/order_row_ambiguous/**order_unmirrored**(stuck·failed 적체)/**order_no_capture**(제출 20분 후에도 캡처 미연결 — `app_settings.reviewer_log_capture_cutoff` 이후 제출분만=소급 오탐 방지). **미해결 (event_type, order_submission_id) 부분유니크**로 크론 도배 방지, 원인 소멸 시 자동해결(`autoResolveHealed` — written 복귀/캡처 도착). 캡처↔주문 연결 = 업로드 페이로드 `orderSubmissionId`(`search-app.js`) → `order_submissions.capture_file_id/uploaded_at`(구형 클라는 탭+수취인 24h 폴백 매칭).
- **표시(2곳, 서버 미해결 상태가 진실원본 — 확인하면 전 화면에서 소멸)**: ① 리뷰웹시스템[3버전] `workdesk.html` **「로그」 탭 → 리뷰어 비정상로그 서브탭**(기본 탭)(`GET /api/trackb/reviewer-logs` 내부인 — **staff(AE)는 담당 업체(inad_pm) 탭 로그만**(`scopedActiveTabs` 서버 강제, 담당 0=빈 목록), via:intranet 격리 범위 안이라 SSO 사용자도 열람) ② 관리자 대시보드 **좌측하단 빨간 중요알림 카드**(critical만, SSE `reviewer_alert` 실시간+2분 폴링, `_raCheckAlerts`/`index-app.js` — wo 카드(우측하단)와 분리). **확인(resolve)은 전역 진실원본이라 `POST /reviewer-logs/resolve` = adminOrMaster 전용**(staff가 타 담당자 미확인 알림 제거 불가, 프론트 버튼도 동일 조건). 회귀가드 `tests/writtenVerify.test.js`.
- **시트에서 지운 행의 처리(취소 vs 유실)**: 시스템은 시트만 봐서는 "사람이 의도로 지움"과 "사고로 사라짐"을 구별할 수 없어 **기본이 재기록**이다(행 삭제·값 전체 비우기 모두 `order_lost`). 그래서 알림에 **[취소 처리] 1클릭**(`POST /api/trackb/reviewer-logs/cancel-order`, adminOrMaster, `CANCELABLE_EVENTS`=order_lost·order_lost_manual·order_unmirrored만)을 두어 사람이 의도를 알려주게 한다 — 누르면 `deleted_at` 소프트삭제(reconcile·사후검증 둘 다 `deleted_at IS NULL` 필터라 되살아남 즉시 중단) + 시트행이 남아 있고 `ORDER_LEDGER_WRITE_ENABLED=true`면 기존 `order_cancel` 큐로 그 행 비우기(큐의 `rowIdentityMatches`가 **그 사이 남이 쓴 행은 안 건드림**) + 같은 주문의 미해결 알림 일괄 정리. 취소 UI는 리뷰웹시스템[3버전] 로그 창과 관리자 중요알림 카드 양쪽에 노출. 참고: 행 삭제로 **아래 행이 밀리는 것은 사후검증이 신원으로 찾아 포인터 자동보정**(시트 무접촉), Track B(`campaign_participants`)는 투영 시 `active=FALSE`로 비활성 표시만 하고 하드삭제하지 않는다.
- 운영 함의: 시스템 구매시간대(오전)엔 시트 수기 입력을 하단(그날 블록 아래)에서 하거나 stuck-export CSV로 일원화 — 상단 빈 행 수기 작업이 이 사건의 유발 조건. **취소는 시트에서 지우지 말고 관리자 화면 [주문취소]로** (직원 안내서 `frontend/docs/구매양식_시트_입력순서_안내.html`).

### ★ 리뷰웹시스템[3버전] 「로그」 = 리뷰어 비정상로그 + 시스템 오류로그 (2탭, 창구 단일화)
- **배경(사용자 확정 2026-08)**: **리뷰웹시스템[3버전]이 메인**이고 관리자 대시보드는 폐기 대상이다. 그래서 nav 「리뷰어 로그」를 **「로그」**로 바꾸고 안에서 **리뷰어 비정상로그(기본) / 시스템 오류로그** 두 서브탭으로 나눈다. 시안 = `frontend/docs/design-log-tabs.html`.
- **데이터는 그대로** — 시스템 오류로그 = 이미 쌓이고 있는 `error_logs`(026/028)이고 수집 경로(`logAbnormal`)도 무접촉. **신규 테이블·마이그레이션 0**, 화면만 옮겼다. 서버는 `/api/trackb/error-logs*` **5경로 프록시**(`_delegate`로 기존 `admin.routes` 핸들러 위임 — SQL·로직 복제 0). Track B 경로에 두는 이유 = C/S·설정과 같다: 인트라넷 SSO 토큰(`via:'intranet'`)은 `/api/admin/*`에 **도달 자체가 불가능**.
- ★★ **표 idiom을 통일한다("모든 오류의 기준 = 리뷰웹시스템[3버전]")** — 시스템 오류도 관리자 대시보드의 *카드 목록 + 우측 패널*이 아니라 **리뷰어 로그와 같은 `.lgtable` + 행 펼침**으로 그린다. 두 탭의 열이 1:1 대응(상태 · 발생시점 · 대상 · 오류내용 · 조치안내)해 탭을 옮겨도 읽는 법이 안 바뀐다. 무거운 **AI 다중에이전트 분석만 모달**(리뷰검수 상세 팝업과 같은 방식, **body 직속** 마운트 — 뷰 스크롤 컨테이너에 넣으면 오버레이가 화면 흐름에 섞인다).
- **권한**: 리뷰어 비정상로그 = 종전대로 내부인 열람(AE는 담당 탭만) · 해결은 adminOrMaster. **시스템 오류로그 = master/admin 전용**(경로·스택·요청 컨텍스트가 실려 AE 담당 스코프로 나눌 수 없다 — 등록리뷰어DB와 같은 판단). AE에겐 **알약 자체를 안 그린다**(서버 게이트가 최종 방어).
- ★★ **관리자 대시보드에서 발생한 오류는 제외 — 단, 로그인 4경로는 예외(완화 금지)**: 판정 단일 출처 = `utils/adminUiErrorFilter.js`. `/api/admin/*`은 이미 `flow='admin'`으로 기록되고(그 표면은 대시보드만 쓴다) 그게 곧 "대시보드에서 난 오류"지만, **`/api/admin/login`·`staff-login`·`advertiser-login`·`intranet-login`은 이름만 admin이고 실제 호출자는 리뷰웹시스템[3버전]**(workdesk 로그인 폴백 4단)다 → 통째로 걸러내면 **작업보드 로그인·SSO 장애가 어느 화면에도 안 뜨는 무신호 상태**가 된다. 회귀가드가 그 4경로가 실제로 workdesk.html에 있는지까지 대조한다.
- ★ **제외는 조용히 하지 않는다** — 목록 상단에 "관리자 대시보드 경로 N건 제외"를 표기하고 `includeAdminUi=1`로 포함해 볼 수 있다. **요약(헤더 뱃지)도 같은 기준으로 센다**(표엔 없는 건이 뱃지에만 잡히면 "미해결 12인데 표는 2줄"이 된다). 원본 `admin.routes` 목록에서 제외는 **opt-in(`excludeAdminUi`)** 이라 다른 소비처 동작 불변. 시트·Drive·AI·DB·큐 등 **공용 인프라 오류는 그대로 남는다**(어느 화면에서 눌러 났든 같은 고장).
- ★ **nav 뱃지 = 두 탭 중요 건수의 합** — `_NAVLG{rv,sys}`에 각각 기억해 두고 합계만 그린다(C/S 뱃지와 같은 규율). **둘 다 미조회/실패면 뱃지를 건드리지 않는다**(숫자가 사라지면 "손볼 것 없음"으로 오독).
- ★ **로딩 자리표시자를 깐 함수는 어떤 예외에도 화면을 종결**시킨다 — `api`는 네트워크 오류에서 throw 하므로 try/catch가 없으면 "불러오는 중…"이 영원히 남는다(리뷰검수 무한로딩 실측 사고와 같은 자리). `_loadSysLogs`는 실패 시 [다시 시도] 버튼으로 끝낸다.
- **제거**: `admin.html`·`admin-siand.html`의 「오류디버깅」 탭 버튼·패널 + `index-app.js`의 그 화면 코드 605줄(탭 분기·뱃지 호출 포함). ★ HTML만 남기고 JS를 지우면 **누르면 ReferenceError로 죽는 탭**이 되므로 회귀가드가 세 화면 모두에서 흔적 0을 고정한다.
- 회귀가드 `tests/workdeskLogTabs.test.js`(라우터 스택 실검사 · 제외 규칙 순수함수 실행 · 대시보드 흔적 0 · **렌더 블록을 vm으로 꺼내 실제 실행**(sandbox에 `isAdmin` 류 전역을 일부러 두지 않아 프리변수가 들어오면 그 자리에서 터진다) · 역할별 알약 렌더 · 제외 표기·포함 재조회). 변이시험 2종(로그인 예외 제거·프론트 권한 게이트 제거)으로 검출 확인. 실제 http 오리진 브라우저 실행으로 admin/staff 렌더·행 펼침·AI 모달 6역할 확인.

### 상시 배치 스케줄러 — 다탭 인터리브(공평) 드레인
- `orderBatchScheduler.js`의 `_cycle`은 기본 "탭 순차 드레인"(한 탭 끝까지 → 다음 탭)이지만, `ORDER_BATCH_INTERLEAVE=1`이면 `_cycleInterleave`로 **탭당 1청크(≤50행)씩 키기반 라운드로빈**(공평) — 다탭 동시버스트에서 뒤 탭이 앞 탭 뒤에 줄서 굶지 않게. reconcile은 사이클당 탭별 1회(`_reconcileOnce`, `order_reconcile` 락, `useLiveMaxRow`). `getDiag`/`GET /api/diag/order-batch-state`로 사이클 이력(mode·busy·perTab·rounds) 관찰. **주의(레드-블루-심판으로 수정된 버그)**: throttle busy(>40)일 때의 폴백을 "첫 1탭 전체 드레인"으로 두면 그 탭이 throttle 제약으로 여러 사이클을 잡아먹어 나머지 탭을 수분 굶긴다 → 반드시 **busy도 "1청크/탭 한 라운드 후 양보"(busy-fair)**. 회귀가드 `tests/interleaveScheduler.test.js`.

### RAW 미러 비활성 시트 완화 (A)
- RAW미러의 유일 소비처는 주문 행배정(claimRow/loadRawTabContext/reconcile)이고 `smartBuild`(리뷰 인덱스)는 시트를 직접 읽어 RAW미러에 의존하지 않음. `RAW_MIRROR_INACTIVE_RELAX=1`(기본 OFF)이면 `INACTIVE_EVERY`(6) 사이클 중 5번은 "최근 `INACTIVE_DAYS`(30)일 내 주문 있는 활성 시트"만 미러, 비활성은 연기(6번째는 전체). 비활성 시트에 주문 오면 `_triggerSheetMirrorOnce`가 즉시 자동미러(자가치유). 요약에 `deferredInactive`.

### 시트→DB 역동기화 (옵션·수동·기본 OFF) — `SHEET_REVERSE_SYNC`
- 주문은 **DB-first**(order_submissions 원본 → 시트는 출력). 시트에서 주문 행을 손으로 고쳐도 DB는 자동 반영 안 함. 이를 보정하는 **옵션·수동** 안전망(레드→블루→심판 설계; 자동 무인 동기·자동취소는 **기각**).
- `migration 039`: `order_submissions.last_sheet_write_sig`(정방향 written 시 기록한 매핑칸 서명=R1 루프차단 provenance) + `reverse_sync_proposals`(제안 감사원장).
- `POST /api/diag/reverse-sync-detect`(읽기전용, gid 필수·throttle busy면 양보·`order_reconcile` 락·라이브 단일사각형읽기): 시트≠DB인 written·미취소·**기본 sig-not-null** 주문만 비교 → identity(연락처+수취인+주소) 통과 시 필드별 `edit` 제안, 전공란/그리드밖은 `cancel_suspect` 플래그만(자동취소 금지). `selected_opt_key`·옵션칸 제외(G4). open 제안 교체(DELETE→INSERT)로 멱등.
- `GET /api/diag/reverse-sync-list` 검토 → `POST /api/diag/reverse-sync-apply {proposalId}`(`ORDER_LEDGER_WRITE_ENABLED=true` 추가게이트): per-order 락 + `deleted_at`·`detected_edit_seq` 불변 재검증(G6) 후 화이트리스트 필드 직접 UPDATE + `enqueue('order_update')` 위임 → `order_update`가 `cur===wantNew` no-op(시트 안 건드림)으로 **핑퐁 0**. `reverse-sync-dismiss`로 기각. **권고: detect만 먼저 켜 제안량 관측 후 apply 활성**(효용 낮으면 기존 `order-edit`/`order-cancel` 관리자 정정으로 일원화가 더 안전).

### Track B — 구글시트 완전대체 백그라운드 평행 트랙 (그림자 → 전환) — 기본 OFF
구글시트(업체별 작업내역·작업세부 열람/편집, 접근자 AE·리뷰웹관리자·광고주)를 **단일 통합 UI + DB**로 무중단 대체하는 평행 트랙. 현 시스템(Track A)은 **그대로 두고**(변경 0), 라이브를 읽어 B 모델을 만들며 **추가만·읽기만·격리** 3원칙으로 라이브에 일절 무영향. 준비되면 작업별로 전환·폐기. 서비스 `trackB.service.js`, 라우트 `/api/trackb`(`trackB.routes.js`), 프론트 `frontend/workdesk.html`(2뷰: 작업보드/소유지정), 마이그레이션 047·048. **라이브 코드가 Track B 참조 0**(되돌리기 = 마운트 제거 + 047/048 드롭).
- **실험 부품(기본 OFF) = `docs/history/track-b-실험트랙.md`**: 격리저장소(047)·그림자 투영(`TRACK_B_PROJECTION`)·parity 판정·관측 대시보드·투영 커버리지. ★★ **판정 사본 금지**: parity 짝짓기는 **phone8**(행번호 금지, 그룹 안에서만 줄번호 보조) · cutover 게이트는 **`setSourceOfTruth` 가 지금 시점 라이브 parity real=0** 로 판정하고 일괄 전환은 **force 를 절대 전달하지 않는다** · 커버리지 분모는 `participants.listActiveTabs` 하나.
- **부품4 소유 UI**(adminOrMaster): `GET /advertisers`(소유수 포함)·`/tabs`(활성탭, participants/tabs가 master전용이라 재노출)·`/ownership` GET/POST/DELETE. workdesk.html 소유지정 뷰(**탭 표기='업체관리'**, `renderOwnershipView` 진입 시 **기본 화면=거래처 추가 폼**(`showAddAdvertiser`)+**잔여 시트 수 배너**(미지정 시트=`Object.keys(sheetMap)−ownedSheetIds`))에서 업체 선택→시트/탭 지정·해제. **＋업체 추가**(프론트→`POST /api/portal/advertisers` 재사용)로 거래처 신규 등록 즉시 목록 반영 — **거래처명은 인트라넷 광고주DB 자동완성**(`GET /api/trackb/intranet/advertisers?q=` adminOrMaster 프록시 = `intranetAdvertisers`가 `INTRANET_API_BASE`(기본 inadd-system.pages.dev, 인트라넷=Cloudflare D1로 리뷰PG와 DB 분리 — 연동은 HTTP뿐, 브라우저는 인트라넷 직접 미호출)의 `api/tables/advertisers`를 fetch, 60s 캐시·5s 타임아웃·fail-soft, 이름은 **`business_name`(사업자명)** 기준 — company_name/manager는 레거시 공란, **자동완성 항목에 대표자명(`ceo_name`)·사업자등록번호(`business_number`) 병기** = 사업자 공개정보만, 급여·근태 등 민감필드 미노출), **담당AE는 로그인 사용자명 자동 지정**(읽기전용; workdesk `parseJwt`는 base64url+UTF-8 디코드로 한글 로그인명 안전). 작업보드 왼쪽 작업목록은 **소유지정 업체별 그룹핑**(`scopedActiveTabs`가 `advertiserId/advertiserName` 주석 — 탭지정>시트전체 우선, 미지정 그룹 마지막). **담당AE 매칭**: 소유지정에서 master/admin이 업체 `advertisers.inad_pm` 조회·수정(`POST /api/trackb/advertisers/inad-pm` adminOrMaster, TRIM 저장 — staff 스코프 키 정합) — AE 후보 자동완성은 인트라넷 users 프록시(`GET /api/trackb/intranet/users` adminOrMaster, display_name·username·부서만 반환 = **비밀번호 등 민감필드 매핑에서 폐기**, 60s 캐시 fail-soft). 업체 목록에 `AE 미지정` 경고 서브라벨, 업체추가 폼은 master/admin이 담당AE 직접 지정(거래처 선택 시 인트라넷 광고주DB `manager` 자동 제안, staff는 본인 고정). 회귀가드 `tests/trackBAeMatch.test.js`. **업체추가 3종 강화**(`tests/trackBIntranetSso.test.js` 2e/2f/2.5): ① **거래처 등록 게이트** — 인트라넷 광고주DB(business_name)에 **정확일치**하는 이름만 등록 허용(`isRegisteredIntranetAdvertiser`→`createAdvertiserScoped` 서버검증; 미등록=422·도달불가=**fail-closed 503**), 프론트는 실시간 자동완성(디바운스 120ms)+등록확인 상태문구로 [＋등록] 버튼 활성화(목록 선택/정확입력 시만). ② **업체 삭제**(`DELETE /api/trackb/advertisers/:id` adminOrMaster·사이드바 × 버튼) = **소프트삭제**(`status='ended'`, 가역)+소유 매핑 해제 — advertisers는 포털 공유 원장이라 하드삭제 금지(FK `ON DELETE CASCADE`로 포털작업 유실 방지), 포털은 status 무필터라 무영향·Track B 사이드바만 숨김. ③ **담당AE 후보 = AE 부서 한정**(`/intranet/users?dept=AE` 서버 필터, datalist 제안만 — 자유입력은 여전히 허용, 입력 시 "✓ 인트라넷 등록 AE 확인됨" 배지). ④ **업체추가 시트 드롭다운은 이미 소유 지정된 시트 제외**(`GET /api/trackb/owned-sheets` = `ownedSheetIds`, 활성 소유 DISTINCT sheet_id — 한 시트 중복 소유 방지, 남은 시트 0이면 안내 옵션). 자동완성 항목에 대표자명·사업자등록번호 병기. **시트그리드 날짜 표기 통일**: 날짜형 헤더(일자/날짜/date)는 표시를 "월/일 (요일)"로 통일(`_fmtKDate` — ISO·일련번호는 요일 계산, 시트식 `7 / 3 (금)`은 정규화, 비정형 텍스트 원본 유지), 셀 편집 시작값은 ISO 정본(`_gridEditVal` 분리 — 표시 축약값으로 오버레이 오염 방지).
- **부품4f-2 업체용(광고주) 뷰어 화면 정돈** (시안 `frontend/docs/design-advertiser-viewer.html`, 사용자 확정): 광고주 role 은 리뷰웹시스템[3버전]이 **전용 레이아웃**으로 뜬다. ① **폭 상한 고정** — `body.advm{--app-max:1680px}`(FHD/QHD 토글·데이터vw 무관 고정, 토글 미노출). ★ **선언 순서가 규칙**: `body[data-vw=…]` 뒤(같은 특이성 → 광고주 고정이 이김) · `body.widemode` 앞(전체화면은 여전히 해제). ② **작업 선택 = 좌측 세로 목록**(`.wrap.advwrap` 3열 = `.awside` 224px + 본문 + 레일, 상단 가로 탭바(tb1/tb2) 미렌더 — `_renderTabList`가 광고주면 `_renderAdvSidebar`로 분기). ③ **접속 기본 화면 = 내 작업 목록 표**(`_renderAdvHome`, 업체관리 연결탭 표 차용): 시작일·작업명·진행상황·참여제출입금·견적서·계산서·총비용·입금액·입금일·**남은 입금액(=총비용−입금액 화면 파생, 0원=완납 ✓)**. 데이터 = **`GET /api/trackb/my-work-summary`**(advertiser 전용, `advertiserId`는 **토큰에서만** — 쿼리 미수신=IDOR 차단; `advertiserWorkSummary` = `ownedTabsForAdvertiser`+`settlementSummaryForAdvertiser` 재사용에 **광고주 렌즈**: 비고 memo·담당 manager·salesId·amountMismatch **응답에서 폐기**, 정산 노출 토글 OFF 면 인트라넷 프록시 0회+`settlementHidden`). ④ **상세**: 헤더(.mh)·요약 스트립·정산 카드 `max-width:1380px` 캡(`body.advm` 스코프 — 내부 화면 불변), 요약 스트립은 담당 표기 대신 **시작일** 칸, **정산 카드 상시 펼침**(스텝퍼 + 금액 4칸 `_advSettleMoney`: 총비용(견적서 금액 우선)/입금액/입금일/남은 입금액 — 재료는 `settlementForTab`에 추가된 `paidAmount`/`paidDate`). ⑤ **내부 용어 미노출**: 원본(sot) 배지·"계약 연결" 문구·발주 담당 실명·Parity 레일탭은 광고주에게 안 그린다(비공개·미연결 정산은 "정산 정보가 아직 준비되지 않았습니다" 한 줄).
- **부품4f-3 업체 첫 화면 = 대시보드** (시안 `frontend/docs/design-advertiser-dashboard.html`, 사용자 확정): 접속 기본 화면이 목록 표 → **대시보드**(`_renderAdvDash`). 표는 사이드바 [📋 전체 작업](`_renderAdvList`)과 대시보드 하단 "최근 작업"(4건)으로 남는다. **신규 API 0 · 신규 저장 0** — 전부 `/my-work-summary` + 기존 `/workdesk/unseen` 파생. 구성 = ① KPI 4칸(진행 중 작업 · 참여 진척 · 총 계약금액 · 미입금 잔액) ② 진행 중 게이지(진한=제출, 연한=참여−제출, 합계 폭=참여율) ③ 정산 파이프라인(견적→계산서→입금 단계별 건수 + 금액 2칸) ④ 확인 필요(잔액 큰 순 → 계산서 미발행 → 미확인 코멘트, 최대 5건) ⑤ 최근 작업.
  - ★★ **판정·파생 단일 출처**: 진행/완료 = `_awDone`(=`_awStatus`) 하나를 사이드바 그룹·KPI·게이지·표 칩이 공유 / 정산 파생(남은 입금액 = 총비용 − 입금액) = `_awSetl` 하나 — 갈리면 "표는 3,250,000인데 KPI는 다른 값"이 된다.
  - ★★ **표 렌더러 사본 금지**: 행 빌더 `_awRowHtml` + `_awListHtml(items, limit)` **한 벌**을 대시보드(limit 4)와 전체 작업(limit 없음)이 함께 쓴다(회귀가드가 `class="awlhead"` 1회만 존재함을 고정 = 사본 부활 차단).
  - ★ **정산 노출 OFF 업체**는 금액 KPI 2칸과 정산 패널을 **통째로 뺀다**(빈 0원 표시 금지 — 있지도 않은 0원이 사실처럼 보인다). 참여 진척은 **진행 중 작업만** 집계(끝난 숫자가 진척을 희석). 총 계약금액 KPI는 **정산 미연결 건수를 부제로 고지**(조용한 누락 금지).
  - ★ **클래스는 전부 `ad*` 접두** — 기존 `.gauge`(원형 게이지)·`.dgrid`(관측 뷰)·`.kpi`(`.stripA` 하위)와 겹치면 남의 화면이 조용히 무너진다(`worow`→`wol` 전례).
  - ★ 미확인 코멘트 갱신(`refreshUnseen`→`_renderTabList`)이 **대시보드까지 재렌더**한다(사이드바만 그리면 [확인 필요]가 stale). 뒤로/앞으로는 히스토리 항목의 `advView`로 대시보드↔전체 작업 전환을 되짚는다.
- **부품4f-4 업체 상세 화면 3종 보강**(사용자 요청): ① **리뷰제출 컬럼 복구** ② **리뷰 이미지 미리보기 패널** ③ **정산 현황 6칸**.
  - ★★ **광고주 컬럼 화이트리스트에 "상태 칸 선점"**(`_advertiserColumns(raw, {submitCol, submitCol2})`): 그 탭의 `submit_col`/`submit_col2`(= `review_index`가 감지해 `campaign_participants`로 복제한 리뷰제출·입금 열 헤더명)를 **키워드 규칙보다 먼저 선점**한다. 두 사고를 동시에 막는다 — ㉮ 헤더가 키워드에 안 걸리는 탭(실측: 카페/블로그 바이럴)에서 **리뷰제출 열이 통째로 누락**(신고 건) ㉯ `입금일자` 같은 헤더를 위쪽 **구매일자 규칙(`/일자|날짜/`)이 먼저 삼켜** 구매일자 칸에 입금일이 들어가고 입금 칸이 사라지는 오배치. 출력 순서는 규칙 순서 그대로라 **결제금액 → 리뷰제출 → 입금** 위치가 유지된다. ★ 실재하지 않는 헤더명은 무시(빈 열 생성 금지), `submit_col`이 비면 **기존 동작 그대로**(무회귀).
  - **리뷰 이미지 미리보기**(`GET /api/trackb/workdesk/review-images`, `_ensureThreadScope` = 내부인 + 소유 광고주): 행별 파일ID를 `{row_index: [{fileId,slot,at}]}`로 반환. 소스는 **원장 `review_submissions`(032) 1순위 + 대표 이미지 `review_index.review_file_id`(031) 폴백**(그 이전 데이터), 같은 파일ID 중복 제거·행당 12장 상한. ★ **이미지는 기존 무인증 프록시 `/api/drive/image/<id>`가 스트리밍** — 신규 저장소·신규 프록시 0. ★ 키 = `row_index` = `campaign_participants.seq`(투영이 그대로 복제).
  - **화면**: 그리드 우측 `#rvPane`(340px sticky, `.advgw` flex). **행 클릭 · ↑↓ 키**로 선택 이동(위임 1회 바인딩 — 재렌더로 tbody가 갈려도 유지, 입력창·모달 위에서는 방향키 미가로채기), 필터·정렬 재렌더 후 `_rvReapply`로 선택 복원. ★ **미제출 행은 "리뷰 미작성 · 미제출"**, 제출 표시는 있는데 이미지가 없으면 **"리뷰 이미지 미등록"**으로 **구분해서** 안내한다(둘을 뭉뚱그리면 거짓말이 된다). 이미지 클릭 = 기존 `woImageModal` 라이트박스. ★ URL은 **bare `API_BASE`**(`window.API_BASE_URL`은 최상위 const라 항상 undefined — 레포 실측 함정) + 파일ID 형식 검증 후에만 생성.
  - **정산 현황 = 6칸**(견적서·계산서·총비용·입금액·입금일·남은 입금액, `_advSettleFields`): 광고주 화면에서 내부 스텝퍼·[계약 변경/해제]를 대체한다(스텝퍼가 담던 견적/계산서/입금 상태가 6칸에 그대로 들어가 중복). **내부(master/admin/staff)는 종전 스텝퍼 그대로**(무회귀). ★ 남은 입금액 = 총비용 − 입금액 = **목록 표·대시보드와 같은 파생식**.
- 회귀가드 `tests/advertiserViewer.test.js`(라우터 실행·렌즈 실행·배선·CSS 선언 순서·대시보드 단일 출처/사본 금지·컬럼 선점 순수함수 실행·리뷰 이미지 병합).
- **부품4g 등록리뷰어DB 탭(상단탭)**: `GET /api/trackb/reviewers`(검색 `q`=이름 부분일치 ∪ 연락처 숫자 부분일치, `status` 필터, 서버 페이지네이션 `limit`≤200·`offset`) + `POST /api/trackb/reviewers/memo`(admin_memo만). **둘 다 `adminOrMasterMiddleware` — AE(staff)·광고주 차단**(리뷰어DB는 담당 업체와 무관한 전사 개인정보라, AE의 "담당 탭" 스코프 원칙을 깨지 않기 위해 master/admin 전용). nav 버튼도 `isAdmin` 분기에만 렌더. **주민번호·계좌번호를 표에 그대로 표시**(사용자 확정)하므로 화면공유 주의 문구를 상시 노출하고 **수정은 관리자 메모만**(정산·신원 필드는 조회 전용, 가드가 `SET` 대상이 `admin_memo` 하나뿐임을 고정). 기존 `/api/reviewer/list`는 전 행을 한 번에 반환해 이 화면엔 부적합 — 재사용하지 않는다. ★ 동적 WHERE의 **자리표시자↔파라미터 개수 정합**이 이 라우트의 유일한 함정(숫자 없는 이름 검색에서 안 쓸 `$n`을 push해 `bind message supplies N parameters` 500이 나던 버그를 개발 중 실측·수정). 회귀가드 `tests/reviewerDbTab.test.js`(라우터 스택 실검사 + 케이스 자리표시자 검증, `PGTEST_URL` 있으면 **진짜 PG로 실행**).
- **부품4h 작업오더·모집공고 탭(상단탭)**: 리뷰웹시스템[3버전]에서 작업오더 접수·상태변경과 모집공고 발행·수정·관제를 그대로 한다.
  - **권한 2단**: **열람 = 내부인 전원**(master/admin/staff — 광고주 차단, `internalMiddleware`), **편집 = 이름 명단**(`workdesk_editors`, migration 079 · `utils/workdeskEditors.js` · `editorOnlyMiddleware`). 작업오더 접수는 시트/탭을 `tab_configs`·`campaigns`에 등록하는 **단일 관문**이고 공고 발행·수정은 정원·금액을 바꾸므로, 보는 사람 전부에게 열 수 없다(사용자 확정). ★ **master 는 명단 무관 항상 허용**(명단 오설정 잠금 방지), ★ **명단 조회 실패는 읽기 전용으로 수렴**(fail-closed — 편집은 되돌리기 어려우니 모르면 안 연다), ★ 명단에 없는 admin 은 **이 화면만** 읽기 전용(관리자 대시보드는 종전대로). 명단 관리는 master/admin 전용 UI(`openEditorList`)이고 **후보는 인트라넷 직원DB 자동완성**(`GET /api/trackb/intranet/users` 재사용). 제거는 소프트(이력 보존), 변경 즉시 캐시 무효화.
  - ★★ **사본 금지 — 관리자 대시보드와 같은 코드**: 공고 카드는 `campaign-cards.js`, **발행·수정 모달은 `js/recruit-modal.js`**(admin.html 인라인 301줄 마크업 + CSS를 **공유 모듈로 이관** — 두 화면이 `#recruitModalMount`에 같은 것을 마운트), 목록·저장 로직은 `index-recruit.js` 그대로 호출. `index-recruit.js`의 하드코딩 경로 10곳은 `_campApi` + `window.CAMPAIGN_ADMIN_API` 로 추상화 — **Track B 네임스페이스를 admin 과 같은 모양**(`/campaigns/list`·`/campaigns/create`·`/campaigns/:id/flags`…)으로 맞춰 **베이스 문자열 치환만으로** 재사용한다(AE 는 `/api/campaign/admin/*` 에 403이라 필수). 서버도 로직 복제 0 — `_delegate`가 기존 order/campaign 핸들러를 그대로 태우고, **대상 라우트가 사라지면 부팅 시 throw**(조용한 404 금지).
  - ⚠ 모달 추출의 영향: `admin.html`의 `rf_*`를 읽던 회귀가드 9종을 "모듈+admin.html"로 넓혔다(검사 의미 불변). 회귀가드 `tests/workdeskOrdersCampaigns.test.js`(라우터 스택 실검사·fail-closed·사본 금지·펼침행).
  - **작업오더 상세 = 팝업 아닌 펼침행**(행 클릭 → 그 행 바로 아래 `colspan` 행, 리뷰어 로그 탭과 같은 방식). 본문은 **`js/work-order-detail.js` 공유 렌더러**(`_woDetailHtml` — `index-app.js`에서 이관) = `▶ 라벨 : 값` + URL 링크화 + **첨부 이미지(guide-image 프록시·Drive) `<img>` 임베드 + 클릭 라이트박스**(`woImageModal`). admin·admin-siand·workdesk 세 화면이 같은 모듈을 로드한다(사본을 두면 "관리자엔 이미지가 뜨는데 작업보드엔 URL만 뜬다"). 상세는 **열 때 한 번만** 그린다(`dataset.filled` — 30건 동시 이미지 로드 방지). 접수·상태변경은 `_woEditActions` **한 곳**에서만 만들어 표 셀·펼침 패널이 같은 게이트를 쓴다.
  - ★★ **프론트 코드를 모듈로 뺄 때의 실측 함정 2종**(회귀가드가 고정): ① **클래스 이름 충돌** — `.worow`/`.wodetail`/`.wochip`은 이미 작업보드 탭 '작업세부' 패널이 쓰고 있어, 그쪽 `display:grid`가 새 표의 `<tr>`에 걸려 **8칸이 2열 그리드로 접혔다**(표가 통째로 무너짐) + `.wochip` 재정의가 그쪽 칩 색까지 바꿨다 → `wol` 접두로 분리하고 **기존 소유자 규칙 생존·중복 정의 0**을 가드가 확인. ② **함수 범위 오절단** — 한 줄짜리 `function _woNormName(v){…}` 다음 함수까지 딸려와 `woCreateCampaign`(admin 전용)이 모듈에 갇혔고, 공개 목록에 없어 **admin의 '공고 발행' 버튼이 조용히 고장**났다 → 모듈이 관리자 전용 전역(`showToast`·`openRecruitModal`·`_woCache`…)을 참조하면 실패하는 검사 + 모듈 내부 미해결 `_wo*` 참조 0 검사.
  - ★ **추출의 무회귀 증명 방법**(다음에도 이렇게): 같은 픽스처를 `origin/main`과 작업본에 각각 넣어 **실제 브라우저에서 렌더 결과를 바이트 비교**한다(빈 입력·HTML/XSS 케이스 포함) + 이동 함수를 쓰는 소비처 전부를 같은 방식으로 대조 + **모듈만 단독 로드**(호스트 전역 0)해 미해결 참조를 잡는다. grep 가드만으로는 셋 다 못 잡는다.
  - **처리 열 재설계(2026-08-04 사용자 신고)**: ① 상태변경이 `prompt` 로 **영문 상태값**(submitted/reviewing/…)을 타이핑시켜 비개발자는 바꿀 상태를 알 수도 바꿀 수도 없었다 ② 행을 누르면 상세가 펼쳐지는데 [상세] 버튼이 또 있었다 ③ 접수하기/접수완료가 안 갈렸다 ④ 작업오더에서 모집공고로 가는 다리가 아예 없었다.
    - ★★ **상태 이름·전이표·접수 판정·발행 프리필은 공유 모듈(`js/work-order-detail.js`) 한 벌**로 모았다(관리자 대시보드 `index-app.js` 에 있던 `WO_LABELS`/`WO_COLORS`/`WO_TRANSITIONS`/`WO_DELIVERY_MAP`/`WO_CHANNEL_HOSTS`/`_woChannel(FromUrl)`/`_woOptionRows`/`_woFirstProductInfo`/`_woBuildInflowHtml`/`_woPlainGuideToHtml`/`_woReviewImgHtml` 이관 + 신설 `_woCampaignPrefill`). 사본을 두면 같은 상태가 한쪽에선 "검토중" 다른 쪽에선 "접수됨"이 되고, 두 화면이 **서로 다른 프리필로 공고를 만든다**. 라벨은 **접수 관점 한국어**로 통일(`submitted=접수대기`·`reviewing=접수완료`·`published=공고발행`) — 관리자 대시보드 표기도 함께 바뀐다.
    - ★★ **화면 전이표 ⊆ 서버 `ORDER_TRANSITIONS`** — 화면이 제시한 버튼이 서버에서 400 으로 튕기면 안 된다. 회귀가드가 **두 파일에서 표를 실제로 읽어 대조**한다(grep 으로는 값 대조를 못 한다). 상태 변경 모달은 그 표가 허용한 상태만 한국어 버튼으로 내고 각 버튼에 `WO_STATUS_HINTS` 한 줄(“누르면 무슨 일이 일어나는가”)을 붙인다. ★ `reviewing` 힌트에 **"탭 등록은 [접수하기] 버튼이 한다"** 를 명시 — 수동 전이로 접수가 된 줄 알면 리뷰어 검색이 안 열린 채 방치된다.
    - ★ **접수하기 ↔ ✓ 접수완료**(초록·`disabled`·등록된 탭 이름 툴팁): 판정은 `_woAcceptable`(= 서버 `ACCEPT_ELIGIBLE`) — 화면에서 다시 세지 않는다. **눌러도 아무 일 없는 버튼은 두지 않는다.** [상세] 버튼은 제거(행 클릭과 하는 일이 겹친다).
    - ★ **모집공고 즉시 미리보기**: 연결 공고 없으면 `📢 모집공고`(공유 프리필로 발행 모달) / 있으면 `👁 모집공고`(그 공고를 수정 모드로) — 어느 쪽이든 모달 오른쪽에 **리뷰어가 참여 후 보는 실제 화면**이 그려진다. 그래서 workdesk 에 `js/campaign-workdetail.js` 를 **`index-recruit.js` 보다 먼저** 로드한다(없으면 `_renderPreview` 가 `window.CampWorkDetail` 체크에 걸려 **그 카드만 조용히 비어** 보인다 — 실측).
    - ★★ **SSO 토큰 격리 때문에 경로 2개를 더 재기준**: ㉮ 연결 탭 목록 `window.RECRUIT_TABS_API`(기본 `/api/tab/dashboard` → workdesk 는 `/api/trackb/tabs`) — 없으면 연결 탭 드롭다운이 **빈 채로** 열린다. ㉯ 공고 상세(수정 프리필)는 원본이 **무인증 공개** `GET /api/campaign/:id` 라 인트라넷 토큰이 **무시되어 공개 화이트리스트 뷰**가 온다(401 이 아니다) → 모달이 조용히 빈 칸으로 열려 "저장했더니 값이 날아간" 것처럼 보인다. 같은 핸들러를 태우는 `GET /api/trackb/campaigns/:id`(authMiddleware+internal)를 신설해 재기준. ★ `/campaigns/list` 가 `/campaigns/:id` **앞에** 등록돼야 목록이 삼켜지지 않는다(회귀가드가 스택 순서를 고정).
    - ★★ **편집 권한자에게는 전체 편집 페이로드**(코드리뷰 P1): 편집 허용명단에는 **`staff`(AE)도 들어갈 수 있는데**, 원본 상세 핸들러의 `_isAdminReq` 는 JWT role 이 admin/master 일 때만 전체 행을 준다 → staff 편집자는 **수정은 되면서** 공개 화이트리스트 뷰를 받아 폼이 `work_detail`·연결탭·정원·옵션을 **빈 기본값으로 초기화**하고, 저장하면 기존 설정이 조용히 `0`·빈값·`options:[]` 로 **지워진다**. Track B 상세 라우트가 `canEdit` 이면 **서버 코드만 세우는 신뢰 플래그 `req._trustedAdminView`** 를 세워 위임하고 `_isAdminReq` 가 그것을 인정한다. ★ 요청(헤더·쿼리·본문)으로는 만들 수 없고, 프로토타입 오염 대비로 **`hasOwnProperty` 자기 프로퍼티 검사**까지 한다. ★ 판정 실패는 공개 뷰(fail-closed) — 편집은 서버 게이트가 막는다. ★ 플래그를 세우는 곳은 **라우트 한 곳뿐**(회귀가드가 전 라우트 파일에서 개수 1을 고정 — 늘리면 권한 상승 경로가 는다).
    - ★ 리뷰어 화면 미리보기(`campaign.html?preview=1`)는 `/api/campaign/admin/:id/preview` 를 먼저 치고 **401/403 일 때만** `/api/trackb/campaigns/:id/preview` 로 한 번 더 시도한다(그 외 오류까지 재시도하면 실패 원인이 가려진다).
    - 회귀가드 `tests/workdeskOrderActions.test.js`(표 대조·사본 부재·라우터 스택 실검사 + **스텁 pool 로 상세 핸들러 실제 호출**(전체/공개/오염 3분기), 변이시험 검출 확인) + **실제 http 오리진 브라우저 실행검증 시나리오**(버튼 분기·모달 렌더·프리필·미리보기 카드·JS 오류 0). 프리필 이관은 **`HEAD` 의 인라인 조립과 같은 입력에 대해 결과 JSON 이 바이트 동일**함을 vm 으로 대조해 무회귀를 증명했다.
  - **부품4i C/S 문의창구 탭(상단탭)**: 관리자 대시보드의 리뷰어 1:1문의 화면을 리뷰웹시스템[3버전]에서 그대로 쓴다. **master/admin 전용**(`adminOrMasterMiddleware`) — 문의 본문에 리뷰어 실명·연락처·주소·주문정보가 그대로 실려 AE의 "담당 탭" 스코프로 나눌 수 있는 데이터가 아니다(등록리뷰어DB와 같은 판단, 기존 `cs.routes.js` 정책 승계). 서버는 `/api/trackb/cs/*` **7경로 프록시**(`_delegate(_csRoutes, …)` — SQL·로직 복제 0)뿐이고 원본 `/api/cs/*`는 무변경. Track B 경로에 두는 이유 = 인트라넷 SSO 토큰(`via:'intranet'`)이 `/api/cs/*`에 **도달 자체가 불가능**하기 때문.
  - ★ **화면은 `js/cs-inquiry.js` 공유 모듈 한 벌**(마크업 + 로직 전부 — admin.html 인라인 + index-app.js 에서 이관). admin·admin-siand·workdesk 세 화면이 `#csInquiryMount`에 같은 판(`#tab-cs-inquiry`)을 마운트한다. 경로 차이는 **`window.CS_API_BASE` 재기준**(`api.js` `_resolveApiPath` — `/api/cs/`로 시작하는 경로만, 전역 미설정이면 동작 불변)으로 흡수한다. 회귀가드 `tests/csInquiryTab.test.js`.
    - ★★ **이미지 교체요청 = 이 화면 위의 "처리 대기 큐" 띠**(전용 상단탭 폐지, 시안 C 확정 · `frontend/docs/design-cs-review-edit-merge.html`): 위 = 주황 띠(교체요청) / 아래 = **지금 그대로의** 문의 화면. 두 기능을 **영역으로** 구분한다 — 목록에 섞으면(시안 B) 이미지 대조 폭이 대화창 안으로 줄고, 서브탭(시안 A)은 클릭이 하나 늘어난다. 띠는 3모드(`rail` 대기 건만 가로 · `full` [모두 보기]=옛 전용탭 화면 그대로(상태 필터+카드 격자) · `collapsed`), **대기 0건이면 자동으로 한 줄**(`.q0`)로 줄어 문의 화면을 안 가린다. 메뉴 이름은 **`C/S`**(문의+교체요청을 함께 담으므로 "문의"를 뺌).
    - ★ **nav 뱃지 = 합계 한 숫자**(사용자 확정 — 문의 미확인 + 교체요청 대기, 예 5+11 → `🔴16`): 두 값은 **서로 다른 시점에 따로 도착**하므로 `_NAVCS{cs,re}`에 **각각 기억해 두고 합계만 그린다**(`_renderCsNavBadge`) — 한쪽만 들어왔다고 다른 쪽을 0으로 접으면 숫자가 들쭉날쭉해진다. **둘 다 null이면 그대로 둔다**(조회 실패로 뱃지가 사라지면 "손볼 것 없음"으로 오독). AE는 문의를 조회하지 않아 `cs`가 계속 null = 교체요청만 합산. `logout`이 `_NAVCS`도 비운다.
    - ★★ **확대 보기(`zoomPair`)는 두 장을 동시에 드러낸다**(실측 신고): 클릭한 쪽은 카드 썸네일로 캐시돼 즉시 뜨는데 '기존'은 그 자리에서 처음 받아, 그냥 두면 한 장이 뒤늦게 끼어들며 레이아웃이 흔들린다(대조하려고 연 화면인데 비교 시작 시점이 어긋난다) → `opacity:0`으로 시작해 **둘 다 `_painted`(load + `decode`)가 끝난 뒤 한꺼번에** 표시(`_revealTogether`), 그동안 "이미지 불러오는 중…" 표기. ★ `decode`까지 기다린다 — `load`만 보면 디코딩 지연으로 여전히 늦게 그려진다. ★ **fail-open**: 한 장이 `error`여도 진행하고 `_PAIR_WAIT_MS`(4s) 뒤엔 무조건 표시(검은 화면으로 남지 않게). ★ 이 회귀는 정적 grep으로 못 잡는다 → 가짜 img로 **한 쪽만 load를 발생**시켜 그때까지 아무것도 안 보이는지 확인하는 런타임 가드(변이시험으로 실제 검출 확인).
    - ★★ **권한 경계가 "메뉴가 보이나" → "AE 화면에 문의 본문을 그리나"로 옮겼다**: 교체요청은 AE도 처리하므로 **C/S 메뉴가 AE에게도 보인다**. 문의 본문은 여전히 master/admin 전용이고(서버 `adminOrMasterMiddleware` 무변경), AE 화면에는 `renderCsView`가 **`CsInquiry.mount`를 부르지 않는다**(그리지 않으면 요청도 안 나감 + 최상단에 "관리자만 열람" 안내). AE는 띠가 화면 전부라 `full` 모드로 연다. ★ 이 판정은 grep 으로는 스코프·실행경로를 못 보므로 **`renderCsView`를 역할별로 실제 실행**해 mount/목록조회 횟수를 세는 가드로 고정(`tests/reviewEditCsBridge.test.js` §8).
    - ★★ **띠가 먹은 세로만큼 아래 문의 화면을 줄인다**(`_reFitCsHeight`) — 안 하면 답장 입력칸이 화면 밖으로 밀린다. 모듈의 대화 영역 높이는 **인라인 스타일** `calc(100vh - 250px)`라 클래스 CSS로 못 이기고, **공유 모듈을 고치면 관리자 대시보드(띠 없음)까지 같이 줄어든다** → 이 화면에서만 실측 높이를 빼서 인라인 값을 다시 쓴다(모드 전환마다 재계산, `100vh`는 그대로라 창 크기 변화는 브라우저가 따라감).
    - ★ 옛 뷰 이름 `reedit` 은 `switchView` 진입부에서 **`cs` 로 한 번 갈아끼운다**(히스토리·북마크가 빈 화면으로 떨어지는 것 방지, 이때 `full` 로 펼침). 처리 후 목록 갱신(`CS_REVIEW_EDIT_ON_RESOLVED`)은 **뷰 이름이 아니라 `#reQueueMount` 존재**로 판정 — 이름으로 보면 이런 통합 때 갱신이 조용히 멈춘다. 승인·반려·카드는 여전히 **C/S 대화창과 같은 코드**(`cs-review-edit-card.js` + `cs-inquiry.js` 핸들러). `logout`은 `re*` 상태도 비운다(목록에 리뷰어 실명·연락처가 남아 다음 로그인 계정에 stale 렌더되는 것 차단).
  - ★★ **`escHtml` 은 호출 시점에 찾는다**(실측 함정): 공유 모듈은 `index-app.js` **앞에** 로드되므로 로드 시점에 `window.escHtml`을 캡처하면 항상 폴백이 잡힌다. 폴백이 원본과 한 글자라도 다르면(`String(s)` vs `s==null?'':s`, 작은따옴표 이스케이프 유무) **관리자 화면 출력이 조용히 달라진다** → `work-order-detail.js`·`cs-inquiry.js` 둘 다 `escHtml(s)` 함수로 감싸 호출 시점에 해석하고, 폴백은 `index-app.js` 구현과 **동일**하게 맞췄다.
  - ★★ **호스트 연동은 "밖에서 감싸기"가 아니라 "안에서 훅 부르기"**(코드리뷰 지적으로 잡은 실측 버그): 모듈 내부 호출(`loadCsRooms`·`csRefreshBadge`)은 **렉시컬 스코프의 `csUpdateBadge` 를 직접** 부르므로, 호스트가 `window.csUpdateBadge` 를 감싸도 래퍼는 **한 번도 안 불린다** → 리뷰웹시스템[3버전] nav 뱃지가 부팅 시각 값에 영영 머물렀다. 모듈이 `window.CS_ON_BADGE(count)` 훅을 **안에서** 부르고 호스트가 등록한다(훅 미설정 = 관리자 대시보드 동작 불변). `#csInquiryBadge`가 없어도 훅은 불려야 하므로 `if(!b) return` early return 도 제거. ⚠ **내 회귀 테스트가 이걸 놓쳤다** — 뱃지에 "숫자가 있는지"만 봐서 부팅값이 남아 있는 것을 통과로 읽었다. 지금은 부팅값(3)과 목록값(7)을 **다르게** 두고 7로 바뀌는지 본다(약한 단언은 잘못된 이유로 통과한다).
  - ★ **리뷰웹시스템[3버전]에는 SSE 가 없다** → 부팅 1회 조회만으로는 그 뒤 들어온 문의가 영영 안 보인다 → `CS_BADGE_POLL_MS`(60초) 주기 갱신 + `visibilitychange`(가려지면 쉬고 복귀 시 즉시 1회). 관리자에게만 돈다. 폰트어썸은 admin 과 **같은 CDN·같은 버전**을 workdesk 에도 로드(공유 모듈이 `<i class="fas">`를 쓴다).
  - ★★ **모달을 모듈로 뺄 때의 3대 함정**(실측 사고 — 모달이 오버레이가 아니라 **페이지 하단 일반 블록**으로 흘렀다): ① **CSS를 한 줄 어긋나게 잘라** 주석 짝이 깨지고(`/*` 한가운데서 시작) 뒷 줄의 남의 규칙(`.dash-filter-btn`)·여분 `}`까지 딸려와 중괄호가 32 대 33 → **브라우저가 에러 없이 조용히 파싱을 포기**해 `.rf-split{display:flex}`가 통째로 죽었다. ② **마운트 지점이 스크롤 컨테이너 안**(`.ovwrap`)이면 오버레이가 화면 흐름에 섞인다 → **body 직속**(모듈이 마운트 지점 없으면 스스로 만들고, `<head>` 로드 시엔 DOM 준비 후 재시도). ③ **호스트 테마 의존** — `var(--border)`·`var(--p)`는 `css/index.css`(admin 전용)에만 있어 리뷰웹시스템[3버전]에선 무효값이 된다 → 마크업·CSS의 **모든 `var`에 폴백**, 폰트어썸도 없어 **닫기 ×가 안 보여 모달을 못 닫던 것**을 마운트 시 1회 탐지해 글리프 폴백 주입(`injectIconFallback` — 폰트어썸 있으면 무동작).
  - ★ **grep 가드로는 못 잡는다**(선언은 멀쩡히 '있다') → `recruitModalLayout.test.js`가 **토큰을 세어** 주석·중괄호 균형 / 최상위 선택자 소속(`#recruitModal`·`.rf-*`·`@media`만 — 남의 규칙 혼입 차단) / `var` 폴백 유무 / 핵심 선언 생존을 고정한다.
  - **부품4j 설정 탭(상단탭)**: 관리자 대시보드의 설정 3종을 리뷰웹시스템[3버전]에서 그대로 한다 — **내 닉네임(1:1문의 표시 이름)** · **회사 사업자번호(제공정보) + 현금영수증 발행방법 이미지 4채널** · **리뷰어 소식·공지**. Track B가 기존 관리자 대시보드를 대체하려면 이 설정들도 작업보드에서 끝나야 한다(사용자 확정).
    - **권한 2단**(서버 게이트와 프론트 노출이 1:1): **내 닉네임 = 내부인 전원**(`internalMiddleware` — master/admin/AE). AE도 이미지 교체요청을 처리하면 리뷰어 채팅에 메시지를 남기므로 표시 이름이 필요하다(리뷰어 화면은 `닉네임 || '관리자'` fail-closed라 실명은 어차피 안 나가지만, 관리자 화면 책임추적과 리뷰어 구분을 위해 본인이 정한다). **사업자번호·현영 이미지·리뷰어 공지 = master/admin 전용**(`adminOrMasterMiddleware`, 원본 라우트와 동일) — 전사 설정이고 공지는 리뷰어 홈에 그대로 나간다. AE에게는 **패널 자체를 안 그린다**(최종 방어는 서버).
    - **서버 = `/api/trackb/settings/*` 9경로 프록시**(`_delegate` — SQL·로직 복제 0, 원본 `/api/admin/my-nickname`·`/api/tab/*`·`/api/reviewer/notices/*`는 무변경). Track B 경로에 두는 이유는 C/S와 같다: 인트라넷 SSO 토큰(`via:'intranet'`)은 그 원본 경로들에 **도달 자체가 불가능**하다. ★ `/settings/guide-image`는 원본이 무인증인데 여기선 **adminOrMaster로 좁힌다**(용도가 현영 이미지 업로드 하나뿐 — 프록시가 원본보다 넓어지면 안 된다).
    - ★★ **화면은 `js/admin-settings.js` 공유 모듈 한 벌**(admin.html 인라인 마크업 + index-app.js 로직에서 이관). `admin.html`(설정탭 `#adminSettingsMount` + 대시보드 `#rvNoticeMount`) · `admin-siand.html`(닉네임만) · `workdesk.html` 셋이 같은 것을 마운트한다. 경로 차이는 **`window.ADMIN_SETTINGS_API` 재기준**(`CAMPAIGN_ADMIN_API`와 같은 장치, 전역 미설정이면 기존 경로 = admin 동작 불변)으로 흡수.
    - ★ **gasGet/gasPost를 쓰지 않는다** — 액션맵(`api.js`)은 경로가 고정이라 Track B로 재기준할 수 없다. 모듈이 **논리 키 → 경로** 표(기본값 8 + 접미사 8)를 들고 직접 fetch 한다(회귀가드가 두 표의 키 일치를 고정 — 한쪽만 늘리면 404).
    - ★★ **호스트 테마 의존을 "마운트 시 1회 감지"로 흡수**: 마크업은 `var(--t1)`·`class="admin-section-header"`를 **원본 그대로** 두고, `getComputedStyle(documentElement)`에 `--t1`이 없으면(=리뷰웹시스템[3버전]) 마운트 지점에 `as-standalone` 클래스를 붙여 같은 토큰과 헤더 레이아웃을 주입한다. 그래서 **admin.html 렌더 결과가 한 바이트도 안 바뀐다**(recruit-modal.js 때처럼 마크업 전체에 `var` 폴백을 박으면 원본과 달라져 대조가 불가능해진다).
    - ★ **현영 4칸은 `CR_GUIDE_CHANNELS` 표에서 생성**(손으로 적어 둔 4칸 → 채널 추가 시 한 칸 누락 사고가 실제로 있었다). 표 ≡ 서버 `utils/cashReceiptChannels.js` 는 기존 가드가 이미 고정하므로 슬롯 존재가 따라온다.
    - ★ **닉네임 저장 후 열린 문의방 갱신은 훅으로**: 활성 threadId 는 `cs-inquiry.js` 모듈 스코프라 밖에서 못 읽는다 → 그 모듈이 `csReloadActiveConversation` 을 노출하고 설정 모듈이 부른다(전역 사본을 두면 두 값이 갈라진다).
    - **로드 타이밍 불변**: admin.html 은 `autoload:false` 로 마운트하고 값 불러오기는 종전대로 `switchAdminTab` 시점에 한다(리뷰웹시스템[3버전]만 마운트 즉시 로드). 폭 상한은 `#sthead .mh`·`#adminSettingsMount` 둘 다 **1120px**(헤더·본문 같은 값 — 위 규칙).
    - **검증**: `origin/main` 의 인라인 마크업과 모듈 생성 마크업을 **실제 브라우저에서 DOM 정규화 비교**해 동일함을 확인했고(두 블록 모두 일치), 실제 http 오리진에서 admin.html·workdesk.html 을 각각 띄워 기존 경로/Track B 경로·저장 페이로드·역할별 패널 노출을 확인했다. 회귀가드 `tests/workdeskSettingsTab.test.js`(라우터 스택 실검사 + 사본 금지 + **전역 0인 가짜 DOM 에서 mount 실제 실행**).
    - ★★ **화면 구조 = 좌측 목차 + 한 번에 한 묶음**(시안 B 확정, 와이어프레임 `frontend/docs/design-admin-settings-wireframe.html` `?v=B` · 색 시안 `design-admin-settings-ui.html`). 배경도 흰색·내용도 흰색이라 묶음 경계가 `<hr>` 1px 뿐이고 이미지 업로드 슬롯 13칸(현영 발행방법 4 + AI 예시 9)이 같은 모양으로 늘어서 번잡하던 것을 고쳤다. **저장 로직·엔드포인트·필드 ID 는 무변경** — 겉모양만.
      - ★★ **패널은 전부 DOM 에 그려 두고 보이기만 토글한다**(`asSelectPanel` — 안 그린 패널은 id 가 없어 로더가 조용히 no-op 이 되고, admin.html 은 탭 전환 때 로더를 **한 번만** 부르므로 나중에 그 묶음을 열면 값이 빈다). 회귀가드가 이 불변식을 고정.
      - ★ **목차는 패널 2개 이상일 때만** — 1개 마운트(대시보드 공지 카드 `#rvNoticeMount`·AE 닉네임)는 종전 그대로 그린다(공지 패널은 자기 테두리를 유지, B 안에서는 패널이 곧 카드라 `.as-b .as-noticebox` 로 벗긴다).
      - ★ **목차 배지 = 등록 현황**(`미설정`·`2 / 4`·`1 / 13`·`15열`·`2건`) — 화면을 열기 전에 비어 있는 설정이 보인다. 값은 **이미 받아 둔 응답에서 세기만** 한다(신규 API 0, `_setNavBadge`). 작업표 배지는 `_wtRenderCols` 안에서 갱신 — 로더에만 두면 열을 더한 뒤 숫자가 옛값에 머문다.
      - ★ **이미지 슬롯은 목록형 한 벌**(`.as-slot` — 발행방법·AI 예시 공용): 썸네일 52 + 이름 + 상태칩 + 버튼. 날것의 `<input type=file>` 은 `.as-file` 로 감추고 `<label for>` 로 누른다 — **id·onchange 경로는 그대로**(회귀가드가 id 를 본다). AI 예시 줄에는 부가설명을 두지 않는다(13번 반복되면 그 자체가 소음).
      - ★★ **패널 머리 정규화 필수**: admin.html 의 `.admin-section-header` 는 **sticky + 좌우 `-16px` 풀블리드 + 페이지색 반투명 배경**(탭 전체를 가로지르는 머리띠로 설계됨)이라, 흰 패널 안에 그대로 두면 회색 띠가 패널을 뚫고 나가고 스크롤 시 화면 위에 붙는다 → `.as-b .admin-section-header` 로 평범한 제목 줄로 되돌린다(as-standalone 규칙보다 **뒤에** 둬야 이긴다).
      - 마지막으로 본 묶음은 `sessionStorage.as_panel` 로 기억(저장하러 다시 들어오는 화면이라 매번 첫 항목으로 되돌아가면 같은 클릭을 반복한다). 목록에 없는 키면 첫 항목.
- **인트라넷 SSO → 리뷰웹시스템[3버전]**(세션 이어받기): 인트라넷 로그인 성공 시 클라이언트(auth.js)가 `POST /api/admin/intranet-login`(전용 리미터 10/분)으로 리뷰 토큰을 발급받아 `localStorage.review_sso` 저장 → 사이드바 "리뷰웹시스템[3버전]" 버튼이 `workdesk.html#sso=<token>` 프래그먼트 핸드오프(도착 즉시 저장+해시 제거). `loginIntranet`은 인트라넷 서버 `/api/auth/login`(D1 서버측 대조)에 자격 프록시 재검증(비밀번호 결속·fail-closed, **`X-Api-Key=INTRANET_API_KEY` 서버간 인증 헤더 필수** — 인트라넷 API 가드 활성 시 키 없는 서버간 호출은 Unauthorized로 차단돼 SSO 토큰 발급이 실패하므로 trackB 프록시와 동일 채널로 전송, 미설정=구버전 호환 미전송) 후 발급 — **권한은 기본 'staff', 'admin'(전체 탭 열람·편집) 승격 = `INTRANET_SSO_ADMIN_USERS`(csv, 인트라넷 username) 또는 `INTRANET_SSO_ADMIN_GROUPS`(csv, '부서' 또는 '부서|파트' — 예 "콘텐츠운영팀|체험단파트", 로그인 시점 직원DB의 department/part 판정=파트 전원 자동 승격·인사이동 반영, part 미포함 구버전 응답은 fail-closed=과승격 방지)**(인트라넷 role 필드는 자기신고라 불신뢰 — 사용자 확정 정책 1a; department/part는 인사DB=관리자 관할이라 그룹 승격 근거로 사용), **진입 게이트 우선순위: `INTRANET_SSO_ALLOWED_GROUPS`(csv, '부서' 또는 '부서|파트' — 예 "AE,콘텐츠운영팀|체험단파트", 로그인 시점 직원DB 판정=개인 수동등록 불필요·인사이동 자동 반영, TRIM·대소문 무시 정확일치, part 판정은 inadd#164 배포 후) → 미설정 시 `INTRANET_SSO_ALLOWED_ROLES`(csv, 인트라넷 role) → 둘 다 미설정=전 직원**(지정 admin 계정은 게이트 무관 항상 통과). `name=display_name`(inad_pm 매칭 키, 인트라넷 실명은 관리자 관할 가정)·`iu=username`·`ir=인트라넷 role`(감사). ★★**`via:'intranet'` 토큰은 authMiddleware에서 `/api/trackb/*` 전용으로 격리**(외부 발급 신원이 Track A authMiddleware-only 파괴 라우트에 도달하는 폭발반경 차단). **staff 초기매핑 스코프**: 소유지정 뷰 접근 + `tabs?forMapping=1`(staff만 전체 탭명·advertiser 무시) + `POST /api/trackb/advertisers`(staff는 inad_pm=자기 로그인명 강제·23505→409) + ownership 쓰기는 자기 담당(inad_pm) 업체 & **무소유(또는 전부 자기 소유) 시트만**(`sheetAssignableByStaff` — 타 AE 시트로의 자가 스코프 확장 차단, 재배치는 admin 소관). 회귀가드 `tests/trackBIntranetSso.test.js`. ⚠ **인트라넷(inadd-webapp) 배포 정정(2026-08-04 실측)**: 앱 코드는 **`main` 머지 = 자동배포**다(`.github/workflows/deploy.yml` → `cloudflare/wrangler-action`, `pages deploy dist --project-name intranet --branch main`) — "wrangler 수동 배포 필요"는 옛 기술. 다만 **D1 마이그레이션은 여전히 수동**(그 워크플로가 의도적으로 제외 — 운영 `d1_migrations` 추적 테이블이 실제 스키마와 어긋나 `d1 migrations apply --remote` 가 실패한다): `npx wrangler d1 execute inadd-production --remote --file=./migrations/<파일>.sql`.
- **부품4b 작업오더(발주) 연동**(adminOrMaster · readiness d4): workdesk에서 이 탭에 **발주(`work_orders`)를 수동 링크** + **작업세부 표 자동표시** + **명단 골격 준비**. ★★**Track A 무접촉(핵심)**: `work_orders.linked_tab_*` 는 **order.routes 발주 승인(accept) 흐름이 읽어 동작을 분기**한다(비적격 상태+linked_tab 설정 시 승인 멱등 skip, idempotent 판정 `order.routes.js:733,757`) → Track B는 그 컬럼을 **절대 안 쓰고** **전용 링크 테이블 `trackb_work_order_links`(migration 051)** 에 발주↔탭 연결을 저장(`advertiser_campaigns`와 동일 격리 패턴, 탭당 활성 1개 upsert). `linkWorkOrder`/`unlinkWorkOrder`는 이 테이블만 write(work_orders는 존재검증 SELECT만). 작업세부·d4·overview·명단준비의 "유효 링크" = **Track B 링크 우선 → 없으면 `work_orders.linked_tab`(Track A 승인 링크) 폴백**으로 **읽기만**. `GET /work-orders`(드롭다운·링크상태)·`POST /work-order/link`{workOrderId,sheetId,tabName}·`/unlink`{sheetId,tabName}·`/prepare-roster`. 명단 골격은 `prepareRosterSlots`(participants) — 발주 `recruit_count` 기준 **부족분만**(target−현재 활성) 빈 manual 슬롯 gap-fill(멱등·상한 2000·옵션 라운드로빈, seq 레이스 23505 재시도). **주의**: 빈 슬롯(phone8=NULL)은 `overview.b_total`에 잡혀 A 대비 인원 초과로 보일 수 있으나 `classifyParity`가 phone8 없는 행을 걸러 **진짜불일치(real)엔 안 잡힘 → cutover 게이트 무영향**(채워지면 정상화). `workdeskTab` detail이 발주 전체 필드(옵션목록·유입·가이드·특이사항·상품URL 등) 반환. 회귀가드 `tests/trackBWorkOrder.test.js`(work_orders 무접촉 assert 포함).
- **부품4c 탭 스레드(협업+내부메모, P1 · migration 053)**: 관제실 시트의 협업(브랜드사 확인요청·양사 코멘트)+탭별 내부 메모를 탭 컨텍스트 **한 타임라인**으로. `trackb_tab_threads`(kind `comment`|`request`, `internal_only`, status open→confirming→done) + `trackb_thread_seen`(미확인 배지). **광고주 노출 게이트**: `listThread`/`unseenCounts`가 role='advertiser'면 `internal_only=FALSE` **서버 필터**, `addThread`는 광고주 internal 강제 FALSE. **교차 테넌트 방어**: 모든 mutation은 `_ensureThreadScope`(canAccessTab (sheetId,tabName)) + 서비스가 UPDATE/DELETE를 `(sheet_id,tab_name)`에 결속(B1 IDOR 봉합), `unseenCounts`는 staff/advertiser를 `scopedActiveTabs` 소유 탭으로 서버 강제(B2 메타유출 봉합). `GET/POST /workdesk/thread`·`/:id/status`·`DELETE`·`POST /workdesk/unseen`. 프론트 = 탭 상세 스레드 섹션(확인요청/내부만 토글·상태버튼) + 작업목록 💬 배지. 회귀가드 `tests/trackBTabThreads.test.js`.
- **부품4d 정산 파이프라인(P2 · migration 054)**: 탭 ↔ **인트라넷 계약(`sales` C-번호)/견적(`quotes` Q-번호)** 수동 링크 → **4단계 스텝퍼**(마감자료→견적서→계산서→선금/잔금)를 인트라넷 **HTTP GET 프록시**로 읽어 표시(관제실 시트 정산 컬럼 대체). ★ 인트라넷 D1 무접촉 — 링크는 `trackb_settlement_links`(탭당 활성 1)에만 write, sales/quote는 **읽기만**(`_intranetGet` 5초 타임아웃·fail-soft `proxyDown`, `INTRANET_API_BASE`). **Q4-c 금액 광고주 노출**: `settlementForTab` 역할 렌즈 — 광고주는 `trackb_advertiser_prefs.settlement_visible`(기본 TRUE) OFF면 `{hidden:true}`(프록시 미호출·금액 미포함), ON이면 **그 탭에 링크된 sales 단건만** 프록시(타 업체 계약 도달 불가). 링크/해제=`_ensureEditScope`(내부만), 검색=internalMiddleware, 노출토글=adminOrMaster. 프론트 = 정산 스텝퍼 카드+계약 검색 모달, 소유지정에 "정산 노출" 토글. 회귀가드 `tests/trackBSettlement.test.js`. **오링크 방어**: 링크 시 계약 업체명 로그·반환. 견적서(②)는 `quotes.sales_id` 역파생(`_quoteForSales`), sales 단건 20초 캐시(`_salesById`), 광고주 렌즈는 linkedBy·담당자 미노출.
- **부품4d-2 계약 "연결" → 계약 "매칭"(작업명 유사도 자동 추천 · 스키마 변경 0)**: 정산의 계약 붙이기가 **전체 계약 자유검색**이라 ① 담당자가 매번 C-번호를 찾아 입력해야 했고 ② **타 업체 계약을 남의 탭에 붙이는 오링크가 구조적으로 가능**했다 → 후보를 **그 작업을 소유한 업체(광고주DB)의 계약**으로 좁히고, 작업명과 가장 닮은 계약을 **추천**한다.
  - ★★ **판정 단일 출처 = `utils/contractMatch.js`**(`parseTabName`·`similarity`·`scoreContract`·`rankContracts`, 순수함수 — DB·네트워크·`Date.now` 미접근). 예) `7/21파미고_올레샷_블로그 바이럴` → 날짜 `7/21` · 업체 `파미고`(`sales.business_name`) · 브랜드 `올레샷`(`brand_product`) · 작업 `블로그 바이럴`(`contract_detail`·`product_name`) 네 조각을 각각 대조. 가중치 = 업체 40 / 브랜드 30 / 작업 20 / 진행월 10인데 **적용 가능한 항목만으로 정규화**한다(계약에 브랜드가 안 적혀 있다고 감점하면 정보가 부실한 계약이 영영 추천되지 않는다).
  - ★★ **추천은 제안까지만, 확정은 사람이**(자동 링크 금지 — 작업표 학습·옵션 자동점검과 같은 규율). 최고점이 `RECOMMEND_MIN_SCORE`(45) 미만이면 **추천 없음**(빈 추천 > 틀린 추천), 최고점 동점이면 `tie` 로 **화면이 그 사실을 말한다**.
  - ★ **프론트에 판정 사본 0** — 화면은 서버가 준 순서·점수·근거 문장을 그대로 그린다(화면에서 다시 매기면 "추천은 A인데 정렬은 B"로 갈린다). 후보 클릭은 **배열 인덱스**로 넘긴다(onclick 문자열에 인트라넷에서 온 값 주입 금지).
  - ★★ **인트라넷 응답을 항상 이름으로 재필터**(`intranetSalesForAdvertiser`): 범용 테이블 API 의 `where` 파서는 `col=value` 한 쌍만 처리하고 값에 `=` 가 섞이면 **절을 통째로 무시해 전체 계약**을 돌려준다 → 서버 필터를 믿으면 남의 계약이 조용히 후보에 섞인다. `business_name`(신형)·`advertiser_name`(레거시) 두 컬럼 조회 → 0건이면 `search=` 폴백, 전부 **정규화 이름 정확일치**로 다시 거른다. 업체별 30초 캐시.
  - ★ **소유 업체 판정 = `advertiserForTab`**(탭지정 소유 > 시트전체 소유 — 작업목록 그룹핑 주석과 같은 규칙, gid 는 `raw_sheet_tabs` 로 재해석). Track B `advertisers.name` 은 인트라넷 `business_name` 과 **정확일치가 등록 게이트로 강제**되므로 그대로 계약 조회 키가 된다.
  - ★ **폴백 사유를 화면이 설명한다**(조용히 전체를 보여주면 왜 남의 계약이 뜨는지 모른다): `no_advertiser`(업체 미지정 → 업체관리 안내) · `no_contracts`(그 업체 계약 0건). **[전체 계약에서 찾기] 탈출구는 유지**(계약이 다른 이름으로 등록된 정상 케이스) — 단 전체 검색에서는 **자동 추천을 내지 않고**(근거 불충분), 다른 업체 계약은 `다른 업체 계약` 배지 + `confirm` 후에만 매칭된다.
  - ★ **오링크는 경고까지만**(하드블록 금지): `linkSettlement` 가 소유 업체와 계약 업체를 대조해 `mismatch` 신호 + `logger.warn` 만 남긴다(막으면 이름이 다른 정상 계약을 영영 못 붙인다). `advertiser_name` 이 레거시 공란인 신형 계약 때문에 **업체명은 `business_name` 우선**으로 읽는다.
  - **API** `GET /api/trackb/settlement/contract-candidates?sheetId&tabName&scope=advertiser|all&q=` — 게이트는 링크와 **같은 `_ensureEditScope`**(후보를 보는 사람 = 매칭할 사람. 목록에 업체명·계약금액이 실리므로 열람 범위를 링크 권한보다 넓히지 않는다). 인트라넷 D1 무접촉(GET 프록시만), 쓰기 표면은 종전대로 `trackb_settlement_links` 하나. 어휘도 화면 전체에서 **계약 매칭 / 다시 매칭 / 매칭 해제**로 통일.
  - **화면 = 시안 C(스코어 레일형, 사용자 확정)** · 시안 문서 `frontend/docs/design-contract-match-modal.html`(A/B/C, `?v=` 단독 보기). 좌측 유사도 레일(숫자+막대) + **계약명이 헤드라인**(계약상품상세 → 상품명 → 브랜드·상품 순 대체) · 계약금액 · 일치근거 ✓ 박스. ★ **계약고유번호는 배지 줄의 보조 표기** — 회귀가드가 `.lkname`/`.lkcno` 의 크기·굵기로 위계를 고정한다(되돌리면 종전처럼 번호가 제목이 된다). ★ 근거가 없으면 "일치하는 항목 없음"을 **문장으로** 말한다(낮은 유사도를 숫자만으로 흘리지 않는다). ★ 금액은 `amount || contract_amount` 이고 **정산 카드의 총비용(견적서 금액 우선)과 기준이 다르므로** `title="계약금액"` 으로 밝힌다. 행 빌더는 `_lkRowHtml` 한 벌.
  - ★★ **창구는 둘, 팝업과 동기화는 한 벌**: 계약 매칭은 ① 작업보드 정산 카드 ② **업체관리 › 연결된 탭 표의 [계약] 칸**(미매칭이면 [계약 매칭] 어포던스, 매칭됐으면 계약번호 = 클릭 시 다시 매칭) 두 곳에서 시작한다 — 정산을 실제로 챙기는 화면은 업체별 목록이라 거기서 표를 훑다가 바로 붙일 수 있어야 한다. 팝업은 **`openContractMatchModal(ctx)` 한 벌**이고 내부(`_lkLoad`/`pickSales`/`unlinkSettle`)는 **`_lkTab`(팝업 문맥)만** 본다 — `STATE.settle`(작업보드 전용 상태)을 직접 참조하면 업체관리에서 연 팝업이 죽는다. ★ onclick 에는 **인덱스만**(`ownMatchContract(event,${i})`) — 시트에서 온 탭명·계약번호를 문자열로 넣지 않는다(M1 실측 XSS 규율).
  - ★★ **매칭/해제 후 동기화 지점은 `_contractMatchApplied` 하나** — ① 업체관리 연결탭 행(`salesId`·`contractNumber`) 즉시 갱신 + 재렌더 + 정산 요약 재조회 ② **그 탭을 열어 둔 작업보드 정산 카드만** 재조회. 화면마다 따로 갱신하면 "업체관리에서 매칭했는데 작업보드는 미매칭"으로 갈린다. ★ **옛 계약의 요약 캐시(`STATE.ownSettle[키]`)를 반드시 지운다** — 안 지우면 새 계약 줄에 **이전 계약의 견적·입금 금액**이 그대로 남는다. ★ 이 검증은 **문자열 grep 으로 못 한다**(`if(false) delete …` 를 통과시킴) — 가드가 함수를 vm 으로 꺼내 **실행**해 상태 변화를 본다.
  - **어휘**: 화면에서 "연결"은 계약에 쓰지 않는다 — 정산 요약 셀은 `미매칭 · 계약 매칭 필요`, 업체관리 안내는 "계약이 매칭된 탭만 표시". (`발주 연결`·`소유`는 별개 개념이라 그대로.)
  - 회귀가드 `tests/contractMatch.test.js`(판정 순수함수 실행 + 스텁 pool·fetch 로 서비스 실제 호출 + 라우터 스택 실검사 + 행 렌더러 vm 실행 + 프론트 배선·사본 부재 + **동기화 함수 vm 실행** + **그리드 열 수 ≡ 헤더 칸 수**를 세어 고정 — 열을 끼워 넣을 때 가장 흔하게 깨지는 자리, 변이시험 검출 확인) + **브라우저 실행검증**(헤더↔행 열 정렬·업체관리에서 매칭→표 즉시 갱신·작업보드 카드 동기화·해제 왕복).
- **★★ 계약 매칭을 없애는 근본 수정 — 인트라넷 리뷰오더 등록에서 계약건 필수 선택 (migration 088, 사용자 확정)**: 위 계약 매칭은 **작업이 들어온 뒤** 사람이 계약을 찾아 붙이는 사후 보정이다. 인트라넷에서 **오더를 낼 때 계약을 고르게** 하면 그 계약이 작업오더를 타고 넘어와 **접수 시점에 자동 연결**되므로 매칭 단계가 사라진다. ⚠ **계약 매칭 화면은 그대로 남긴다** — 이 규칙 이전에 들어온 과거 작업건을 메우는 용도(사용자 확정: "지금 매칭하는 과정은 직전까지의 작업건들에 계약을 매칭하는 과정").
  - **인트라넷(inadd-webapp)**: 리뷰오더 폼 상단에 `계약건 *` 카드 + 계약 피커(`GET /api/review-order/contracts?scope=mine|all&q=`, D1 읽기 전용). ★ **내 계약 우선 + 전체 검색**(사용자 확정 — 리뷰오더 폼엔 업체 칸이 없어 좁힐 기준이 필요하다). ★★ **견적서 없는 계약은 선택 불가**(사용자 확정) — 단 **목록에서 숨기지 않고** 비활성 줄 + `견적서 없음 · 선택 불가` + [견적서 작성 →]로 보여준다(숨기면 "내 계약이 왜 없지?"가 된다). ★ 안내 문구 = **"상품구입비용이 아직 확정되지 않았다면 견적서를 먼저 작성해 계약을 만들고, 확정된 뒤 견적을 보정하면 된다"**(사용자 확정 — 견적 후보정이 정상 흐름임을 화면이 말해야 한다).
  - ★★ **서버가 최종 방어**(프론트 우회 차단): `POST /api/review-order` 가 `sales_id` 필수 + **계약 실존 + 견적서 존재**를 D1 에서 재검증하고, `contract_number`·`quote_id` 는 **클라이언트 값을 믿지 않고 계약 원본에서 다시 채운다**(낡은 화면·조작 요청 차단). 계약 없는 오더는 리뷰웹으로 **보내지 않는다**.
  - **리뷰웹**: `work_orders.sales_id/contract_number/quote_id`(088 — **컬럼 추가만·백필 0·FK 없음**, 인트라넷 D1 무접촉 규율은 054 와 동일). `INTAKE_EDITABLE_FIELDS`·`_insertWorkOrder` 에 합류하고 `REQUIRED_SCHEMA` 에 `work_orders.sales_id` 등록(컬럼 누락 = **인트라넷 오더 접수 전면 42703**). 작업오더 상세에 `계약건` 줄(값 없는 과거 오더는 줄 자체가 안 나옴 = 화면 불변).
  - ★★ **접수(accept) 시 자동 연결**: `o.sales_id` 가 있으면 `trackb_settlement_links` 에 INSERT(`linked_by='자동(작업오더)'`). ★ **이미 링크가 있으면 덮지 않는다**(`kept_existing` — 사람이 고쳐 놓은 매칭을 재접수 한 번으로 되돌리면 안 된다). ★ **fail-soft** — 연결 실패해도 접수는 성공하고 사유만 응답에 싣는다(계약은 나중에 화면에서 매칭 가능). 쓰기 표면은 링크 INSERT 하나(기존 행 UPDATE 없음).
  - ★★ **INSERT 컬럼 수 ≡ VALUES 자리 수 ≡ 파라미터 수**를 회귀가드가 **세어서** 고정한다 — 컬럼을 중간에 추가할 때 가장 흔하게 깨지는 자리다(변이시험으로 검출 확인). ⚠ 이 변경으로 `workOrderAutoFill` 가드의 옛 패턴(`manager_name, work_manager, status, created_by`)이 드리프트해 함께 갱신했다(검사 의미 불변).
  - **배포**: 리뷰웹·인트라넷 **둘 다 `main` 머지 = 자동배포**(인트라넷은 `.github/workflows/deploy.yml` → `cloudflare/wrangler-action` 이 `pages deploy dist --project-name intranet --branch main`). ★ 두 배포는 서로를 기다리지 않아도 된다 — 리뷰웹만 먼저면 계약 필드가 안 실려 올 뿐 종전 동작 그대로이고, 인트라넷만 먼저면 리뷰웹이 `sales_id` 를 버려 자동 연결만 안 된다(**양방향 가산적**).
- **부품4e 마감자료 자동생성(P3 · migration 055)**: `trackb_tab_closeouts`(마감일·건수·제출건수, 이력 보존). `generateCloseout`(활성 명단 `campaign_participants` 집계→INSERT, 재생성 보존·빈명단 거부)·`latestCloseout`(스텝퍼 ① 소스 — 정의됨=정산 `closeoutAvailable` 라이브)·`closeoutCsv`(UTF-8 BOM, 광고주 렌즈 연락처 끝4자리 마스킹). `POST /settlement/closeout`(내부만·_ensureEditScope)·`GET /settlement/closeout.csv`(내부+소유 광고주·PII, 광고주는 정산 노출 OFF면 차단). 프론트 = 정산 스텝퍼 ① "마감자료 생성/CSV/재생성" 링크. Track A·시트 무접촉(명단 읽기+trackb write만). 회귀가드 `tests/trackBCloseout.test.js`.
- **부품4f 소유지정 연결탭 정산 컬럼(migration 056)**: 소유지정 "연결된 탭" 표(15컬럼 그리드)에 관제실 정산 컬럼 대응 — 참여/제출/입금 우측에 **견적서일·마감자료일·계산서일·입금액/총비용·입금일·비고**. `settlementSummaryForAdvertiser`(내부 전용 배치): 정산 **링크된 탭만** 인트라넷 프록시(같은 sales 공유 탭은 1회 조회 — `_salesById`/`_quoteForSales` 20초 캐시 공유), 입금액=`sales.matched_bank_amount`(입금매칭 누계)·입금일=`matched_bank_date`(최근, `payment_date` 폴백, `_normIntraDate` YYYYMMDD 정규화)·**총비용=견적서상 금액 우선**(계약금액 폴백, 견적≠계약이면 `amountMismatch` ⚠ 신호만 — 자동판정 없음), 실패=`proxyDown`(fail-soft). `GET /ownership/settlement`(**internalMiddleware — 광고주 미도달**, 소유지정 뷰 자체가 내부 전용), 비고=`trackb_tab_memos`(056, 탭당 1행 upsert 자유텍스트) `POST /tab-memo`(_ensureEditScope: staff 담당 탭만). `ownedTabsForAdvertiser`가 정산링크·최신마감·비고 LATERAL 동봉(전부 LIMIT 1 — 행 곱증식 없음). 프론트: 정산 셀 비동기 필(`loadOwnSettlement`, 업체 전환 경합 `ownSettleFor` 가드), 비고 인라인 편집(클릭→input→Enter/blur 저장·Esc 취소, 행 클릭과 stopPropagation 격리). 회귀가드 `tests/trackBOwntabSettlement.test.js`.
- **부품5 편집 가능 작업보드(레드-블루-심판)** — `migration 048 participant_edits`: **오버레이-only + read-time 합성**. 편집을 `campaign_participants` 물리컬럼에 **절대 안 쓰고** 별도 원장에 저장 → `workdeskTab`이 읽을 때 합성. **정렬/재투영이 물리행을 덮어써도 편집 무손실·무오염**(교차노출 근본 차단). 편집은 **B 전용 리허설**(구글시트·주문·검색 무접촉).
  - **앵커 우선순위**: `order_submission_id`(불변 UUID) > `manual`(물리행 id, 재투영 면역) > `identity_key`(중복 아니면) > **거부**(`no_stable_anchor`/`ambiguous_identity`, fail-closed). seq(물리행) 앵커 금지 = 정렬 면역. `_deriveAnchor`가 편집/합성/revert 공유.
  - **안전장치**: bool은 `value_bool` 전용 컬럼(캐스팅 예외 차단, `to_jsonb(NULL::bool)=SQL NULL`이라 COALESCE 합성이 FALSE 보존)·부분유니크 `uq_participant_edits_active`(활성 1행 backstop, 23505→409)·단일 tx+대상행 FOR UPDATE·append-only `reverted_at`(감사 이력)·중복 identity read/write 양쪽 게이트·미부착(orphan) 편집 노출(조용한 증발 방지).
  - **동시성**: 편집 뮤테이션 `/workdesk/edit·/revert·/hide·/add·/edits`는 `authMiddleware`+`_ensureEditScope`(master/admin=전체 · **staff(AE)=담당 탭만**(`canAccessTab`) · advertiser=차단). 투영 cron·수동 bulk(`/project`, participants `/sync`)를 `withJobLock('trackb_project')`로 상호배제(멀티인스턴스 이중투영·seen-set 플래핑 차단, 락키 기존 6개와 비충돌). remove=import행 hidden 오버레이/manual행 soft-delete, add=manual 물리행(retry-on-23505로 seq 원자화). 회귀가드 `tests/participantEdits.test.js`.
  - **역할 스코프·AE 접근**: `/workdesk` read = master/admin(전체 PII) · **staff(AE)=담당 업체 탭+전체 PII·편집** · **advertiser=소유 탭 · 주문/배송/정산 화이트리스트 시트컬럼만 전체노출**(번호·구매일자·주문번호·수취인·ID·연락처·주소·결제금액·리뷰제출일·입금일 — `_advertiserColumns` 키워드매핑, ★은행/계좌번호/예금주 등 미포함 컬럼은 `rowJson`에도 안 실어 전송=데이터 최소화·유출차단; '입금'룩업은 계좌/은행 포함 컬럼 부정룩어헤드로 제외; DB 신원열(참여자 이름·phone8)은 광고주 그리드 미렌더, 동명 시트열 '연락처'는 rowJson값 표시(gCellDisp 광고주분기), 읽기전용, reviewer 차단, 회귀가드 `tests/advertiserColumns.test.js`). 광고주는 workdesk 로그인에 `advertiser-login` 폴백으로 진입(소유 탭만) — 광고주 역할은 nav에 작업보드 탭만 노출(관측·소유지정 숨김)·읽기전용(`canEdit=role!=='advertiser'`). **광고주 로그인 계정은 업체별 발급**(`advertiser_users`, `advertiser_id` 연결): 소유지정 업체 패널의 "광고주 계정" 섹션에서 아이디/비번 발급·재설정·활성토글·삭제 → `POST /api/trackb/advertiser-account`(adminOrMaster, auth.service CRUD 재사용). ★ `/api/admin/advertiser-users`와 로직 동일하나 **Track B 경로로도 노출**한 이유 = 인트라넷 SSO 관리자 토큰(`via:intranet`)은 `/api/admin/*` 격리라 소유지정 UI에서 계정 발급하려면 `/api/trackb/*` 경로 필요. **광고주 전용 접속 링크(매직 링크, migration 058 `trackb_advertiser_links`)**: **업체마다 고유 URL 자동 존재**(`ensureAdvertiserLink` = INSERT ON CONFLICT DO NOTHING, 소유지정 진입 시 selAdv가 ensure 호출 → 모든 업체가 항상 URL 보유, 수동생성 불필요). 소유지정 "광고주 접속 링크" 섹션에서 복사/회전/폐기(`POST /api/trackb/advertiser-link` adminOrMaster: get/ensure/generate(회전)/revoke/enable, 업체당 1토큰). 광고주는 `workdesk#adv=<token>` 열면 boot이 `POST /api/trackb/advertiser-link-login`(무인증·`advertiserLinkLimiter` 30/분)으로 `loginByLinkToken`(auth.service) 교환. ★★ **로그인 게이트 = 명시 토글(migration 083, 완화 금지)**: 링크는 **기본 공개**(링크만으로 advertiser JWT `via:'link'` 입장)이고, 로그인을 요구할지는 `trackb_advertiser_links.login_required`(기본 FALSE) **하나로만** 정한다 — 업체관리 "광고주 계정" 카드의 **사용/미사용 토글**(`POST /api/trackb/advertiser-link {action:'login-required'}` adminOrMaster). **계정 발급 = 보안 적용이 아니다**(종전엔 활성 계정 존재만으로 자동 잠겨, 계정을 두면서 링크를 열어 두는 선택지가 없었다). 컬럼을 링크 행에 둔 이유 = `loginByLinkToken`이 이미 그 행을 SELECT 해 **쿼리 순증 0**. ★ **켤 때만** 활성 계정 1개 이상을 요구(0개로 켜면 링크도 로그인도 막혀 아무도 못 들어온다) — 끄기는 검사 없음(계정 무손상). ★★ **켠 뒤 계정이 사라져도 열리지 않는다**(fail-closed — "계정 없으면 통과" 폴백을 두면 켜 둔 보안이 조용히 풀린다. 복구는 토글 OFF 또는 계정 재발급). ★ 백필은 **활성 계정이 있던 업체를 TRUE로** 채워 **배포 순간 동작 불변** — 083은 링크 행이 이미 있는 업체(`login_required IS NULL` 인 행만 = 재실행해도 관리자가 끈 값 안 되켬), **084는 "계정은 있는데 링크 행이 아직 없는" 업체**(링크 행은 058이 일괄 생성하지 않고 패널을 열 때 `ensureAdvertiserLink`로 지연 생성되므로, 083만 두면 그 업체들이 배포 후 패널을 여는 순간 DEFAULT FALSE로 공개가 된다 — 코드리뷰로 잡은 실측 구멍). ★ 회전(`generateAdvertiserLink`)·폐기/재활성(`setAdvertiserLinkActive`)의 UPDATE SET 목록에 **`login_required`를 넣지 말 것** — 넣으면 링크 회전 한 번에 잠금이 조용히 풀린다(회귀가드 2.7-2g/h가 고정). ★ 부팅 프리플라이트 `REQUIRED_SCHEMA`에 등록 — 미적용 시 컬럼 참조 3곳이 42703으로 죽어 링크 로그인·업체관리 카드가 전면 500(무신호 전면장애). ★ 켜기/검사는 **한 UPDATE의 `WHERE EXISTS`**(TOCTOU — 검사 직후 마지막 계정이 꺼지는 경합 차단). ⚠ **토글 ON은 이미 접속 중인 세션을 끊지 않는다**(링크 JWT 8시간, 링크 폐기도 동일 한계) — 켤 때 토스트로 고지. ⚠ 공개 링크 안내에 **"마스킹"이라 쓰지 말 것** — 광고주 뷰의 수취인·연락처·주소는 마스킹되지 않는다(`_advertiserColumns`). 프론트: 토글 OFF면 계정 영역을 **흐리게만** 표시하고 **클릭은 막지 않는다**(막으면 계정을 못 만들어 토글을 켤 수 없는 교착), 마지막 활성 계정 비활성/삭제 시 잠금 경고, 안내문 자리에 2줄분 `min-height` 예약(토글 ON/OFF에 카드 높이 불변 — 실측 diff 0). 회귀가드 `tests/advertiserLinkLoginToggle.test.js`, 시안 `frontend/docs/design-advertiser-link-account-toggle.html`. 토큰=추측불가 base64url 24B, **프래그먼트(#)라 서버로그·Referer 미유출**, 유출 대비 즉시 회전/폐기(active), 종료 거래처 거부. 광고주 뷰 자체가 마스킹·읽기전용·소유탭만이라 노출 최소. 링크 섹션 모드 배지(🔓공개/🔒로그인필요/⛔폐기)는 **`login_required` 플래그로 판정**(서버 게이트와 같은 값 — 계정 존재로 추정하지 않는다). 회귀가드 `tests/trackBIntranetSso.test.js`(2.7 링크 CRUD·ensure·2.7-2 계정 사용 토글·2.8 loginByLinkToken 플래그 게이트). 담당 매핑 = `advertisers.inad_pm=로그인명` 재사용(`scopedTabsForStaff`, **양쪽 `TRIM`으로 공백·표기차 footgun 방어**; 안정 식별자 staff_id 연결은 후속). ★**스코프 판정은 클라이언트가 보낸 `tabGid`를 신뢰하지 않고 `(sheetId,tabName)`으로 gid 재해석**(`canAccessTab`) — 명단·PII가 전부 tabName으로 조회되므로 스코프도 tabName 기준이어야 함(과거 read가 소유 gid를 쿼리스트링에 실어 타 탭 명단을 긁던 교차열람 봉합, read/edit 일원화). 회귀가드 `tests/participantEdits.test.js`(6a~6f2).
- **부품5b 커스텀 열(행별 자유메모) + 셀 배경색(migration 080, 8/3)** — 리뷰웹시스템[3버전] 그리드 사용성 보강. **participant_edits/write-back 파이프라인과 완전 분리된 신규 테이블만**(`trackb_custom_columns`·`trackb_custom_column_values`·`trackb_cell_colors`) — 시트에 절대 안 쓴다(그 쪽 disjoint·guard·tier 불변식 무접촉). 앵커는 부품5와 동일 철학(order>manual>identity, `_resolveAnchorForWrite`가 identity 중복까지 재검증해 ambiguous면 거부 — `workdeskTab` 합성측 게이트와 대칭, 안 맞추면 동명이인 등 중복 identity 행끼리 메모·색이 새 나간다). 열 추가/삭제·값 저장·색 저장 4개 라우트(`/workdesk/custom-column`·`/workdesk/custom-value`·`/workdesk/cell-color`) 전부 `_ensureEditScope`(master/admin 전체·staff 담당 탭만·advertiser 차단) — **advertiser 그리드엔 애초에 `customColumns` 미동봉**(showEdits만). 커스텀 열 삭제는 `deleted_at` 소프트(정의는 보존, 표시열에서만 제외), 값은 `(column_id, anchor)` 유니크 upsert. 셀 배경색은 프론트 드래그로 잡은 (rowId, field) 목록에 색 1개를 일괄 적용(`field`='col:<헤더>'|'ccol:<열id>'|'idname'|'idphone', `#`열 제외) — 행별 FOR UPDATE 트랜잭션으로 순차 적용(레이스 방지, 대용량 드래그는 순차 왕복이라 다소 느릴 수 있음·상한 3000셀). 프론트(`workdesk.html`): 헤더 드래그(열 선택/숨기기)와 별개로 **본문 셀 드래그**(`_ensureCellColorUI`, mousedown~mouseup 이동 감지)로 사각형 범위 선택 → 플로팅 팔레트(`_openColorPopover`)에서 색 클릭 시 낙관적 반영(inline `background-color`, 클래스 기반 배경(`gok` 등)보다 우선)+백그라운드 저장. **단순 클릭(이동 없음)은 종전 셀 편집(`beginEditCell`)이 그대로 처리** — 드래그로 다른 셀까지 이동했을 때만 색상 UI 개입. 커스텀 열 셀도 `col:` 오버레이 셀과 같은 `eCell`/`beginEditCell`/`commitCellEdit` 경로를 타되 되돌리기(↩)는 없음(원본 개념이 없음). 컬럼 표시명은 `_colLabel`(내부 key `ccol:<uuid>` → 사용자가 지은 이름)로 항상 변환해서 보여준다.
- **진실원천 전환(부품6)·write-back 엔진(P2/P2-2)도 같은 문서로 분리** — `source_of_truth` 플래그를 읽는 소비처는 **write-back 엔진뿐**이고 Track A 핫패스(검색·주문원장·인덱스빌드·행배정·큐워커·RAW미러)는 **참조 0**(회귀가드 `trackBSourceOfTruth` 가 fs 스캔으로 고정). 플립은 master 전용, `TRACK_B_WRITEBACK`·`TRACK_B_WRITEBACK_FULL` 이중게이트, 컬럼 disjoint(상태칸 2열만)·blank-only·행이동 재검증이 Track A 무영향의 근거.

### ★★ AE(staff) 권한 확대 (사용자 확정 2026-08-19) — 6개 기능을 `adminOrMaster` → `internalMiddleware`
- **계기**: AE 가 모집공고 카드의 **[📅 날짜별 인원 조절]** 을 눌렀는데 팝업이 `관리자 권한이 필요합니다`(403) 로만 뜨는 막다른 길. 버튼에는 역할 게이트가 없고 라우트만 `adminOrMaster` 라 **보이는데 안 되는** 비대칭이었다.
- **개방한 것 6묶음**(전부 광고주·리뷰어는 계속 차단): ① 날짜별 인원 조절·차수·작업표 재구성(`campaigns/:id/daily-plan|rounds|worktable-rebuild`) ② 참여 리뷰어 게이트 + 블랙리스트 관리기준(`campaigns/:id/reviewer-gate*`·`settings/reviewer-gate-criteria`) ③ 관측(`overview`·`parity`·`parity-all`·`parity-trend`·`source-of-truth` **읽기**) ④ 업체 이관·접속링크·광고주 계정·담당AE 지정·거래처 삭제(+후보 자동완성 `intranet/users`) ⑤ 주문 삭제·번호 배정·수동 리뷰제출·줄 정리 ⑥ 편집 허용명단 관리.
- ★★ **화면 판정 단일 출처 = `_isInternalRole`**(workdesk.html) — 서버 `internalMiddleware` 와 1:1. 역할 문자열 비교 사본을 다시 만들면 **버튼과 서버가 갈려** 이번 사고(보이는데 403)나 그 반대(안 보이는데 권한은 있음)가 재현된다.
- ★ **열지 않은 것**(의도): 등록리뷰어DB·입금관리·C/S 문의 본문·전사 설정(작업표 표준열·AI 예시·안내문구·공지·사업자번호)·시스템 오류로그·시트 가져오기·탈시트 전환·시트 반영 점검 — 전사 PII 이거나 등록/전환 창구라 `adminOrMaster` 유지. **`source-of-truth` 플립·write-back·그림자 투영은 그대로 master 전용.**
- ★ **AE 에게 안 여는 화면은 nav·타일·버튼도 안 그린다**(눌러도 403 인 막다른 길 금지) — 이번에 staff nav 에 **[관측]** 을, 설정 탭에 **gatecriteria** 패널을 함께 열었다(공고별 [🚫 참여 리뷰어 관리]의 판정 기준이라 그 기능과 같은 범위).
- ★ **업체 등록 폼의 담당AE 칸은 staff 에게 여전히 읽기 전용** — 서버 `createAdvertiserScoped` 가 staff 생성 시 담당자를 본인으로 강제하므로, 열면 입력이 조용히 무시되는 no-op 이 된다. 등록 직후 업체 패널의 **[변경]**(`advertisers/inad-pm`, 이번에 개방)으로 지정한다.
- ⚠ **편집 허용명단(`workdesk_editors`)은 이제 AE 도 고칠 수 있다** — 접수·발행 권한을 스스로 넣을 수 있다는 뜻(사용자 확정). `editorOnlyMiddleware` 로 자기 자신을 게이트하지 않는 규율은 그대로(명단에서 빠지면 아무도 못 고치는 잠금 방지).
- ★★ **2026-08-19 추가 확정(빨간 가드 정리 중 드러남)**: **저장폴더(`/tab-folders`)·공유 링크(`/share-link/:code`)·편집 허용명단 UI** 도 같은 규칙이다 — AE 는 담당이 아니어도 연다. 그 셋은 이미 코드가 그렇게 동작하고 있었는데(`/tab-folders` 는 `staff는 작업보드 전체 운영 권한` 주석까지 달려 있었다) **가드와 CLAUDE.md 만 옛 규칙에 머물러 빨간 상태였다**. ★ 좁히기로 되돌린다면 `/workdesk`·`/tabs` 와 **함께** 좁혀야 한다(한 곳만 좁히면 "목록엔 보이는데 링크·폴더로는 안 열리는" 막다른 길). ★ 남은 경계는 **광고주 차단**이고 그것은 라우터 단계(`internalMiddleware`)가 맡으므로 캐시가 그 앞을 우회할 수 없다.
- 회귀가드는 **의미를 바꿔 갱신**했다(기대값 = 새 사용자 확정): `ownershipTransfer`·`sheetlessRetireRows`·`workdeskOrderDelete`·`workdeskManualReviewSubmit`·`workdeskOrdersCampaigns`·`campaignReviewerGate`·`campaignDailyPlan`·`trackBProjectionCoverage`·`workdeskHome`·`vendorManagementRedesign`·`worktableTemplate`·`workdeskSettingsTab`·`sheetImport`. ★ 패널·타일 목록 가드는 "목록 전체 일치"가 아니라 **"관리자 목록에 있고 AE 목록엔 없다"** 로 바꿨다 — 목록이 늘 때마다 무관한 가드가 조용히 빨개지지 않게.

## 디자인 스킬 2종 (.claude/skills)
- `impeccable`(새 화면·전면 리디자인·디자인 오딧) · `design-taste-frontend`(새 홍보/랜딩·소비자용 페이지). 트리거는 각 SKILL.md 의 description 이 갖고 있다.
- ★★ **기존 리뷰웹시스템(workdesk/admin) 화면의 기능 수정·버그 수정·소규모 UI 조정에는 쓰지 않는다** — 이 CLAUDE.md 의 규율(단일 출처·회귀가드·기존 idiom·시안 문서의 외부 리소스 0)이 우선한다. 필요하면 사용자가 `/impeccable` 로 명시 호출한다.

## 브라우저 자동화(Playwright)
- 세션 시작 훅(`.claude/hooks/session-start.sh`)이 준비한다. 쓰는 법은 두 줄:
  ```js
  const { chromium } = require('playwright');
  const b = await chromium.launch({ executablePath: process.env.PW_CHROMIUM });
  ```
- ★★ **`executablePath` 를 빼지 말 것**(컨테이너 Chromium 빌드 번호와 전역 playwright 기대 번호가 다르다) · ★ **`npx playwright install` 금지**(이미 `/opt/pw-browsers` 에 있다) · 훅이 `NODE_PATH` 를 세우므로 그게 없으면 설치돼 있어도 `require` 가 실패한다.
- ★ **브라우저 실측이 필요한 검증**(레이아웃·IME·스크롤·팝업)은 반드시 **실제 http 오리진**에서 — `file://` 은 상대경로 버그를 가린다.

## 배포 (자동)
- `main` 브랜치에 머지되면 **Cloudflare Pages(프론트)와 Railway(백엔드)가 GitHub 연동으로 자동 배포**합니다.
- 별도의 빌드/배포 GitHub Action은 없습니다. `main` 머지 = 배포.

### ★★ 테섭 / 본섭 분리 (완화 금지 · 2026-08-19 정리)
- **`main` = 본섭(운영)**, **`TEST_REVIEW` = 테섭(테스트)**. 두 브랜치는 **의도적으로 분기**돼 있다(2026-08-19 실측: 테섭이 main보다 **1718커밋 앞**, main도 테섭보다 **120커밋 앞** — 양방향).
- ★★ **`TEST_REVIEW`(및 `TEST_REVIEW-deploy`)를 `main`에 병합하지 않는다.** 본섭 반영 방식은 **"검증된 변경만 선별 적용"**이다(2026-08-18 패치노트: *"TEST_REVIEW 브랜치 병합이 아니라 검증된 권한 변경 두 건만 본섭 최신 코드에 선별 적용"*). 한 번 병합하면 **미검증 1718커밋이 운영에 유입**되고, 되돌리기가 사실상 불가능하다.
  - "테섭 내용 반영해줘" = **그 기능의 변경만 최신 `main` 위에서 다시 구현하거나 cherry-pick** 하라는 뜻이다. 병합으로 처리하지 말 것.
  - 반대 방향(`main` → `TEST_REVIEW`)은 무관(테섭 최신화는 자유).
- ★★ **본섭 PR은 반드시 최신 `origin/main` 기준으로 만든다.** 착수 전 `git fetch origin main && git log --oneline HEAD..origin/main` 로 뒤처짐 0을 확인한다. 수십 커밋 뒤처진 브랜치를 병합하면 **그 시점 파일로 회귀**한다(2026-08-19에 이 사유로 열린 PR 6건을 전부 닫았다 — #802·#769는 내용이 이미 main에 있었고 workdesk.html·payment 서비스에서 충돌).
- ★ **병합 전 3줄 점검**(코덱스·클로드 어느 쪽이 만든 PR이든 동일):
  1. `git log --oneline HEAD..origin/main` → 뒤처진 커밋 수 확인
  2. `git merge-tree --write-tree origin/main <PR head>` → **충돌 0** 확인(충돌이 있으면 최신 main 위에서 다시 만든다)
  3. `ls server/migrations` → **같은 번호·같은 이름의 마이그레이션이 이미 있는지** 확인(있으면 그 PR의 내용은 이미 배포된 것)
- ★ **테섭 URL 분기는 코드에 있다**(`frontend/api.js`): `test-review-wdb-web-*` 프론트는 `test-review-wdb-*` API로만 연결되고, Railway PR 테섭은 PR 번호를 보존해 짝을 맞춘다. **테섭 전용 설정(테스트 API 주소·자동로그인 `TEST_AUTO_LOGIN`·테스트 Origin CORS)은 본섭에 반영하지 않는다**(2026-08-15 패치노트).
- ★ 운영 반영 후에는 `docs/patch-notes/YYYY-MM-DD-*.md` 에 **적용 대상·변경·검증·롤백**을 남긴다(기존 관행).

## 요구사항 확인 (질문 우선)
- **사용자의 입력 메시지에서 의미가 모호하거나 불확실한 부분이 있으면, 추측해서 진행하지 말고 반드시 먼저 질문해서 답을 받은 뒤 구현한다.**
- 한 번에 명확히 하기 위해 필요한 질문은 모아서 묻되, 답이 없으면 해당 부분의 구현을 시작하지 않는다.
- 질문이 필요한 예시: 변경 대상/범위가 애매할 때, 용어·기능명이 여러 의미로 해석될 때, 데이터 모델/엔드포인트 설계 선택지가 갈릴 때, UI 동작·문구가 특정되지 않았을 때.
- 단, 합리적인 기본값이 명백하거나 코드베이스에서 직접 확인 가능한 사실은 질문하지 말고 그 기준으로 진행한 뒤 결과에 명시한다.
- 큰 작업일수록 **목적·대상(독자/사용자)·성공기준(KPI)·제약**을 먼저 파악한 뒤 착수한다(질문은 모아서 묻는다).
- 이 규칙은 "사용자에게 다시 묻지 않고 자동 진행"하는 아래 워크플로보다 **우선**한다(불확실하면 먼저 질문 → 확정된 뒤 자동 워크플로 진행).

## 큰 작업의 진행 방식 (품질 강화)
범위가 크거나 복잡한 작업(여러 파일·엔드포인트·UI 흐름이 얽히거나 영향 범위가 넓은 변경)에 적용한다.
작고 명확한 변경에는 적용하지 않는다(위 자동 워크플로를 그대로 따른다).

### 과업 분해 & 종속성
- 큰 요청은 독립적으로 처리 가능한 작은 과업으로 쪼갠 뒤, **숨은 종속성과 올바른 순서**를 먼저 정리한다.
  - 예: `마이그레이션 → 라우트 → 프론트 연동 → 권한 검증` 순서, "스키마 확정 전 프론트 UI 확정 불가" 같은 제약.
- 분해 결과(과업 목록 + 순서)는 착수 전에 짧게 공유한다.

### 단계적 진행 (한 번에 다 쓰지 않기)
- 긴 문서·대규모 변경은 단번에 작성하지 말고 단계로 나눈다:
  ① **요구 정리**(목적·대상·핵심질문) → ② **조사/영향분석**(데이터·리스크·기존 코드 확인) → ③ **설계 개요**(섹션·모듈·엔드포인트 배치) → ④ **구현/최종본**.
- 각 단계에서 맥락을 충분히 확보한 뒤 다음 단계로 넘어간다.

### 다각도 비판 검토
- 구현·제안을 완료하기 전에 최소 세 관점에서 스스로 비판적으로 점검한다(필요 시 `code-reviewer`/`proposal-judge` 서브에이전트 활용):
  - **운영/보안 관점**: 역할 권한(master/admin/staff) 누락, 인증·토큰·데이터 노출, 되돌리기 어려운 작업의 위험.
  - **실사용자 관점**: 관리자·AE담당자·리뷰어의 실제 흐름에서 어색하거나 상황에 안 맞는 부분.
  - **운영 리스크 관점**: 자동배포 영향, 마이그레이션 리스크, 논리적 허점·엣지케이스.
- 검토에서 발견한 문제는 머지 전에 반영하거나, 반영이 어려우면 사용자에게 보고한다.

### 레드팀·블루팀·심판 (적대적 설계 프로세스)
라이브 핫패스 변경·최적화처럼 **오류 리스크가 큰 작업**은 다음 3역 적대적 프로세스로 설계·검증한다(서브에이전트 정의: `.claude/agents/red-team.md`·`blue-team.md`·`judge.md`).
- **레드팀**: 예상되는 오류 상황을 적대적으로 분석·제시(동시성·엣지케이스·쿼터·권한·데이터정합성·폴백소실). 읽기 전용.
- **블루팀**: 레드팀의 각 오류를 막는 방어 코드구조를 설계(레드 항목 1:1 대응 + 구체 코드). 읽기 전용 산출(텍스트 코드).
- **심판**: 레드↔블루를 대조 검증(빠진 방어·과잉 방어·새 위험 식별) 후 **오류 없는 최종 코드구조**를 구체 코드로 산출. 적용·테스트·배포는 메인루프가 한다.
- 실행: 워크플로 `Workflow({ name: 'red-blue-judge', args: { task: '...' } })`(`.claude/workflows/red-blue-judge.js`, 레드→블루→심판 순차) 또는 `Agent`로 역할별 개별 호출. 심판의 최종 코드를 메인루프가 적용→`node --check`/단위테스트→PR→머지.

### 맥락 유지 (CLAUDE.md 최신화)
- 디렉터리 구조·규칙·용어·워크플로가 바뀌면 그 변경의 일부로 이 `CLAUDE.md`를 함께 갱신해, 다음 작업에서도 맥락이 그대로 유지되도록 한다.

## 작업 워크플로 (기본 동작)
요청받은 변경을 완료하면 **사용자에게 다시 묻지 않고 아래를 자동으로 진행**합니다.
(작업 지정 브랜치가 있으면 그 브랜치에서, 없으면 `claude/<설명>` 브랜치를 만들어 작업)

> **표준 권한 부여 (standing authorization)**
> 이 저장소에서는 **PR 생성과 `main` 머지(=자동배포)를 사용자에게 매번 다시 묻지 않고 자동으로 수행**하도록 사전 승인합니다.
> "PR은 명시적으로 요청할 때만 생성한다"는 일반 기본동작보다 이 지침이 우선합니다.
> 단, 아래 **주의** 항목(파괴적/되돌리기 어려운 변경, 설계상 모호한 대규모 변경)에 해당하면 자동 머지 전에 먼저 확인합니다.

1. **커밋 & 푸시**: 명확한 메시지로 작업 브랜치에 커밋하고 `git push -u origin <branch>`.
2. **PR 생성**: 작업 브랜치 → `main` 으로 PR을 만든다.
3. **머지**: 생성한 PR을 `main` 에 머지한다 (→ 자동배포 트리거). **사용자 추가 확인 없이 자동 머지.**
4. **채팅 보고**: 머지/배포 후 **채팅방에 추가·수정된 내용을 간략히 보고**한다.
   - 형식: 변경 요약(불릿 2~5줄) + PR 번호/링크 + "배포 자동 진행 중" 한 줄.
   - 길게 늘어놓지 말고 핵심만. 파일 단위 변경은 필요한 것만 언급.

### 보고 예시
> ✅ 머지 완료 (PR #NN) — 자동배포 진행 중
> - 관리자 대시보드에 "업무포털" 버튼 추가 (AE담당자 왼쪽)
> - 클릭 시 자동 로그인되도록 토큰 핸드오프 구현

### 주의
- 파괴적이거나 되돌리기 어려운 변경(데이터 삭제, 시크릿 변경, 마이그레이션 파괴 등)은 자동 머지 전에 먼저 확인한다.
- 변경 범위가 크거나 설계상 모호한 부분이 있으면 머지 전에 사용자에게 확인한다.
- 커밋/푸시는 항상 작업 지정 브랜치 기준. `main` 에 직접 푸시하지 않는다(PR 경유).
