# CLAUDE.md

## Shared UI naming

### UI style proposal gate

- UI 변경·신규 화면·시안 요청을 받으면, 구현 전에 반드시 **A / B / C 스타일 중 어떤 방향이 적합한지 먼저 제안**하고 사용자의 선택을 받는다. 사용자가 이미 특정 스타일을 지목한 경우에는 그 스타일을 바로 적용한다.
- **A·B스타일은 아직 정의되지 않았다** — 임의로 정의하지 말고 기존 화면의 근거를 찾거나 사용자에게 기준을 확인한다.
- **C스타일**: Track B 입금관리의 담당자·작업 선택 UI를 뜻한다. 담당자는 `전체 / 망고 / 만두` 등 단일 선택 토글로 고르고, 작업은 펼쳐진 목록에서 행 전체를 눌러 복수 선택·해제한다. 체크박스나 드롭다운 작업 선택은 사용하지 않는다. 선택 행은 배경·테두리·확인 표시로 구분하며, 합계 금액과 다운로드 대상은 선택된 작업들의 체크된 입금 항목만 기준으로 계산한다.
- C스타일 영역의 담당자 표기는 `박은비`가 아니라 항상 `망고`로 정규화한다.
- ★★★ **시안·UI 변경에서 "정렬을 맞췄다 / 폭을 고쳤다"는 배포본에서 픽셀로 재서만 말한다**(사용자 확정 2026-09-21) — 눈으로 보고 판단하지 않고, 머리글·값을 **같은 기준**(글자는 글자끼리 `Range`)으로, **수정 전·후를 함께** 잰다. 섞어 재면(칸 박스 vs 글자) 정렬과 무관하게 `0.0` 이 나와 **어긋난 화면을 "맞췄다"고 보고하게 된다**(실제로 두 번 밟았다). 측정 방법·CSS 우선순위 함정은 [162](docs/decisions/162-디자인-시안-맞췄다는-배포본에서-픽셀로-재서만-말한다-사용자-확정-2026-09-21.md).

이 파일은 Claude Code가 이 저장소에서 작업할 때 따르는 지침입니다.

## 프로젝트 개요
리뷰(체험단) 관리 시스템.
- **백엔드**: Node.js + Express + PostgreSQL → Railway 배포
- **프론트엔드**: Vanilla JS + HTML → Cloudflare Pages 배포 (https://review-web-system.pages.dev)
- **인증**: JWT (`/api/admin/login` 발급, `Authorization: Bearer` 헤더)

## 디렉터리 구조
- `frontend/` — 정적 페이지(`admin.html` 관리자 대시보드, `portal.html` 업무포털, `search.html` 리뷰제출, `staff.html` AE담당자 등)와 `js/`, `css/`. `docs/prd-userflow.html` = 시스템 소개서(PRD·유저플로우·와이어프레임, 관리자 헤더 "시스템 소개서" 버튼에서 새창 열람)
- `frontend/api.js` — GAS 호환 API 래퍼(`gasGet`/`gasPost`), 토큰은 `sessionStorage.admin_token`
- `server/src/routes/` — Express 라우트(`*.routes.js`), `server/src/middleware/auth.middleware.js`에 역할별 미들웨어(master/admin/staff)
- `server/migrations/` — DB 마이그레이션
- `.github/workflows/` — DB/Drive 백업·복구 리허설 (앱 빌드/배포 워크플로 아님)
- `docs/decisions/` — **사고·결정 기록**(항목당 파일 1개 · 목차 `INDEX.md`). 작업 전 반드시 검색 — 바로 아래 규칙.
- `docs/history/` — 완료된 대형 이관의 진행 기록(탈구글시트·블로그체험단·Track B 실험트랙)

### ★★★ 사고·결정 기록 = `docs/decisions/` (작업 전 검색 필수)

이 저장소의 **사고 경위 · 설계 결정 · 완화 금지 규율**은 항목당 파일 하나로 `docs/decisions/` 에 있다(**166항목**).
목차 = **[`docs/decisions/INDEX.md`](docs/decisions/INDEX.md)**.

- ★★★ **코드를 한 줄이라도 고치기 전에 INDEX 를 먼저 검색한다(생략 불가)** — 내가 건드릴 기능·화면·표·파일 이름으로 훑고, 걸리는 항목의 **본문을 끝까지 읽은 뒤** 착수한다. 아래 「★★★ 사전 충돌 점검」의 1번 축(단일 출처·사본)이 여기서 시작된다.
  ```bash
  grep -n  "<키워드>" docs/decisions/INDEX.md   # 제목·migration 번호로 항목 찾기
  grep -rln "<키워드>" docs/decisions/          # 본문까지 뒤져 관련 항목 찾기
  ```
- ★★ **못 찾았다고 "없다"로 단정하지 않는다** — 같은 주제가 여러 항목에 나뉘어 있다. **기능 이름 · 표 이름 · migration 번호 · 회귀가드 파일명** 네 가지로 각각 찾는다.
- ★ 아래 목록은 **되돌리는 순간 그 사고가 재현되는** 항목(본문에 「완화 금지」가 있는 것)만 제목을 남긴 것이다. **제목만 보고 판단하지 말고 반드시 본문을 연다.**
- ⚠ ★★★ 표시 항목 16개 중 **7개는 「완화 금지」 문구가 없어 아래 목록에 없다**(전면 백지 장애·등록리뷰어 오삭제 등) — INDEX 의 ★★★ 열로 확인한다.

#### 완화 금지 항목 58개 — 되돌리기 전에 반드시 본문을 읽는다

- [★★ 접수 시 "동일 이름의 기존 광고주" — 자동 병합 금지 / 사람이 확인하면 연결 (2026-08-19)](docs/decisions/004-접수-시-동일-이름의-기존-광고주-자동-병합-금지-사람이-확인하면-연결-2026-08-19.md)
- [작업오더 관리자 수동 수정 (인트라넷 보낸오더카드 동기화)](docs/decisions/005-작업오더-관리자-수동-수정-인트라넷-보낸오더카드-동기화.md)
- [★★ Drive 이미지 프록시 = 왕복 1회 (2026-08-24 실측)](docs/decisions/008-Drive-이미지-프록시-왕복-1회-2026-08-24-실측.md)
- [★★ 캠페인 기간별 리뷰비 (migration 082) — 리뷰비는 "그 건의 참여 시점" 금액이다](docs/decisions/010-캠페인-기간별-리뷰비-migration-082-리뷰비는-그-건의-참여-시점-금액이다.md)
- [★★ C/S 문의방 제목 → 그 작업의 작업보드 (2026-08-25 · 스키마 변경 0 · 서버 변경 0)](docs/decisions/012-C-S-문의방-제목-그-작업의-작업보드-2026-08-25-스키마-변경-0-서버-변경-0.md)
- [★★ 작업보드 우클릭 → 리뷰어에게 메시지 (관리자가 문의방을 먼저 연다 · 2026-08-21, 스키마 변경 0)](docs/decisions/013-작업보드-우클릭-리뷰어에게-메시지-관리자가-문의방을-먼저-연다-2026-08-21-스키마-변경-0.md)
- [★★ 브랜드별 담당자 — 업체가 작업마다 셀프 입력 (migration 136, 사용자 확정 2026-08-24)](docs/decisions/014-브랜드별-담당자-업체가-작업마다-셀프-입력-migration-136-사용자-확정-2026-08-24.md)
- [관리자 닉네임 — 1:1문의 실명 노출 차단 (migration 078)](docs/decisions/015-관리자-닉네임-1-1문의-실명-노출-차단-migration-078.md)
- [★★ 작업표 옵션 칸 — 칸이 없으면 리뷰어가 고른 옵션이 조용히 사라진다 (2026-08-20 실사고, 스키마 변경 0)](docs/decisions/019-작업표-옵션-칸-칸이-없으면-리뷰어가-고른-옵션이-조용히-사라진다-2026-08-20-실사고-스키마.md)
- [★★ 탭 이름이 바뀌면 모집공고의 연결 탭 이름도 따라간다 (2026-08-24 실측 · 「맛고」)](docs/decisions/020-탭-이름이-바뀌면-모집공고의-연결-탭-이름도-따라간다-2026-08-24-실측-맛고.md)
- [★★ 발주 정원 폴백 — 공고 총인원·일건수가 0이면 발주서 값이 실제 정원이다 (2026-08-21, 스키마 변경 0)](docs/decisions/030-발주-정원-폴백-공고-총인원-일건수가-0이면-발주서-값이-실제-정원이다-2026-08-21-스키마-변.md)
- [★★ 모집 누적·정원의 표(주문 원장) 기준 전환 (2026-08-20 — "작업보드 169 vs 카드 49" 신고)](docs/decisions/031-모집-누적-정원의-표주문-원장-기준-전환-2026-08-20-작업보드-169-vs-카드-49-신고.md)
- [★★ 관제 "확정으로 안 잡힌 작업표 줄" — 대조는 세 키, 종류별로 할 일이 다르다 (2026-08-19 위프 800건 실측)](docs/decisions/033-관제-확정으로-안-잡힌-작업표-줄-대조는-세-키-종류별로-할-일이-다르다-2026-08-19-위프-8.md)
- [일 모집한도 미달분 자동 이월 (migration 066)](docs/decisions/034-일-모집한도-미달분-자동-이월-migration-066.md)
- [★★ 이월 보류·수동 반영 (carry_mode, migration 098) — 이월의 반영 시점을 사람이 고른다](docs/decisions/036-이월-보류-수동-반영-carry_mode-migration-098-이월의-반영-시점을-사람이-고른다.md)
- [★★ 모집인원 조절 = 총량 균형 모드 (요구 ①~⑥, 시안 `frontend/docs/design-recruit-quota-balance.html`)](docs/decisions/037-모집인원-조절-총량-균형-모드-요구-시안-frontend-docs-design-recruit-quot.md)
- [★★★ 복합유형 작업의 「상품」 열 — 고른 상품이 표에서 사라지던 것 (migration 138, 사용자 확정 2026-08-25)](docs/decisions/043-복합유형-작업의-상품-열-고른-상품이-표에서-사라지던-것-migration-138-사용자-확정-202.md)
- [★★ 복합유형 작업 — 선택 단위 + 선택지별 유입가이드 (migration 137, 사용자 확정 D1~D6 2026-08-20)](docs/decisions/044-복합유형-작업-선택-단위-선택지별-유입가이드-migration-137-사용자-확정-D1-D6-2026.md)
- [★★ 모집공고 보관(폐기) — 끝난 공고를 목록에서 내린다 (migration 130, 사용자 확정 2026-08-19)](docs/decisions/048-모집공고-보관폐기-끝난-공고를-목록에서-내린다-migration-130-사용자-확정-2026-08-1.md)
- [★★★ 작업보드 줄이 "중복"으로 잘못 지워진 사고 — 주문 링크는 표 주문번호가 정한다 (2026-08-19)](docs/decisions/051-작업보드-줄이-중복-으로-잘못-지워진-사고-주문-링크는-표-주문번호가-정한다-2026-08-19.md)
- [★★★ 초과 상태 공고의 저장 잠금 해제 — 정원 게이트는 "값이 바뀔 때만" (사용자 확정 2026-08-24)](docs/decisions/055-초과-상태-공고의-저장-잠금-해제-정원-게이트는-값이-바뀔-때만-사용자-확정-2026-08-24.md)
- [★★★ 총건수 초과는 감추지 않고 표시한다 — 줄을 정원에 맞춰 자르지 않는다 (사용자 확정 2026-08-24)](docs/decisions/056-총건수-초과는-감추지-않고-표시한다-줄을-정원에-맞춰-자르지-않는다-사용자-확정-2026-08-24.md)
- [★★★ 같은 주문이 10분마다 새 줄로 늘어난 사고 — 중복 방어를 링크에서 떼어낸다 (2026-08-19)](docs/decisions/060-같은-주문이-10분마다-새-줄로-늘어난-사고-중복-방어를-링크에서-떼어낸다-2026-08-19.md)
- [★★★ 🧹 줄 정리(은퇴) — 사용자 기능 제거 · 내부 실행부는 유지 (2026-08-21 · 사용자 확정)](docs/decisions/062-🧹-줄-정리은퇴-사용자-기능-제거-내부-실행부는-유지-2026-08-21-사용자-확정.md)
- [★★ 작업보드 [⋯] 도구 메뉴 정리 (2026-08-21 · 사용자 확정)](docs/decisions/063-작업보드-⋯-도구-메뉴-정리-2026-08-21-사용자-확정.md)
- [★★ 고아 캡처 정리 — A 자동 · B·C 수동 (2026-08-21 A / 2026-08-24 B·C · migration 134)](docs/decisions/064-고아-캡처-정리-A-자동-B-C-수동-2026-08-21-A-2026-08-24-B-C-migrati.md)
- [★ 작업표 줄 ↔ 주문 링크 교정 도구 (2026-08-19 · 스키마 변경 0)](docs/decisions/065-작업표-줄-주문-링크-교정-도구-2026-08-19-스키마-변경-0.md)
- [★★ 작업보드·업체 공유 링크 — 메신저로 그 화면 주소를 바로 전달 (migration 131, 사용자 확정 2026-08-19)](docs/decisions/075-작업보드-업체-공유-링크-메신저로-그-화면-주소를-바로-전달-migration-131-사용자-확정-2.md)
- [★★★ 접수하기 → 모집공고 두 단계 (사용자 확정 2026-08-21 · 스키마 변경 0)](docs/decisions/076-접수하기-모집공고-두-단계-사용자-확정-2026-08-21-스키마-변경-0.md)
- [★★★ 업체 지정 단위 = 작업(작업보드) 하나 — 시트 축 폐지 (사용자 확정 2026-08-23, 스키마 변경 0)](docs/decisions/082-업체-지정-단위-작업작업보드-하나-시트-축-폐지-사용자-확정-2026-08-23-스키마-변경-0.md)
- [★★★ 배송유형 6종 — 회수·혼합은 "값 + 부속정보"다 (migration 135, 2026-09-16 직접배송 추가)](docs/decisions/101-배송유형-6종-회수-혼합은-값-부속정보-다-migration-135-2026-09-16-직접배송-추가.md)
- [★★★ 접수 후 인트라넷 원본 수정 — 상품 구성은 "잠긴 부분만" 잠근다 (사용자 확정 2026-09-21 · 스키마 변경 0)](docs/decisions/160-접수-후-인트라넷-원본-수정-상품-구성은-잠긴-부분만-잠근다-2026-09-21.md)
- [★★★ 작업오더 결제금액 수정 → 모집공고까지 전파 (사용자 확정 2026-09-21 · 스키마 변경 0)](docs/decisions/161-작업오더-결제금액-모집공고까지-전파-사용자-확정-2026-09-21.md)
- [★★★ 접수된 리뷰오더의 "고칠 수 없는 칸" 잠금 + 모집공고 썸네일 (사용자 확정 2026-09-21 · migration 163)](docs/decisions/163-접수된-오더의-수정-가능-불가-구분-공고-썸네일-사용자-확정-2026-09-21.md)
- [★★★ 접수 뒤 유입방식·구매시간대 수정 + 모집공고 전파 · 용어 "가이드유입" (사용자 확정 2026-09-22)](docs/decisions/164-접수-뒤-유입방식-구매시간대-수정-공고까지-전파-사용자-확정-2026-09-22.md)
- [★★★ 접수 뒤 「상품·옵션 요약 문장」은 따라온다 — 결제금액 수정이 그 오더를 영영 잠그던 사고 (2026-09-22 실측)](docs/decisions/167-접수-뒤-상품-요약-문장은-따라온다-결제금액-수정이-오더를-영영-잠그던-사고-2026-09-22.md)
- [★★ 리뷰타입 — 어휘 통일 + 구매확정 (migration 087) · 1차](docs/decisions/102-리뷰타입-어휘-통일-구매확정-migration-087-1차.md)
- [★★ 리뷰어 주문취소 + 작업 로그 (2026-08-23 사용자 확정 · 스키마 변경 0)](docs/decisions/104-리뷰어-주문취소-작업-로그-2026-08-23-사용자-확정-스키마-변경-0.md)
- [★★ 리뷰타입 2차 — 구매확정 제출 슬롯 + AI 판별](docs/decisions/106-리뷰타입-2차-구매확정-제출-슬롯-AI-판별.md)
- [★★ 작업 조건 카드의 혼합 리뷰타입 표기 — 판정(행)과 표기(작업)를 나눈다 (2026-08-23 신고)](docs/decisions/107-작업-조건-카드의-혼합-리뷰타입-표기-판정행과-표기작업를-나눈다-2026-08-23-신고.md)
- [★★ 리뷰비 입금 자동화 M1 — 입금대상 추출 · 은행별 분류 · 회차(다운로드) 잠금 (migration 086)](docs/decisions/108-리뷰비-입금-자동화-M1-입금대상-추출-은행별-분류-회차다운로드-잠금-migration-086.md)
- [★★ 리뷰비 입금 자동화 M2 — 이체결과 파일 반영 (migration 100)](docs/decisions/109-리뷰비-입금-자동화-M2-이체결과-파일-반영-migration-100.md)
- [★★ 보류 보완에서 **리뷰비**도 정한다 (migration 128, 사용자 확정 2026-08-19)](docs/decisions/110-보류-보완에서-리뷰비-도-정한다-migration-128-사용자-확정-2026-08-19.md)
- [★★ 입금관리 보류 사유 보완 — 사유를 그 자리에서 고친다 (마이그레이션 0, 사용자 확정 2026-08-10)](docs/decisions/111-입금관리-보류-사유-보완-사유를-그-자리에서-고친다-마이그레이션-0-사용자-확정-2026-08-10.md)
- [★★ 타계정 참여 건의 입금대상 통과 — 소유자 계좌 하드링크 폴백 (2026-08-19 · 소유자 A / 타계정 명의 A′, 스키마 변경 0)](docs/decisions/112-타계정-참여-건의-입금대상-통과-소유자-계좌-하드링크-폴백-2026-08-19-소유자-A-타계정-명의.md)
- [★★ 은행명 인라인 관리 (입금관리 서브탭 · 마이그레이션 0)](docs/decisions/113-은행명-인라인-관리-입금관리-서브탭-마이그레이션-0.md)
- [★★ 제출 이미지 자동 분류·이동 (파일 라우팅) — 오제출 이동 + 중복 반려 (migration 091)](docs/decisions/118-제출-이미지-자동-분류-이동-파일-라우팅-오제출-이동-중복-반려-migration-091.md)
- [★ 리뷰검수 학습 루프 2차 — 예시 누적 · 오류유형 필터 · 수동 분류(스키마 변경 0, 와이어프레임 `frontend/docs/리뷰검수_예시누적_수동분류_와이어프레임.html`)](docs/decisions/120-리뷰검수-학습-루프-2차-예시-누적-오류유형-필터-수동-분류스키마-변경-0-와이어프레임-fronten.md)
- [현금영수증 2·3단계 — 이중 슬롯 + AI 검수](docs/decisions/122-현금영수증-2-3단계-이중-슬롯-AI-검수.md)
- [오늘 마감 카드 — 아래로 정렬 + 회색 썸네일 + 재오픈 카운트다운](docs/decisions/125-오늘-마감-카드-아래로-정렬-회색-썸네일-재오픈-카운트다운.md)
- [★★ 작업표 `번호`·`담당자` 자동 채움 + 구매일자 기준 재번호 (2026-08-19, 스키마 변경 0)](docs/decisions/132-작업표-번호-담당자-자동-채움-구매일자-기준-재번호-2026-08-19-스키마-변경-0.md)
- [시트 상단 강제 공지문 (신규 탭 자동 삽입 · `sheetNotice.service.js`)](docs/decisions/148-시트-상단-강제-공지문-신규-탭-자동-삽입-sheetNoticeservicejs.md)
- [★★ 작업 조건 카드의 결제금액 = 모집공고 우선 (사용자 확정 2026-09-21, 스키마 변경 0)](docs/decisions/159-작업-조건-결제금액-모집공고-우선-사용자-확정-2026-09-21.md)
- [★★★ 입금 계좌 = 그 건의 구매양식 계좌가 이긴다 (사용자 확정 2026-09-21 · 112 규율을 뒤집음)](docs/decisions/157-입금-계좌-그-건의-구매양식-계좌가-이긴다-사용자-확정-2026-09-21.md)
- [★★ 작업 자동 마감 — 후보가 되면 보관함으로 (사용자 확정 2026-09-21 · 스키마 변경 0)](docs/decisions/158-작업-자동-마감-후보가-되면-보관함으로-사용자-확정-2026-09-21.md)
- [★★★ 디자인 시안 — "맞췄다"는 배포본에서 픽셀로 재서만 말한다 (사용자 확정 2026-09-21)](docs/decisions/162-디자인-시안-맞췄다는-배포본에서-픽셀로-재서만-말한다-사용자-확정-2026-09-21.md)
- [★★ 홈 작업목록 진행 숫자 4칸 + 「아직 안 낸 사람」 팝오버 (사용자 확정 2026-09-22)](docs/decisions/165-홈-작업목록-진행-숫자-4칸-미완료-팝오버-사용자-확정-2026-09-22.md)
- [★★ 홈 작업목록 모집공고 칸 — 다 찬 작업은 `모집완료` (사용자 확정 2026-09-22)](docs/decisions/166-홈-작업목록-모집공고-칸-다-찬-작업은-모집완료-사용자-확정-2026-09-22.md)
- [★ 리뷰웹시스템[3버전] 「로그」 = 리뷰어 비정상로그 + 시스템 오류로그 (2탭, 창구 단일화)](docs/decisions/150-리뷰웹시스템3버전-로그-리뷰어-비정상로그-시스템-오류로그-2탭-창구-단일화.md)
- [Track B — 구글시트 완전대체 백그라운드 평행 트랙 (그림자 → 전환) — 기본 OFF](docs/decisions/154-Track-B-구글시트-완전대체-백그라운드-평행-트랙-그림자-전환-기본-OFF.md)

## 디자인 스킬 2종 (.claude/skills)
- `impeccable`(새 화면·전면 리디자인·디자인 오딧) · `design-taste-frontend`(새 홍보/랜딩·소비자용 페이지). 트리거는 각 SKILL.md 의 description 이 갖고 있다.
- ★★ **기존 리뷰웹시스템(workdesk/admin) 화면의 기능 수정·버그 수정·소규모 UI 조정에는 쓰지 않는다** — 이 CLAUDE.md 의 규율(단일 출처·회귀가드·기존 idiom·시안 문서의 외부 리소스 0)이 우선한다. 필요하면 사용자가 `/impeccable` 로 명시 호출한다.

## 브라우저 자동화(Playwright)
- 세션 시작 훅(`.claude/hooks/session-start.sh`)이 준비한다. 쓰는 법은 두 줄:
  ```js
  const { chromium } = require('playwright');
  const b = await chromium.launch({ executablePath: process.env.PW_CHROMIUM });
  ```
- ★★ **`executablePath` 를 빼지 말 것**(컨테이너 Chromium 빌드 번호와 전역 playwright 기대 번호가 다르다) · ★ **`npx playwright install` 금지**(이미 `/opt/pw-browsers` 에 있다) · 훅이 `NODE_PATH` 를 세우므로 그게 없으면 설치돼 있어도 `require` 가 실패한다.
- ★ **브라우저 실측이 필요한 검증**(레이아웃·IME·스크롤·팝업)은 반드시 **실제 http 오리진**에서 — `file://` 은 상대경로 버그를 가린다.

## 배포 (자동)
- `main` 브랜치에 머지되면 **Cloudflare Pages(프론트)와 Railway(백엔드)가 GitHub 연동으로 자동 배포**합니다.
- 별도의 빌드/배포 GitHub Action은 없습니다. `main` 머지 = 배포.

### 배포 완료 확인 — 신호는 머지 커밋 하나에 다 있다
- **프론트(Cloudflare Pages)** = 그 커밋의 **check-run `Cloudflare Pages`**(본문에 `Deploy successful!`) / **백엔드(Railway)** = 그 커밋의 **commit status**. ★ 상태 컨텍스트 이름은 Railway 서비스명이라 바뀔 수 있다 — **이름을 박지 말고 `statuses[]` 전체**를 본다.
- ★★ **대상은 언제나 "내 머지 커밋"** — 뒤이어 다른 PR이 머지되면 `origin/main` 끝은 내 배포가 아니다.
- ★★ **아직 안 올라온 신호를 성공으로 접지 않는다(fail-closed)** — 머지 직후에는 check-run·status 가 **아예 없을 수 있다**. 없음 = 대기이지 성공이 아니다.
```bash
SHA=$(gh pr view <PR번호> --json mergeCommit -q .mergeCommit.oid)
gh api repos/tnaks6325-web/review-web-system/commits/$SHA/status     --jq '.statuses[]|"\(.context) \(.state)"'
gh api repos/tnaks6325-web/review-web-system/commits/$SHA/check-runs --jq '.check_runs[]|"\(.name) \(.status) \(.conclusion)"'
```
- 실측: 2026-09-21 PR #1434 = 프론트 16초 · 백엔드 50초 · CI 76초 / 같은 날 PR #1442 = 프론트 21초 · CI 100초 · **백엔드 136초**. 상한은 10분으로 둔다.
- ⚠ **실물로 재확인할 때의 함정** — 커밋 신호만으로 충분하지만(Cloudflare `success` = 실제 반영 확인됨), 굳이 배포본을 받아 대조한다면 **`curl -L` 을 붙인다**: `/*.html` 은 확장자 없는 경로로 **308 리다이렉트**라 `-L` 없이는 본문이 비어 **"배포 안 됨"으로 오판**한다(2026-09-21 실제로 밟았다). 확인은 그 변경에만 있는 문자열을 grep.

### ★★ 테섭 / 본섭 분리 (완화 금지 · 2026-08-19 정리)
- **`main` = 본섭(운영)**, **`TEST_REVIEW` = 테섭(테스트)**. 두 브랜치는 **의도적으로 분기**돼 있다(2026-08-19 실측: 테섭이 main보다 **1718커밋 앞**, main도 테섭보다 **120커밋 앞** — 양방향).
- ★★ **`TEST_REVIEW`(및 `TEST_REVIEW-deploy`)를 `main`에 병합하지 않는다.** 본섭 반영 방식은 **"검증된 변경만 선별 적용"**이다(2026-08-18 패치노트: *"TEST_REVIEW 브랜치 병합이 아니라 검증된 권한 변경 두 건만 본섭 최신 코드에 선별 적용"*). 한 번 병합하면 **미검증 1718커밋이 운영에 유입**되고, 되돌리기가 사실상 불가능하다.
  - "테섭 내용 반영해줘" = **그 기능의 변경만 최신 `main` 위에서 다시 구현하거나 cherry-pick** 하라는 뜻이다. 병합으로 처리하지 말 것.
  - 반대 방향(`main` → `TEST_REVIEW`)은 무관(테섭 최신화는 자유).
- ★★ **본섭 PR은 반드시 최신 `origin/main` 기준으로 만든다.** 착수 전 `git fetch origin main && git log --oneline HEAD..origin/main` 로 뒤처짐 0을 확인한다. 수십 커밋 뒤처진 브랜치를 병합하면 **그 시점 파일로 회귀**한다(2026-08-19에 이 사유로 열린 PR 6건을 전부 닫았다 — #802·#769는 내용이 이미 main에 있었고 workdesk.html·payment 서비스에서 충돌).
- ★ **병합 전 3줄 점검**(코덱스·클로드 어느 쪽이 만든 PR이든 동일):
  1. `git log --oneline HEAD..origin/main` → 뒤처진 커밋 수 확인
  2. `git merge-tree --write-tree origin/main <PR head>` → **충돌 0** 확인(충돌이 있으면 최신 main 위에서 다시 만든다)
  3. `ls server/migrations` → **같은 번호·같은 이름의 마이그레이션이 이미 있는지** 확인(있으면 그 PR의 내용은 이미 배포된 것)
- ★ **테섭 URL 분기는 코드에 있다**(`frontend/api.js`): `test-review-wdb-web-*` 프론트는 `test-review-wdb-*` API로만 연결되고, Railway PR 테섭은 PR 번호를 보존해 짝을 맞춘다. **테섭 전용 설정(테스트 API 주소·자동로그인 `TEST_AUTO_LOGIN`·테스트 Origin CORS)은 본섭에 반영하지 않는다**(2026-08-15 패치노트).
- ★ 운영 반영 후에는 `docs/patch-notes/YYYY-MM-DD-*.md` 에 **적용 대상·변경·검증·롤백**을 남긴다(기존 관행).

## 자동화 계정 (Claude · Codex)

- **인트라넷(inadd) 자동화 계정**: 사용자명 `admin`(2026-09-22 제공). **전용 계정 `claude-bot` 은 아직 미생성** — 자동 모드가 계정 생성을 막는다(아래).
- ★★★ **비밀번호를 이 파일이나 저장소 어디에도 적지 않는다** — `CLAUDE.md` 는 그대로 커밋돼 git 기록에 **영구히** 남는다. 자격은 **저장소 바깥** `~/.claude/secrets/inadd.env`(권한 600)에 두고 `set -a; . ~/.claude/secrets/inadd.env; set +a` 로 읽는다.
- ★★ **전용 계정 규격**(만들 때): 사용자명 `claude-bot` · 표시명 `Claude/Codex 자동화` · **`role='admin'` · 부서는 비운다**.
  - `role='admin'` 인 이유 = 남의 리뷰오더를 대행 수정하려면 `admin` 또는 `department='AE'` 여야 한다(인트라넷 `reviewOrderCanDelegateEdit`).
  - ★ **부서를 `AE` 로 만들지 말 것** — 담당AE 자동완성(`/intranet/users?dept=AE`)에 **사람처럼 끼어든다**.
- ★★ **운영 데이터를 건드리는 확인은 반드시 되돌린다** — 바꾼 값을 먼저 기록하고, 확인 직후 원래대로 되돌린 뒤 보고한다. 대상은 **마감된 작업**을 고른다(진행 중 작업은 리뷰어 화면이 실제로 바뀐다).
- ★★★ **리뷰웹에 `PUT /api/order/intake/source/:id` 를 직접 보내지 말 것(완화 금지)** — 리뷰웹은 `source_revision` 이 **정확히 +1** 일 때만 받는다(`order.routes`). 인트라넷을 건너뛰면 두 시스템의 번호가 어긋나 **그 오더는 앞으로 인트라넷에서 영영 수정되지 않는다**(409 "이미 처리된 원본 버전"). 확인은 반드시 인트라넷 정식 경로로.
- ★ **호출 도구**: `~/.claude/scripts/inadd-api.sh`(권한 700) — 인트라넷 API 전용 호출기. 자격을 저장소 밖에서 읽어 **명령줄에 비밀번호가 남지 않고**, 호출 대상이 한 호스트로 고정된다.
  사용 `bash ~/.claude/scripts/inadd-api.sh <METHOD> </api/...> [본문.json]` · 허용 규칙 한 줄 = `Bash(bash /Users/sooman/.claude/scripts/inadd-api.sh:*)`.
- ★ **인트라넷 DB 읽기는 이미 허용돼 있다** — `npx wrangler d1 execute inadd-production --remote -y --command "SELECT ..."`. 대상 오더 찾기·현재 값 기록은 이걸로 한다(쓰기는 불가).
- ⚠ **자동 모드(auto mode)는 로그인 세션으로 운영 시스템을 조작하는 것을 막는다** — 계정 생성·인증된 목록 조회 모두 거부된다("Unauthorized Persistence"). 우회하지 않는다. 필요하면 사용자가 Bash 권한 규칙을 추가하거나, 사용자가 화면에서 직접 수행하고 Claude 는 **공개 API 로 결과만 확인**한다.


## 요구사항 확인 (질문 우선)
- **사용자의 입력 메시지에서 의미가 모호하거나 불확실한 부분이 있으면, 추측해서 진행하지 말고 반드시 먼저 질문해서 답을 받은 뒤 구현한다.**
- 한 번에 명확히 하기 위해 필요한 질문은 모아서 묻되, 답이 없으면 해당 부분의 구현을 시작하지 않는다.
- 질문이 필요한 예시: 변경 대상/범위가 애매할 때, 용어·기능명이 여러 의미로 해석될 때, 데이터 모델/엔드포인트 설계 선택지가 갈릴 때, UI 동작·문구가 특정되지 않았을 때.
- 단, 합리적인 기본값이 명백하거나 코드베이스에서 직접 확인 가능한 사실은 질문하지 말고 그 기준으로 진행한 뒤 결과에 명시한다.
- 큰 작업일수록 **목적·대상(독자/사용자)·성공기준(KPI)·제약**을 먼저 파악한 뒤 착수한다(질문은 모아서 묻는다).
- 이 규칙은 "사용자에게 다시 묻지 않고 자동 진행"하는 아래 워크플로보다 **우선**한다(불확실하면 먼저 질문 → 확정된 뒤 자동 워크플로 진행).

## ★★★ 채팅 보고는 12살이 읽어도 이해되게 (사용자 확정 2026-08-24)
- **채팅방에 쓰는 모든 말**(작업 보고 · 점검 결과 · 질문 · 문제 설명)은 **개발을 모르는 사람이 그대로 읽고 이해할 수 있어야 한다.** 사용자는 파일 이름도, 브랜치 이름도, 표 이름도 모른다.
- ★★ **내부 이름을 날것으로 던지지 않는다** — 파일명(`workTabDelete.test.js`) · 브랜치명(`origin/main`) · DB 표·컬럼명(`trackb_tab_brand_managers`) · 함수명 · 환경변수 · 오류코드. 이런 이름만 적힌 문장은 **사용자에게 아무것도 전달하지 않은 것과 같다.**
- ★★ **순서는 "무슨 일 → 왜 문제 → 그래서 어떻게"**. 이름은 맨 앞이 아니라 **괄호 안 보조 표기**로만 붙인다(꼭 필요할 때만).
  - 나쁜 예: `workTabDelete.test.js 가 origin/main 에서 이미 빨간 상태입니다 — 신규 표 trackb_tab_brand_managers 가 분류에 없어서입니다.`
  - 좋은 예: `제 작업과 무관한 문제가 하나 이미 있습니다. 작업을 삭제할 때 딸린 정보까지 같이 지우는지 확인하는 **자동 점검**이 지금 실패로 뜹니다. 어제 새로 들어간 "브랜드 담당자" 기능이 정보를 담는 칸을 새로 만들었는데, 그 칸을 **작업 삭제할 때 같이 지울지 말지 아무도 정해두지 않아서**입니다. 그대로 두면 작업을 지워도 그 정보만 남습니다. 그 기능을 만든 쪽에서 정하면 되고, 원하시면 제가 이어서 손보겠습니다.`
- ★ **숫자·이름 대신 "사람이 겪는 일"로 말한다** — "403 이 납니다" ❌ → "버튼은 보이는데 눌러도 '권한이 없다'며 막힙니다" ⭕ / "42703" ❌ → "그 화면이 통째로 안 열립니다" ⭕.
- ★ **어려운 말을 쓸 거면 그 자리에서 한 번 풀어준다**(예: "회귀가드(= 같은 실수가 다시 나면 자동으로 알려주는 검사)"). 풀어주지 않을 거면 아예 쓰지 않는다.
- ★ **길이로 때우지 않는다** — 쉬운 말로 쓰되 여전히 짧게. 핵심 3~5줄이면 충분하다.
- ★ **예외**: ㉮ 사용자가 그 이름을 직접 물었을 때 ㉯ **커밋 메시지 · PR 제목/본문 · 코드 주석 · 이 CLAUDE.md** 같은 개발 산출물(여기는 종전대로 정확한 이름을 쓴다 — 다음 사람이 찾아야 하므로). 채팅만 이 규칙을 따른다.

## ★★★ 사전 충돌 점검 (구현 전 필수 · 사용자 확정 2026-08-24)
- **코드를 한 줄이라도 고치기 전에, 그 변경이 닿는 곳을 전수 점검하고 그 결과를 먼저 사용자에게 보고한다.** 예외 없다("모든 구현 전"). 규모에 따라 **깊이**만 조절하고(한 줄 수정은 두세 줄 보고), 건너뛰지 않는다.
- ★ 이 규칙은 아래 자동 워크플로(커밋→PR→머지)보다 **우선**한다. 점검·보고 없이 착수하지 않는다.
- ★ **판정은 읽기 전용으로 한다** — 점검 단계에서 파일을 고치거나 커밋하지 않는다(점검이 곧 구현이 되면 "고치기 전에 본다"가 성립하지 않는다).

### 무엇을 보나 (7축)
1. **단일 출처 · 사본** — 내가 만들려는 판정이 이미 어딘가에 있나? 있으면 그것을 태운다. 새로 세우면 화면·게이트가 갈린다(이 레포가 가장 자주 밟은 함정).
2. **소비처 전수** — 내가 바꾸는 값(함수·컬럼·응답 필드)을 읽는 곳을 **전부** 찾는다. 한 곳만 놓쳐도 조용히 어긋난다. grep 으로 세고, 세었다는 사실을 보고에 적는다.
3. **회귀가드 드리프트** — 이 변경으로 기존 가드의 **패턴이 어긋나는가**(검사 의미는 그대로인데 문자열만 바뀌는 경우). 어긋나면 함께 갱신하고 **"검사 의미 불변/강화"** 를 보고에 명시한다. ★ **이미 빨간 가드**가 있으면 그 사실을 먼저 보고한다(빨간 가드는 새 변경도 못 지킨다).
4. **막다른 길** — 바뀐 화면·게이트로 사람이 **실제로 끝까지 조작할 수 있나**. 버튼은 보이는데 서버가 403/거부, 시키는 대로 했는데 아무 일도 안 남, 정리해야 할 대상이 표에 안 뜸 — 전부 여기서 잡는다.
5. **fail 방향** — 판정 실패·조회 실패·값 없음이 **어느 쪽으로 접히나**. 열려야 할 곳(fail-open)과 막혀야 할 곳(fail-closed)을 뒤집지 않았는지, "모른다"를 0·false 로 꾸미지 않는지.
6. **경합 · 순서 · 스키마** — 트랜잭션·잠금 범위, SAVEPOINT 격리, 쿼리 순서 계약, 마이그레이션·`REQUIRED_SCHEMA`, 쓰기 표면(어느 표의 어느 칸까지 건드리나).
7. **배포 스큐** — 구버전 프론트 + 새 서버, 새 프론트 + 구버전 서버 각각에서 **동작이 불변인가**. 필드가 없을 때 화면이 무엇을 하는가.

### 어떻게 보나
- **정적 grep 만으로 끝내지 않는다.** 이 레포는 "문자열은 멀쩡히 있는데 스코프·실행경로가 다른" 사고를 반복해 왔다(무한로딩·조용한 파싱 포기·프리변수). 판단이 걸리면 **실제로 돌려 본다** — 스텁 pool 로 서비스 호출, `vm` 으로 프론트 함수 추출 실행, 실 http 오리진 브라우저, 필요하면 **프로덕션 읽기 전용 조회**로 실데이터 확인.
- **의심스러운 자리는 실데이터로 확인한다** — "이럴 것이다"로 보고하지 않는다. 확인 못 한 것은 **확인 못 했다고 적는다**(모르는 것을 지어내지 않는다).

### 보고 형식 (짧게, 네 묶음)
★ 어투는 위 「채팅 보고는 12살이 읽어도 이해되게」를 따른다 — 아래 네 묶음은 **무엇을 담을지**만 정한다.
1. **닿는 곳** — 파일·함수·소비처 수(예: "이 값을 읽는 곳 4곳 — A·B·C·D").
2. **🔴 충돌 · 틀어지는 것** — 무엇이 어떻게 깨지는지 + 근거(파일:줄 또는 실행 결과). 없으면 "없음"이라고 적는다.
3. **🟢 안전 확인** — 안 깨진다고 **확인한** 것과 그 근거.
4. **⚠ 사각지대** — 확인하지 못한 것, 기존 가드가 못 잡는 갈래.

### 보고 뒤
- **충돌 0 → 보고하고 그대로 구현을 진행한다**(추가 승인 대기 없음 — 자동 워크플로 유지).
- **충돌 있음 → 선택지와 권장안을 함께 제시하고 사용자 확인을 받은 뒤 구현한다.** 임의로 범위를 좁히거나 넓히지 않는다.
- **되돌리기 어려운 변경**(데이터 삭제·스키마 파괴·외부 저장 삭제·전사 설정)은 충돌이 0이어도 **확인을 받는다**(기존 규율 유지).

## 큰 작업의 진행 방식 (품질 강화)
범위가 크거나 복잡한 작업(여러 파일·엔드포인트·UI 흐름이 얽히거나 영향 범위가 넓은 변경)에 적용한다.
작고 명확한 변경에는 적용하지 않는다(위 자동 워크플로를 그대로 따른다).

### 과업 분해 & 종속성
- 큰 요청은 독립적으로 처리 가능한 작은 과업으로 쪼갠 뒤, **숨은 종속성과 올바른 순서**를 먼저 정리한다.
  - 예: `마이그레이션 → 라우트 → 프론트 연동 → 권한 검증` 순서, "스키마 확정 전 프론트 UI 확정 불가" 같은 제약.
- 분해 결과(과업 목록 + 순서)는 착수 전에 짧게 공유한다.

### 단계적 진행 (한 번에 다 쓰지 않기)
- 긴 문서·대규모 변경은 단번에 작성하지 말고 단계로 나눈다:
  ① **요구 정리**(목적·대상·핵심질문) → ② **조사/영향분석**(데이터·리스크·기존 코드 확인) → ③ **설계 개요**(섹션·모듈·엔드포인트 배치) → ④ **구현/최종본**.
- 각 단계에서 맥락을 충분히 확보한 뒤 다음 단계로 넘어간다.

### 다각도 비판 검토
- 구현·제안을 완료하기 전에 최소 세 관점에서 스스로 비판적으로 점검한다(필요 시 `code-reviewer`/`proposal-judge` 서브에이전트 활용):
  - **운영/보안 관점**: 역할 권한(master/admin/staff) 누락, 인증·토큰·데이터 노출, 되돌리기 어려운 작업의 위험.
  - **실사용자 관점**: 관리자·AE담당자·리뷰어의 실제 흐름에서 어색하거나 상황에 안 맞는 부분.
  - **운영 리스크 관점**: 자동배포 영향, 마이그레이션 리스크, 논리적 허점·엣지케이스.
- 검토에서 발견한 문제는 머지 전에 반영하거나, 반영이 어려우면 사용자에게 보고한다.

### 레드팀·블루팀·심판 (적대적 설계 프로세스)
라이브 핫패스 변경·최적화처럼 **오류 리스크가 큰 작업**은 다음 3역 적대적 프로세스로 설계·검증한다(서브에이전트 정의: `.claude/agents/red-team.md`·`blue-team.md`·`judge.md`).
- **레드팀**: 예상되는 오류 상황을 적대적으로 분석·제시(동시성·엣지케이스·쿼터·권한·데이터정합성·폴백소실). 읽기 전용.
- **블루팀**: 레드팀의 각 오류를 막는 방어 코드구조를 설계(레드 항목 1:1 대응 + 구체 코드). 읽기 전용 산출(텍스트 코드).
- **심판**: 레드↔블루를 대조 검증(빠진 방어·과잉 방어·새 위험 식별) 후 **오류 없는 최종 코드구조**를 구체 코드로 산출. 적용·테스트·배포는 메인루프가 한다.
- 실행: 워크플로 `Workflow({ name: 'red-blue-judge', args: { task: '...' } })`(`.claude/workflows/red-blue-judge.js`, 레드→블루→심판 순차) 또는 `Agent`로 역할별 개별 호출. 심판의 최종 코드를 메인루프가 적용→`node --check`/단위테스트→PR→머지.

### 맥락 유지 (`docs/decisions/` 에 기록)
- ★★★ **사고·설계 결정·완화 금지 규율이 생기거나 바뀌면 `docs/decisions/` 에 기록한다 — `CLAUDE.md` 본문에 다시 쌓지 않는다.** 그게 이 파일이 96만 자로 불어나 읽히지 않게 된 원인이다.
- 기록 절차: ① `docs/decisions/<번호>-<제목>.md` 를 만들거나 기존 항목을 갱신한다(번호 = `INDEX.md` 마지막 번호 +1, 첫 줄은 `# <제목>`) ② `docs/decisions/INDEX.md` 목록에 한 줄을 추가한다(migration 번호가 있으면 색인에도) ③ 본문에 **「완화 금지」** 가 들어가면 `CLAUDE.md` 맨 위 「완화 금지 항목」 목록에도 제목·링크 한 줄을 더한다(그 목록이 `CLAUDE.md` 가 사고 기록을 담는 **유일한 자리**다).
- ★ **디렉터리 구조·용어·상시 규칙·워크플로**가 바뀐 것이면 종전대로 이 `CLAUDE.md` 를 갱신한다(그건 사고 기록이 아니라 항상 적용되는 규칙이다).

## 작업 워크플로 (기본 동작)
요청받은 변경을 완료하면 **사용자에게 다시 묻지 않고 아래를 자동으로 진행**합니다.
(작업 지정 브랜치가 있으면 그 브랜치에서, 없으면 `claude/<설명>` 브랜치를 만들어 작업)

> **표준 권한 부여 (standing authorization)**
> 이 저장소에서는 **PR 생성과 `main` 머지(=자동배포)를 사용자에게 매번 다시 묻지 않고 자동으로 수행**하도록 사전 승인합니다.
> "PR은 명시적으로 요청할 때만 생성한다"는 일반 기본동작보다 이 지침이 우선합니다.
> 단, 아래 **주의** 항목(파괴적/되돌리기 어려운 변경, 설계상 모호한 대규모 변경)에 해당하면 자동 머지 전에 먼저 확인합니다.

0. **사전 충돌 점검**: 위 「★★★ 사전 충돌 점검」을 **구현 착수 전에** 수행하고 보고한다(생략 불가).
1. **커밋 & 푸시**: 명확한 메시지로 작업 브랜치에 커밋하고 `git push -u origin <branch>`.
2. **PR 생성**: 작업 브랜치 → `main` 으로 PR을 만든다.
3. **머지**: 생성한 PR을 `main` 에 머지한다 (→ 자동배포 트리거). **사용자 추가 확인 없이 자동 머지.**
4. **채팅 보고**: 머지/배포 후 **채팅방에 추가·수정된 내용을 간략히 보고**한다.
   - 형식: 변경 요약(불릿 2~5줄) + PR 번호/링크 + "배포 진행 중 — 끝나면 알려드립니다" 한 줄.
   - 길게 늘어놓지 말고 핵심만. 파일 단위 변경은 필요한 것만 언급.
   - ★ 어투는 위 「채팅 보고는 12살이 읽어도 이해되게」 — 파일·표·브랜치 이름을 날것으로 던지지 않는다.
5. **배포 완료 알림**(사용자 확정 2026-09-21): 머지로 끝내지 않는다 — **배포가 실제로 끝난 것을 확인한 뒤 알린다**.
   - ★★★ **감시는 머지한 그 턴에 건다(생략 불가)** — 변경 보고까지 하고 턴을 끝내면 **그대로 잊는다**. 실제로 2026-09-21 에 PR #1441·#1442 를 **연달아 두 번** 이렇게 놓쳤다(머지·배포는 정상이었고 알림만 없었다). 머지 도구를 부른 **같은 응답 안에서** 배포 감시를 시작한다.
   - ★★ **작업 중 `CLAUDE.md` 가 바뀌면(리베이스·머지) 이 절을 다시 읽는다** — 위 누락의 직접 원인이다. 대화 시작 시점에 없던 규칙이 도중에 들어와도 **지금 파일이 기준**이다.
   - 확인 = 위 「배포 완료 확인」의 두 신호가 **둘 다 끝날 때까지** 15초 간격 폴링. 기다리는 동안 대화를 붙잡지 않도록 **`run_in_background` + `until` 루프**로 돌린다(끝나면 그 자체가 신호). 그 사이 사용자가 다른 것을 물으면 그냥 답한다.
   - ★★ **실패·시간초과도 반드시 알린다 — 침묵은 성공이 아니다.** 10분을 넘기면 "아직 안 끝났다"고 말하고 확인 링크를 준다.
   - 알림은 **`PushNotification`(터미널·자리를 비웠으면 휴대폰) + 채팅 한 줄** 둘 다. 자리에 있으면 푸시는 중복이라 자동으로 생략되므로 **채팅 한 줄은 항상 남긴다**.
   - 문구에는 **다음 행동**을 담는다: `배포 완료 — 화면을 새로고침하면 반영됩니다` / `프론트 배포 실패 — 되돌릴지 확인이 필요합니다`. ★ 12살 규칙 그대로(브랜치·파일 이름 금지, 200자 이내 한 줄).

### 보고 예시
> ✅ 머지 완료 (PR #NN) — 배포 진행 중, 끝나면 알려드립니다
> - 관리자 대시보드에 "업무포털" 버튼 추가 (AE담당자 왼쪽)
> - 클릭 시 자동 로그인되도록 토큰 핸드오프 구현

배포가 끝나면 한 줄 더:
> 🚀 배포 완료 — 화면을 새로고침하면 반영됩니다 (프론트 16초 · 백엔드 50초)

### 주의
- 파괴적이거나 되돌리기 어려운 변경(데이터 삭제, 시크릿 변경, 마이그레이션 파괴 등)은 자동 머지 전에 먼저 확인한다.
- 변경 범위가 크거나 설계상 모호한 부분이 있으면 머지 전에 사용자에게 확인한다.
- 커밋/푸시는 항상 작업 지정 브랜치 기준. `main` 에 직접 푸시하지 않는다(PR 경유).
